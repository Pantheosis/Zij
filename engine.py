"""The calculation engine: positions, dignities, connections, the lots, the
timing techniques and the pictures drawn from them. The pages that show all
of it live in app.py, beside this file, which takes this module whole.

Nothing here calls Streamlit's page API. The ``st.cache_data`` decorators on
the heavy calculations are the one Streamlit use in this file, and they stay:
they are what keeps a rerun from recomputing a chart.
"""
import swisseph as swe
import streamlit as st
import pandas as pd
from datetime import datetime, timezone, time, timedelta
from itertools import combinations, product
from contextlib import contextmanager
from xml.sax.saxutils import escape
import html
from timezonefinder import TimezoneFinder
import pytz
from pathlib import Path
import math
import re
import json
import sqlite3
import os
import shutil
import sys
import threading
from dataclasses import dataclass
from functools import wraps
# The wheel's embedded symbol-font subset (process/tae_docs/UI_CHANGES_2026-09-15_symbol_font.md).
# glyph_font.py sits beside this file precisely as engine.py sits beside app.py, and
# build.spec bundles it as data for the same reason: nothing imports it at build time.
from glyph_font import font_face_css


# ==========================================
# INDETERMINATE RESULTS
# ==========================================
# A doctrinal question can fail to produce a value for several different
# reasons. Keep that state in the engine instead of encoding it as a string,
# zero or False: those substitutes can silently enter arithmetic and Boolean
# tests. The page turns this object into ordinary display text at its
# rendering boundary.
INDETERMINATE_STATUSES = frozenset({'unavailable', 'unresolved', 'unassigned', 'not decided'})


@dataclass(frozen=True)
class UnresolvedResult:
    """An immutable result for a question the adopted rules do not settle.

    ``alternatives`` names the readings that remain open; each item is a
    ``(name, value)`` pair so callers can preserve numbers and booleans as
    values rather than flattening them into prose.
    """

    reason: str
    source: str
    alternatives: tuple = ()
    status: str = 'unresolved'
    display_label: str = ''
    display_alternatives: bool = True

    def __post_init__(self):
        if self.status not in INDETERMINATE_STATUSES:
            raise ValueError(f"unknown indeterminate status: {self.status!r}")
        object.__setattr__(self, 'alternatives', tuple((str(name), value) for name, value in self.alternatives))

    def __bool__(self):
        raise TypeError(f"{self.status} result has no truth value; resolve or display it explicitly")


@dataclass(frozen=True)
class YearsOutcome:
    """One complete result from a planetary-years route.

    Conditional years keep the deciding sentence, grade, number, and unit
    together so a consumer cannot accidentally display or rank only the
    numeric part of one alternative.
    """

    sentence: str | None
    grade: str | None
    number: float | None
    unit: str | None
    text: str


def _same_years_outcome(left, right):
    """Whether two routes settle the same grant despite different steps."""
    return (isinstance(left, YearsOutcome) and isinstance(right, YearsOutcome)
            and (left.sentence, left.grade, left.number, left.unit)
            == (right.sentence, right.grade, right.number, right.unit))


def is_unresolved(value):
    return isinstance(value, UnresolvedResult)


def doctrinal_and(*values):
    """Three-valued conjunction: known False settles it; otherwise an
    indeterminate operand survives. Only booleans and UnresolvedResult are
    admitted so a number or string cannot acquire a truth value here."""
    unknown = None
    for value in values:
        if value is False:
            return False
        if is_unresolved(value):
            if unknown is None:
                unknown = value
        elif value is not True:
            raise TypeError('doctrinal_and accepts only bool or UnresolvedResult values')
    return unknown if unknown is not None else True


def doctrinal_or(*values):
    """Three-valued disjunction: known True settles it; otherwise an
    indeterminate operand survives."""
    unknown = None
    for value in values:
        if value is True:
            return True
        if is_unresolved(value):
            if unknown is None:
                unknown = value
        elif value is not False:
            raise TypeError('doctrinal_or accepts only bool or UnresolvedResult values')
    return unknown if unknown is not None else False

# Connection uncertainty is evaluated at the outer judgment boundary. Each
# branch is a permitted Boolean completion of a missing connection fact, not
# a doctrinal ruling. Repeated uses of the same fact share the same decision.
# Nested evaluators run within that branch, so counts and exclusions are
# calculated before alternative outcomes are compared.
_CONNECTION_EVALUATION = threading.local()


class _ConnectionChoice(BaseException):
    def __init__(self, key, result, options=(False, True)):
        self.key, self.result, self.options = key, result, options


def _connection_decision(row, value):
    decisions = getattr(_CONNECTION_EVALUATION, 'decisions', None)
    if not is_unresolved(value) or decisions is None:
        return value
    if row.get('strike_tie') and 'equally placed' in value.reason:
        key = ('strike first body', row['body_connection_from'], row['strike_tie'])
        if key not in decisions:
            raise _ConnectionChoice(key, value, tuple(p for p, _ in row['strike_tie']))
        return decisions[key] == row['body_connection_to']
    positions = row.get('positions', (None, None))
    pair = tuple(sorted(((row['p1'], positions[0]), (row['p2'], positions[1]))))
    key = (reading('CONNECTION_PROFILE'), pair, row.get('deviation'), row.get('relative_speed'), value.reason)
    if key not in decisions:
        raise _ConnectionChoice(key, value)
    return decisions[key]


def _connection_unknown(values, reason='the result depends on an unresolved connection'):
    unique = []
    for value in values:
        if not any(repr(value) == repr(v) for v in unique):
            unique.append(value)
    if len(unique) == 1:
        return unique[0]
    return UnresolvedResult(reason, 'Sahl, The Introduction Ch. 3, 7–10, 20–21',
        alternatives=tuple((f'permitted outcome {i + 1}', v) for i, v in enumerate(unique)),
        display_alternatives=all(isinstance(v, (str, int, float, bool, type(None))) for v in unique))


def _connection_merge(values):
    """Retain common facts; mark changing cells and conditional rows.

    Testimony counts count only confirmed testimonies. An uncertain row is
    visible with an explicit Status, never silently promoted to a finding.
    """
    if all(repr(v) == repr(values[0]) for v in values[1:]):
        return values[0]
    if all(isinstance(v, dict) for v in values):
        keys = list(dict.fromkeys(k for v in values for k in v))
        result = {k: _connection_merge([v.get(k) for v in values]) for k in keys}
        if 'Testimonies' in result and 'Count' in result:
            testimonies = result['Testimonies']
            if isinstance(testimonies, list):
                uncertain = [t for t in testimonies if is_unresolved(t.get('Status')) or is_unresolved(t.get('result'))]
                certain = [t for t in testimonies if t not in uncertain]
                for t in uncertain:
                    t['result'] = t.get('Status') if is_unresolved(t.get('Status')) else t['result']
                result['Count'] = len(certain)
                if uncertain:
                    result['Unresolved Count'] = len(uncertain)
                result['Labels'] = [t['sentence'] if isinstance(t['sentence'], str) else f"Established testimony ({t['n']})" for t in certain]
                for k in ('Strength Testimonies', 'Weakness Testimonies'):
                    if k in result:
                        result[k] = ', '.join(result['Labels'])
        return result
    if all(isinstance(v, list) for v in values) and all(isinstance(r, dict) for v in values for r in v):
        identity = ('Chart', 'Planet', 'n', 'Number', 'Type', 'Kind', 'Condition', 'Companion',
                    'Receiver', 'Received', 'Carrier', 'Separates From', 'Connects To',
                    'Connecting', 'With', 'Hands Over To', 'Blocked', 'Blocked By', 'From Reaching',
                    'Collector', 'Collects', 'Manner', 'Returned By', 'Yields To', 'Other Contact',
                    'Favored By', 'Direction', 'Via', 'Enclosed By', 'Separating From', 'Connecting To',
                    'Pair', 'Aspect (whole sign)', 'Item', 'Testimony', 'Question', 'Refinement', 'Factor')
        def key(row):
            parts = tuple((k, repr(row[k])) for k in identity if k in row)
            return parts or tuple((k, repr(v)) for k, v in row.items())
        # Never collapse distinct rows just because a table shares an identity
        # field, and preserve multiplicity when identical rows are intentional.
        ambiguous = set()
        for rows in values:
            seen = set()
            for row in rows:
                k = key(row)
                if k in seen:
                    ambiguous.add(k)
                seen.add(k)
        maps = []
        for rows in values:
            mapping, occurrences = {}, {}
            for row in rows:
                k = key(row)
                if k in ambiguous:
                    k = (k, repr(row))
                n = occurrences.get(k, 0)
                occurrences[k] = n + 1
                mapping[(k, n)] = row
            maps.append(mapping)
        keys = list(dict.fromkeys(k for m in maps for k in m))
        result = []
        for k in keys:
            present = [m[k] for m in maps if k in m]
            row = _connection_merge(present)
            if len(present) != len(values):
                row = dict(row, Status=_connection_unknown([True, False],
                           'this finding depends on an unresolved connection'))
                if isinstance(row.get('Testimonies'), list):
                    row['Testimonies'] = [dict(t, result=row['Status']) for t in row['Testimonies']]
                    row['Count'], row['Labels'] = 0, []
                    row['Unresolved Count'] = len(row['Testimonies'])
                    for field in ('Strength Testimonies', 'Weakness Testimonies'):
                        if field in row:
                            row[field] = ''
            result.append(row)
        return result
    return _connection_unknown(values)


def evaluate_with_connection_uncertainty(function, *args, **kwargs):
    """Evaluate a complete judgment before collapsing missing connection facts.

    Can also surround a harness predicate, keeping its original meaning while
    yielding an unresolved result if its possible Boolean answers disagree.
    """
    if getattr(_CONNECTION_EVALUATION, 'decisions', None) is not None:
        return function(*args, **kwargs)
    pending, outcomes = [{}], []
    try:
        while pending:
            decisions = pending.pop()
            _CONNECTION_EVALUATION.decisions = decisions
            try:
                outcomes.append(function(*args, **kwargs))
            except _ConnectionChoice as choice:
                pending.extend(decisions | {choice.key: option} for option in choice.options)
    finally:
        _CONNECTION_EVALUATION.decisions = None
    return _connection_merge(outcomes)


def _connection_aware(function):
    @wraps(function)
    def run(*args, **kwargs):
        return evaluate_with_connection_uncertainty(function, *args, **kwargs)
    return run


# ==========================================
# THE READINGS OF ONE RUN
# ==========================================
# The seven configurable readings are the reader's, and they belong to one
# run of the script rather than to this module. While the engine was the top
# half of app.py each run executed it into a namespace of its own, so a
# reading assigned at the top of a run reached that run's evaluators and no
# one else's. The engine is a module now, imported once, and Streamlit runs
# every session's script in its own thread against that one module: a plain
# module global would be shared, and an evaluator could be handed another
# reader's rule in the middle of a run. So a reading is per-thread state --
# app.py pins them with set_readings() at the top of each run, and every
# evaluator asks for one through reading().
#
# The module constant of each name stays where it always was, next to the
# doctrine that explains it, and is the default a thread that has pinned
# nothing falls back to: an engine imported with no run behind it answers
# with the course text's own reading, as it did before the split.
_RUN = threading.local()

READINGS = ('CONNECTION_PROFILE', 'EASTERN_RULE', 'MOON_RAYS_ORB', 'MARS_WEST_RAYS_18',
            'DOMAIN_RULE', 'LOT_HOUSE_CUSP', 'SOFTENED_INFORTUNE')

def set_readings(**values):
    """Pin this thread's readings for this run. An unrecognised name is an
    error, not a setting that silently does nothing: a reading the engine
    does not read is a reading the page only thinks it has changed."""
    for name, value in values.items():
        if name not in READINGS:
            raise KeyError(f"{name!r} is not a reading the engine reads; it reads {', '.join(READINGS)}")
        setattr(_RUN, name, value)

def reading(name):
    """This thread's value for `name`, or the module's own default."""
    return getattr(_RUN, name, globals()[name])

# ==========================================
# 0. SAVED CHART PERSISTENCE
# ==========================================
# What this app calls itself. Until 2026-09-16 nothing in the code carried a
# version at all: a record written by one build and an analysis exported from
# another could not be told apart, which is half of what F08 of the review of
# that date asked for. It is written into every saved record ("saved_with")
# and into every exported analysis, it is shown in one place on the pages --
# the Sources page's citation-key caption, the owner's one exception to the
# rule that page text carries no build process -- and docs/RELEASING.md sets
# it before a tag. A release's value is the tag without the 'v' (1.4.0); a
# '-dev' suffix means a working tree between releases.
APP_VERSION = "1.0.0"

# Charts are saved as {name: {date_string, time_string, location_query}} in a
# small JSON file. Only the raw natal inputs are stored -- the full chart is
# cheaply recomputed on load rather than serialized.
#
# This lives in the OS's per-user data directory, NOT next to app.py: a
# packaged build's own folder (_internal) gets replaced whole by every new
# download, which used to silently wipe saved charts on every update unless
# the file was manually copied out and back in first.
APP_DIR_NAME = "Zij"
# What the folder was called while the app was the Traditional Astrology
# Engine (renamed 2026-09-22). A chart saved then is not left behind: the
# first run under the new name moves the whole folder across, once, and
# only when the new one does not exist yet.
_LEGACY_APP_DIR_NAME = "TraditionalAstrologyEngine"

def _user_data_dir() -> Path:
    if sys.platform == "win32":
        base = os.environ.get("APPDATA", str(Path.home() / "AppData" / "Roaming"))
    elif sys.platform == "darwin":
        base = str(Path.home() / "Library" / "Application Support")
    else:
        base = os.environ.get("XDG_DATA_HOME", str(Path.home() / ".local" / "share"))
    path = Path(base) / APP_DIR_NAME
    legacy = Path(base) / _LEGACY_APP_DIR_NAME
    if not path.exists() and legacy.is_dir():
        # One move, atomic on the one filesystem these two share; a copy
        # only if the move is refused, and then the old folder stays put
        # rather than leaving the charts in neither place.
        try:
            legacy.rename(path)
        except OSError:
            try:
                shutil.copytree(legacy, path)
            except OSError:
                pass
    path.mkdir(parents=True, exist_ok=True)
    return path

SAVED_CHARTS_PATH = _user_data_dir() / "saved_charts.json"
# Where this file used to live -- checked once so anyone upgrading from
# before this change doesn't have to move it by hand.
_LEGACY_SAVED_CHARTS_PATH = Path(__file__).parent / "saved_charts.json"

def _validate_chart_mapping(charts):
    """The saved-charts file is {chart name: entry dict}. Valid JSON is not
    enough: '[]' parsed fine and then crashed the sidebar at .keys(). Only
    the shape the UI actually indexes is checked -- a mapping of string
    names to mapping entries -- so an older entry missing a field still
    loads.

    The SHAPE only, deliberately: one entry whose fields are the wrong type
    must not condemn the whole file, which is what raising here would do
    (the loader would copy the file aside and open with an empty picker).
    An entry's own fields are checked, one entry at a time and without
    raising, by chart_record_fault() below -- a record that fails it stays
    in the mapping and in the file, and is refused only at the point where
    the sidebar would otherwise hand it to a widget."""
    if not isinstance(charts, dict):
        raise ValueError(f"saved charts root must be a mapping, got {type(charts).__name__}")
    for name, entry in charts.items():
        if not isinstance(name, str) or not isinstance(entry, dict):
            raise ValueError(f"saved chart {name!r} is not a name -> mapping entry")
    return charts


def _is_real_number(value):
    """A finite int or float -- and not a bool, which is an int in Python
    and is never a coordinate, an offset or an age."""
    if isinstance(value, bool) or not isinstance(value, (int, float)):
        return False
    return math.isfinite(value)


def record_number(value):
    """A stored field as the number it means, or None when it means no
    number. A number WRITTEN AS TEXT counts: '43.7792' is that latitude,
    and _records_match already reads it as that latitude when it decides
    whether a record has been edited -- so a loader that refused it would
    have the app calling one record two things at once. What does not
    count is text that is not a number, a bool, or an infinity."""
    if _is_real_number(value):
        return float(value)
    if isinstance(value, str):
        try:
            number = float(value.strip())
        except ValueError:
            return None
        return number if math.isfinite(number) else None
    return None


def _is_clock_string(value):
    """'HH:MM:SS' (the form _chart_record writes), as a real time."""
    if not isinstance(value, str):
        return False
    m = re.fullmatch(r"(\d{1,2}):(\d{2}):(\d{2})", value.strip())
    if not m:
        return False
    h, mi, s = (int(part) for part in m.groups())
    return h < 24 and mi < 60 and s < 60


# Each field of a saved record, and what the sidebar needs it to be before
# it can be written into a widget key. The check is by TYPE and, for the
# numbers, by RANGE: a type the sidebar cannot consume raises where it is
# consumed (an int time_string has no .split, a str latitude will not
# compare with a float), and that took the whole script down on load and at
# launch. A wrong VALUE inside the right type is left to the draft rules
# that already handle one -- a date_string of "not-a-date" goes into the
# date box and gets the box's own error, a target_date that will not parse
# keeps the last target that did, a time_standard the app no longer offers
# raises the "saved before the time standard was stored" flag. The one
# exception is a number out of range, which the widgets would silently pull
# to a bound: it is a fault here, so that no chart is cast on it.
# (utc_offset out of +-14 is its own case, reported by the loader rather
# than refused, so that the record still loads and says what it holds.)
CHART_RECORD_FIELDS = (
    ('date_string', 'date', 'date, written down as text'),
    ('time_string', 'time', 'time of day as HH:MM:SS'),
    ('time_standard', 'time standard', 'time standard, written down as text'),
    ('utc_offset', 'UTC offset', 'number of hours'),
    ('lat', 'latitude', 'number between -90 and 90'),
    ('lon', 'longitude', 'number between -180 and 180'),
    ('location_query', 'place', 'place, written down as text'),
    ('target_mode', 'target', 'target, written down as text'),
    ('target_date', 'target date', 'date, written down as text'),
    ('target_age', 'target age', 'whole number of years, zero to 9999'),
)


def _chart_field_is_usable(field, value):
    if field in ('date_string', 'time_standard', 'location_query', 'target_mode', 'target_date'):
        return isinstance(value, str)
    if field == 'time_string':
        return _is_clock_string(value)
    number = record_number(value)
    if number is None:
        return False
    if field == 'lat':
        return -90.0 <= number <= 90.0
    if field == 'lon':
        return -180.0 <= number <= 180.0
    if field == 'target_age':
        # Bounded above as well: a value past a C long overflows the
        # ephemeris call on load (the hostile pass of 2026-09-22, H1);
        # 9999 is past any birthday the ephemeris can reach, so an age
        # merely out of reach still loads and is named as such.
        return 0 <= number <= 9999 and number.is_integer()
    return True                                    # utc_offset: any number; the range is H5's


# --- The readings a record carries (schema 2, 2026-09-16) ----------------
# F03 of the review of 2026-09-16: a chart saved under Abu Ma'shar's
# connection rule came back under Sahl, because a record held the nativity
# and nothing about how it had been read. From this version a record also
# carries the doctrinal readings in force when it was saved, under
# "readings" (store key -> value, app.py's READINGS_REGISTRY), and says so
# in "saved_with": {"app": APP_VERSION, "schema": CHART_RECORD_SCHEMA}.
#
# A record written before this carries neither, and is schema 1: it loads
# exactly as it always did and is told apart by the absence, never by a
# migration -- nothing is written into an old record until the reader saves
# it again.
CHART_RECORD_SCHEMA = 2

# Each stored reading and what it may hold: the NAME of a module-level
# tuple or mapping of its options, read at call time because most of them
# are defined further down this file, or bool for a checkbox. The store
# keys are app.py's READINGS_REGISTRY's, and a test holds the two in step.
CHART_RECORD_READINGS = (
    ('_connection_rule', 'CONNECTION_PROFILES'),
    ('_eastern_rule', 'EASTERN_RULE_OPTIONS'),
    ('_fitting_infortune', bool),
    ('_moon_rays_15', bool),
    ('_mars_west_18', bool),
    ('_domain_rule', 'DOMAIN_RULE_OPTIONS'),
    ('_lot_house_cusp', 'LOT_HOUSE_CUSP_OPTIONS'),
    ('_pn4_monthly_turn', 'PN4_MONTHLY_TURN_OPTIONS'),
    ('_reading_depth', 'READING_DEPTH_OPTIONS'),
)


def _reading_options(spec):
    """The values a stored reading may hold. A string spec names a
    module-level tuple (or a mapping, whose KEYS are the options, which is
    how the connection rule's profiles are written)."""
    if spec is bool:
        return (False, True)
    values = globals()[spec]
    return tuple(values.keys() if isinstance(values, dict) else values)


def _reading_value_is_usable(store_key, value):
    for key, spec in CHART_RECORD_READINGS:
        if key != store_key:
            continue
        if spec is bool:
            return isinstance(value, bool)
        return value in _reading_options(spec)
    return True            # a key this app does not know: ignored, not a fault


def _readings_fault(readings):
    """The first stored reading this app cannot set, as (its name for a
    reader, what it is not), or None.

    A key this app does not know is IGNORED, not refused: a record written
    by a later build, or by one with a reading since withdrawn, is still
    this reader's nativity and still loads. What is refused is a value the
    widget could not take -- 'Ptolemy' for the connection rule, a string
    for a checkbox -- because seeding one into a widget key raises where the
    widget reads it, which is H3's lesson about every other field."""
    if not isinstance(readings, dict):
        return ('saved readings', 'mapping of readings')
    for store_key, value in readings.items():
        if not _reading_value_is_usable(store_key, value):
            return (store_key.lstrip('_').replace('_', ' '), 'reading this app offers')
    return None


def chart_record_fault(entry):
    """The first field of a saved record the sidebar cannot load, as
    (field's name for a reader, what it is not), or None when the record
    can be loaded. A field the record does not carry, or carries as null,
    is not a fault: records written before a field existed still load, and
    a record carrying no "readings" at all is schema 1 and loads as it
    always did.

    The record is never altered and never dropped -- the caller reports the
    fault and leaves the form as it was, and the file keeps the record
    exactly as it found it."""
    if not isinstance(entry, dict):
        return ('record', 'mapping of fields')
    for field, label, expected in CHART_RECORD_FIELDS:
        value = entry.get(field)
        if field not in entry or value is None:
            continue
        if not _chart_field_is_usable(field, value):
            return (label, expected)
    if entry.get('readings') is not None:
        fault = _readings_fault(entry['readings'])
        if fault is not None:
            return fault
    return None


def chart_record_schema(entry):
    """Which schema a stored record is written in: 2 when it carries the
    readings that were in force, 1 when it was written before this app
    stored them. What the number is READ FOR is the load: a schema-1 record
    has nothing to restore and gets a sentence saying so, while a schema-2
    record's readings are offered. A record that merely says it is schema 2
    without carrying readings is read by what it holds, not by what it
    claims."""
    if not isinstance(entry, dict) or not isinstance(entry.get('readings'), dict):
        return 1
    return CHART_RECORD_SCHEMA


def chart_record_readings(entry):
    """The readings a record was saved under, as {store key: value}, or {}
    when it carries none. Only the keys this app knows are returned -- the
    rest are kept in the file and not acted on."""
    if chart_record_schema(entry) == 1:
        return {}
    known = {key for key, _spec in CHART_RECORD_READINGS}
    return {key: value for key, value in entry['readings'].items() if key in known}


# The offset a manual time standard can be cast at, east positive. The
# widget's own bounds, kept here so the loader can read a stored offset
# against them BEFORE the widget silently pulls an out-of-range one to a
# bound -- and to the wrong bound: a stored 99 came back as -14.
UTC_OFFSET_LIMIT = 14.0


def utc_offset_in_range(value):
    number = record_number(value)
    return number is not None and -UTC_OFFSET_LIMIT <= number <= UTC_OFFSET_LIMIT

def _read_chart_mapping(path):
    """The validated mapping in `path`, or None if it is unreadable,
    malformed, or the wrong shape."""
    try:
        return _validate_chart_mapping(json.loads(path.read_text()))
    except (json.JSONDecodeError, OSError, ValueError):
        return None

# The name of the recovery copy the last load made, or None. Read by the
# sidebar, which says so once per session (F04 of the review of 2026-09-16:
# a malformed store showed an empty picker and the next save overwrote the
# only copy of the bytes). A module-level name rather than a second return
# value, because load_saved_charts() is called for its mapping in the app
# and in the tests alike and that signature stays as it is.
LAST_STORE_ERROR = None


def _preserve_unreadable_store(path):
    """Copy an unreadable store beside itself, once per content, as
    ``saved_charts.json.unreadable-<YYYYMMDD-HHMMSS>``; the copy's file name,
    or None if the bytes could not be read or written. The same bytes are
    not copied twice: a session that opens the app repeatedly against one
    broken file leaves one recovery copy, not one per launch."""
    try:
        data = path.read_bytes()
    except OSError:
        return None
    for previous in sorted(path.parent.glob(path.name + ".unreadable-*")):
        try:
            if previous.read_bytes() == data:
                return previous.name
        except OSError:
            continue
    stem = f"{path.name}.unreadable-{datetime.now():%Y%m%d-%H%M%S}"
    copy = path.with_name(stem)
    # Two different wrecks within one second: the second keeps its own copy
    # rather than writing over the first one's bytes.
    suffix = 2
    while copy.exists():
        copy = path.with_name(f"{stem}-{suffix}")
        suffix += 1
    try:
        copy.write_bytes(data)
    except OSError:
        return None
    return copy.name

def _write_text_atomically(path, text):
    """Write to a sibling temp file and os.replace() it over the target, so
    a crash or full disk mid-write leaves the previous file intact rather
    than a truncated one that loses every saved chart."""
    tmp = path.with_name(path.name + '.tmp')
    tmp.write_text(text)
    os.replace(tmp, path)

def load_saved_charts():
    global LAST_STORE_ERROR
    LAST_STORE_ERROR = None
    if not SAVED_CHARTS_PATH.exists() and _LEGACY_SAVED_CHARTS_PATH.exists():
        legacy = _read_chart_mapping(_LEGACY_SAVED_CHARTS_PATH)
        if legacy is not None:
            try:
                _write_text_atomically(SAVED_CHARTS_PATH, json.dumps(legacy, indent=2))
            except OSError:
                pass
    if SAVED_CHARTS_PATH.exists():
        charts = _read_chart_mapping(SAVED_CHARTS_PATH)
        if charts is None:
            # Malformed JSON, or valid JSON of the wrong shape. The bytes are
            # kept beside the file before anything is allowed to write over
            # them, and the name of the copy is left where the sidebar can
            # name it.
            LAST_STORE_ERROR = _preserve_unreadable_store(SAVED_CHARTS_PATH)
            return {}
        return charts
    return {}

def write_saved_charts(charts):
    try:
        _validate_chart_mapping(charts)
        _write_text_atomically(SAVED_CHARTS_PATH, json.dumps(charts, indent=2))
        return True
    except (OSError, ValueError):
        return False

# --- Preferences (2026-09-10) -------------------------------------------
# The readings and display choices, kept across runs in the same per-user
# directory as the saved charts (UI_REVIEW_2026-09-10.md §2). Streamlit's
# own persist_state lives only as long as the session, so a restart needs
# a file. Only the store keys listed here are kept, plus the name of the
# last chart loaded, which the app opens on. Missing or unknown keys are
# ignored on read, as the saved-charts loader ignores them. The harness
# sets ALMUTEN_NO_PREFERENCES=1 so a test can neither read a real file
# nor leak a reading into the next test.
PREFERENCES_PATH = _user_data_dir() / "preferences.json"
READING_DEPTH_OPTIONS = ("Course text", "Course text and supplement")
PREFERENCE_KEYS = (
    # doctrinal readings, each set on the page it affects
    '_connection_rule', '_eastern_rule', '_moon_rays_15', '_mars_west_18',   # '_five_degree_all_cusps' retired 2026-09-11
    '_fitting_infortune', '_domain_rule', '_lot_house_cusp', '_pn4_monthly_turn', '_reading_depth',
    # display
    '_wheel_layout', '_chart_bounds', '_timing_bounds', '_wheel_order', '_timing_lots', '_timing_rays',
    '_timing_twelfths', '_timing_wheel_view', '_target_mode', '_wheel_dark',
    # how many times the app has been opened, counted so that the Chart
    # page's introduction can fold itself once it has been read
    '_launches',
    # the reader's home place, set from the birthplace the sidebar has
    # resolved, for the "Here & Now" button to cast a chart at: {"label",
    # "lat", "lon"} -- validated by home_place_is_valid below
    'home_place',
)

PREFERENCE_RENAMES = (
    ('_pn4_monthly_turn', "Abu Ma'shar IX.1, 26-34", "PN IV IX.1, 26-34"),
    ('_wheel_order', "Revolution inside (Abu Ma'shar, I.6)", "Revolution inside (Abu Ma'shar's order, PN IV I.6)"),
)

# What each preference must BE. The app writes this file itself, so it is
# normally well formed; a hand-edit, a downgrade or a half-written file is
# not, and the launch block does int() on '_launches' and indexes the saved
# charts by 'last_chart' -- so a string where an int belongs used to stop
# the app from opening at all, on every page. Every key is checked against
# the type or the option tuple its own widget expects, and an entry that
# fails is dropped to its default. Silently: preferences are silent by
# design (nothing announces that a reading was restored either), and
# nothing is written back until the next _persist / _remember does it.
PREFERENCE_BOOL_KEYS = ('_moon_rays_15', '_mars_west_18', '_fitting_infortune', '_chart_bounds',
                        '_timing_bounds', '_timing_lots', '_timing_rays', '_timing_twelfths',
                        '_wheel_dark')


def _preference_option_tuples():
    """Each preference whose value is one of a named set, and that set --
    the very tuple its widget is built from, read at call time because most
    of them are defined further down this file."""
    return {
        '_connection_rule': tuple(CONNECTION_PROFILES),
        '_eastern_rule': EASTERN_RULE_OPTIONS,
        '_domain_rule': DOMAIN_RULE_OPTIONS,
        '_lot_house_cusp': LOT_HOUSE_CUSP_OPTIONS,
        '_pn4_monthly_turn': PN4_MONTHLY_TURN_OPTIONS,
        '_reading_depth': READING_DEPTH_OPTIONS,
        '_wheel_layout': WHEEL_LAYOUT_OPTIONS,
        '_wheel_order': WHEEL_ORDER_OPTIONS,
        '_timing_wheel_view': WHEEL_VIEW_OPTIONS,
        '_target_mode': TARGET_MODE_OPTIONS,
    }


def coordinates_in_range(lat, lon):
    """A pair the engine can be asked about at all: both finite, latitude
    within [-90, 90] and longitude within [-180, 180]. Polar is valid.
    The sidebar's one validation of a place (it stood in app.py until the
    home place needed it here, for the preferences file)."""
    try:
        lat, lon = float(lat), float(lon)
    except (TypeError, ValueError):
        return False
    if not (math.isfinite(lat) and math.isfinite(lon)):
        return False
    return -90.0 <= lat <= 90.0 and -180.0 <= lon <= 180.0


HOME_PLACE_FIELDS = frozenset(("label", "lat", "lon"))


def home_place_is_valid(value):
    """Whether `value` is a home place the sidebar can cast a chart at:
    exactly the three fields, the label a non-empty string, the
    coordinates floats (an int is not one -- the app writes floats and a
    hand-edit that wrote 43 is refused with the rest), finite and in range.
    A bool, a NaN, a 91, a missing field and an extra field are all
    refused, so a hand-edited preferences.json cannot take the app down:
    the loader drops the entry and the button stands disabled."""
    if not isinstance(value, dict) or set(value) != HOME_PLACE_FIELDS:
        return False
    label, lat, lon = value["label"], value["lat"], value["lon"]
    if not isinstance(label, str) or not label.strip():
        return False
    if not (isinstance(lat, float) and isinstance(lon, float)):
        return False
    return coordinates_in_range(lat, lon)


def preference_is_valid(key, value):
    """Whether `value` is a value the widget behind `key` could hold."""
    if key == '_launches':
        return isinstance(value, int) and not isinstance(value, bool) and value >= 0
    if key == 'last_chart':
        return isinstance(value, str)
    if key == 'home_place':
        return home_place_is_valid(value)
    if key in PREFERENCE_BOOL_KEYS:
        return isinstance(value, bool)
    options = _preference_option_tuples().get(key)
    if options is not None:
        return value in options
    return False


def preferences_enabled():
    return os.environ.get("ALMUTEN_NO_PREFERENCES") != "1"

def load_preferences():
    """The stored preferences as a dict, keys limited to PREFERENCE_KEYS and
    'last_chart', values limited to what the widget behind each key can
    hold; an empty dict when there is no file, or it is unreadable, or the
    harness has switched preferences off.

    An entry whose value is the wrong type or not one of its options is
    dropped, so the reading falls to its default -- the app opens, and the
    file is left as it is until the next reading is set."""
    if not preferences_enabled() or not PREFERENCES_PATH.exists():
        return {}
    try:
        raw = json.loads(PREFERENCES_PATH.read_text())
    except (json.JSONDecodeError, OSError):
        return {}
    if not isinstance(raw, dict):
        return {}
    prefs = {k: v for k, v in raw.items() if k in PREFERENCE_KEYS or k == 'last_chart'}
    # Option values renamed by the citation convention of 2026-09-10 (a
    # locator names its volume, never the author alone); a file written
    # before it still loads.
    for key, old_value, new_value in PREFERENCE_RENAMES:
        if prefs.get(key) == old_value:
            prefs[key] = new_value
    # The renames run first: a value written under the old name is a valid
    # value once it has been brought forward.
    return {k: v for k, v in prefs.items() if preference_is_valid(k, v)}

def write_preferences(prefs):
    if not preferences_enabled():
        return False
    try:
        _write_text_atomically(PREFERENCES_PATH, json.dumps(prefs, indent=2, sort_keys=True))
        return True
    except OSError:
        return False

# --- The clock ----------------------------------------------------------
# The two readings of this computer's clock the "Here & Now" button makes,
# each one small function so that a test can monkeypatch it on this module
# and no test depends on the wall clock. The button's callback reads them
# through `engine.` at call time, so a patch on the module object holds
# whether the app took the names through its star import or not.

def now_utc():
    """This moment, aware, in UTC."""
    return datetime.now(timezone.utc)


def local_clock(instant):
    """`instant` as this computer's own clock shows it: the same moment
    in the zone the operating system is set to, offset and all."""
    return instant.astimezone()

# ==========================================
# 1. CORE CALCULATION ENGINE
# ==========================================

@st.cache_data(max_entries=32, show_spinner=False)
def _sin_altitude(ecl_lon, ecl_lat, distance, obliquity, armc, geo_lat):
    """Sine of a body's geocentric altitude above the horizon of geo_lat:
    sin(alt) = sin(phi) sin(delta) + cos(phi) cos(delta) cos(H), with the
    hour angle H = ARMC - RA and RA/delta from the body's actual ecliptic
    longitude, latitude and distance (swe.cotrans). No refraction, no
    parallax. Positive is above the horizon. This, not the ecliptic proxy
    (lon - Ascendant) % 360 > 180, decides sect and which luminary was up:
    the proxy inverts near the poles and on the horizon (fixed 2026-09-08)."""
    ra, decl, _r = swe.cotrans((ecl_lon, ecl_lat, distance), -obliquity)
    p, d, h = map(math.radians, (geo_lat, decl, armc - ra))
    return math.sin(p) * math.sin(d) + math.cos(p) * math.cos(d) * math.cos(h)

# --- The civil calendar ---------------------------------------------------
# ONE policy, in one place. The digits of a civil date are a JULIAN-calendar
# date before 1582-10-15 and a Gregorian one from that day on, as Solar Fire
# and astro.com read them; the Swiss Ephemeris manual (s. 9.1) keeps the two
# flags apart for exactly this reason. Using the wrong flag mis-dates a
# historical chart by days (seven in the 1200s), which the Moon turns into
# tens of degrees.
#
# Python's datetime is proleptic Gregorian and CANNOT HOLD a Julian-only day:
# 1300-02-29 exists in the Julian calendar and not in the Gregorian. So the
# carrier of a civil date in this file is CivilDate / CivilMoment below, and a
# shift by a UTC offset is done on the Julian Day, never by datetime
# arithmetic. The earlier adapter subtracted the offset with datetime,
# producing Gregorian components, and then read those digits back as Julian:
# local Julian 1300-03-01 00:30 at +02:00 came out a full day wrong (Astra
# audit 2026-09-11, F01). Rounding a moment to the second is done on the JD
# too, so that a carry across midnight is a date carry, not a clamp.
PN4_GREGORIAN_REFORM_JD = 2299160.5     # 1582-10-15 00:00 UT, the first Gregorian day
_MONTH_ABBR = ('Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec')
_MONTH_NAME = ('January', 'February', 'March', 'April', 'May', 'June', 'July', 'August',
               'September', 'October', 'November', 'December')

def civil_calendar(year, month, day):
    """swe.JUL_CAL or swe.GREG_CAL for a civil date's digits, by the policy above."""
    return swe.GREG_CAL if (int(year), int(month), int(day)) >= (1582, 10, 15) else swe.JUL_CAL

def civil_is_valid(year, month, day):
    """Whether the digits name a day in the calendar the policy assigns them:
    1300-02-29 is valid (Julian), 1900-02-29 is not (Gregorian)."""
    year, month, day = int(year), int(month), int(day)
    if not (1 <= year <= 9999 and 1 <= month <= 12 and 1 <= day <= 31):
        return False
    cal = civil_calendar(year, month, day)
    y, m, d, _h = swe.revjul(swe.julday(year, month, day, 0.0, cal), cal)
    return (int(y), int(m), int(d)) == (year, month, day)

def civil_to_jd(year, month, day, hour=0.0):
    """The Julian Day of a civil moment (UT hour, decimal), calendar by policy."""
    return swe.julday(int(year), int(month), int(day), float(hour), civil_calendar(year, month, day))

def civil_local_to_jd_ut(year, month, day, hour_local, utc_offset_hours):
    """A LOCAL civil moment to UT as a Julian Day: the calendar is decided by
    the local digits, the offset (east positive, hours) is subtracted on the
    JD. This is the sidebar's adapter for LMT and manual offsets."""
    return civil_to_jd(year, month, day, hour_local) - float(utc_offset_hours) / 24.0

def _format_civil(c, spec):
    """strftime's common directives for CivilDate / CivilMoment: %Y %m %d %b
    %B %H %M %S %%. Anything else is left in place."""
    if not spec:
        return c.isoformat()
    out, i = [], 0
    while i < len(spec):
        ch = spec[i]
        if ch == '%' and i + 1 < len(spec):
            d = spec[i + 1]
            rep = {'Y': f"{c.year:04d}", 'm': f"{c.month:02d}", 'd': f"{c.day:02d}",
                   'b': _MONTH_ABBR[c.month - 1], 'B': _MONTH_NAME[c.month - 1],
                   'H': f"{getattr(c, 'hour', 0):02d}", 'M': f"{getattr(c, 'minute', 0):02d}",
                   'S': f"{getattr(c, 'second', 0):02d}", '%': '%'}.get(d)
            if rep is not None:
                out.append(rep)
                i += 2
                continue
        out.append(ch)
        i += 1
    return ''.join(out)

class CivilDate:
    """A civil date as digits, read in the calendar civil_calendar() assigns
    them. Carries a birth or target date where datetime.date used to, and
    unlike it can hold a Julian-only day. Compares equal to anything with
    the same year, month and day (a datetime.date included)."""
    __slots__ = ('year', 'month', 'day')

    def __init__(self, year, month, day):
        self.year, self.month, self.day = int(year), int(month), int(day)

    @classmethod
    def of(cls, d):
        return d if isinstance(d, cls) else cls(d.year, d.month, d.day)

    @classmethod
    def from_jd(cls, jd):
        cal = swe.GREG_CAL if jd >= PN4_GREGORIAN_REFORM_JD else swe.JUL_CAL
        y, m, d, _h = swe.revjul(jd, cal)
        return cls(y, m, d)

    def tuple(self):
        return (self.year, self.month, self.day)

    def __eq__(self, other):
        try:
            return self.tuple() == (int(other.year), int(other.month), int(other.day))
        except (AttributeError, TypeError, ValueError):
            return NotImplemented

    def __lt__(self, other):
        return self.jd() < CivilDate.of(other).jd()

    def __hash__(self):
        return hash(self.tuple())

    def __sub__(self, other):
        # a timedelta (days) back, or the number of days between two dates
        if hasattr(other, 'days') and not hasattr(other, 'year'):
            return CivilDate.from_jd(self.jd() - other.days)
        return self.jd() - CivilDate.of(other).jd()

    def __add__(self, other):
        return CivilDate.from_jd(self.jd() + other.days)

    def jd(self, hour=0.0):
        return civil_to_jd(self.year, self.month, self.day, hour)

    def replace(self, **kw):
        return CivilDate(kw.get('year', self.year), kw.get('month', self.month), kw.get('day', self.day))

    def calendar_name(self):
        return 'Julian' if civil_calendar(self.year, self.month, self.day) == swe.JUL_CAL else 'Gregorian'

    def isoformat(self):
        return f"{self.year:04d}-{self.month:02d}-{self.day:02d}"

    def __format__(self, spec):
        return _format_civil(self, spec)

    def __str__(self):
        return self.isoformat()

    def __repr__(self):
        return f"CivilDate({self.year}, {self.month}, {self.day})"

class CivilMoment(CivilDate):
    """A CivilDate with a time of day (whole seconds), UT or local as the
    caller says. from_jd rounds the JD to the second FIRST, so a moment a
    fraction of a second before midnight carries into the next day."""
    __slots__ = ('hour', 'minute', 'second')

    def __init__(self, year, month, day, hour=0, minute=0, second=0):
        super().__init__(year, month, day)
        self.hour, self.minute, self.second = int(hour), int(minute), int(second)

    @classmethod
    def from_jd(cls, jd, calendar=None):
        """`calendar` (swe.JUL_CAL / swe.GREG_CAL) writes the moment in that
        calendar instead of the epoch's own: the sidebar writes a UT in the
        birth date's calendar, so a Julian birth whose UT falls after the
        reform's JD is not printed as a Gregorian date under the "Julian
        calendar" label (the hostile pass of 2026-09-22, M4)."""
        jd = round(float(jd) * 86400.0) / 86400.0
        cal = calendar if calendar is not None else (swe.GREG_CAL if jd >= PN4_GREGORIAN_REFORM_JD else swe.JUL_CAL)
        y, m, d, hour = swe.revjul(jd, cal)
        total = min(int(round(hour * 3600.0)), 24 * 3600 - 1)
        return cls(y, m, d, total // 3600, (total % 3600) // 60, total % 60)

    def hour_decimal(self):
        return self.hour + self.minute / 60.0 + self.second / 3600.0

    def jd(self, hour=None):
        return civil_to_jd(self.year, self.month, self.day, self.hour_decimal() if hour is None else hour)

    def isoformat(self):
        return f"{super().isoformat()} {self.hour:02d}:{self.minute:02d}:{self.second:02d}"

    def __repr__(self):
        return f"CivilMoment({self.year}, {self.month}, {self.day}, {self.hour}, {self.minute}, {self.second})"

def calculate_traditional_chart(dt_utc, lat, lon):
    """A chart from a UT moment given as anything with year, month, day,
    hour, minute and second (a datetime or a CivilMoment); the digits are
    read in the calendar the policy above assigns them."""
    jd = civil_to_jd(dt_utc.year, dt_utc.month, dt_utc.day,
                     dt_utc.hour + dt_utc.minute / 60.0 + dt_utc.second / 3600.0)
    return calculate_traditional_chart_jd(jd, lat, lon)

def calculate_traditional_chart_jd(jd, lat, lon):
    """The chart at a Julian Day (UT). The sidebar arrives here directly, so
    a Julian-only date and an offset across midnight need no datetime."""
    # The seven planets' ephemeris ids are PLANET_SWE_IDS, defined once with
    # the VII.6 material; the chart adds the TRUE Node (owner's decision of
    # 2026-09-07, matching the reference charts; the mean node sat 1-2 degrees
    # off them). The true node's speed is carried like a planet's, and it is
    # not always retrograde: it has short direct spells, which the wheel
    # reports as they are rather than assuming the mean node's constant
    # backward motion.
    targets = {**PLANET_SWE_IDS, 'North Node': swe.TRUE_NODE}
    
    planetary_data = {}
    for name, obj_id in targets.items():
        res, _ = swe.calc_ut(jd, obj_id)
        planetary_data[name] = {
            'longitude': res[0],
            'latitude': res[1],
            'distance': res[2],
            'speed_in_lon': res[3],
            # Latitude and distance speeds are carried so that Abu Ma'shar's
            # conditions "in themselves" (Gr. Intr. VII.1) can be
            # told apart from their static counterparts: VII.6, 22 and 38
            # distinguish "RISING UP in the north" from merely "being
            # northern," and "GOING DOWN in the south" from being southern.
            'speed_in_lat': res[4],
            'speed_in_dist': res[5],
        }

    cusps, ascmc = swe.houses(jd, lat, lon, b'B')
    ascendant = ascmc[0]
    mc = ascmc[1]
    descendant = (ascendant + 180.0) % 360.0
    ic = (mc + 180.0) % 360.0
    
    # True obliquity of the ecliptic at the moment (Lesson 5 worksheet
    # line 15); computed here because sect needs it below.
    obliquity = swe.calc_ut(jd, swe.ECL_NUT)[0][0]

    # Sect from the Sun's ALTITUDE (_sin_altitude above), not from its
    # ecliptic longitude against the Ascendant. The old test, (Sun -
    # Ascendant) % 360 > 180, asked whether the Sun's ecliptic degree lies
    # in the eastern or western half-zodiac, which agrees with the horizon
    # at ordinary latitudes but not near the poles (2026-01-01 00:00 UT at
    # 70S: Sun three degrees up, read as Nocturnal) nor exactly on the
    # horizon, where the Sun's own ecliptic latitude decides. Fixed
    # 2026-09-08.
    sun = planetary_data['Sun']
    is_diurnal = _sin_altitude(sun['longitude'], sun['latitude'], sun['distance'], obliquity, ascmc[2], lat) > 0.0
    sect = 'Diurnal' if is_diurnal else 'Nocturnal'

    # From its LOT_DEFINITIONS row, like every other Lot in the file.
    lot_of_fortune = lot_by_id('fortune', planetary_data, ascendant, cusps, sect)

    return {
        'julian_day': jd,
        'planetary_data': planetary_data,
        'houses': cusps,
        'ascendant': ascendant,
        'descendant': descendant,
        'mc': mc,
        'ic': ic,
        'sect': sect,
        'lot_of_fortune': lot_of_fortune,
        # Lesson 5 worksheet lines 14 and 15: the right ascension of the
        # meridian from the same houses call, and the true obliquity of the
        # ecliptic at the moment. Read by the Chart page's Calculation table.
        'armc': ascmc[2],
        'obliquity': obliquity,
        # The place, so that evaluators needing the horizon (hayz: above or
        # below the earth by ALTITUDE, Astra F04) can be handed it from the
        # chart alone.
        'geo_lat': lat,
        'geo_lon': lon,
    }

# ==========================================
# 2. HELPER FUNCTIONS & VARIATION-SELECTOR-FREE RENDERER
# ==========================================

SIGN_ORDER = ['Aries', 'Taurus', 'Gemini', 'Cancer', 'Leo', 'Virgo', 'Libra', 'Scorpio', 'Sagittarius', 'Capricorn', 'Aquarius', 'Pisces']

def get_zodiac_sign(longitude):
    # Longitudes are circular: exactly 360.0 is 0 Aries, not a thirteenth
    # sign. Every public geometry helper normalises at its own door.
    return SIGN_ORDER[int((longitude % 360.0) // 30)]

def get_degree_string(longitude):
    """A longitude as DD° Sgn MM'. Display policy: whole arcminutes,
    TRUNCATED (12°27'40" prints 27'), with one tolerance -- a value within a
    millionth of a minute of a whole minute IS that minute. Binary floats
    put 30 + 1/60 a hair under 00° Tau 01' and int() printed 00' (Astra
    F13); an entered whole minute now survives display. The carry at 60',
    30° and 360° follows from working in total minutes. Nothing here
    rounds the longitude itself; bound and degree tests read the float."""
    total = (longitude % 360.0) * 60.0
    nearest = round(total)
    if abs(total - nearest) < 1e-6:
        total = nearest
    total = int(total) % (360 * 60)
    deg_total, minute = divmod(total, 60)
    return f"{deg_total % 30:02d}° {SIGN_ORDER[deg_total // 30][:3]} {minute:02d}'"

# ---- The chart wheel ---------------------------------------------------
# Redesigned 2026-09-07 (brief: process/tae_docs/WHEEL_REDESIGN_2026-09-07.md, decisions
# D1-D10). generate_hybrid_svg() is a pure function -- no Streamlit calls --
# returning an SVG string that the Chart page shows through st.image(), which
# wraps it in Streamlit's own fullscreen control, the same expand arrows the
# tables carry. Inside an <img> the SVG has no page CSS, web fonts or scripts,
# so everything here is attributes and system fonts.
#
# Rings, outside in: a rim band with the WHOLE-SIGN place numbers; the sign
# band with the glyphs, shaded by triplicity, and a one-degree scale on its
# inner edge; the planet zone, where each point's label runs radially --
# glyph, degree, sign, minute, and a black retrograde mark -- as the owner's
# reference charts draw it; a narrow ring with the ALCHABITIUS house numbers;
# and the hub, which names the chart. The rising sign's boundary sits at
# 9 o'clock (D1: the app's own convention, kept), so whole-sign places fall on
# the twelve clock positions. Alchabitius cusps stay dashed (the app's
# convention) and now run from the quadrant ring to the degree scale.
WHEEL_LAYOUT_OPTIONS = ('Square', 'Wide')
WHEEL_SIZE = 1000                     # viewBox of the square wheel
WHEEL_WIDE_WIDTH = 1760               # the wide variant: wheel plus a positions panel
_R_RIM, _R_WS_IN, _R_SIGN_IN = 492, 462, 402
_R_GLYPH, _R_DEG, _R_SIGN, _R_MIN, _R_RX = 344, 306, 272, 242, 214
_R_Q_OUT, _R_Q_IN = 152, 122
_R_BOUNDS_BAND = 24                   # the Egyptian-bounds ring, inside the degree scale, when drawn
LABEL_MIN_SEP = 10.5                  # degrees between neighbouring label stacks
LABEL_STAGGER = 30                    # px inward for alternate members of a crowded run
# One tint per triplicity (D3): fire, earth, air, water; sign i uses i % 4.
TRIPLICITY_TINT = ('#f7f7f7', '#dedede', '#ededed', '#cdcdcd')
_WHEEL_FONT = "'TAE Symbols', 'Noto Sans Symbols', 'Segoe UI Symbol', 'DejaVu Sans', sans-serif"
_CUSP_COLOUR = '#a94442'
_AXIS_COLOUR = {0: '#0000cc', 6: '#0000cc', 9: '#1e7b1e', 3: '#1e7b1e'}   # horizon blue, meridian green (D4)
_VS = '︎'                        # text-presentation selector: never an emoji
SIGN_GLYPHS = ['♈', '♉', '♊', '♋', '♌', '♍',
               '♎', '♏', '♐', '♑', '♒', '♓']
POINT_GLYPHS = {'Sun': '☉', 'Moon': '☽', 'Mercury': '☿', 'Venus': '♀',
                'Mars': '♂', 'Jupiter': '♃', 'Saturn': '♄',
                'North Node': '☊', 'South Node': '☋', 'Lot of Fortune': '⊗'}


def _spread_labels(bearings, min_sep=LABEL_MIN_SEP):
    """Push neighbouring bearings apart on the circle until every adjacent
    pair is at least min_sep degrees apart. bearings are in ascending order
    around the circle; that circular order is preserved and the result
    comes back in the same order. Input already separated is unchanged.

    The sequence is unwrapped to a monotone one first and the gaps kept
    SIGNED, so a pair pushed apart cannot leapfrog its neighbours (a
    modular gap would read the overshoot as a wide gap and leave the order
    scrambled). A negative gap is a violation like any other and is pushed
    open on the next sweep."""
    n = len(bearings)
    if n < 2:
        return [b % 360.0 for b in bearings]
    min_sep = min(min_sep, 0.9 * 360.0 / n)       # n labels must fit the circle
    pos = [bearings[0] % 360.0]
    for b in bearings[1:]:
        b = b % 360.0
        while b < pos[-1]:
            b += 360.0
        pos.append(b)
    for _ in range(1000):
        moved = False
        for k in range(n):
            j = (k + 1) % n
            gap = (pos[j] + (360.0 if j == 0 else 0.0)) - pos[k]
            if gap < min_sep - 1e-6:
                push = (min_sep - gap) / 2.0
                pos[k] -= push
                pos[j] += push
                moved = True
        if not moved:
            break
    return [p % 360.0 for p in pos]


def _stagger_offsets(true_bearings, shown_bearings, step=LABEL_STAGGER):
    """Within each run of consecutive displaced labels, every other one
    steps inward by `step` px, as Solar Fire does for stellia."""
    displaced = [abs(((s - t + 180.0) % 360.0) - 180.0) > 0.05 for s, t in zip(shown_bearings, true_bearings)]
    offsets = [0] * len(displaced)
    k = 0
    while k < len(displaced):
        if displaced[k]:
            end = k
            while end < len(displaced) and displaced[end]:
                end += 1
            for n, idx in enumerate(range(k, end)):
                offsets[idx] = step if n % 2 else 0
            k = end
        else:
            k += 1
    return offsets


def _wheel_dm(longitude):
    """Degree and minute within the sign, minutes truncated exactly as
    get_degree_string() truncates them, so the wheel and the tables agree."""
    longitude = longitude % 360.0
    return int(longitude % 30), int((longitude % 1) * 60)


def generate_hybrid_svg(chart_data, chart_name, location_query, lat, lon, dt_local, tz_name,
                        wide=False, chronocrats=None, bounds=False, theme=None):
    pal = _wheel_palette(theme)
    size = WHEEL_SIZE
    cx = cy = size / 2.0
    asc = chart_data['ascendant']
    mc = chart_data['mc']
    cusps = chart_data['houses']
    asc_sign = int((asc % 360.0) // 30)
    angles = (asc, mc, (asc + 180.0) % 360.0, (mc + 180.0) % 360.0)

    # D1: the rising sign's boundary at 9 o'clock, zodiac counter-clockwise.
    # The geometry and the two primitives are shared with the multi-ring
    # wheels of the Timing page (2026-09-10).
    ang, xy, sector, _arc = _wheel_geometry(asc_sign, cx, cy)
    line, text = _svg_line, _svg_text
    # An Egyptian-bounds ring inside the degree scale (2026-09-10): PN IV's
    # nativities carry one (Figures 1, 22 and 25), and the revolution of
    # Figure 26 does; Figure 51, the simplified I.6 wheel, does not. It sits in
    # the leader zone, so the planet stack is not moved.
    r_planet_edge = _R_SIGN_IN - _R_BOUNDS_BAND if bounds else _R_SIGN_IN

    def sign_glyph(longitude):
        return SIGN_GLYPHS[int((longitude % 360.0) // 30)] + _VS

    width = WHEEL_WIDE_WIDTH if wide else size
    svg = [f'<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 {width} {size}" width="{width}" height="{size}" '
           f'style="font-family:{_WHEEL_FONT}">',
           f'<style>{font_face_css()}</style>',
           f'<rect width="{width}" height="{size}" fill="{pal["bg"]}"/>']

    # 1. Sign band shaded by triplicity, sign glyphs, whole-sign place
    #    numbers on the rim, boundary spokes through both bands.
    #
    #    Each sign's own elements stand in a <g class="sign" data-sign="0-11">
    #    and each point's in a <g class="pt" data-point data-lon>, the
    #    convention the revolution wheels have carried since 2026-09-10
    #    (_draw_ring_points). The groups add a handle and nothing else: strip
    #    the wrappers and the picture is the picture it always was, which is
    #    what tests/test_clickable_wheel_2026_09_15.py asserts against main.
    for i in range(12):
        a0, a1 = ang(i * 30), ang(i * 30 + 30)
        svg.append(f'<g class="sign" data-sign="{_esc_attr(i)}">')
        svg.append(f'<path d="{sector(_R_SIGN_IN, _R_WS_IN, a0, a1)}" fill="{pal["tints"][i % 4]}"/>')
        svg.append(f'<path d="{sector(_R_WS_IN, _R_RIM, a0, a1)}" fill="{pal["panel"]}"/>')
        # An angle box (step 4) sits wherever the Ascendant or MC falls in its
        # sign; a glyph within 7 degrees of one steps 8 degrees aside.
        glyph_lon = i * 30 + 15
        for angle_lon in angles:
            gap = ((angle_lon - glyph_lon + 180.0) % 360.0) - 180.0
            if abs(gap) < 7.0:
                glyph_lon = i * 30 + 15 - (8 if gap > 0 else -8)
        gx, gy = xy((_R_SIGN_IN + _R_WS_IN) / 2, ang(glyph_lon))
        svg.append(text(gx, gy, SIGN_GLYPHS[i] + _VS, 30, fill=pal['ink']))
        hx, hy = xy((_R_WS_IN + _R_RIM) / 2, ang(i * 30 + 15))
        svg.append(text(hx, hy, str((i - asc_sign) % 12 + 1), 17, 'bold', pal['ink']))
        x0, y0 = xy(_R_SIGN_IN, a0); x1, y1 = xy(_R_RIM, a0)
        svg.append(line(x0, y0, x1, y1, pal['rule'], 1.2))
        svg.append('</g>')

    # 2. Degree scale on the inner edge of the sign band: 1, 5 and 10 degrees.
    for d in range(360):
        ln = 14 if d % 10 == 0 else 10 if d % 5 == 0 else 5
        x0, y0 = xy(_R_SIGN_IN, ang(d)); x1, y1 = xy(_R_SIGN_IN + ln, ang(d))
        svg.append(line(x0, y0, x1, y1, pal['rule'], 0.9 if ln > 5 else 0.5))
    if bounds:
        svg.extend(_bounds_ring_svg(ang, xy, sector, r_planet_edge, _R_SIGN_IN, glyph_px=12, pal=pal))
    for r, w in ((_R_RIM, 2.5), (_R_WS_IN, 1.2), (_R_SIGN_IN, 1.6), (_R_Q_OUT, 1.2), (_R_Q_IN, 1.6)):
        svg.append(f'<circle cx="{cx}" cy="{cy}" r="{r}" fill="none" stroke="{pal["rule"]}" stroke-width="{w}"/>')

    # 3. Alchabitius cusps, dashed as before but reaching the degree scale;
    #    the four stakes solid and coloured. House numbers at mid-house on
    #    the quadrant ring.
    for i, cusp_lon in enumerate(cusps):
        x0, y0 = xy(_R_Q_IN, ang(cusp_lon)); x1, y1 = xy(r_planet_edge, ang(cusp_lon))
        if i in pal['axis']:
            svg.append(line(x0, y0, x1, y1, pal['axis'][i], 2.6))
        else:
            svg.append(line(x0, y0, x1, y1, pal['cusp'], 1.4, '6 4'))
        mid = cusp_lon + ((cusps[(i + 1) % 12] - cusp_lon) % 360.0) / 2.0
        mx, my = xy((_R_Q_OUT + _R_Q_IN) / 2, ang(mid))
        svg.append(text(mx, my, str(i + 1), 14, 'bold', pal['cusp']))

    # 4. Angle boxes on the sign band: degree over minute, in the axis colour.
    #    The four angles carry data-point under the names the Calculated
    #    Points table prints, so a click on a box reaches the same panel a
    #    click on a planet does.
    for angle_lon, colour, angle_name in zip(angles, (pal['horizon'], pal['meridian'], pal['horizon'], pal['meridian']),
                                             ('Ascendant', 'Midheaven', 'Descendant', 'Imum Coeli')):
        d, m = _wheel_dm(angle_lon)
        svg.append(f'<g class="pt" data-point="{_esc_attr(angle_name)}" data-lon="{angle_lon % 360.0:.6f}">')
        bx, by = xy((_R_SIGN_IN + _R_WS_IN) / 2, ang(angle_lon))
        svg.append(f'<rect x="{bx - 19:.1f}" y="{by - 17:.1f}" width="38" height="34" rx="3" '
                   f'fill="{pal["panel"]}" stroke="{colour}" stroke-width="1.2"/>')
        svg.append(text(bx, by - 8, f'{d:02d}°', 13, 'bold', colour))
        svg.append(text(bx, by + 8, f"{m:02d}'", 13, 'bold', colour))
        svg.append('</g>')

    # 5. Points. The seven planets and the north node from the ephemeris, the
    #    south node opposite it with the same motion (D2), the Lot of Fortune
    #    with no motion. A retrograde mark goes on any point whose longitude
    #    speed is negative, nodes included (D9): the true node has direct
    #    spells and is shown as it moves.
    p_data = chart_data['planetary_data']
    node = p_data['North Node']
    points = [(name, d['longitude'], d.get('speed_in_lon', 0.0)) for name, d in p_data.items()]
    points.append(('South Node', (node['longitude'] + 180.0) % 360.0, node.get('speed_in_lon', 0.0)))
    points.append(('Lot of Fortune', chart_data['lot_of_fortune'], None))
    points.sort(key=lambda p: ang(p[1]))
    true_bearings = [ang(p[1]) for p in points]
    shown = _spread_labels(true_bearings)
    offsets = _stagger_offsets(true_bearings, shown)
    for (name, lon_val, speed), a_true, a_shown, off in zip(points, true_bearings, shown, offsets):
        # A tick on the ring at the true degree, a hairline from it to the glyph.
        svg.append(f'<g class="pt" data-point="{_esc_attr(name)}" data-lon="{lon_val % 360.0:.6f}">')
        x0, y0 = xy(r_planet_edge, a_true); x1, y1 = xy(r_planet_edge - 10, a_true)
        svg.append(line(x0, y0, x1, y1, pal['rule'], 1.6))
        x2, y2 = xy(_R_GLYPH - off + 22, a_shown)
        svg.append(line(x1, y1, x2, y2, pal['leader'], 0.8))
        d, m = _wheel_dm(lon_val)
        gx, gy = xy(_R_GLYPH - off, a_shown); svg.append(text(gx, gy, POINT_GLYPHS.get(name, name[:2]) + _VS, 34, fill=pal['ink']))
        dx_, dy_ = xy(_R_DEG - off, a_shown); svg.append(text(dx_, dy_, f'{d:02d}°', 18, 'bold', pal['ink']))
        sx, sy = xy(_R_SIGN - off, a_shown); svg.append(text(sx, sy, sign_glyph(lon_val), 19, fill=pal['ink']))
        mx, my = xy(_R_MIN - off, a_shown); svg.append(text(mx, my, f"{m:02d}'", 15, fill=pal['ink']))
        if speed is not None and speed < 0:
            rx, ry = xy(_R_RX - off, a_shown); svg.append(text(rx, ry, '℞' + _VS, 15, fill=pal['ink']))   # the ink, never red (D6)
        svg.append('</g>')

    # 6. Hub: the chart's name first (D5 supplies "Transits" when none was
    #    given), then when, where, and the systems in force. User strings
    #    are escaped: a name or place with & or < would otherwise break the
    #    whole wheel.
    svg.append(f'<circle cx="{cx}" cy="{cy}" r="{_R_Q_IN - 1}" fill="{pal["panel"]}"/>')
    name = str(chart_name)
    if len(name) > 30:
        name = name[:29] + '…'
    name_px = 17 if len(name) <= 16 else 14 if len(name) <= 22 else 11
    lat_s = f"{int(abs(lat))}°{int((abs(lat) % 1) * 60):02d}′{'N' if lat >= 0 else 'S'}"
    lon_s = f"{int(abs(lon))}°{int((abs(lon) % 1) * 60):02d}′{'E' if lon >= 0 else 'W'}"
    hub_lines = [
        (_esc_text(name), name_px, 'bold'),
        (f'{dt_local.day} {dt_local:%b} {dt_local.year}  {dt_local:%H:%M}', 12, 'normal'),
        (_esc_text(tz_name), 11, 'normal'),
        (_esc_text(location_query), 12, 'normal'),
        (f'{lat_s}  {lon_s}', 11, 'normal'),
        (_esc_text(chart_data['sect']), 12, 'bold'),
        ('Whole sign · Alchabitius', 10, 'normal'),
        ('Tropical · True node', 10, 'normal'),
    ]
    y = cy - 7 * len(hub_lines)
    for s, px, w in hub_lines:
        svg.append(text(cx, y, s, px, w, pal['ink']))
        y += 14

    # 7. The wide variant: a positions panel beside the wheel, so a full-
    #    window view uses the window's width as well as its height (D10).
    if wide:
        x0 = size + 40
        svg.append(text(x0, 60, 'Positions', 22, 'bold', pal['ink'], anchor='start'))
        for dx, h in ((0, 'Point'), (150, 'Position'), (330, 'WS place'), (460, 'Quadrant'), (590, 'Motion')):
            svg.append(text(x0 + dx, 100, h, 15, 'bold', pal['label'], anchor='start'))
        svg.append(line(x0, 112, x0 + 680, 112, pal['rule'], 1))
        y = 140
        for name_, lon_val, speed in sorted(points, key=lambda p: list(POINT_GLYPHS).index(p[0]) if p[0] in POINT_GLYPHS else 99):
            d, m = _wheel_dm(lon_val)
            if speed is None:
                motion = '–'
            else:
                motion = '℞' + _VS + ' retrograde' if speed < 0 else 'direct'
            svg.append(text(x0, y, POINT_GLYPHS.get(name_, '') + _VS, 22, fill=pal['ink'], anchor='start'))
            svg.append(text(x0 + 34, y, _esc_text(name_), 16, fill=pal['ink'], anchor='start'))
            svg.append(text(x0 + 150, y, f'{d:02d}° {sign_glyph(lon_val)} {m:02d}′', 16, fill=pal['ink'], anchor='start'))
            svg.append(text(x0 + 330, y, str(get_wsh_house(lon_val, asc)), 16, fill=pal['ink'], anchor='start'))
            svg.append(text(x0 + 460, y, str(get_effective_house(lon_val, cusps)), 16, fill=pal['ink'], anchor='start'))
            svg.append(text(x0 + 590, y, motion, 15, fill=pal['ink'], anchor='start'))
            y += 36
        y += 20
        svg.append(text(x0, y, 'Angles and cusps (Alchabitius)', 18, 'bold', pal['ink'], anchor='start'))
        y += 34
        for i, cusp_lon in enumerate(cusps):
            d, m = _wheel_dm(cusp_lon)
            label = {0: 'Asc', 3: 'IC', 6: 'Des', 9: 'MC'}.get(i, '')
            col = x0 + (0 if i < 6 else 340)
            yy = y + (i % 6) * 32
            svg.append(text(col, yy, f'{i + 1:2d}', 15, 'bold', pal['cusp'], anchor='start'))
            svg.append(text(col + 40, yy, f'{d:02d}° {sign_glyph(cusp_lon)} {m:02d}′', 15, fill=pal['ink'], anchor='start'))
            if label:
                svg.append(text(col + 190, yy, label, 15, 'bold', pal['axis'][i], anchor='start'))
        y += 6 * 32 + 20
        sect_line = f"Sect: {_esc_text(chart_data['sect'])}"
        if chronocrats:
            sect_line += (f" · Lord of the day: {_esc_text(chronocrats.get('Day Lord', ''))}"
                          f" · Lord of the hour: {_esc_text(chronocrats.get('Hour Lord', ''))}")
        svg.append(text(x0, y, sect_line, 15, fill=pal['ink'], anchor='start'))

    svg.append('</svg>')
    return ''.join(svg)


# ---- Multi-ring wheels and direction strips for the Timing page (2026-09-10) ----
# Built from the book's own figures (UI_CHART_INPUT_EVALUATION_2026-09-10.md
# §6). The revolution over the root is PN IV I.6 (Figure 51, fn 33); the
# month over the year and the root is IX.3, 4-6 (Figures 39 and 109, fn 58);
# the profection is Figures 3, 15 and 33. What Dykes' wheels carry, read
# from the images: the outer charts in whole signs, the sign of the year
# shaded, the profection as a dashed arc from the natal Ascendant, an
# Egyptian-bounds ring, and "only the seven traditional planets, the Nodes,
# Lot of Fortune, and axial degrees" (p. 12) unless more is asked for. On
# the order: "Abu Ma'shar seems to prefer that the SR be the inner wheel,
# but to me this seem unnatural and I only do it to illustrate his
# instructions in Ch. I.6" (p. 12) -- the caller chooses; the page offers
# both. Pure functions, no Streamlit, SVG strings out; every point carries
# data- attributes so a test can hold the picture to the inventory table.

_M_RIM, _M_SIGN_IN, _M_BOUNDS_IN, _M_HUB_RING, _M_HUB = 478, 430, 408, 150, 118
WHEEL_RING_COLOURS = ('#000000', '#1f3a93', '#8b1a1a')     # inner, middle, outer ring text
WHEEL_SHADE = '#e2e2e2'                                    # the sign of the year (fn 33)
WHEEL_BOUND_TINT = '#ffe08a'                               # the bound the distribution stands in
WHEEL_ORDER_OPTIONS = ("Nativity inside (Dykes)", "Revolution inside (Abu Ma'shar's order, PN IV I.6)")
WHEEL_VIEW_OPTIONS = ("Year", "Year over root", "Month over year and root", "Month", "Profection")
_ASPECT_GLYPH = {'body': '☌', 'sextile': '⚹', 'square': '□', 'trine': '△', 'opposition': '☍'}


def _wheel_geometry(asc_sign, cx=500.0, cy=500.0):
    """ang/xy/sector for a wheel whose rising SIGN's boundary sits at
    9 o'clock (D1), the zodiac running counter-clockwise."""
    def ang(longitude):
        return (longitude - asc_sign * 30 + 180.0) % 360.0

    def xy(r, a):
        t = math.radians(a)
        return cx + r * math.cos(t), cy - r * math.sin(t)

    def sector(r_in, r_out, a0, a1):
        x0o, y0o = xy(r_out, a0); x1o, y1o = xy(r_out, a1)
        x0i, y0i = xy(r_in, a0); x1i, y1i = xy(r_in, a1)
        large = 1 if (a1 - a0) % 360 > 180 else 0
        return (f'M{x0o:.1f},{y0o:.1f} A{r_out},{r_out} 0 {large} 0 {x1o:.1f},{y1o:.1f} '
                f'L{x1i:.1f},{y1i:.1f} A{r_in},{r_in} 0 {large} 1 {x0i:.1f},{y0i:.1f} Z')

    def arc(r, a0, a1):
        """An open arc from bearing a0 to a1 the zodiacal way round."""
        x0, y0 = xy(r, a0); x1, y1 = xy(r, a1)
        large = 1 if (a1 - a0) % 360 > 180 else 0
        return f'M{x0:.1f},{y0:.1f} A{r},{r} 0 {large} 0 {x1:.1f},{y1:.1f}'

    return ang, xy, sector, arc


def _svg_line(x0, y0, x1, y1, stroke, width, dash=None):
    d = f' stroke-dasharray="{dash}"' if dash else ''
    return f'<line x1="{x0:.1f}" y1="{y0:.1f}" x2="{x1:.1f}" y2="{y1:.1f}" stroke="{stroke}" stroke-width="{width}"{d}/>'


def _svg_text(x, y, s, px, weight='normal', fill='#000', anchor='middle', rotate=None, cls=None):
    rot = f' transform="rotate({rotate:.1f} {x:.1f} {y:.1f})"' if rotate is not None else ''
    c = f' class="{cls}"' if cls else ''
    return (f'<text{c} x="{x:.1f}" y="{y:.1f}" text-anchor="{anchor}" dominant-baseline="central" '
            f'font-size="{px}" font-weight="{weight}" fill="{fill}"{rot}>{s}</text>')


def _svg_arrowhead(xy, r, a, colour, size=9, forward=True):
    """A small filled triangle at bearing a on radius r, pointing the
    zodiacal way (forward) along the circle."""
    tip_a = a + (1.2 if forward else -1.2) * (size * 0.5) * 57.2958 / r
    tx, ty = xy(r, tip_a)
    bx1, by1 = xy(r + size * 0.6, a)
    bx2, by2 = xy(r - size * 0.6, a)
    return f'<polygon points="{tx:.1f},{ty:.1f} {bx1:.1f},{by1:.1f} {bx2:.1f},{by2:.1f}" fill="{colour}"/>'


def _bounds_ring_svg(ang, xy, sector, r_in, r_out, highlight=None, glyph_px=12, pal=None):
    """The Egyptian bounds as a ring, a cell per bound with its lord's
    glyph, as every PN IV wheel carries (Figures 1, 22, 25, 26, 109).
    `highlight` is a longitude whose bound is tinted -- the bound the
    distribution stands in. Every cell is class="bound"."""
    pal = pal or _PALETTE_LIGHT
    out = []
    for i, sign in enumerate(SIGN_ORDER):
        start = 0
        for limit, lord in EGYPTIAN_TERMS[sign]:
            a0, a1 = ang(i * 30 + start), ang(i * 30 + limit)
            tint = pal['panel']
            if highlight is not None and int((highlight % 360.0) // 30) == i and start <= (highlight % 30.0) < limit:
                tint = pal['bound_tint']
            out.append(f'<path class="bound" d="{sector(r_in, r_out, a0, a1)}" fill="{tint}" '
                       f'stroke="{pal["rule"]}" stroke-width="0.6"/>')
            mx, my = xy((r_in + r_out) / 2.0, ang(i * 30 + (start + limit) / 2.0))
            out.append(_svg_text(mx, my, POINT_GLYPHS[lord] + _VS, glyph_px, fill=pal['note'], cls='bound-lord'))
            start = limit
    return out


def _wheel_points(chart_data):
    """The points a wheel draws by default (p. 12): the seven planets, the
    nodes, the Lot of Fortune. (name, longitude, speed-or-None)."""
    p_data = chart_data['planetary_data']
    node = p_data['North Node']
    points = [(name, d['longitude'], d.get('speed_in_lon', 0.0)) for name, d in p_data.items()]
    points.append(('South Node', (node['longitude'] + 180.0) % 360.0, node.get('speed_in_lon', 0.0)))
    points.append(('Lot of Fortune', chart_data['lot_of_fortune'], None))
    return points


def _ring_layout(n, bounds):
    """(r_in, r_out) for n chart rings, inner to outer, between the hub
    ring and the bounds ring (or the degree scale)."""
    top = _M_BOUNDS_IN - 4 if bounds else _M_SIGN_IN - 4
    bottom = _M_HUB_RING
    if n == 1:
        return [(bottom, top)]
    if n == 2:
        split = bottom + (top - bottom) * 0.58
        return [(bottom, split), (split, top)]
    a = bottom + (top - bottom) * 0.40
    b = a + (top - bottom) * 0.30
    return [(bottom, a), (a, b), (b, top)]


def _draw_ring_points(svg, ang, xy, ring_idx, label, chart_data, r_in, r_out, colour, badges=None, pal=None):
    """One chart's default points in its annulus: a tick at the true
    degree on the ring's outer edge, a hairline to a radial stack (glyph,
    degree, sign, minute, retrograde mark, badge letters), spread apart
    as the natal wheel spreads them. Each point is a <g class="pt"> with
    data-ring/data-chart/data-point/data-lon."""
    # The stack -- glyph, degree, sign, minute, retrograde, badge -- fits the
    # annulus: sizes scale with its width (capped at the natal wheel's),
    # the lines are laid out between the ring's edges, and the stagger
    # for crowded runs is used only where the ring is wide enough for it.
    pal = pal or _PALETTE_LIGHT
    width = r_out - r_in
    scale = max(0.42, min(1.0, width / 150.0))
    px_glyph, px_deg, px_sign, px_min, px_rx = (round(34 * scale), round(18 * scale), round(19 * scale),
                                                 round(15 * scale), round(14 * scale))
    r_glyph = r_out - 12 - px_glyph * 0.55
    span = min(r_glyph - (r_in + 8), 118.0 * scale + 12.0)
    step = span / 4.6
    r_deg, r_sign, r_min, r_rx = r_glyph - step, r_glyph - 2.0 * step, r_glyph - 2.9 * step, r_glyph - 3.7 * step
    r_badge = r_glyph - 4.6 * step
    points = sorted(_wheel_points(chart_data), key=lambda p: ang(p[1]))
    true_b = [ang(p[1]) for p in points]
    min_sep = max(4.0, (px_glyph + 6) * 57.2958 / r_glyph)
    shown = _spread_labels(true_b, min_sep)
    stagger = round(min(30.0 * scale, max(0.0, width - span - 22.0)))
    offsets = _stagger_offsets(true_b, shown, step=stagger) if stagger >= 8 else [0] * len(points)
    for (name, lon_val, speed), a_true, a_shown, off in zip(points, true_b, shown, offsets):
        d, m = _wheel_dm(lon_val)
        svg.append(f'<g class="pt" data-ring="{ring_idx}" data-chart="{_esc_attr(label)}" data-point="{_esc_attr(name)}" '
                   f'data-lon="{lon_val % 360.0:.6f}">')
        x0, y0 = xy(r_out, a_true); x1, y1 = xy(r_out - 8, a_true)
        svg.append(_svg_line(x0, y0, x1, y1, colour, 1.4))
        x2, y2 = xy(r_glyph - off + px_glyph * 0.6, a_shown)
        svg.append(_svg_line(x1, y1, x2, y2, pal['leader_ring'], 0.7))
        gx, gy = xy(r_glyph - off, a_shown); svg.append(_svg_text(gx, gy, POINT_GLYPHS.get(name, name[:2]) + _VS, px_glyph, fill=colour))
        dx_, dy_ = xy(r_deg - off, a_shown); svg.append(_svg_text(dx_, dy_, f'{d:02d}°', px_deg, 'bold', colour))
        sx, sy = xy(r_sign - off, a_shown); svg.append(_svg_text(sx, sy, SIGN_GLYPHS[int((lon_val % 360.0) // 30)] + _VS, px_sign, fill=colour))
        mx, my = xy(r_min - off, a_shown); svg.append(_svg_text(mx, my, f"{m:02d}'", px_min, fill=colour))
        if speed is not None and speed < 0:
            rx, ry = xy(r_rx - off, a_shown); svg.append(_svg_text(rx, ry, '℞' + _VS, px_rx, fill=colour))
        if badges and badges.get(name):
            bx, by = xy(r_badge - off, a_shown)
            svg.append(_svg_text(bx, by, _esc_text(badges[name]), px_rx, 'bold', pal['badge'], cls='badge'))
        svg.append('</g>')


def _draw_ring_extras(svg, ang, xy, ring_idx, label, extras, r_out, colour, pal=None):
    """Optional points (Lots, rays, twelfth-parts): a short tick at the
    ring's outer edge and a tiny label, spread thinly. <g class="extra">."""
    pal = pal or _PALETTE_LIGHT
    if not extras:
        return
    items = sorted(extras, key=lambda e: ang(e[1]))
    true_b = [ang(e[1]) for e in items]
    shown = _spread_labels(true_b, 2.2)
    for item, a_true, a_shown in zip(items, true_b, shown):
        name, lon_val = item[0], item[1]
        short = item[2] if len(item) > 2 else (name if len(name) <= 6 else name[:6])
        svg.append(f'<g class="extra" data-ring="{ring_idx}" data-chart="{_esc_attr(label)}" '
                   f'data-point="{_esc_attr(name)}" data-lon="{lon_val % 360.0:.6f}">')
        x0, y0 = xy(r_out, a_true); x1, y1 = xy(r_out - 5, a_true)
        svg.append(_svg_line(x0, y0, x1, y1, colour, 0.8))
        tx, ty = xy(r_out - 12, a_shown)
        svg.append(_svg_text(tx, ty, _esc_text(short) + _VS, 7, fill=colour, rotate=-(a_shown - 90.0) % 360.0 - 90.0))
        svg.append('</g>')


def generate_multiwheel_svg(rings, chart_name, wide=False, bounds=True, shade_sign=None, shade_label='Sign of year',
                            outline_sign=None, outline_label='Sign of month', profection_from=None,
                            distribution=None, marks=(), badges=None, extras=None, hub_lines=(), theme=None):
    """One to three charts on one zodiac. `rings` are dicts inner to outer:
    {'label', 'chart' (a chart_data), 'when' (a short line for the hub)}.
    shade_sign / outline_sign: sign indices 0-11 (the sign of the year,
    the sign of the month). profection_from: a longitude whose sign the
    dashed arc starts from, ending at shade_sign (Figures 3, 33).
    distribution: {'start': lon, 'end': lon} draws the directed
    Ascendant's arc and tints the bound of `end` (Figures 2, 65). marks:
    (label, lon, ring_idx) degrees ticked on a ring's outer edge. badges:
    {ring_idx: {planet: letters}}. extras: {ring_idx: [(name, lon)]}."""
    pal = _wheel_palette(theme)
    rings = list(rings)
    assert 1 <= len(rings) <= 3
    size = WHEEL_SIZE
    cx = cy = size / 2.0
    inner = rings[0]['chart']
    asc_sign = int((inner['ascendant'] % 360.0) // 30)
    ang, xy, sector, arc = _wheel_geometry(asc_sign, cx, cy)
    width = WHEEL_WIDE_WIDTH if wide else size
    svg = [f'<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 {width} {size}" width="{width}" height="{size}" '
           f'style="font-family:{_WHEEL_FONT}">',
           f'<style>{font_face_css()}</style>',
           f'<rect width="{width}" height="{size}" fill="{pal["bg"]}"/>']
    layout = _ring_layout(len(rings), bounds)
    r_top = layout[-1][1]

    # 0. The shaded sign of the year, under everything (fn 33).
    if shade_sign is not None:
        a0, a1 = ang(shade_sign * 30), ang(shade_sign * 30 + 30)
        svg.append(f'<path class="shade" data-sign="{shade_sign}" d="{sector(_M_HUB, _M_RIM, a0, a1)}" fill="{pal["shade"]}"/>')
    # 1. Sign band, degree scale, spokes through every ring (whole signs are
    #    every chart's houses here, as Dykes draws them).
    for i in range(12):
        a0, a1 = ang(i * 30), ang(i * 30 + 30)
        fill = pal['tints'][i % 4] if shade_sign != i else pal['shade']
        svg.append(f'<path d="{sector(_M_SIGN_IN, _M_RIM, a0, a1)}" fill="{fill}"/>')
        gx, gy = xy((_M_SIGN_IN + _M_RIM) / 2.0 + 6, ang(i * 30 + 15))
        svg.append(_svg_text(gx, gy, SIGN_GLYPHS[i] + _VS, 26, fill=pal['ink']))
        x0, y0 = xy(_M_HUB, a0); x1, y1 = xy(_M_RIM, a0)
        svg.append(_svg_line(x0, y0, x1, y1, pal['rule'], 1.0))
    for d in range(360):
        ln = 12 if d % 10 == 0 else 8 if d % 5 == 0 else 4
        x0, y0 = xy(_M_SIGN_IN, ang(d)); x1, y1 = xy(_M_SIGN_IN + ln, ang(d))
        svg.append(_svg_line(x0, y0, x1, y1, pal['rule'], 0.8 if ln > 4 else 0.45))
    if bounds:
        svg.extend(_bounds_ring_svg(ang, xy, sector, _M_BOUNDS_IN, _M_SIGN_IN,
                                    highlight=(distribution or {}).get('end'), pal=pal))
    for r, w in ((_M_RIM, 2.2), (_M_SIGN_IN, 1.4), (_M_HUB_RING, 1.0), (_M_HUB, 1.4)):
        svg.append(f'<circle cx="{cx}" cy="{cy}" r="{r}" fill="none" stroke="{pal["rule"]}" stroke-width="{w}"/>')
    if outline_sign is not None:
        a0, a1 = ang(outline_sign * 30), ang(outline_sign * 30 + 30)
        svg.append(f'<path class="outline" data-sign="{outline_sign}" d="{sector(_M_HUB, _M_RIM, a0, a1)}" '
                   f'fill="none" stroke="{pal["rule"]}" stroke-width="2.2" stroke-dasharray="7 5"/>')

    # 2. The rings, inner to outer: separator, whole-sign numbers of that
    #    chart, its angles across its own annulus, its points.
    for idx, (ring, (r_in, r_out)) in enumerate(zip(rings, layout)):
        chart = ring['chart']
        colour = pal['rings'][idx if len(rings) > 1 else 0]
        if idx > 0:
            svg.append(f'<circle cx="{cx}" cy="{cy}" r="{r_in}" fill="none" stroke="{pal["rule"]}" stroke-width="1.0"/>')
        c_asc_sign = int((chart['ascendant'] % 360.0) // 30)
        num_r = (_M_HUB + _M_HUB_RING) / 2.0 if idx == 0 else r_out - 9
        for i in range(12):
            nx, ny = xy(num_r, ang(i * 30 + 15 if idx > 0 else i * 30 + 15))
            svg.append(_svg_text(nx, ny, str((i - c_asc_sign) % 12 + 1), 15 if idx == 0 else 10,
                                 'bold', colour, cls='house'))
        a_lo = _M_HUB if idx == 0 else r_in
        for lon_val, col, lab in ((chart['ascendant'], pal['horizon'], 'As'), (chart['mc'], pal['meridian'], 'Mc'),
                                  ((chart['ascendant'] + 180.0) % 360.0, pal['horizon'], 'Ds'),
                                  ((chart['mc'] + 180.0) % 360.0, pal['meridian'], 'Ic')):
            x0, y0 = xy(a_lo, ang(lon_val)); x1, y1 = xy(r_out, ang(lon_val))
            svg.append(_svg_line(x0, y0, x1, y1, col, 2.0 if idx == 0 else 1.5))
            lx, ly = xy(r_out - 9, ang(lon_val) + 2.5 * 57.2958 / r_out * 3)
            svg.append(_svg_text(lx, ly, lab, 9, 'bold', col, cls='angle'))
        _draw_ring_points(svg, ang, xy, idx, ring['label'], chart, r_in, r_out, colour,
                          badges=(badges or {}).get(idx), pal=pal)
        _draw_ring_extras(svg, ang, xy, idx, ring['label'], (extras or {}).get(idx), r_out, colour, pal=pal)

    # 3. Marks: named degrees on a ring's outer edge (the terminal point ...).
    for lab, lon_val, ring_idx in marks:
        r_in, r_out = layout[min(ring_idx, len(layout) - 1)]
        a = ang(lon_val)
        x0, y0 = xy(r_out, a); x1, y1 = xy(r_out - 14, a)
        svg.append(f'<g class="mark" data-point="{_esc_attr(lab)}" data-lon="{lon_val % 360.0:.6f}">')
        svg.append(_svg_line(x0, y0, x1, y1, pal['badge'], 2.2))
        tx, ty = xy(r_out - 22, a)
        svg.append(_svg_text(tx, ty, _esc_text(lab), 9, 'bold', pal['badge']))
        svg.append('</g>')

    # 4. The profection: a dashed arc outside the rim from the natal
    #    Ascendant's sign to the sign of the year, arrowhead at the end.
    if profection_from is not None and shade_sign is not None:
        s0 = int((profection_from % 360.0) // 30)
        if s0 != shade_sign:
            a0, a1 = ang(s0 * 30 + 15), ang(shade_sign * 30 + 15)
            svg.append(f'<path class="profection" d="{arc(_M_RIM + 11, a0, a1)}" fill="none" stroke="{pal["rule"]}" '
                       f'stroke-width="2.4" stroke-dasharray="9 6"/>')
            svg.append(_svg_arrowhead(xy, _M_RIM + 11, a1, pal['rule']))
    # Sign labels run along the arc in the sign band's inner margin,
    # upright on either half of the wheel, as Dykes letters "Sign of year".
    def _tangential(r, lon_val, s, cls):
        a = ang(lon_val)
        rot = (90.0 - a) % 360.0
        if 180.0 < a % 360.0 < 360.0:
            rot += 180.0
        x, y = xy(r, a)
        return _svg_text(x, y, _esc_text(s), 10, 'bold', pal['label'], rotate=rot, cls=cls)
    if shade_sign is not None:
        svg.append(_tangential(_M_SIGN_IN + 15, shade_sign * 30 + 15, shade_label, 'shade-label'))
    if outline_sign is not None:
        svg.append(_tangential(_M_SIGN_IN + 15 if outline_sign != shade_sign else _M_SIGN_IN + 26,
                               outline_sign * 30 + 15, outline_label, 'outline-label'))

    # 5. The distribution: the directed Ascendant from its degree to the
    #    degree reached now, solid, on the inner edge of the bounds ring.
    if distribution:
        r_arc = r_top + 1
        a0, a1 = ang(distribution['start']), ang(distribution['end'])
        svg.append(f'<path class="distribution" data-start="{distribution["start"] % 360.0:.6f}" '
                   f'data-end="{distribution["end"] % 360.0:.6f}" d="{arc(r_arc, a0, a1)}" fill="none" '
                   f'stroke="{pal["horizon"]}" stroke-width="2.6"/>')
        svg.append(_svg_arrowhead(xy, r_arc, a1, pal['horizon'], size=11))

    # 6. Hub: the name, then one line per ring, inner first.
    svg.append(f'<circle cx="{cx}" cy="{cy}" r="{_M_HUB - 1}" fill="{pal["panel"]}"/>')
    name = str(chart_name)
    if len(name) > 30:
        name = name[:29] + '…'
    lines = [(_esc_text(name), 15 if len(name) <= 18 else 12, 'bold', pal['rings'][0])]
    for idx, ring in enumerate(rings):
        colour = pal['rings'][idx if len(rings) > 1 else 0]
        place = ('Inner', 'Middle', 'Outer')[idx] if len(rings) == 3 else ('Inner', 'Outer')[idx] if len(rings) == 2 else ''
        head = f"{place}: {ring['label']}" if place else ring['label']
        lines.append((_esc_text(head), 11, 'bold', colour))
        if ring.get('when'):
            lines.append((_esc_text(ring['when']), 9, 'normal', colour))
    for s in hub_lines:
        lines.append((_esc_text(s), 9, 'normal', pal['note']))
    y = cy - 6.5 * (len(lines) - 1)
    for s, px, w, col in lines:
        svg.append(_svg_text(cx, y, s, px, w, col))
        y += 13

    # 7. Wide: a positions column per ring.
    if wide:
        col_w = (width - size - 60) / len(rings)
        for idx, ring in enumerate(rings):
            colour = pal['rings'][idx if len(rings) > 1 else 0]
            x0 = size + 40 + idx * col_w
            svg.append(_svg_text(x0, 60, _esc_text(ring['label']), 18, 'bold', colour, anchor='start'))
            if ring.get('when'):
                svg.append(_svg_text(x0, 84, _esc_text(ring['when']), 11, fill=colour, anchor='start'))
            svg.append(_svg_line(x0, 98, x0 + col_w - 30, 98, pal['rule'], 1))
            chart = ring['chart']
            y = 122
            pts = sorted(_wheel_points(chart), key=lambda p: list(POINT_GLYPHS).index(p[0]) if p[0] in POINT_GLYPHS else 99)
            for name_, lon_val, speed in pts:
                d, m = _wheel_dm(lon_val)
                motion = '' if speed is None else ('℞' + _VS if speed < 0 else '')
                svg.append(_svg_text(x0, y, POINT_GLYPHS.get(name_, '') + _VS, 18, fill=colour, anchor='start'))
                svg.append(_svg_text(x0 + 30, y, f'{d:02d}° {SIGN_GLYPHS[int((lon_val % 360.0) // 30)]}{_VS} {m:02d}′ {motion}',
                                     14, fill=colour, anchor='start'))
                svg.append(_svg_text(x0 + col_w - 50, y, str(get_wsh_house(lon_val, chart['ascendant'])), 13, fill=colour))
                y += 28
            y += 10
            for lon_val, lab in ((chart['ascendant'], 'Asc'), (chart['mc'], 'MC')):
                d, m = _wheel_dm(lon_val)
                svg.append(_svg_text(x0, y, lab, 13, 'bold', colour, anchor='start'))
                svg.append(_svg_text(x0 + 30, y, f'{d:02d}° {SIGN_GLYPHS[int((lon_val % 360.0) // 30)]}{_VS} {m:02d}′',
                                     14, fill=colour, anchor='start'))
                y += 26
            svg.append(_svg_text(x0, y, f"Sect: {_esc_text(chart['sect'])}", 12, fill=colour, anchor='start'))

    svg.append('</svg>')
    return ''.join(svg)


# ---- Direction strips --------------------------------------------------------
# The four distributions as timelines (evaluation B.4): one bar per bound
# tinted by its distributor, the distributor's glyph in it, a full-height
# line where the bound changes and a half tick where only the partner
# does, and the present as a red line. PN IV prints distributions as a
# table with dates (Figure 22) and as an arc on the wheel (Figures 2, 65);
# nothing in the book replaces a timeline, and a student needs one.
# 1200 x 240 (was 1600 x 170, 2026-09-22): the page shows a strip in a
# column about 550 px wide, so a 1600-wide canvas drew its glyphs at a
# third of their size; the narrower canvas with taller bars puts a
# distributor's glyph at about 12 px on screen instead of 7.
STRIP_WIDTH, STRIP_HEIGHT = 1200, 240
STRIP_TINT = {'Saturn': '#d9d9d9', 'Jupiter': '#cfe0f5', 'Mars': '#f5cdc7', 'Sun': '#fbe9a6',
              'Venus': '#d4efd0', 'Mercury': '#f8ddb8', 'Moon': '#e2d9f3'}


# ---- The two palettes ---------------------------------------------------
# Every picture used to be drawn on an opaque white rectangle with black
# ink, so a reader in the dark theme got a white square on a dark page.
# The renderers take theme=: None is the palette above, exactly as it has
# always been drawn, and 'dark' is the same picture on Streamlit's own
# dark ground. One dict per theme, read once at the top of a renderer; the
# geometry does not know which it is drawing in.
_PALETTE_LIGHT = {
    'bg': '#ffffff',            # the picture's ground
    'panel': '#ffffff',         # opaque fills over it: the rim band, the angle boxes, the hub, a bound's cell
    'ink': '#000',              # text, where _svg_text's own default was taken
    'rule': '#000000',          # circles, spokes, the degree scale, a strip's axis
    'leader': '#666666',        # the hairline from a point's tick to its glyph (the natal wheel)
    'leader_ring': '#777777',   # the same on a revolution ring
    'label': '#444444',         # the wide panel's column heads, the sign-of-year lettering
    'note': '#333333',          # a bound's lord, the hub's added lines, a strip's partner
    'axis_note': '#555555',     # the strip's axis caption
    'tints': TRIPLICITY_TINT,   # the sign band by triplicity
    'shade': WHEEL_SHADE,
    'bound_tint': WHEEL_BOUND_TINT,
    'axis': _AXIS_COLOUR,       # by cusp index
    'horizon': '#0000cc',       # Ascendant and Descendant, and the distribution's arc
    'meridian': '#1e7b1e',      # MC and IC
    'cusp': _CUSP_COLOUR,
    'badge': '#b8860b',
    'rings': WHEEL_RING_COLOURS,
    'now': '#c00000',
    'strip_tint': STRIP_TINT,
    'strip_other': '#eeeeee',
}
# The dark ground is Streamlit's own default (#0e1117): st.context.theme
# reports the theme's type and not its colours, so the value is taken from
# the framework's default rather than guessed. The semantic colours move
# only as far as a dark ground requires -- the horizon stays blue, the
# meridian green, the cusps red, the badge gold, the sign band still four
# steps apart, each at the distance from the ground its light counterpart
# keeps from white.
_PALETTE_DARK = {
    'bg': '#0e1117',
    'panel': '#0e1117',
    'ink': '#fafafa',
    'rule': '#d2d6de',
    'leader': '#8b919b',
    'leader_ring': '#9aa1ac',
    'label': '#b4b9c2',
    'note': '#c2c7d0',
    'axis_note': '#a8adb6',
    'tints': ('#161a22', '#2f3440', '#202530', '#40485a'),
    'shade': '#2b313d',
    'bound_tint': '#584717',
    'axis': {0: '#7aa7ff', 6: '#7aa7ff', 9: '#5fcf5f', 3: '#5fcf5f'},
    'horizon': '#7aa7ff',
    'meridian': '#5fcf5f',
    'cusp': '#e0736f',
    'badge': '#e3b341',
    'rings': ('#fafafa', '#8ab4ff', '#f08a80'),
    'now': '#ff5f56',
    'strip_tint': {'Saturn': '#3a3a3a', 'Jupiter': '#25384f', 'Mars': '#4d2e2a', 'Sun': '#4d431c',
                   'Venus': '#26402a', 'Mercury': '#4a3a23', 'Moon': '#37304a'},
    'strip_other': '#2a2a2a',
}


def _wheel_palette(theme=None):
    """The colours a picture is drawn in. None -- and anything the viewer's
    browser does not report as dark -- is the palette the app has always
    used."""
    return _PALETTE_DARK if str(theme).lower() == 'dark' else _PALETTE_LIGHT


def _esc_attr(s):
    """A value on its way into an SVG attribute. Double quotes included: a
    Lot name carrying one closed the attribute and broke a whole wheel."""
    return html.escape(str(s), quote=True)


def _esc_text(s):
    """A value on its way into SVG text content."""
    return html.escape(str(s))


def generate_distribution_strip_svg(segments, now, unit='years', span=None, title='', theme=None):
    """segments as _pn4_distribute returns them (from/to/distributor/partner/
    partner_aspect); `now` in the strip's unit (completed years, or the day
    of the year) or None; span the bar's full length (120 years, 365 days
    ...). Each bar is <rect class="seg"> with data-from/to/distributor/
    partner; the present is <line class="now">. The segment the present
    falls in is outlined in the present's colour; a bound change is a
    full-height rule, a partner change a tick at the top; a bar too
    narrow for its glyph is left to its tint alone (2026-09-22)."""
    pal = _wheel_palette(theme)
    if unit not in ('years', 'days'):
        raise ValueError(f"unit must be 'years' or 'days', not {unit!r}")
    segments = list(segments or [])
    if span is None:
        span = max((s['to'] for s in segments), default=1.0)
    span = float(span) or 1.0
    x_left, x_right = 40.0, STRIP_WIDTH - 40.0
    y_top, y_bot = 78.0, 172.0

    def x_of(v):
        return x_left + (x_right - x_left) * max(0.0, min(1.0, v / span))

    svg = [f'<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 {STRIP_WIDTH} {STRIP_HEIGHT}" width="{STRIP_WIDTH}" '
           f'height="{STRIP_HEIGHT}" style="font-family:{_WHEEL_FONT}">',
           f'<style>{font_face_css()}</style>',
           f'<rect width="{STRIP_WIDTH}" height="{STRIP_HEIGHT}" fill="{pal["bg"]}"/>']
    if title:
        svg.append(_svg_text(x_left, 30, _esc_text(title), 22, 'bold', pal['ink'], anchor='start'))
    current = None
    if now is not None:
        current = next((seg for seg in segments if seg['from'] <= now < seg['to']), None)
    rules, prev = [], None
    for seg in segments:
        x0, x1 = x_of(seg['from']), x_of(seg['to'])
        w = max(0.0, x1 - x0)
        tint = pal['strip_tint'].get(seg['distributor'], pal['strip_other'])
        svg.append(f'<rect class="seg" x="{x0:.1f}" y="{y_top}" width="{w:.1f}" height="{y_bot - y_top}" fill="{tint}" '
                   f'data-from="{seg["from"]:.4f}" data-to="{seg["to"]:.4f}" '
                   f'data-distributor="{_esc_attr(seg["distributor"])}" data-partner="{_esc_attr(pn4_result_text(seg["partner"], "None"))}" '
                   f'data-aspect="{_esc_attr(pn4_result_text(seg["partner_aspect"], "None"))}"/>')
        bound_change = prev is None or prev['distributor'] != seg['distributor']
        # The rules go on after the bars so no bar's fill covers a neighbour's edge.
        rules.append(_svg_line(x0, y_top, x0, y_bot if bound_change else y_top + 18,
                               pal['rule'], 1.6 if bound_change else 1.0))
        if w >= 18:
            svg.append(_svg_text((x0 + x1) / 2.0, y_top + 32, POINT_GLYPHS.get(seg['distributor'], '') + _VS, 30,
                                 fill=pal['ink']))
        if w >= 26 and is_unresolved(seg['partner']):
            svg.append(_svg_text((x0 + x1) / 2.0, y_bot - 22, '?', 18, fill=pal['note']))
        elif w >= 26 and seg['partner']:
            svg.append(_svg_text((x0 + x1) / 2.0, y_bot - 22,
                                 _ASPECT_GLYPH.get(seg['partner_aspect'], '') + POINT_GLYPHS.get(seg['partner'], '') + _VS,
                                 19, fill=pal['note']))
        prev = seg
    svg.extend(rules)
    if current is not None:
        x0, x1 = x_of(current['from']), x_of(current['to'])
        svg.append(f'<rect class="current" x="{x0:.1f}" y="{y_top - 1.5}" width="{max(0.0, x1 - x0):.1f}" '
                   f'height="{y_bot - y_top + 3}" fill="none" stroke="{pal["now"]}" stroke-width="3"/>')
    svg.append(_svg_line(x_left, y_bot, x_right, y_bot, pal['rule'], 1.4))
    tick = 10.0 if unit == 'years' else 30.0
    v = 0.0
    while v <= span + 1e-9:
        x = x_of(v)
        svg.append(_svg_line(x, y_bot, x, y_bot + 8, pal['rule'], 1.0))
        svg.append(_svg_text(x, y_bot + 24, f'{v:g}', 15, fill=pal['ink']))
        v += tick
    svg.append(_svg_text(x_right, y_bot + 50, 'age in completed years' if unit == 'years' else 'day of the year',
                         14, fill=pal['axis_note'], anchor='end'))
    if now is not None and 0.0 <= now <= span:
        x = x_of(now)
        svg.append(f'<line class="now" x1="{x:.1f}" y1="{y_top - 20}" x2="{x:.1f}" y2="{y_bot + 8}" stroke="{pal["now"]}" '
                   f'stroke-width="2.6" data-now="{now:.4f}"/>')
        # The label beside the line, not on it; on whichever side has room.
        label = ('now: age ' if unit == 'years' else 'now: day ') + f'{now:.1f}'
        at_right = x > (x_left + x_right) / 2.0
        svg.append(_svg_text(x - 8 if at_right else x + 8, y_top - 30, label, 15, 'bold', pal['now'],
                             anchor='end' if at_right else 'start'))
    svg.append('</svg>')
    return ''.join(svg)


def generate_hit_strip_svg(rows, now, span=None, title='', theme=None):
    """The house-master's direction (Sahl, On Nativities 1.23, 2) as a
    strip in the family of generate_distribution_strip_svg: the same
    0-to-span axis in completed years, one tick per target reached
    (<line class="hit"> with data-arc/data-target), labelled with the
    infortune's glyph and the aspect's, alternating above and below the
    axis so neighbours do not collide; the present as <line class="now">.
    `rows` are sahl_house_master_direction's rows (Target, Arc (years))."""
    pal = _wheel_palette(theme)
    rows = list(rows or [])
    if span is None:
        span = max((float(r['Arc (years)']) for r in rows), default=1.0)
    span = float(span) or 1.0
    x_left, x_right = 40.0, STRIP_WIDTH - 40.0
    y_axis = 128.0

    def x_of(v):
        return x_left + (x_right - x_left) * max(0.0, min(1.0, v / span))

    def label_of(target):
        m = re.match(r"(?:the )?(\w+)'s (body|opposition|square|degree)", target)
        if not m:
            return _esc_text(target)
        planet, what = m.group(1), m.group(2)
        glyph = POINT_GLYPHS.get(planet, planet)
        return (glyph + _VS) if what == 'degree' else (_ASPECT_GLYPH.get(what, '') + glyph + _VS)

    svg = [f'<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 {STRIP_WIDTH} {STRIP_HEIGHT}" width="{STRIP_WIDTH}" '
           f'height="{STRIP_HEIGHT}" style="font-family:{_WHEEL_FONT}">',
           f'<style>{font_face_css()}</style>',
           f'<rect width="{STRIP_WIDTH}" height="{STRIP_HEIGHT}" fill="{pal["bg"]}"/>']
    if title:
        svg.append(_svg_text(x_left, 30, _esc_text(title), 22, 'bold', pal['ink'], anchor='start'))
    svg.append(_svg_line(x_left, y_axis, x_right, y_axis, pal['rule'], 1.4))
    v = 0.0
    while v <= span + 1e-9:
        x = x_of(v)
        svg.append(_svg_line(x, y_axis, x, y_axis + 8, pal['rule'], 1.0))
        svg.append(_svg_text(x, y_axis + 24, f'{v:g}', 15, fill=pal['ink']))
        v += 10.0
    svg.append(_svg_text(x_right, y_axis + 50, 'age in completed years', 14, fill=pal['axis_note'], anchor='end'))
    for i, r in enumerate(sorted(rows, key=lambda r: float(r['Arc (years)']))):
        arc = float(r['Arc (years)'])
        if arc > span:
            continue
        x = x_of(arc)
        above = i % 2 == 0
        y0, y1 = (y_axis - 34, y_axis) if above else (y_axis, y_axis + 34)
        svg.append(f'<line class="hit" x1="{x:.1f}" y1="{y0}" x2="{x:.1f}" y2="{y1}" stroke="{pal["rule"]}" '
                   f'stroke-width="2" data-arc="{arc:.4f}" data-target="{_esc_attr(r["Target"])}"/>')
        svg.append(f'<circle cx="{x:.1f}" cy="{y_axis}" r="4" fill="{pal["rule"]}"/>')
        svg.append(_svg_text(x, y_axis - 50 if above else y_axis + 54, label_of(str(r['Target'])), 26, fill=pal['ink']))
        svg.append(_svg_text(x, y_axis - 74 if above else y_axis + 78, f'{arc:.1f}', 14, fill=pal['note']))
    if now is not None and 0.0 <= now <= span:
        x = x_of(now)
        svg.append(f'<line class="now" x1="{x:.1f}" y1="{y_axis - 86}" x2="{x:.1f}" y2="{y_axis + 8}" stroke="{pal["now"]}" '
                   f'stroke-width="2.6" data-now="{now:.4f}"/>')
        at_right = x > (x_left + x_right) / 2.0
        svg.append(_svg_text(x - 8 if at_right else x + 8, y_axis - 96, f'now: age {now:.1f}', 15, 'bold', pal['now'],
                             anchor='end' if at_right else 'start'))
    svg.append('</svg>')
    return ''.join(svg)


# ==========================================
# 3. DIGNITY & ASPECT EVALUATORS
# ==========================================

# --- Shared classical dignity reference tables --------------------------
# Pulled out to module level so both evaluate_essential_dignities() and the
# prenatal syzygy engine (which needs to look up dignities at an arbitrary
# degree, not just at a planet's own position) can use the same data.
DOMICILES = {'Sun': ['Leo'], 'Moon': ['Cancer'], 'Mercury': ['Gemini', 'Virgo'], 'Venus': ['Taurus', 'Libra'], 'Mars': ['Aries', 'Scorpio'], 'Jupiter': ['Sagittarius', 'Pisces'], 'Saturn': ['Capricorn', 'Aquarius']}
EXALTATIONS = {'Sun': ['Aries'], 'Moon': ['Taurus'], 'Mercury': ['Virgo'], 'Venus': ['Pisces'], 'Mars': ['Capricorn'], 'Jupiter': ['Cancer'], 'Saturn': ['Libra']}
# The degrees of exaltation in the standard scheme, Gr. Intr. V.5, Figure
# 38 (Hermes's, V.7, Figure 39, differ only for Jupiter and Mercury: the
# 16th degree of Cancer and of Virgo -- checked against the corpus
# 2026-09-12 after the fourth check). DISPLAY ONLY, for the Reference tables page (2026-09-10): nothing
# in this engine reads a degree of exaltation, and nothing may start to
# without a source in hand.
EXALTATION_DEGREES = {'Saturn': 21, 'Jupiter': 15, 'Mars': 28, 'Sun': 19, 'Venus': 27, 'Mercury': 15, 'Moon': 3}
# The natures of the seven planets as Abu Ma'shar reports Ptolemy stating
# them, Gr. Intr. IV.1, 6-12 (his own objections follow at 15-43 and are not
# tabled). 'As stated' is the sentence's words, verbatim from the corpus;
# 'Active' and 'Passive' are the hot/cold and wet/dry read off it, a dash
# where the sentence names neither. DISPLAY ONLY, for the Reference tables
# page at the supplement depth: nothing in this engine reads a planet's
# nature, and nothing may start to without a source in hand.
PLANET_NATURES_IV1 = [
    {'Planet': 'Saturn', 'Active (hot/cold)': 'Cold', 'Passive (wet/dry)': 'Dry',
     'As stated': "the nature of Saturn is the cold and the dry, due to the distance of his sphere from the hotness "
                  "of the Sun, and its distance from the wetness of the earth's vapor",
     'Source': 'IV.1, 8'},
    {'Planet': 'Jupiter', 'Active (hot/cold)': 'Hot (temperate)', 'Passive (wet/dry)': 'Wet (temperate)',
     'As stated': "Jupiter is temperate in mixture, because his sphere is between the spheres of Saturn and Mars, "
                  "and indeed for this reason his nature comes to be temperate hotness and wetness",
     'Source': 'IV.1, 10'},
    {'Planet': 'Mars', 'Active (hot/cold)': 'Hot', 'Passive (wet/dry)': 'Dry',
     'As stated': "the nature of Mars is hotness and the dry, due to his color resembling fire, and his nearness to "
                  "the Sun, and because [the Sun] is under him, so that [the Sun's] heat rises up towards him and warms him",
     'Source': 'IV.1, 9'},
    {'Planet': 'Sun', 'Active (hot/cold)': 'Hot', 'Passive (wet/dry)': 'Dry',
     'As stated': "the Sun warms and dries in a maturing, unhurried way, and [the Sun] is more apparent in that and "
                  "more evident in activity than the rest of the planets due to his might",
     'Source': 'IV.1, 6'},
    {'Planet': 'Venus', 'Active (hot/cold)': 'Hot (temperate)', 'Passive (wet/dry)': 'Wet (temperate)',
     'As stated': "the nature of Venus is temperate heating and moistening: as for her heating, it is due to the "
                  "nearness of her sphere to the Sun",
     'Source': 'IV.1, 11'},
    {'Planet': 'Mercury', 'Active (hot/cold)': '-', 'Passive (wet/dry)': 'Dry or wet by turns',
     'As stated': "the nature of Mercury is sometimes dryness, sometimes wetness: as for his dryness, it is due to "
                  "his nearness to the Sun, and that he is not distant from [the] Sun by a great interval; as for his "
                  "wetness, it is due to the nearness of his sphere to the circle of the Moon",
     'Source': 'IV.1, 12'},
    {'Planet': 'Moon', 'Active (hot/cold)': '-', 'Passive (wet/dry)': 'Wet',
     'As stated': "the nature of the Moon is wetness due to her sphere's nearness to the earth, and her "
                  "receptiveness to the vapors which rise up from it",
     'Source': 'IV.1, 7'},
]
DETRIMENTS = {'Sun': ['Aquarius'], 'Moon': ['Capricorn'], 'Mercury': ['Sagittarius', 'Pisces'], 'Venus': ['Scorpio', 'Aries'], 'Mars': ['Libra', 'Taurus'], 'Jupiter': ['Gemini', 'Virgo'], 'Saturn': ['Cancer', 'Leo']}
FALLS = {'Sun': ['Libra'], 'Moon': ['Scorpio'], 'Mercury': ['Pisces'], 'Venus': ['Virgo'], 'Mars': ['Cancer'], 'Jupiter': ['Capricorn'], 'Saturn': ['Aries']}
# Egyptian bounds (terms), as the course tables give them: TNAC Handy Tables
# from Part 1, p. 1, "Table of Egyptian bounds" (Dykes 2023), read cell by
# cell from a 300-dpi render on 2026-09-07 and pinned sign by sign in
# tests/test_doctrine_fixtures.py. Each entry is (upper limit, lord): a
# degree belongs to the first bound whose limit exceeds it.
#
# Gemini and Aquarius each had two adjacent lords transposed from the
# initial commit until 2026-09-07 -- Gemini 6-17 read Venus then Jupiter,
# Aquarius 0-13 read Venus then Mercury -- so every degree in those 24
# carried the wrong term lord in dignity scoring, the victor grids, the
# prenatal syzygy and distribution. Sahl is the independent witness for
# the correction: On Nativities Ch. 10.2.7, 21-22 has Mercury at Aquarius 5
# "in his own bound", and fn. 198 there gives Capricorn 0-7 to Mercury as
# this table does.
EGYPTIAN_TERMS = {
    'Aries':       [(6, 'Jupiter'), (12, 'Venus'),   (20, 'Mercury'), (25, 'Mars'),    (30, 'Saturn')],
    'Taurus':      [(8, 'Venus'),   (14, 'Mercury'), (22, 'Jupiter'), (27, 'Saturn'),  (30, 'Mars')],
    'Gemini':      [(6, 'Mercury'), (12, 'Jupiter'), (17, 'Venus'),   (24, 'Mars'),    (30, 'Saturn')],
    'Cancer':      [(7, 'Mars'),    (13, 'Venus'),   (19, 'Mercury'), (26, 'Jupiter'), (30, 'Saturn')],
    'Leo':         [(6, 'Jupiter'), (11, 'Venus'),   (18, 'Saturn'),  (24, 'Mercury'), (30, 'Mars')],
    'Virgo':       [(7, 'Mercury'), (17, 'Venus'),   (21, 'Jupiter'), (28, 'Mars'),    (30, 'Saturn')],
    'Libra':       [(6, 'Saturn'),  (14, 'Mercury'), (21, 'Jupiter'), (28, 'Venus'),   (30, 'Mars')],
    'Scorpio':     [(7, 'Mars'),    (11, 'Venus'),   (19, 'Mercury'), (24, 'Jupiter'), (30, 'Saturn')],
    'Sagittarius': [(12, 'Jupiter'), (17, 'Venus'),  (21, 'Mercury'), (26, 'Saturn'),  (30, 'Mars')],
    'Capricorn':   [(7, 'Mercury'), (14, 'Jupiter'), (22, 'Venus'),   (26, 'Saturn'),  (30, 'Mars')],
    'Aquarius':    [(7, 'Mercury'), (13, 'Venus'),   (20, 'Jupiter'), (25, 'Mars'),    (30, 'Saturn')],
    'Pisces':      [(12, 'Venus'),  (16, 'Jupiter'), (19, 'Mercury'), (28, 'Mars'),    (30, 'Saturn')],
}
CHALDEAN_ORDER = ['Mars', 'Sun', 'Venus', 'Mercury', 'Moon', 'Saturn', 'Jupiter']
# The weighted five-fold dignity claim used wherever this file totals
# essential dignities at a degree: a planet's own score, the prenatal
# syzygy's almuten, and the newer victor scheme (al-Qabisi / Abu Ma'shar,
# triplicity above bound). Stated by al-Qabisi I.22 (ITA I.18, p. 81):
# domicile 5, exaltation 4, triplicity 3, bound 2, face 1 (ITA printed p. 81, read against the photograph 2026-09-17).
# The older victor scheme swaps bound and triplicity and is written out at
# VICTOR_WEIGHTS.
ESSENTIAL_DIGNITY_WEIGHTS = {'domicile': 5, 'exaltation': 4, 'triplicity': 3, 'term': 2, 'face': 1}

# Common triplicity data: Sahl, Introduction 1.35-41 / Figure 4;
# al-Qabisi I.16; Valens II.1-2. A named-source lookup below supplies
# the Great Introduction's Virgo exception (V.14, 7 / Figure 53 / fn 100).
TRIPLICITY = {
    'Fire':  {'Day': 'Sun',   'Night': 'Jupiter', 'Participating': 'Saturn'},
    'Earth': {'Day': 'Venus', 'Night': 'Moon',    'Participating': 'Mars'},
    'Air':   {'Day': 'Saturn','Night': 'Mercury', 'Participating': 'Jupiter'},
    'Water': {'Day': 'Venus', 'Night': 'Mars',    'Participating': 'Moon'},
}
SIGN_ELEMENT = {
    'Aries': 'Fire', 'Leo': 'Fire', 'Sagittarius': 'Fire',
    'Taurus': 'Earth', 'Virgo': 'Earth', 'Capricorn': 'Earth',
    'Gemini': 'Air', 'Libra': 'Air', 'Aquarius': 'Air',
    'Cancer': 'Water', 'Scorpio': 'Water', 'Pisces': 'Water',
}
TRIPLICITY_TABLE_SOURCES = {
    'Sahl': 'Sahl, The Introduction 1.37, Figure 4',
    'al-Qabisi': 'al-Qabisi I.16c',
    'Valens': 'Valens II.1–2',
    'Great Introduction': 'Great Introduction V.14, 7, Figure 53, fn 100',
}
VIRGO_PARTNER_NOTE = "Virgo's partnering lord follows the source named above: Sahl, al-Qabīsī, and Valens give Mars, while Abū Maʿshar's Great Introduction favors Mercury (V.14.7, Dykes fn 100)."


def triplicity_rulers(sign, *, table):
    """One day lord, night lord and partner from the explicitly named source.
    The reference point's sign decides the Virgo exception, never Mercury's
    position or condition. V.14, 7 with fn 100 selects one partner, not two.
    """
    if table not in TRIPLICITY_TABLE_SOURCES:
        raise ValueError(f'Unknown triplicity table: {table!r}')
    rulers = dict(TRIPLICITY[SIGN_ELEMENT[sign]])
    if table == 'Great Introduction' and sign == 'Virgo':
        rulers['Participating'] = 'Mercury'
    return rulers


def triplicity_source_fields(sign, *, table):
    """Provenance travels with each reference point, including exports."""
    return {'Triplicity table': TRIPLICITY_TABLE_SOURCES[table],
            'Virgo source note': VIRGO_PARTNER_NOTE if sign == 'Virgo' else ''}


# Inverse lookups: sign -> the single planet that has domicile/exaltation there
SIGN_TO_DOMICILE = {sign: planet for planet, signs in DOMICILES.items() for sign in signs}
SIGN_TO_EXALTATION = {sign: planet for planet, signs in EXALTATIONS.items() for sign in signs}

# Masha'allah's delineations for a topical house's lord, keyed by
# [placed_in_house][lord_of_house] (i.e. outer key = the WSH house the lord
# is physically placed in, inner key = the topical house it rules). Each
# cell is {'text': this app's paraphrase of Sahl's own sentence, 'cite':
# its locator in On Nativities (chapter, sentence; a range where the cell
# rests on two or three)}; evaluate_house_lords() prints the text with the
# locator in parentheses. The wording is paraphrased from Sahl's twelve
# lords-of-places passages -- the lord of the first 1.36, 79-97; the
# second 2.14, 9-28; the third 3.10, 1-13; the fourth 4.11, 2-23; the
# fifth 5.1, 78-90; the sixth 6.3.4, 12-23; the seventh 7.1, 205-216; the
# eighth 8.5, 2-13; the ninth 9.4, 23-34; the tenth 10.2.4, 1-12; the
# eleventh 11.1, 16-27; the twelfth 12.1, 35-46 -- with Sahl's own
# conditions (if received, if a fortune or an infortune looked at it) kept
# in the cell. Every chapter has a sentence for all twelve places, so no
# cell is empty. The arrangement (those twelve chapters laid out as a
# 12 x 12 grid) is the TNAC Reference Guide for the Planets and Places
# (Dykes, 2023); the wording is not the Guide's. Cell [8][5] (the lord of
# the fifth in the eighth) is 5.1, 85, which Dykes prints with an
# [illegible] bracket -- manuscript E is smudged there -- and glosses in
# his fn 47; the cell says so. tests/test_prose_tables.py pins every cell
# to its sentence by locator and three shared anchor words.
MASHAALLAH_LORDS = {
    1: {
        1: {'text': "Respected in his family; if it connects with a planet in the Midheaven, rank from the Sultan by that planet's dignity, or rank through the loss of his religion and honor if the planet is in its fall", 'cite': '1.36, 79-81'},
        2: {'text': 'Works with his own hands, blessed without searching or need; better if received', 'cite': '2.14, 9'},
        3: {'text': 'Good for his siblings: they get good from him and are sincere towards him', 'cite': '3.10, 1'},
        4: {'text': 'Master of his family, their livelihood from him, charitable to his parents and an authority to them; if an infortune looked at it, hardship for the parents over authority', 'cite': '4.11, 2-3'},
        5: {'text': 'Blessed with children in his youth; his eye pleased, and he happy', 'cite': '5.1, 78'},
        6: {'text': 'An illness of the essence of that planet; the death of his animals, if he has them, and of his servants', 'cite': '6.3.4, 12'},
        7: {'text': 'Good from women, and he successful', 'cite': '7.1, 205'},
        8: {'text': 'If free of the infortunes, a long lifespan, but frustrated and hard put to find his necessities', 'cite': '8.5, 2'},
        9: {'text': 'Of fine religion and a good soul, endearing, knowing the Sunnah', 'cite': '9.4, 23'},
        10: {'text': 'An associate of authority, proficient in his work; the Sultan comes to him without his seeking it', 'cite': '10.2.4, 1'},
        11: {'text': 'Successful, with a good livelihood and condition, and glad', 'cite': '11.1, 16'},
        12: {'text': 'Unhappy; his enemies multiply and are victorious over him; tribulations, and he belligerent', 'cite': '12.1, 35'},
    },
    2: {
        1: {'text': 'A corruptor of assets; if the lord of its house looked at it and received it, he gains properly, from the essence of the sign the lord of the second is in', 'cite': '1.36, 83'},
        2: {'text': 'A livelihood from a known source; if the lord of the third looked at it, suffering siblings; if an infortune looked at it, his assets ruined and scattered', 'cite': '2.14, 10-11'},
        3: {'text': 'His siblings contend with him over his assets; sorrows come upon them, and they seek him out', 'cite': '3.10, 2'},
        4: {'text': 'Prosperous parents, good in what they leave behind; he inherits their assets and is the most distinguished of his brothers over the parents', 'cite': '4.11, 4'},
        5: {'text': 'Children blessed with a livelihood under their own authority, and with the Sultan', 'cite': '5.1, 79', 'note': "Referent uncertain; fn 45 suggests 'he,' one of the children."},
        6: {'text': 'A livelihood from what slaves produce and the renting of animals; blessed from a lowly thing', 'cite': '6.3.4, 13'},
        7: {'text': 'Corrupts his assets because of a contention; his women die, and have defects', 'cite': '7.1, 206'},
        8: {'text': 'Blessed by inheritance; sometimes in the steady employ of the Sultan', 'cite': '8.5, 3'},
        9: {'text': 'Assets from a country not his own; blessed in relation to journeys', 'cite': '9.4, 24'},
        10: {'text': 'A livelihood from the Sultan and because of him; he accumulates assets', 'cite': '10.2.4, 2'},
        11: {'text': 'Blessed by his friends, getting assets because of them', 'cite': '11.1, 17'},
        12: {'text': 'A work he is embarrassed about; a bad character and a bad livelihood, with deception in it', 'cite': '12.1, 36'},
    },
    3: {
        1: {'text': 'Siblings suited to him who get good from him; he depends on them and travels much; if a fortune looked at it, good religion, if an infortune, wicked in his mind', 'cite': '1.36, 84-85'},
        2: {'text': 'Gain from travels and because of siblings; if it is a fortune, religion, and gain by it', 'cite': '2.14, 12'},
        3: {'text': 'Well-known siblings who go to him and protect him, since he is loved', 'cite': '3.10, 3'},
        4: {'text': "Siblings from whom the parents meet hardship and hostility; he is better than his siblings in his parents' opinion; if an infortune looked at it, the parents confined in prisons, tribulation and hardship at their death, and the siblings most wretched", 'cite': '4.11, 5-6', 'note': "Variant — 4.11, 5 fn 160. E: 'he'; M: 'they,' referring to the parents' siblings."},
        5: {'text': 'Children named with the names of his siblings, successful in travels', 'cite': '5.1, 80'},
        6: {'text': "Siblings with defects or illness, or doing the work of slaves; it shows the siblings' livelihood", 'cite': '6.3.4, 14'},
        7: {'text': 'His brother marries a woman of his; he is hostile to his siblings, or marries his relatives, and his marriage is abroad', 'cite': '7.1, 207'},
        8: {'text': 'Siblings with defects, chronic illness and disease, doing the work of slaves, and diminished', 'cite': '8.5, 4'},
        9: {'text': 'His siblings marry foreign women; he moves from his own country to another', 'cite': '9.4, 25'},
        10: {'text': 'Few siblings, and those he has ruined; it multiplies travels', 'cite': '10.2.4, 3'},
        11: {'text': 'Well-known siblings, their condition good, blessed in their youth', 'cite': '11.1, 18'},
        12: {'text': 'His siblings hostile towards him; hardship from them, and the badness of their condition', 'cite': '12.1, 37'},
    },
    4: {
        1: {'text': 'Reverent towards his parents, hardship from the Sultan, a livelihood he is pleased with; if received where it stands, good or rank from his fathers', 'cite': '1.36, 86'},
        2: {'text': "Gain in relation to fathers and ancestors: the fathers' assets good, the house he was born in thriving, and he devoted to his parents", 'cite': '2.14, 13'},
        3: {'text': "The siblings steal the parents' assets, and the family recognizes them as the thieves", 'cite': '3.10, 4'},
        4: {'text': "Parents well known among the people, with importance and a reputation; if an infortune looked at it, or the Sun made it unfortunate, the parents' lifespan is short", 'cite': '4.11, 7-8'},
        5: {'text': 'Children who are wretches; hardship and enmity because of them', 'cite': '5.1, 81'},
        6: {'text': 'Of the children of slaves, or of those who do the work of slaves', 'cite': '6.3.4, 15'},
        7: {'text': 'Marries a woman from the people of his own house, well known and virtuous', 'cite': '7.1, 208'},
        8: {'text': 'The fathers foreigners, or with defects or chronic illness, and their lifespans diminished', 'cite': '8.5, 5'},
        9: {'text': 'Parents with hidden illnesses, dying outside their homeland', 'cite': '9.4, 26'},
        10: {'text': 'The parents of the people of the house well known at the doors of the Sultan, and hardship from the Sultan', 'cite': '10.2.4, 4'},
        11: {'text': "His father with a chronic illness, the parents' lives shortened, and his condition diminished", 'cite': '11.1, 19'},
        12: {'text': 'The parents and family hostile towards him and quarrelling with him; he destroys the home he was born in and moves from it', 'cite': '12.1, 38'},
    },
    5: {
        1: {'text': 'Happy with children and many friends; if an infortune looked, that is undermined', 'cite': '1.36, 87'},
        2: {'text': 'Gain in relation to women and his children, or those known at the door of the Sultan; they gain the good, or importance among their class', 'cite': '2.14, 14'},
        3: {'text': 'Siblings away from the homeland who travel much and are blessed with suitable children', 'cite': '3.10, 5'},
        4: {'text': 'Prosperous parents who see children and grandchildren and reach a full lifespan, if no infortune looks while the fortunes do; they increase in the good', 'cite': '4.11, 9', 'note': "Conjecture — 4.11, 9 fn 163: 'prosperity' is a conjectured alternative whose required accusative ending is absent from both manuscripts."},
        5: {'text': 'Well-known children, and happy', 'cite': '5.1, 82'},
        6: {'text': 'The upbringing of his children hard, and a defect in them', 'cite': '6.3.4, 16'},
        7: {'text': 'Marries a woman younger than himself, with compassion and goodness of character', 'cite': '7.1, 209'},
        8: {'text': 'His children die in their youth, or have more power over the people because of the Sultan', 'cite': '8.5, 6'},
        9: {'text': 'Children in a country not his own, and he will marry; his eye delights in the children', 'cite': '9.4, 27'},
        10: {'text': 'His children with a chronic illness or disease, and they die; they meet hardship from the Sultan', 'cite': '10.2.4, 5'},
        11: {'text': 'Delightful children, blessed with good and comfort from the first of them to the last', 'cite': '11.1, 20'},
        12: {'text': 'His children disobey him and are hostile towards him; defects in them, and their condition bad', 'cite': '12.1, 39'},
    },
    6: {
        1: {'text': "Miserable, doing slaves' work; if received, illnesses occur; if the Moon is also corrupted and connects with a planet in a corrupt place, he becomes a slave", 'cite': '1.36, 88-89', 'note': "Interpretive note — fn 489: Dykes says Masha'allah seems to regard sickness as preferable to slavery; this does not cancel the following Moon clause."},
        2: {'text': 'Gain in relation to slaves, riding animals and medications; if received, or the lord of the Ascendant looked at it, gain from medications and the illness of animals and slaves; if not received, or made unfortunate, his animals ruined, disaster in his assets, a livelihood of toil and hardship', 'cite': '2.14, 15-16'},
        3: {'text': 'His siblings are hostile to him, seek his calamity and crave his ruin', 'cite': '3.10, 6'},
        4: {'text': "Parents unknown in the country they are in, their condition read from the planets connected with it: if an infortune looked, harm to them by illnesses of the sign's nature; if a fortune looked, good abroad, and at home too if that fortune received it", 'cite': '4.11, 10-12'},
        5: {'text': 'Fortunate children, but defects appear in them', 'cite': '5.1, 83'},
        6: {'text': 'Healthy, if the lord of the Ascendant does not look at it', 'cite': '6.3.4, 17'},
        7: {'text': 'Associates with slave girls or women with defects', 'cite': '7.1, 210'},
        8: {'text': 'His calamities in slaves and riding animals; no blessing from them', 'cite': '8.5, 7'},
        9: {'text': 'Blessed with slaves and riding animals; ill on journeys, or if healthy, his slaves corrupted, one of the two without escape', 'cite': '9.4, 28'},
        10: {'text': 'A short lifespan; he lives by walking about; he enslaves free people and has authority over them', 'cite': '10.2.4, 6'},
        11: {'text': 'A bad condition in his livelihood, little good, creating discord', 'cite': '11.1, 21'},
        12: {'text': 'Saddened by his slaves and riding animals, and no good in them', 'cite': '12.1, 40'},
    },
    7: {
        1: {'text': 'Many lawsuits; deceptive; subordinate to women in what they say', 'cite': '1.36, 90'},
        2: {'text': "Gain from women and lawsuits; if nothing looks at it, it is not received and the lord of the Ascendant does not look, he gathers assets mostly outside contention, his women die and he marries servant women; If the Ascendant lord aspects the second lord and 'it' is an infortune, assets are wasted through lawsuits and women, with hardship [antecedent of 'it' uncertain: either lord; 2.14, 19]", 'cite': '2.14, 17-19'},
        3: {'text': 'Either his brothers marry some of his women and have children by them, or they are hostile to him', 'cite': '3.10, 7'},
        4: {'text': 'He marries a woman somewhat above him, from his own family; the fathers held base, and the father hostile to him and contending with him', 'cite': '4.11, 13'},
        5: {'text': 'His children mostly from his maids and the women of his service; they are hostile to him, and he deceives himself about the women', 'cite': '5.1, 84'},
        6: {'text': 'Associates with women who have no social esteem, and bad words are said about him', 'cite': '6.3.4, 18'},
        7: {'text': 'Marries a well-known woman, an equal match for him, whom he loves', 'cite': '7.1, 211'},
        8: {'text': 'Marries women with inheritances, inherits and gets their assets from them; dies in exile', 'cite': '8.5, 8'},
        9: {'text': 'Marries a foreign woman of good, pleasing character; if it is a fortune, she is pious', 'cite': '9.4, 29'},
        10: {'text': "Marries a woman more powerful and significant than himself, from the people of the Sultan's house, sensible and upright", 'cite': '10.2.4, 7'},
        11: {'text': 'A woman he loves, who is lucky; children by her, and benefit from them', 'cite': '11.1, 22'},
        12: {'text': 'Mixes with low women with defects in them, and they are hostile towards him', 'cite': '12.1, 41'},
    },
    8: {
        1: {'text': 'A wicked soul, much distress, faint-hearted', 'cite': '1.36, 91'},
        2: {'text': 'Gain in relation to inheritance and the dead; generous with his gains, careless where they come from and go; if it connects with the lord of the eighth, his assets taken by force and he one who imposes taxes; if the lord of the eighth connects with it, gain from the dead and inheritances', 'cite': '2.14, 20-21'},
        3: {'text': "His brothers' women do not survive, and they get inheritances in relation to women", 'cite': '3.10, 8', 'note': 'Translation speculative; fn 115.'},
        4: {'text': "Diminishes the fathers' lifespan; fear for the native; the mother dies in childbirth, worse if the Moon is corrupted or connecting with a retrograde planet; if it is the Sun, fear for the father; a fortune looking changes some of this", 'cite': '4.11, 14-15'},
        5: {'text': "Partly illegible. Dykes's tentative interpretation: the children suffer or die, and those surviving are premature.", 'cite': '5.1, 85 fn 47', 'detail': "Surviving printed wording: 'they will survive and will be miscarried'. Dykes's fn 47 says 'miscarried' can also mean very premature; his interpretation remains tentative."},
        6: {'text': 'Healthy, if the lord of the Ascendant does not look at it', 'cite': '6.3.4, 19'},
        7: {'text': 'Consumes the inheritance of the women; a foreign woman', 'cite': '7.1, 212'},
        8: {'text': 'Healthy, his illness insignificant, and his death light', 'cite': '8.5, 9'},
        9: {'text': 'Highway robbery on journeys; eager in the accumulation of assets', 'cite': '9.4, 30'},
        10: {'text': 'Authority in his younger years; a follower who seeks the leadership and is boastful', 'cite': '10.2.4, 8'},
        11: {'text': 'Not well known nor of distinguished descent; low work such as commerce', 'cite': '11.1, 23', 'note': "Variant — 11.1, 23 fn 16: the Latin WSM reads 'it signifies the loss of his neighbors and friends'; the printed translation retains commerce."},
        12: {'text': 'Few enemies; many of his slaves die', 'cite': '12.1, 42'},
    },
    9: {
        1: {'text': 'Stays in a land other than his birthplace, travels much, speaks of knowledge; if free of the infortunes, knowledgeable and sensible', 'cite': '1.36, 92'},
        2: {'text': 'Gain in relation to travels, piety and religion; if the lord of the Ascendant looked at it, a livelihood from travels and absence from home; if a fortune looked at it and received it, from religion and devoutness; if an infortune, or one inimical to it, a master of magic and remedies', 'cite': '2.14, 22-24'},
        3: {'text': 'His siblings marry foreign women away from the homeland, and live and shelter with them', 'cite': '3.10, 9'},
        4: {'text': 'The fathers unknown, moving from their homes, with a defect in them, dying a bad death with pains; if the sign is four-footed, one who employs riding animals, a deceiver without piety', 'cite': '4.11, 16-17'},
        5: {'text': 'Children while absent from his homeland; they make him happy and please his eye', 'cite': '5.1, 86'},
        6: {'text': 'Excellent intentions; ill while away from home, and hardship met', 'cite': '6.3.4, 20'},
        7: {'text': 'Marries a foreign woman whom her brother gives in marriage, and he loves her', 'cite': '7.1, 213'},
        8: {'text': 'Bad thoughts and work; dies in exile', 'cite': '8.5, 10'},
        9: {'text': 'Few journeys; upright in the religion of his fathers, good in intention', 'cite': '9.4, 31'},
        10: {'text': 'Authority in traveling abroad, his leadership in it, and the good offered to him', 'cite': '10.2.4, 9', 'note': "Variant — 10.2.4, 9 fn 163. M: 'the good will be strengthened.'"},
        11: {'text': 'Good fortune and the good when out of the country; happy until the end of his life', 'cite': '11.1, 24'},
        12: {'text': 'His siblings meet hardship from enemies, and he too if he travels; bad religion', 'cite': '12.1, 43'},
    },
    10: {
        1: {'text': 'At the doors of the Sultan, known to him and making himself known, living by them', 'cite': '1.36, 93'},
        2: {'text': 'Gain at the doors of the Sultan and because of him', 'cite': '2.14, 25'},
        3: {'text': 'Death and ruin of the siblings; he is jealous of them, and they hate each other', 'cite': '3.10, 10'},
        4: {'text': 'The fathers well known in the house of the Sultan, and he grows in knowledge with them, if received; if an infortune looked and it is not received, hardship, tribulation and conflict from the Sultan', 'cite': '4.11, 18-19'},
        5: {'text': 'Illness and a defect appearing in his children', 'cite': '5.1, 87'},
        6: {'text': 'He will encounter hardship from the Sultan', 'cite': '6.3.4, 21'},
        7: {'text': 'Marries one of the family of the house of the Sultan, a fortunate woman, and gets good from her', 'cite': '7.1, 214'},
        8: {'text': 'His ruin because of the Sultan, and at his hands', 'cite': '8.5, 11'},
        9: {'text': "His siblings marry women better than themselves, or of the family of the Sultan's house; he is pious", 'cite': '9.4, 32'},
        10: {'text': 'Proficient in work, with influence in it and informed in it; his livelihood from it', 'cite': '10.2.4, 10'},
        11: {'text': 'Authority in his friendship; the Sultan of his people not hostile', 'cite': '11.1, 25', 'note': "Variant — 11.1, 25 fn 17: E's 'youth'. Alternative translation — fn 18: '[his] authority over his family will not be hostile'."},
        12: {'text': 'The Sultan and those who wield power over him hostile; his sorrow and griefs last a long time', 'cite': '12.1, 44'},
    },
    11: {
        1: {'text': 'A good character and many friends, but harsh towards children, and few of them', 'cite': '1.36, 94'},
        2: {'text': 'Gain in relation to friends and commerce; if the lord of the Ascendant looked at it, a good condition and livelihood, his friends in need of him; if not, he in need of his friends', 'cite': '2.14, 26-27'},
        3: {'text': 'Pious siblings renowned for it, and they attribute it to him', 'cite': '3.10, 12'},
        4: {'text': "The father's lifespan short and the badness of the parents' condition, unless received by the lord of its house, which dissolves that and raises them up; better still if a fortune looked at it", 'cite': '4.11, 20'},
        5: {'text': 'His eye pleased with his children and the family of his house, and he praised', 'cite': '5.1, 88', 'note': "Pronoun disputed; fn 48 prefers 'they'."},
        6: {'text': 'Befriends and associates with people who are not well known', 'cite': '6.3.4, 22'},
        7: {'text': 'Marries a fertile woman, loves her, and lives in luxury because of her', 'cite': '7.1, 215'},
        8: {'text': 'His friends diminished and what is between them corrupted; he dies when his condition is good', 'cite': '8.5, 12'},
        9: {'text': 'Pious friends who love him in the religion of God; his siblings marry foreign women', 'cite': '9.4, 33'},
        10: {'text': 'His friends get good from him; his child inherits assets which he collects from the Sultan', 'cite': '10.2.4, 11'},
        11: {'text': 'A comfortable life, imputed with goodness, with many friends and much culture', 'cite': '11.1, 26'},
        12: {'text': 'He leaves the goodness of friends, and they return to enmity; unhappy', 'cite': '12.1, 45'},
    },
    12: {
        1: {'text': 'Miserable, a bad livelihood, many enemies who prevail over him; if it connects with no planet in a stake or an excellent place, the planet in its nature, miserable until ruined; if the planet it connects with is an infortune or harmed, enemies kill him', 'cite': '1.36, 95-97'},
        2: {'text': 'Gain in relation to prisons and enemies; poor in soul, much distressed', 'cite': '2.14, 28'},
        3: {'text': 'His siblings are hostile to him, take his authority and are superior to him', 'cite': '3.10, 13'},
        4: {'text': 'The fathers foreigners who left their own land for the badness of their condition, one of them devout; if a fortune looked at it, good in the exile, if an infortune, tribulation; best if the fortune received it, then good or elevated status in the exile', 'cite': '4.11, 21-23'},
        5: {'text': 'Children with a defect or chronic illness, dying of that disease; if made unfortunate, no children', 'cite': '5.1, 89-90'},
        6: {'text': 'Hostile to people who <do not> have social esteem, and they come to harm', 'cite': '6.3.4, 23', 'note': "Variant — 6.3.4, 23 fn 161. M reverses the clause: 'they will not come' [to harm]."},
        7: {'text': 'Marries a woman with no social esteem; hardship from her, and she hostile to him', 'cite': '7.1, 216'},
        8: {'text': 'Fear that enemies kill him, or that the foolish fight him', 'cite': '8.5, 13'},
        9: {'text': 'Wicked in his intention, a corruptor of religion, thinking himself in the right', 'cite': '9.4, 34'},
        10: {'text': 'Dispossessed by the authorities; griefs and hardship from them', 'cite': '10.2.4, 12'},
        11: {'text': 'Little good, miserable in his way of living, few friends and many enemies', 'cite': '11.1, 27'},
        12: {'text': 'Few enemies, who do not appear to him; safe from their evil', 'cite': '12.1, 46'},
    },
}
# Delineations for a planet occupying a given whole-sign house, keyed by
# [wsh_house][planet]. Each cell is {'Rhetorius': [entries], 'PN IV':
# {'Good': {'text', 'cite'}, 'Bad': {'text', 'cite'}}}. evaluate_planets_in_houses()
# prints the two PN IV halves in the page's two condition columns, 'If in a
# suitable condition' and 'If in a bad condition', each with its locator,
# and the entry list in a third column, 'Rhetorius and Firmicus, as the
# texts state it'.
#
# The Rhetorius list is this app's paraphrases of Rhetorius, Astrological
# Compendium Ch. 57 (Holden), the significations of the twelve houses, read
# house by house (he opens with the twelfth), and of Firmicus Maternus,
# Mathesis III.2-III.7 and III.13 (Dykes), the planets in the twelve places
# -- one tradition (Dykes's fn 28 to III.2 fills a lacuna from Rhetorius),
# but two witnesses, kept apart. Neither text divides a planet's reading
# into well and badly placed: they divide by sect and by conditions, so
# their testimony is a LIST of entries, not a pair of halves. Each entry is
# {'author', 'cite', 'axis', 'text', 'portional', 'conditional'}:
#   author  'Rhetorius' | 'Firmicus' | 'Rhetorius, as summarized by Dykes'
#           (the last for Dykes's fnn 284-285 to III.13, which supply
#           Rhetorius's Moon in the fifth and seventh where Firmicus's own
#           sentences, III.13, 19-22, are lost);
#   cite    Rhetorius by chapter, house and Holden's page or pages
#           ("Ch. 57, the sixth, p. 76"; "pp. 57-61" where the sentences run
#           over a page break or fold in the house's earlier general
#           sentences; "pp. 65, 88-89" where the paragraph the translators
#           reassign to this house sits on another house's pages); Firmicus
#           by chapter and sentence or run ("III.2, 8", "III.2, 4-5"); the
#           summaries "III.13 fn 284" / "III.13 fn 285";
#   axis    the author's OWN division, never converted: 'by day' / 'by
#           night' where he says so, 'in sect' / 'out of sect' where he says
#           so (Rhetorius), 'unsplit' where the sentence makes no such
#           division (a gradation such as "especially by day" stays in the
#           text), and 'general malefic' / 'general benefic' for Ch. 57's
#           "the malefics there ..." / "the benefics ..." sentences, entered
#           for Saturn and Mars, and for Jupiter and Venus, in that house;
#           'joint' for the four named pairs, sharing a testimony identity;
#   text    the paraphrase, its antecedents whole -- an entry keeps every
#           condition its sentence states or is left out whole; a
#           configuration that is not the planet's occupancy of the place
#           (the full Moon moving toward Mars) is not an entry;
#   portional  True where Firmicus states the placement "portionally" (in the
#           portion of the Hour-marker, portionally in the Nth place) -- or
#           where the cited run refers back to the place's opening sentence
#           that states it ("so put", "in this place", "in the Midheaven":
#           III.2, 4-5; 30; 41-43; 44-48; III.4, 22-25; 80-81), since dropping
#           the flag there would widen his scope silently; the reader prints
#           "(portionally)";
#   conditional  True where the entry's whole reading rests on a stated
#           configuration beyond the placement and the axis (Saturn in the
#           Hour-marker by day AND Mars in another pivot; Venus in the twelfth
#           joined by a ray with Jupiter); a cell whose entries run past
#           RHETORIUS_CELL_WORDS prints its other entries and sends these to
#           the row's detail, so that no condition is ever dropped to fit.
# Where the two authors disagree, both entries stand, each under its own
# name; no entry is a merged "both" cite. A sect word the translator
# supplies in brackets or angle brackets (Holden's "[by night]" for Saturn
# in the second, Dykes's "<by night>" for III.2, 8) is kept as the axis with
# the supplement named in the text. Where the translators mark a paragraph
# as printed under the wrong house (Holden's and Dykes's notes: Rhetorius's
# second set of third-house paragraphs is the ninth's, repeated there, and
# his second set of eleventh-house paragraphs for Saturn, Jupiter, Mars and
# Mercury is the fifth's), the paragraph serves the house it belongs to and
# is cited under it; Holden's fn 4 to p. 65 keeps the Sun paragraph there in
# the third, and the Venus paragraph there names the God or the Goddess, so
# both serve the third as well, with the note that its sect labels are the
# reverse of the ninth's copy. Rhetorius's Moon in the sixth, eighth and
# twelfth is lost in the manuscripts and Firmicus's Moon in the fifth to the
# eighth is missing; an empty list prints as a dash, which is the absence of
# testimony, not a neutral reading.
#
# The PN IV halves are this app's paraphrases of Abu Ma'shar's Book II
# chapters on the lord of the year in the houses of the circle -- Saturn
# II.6, Jupiter II.9, Mars II.12, the Sun II.15, Venus II.18, Mercury
# II.21 -- applied to natal planets, which is the TNAC Reference Guide's
# reading of those chapters and is followed here; the page says visibly
# that this is an adaptation of annual rules whose prerequisites (the root
# and the revolution together, the planet as lord of the year) the natal
# lookup does not establish. Each chapter's division is the planet's
# CONDITION -- suitable, received, in his own house, against alien, not
# received, made unfortunate, retrograde -- with the house among its
# factors, so the halves are 'If in a suitable condition' and 'If in a bad
# condition', and each keeps its own qualifications inside the cell (II.6,
# 15's retrograde alternative; II.6, 24's leisure inside the suitable
# reading; II.6, 1-3's for-himself / for-another distinction). Where a
# chapter groups houses (the eleventh or fifth, the ninth or third, the
# second or eighth, the sixth or twelfth, the four stakes) the group's
# sentence is cited for each house in it with the sharing named in the
# locator ("II.6, 4 (shared with the 5th)"). Saturn's four falling places
# (II.6, 22-24) are a common layer over his second, sixth, eighth and
# twelfth beside each house's own sentence, carried whole -- 22's "in the
# revolution", 23's intensifications, 24's residual leisure; Jupiter's four
# places not looking at the Ascendant (II.9, 15) and Venus falling from
# the stakes (II.18, 18) occupy Shared passages, outside the condition
# split; Jupiter II.9, 16 is a separate made-unfortunate addition. Mercury's II.21, 8 (the ninth or third)
# states no condition, unlike its adverse pair II.21, 9; the half says so.
# The Moon has no Book II houses chapter -- II.22, 13 says to judge her "in
# the manner of ... the rest of the planets", and the translator's fn 312
# points to VII.8, her transit through the twelve houses. VII.8 is a
# transit list with its own frame (the three positions, "so long as she is
# in it") and supplies no condition split, so it is NOT sorted into these
# two columns: the Moon's PN IV halves point to MOON_IN_HOUSES_VII8 below,
# her own unsplit table, one reading per house.
#
# The arrangement (a 12 x 7 grid) is the Guide's; tests/test_prose_tables.py
# pins every entry and every PN IV half to its sentence by locator and
# three shared anchor words.
def _half(text, cite=''):
    return {'text': text, 'cite': cite}
_DASH = _half('—')
def _P(good, gcite, bad, bcite, shared=()):
    return {'Good': _half(good, gcite) if good else _DASH,
            'Bad': _half(bad, bcite) if bad else _DASH, 'Shared': tuple(shared)}

RHETORIUS_AUTHORS = ('Rhetorius', 'Firmicus', 'Rhetorius, as summarized by Dykes')
RHETORIUS_AXES = ('by day', 'by night', 'in sect', 'out of sect', 'unsplit', 'general malefic', 'general benefic', 'joint')
_R, _F, _RD = RHETORIUS_AUTHORS
def _E(author, axis, cite, text, portional=False, conditional=False):
    assert author in RHETORIUS_AUTHORS and axis in RHETORIUS_AXES, (author, axis)
    return {'author': author, 'cite': cite, 'axis': axis, 'text': text,
            'portional': portional, 'conditional': conditional}

JOINT_CONFIGURATION_LABEL = 'Joint configuration — both named planets required; no day/night division stated'

_JOINT_EIGHTH_MALEFICS = dict(_E(_R, 'joint', 'Ch. 57, the eighth, p. 85', 'Saturn and Mars here without Jupiter and Venus: banished', conditional=True), testimony_id='rhetorius-57-p85-pair')
_JOINT_EIGHTH_BENEFICS = dict(_E(_R, 'joint', 'Ch. 57, the eighth, p. 86', "Jupiter and Venus in the eighth 'alone': an easy death and great good fortune — Node scope uncertain; may continue the preceding Ascending Node premise", conditional=True), testimony_id='rhetorius-57-p86-pair')
_JOINT_NINTH_MALEFICS = dict(_E(_R, 'joint', 'Ch. 57, the ninth, p. 88', "Saturn and Mars here, matutine or stationary, aspecting the Lot of Fortune in the Ascendant: a great tyrant, a despoiler of temples, impious, and without a god's wrath", conditional=True), testimony_id='rhetorius-57-p88-pair')
_JOINT_TENTH_MALEFICS = dict(_E(_R, 'joint', 'Ch. 57, the tenth, p. 92', 'Saturn and Mars in the Midheaven aspecting Jupiter, Venus and the Sun: a very bad figure -- miserable, thieves or living with cheap women, losing whatever they get or having it confiscated, short-lived or dying a violent death, especially with the Moon there', conditional=True), testimony_id='rhetorius-57-p92-pair')

# The Moon's two PN IV halves: VII.8 is her own table.
_MOON_POINTER = "see the Moon's table below (VII.8, by her transit)"

# Saturn's four-falling-places sentences (II.6, 22-24), a common layer over
# his second, sixth, eighth and twelfth, carried whole beside each house's
# own sentence.
_SAT_FALL_BAD = "II.6, 22-24 add, for any of the four places falling from the Ascendant, that in the revolution, in a bad condition, there is leisure, laziness and despair of being blessed and of beneficial things; not received as well, it is harsher -- dispossessed, hardship, evil said about him, and illness from cold and moisture or cold and dryness; retrograde, harsher again; but eastern, received or in his own house, a suitable condition except that he is at leisure, little occupied in what would benefit him"
_SAT_FALL_GOOD = "II.6, 24 adds that in any of those falling places, eastern, received or in his own house, a suitable condition except for being at leisure"
_JUP_FALL_AFFLICTED = "II.9, 16 adds, in those four places: made unfortunate, something detestable will find him because of these things"
_JUP_FALL_SHARED = _half(
    "II.9, 15 — reception reading disputed: <not> received ('not' supplied by the translator); fn 184: possibly 'even if he is received'. "
    "Jupiter as lord of the year, in the revolution of the year especially, in the four positions not looking at the Ascendant: "
    "no eminence, no increase in rank, brothers and acquaintances shun him, little occupied in what would benefit him. "
    "The four places are 2, 6, 8, 12 by II.6, 22; II.9, 15 does not enumerate them.", 'II.9, 15 fn 184')
_VEN_FALL_SHARED = _half(
    "II.18, 18 — reception/affliction condition not stated. Venus, in the lord-of-the-year chapter, in the revolution "
    "falling from the stakes or what follows them: the owner of the revolution loathes amusements and delight, eating and drinking, or is harmed by them. "
    "Strengthening only: firmer if the year terminated at the position of Saturn in the root and Saturn was in a stake of the "
    "terminal sign or the revolution's Ascendant; fn 289: probably the transiting Saturn at the revolution. "
    "Fn 290 explains Saturn's spoiling Venus's pleasure in that case. The houses 3, 6, 9, 12 are this app's mapping, not an enumerated list in the sentence.",
    'II.18, 18 fnn 289-290')

_SAT_STAKE_GOOD = "In his own house or a position where he has a claim, in a suitable condition: he takes possession of villages for himself, devoted to building, the improvement of lands and the digging of rivers; in an alien sign but received, he assumes responsibility for such things on behalf of someone else, as a trust and as household manager, is praised, and good comes to him from it"
_SAT_STAKE_BAD = "In an alien sign, not received: he still assumes that responsibility for someone else, but is blamed and accused in it; if a corrupting planet also looks at him, harm and something detestable from it"
_JUP_STAKE_GOOD = "In his own house or a sign where he has a claim, or received, not made unfortunate: celebrated by the people of his class, praised, respected, increased in status, rank and assets"
_JUP_STAKE_BAD = "In a bad condition, retrograde, not received: the decrease of his assets and their corruption, a scarcity of eagerness to gather them, worries and distresses, from what the stake indicates"
_MARS_STAKE_GOOD = "Received, not made unfortunate: successful in what he seeks from the Sultan's authority and the masters of wars, inspiring awe in the people of his class, defeating whoever contends with him, praised, gaining status and power"
_MARS_STAKE_BAD = "Made unfortunate, not received: misfortune feared from conflagration, robbers, blood and every hot, bad, moist illness; retrograde, he flees his country, iron is feared for him, he spends because of travel and children"
_SUN_STAKE_GOOD = "Received, free of the infortunes: increase in rank, gaining good, commended and praised"
_SUN_STAKE_BAD = "Made unfortunate, not received: something detestable from what the stake indicates, little benefit, and fear of the Sultan"
_VEN_STAKE_GOOD = "Received, not made unfortunate: delight and rejoicing in various ways due to the Sultan and those having importance, increase in assets, rank and animals, reaching the gates of kings, gaining clothing and possessions, delighting in women with much sexual intercourse, successful in what he needs, increase in real estate; retrograde, he gains all that, but from a direction that is not good, and some of it spoiled"
_VEN_STAKE_BAD = "Made unfortunate, not received: disturbed in his way of life, loss, evil reports, quarrels, confusion and distress from what the stake indicates; by Saturn, cold bad pains such as paralysis of one side, cold and pleurisy; by Mars, something detestable because of women and sex, possessions burned and stolen, agitated blood"
_MERC_STAKE_GOOD = "Received, not made unfortunate: status, rank and benefits from writing, business and serving the Sultan, quick in instruction and in accepting the sciences and their preservation, praised and well spoken of"
_MERC_STAKE_BAD = "In a bad condition, made unfortunate, not received: something detestable because of writing, writers and calculation, illnesses, accused in the works he pursues, loss if devoted to business, selling and buying"
_MERC_RETRO = "II.21, 4 adds that retrograde, something detestable is reported about him because of sexual intercourse, and an illness befalls him of the nature of the star making him unfortunate"

PLANETS_IN_HOUSES = {
    1: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by day', 'Ch. 57, the first, pp. 51-52', "A loud outcry at the birth and what is done for the native through sound (the translator does not know what this means); first born or first reared, or a lack of brothers before him -- as always when he is angular, where he makes the first born or first reared or destroys the brothers before him"),
                       _E(_R, 'by night', 'Ch. 57, the first, p. 52', "Damages, opposition and hardships, and actions in wet places"),
                       _E(_R, 'unsplit', 'Ch. 57, the first, p. 48', "First-born or first-raised"),
                       _E(_R, 'general malefic', 'Ch. 57, the first, p. 48', "A malefic in the Ascendant aspecting the Sun and the Moon: those born are not reared, or short-lived, injured, sickly, or end in orphanhood", conditional=True),
                       _E(_F, 'by day', 'III.2, 1-3', "The birth announced by a great cry; the eldest of all the brothers, or, if one was born before him, that older one separated from the parents (by day in all four pivots he always makes the first-born or the first nourished, or the brothers born before destroyed -- the destruction is the translator's filling of a lacuna from Rhetorius -- haughty and encouraged by a spirit of pride)", portional=True),
                       _E(_F, 'by day', 'III.2, 4-5', "With Mars in another pivot or in the post-ascension of the pivot: the arrival of many evils, the greatest dangers and the slipping away of the patrimony; with no benefic in a good place joined to them by a strong ray, if Mars takes up the rays of the waxing Moon, a violent death", portional=True, conditional=True),
                       _E(_F, 'by night', 'III.2, 6-7', "Hindered by the greatest sluggishness and always pressed down by great labor; for some, actions around water, always worn out by laborious dealings", portional=True)],
                   'PN IV': _P(_SAT_STAKE_GOOD, 'II.6, 1-2 (shared with the 4th, 7th and 10th)', _SAT_STAKE_BAD, 'II.6, 3 (shared with the 4th, 7th and 10th)')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'in sect', 'Ch. 57, the first, p. 52', "Notable persons, ingenious, leaders in war, living pleasantly and of good courage; and always, angular, first born or first reared, or the destruction of the elder brothers"),
                        _E(_R, 'out of sect', 'Ch. 57, the first, p. 52', "First born or first reared, or he destroys the brothers born before them; he grows up well and in happiness with his parents, but destroys their things and loses his own"),
                        _E(_R, 'unsplit', 'Ch. 57, the first, p. 49', "Without the malefics, always famous persons, commanders or rulers of their own fatherland"),
                        _E(_R, 'general benefic', 'Ch. 57, the first, p. 48', "A benefic in the Ascendant without the malefics' aspect, with the ruler of the Ascendant and the rulers of its triplicity well situated: well nourished, and a fine first age of life", conditional=True),
                        _E(_F, 'unsplit', 'III.3, 1-2', "In the signs where he rejoices, his domiciles, bounds or height: nobles, glorious, always put in charge of great cities (sometimes among the ten leading citizens), good, charming, benevolent, happy, rich, especially by day with no malefic resisting him; a malefic resisting him by a contrary ray diminishes most of it", portional=True),
                        _E(_F, 'by night', 'III.3, 3-5', "The first-born among the brothers, or an older one perishes or is separated and settled in faraway places, so that he has the principal place in the paternal home; nourished well, the parents in the greatest luckiness; a malefic resisting him squanders the whole paternal and maternal assets", portional=True)],
                    'PN IV': _P(_JUP_STAKE_GOOD + ' -- in the Ascendant, in relation to his own motives and the work he does', 'II.9, 1-2 (shared with the 4th, 7th and 10th)', _JUP_STAKE_BAD, 'II.9, 3 (shared with the 4th, 7th and 10th)')},
        'Mars': {'Rhetorius': [
                     _E(_R, 'in sect', 'Ch. 57, the first, p. 52', "Military commanders, soldiers, troop commanders, rulers of life and death, terrible in their actions against cities and countries"),
                     _E(_R, 'out of sect', 'Ch. 57, the first, p. 52', "Hot-blooded, reckless, living abroad, fickle in their opinions and failing in their undertakings, mostly craftsmen in fire or iron, or living from military service"),
                     _E(_R, 'unsplit', 'Ch. 57, the first, p. 48', "Hot-headed, reckless, daring and foolhardy, sometimes injured or sickly; a quick birth, or one on the road or in the bath"),
                     _E(_R, 'general malefic', 'Ch. 57, the first, p. 48', "A malefic in the Ascendant aspecting the Sun and the Moon: those born are not reared, or short-lived, injured, sickly, or end in orphanhood", conditional=True),
                     _E(_F, 'by night', 'III.4, 1-3', "In masculine signs, military men; a morning star without Jupiter's ray, (a word is lost here) others more generous, giving beyond what the patrimony allows, seen as extravagant, their actions always made public; with the Hour-marker in a house of Mars and Jupiter with him there portionally or in a firm ray, the leaders of wars to whom every army is handed over, lords of death and life, at whose arrival cities and provinces shudder, lucky, powerful, grave, their anger known by great indignation -- yet esteeming neither spouse nor children with dear affection", portional=True),
                     _E(_F, 'by day', 'III.4, 4-6', "Hot, bold, furious, traveling men, always unstable, unable to complete what they dare, whatever they obtain flowing out of their hands, the patrimony squandered; they lose every asset of life, even wives and children, and perish in the first age -- mitigated to some extent if Jupiter in his own domicile, height or bounds regards him by trine or is with him portionally", portional=True)],
                 'PN IV': _P(_MARS_STAKE_GOOD + '; in the other stakes, according to the indication of that stake', 'II.12, 1-2 (shared with the 4th, 7th and 10th)', _MARS_STAKE_BAD + '; harsher by day in the Ascendant or the Midheaven', 'II.12, 3-5 (shared with the 4th, 7th and 10th)')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the first, pp. 48, 52-53', "In his own triplicity, first-born or first-raised, with rulership and advancement without the malefics' aspect; in the same angle (with Saturn, the editor supplies from Firmicus), kings or commanders, and with Mars with them or in square or opposition their kingdoms or commands beset with jealousies, dangers and wars; always, in the angular parts of the signs, the destroyer of the elder brothers, though some are first born or first nourished"),
                    _E(_R, 'by night', 'Ch. 57, the first, p. 53', "The parents of the lower class; aspected by Saturn or Mars by square or opposition or conjoined to them, the destroyer of the elder brothers, of their life or of all their possessions and actions"),
                    _E(_F, 'unsplit', 'III.5, 1-2', "Brothers, but with difficulty; in a masculine sign, his own house or height, joined by a ray to a benefic or protected by benefics, the trappings of great nobility; the malefics approaching him near the rays weaken the eyesight greatly", portional=True),
                    _E(_F, 'by day', 'III.5, 4-5', "With Saturn in the pivot of the Hour-marker: emperors and kings, or, his powers slightly hindered, consuls and proconsuls; Mars looking at him or with him, generalships, commands and positions of authority with great jealousies, dangers, hindrances, fights or difficulties", portional=True, conditional=True),
                    _E(_F, 'unsplit', 'III.5, 16', "Broadly in the sign of the Hour-marker: clever people, and nobles"),
                    _E(_F, 'by night', 'III.5, 21-23', "Born of a more sordid class; Saturn or Mars regarding him by square or diameter or with him, he kills the older brothers and dissipates the substance of his life, patrimony and actions; in all the pivots he always weakens the older brothers, or is the first born")],
                'PN IV': _P(_SUN_STAKE_GOOD + ' -- in the Ascendant or the Midheaven, renowned, a voice among the people of his class, good from the Sultan', 'II.15, 1-2 (shared with the 4th, 7th and 10th)', _SUN_STAKE_BAD, 'II.15, 3 (shared with the 4th, 7th and 10th)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'in sect', 'Ch. 57, the first, pp. 53-54', "Demigods in thought, magnates with great friends, friends of kings entrusted with the business affairs of princes, worthy professors according to the sign: in the signs of human form high priests adorned with gold, bearers of golden objects, predicting the future, the greatest in the world; in the half-voiced signs crafty hunters, friends of kings, makers of marvels for kings, growing rich on royal allowances; in the earth or water signs perfumers, those with a good wardrobe, or dyers; in the quadrupedal signs haughty, fond of praise, good, fond of nurturing animals or in charge of them"),
                      _E(_R, 'out of sect', 'Ch. 57, the first, p. 54', "Sexually promiscuous, reprehensible, lustful, indecent; linen-weavers, makers of clothes or of pigments or dyes, inventors of foods or drinks"),
                      _E(_R, 'unsplit', 'Ch. 57, the first, p. 48', "Cheerful, agreeable, fond of the arts, promiscuous, and not marrying well"),
                      _E(_R, 'general benefic', 'Ch. 57, the first, p. 48', "A benefic in the Ascendant without the malefics' aspect, with the ruler of the Ascendant and the rulers of its triplicity well situated: well nourished, and a fine first age of life", conditional=True),
                      _E(_F, 'by night', 'III.6, 1-8', "Divine talents, friends of emperors and powerful men, credited with their business affairs; good and the greatest orators by the sign: in human signs the chiefs among priests, dressed in purple or golden vestments, foretelling the future; charming, beautiful and rich if no malefic stands in her way; with Mercury portionally with her, crowned with wreaths and ribbons, musicians praised by all; in the sounding and half-voiced signs hunters with manifold efforts, friends of emperors, in charge of royal pleasures, receiving the greatest provisions and riches from the Emperor; in earthy or watery signs makers of pigments, weavers of magnificent vestments, the greatest dyers; in four-footed signs noble, glorious, good, lovers of cattle, in charge of the kings' animals", portional=True),
                      _E(_F, 'by day', 'III.6, 9-10', "Disgraced, lustful, impure; linen-weavers, feather-decorators, inventors of colors, dyers, shop-keepers or bar-keepers; regarding Saturn from any portion, soft men, bottom-boys, or those who give themselves to foul acts; (a lacuna) a wife at an early age", portional=True)],
                  'PN IV': _P(_VEN_STAKE_GOOD, 'II.18, 1-2 (shared with the 4th, 7th and 10th)', _VEN_STAKE_BAD, 'II.18, 3-4 (shared with the 4th, 7th and 10th)')},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'by day', 'Ch. 57, the first, p. 54', "Philosophers and grammarians, sometimes orators or geometers, those who know the secrets of the heavens, care for the revenues of the gods or know about secret transactions; aspected by a planet of the sect, magnates, those crowned with a wreath and entrusted with the transactions of kings; Mars casting an aspect by opposition, square or partile conjunction, thrice evil, but by trine more effective and better"),
                        _E(_R, 'by night', 'Ch. 57, the first, p. 54', "Semi-divine in their thoughts, capable in their efforts, elegant in their way of living, in charge of governmental departments, disbursements, accounting or tax revenues, accomplished according to the sign"),
                        _E(_R, 'unsplit', 'Ch. 57, the first, p. 48', "Intelligent, prudent and ingenious"),
                        _E(_F, 'by day', 'III.7, 1-2', "In the signs where he rejoices: philosophers, masters of the grammatical art, geometers, those dealing with heavenly things or gazing on the presence of the gods, experienced in sacred letters, often orators and legal advocates, especially in his own or other voiced signs; the Sun, Saturn or Jupiter regarding him, great men crowned with sacred and glorious lineages, entrusted with the greatest business of emperors", portional=True),
                        _E(_F, 'by day', 'III.7, 3-4', "Mars regarding him by square or diameter, or with him portionally in the Hour-marker: knocked down by a manifold series of evils that can be neither defined nor designated; Mars by trine, busy with good and prosperous actions", portional=True, conditional=True),
                        _E(_F, 'by night', 'III.7, 5-6', "Divine in their senses, obtaining everything they want in the easiest ways, respectable in moral qualities and serious, in charge of accounting, renting, legal instruments, taxes, business transactions, banking and lending at interest; often the spokesmen of emperors or of very important judges, according to the signs", portional=True)],
                    'PN IV': _P(_MERC_STAKE_GOOD + ' -- stronger in the Ascendant or the Midheaven', 'II.21, 1 (shared with the 4th, 7th and 10th)', _MERC_STAKE_BAD + '; ' + _MERC_RETRO, 'II.21, 2 (shared with the 4th, 7th and 10th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'in sect', 'Ch. 57, the first, pp. 54-55', "Applying to no planet, not good; in the same angle of the Ascendant, those in command, rulers, magnates and kings, when Mars is not opposite her and Saturn neither with her nor in square or opposition -- so aspected by them or by either one, short-lived infants, the exposed, or those dying violently by the nature of the signs; attended by the stars of the sect (the doryphory, the stars following her in their vespertine appearances), priests handling temple funds, high priests, presidents of the sacred games, with actions and benefits in sacred things"),
                     _E(_R, 'by day', 'Ch. 57, the first, pp. 55-56', "Angular, ship-masters or pirates or those in charge of impassable places; seen by Mars by conjunction, opposition or square, injuries and sicknesses, or in general short-lived and dying violently (and void of course in the important houses, no good)"),
                     _E(_F, 'by night', 'III.13, 1', "Full and in the signs where she rejoices: great increases of fortune, put in charge of his brothers by age and merit", portional=True),
                     _E(_F, 'by day', 'III.13, 2-4', "Helmsmen of the greater ships; with Mars and Saturn in another pivot (in square, diameter or with her), pirates famous for cruel wildness, or judges in charge of inaccessible places; no benefic regarding her so placed with the malefics, exposed, or no time of life and death at once, or a bad end of life and a violent death", portional=True)],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    2: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by night', 'Ch. 57, the second, p. 59', "The loss of children, sizable disturbances, the livelihood reduced, put to shame, the paternal and maternal inheritance lost; lazier in their actions, and some stricken by bodily illness (the sect word is the editor's, by comparison with Firmicus)"),
                       _E(_R, 'by day', 'Ch. 57, the second, p. 59', "The livelihood increases with the passage of time, though undistinguished and poor in spirit; some getting their living from watery things"),
                       _E(_R, 'general malefic', 'Ch. 57, the second, p. 56', "The malefics present here make impractical and lazy individuals and injuries around the eyes"),
                       _E(_F, 'by night', 'III.2, 8-11', "Serious illnesses and the greatest destructions; a killer of the wife and children, the inciter of the greatest and frequent turmoils, squandering the paternal and maternal assets; slower in all actions, and for some perpetual illnesses of the body; with Mars in any pivot seeing the Sun by square or diameter, or the waxing Moon likewise, miserable, destitute paupers exposing their bodies to danger in daily works (the sect word is the translator's supplement)"),
                       _E(_F, 'by day', 'III.2, 12-13', "Increases to the patrimony, slowly and step by step, though obscure, unnoticed, poor in spirit, always denying themselves anything; for some the supports of life from watery activities or moist business")],
                   'PN IV': _P('In a suitable condition, in his own house or received: his situation in his assets set right and increased; in an alien sign but received, increase in assets from a direction he had not hoped for or been aware of, and blessed by sowing; ' + _SAT_FALL_GOOD, 'II.6, 12-13',
                               'In a corrupt condition, not received: the corruption of assets, vegetation and fields, from sinking or water; ' + _SAT_FALL_BAD, 'II.6, 14')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the second, pp. 59-60', "By day or by night: inheritances from outside his family, adoption by someone, and lordship of lands and buildings"),
                        _E(_R, 'general benefic', 'Ch. 57, the second, p. 56', "The benefics dwelling here are more idle, yet denote fine things that are hoped for and grant hopes from things of the dead"),
                        _E(_F, 'unsplit', 'III.3, 8', "Foreign inheritances and adoption by foreigners; rulerships over noble estates and the greatest possessions if Mercury, himself well placed, joins him by a good ray"),
                        _E(_F, 'unsplit', 'III.3, 9', "Mars or Saturn regarding him so joined with Mercury, from any side: his standing changes often, shifted from good actions to bad, changeable in a varied diversity all his life, sometimes abounding in riches, sometimes pressed down by the burden of poverty", conditional=True)],
                    'PN IV': _P('Received, in a suitable condition: leisure and a scarcity of work, except that benefits are produced for him without seeking them, or because of the dead', 'II.9, 10 (shared with the 8th)',
                                'Made unfortunate, not received: an abundance of spending without cheerfulness, distresses, and contention because of assets; ' + _JUP_FALL_AFFLICTED, 'II.9, 11 (shared with the 8th)', shared=(_JUP_FALL_SHARED,))},
        'Mars': {'Rhetorius': [
                     _E(_R, 'by day', 'Ch. 57, the second, p. 60', "Estrangement from his own country and dwelling abroad, injuries and cuts from iron; the Moon in an angle or a succedent, a fall from a height or from a quadruped, or exposure, captivity or enslavement, abandoned and in straitened circumstances; Jupiter or Venus bearing testimony to him, the dangers are passed and they escape enslavement, not lacking the necessities of life, though sometimes pinched"),
                     _E(_R, 'by night', 'Ch. 57, the second, p. 60', "Ready for military campaigns and combat, though with dangers in those activities, and glories (the translator's question mark)"),
                     _E(_R, 'unsplit', 'Ch. 57, the second, pp. 56-57', "As ruler of the Lot of Fortune or the Ascendant: the greatest losses, and sometimes injuries around the eyes; with Venus in the house too, injuries from women; in his own face, sometimes prison guards or a livelihood from prisons", conditional=True),
                     _E(_R, 'general malefic', 'Ch. 57, the second, p. 56', "The malefics present here make impractical and lazy individuals and injuries around the eyes"),
                     _E(_F, 'by day', 'III.4, 8-11', "The greatest evils and misfortunes, especially at his morning rising: estranged from his parents and home, always wandering in foreign territories; the waxing Moon with him, opposite or square, slow, weak, often wounded by iron, weakened by cold or by four-footed beasts; some exposed, others captive or dragged from free birth into slavery, always in agitation of mind and the greatest necessities; Jupiter in his own house or bounds joining him by a good ray, they escape all dangers and are freed or ransomed from captivity, though sometimes lacking the necessities of life"),
                     _E(_F, 'by night', 'III.4, 12-13', "Soldiers or athletes, frustrated by a ready series of dangers -- present in a similar way even if Jupiter and Venus join him by a good ray")],
                 'PN IV': _P('Received, not made unfortunate: good and benefit from a direction he is not aware of', 'II.12, 11',
                             'In a bad condition, made unfortunate, not received: he will spend his money and squander it', 'II.12, 12')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the second, p. 60', "Private owners of property and a pleasant existence"),
                    _E(_R, 'unsplit', 'Ch. 57, the second, p. 57', "Ruling the Lot of Fortune or the Ascendant, partilely in his own face, with Mars in the Ascendant in a sign of Saturn: he loses his eyesight in the period of Mars and begs, or is deprived of his inheritance", conditional=True),
                    _E(_F, 'unsplit', 'III.5, 24-28', "Seeking a patrimony by his own effort, sweet and good all his life, yet listless and short-lived, hindered by various contrarieties and always worried by a trembling terror; Venus and Jupiter regarding him by trine or sextile or with him, something good suddenly and no small increase of luck, downcast and abject men brought to public affairs with increased dignity -- but with great labor, the greatest hindrance and the voluntary throwing away of the patrimony, so that he strives for and dares nothing")],
                'PN IV': _P('Received, not made unfortunate: mild-temperedness and leisure time, benefits and revenue prepared for him in accordance with his spending, without labor', 'II.15, 8 (shared with the 8th)',
                            'Made unfortunate, not received: leisure time but a scarcity of benefit, a bad condition in his way of life, negligence and laziness in what would benefit him', 'II.15, 9 (shared with the 8th)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'by day', 'Ch. 57, the second, p. 60', "Contrarieties, late marriages and disputes with women; some see the death of their wives, though not living poorly in old age"),
                      _E(_R, 'by night', 'Ch. 57, the second, p. 60', "More prosperous and, with the passage of time, more fortunate, delightful in making love and pleasing"),
                      _E(_R, 'unsplit', 'Ch. 57, the second, p. 57', "Ruling the Lot of Fortune or the Ascendant: he leaves his native country, or, matutine, acts to do with wet things; vespertine, refuge in the temples of goddesses, abounding in necessities, keeping the mysteries and rites of the gods; a good old age", conditional=True),
                      _E(_R, 'general benefic', 'Ch. 57, the second, p. 56', "The benefics dwelling here are more idle, yet denote fine things that are hoped for and grant hopes from things of the dead"),
                      _E(_F, 'by night', 'III.6, 13', "Inventors of great arts, overflowing in assets, though often struck by disgrace; luckiness conferred step by step; charming and pleasant in Venereal desires"),
                      _E(_F, 'by day', 'III.6, 14-16', "The greatest contrarieties and delayed nuptials, legal problems with a woman, some widowed by the sudden deaths of wives; the Sun or Saturn regarding her by square or diameter or with her, sterile, ineffective at Venereal acts, never taking wives, always in the loves of boys -- more powerfully by day in a house or the bounds of Saturn, Mars or Mercury")],
                  'PN IV': _P('Received, not made unfortunate: benefit from the underclass or from base work', 'II.18, 10 (shared with the 8th)',
                              'Made unfortunate, not received: the corruption of his assets, negligence and idleness, and the stagnation of his work if he practices a trade', 'II.18, 11')},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'by night', 'Ch. 57, the second, pp. 60-61', "Matutine, foolish, devious, strangers to letters and lacking a livelihood; vespertine, money-lenders, merchants, managers of others' property and clever trustees"),
                        _E(_R, 'by day', 'Ch. 57, the second, p. 61', "Fond of learning, single-minded or peculiar, learning what they have not been taught, living in filth and punishing themselves"),
                        _E(_R, 'unsplit', 'Ch. 57, the second, p. 57', "Under the Sun's beams, uninitiated and unlettered; oriental, intelligent and inquisitive, taking up what they have not learned, and skilled in astrology"),
                        _E(_F, 'unsplit', 'III.7, 7', "At his morning rising: obscure men of a wicked mind, estranged from the knowledge of letters, destitute of every support of life"),
                        _E(_F, 'by night', 'III.7, 8', "An evening star: lending at interest, business men, or those who support the care of others and the procuring of their things"),
                        _E(_F, 'by day', 'III.7, 9', "An evening star: philologists, experienced in laborious letters, not comparing their own nature to the rest of men, aspiring to what no discipline teaches; miserable in life, always in difficult straits")],
                    'PN IV': _P('Received, not made unfortunate: benefit and profit from selling and buying, his associations with people praised, a good condition', 'II.21, 10 (shared with the 8th)',
                                'In a bad condition, made unfortunate, not received: loss and a downturn in business, incriminated, blamed in his management, and he will quarrel because of assets', 'II.21, 11 (shared with the 8th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'unsplit', 'Ch. 57, the second, p. 57', "With Saturn in the Ascendant: cataracts in his eyes", conditional=True),
                     _E(_F, 'by night', 'III.13, 5', "Brilliant, with conspicuous actions, but extravagant and given to the charming pleasures of luxury; the greater assets of the patrimony conferred over the course of time"),
                     _E(_F, 'by day', 'III.13, 6-8', "The parents die at once, or he is divided from them, or they part by a dissolved marriage, the substance of the patrimony diminished by various sacrifices; others occupied by constant foreign travels, their actions divided over many places, their life subject to a foreign power; diminished in light with Saturn in the portion or sign of the Hour-marker, blindness through sudden wounds to the eyes; with Mars there instead, pains of the joints, dislocations or fractures, or the continuous illness of hemorrhoids")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    3: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'unsplit', 'Ch. 57, the third, pp. 63-64', "With Mercury there too, revelations from dreams and mysterious things; with Jupiter there and Mars absent, good fortune", conditional=True),
                       _E(_R, 'general malefic', 'Ch. 57, the third, p. 63', "A malefic here makes damages from friends and on account of brothers, and sometimes blasphemers and dangers abroad"),
                       _E(_F, 'unsplit', 'III.2, 14-16', "Idle and slow, seeking no substance from the patrimony; with Mercury and the Moon there, malicious with stupidity, sacrilegious words against the Divine, always losing the patrimony, thieves whom no prosperous outcome follows; Mars joining them in any way, accusers, sycophants, evil, malevolent, envious, malicious, always perishing with a bad death")],
                   'PN IV': _P('In his own house or received, in a suitable condition: works in which he hopes for recompense and reward by God, benefit from some of the people, the management of a building for himself or someone in his care, a journey to a place he knows or toil multiplied for a hoped-for benefit, devoted to the conditions of relatives, brothers and foreigners; alien but received, the same, except that he goes on a distant journey and finds the benefit in it', 'II.6, 7-8 (shared with the 9th)',
                               'Alien, not received: evil gossip about him because of religion and worship, evil ideas get the better of his motives, something detestable abroad, contention with family and relatives; retrograde, his opinions mixed up, doubts in his religion, a work attributed to corruption in religion; Mars looking at him harmfully adds something detestable from theft and fire', 'II.6, 9-11 (shared with the 9th)')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the third, p. 64', "With Saturn there and Mars absent: good fortune", conditional=True),
                        _E(_R, 'general benefic', 'Ch. 57, the third, p. 63', "With the ruler of the third well situated by phase, house or exaltation and aspected by a benefic: friends of great men, and benefits from friends and brothers, especially with a benefic here", conditional=True),
                        _E(_F, 'unsplit', 'III.3, 10', "Neither good nor bad but always a balanced moderation: sometimes the patrimony squandered, sometimes unhoped-for riches conferred")],
                    'PN IV': _P('Received, in a suitable condition: good things reported about him in his religion, a journey for the sake of piety, religion and good deeds, praised, delighting in brothers and reports', 'II.9, 8 (shared with the 9th)',
                                'In a bad condition, or made unfortunate, not received: reports that distress him, harms and distresses in relation to brothers, negligent in his religion and with doubts about it, something detestable if he travels', 'II.9, 9 (shared with the 9th)')},
        'Mars': {'Rhetorius': [
                     _E(_R, 'general malefic', 'Ch. 57, the third, p. 63', "A malefic here makes damages from friends and on account of brothers, and sometimes blasphemers and dangers abroad"),
                     _E(_F, 'unsplit', 'III.4, 14-18', "Glory, but with the greatest labor; he desires others' things, envies everyone, and has a bad conscience about a certain great crime; with Jupiter in the eleventh or the seventh, public administration obtained with great labor, and the greatest resources of patrimony; so joined to Jupiter, the leadership and rule of great authority and honor, surpassing all his kindred; in his own house, portions, domiciles or height with Jupiter so joined, guardians of the riverbanks, generals, deputies, commanders of the praetorians, those to whom supreme rule is handed over")],
                 'PN IV': _P('Received, not made unfortunate: he travels and gains good in it, strong and praised in his travel', 'II.12, 9 (shared with the 9th)',
                             'In a bad condition, made unfortunate, not received: evil things reported about him, some true and some false; he travels, with loss and hardship, an illness from heat, fear, and misfortune from wild animals, especially in Leo or Scorpio', 'II.12, 10 (shared with the 9th)')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the third, p. 65', "God-fearing persons, inspired by the gods, or with ignoble or servile duties in temples"),
                    _E(_R, 'unsplit', 'Ch. 57, the third, p. 62', "With the Moon there: dwelling abroad", conditional=True),
                    _E(_F, 'unsplit', 'III.5, 29-32', "A bad death for the father, and he an invalid, though good and serious in counsel; Jupiter and Mercury joined by a good ray, responsible for cities, public accounts, the imperial treasury or its legal protection, though wife and children come slowly; in his own house or those of Jupiter, Venus or Mercury, or in his own height or theirs, religious men who dedicate images of the gods and preside over the building of temples at the command of princes or judges; in other signs, ignoble duties in temples or some slavish task; Saturn or Mars joined by some ray or with him, perjurers, treacherous, made miserable by frequent censures, gluttons who haunt dirty and hidden places, irreligious words against the Divinity, seething with constant worries of the mind")],
                'PN IV': _P('Received, not made unfortunate: he travels because of the Sultan, beautiful things said about him because of religion, good from brothers and relatives, and theirs from him', 'II.15, 6 (shared with the 9th)',
                            'Made unfortunate, not received: ugly things reported about him because of religion, a journey without benefit and something detestable in it, distressed because of travelers, brothers and relatives', 'II.15, 7 (shared with the 9th)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'unsplit', 'Ch. 57, the third, p. 63', "Favors from women and gifts"),
                      _E(_R, 'in sect', 'Ch. 57, the third, p. 65', "Involved with demons or wearing rags in temples, speaking from God, philosophers, especially aspected by Saturn (this paragraph names the house of the God or the Goddess; its copy under the ninth, p. 90, gives this reading out of sect)"),
                      _E(_R, 'out of sect', 'Ch. 57, the third, p. 66', "By night, good prophets, explainers of prodigies, God-fearing persons, divines, some in sacred orders or with gifts; Saturn or Mars with her or in square or opposition, disputes on account of women, quarrels, misfortunes and ingratitudes, especially in the tropical signs; Jupiter with her or in trine, square or opposition, charming, successful, rulers of the goods of the wife, some managers of women living in the households of great men and advancing through them (the ninth's copy, p. 90, gives this reading in sect)"),
                      _E(_R, 'unsplit', 'Ch. 57, the third, p. 62', "With the Lot of Fortune in her face (especially by night) and the Moon, ruling the Lot, there in her own face: great, rich, kindness from a woman, positions of authority, dealing in spices", conditional=True),
                      _E(_R, 'general benefic', 'Ch. 57, the third, p. 63', "With the ruler of the third well situated by phase, house or exaltation and aspected by a benefic: friends of great men, and benefits from friends and brothers, especially with a benefic here", conditional=True),
                      _E(_F, 'unsplit', 'III.6, 17-18', "Good if Jupiter regards her by any ray, for he takes a wife from a temple, a priestess or the daughter of a priest; with Mercury or the Moon with her, prophets, temple wardens, or those in charge of temples and religions")],
                  'PN IV': _P('Received, not made unfortunate: he travels, with status, rank and good on the journey, good social relationships with friends, well commended, benefit from brothers, the family and relatives, some of his women travel, well dressed, he prays and is kind to the people', 'II.18, 8 (shared with the 9th)',
                              'Made unfortunate, not received: reports that distress him, a distant journey on which something detestable happens, defamed in his religion, contending with his brothers, assets ruined, something detestable from buying and selling', 'II.18, 9 (shared with the 9th)', shared=(_VEN_FALL_SHARED,))},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the third, pp. 62-63', "With Saturn there, revelations from dreams and mysterious things; with the Moon there, in his own face, ruling the Ascendant or the Lot of Fortune, he foretells the future like a god, takes part in secret mysteries, a participant in celestial things, experienced, receiving divine warnings in dreams, like a prophet", conditional=True),
                        _E(_F, 'unsplit', 'III.7, 12-14', "Priests, magicians, chief physicians, mathematicians, those who learn by themselves what no master taught, prudent and lucky, easy in every action; Jupiter by trine or sextile or with him, a great mind and divine counsels; Mars bound to them as well, the managers of kings, famous generals, powerful administrators entrusted with the royal resources, overcoming all things by their own mind and luckiness")],
                    'PN IV': _P('He travels and sees what he loves on his journey, good visions with a true interpretation, good spoken of him for his commitment to religion, increase in reason, good praised management, good for his brothers and acquaintances, praised in his situations, increase in knowledge and insight into things (II.21, 8: condition not explicitly stated; followed by an expressly adverse case in §9; placement in the suitable-condition column inferred from that contrast)', 'II.21, 8 (shared with the 9th)',
                                'In a bad condition, made unfortunate, not received: he travels, and something detestable and damage afflict him on the journey, doubts in religion and ugly things reported about him because of it, bad visions, loss in buying, selling and business, and what is between him and his brothers and the people of his house corrupted', 'II.21, 9 (shared with the 9th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'unsplit', 'Ch. 57, the third, p. 62', "Ruling the Ascendant or the Lot of Fortune, especially in her own face, of the sect, full and swift: great, rich, an adviser from the gods, moneys from sacred places, ruling cities; with the Sun following the new Moon, a priest; with Saturn, a haruspex, fond of lawsuits, causing damage, under the wrath of a god, a blasphemer; with Jupiter, a soothsayer, an interpreter of oracles, rich; with Mars, energetic and a robber of others' things, and in their own faces, especially by night, a general, a perjurer for gold, sometimes killed by the wrath of the queen; with Mercury there in his own face and ruling the Ascendant or the Lot, foretelling the future like a god; with the Sun, dwelling abroad", conditional=True),
                     _E(_F, 'by day', 'III.13, 9', "An ignoble mother, or one polluted by some mark of infamy (the sect word is the translator's supplement)"),
                     _E(_F, 'unsplit', 'III.13, 10-12', "With Jupiter: the means of livelihood from government funds or the public grain tax; Mercury joined by any ray to Jupiter and the Moon so placed, high priests of a great goddess, priests of a most powerful religion, or managers or reformers of public accounts; with Venus, actions from pigments, aromatics, wine or flowers", conditional=True),
                     _E(_F, 'by night', 'III.13, 13', "With Saturn alone: slow, obtaining nothing they desire, sacrilegious words about all religions, the greatest misfortunes from the anger of the gods", conditional=True),
                     _E(_F, 'by day', 'III.13, 14', "With Mercury and Mars: sacrilegious, despoilers of temples, unjust, impious, irreligious and murderous", conditional=True)],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    4: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by day', 'Ch. 57, the fourth, p. 69', "He piles up wealth and guards his gold"),
                       _E(_R, 'by night', 'Ch. 57, the fourth, p. 69', "Destruction of the parents, danger to the father, illnesses to the native, and an ignoble youth"),
                       _E(_R, 'unsplit', 'Ch. 57, the fourth, pp. 69-70', "Aspecting the Moon by opposition or square, or with her: childlessness, and fathers or guardians of the children of others; and it makes severe illnesses internally and affecting the stomach, widowhood to the mother, and distress from feminine complaints", conditional=True),
                       _E(_R, 'in sect', 'Ch. 57, the fourth, p. 68', "In a domicile or the exaltation of Jupiter: finders of treasure, and, especially stationary, loss of the parental inheritance; hidden things given, and the children destroyed"),
                       _E(_R, 'general malefic', 'Ch. 57, the fourth, p. 67', "Mars or Saturn here give not only a miserable death but injury and suffering, and totally destroy the ancestral possessions"),
                       _E(_F, 'by day', 'III.2, 17', "Greedy for monies, guardians of gold and silver", portional=True),
                       _E(_F, 'by night', 'III.2, 18-20', "The paternal patrimony squandered and the quick death of the father, serious illnesses and cold humors of the body, and disgrace in the first age; seeing the Moon by square or diameter, or with her in the same portion, sterility, adopting children foreign to him or tutoring little boys, deformed by the defect of perpetual bad health from hidden pains; the mother quickly a widow, serious illnesses from feminine people, worn out by constant pains of the stomach")],
                   'PN IV': _P(_SAT_STAKE_GOOD, 'II.6, 1-2 (shared with the 1st, 7th and 10th)', _SAT_STAKE_BAD, 'II.6, 3 (shared with the 1st, 7th and 10th)')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'in sect', 'Ch. 57, the fourth, p. 70', "Great commanders, persons known to rulers, in charge of public affairs, God-fearing, trusting in the gods and receiving guidance from them about the future; some with positions of importance in temples, some deemed worthy of windfalls; an enjoyable old age"),
                        _E(_R, 'out of sect', 'Ch. 57, the fourth, p. 70', "Moderate individuals, more fortunate and better known with the passage of time"),
                        _E(_R, 'unsplit', 'Ch. 57, the fourth, p. 68', "Profit from forbidden activities or from inheritances or the finding of treasure; in middle age loss, but gain in old age; the rising of good fortune and an easy death"),
                        _E(_R, 'general benefic', 'Ch. 57, the fourth, p. 67', "A good old age and death from Jupiter or Venus here"),
                        _E(_F, 'unsplit', 'III.3, 11-15', "The greatest men, jurists, proposers and interpreters of laws, legal experts; lovers of beasts of burden and farmers by the signs, the messengers of kings, generals or judges, entrusted with the imperial courier service and sent here and there at the command of princes; known to princes, in charge of public acts, committed to the protection of the gods and shown the right course by them; many know the future beforehand and have the greatest honors in temples, others discover treasures by the signs; always a happy old age until death", portional=True),
                        _E(_F, 'unsplit', 'III.3, 16-17', "Opposite the Sun: a noble and glorious father, but the patrimony squandered around middle age and got back afterwards; slower with women, and separated from the affections of his children", conditional=True),
                        _E(_F, 'by night', 'III.3, 18', "Men middling in assets, and luckier in the course of time")],
                    'PN IV': _P(_JUP_STAKE_GOOD + ' -- in the stake of the earth, because of fathers, the family, lands and real estate', 'II.9, 1-2 (shared with the 1st, 7th and 10th)', _JUP_STAKE_BAD, 'II.9, 3 (shared with the 1st, 7th and 10th)')},
        'Mars': {'Rhetorius': [
                     _E(_R, 'by night', 'Ch. 57, the fourth, p. 70', "Generals, toilers, travelers in the desert, those in charge of wild animals, foot-soldiers, royal messengers"),
                     _E(_R, 'by day', 'Ch. 57, the fourth, p. 70', "Epileptics, sickly persons and those who risk surgery because of sickness, wronged by women and suffering from their ingratitude; aspected by the Sun with Saturn with him or in square or opposition, many dangers, some lunatics and epileptics, particularly as ruler of the Lot of Fortune, the Ascendant or the Lot of the Daemon"),
                     _E(_R, 'out of sect', 'Ch. 57, the fourth, pp. 68-69', "In the sign of another planet: struck by a poisonous animal"),
                     _E(_R, 'general malefic', 'Ch. 57, the fourth, p. 67', "Mars or Saturn here give not only a miserable death but injury and suffering, and totally destroy the ancestral possessions"),
                     _E(_F, 'by night', 'III.4, 19-21', "Military men, full of labor, always spending time in desert regions, or in charge of wild animals and their tenders; sometimes overthrowers of emperors or judges by the signs; yet the life and patrimony of the parents squandered, the wounds of widowhood and ruin, one child or none, and always hindered in all his acts", portional=True),
                     _E(_F, 'by day', 'III.4, 22-25', "The whole body weakened, sluggish, unsteady, often wounded by iron, invalids, always fought and worn down by women, thanked by no one they help; the Sun regarding him by square or diameter or with him, these evils stronger and more repeated, with the greatest dangers; unsound lunatics if the Hour-marker is in a house of Mars, or the Lot of Fortune in his place or house, or the Lot of Eros or of the Daimon in a house of Mars", portional=True)],
                 'PN IV': _P(_MARS_STAKE_GOOD + '; in the other stakes, according to the indication of that stake', 'II.12, 1-2 (shared with the 1st, 7th and 10th)', _MARS_STAKE_BAD + '; in the fourth, his real estate, home and dwelling corrupted, spending, something detestable from different affairs, but then rescued', 'II.12, 3-5 (shared with the 1st, 7th and 10th)')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the fourth, pp. 70-71', "He destroys the native together with his parents, or destroys his livelihood, according to the diversity of the stars in aspect with him"),
                    _E(_F, 'unsplit', 'III.5, 33-35', "Struck by any ray of Mars and Saturn, first the father's death and the overthrow of the whole family, or the slipping away of the whole patrimony; for the most part interruptions, many annoyances, contrarieties and hindrances; yet a good old age, in which the greatest profits are conveyed to him, though unstable in the affections of his wife", portional=True)],
                'PN IV': _P(_SUN_STAKE_GOOD + ' -- in the fourth, good because of real estate, fathers and old men', 'II.15, 1-2 (shared with the 1st, 7th and 10th)', _SUN_STAKE_BAD, 'II.15, 3 (shared with the 1st, 7th and 10th)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'unsplit', 'Ch. 57, the fourth, p. 71', "More fortunate with the passage of the years, friends of magnates, charming, lacking for nothing in youth, but strongly emotional; in a tropical or bicorporeal sign, widowhoods, damages and fights over women; in fixed signs, well thought of and benefited by women, unless in Aquarius opposed by the Moon, which denotes those without posterity, sterile, unhappily married, and homosexuals"),
                      _E(_R, 'unsplit', 'Ch. 57, the fourth, pp. 67-69', "Bad marriages, but old age free from toil and trouble; aspected by Saturn or Mars, he sees the death of his wife, and in a tropical sign not one but many; alone and stationary, adulterers and effeminates; in tropical signs, especially Cancer or Capricorn, marrying whores and falling into debt through this"),
                      _E(_R, 'general benefic', 'Ch. 57, the fourth, p. 67', "A good old age and death from Jupiter or Venus here"),
                      _E(_F, 'by day', 'III.6, 19-20', "The greatest throwing away of the patrimony, constant widowhoods, and achieving anything only with difficulty; Mercury with her, joined portionally by a ray, adulterers, often punished with confiscation on account of bad administrations, public or private"),
                      _E(_F, 'by night', 'III.6, 21-23', "Praises over the course of time, the friends of great men, full of charm and decent, in their first age respectable and lacking nothing, yet wrapped up in a certain defect of the mind by the sign's nature: in the tropical or double signs, widowhoods, losses and legal cases on account of women; in the solid signs, compliant wives who love him greatly, glory and patrimonies from women -- except in Aquarius, where with the Moon in square or opposition or with her, sterile and unable to produce children, never wanting conjugal affection, always rejoicing in intercourse with boys")],
                  'PN IV': _P(_VEN_STAKE_GOOD, 'II.18, 1-2 (shared with the 1st, 7th and 10th)', _VEN_STAKE_BAD + '; in the fourth, perhaps his woman will die', 'II.18, 3-5 (shared with the 1st, 7th and 10th)')},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the fourth, pp. 68-71', "Matutine, participants in forbidden mysteries, and with Saturn or Mars aspecting him so placed, judgments, restraint or condemnation; vespertine, those who pile up money or find it, initiates in secret rites, experts in numbers, those in charge of wrestlers; in a house or the exaltation of Jupiter, the father of others' children, begetting children by a cripple"),
                        _E(_R, 'unsplit', 'Ch. 57, the fourth, p. 68', "With Venus there: harm to the matter of brothers and children; prudent persons, but false accusations and charges, especially aspected by Mars and in signs not his own; and female sorcerers", conditional=True),
                        _E(_F, 'unsplit', 'III.7, 15-18', "Sharp, in charge of public accounts, powerful with manifold teachings; at his morning rising, masters of the secrets of certain arts; with Mars and Saturn in other pivots regarding him by square or diameter, accusations, the custody of prison, and frequently condemnations; an evening star, goldsmiths or diggers for gold, masters of certain hidden arts, instructed in computation or calculation, the leaders of and people in charge of gymnastics")],
                    'PN IV': _P(_MERC_STAKE_GOOD, 'II.21, 1 (shared with the 1st, 7th and 10th)', _MERC_STAKE_BAD + '; in the seventh and fourth especially, quarreling and contention with his family; ' + _MERC_RETRO, 'II.21, 2-4 (shared with the 1st, 7th and 10th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'in sect', 'Ch. 57, the fourth, p. 71', "The mother honored, and the native's standard of living increased"),
                     _E(_R, 'out of sect', 'Ch. 57, the fourth, p. 71', "The mothers low-born but wealthy, and the natives involved in exchange, if the Sun is not in the Ascendant; with the Sun in the Ascendant, thought worthy of praise, presidency, sovereignty or a kingdom (the rest of the sentence is muddled: with Saturn or Mars by day, and by night with the Sun absent from the Ascendant)"),
                     _E(_R, 'unsplit', 'Ch. 57, the fourth, pp. 67-68', "The mother of lower birth, and with the ruler of her sign cadent a slave, with a miserable death; aspected by Saturn or Mars, hardships in secret places or on account of secret matters, restrictions and imprisonment, and burying wives and children; aspected by the benefics, profit from secret works; in a domicile of Mars aspected by a malefic, banishment and the loss of the paternal inheritance, if not in a domicile or the exaltation of Jupiter"),
                     _E(_F, 'by day', 'III.13, 15-16', "An ignoble mother, who leaves behind a surviving husband; Saturn in another pivot and Venus in the Setting, the mother transferred from noble birth to slavery, and sometimes a captive"),
                     _E(_F, 'by night', 'III.13, 17', "Full of light: a noble, rich mother, and the native supported by constant increases in riches")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    5: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by day', 'Ch. 57, the fifth, pp. 73, 97-98', "Kingships and positions of command, but with considerable delay; with the Sun in the Ascendant receiving the conjunction of the waxing Moon, rule with good, the good things in full and positions of greater authority; with the Sun not in the Ascendant, ends obtained in the course of time, ups and downs in youth, and the possessions of the young diminished (the fifth's own paragraph, p. 73, has the day word supplied by the translator from Firmicus; its copy printed under the eleventh says it)"),
                       _E(_R, 'by night', 'Ch. 57, the fifth, pp. 73, 98', "Delayed in their projects and sluggish in their actions, some deprived of their possessions and losing what they had acquired"),
                       _E(_R, 'unsplit', 'Ch. 57, the fifth, p. 72', "Very rich persons, rulers of lands, founders of buildings or countries or cities"),
                       _E(_F, 'by day', 'III.2, 21-24', "Kings and leaders, the greatest trappings of power; with the Sun in the portion of the Hour-marker and Saturn taking up the conjunction of the waxing Moon, constant luckiness and the ornaments of greater authority; with Jupiter in the Hour-marker instead, the magistrates of a lesser place governing individual cities, owners of the greatest patrimony, able to support others; with neither, taking up the waxing Moon's conjunction, assets and inheritances, but often no wife, or an unworthy marriage"),
                       _E(_F, 'by night', 'III.2, 25', "A little luckiness with the advancement of age, inconsistent in virtue, slow in all actions and idle; the waning Moon joined to him, the patrimony lost and what was sought before ruined with miserable wasting")],
                   'PN IV': _P('In his own house or received, in a suitable condition: he delights in friends, devoted to guarantees, building and expenditure on lands and things whose benefit he hopes for, and praised in that', 'II.6, 4 (shared with the 11th)',
                               'Alien, not received: distressed because of children and brothers, blamed, with a shortage in his possessions; retrograde as well, some of his dwellings or real estate destroyed, a loss in his assets, some crops and revenues corrupted; Mars looking at him harmfully adds damage in assets, the corruption of friends and distress over children', 'II.6, 5-6 (shared with the 11th)')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the fifth, pp. 74, 98', "By day and by night strong: authority for their own good (much of the paragraph is lost); in contests victors, giving divine doctrines to mortals, and perfect in their bones (the copy printed under the eleventh)"),
                        _E(_R, 'general benefic', 'Ch. 57, the fifth, p. 72', "Jupiter or Venus here ruling the Lot of Fortune or the Ascendant: the native rules through the people -- Venus especially, in her proper face and not under the Sun's beams, rules nations, charming and handsome, with honors before all if no malefic aspects her, and Jupiter too gives a good indication for what is said", conditional=True),
                        _E(_F, 'unsplit', 'III.3, 19-23', "Great increases of luckiness, honored among the greatest peoples, especially in his own domiciles, bounds or height or in the house of the Sun; Mars joined to him by a good ray, leaders with the power over life and death; without Mars, Mercury and the Moon joined to him, the administrators or accountants of kings; Venus alone joined, adorned with the affluence of luckiness; with Mars, conspicuous with royal power, his opinions like divine teachings to all men; strong in body, with powerful bones"),
                        _E(_F, 'by night', 'III.3, 32-41', "Mars, Venus or the Moon contrary in course to him: enveloped by the greatest defects by the signs' nature -- in the human signs insane passions and doing what women do, in the four-footed signs hunters in constant labors, in the earthy signs herbalists, in the watery ones fishermen living from rivers, the sea or swamps; those planets resisting him and the Moon receding, an uneven life and frequent miseries (tempered if she joins Venus by a suitable ray), and with the conjunction of no star the greatest unluckiness, labors and miseries; the Moon fleeing him into the square or opposition of Mars, violent deaths, quarrels and dangers -- so that Jupiter here by night often decrees evil calamities")],
                    'PN IV': _P('Received, in a suitable condition: blessed with children (if the root of his nativity indicated children), or delighting in the children he has', 'II.9, 6',
                                'Not received, or made unfortunate: distressed because of children, messengers and gifts', 'II.9, 7')},
        'Mars': {'Rhetorius': [
                     _E(_R, 'by night', 'Ch. 57, the fifth, pp. 74, 98', "Possessions of many good things, renowned and honored by the people, friends among the powerful, ruling their acquaintances; the Moon with him in the same domicile, degree or decan, great dynasts terrible in the army, great rulers with the power of the sword; the Moon not present, Venus there or in trine or sextile (or square, in the copy printed under the eleventh), fortunate, careful in their actions, charming, profiting in their activities"),
                     _E(_R, 'by day', 'Ch. 57, the fifth, pp. 74, 98-99', "Foreign travel hurtful and very dangerous, changes of place and living abroad on account of strange events, involved in unfavorable dangers; Jupiter or Venus beholding him help with the travel, making the time abroad profitable and the return to his own country quicker"),
                     _E(_R, 'unsplit', 'Ch. 57, the fifth, p. 72', "It makes a tyrant or general"),
                     _E(_F, 'by night', 'III.4, 26-28', "In his own or Jupiter's house, bounds or height, with Jupiter with him or regarding him by trine from his own house: the greatest glories and great increases of patrimony, every kind of luckiness, honors from the people, joined with powerful men and the greatest administrators in faithful friendship; the waning Moon joined to Mars so placed with Jupiter, all-powerful, horrible generals to whom the greatest armies are handed over, with authority over life and death and the trying of capital crimes; the Moon withdrawn but Venus with him or in trine, lucky, rich, gaining glory by themselves, graced with beauty of body", conditional=True),
                     _E(_F, 'by day', 'III.4, 29-35', "The patrimony undermined, his home often changed, always dwelling abroad because of misfortunes, in not-moderate dangers; Jupiter or Venus regarding him by trine or sextile or with him, the greatest profits from the trouble of foreign travel and a quick recall to the fatherland; the waxing Moon opposite him, the greatest labors approaching the imitation of slavery, luck only under a foreign power, advisers of the greatest judges, accusations, seditions, crowds, damages, sudden dangers, weakness of some part of the body, insanity from the excessive heat of fever, chains, prisons and confinements unless Jupiter regarding that Moon or Mars mitigates them -- freed by sudden protections; by day here he denies children without the testimony of Jupiter")],
                 'PN IV': _P('Received, not made unfortunate: delighting in the people, an increase in children, and rank and status from friends and brothers, generally from the masters of allies, wars and authorities, benefits from the working of fire and blood; time after time he plans to travel, though perhaps it is not prepared for him; if already away from his country, he returns to it', 'II.12, 6-7 (shared with the 11th)',
                             'Made unfortunate, not received: distress and accidents in the family and children, and feuding with friends and brothers', 'II.12, 8 (shared with the 11th)')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the fifth, p. 72', "In his own face: friends of kings, ruling sacred and divine persons and things"),
                    _E(_R, 'unsplit', 'Ch. 57, the fifth, p. 74', "More moderate in their fortunes, and childless or unfortunate in their children"),
                    _E(_F, 'unsplit', 'III.5, 36-39', "Lovable, honored, obtaining everything easily, all things conferred with the protection of friends; Venus in the same sign or in sextile, the trappings of crowns and bordered vestments, the greatest honors from the people; the malefics with him can decree nothing evil here, the place being good by nature, but harm the children; alone there, the luckiness less, and the children so harmed as to mean perpetual orphanhoods, or the greatest torments of pain from their misfortune")],
                'PN IV': _P('Received, not made unfortunate: a suitable condition in his food and clothing, he sees what he loves from his brothers, delights in children and increases in them and in his crops and revenue, devoted to gifts and messengers', 'II.15, 4 (shared with the 11th)',
                            'Made unfortunate, not received: distressed because of friends who have authority, who undermine him, and he will contend with them, and distressed because of children', 'II.15, 5 (shared with the 11th)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'unsplit', 'Ch. 57, the fifth, p. 74', "Good prize-fighters, victors in rich contests, masterful, lucky in everything, prophesying the future, ruling their acquaintances, subsidized by women or advanced by amorous endeavors, and acquiring property with the passage of the years"),
                      _E(_R, 'general benefic', 'Ch. 57, the fifth, p. 72', "Jupiter or Venus here ruling the Lot of Fortune or the Ascendant: the native rules through the people -- Venus especially, in her proper face and not under the Sun's beams, rules nations, charming and handsome, with honors before all if no malefic aspects her, and Jupiter too gives a good indication for what is said", conditional=True),
                      _E(_F, 'unsplit', 'III.6, 24-27', "Good and benign, easily obtaining whatever they want, crowned with ribbons and wreaths, and with Jupiter regarding her by any ray famous or ennobled by some honor; with Mercury with her, increases of life and advancement, but with the greatest infamy; victors in every engagement with the pleasing rewards of victory, powerful in all things, knowing the future beforehand, known to the powerful, the greatest patrimonies from women or wives, the greatest dignity through the protection or patronage of women or wives, all bestowed full and perfect in the course of time"),
                      _E(_F, 'unsplit', 'III.6, 28', "Taking up the light of the waxing Moon, or the waxing Moon joined to her: administrations and glories with great envy; unwavering, separated from every lie, refusing to give or accept an oath even about true things; yet always quarrelling with wives over certain suspicions, with contentious fights", conditional=True)],
                  'PN IV': _P('Received, not made unfortunate: increase in his friends, women and possessions, delighting in children and increasing in them, if the root of his nativity indicated that', 'II.18, 6 (shared with the 11th)',
                              'Made unfortunate, not received: distresses for no purpose, generally because of women and children (if he had them), and hostile to his friends', 'II.18, 7 (shared with the 11th)')},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the fifth, pp. 72-75, 99', "Secretaries of wide experience, advancing through words and entrusted with money, especially not under the Sun's beams, in proper face and not beholding Mars or Saturn; matutine (oriental), those piling up gold, finders of good things, managers of other people's money, with the bearing of a demigod, long-lived and blessed with many children; vespertine (occidental), no custodian or guardian of money but a squanderer of what was laid up in secret -- professional men or agents, those seen in accounting offices, teachers, geometers, calculators, astronomers or inventors of astronomical numbers, masters of wrestlers; receiving the conjunction of the waxing Moon, white spots on the skin (white leprosy) or easily-contracted diseases by the sign's nature; the waning Moon's, unseemly injuries or a damaged mind"),
                        _E(_F, 'unsplit', 'III.7, 19-25', "At his morning rising, the concealers of gold and riches, making the greatest resources of assets through their own foresight, in charge of others' great monies, of divine moral qualities; the greatest assets of life, and rich in children; an evening star, unable to preserve monies -- whatever was set aside squandered with lavish spending -- men of business, in charge of public accounts, and by the signs teachers, geometers or astrologers discovering the course of the stars, the leaders of gymnastics; taking up the conjunction of the waxing Moon, the whole body variegated with white blotches, or vicious illnesses by the signs; the waning Moon joined to him, unbecoming illnesses and the wanderings of insanity")],
                    'PN IV': _P('Received, not made unfortunate: much good, he befriends the nobles and authorities, profits in business, selling and buying, delights in his children or has children (if the root of his nativity indicated that)', 'II.21, 5 (shared with the 11th)',
                                'In a bad condition, made unfortunate, not received: his friends hostile to him, slowness of understanding, an ailment and illness afflicting a child of his with death feared; retrograde, he is confused', 'II.21, 6-7 (shared with the 11th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'by night', 'Ch. 57, the fifth, p. 75', "Most gracious: leaders, fortunate, illustrious, presidents, and those having good things from their parents, if she is not aspected by Saturn or Mars"),
                     _E(_R, 'by day', 'Ch. 57, the fifth, p. 75', "Foreign travel, estrangement of parents or orphanhood, but with the passage of time the native becomes fortunate"),
                     _E(_R, 'unsplit', 'Ch. 57, the fifth, p. 72', "Well situated, good fortune, especially ruling the Lot of Fortune or the Ascendant, in proper face and increasing her motion; the benefics with her give good things, while the malefics shatter them"),
                     _E(_RD, 'by night', 'III.13 fn 284', "Leaders, the fortunate and illustrious, and good things from parents, if she is not looked at by the infortunes"),
                     _E(_RD, 'by day', 'III.13 fn 284', "Foreign travel and estrangement from the parents or orphanhood in early life, with improvement in one's situation later")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    6: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by night', 'Ch. 57, the sixth, p. 78', "The extinction of his inheritance, dangers and plots for the sake of slaves, and some injured; square or opposite the Moon, unsteadiness and consumption, and Jupiter aspecting either helps to some extent but still leaves him sickly"),
                       _E(_R, 'by day', 'Ch. 57, the sixth, p. 78', "The evils will be moderate"),
                       _E(_R, 'unsplit', 'Ch. 57, the sixth, p. 76', "Injury and sickness arising from wetness, cold and black bile, sometimes paralysis, gout, gout of the feet and of the hands, according to the nature of the sign, especially by night"),
                       _E(_R, 'general malefic', 'Ch. 57, the sixth, p. 75', "The malefics here make sicknesses or injuries involving the feet, and the loss of money"),
                       _E(_F, 'unsplit', 'III.2, 26-28', "All the patrimony squandered, for he is always established badly here; illnesses, especially with the waning Moon joined to him, opposite or square -- unlucky, disgraced, erratic, never able to remain in one place; Mars regarding him by square or diameter or with him in the sign, hidden pains of the body, tuberculosis or dysentery, and death from these bad states of health if the ray of no benefic mitigates them")],
                   'PN IV': _P('In a suitable condition: few pains, from cold and moisture, and benefit from being treated for them; received and made fortunate, he recovers, the days of his illnesses few, benefiting from remedies; the lord of the sixth received and in a suitable condition, an ailment afflicts his slaves and riding animals but they escape it; ' + _SAT_FALL_GOOD, 'II.6, 16-18',
                               'In a bad condition: pleurisy and chronic illnesses; the lord of the sixth made unfortunate as well, the ruin of his slaves and riding animals; ' + _SAT_FALL_BAD, 'II.6, 16-18')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the sixth, pp. 76-78', "Loss in civil affairs; fights and disputes with those in authority; Jupiter always becomes evil in the sixth and the twelfth, especially by night"),
                        _E(_F, 'unsplit', 'III.3, 42-45', "The greatest evils: frequently exposure as an infant, especially as lord of the sign of the Hour-marker or the Midheaven, particularly by night regarding the Sun, Saturn or Mars by diameter and the waning Moon by square or with her in the same sign; taking up the light of the waxing Moon with no malefic ray, somewhat mitigated; goldsmiths, those who paint garments with gold thread, sometimes silversmiths, yet worn out with frequent subversion even so, for the place has its own privilege for malice and turns whatever star is in it to decreeing evils")],
                    'PN IV': _P('Received, in a suitable condition: the lowest people praise him, benefit from those who are confined, and enemies make peace with him', 'II.9, 12 (shared with the 12th)',
                                'Not received, or made unfortunate: illnesses from windiness and other things, and distresses from enemies or confinement; ' + _JUP_FALL_AFFLICTED, 'II.9, 13 (shared with the 12th)', shared=(_JUP_FALL_SHARED,))},
        'Mars': {'Rhetorius': [
                     _E(_R, 'unsplit', 'Ch. 57, the sixth, pp. 76-78', "Injury to the feet, harm in matters concerning slaves, uprisings of enemies and dangers abroad, and injury in the part of the body ruled by the sign he is in; cuts, burns, the bites of wild animals, attacks, wounds and attacks by robbers, especially matutine, and under the Sun's beams hidden sicknesses of the internal organs or hemorrhage by the sign, more so by day; sicknesses, injuries, dangers and plots on account of slaves or of persons convicted or arrested, especially by day; angular by day, in general the danger of sudden death"),
                     _E(_R, 'general malefic', 'Ch. 57, the sixth, p. 75', "The malefics here make sicknesses or injuries involving the feet, and the loss of money"),
                     _E(_F, 'unsplit', 'III.4, 36-37', "Many evils: he harms the children and makes an unevenness of life, and decrees illnesses according to the nature of the signs -- in the crooked signs premature death, sometimes the lame and hunchbacks; here the vices of all illnesses are determined, if the place is discovered portionally and Mars is in that sign portionally", portional=True)],
                 'PN IV': _P('Received, not made unfortunate: his body healthy, and victorious over his enemies', 'II.12, 17',
                             'Made unfortunate, not received: pains from heat and moisture, and a disturbance of the blood; Saturn looking at him or with him, an illness from bile and pus, and from black bile', 'II.12, 18-19')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the sixth, pp. 75-78', "Oppressed by superiors and in subordination; the father a slave, low-born or crippled, always a foreigner, especially with the Sun's ruler, the Lot of the Father, the Sun's twelfth-part or his triplicity rulers there too -- the parents low-born, crippled, banished or foreigners; the luminaries in the Descending Node, the parents ill-treated; Saturn also present, wandering and losses, and he leaves his things behind with sorrow; in the domicile of Venus, injured through a woman; badly configured with Mars and the Moon there, the vision damaged, and in conjunction with the Moon there, madmen, epileptics, the deranged; the father dies miserably or is condemned, injured, his property destroyed, if no star is in the tenth, but with a star in the tenth, good fortune from the parents"),
                    _E(_F, 'unsplit', 'III.5, 40-43', "Many evils, always harming, with serious and long-lasting illnesses; Mars in the Hour-marker, public actions but frequent wounds from iron, the father dying a bad death or quickly, or condemned, and every asset lost in various ways -- if no star is in the Midheaven; any star in the Midheaven, many resources, rich, affluent, the greatest luckiness prepared by the paternal succession; nothing in the Midheaven but Jupiter and Venus with him in the sixth, the evil decreed is mitigated and the whole wickedness of unluckiness hindered")],
                'PN IV': _P('Received, not made unfortunate: mild-temperedness and safety', 'II.15, 10',
                            'Made unfortunate, not received: illness, heat and dryness, and a pain in his eyes, the upper part of the body and the head', 'II.15, 11')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'unsplit', 'Ch. 57, the sixth, pp. 76-79', "Loss because of a woman or for a woman, and disputes over birth; intercourse with slave-girls, servant-girls or cripples, lacking in desire and charm; by day and by night, intercourse with lower-born women, widows or cripples, and treated unkindly by their wives, especially with nothing in the tenth; in a woman's chart, difficulties in childbirth or embryotomies; with a star in the tenth, especially by night and of the sect, fascinating and charming persons, good fortune through women, and successful; declining from the angles with the malefics holding the angles and impeding the Moon, short-lived or exposed"),
                      _E(_F, 'unsplit', 'III.6, 29-30', "By day and by night: more ignoble wives, widows or weak ones, never compliant with their husbands, if no star is in the Midheaven; in a woman's own nativity, great difficulties and great dangers from childbirth -- frequent miscarriage, or a hard birth in which a mangled fetus is brought out by doctors"),
                      _E(_F, 'by night', 'III.6, 31', "With some star in the Midheaven: charming, graceful men to whom wives are compliant, luckiness conferred by wives, and obtaining all things they wish", conditional=True),
                      _E(_F, 'unsplit', 'III.6, 32', "Estranged from the pivots, with the malefics in the pivots holding the Moon by a strong ray: exposed by the parents, and coming to a quick end", conditional=True)],
                  'PN IV': _P('Received, not made unfortunate: benefit because of the underclass, remedies, drugs, food and provisions', 'II.18, 13 (shared with the 12th)',
                              'Made unfortunate, not received: leisure time, and illness from the essence of the infortune -- Saturn, black bile; Mars, blood; the Sun, heat', 'II.18, 14', shared=(_VEN_FALL_SHARED,))},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'by day', 'Ch. 57, the sixth, p. 79', "Advancements and good fortune from talking, speech-making, business or the actions of the gods, especially with any star in the tenth"),
                        _E(_R, 'by night', 'Ch. 57, the sixth, p. 79', "Matutine, interpreters, fishermen, bird-catchers and carvers if any star is in the tenth, but with none there, evil-minded, idle, unsuccessful and conceited persons; vespertine, intelligent, sensible, discerning, in charge of offices, banking, storehouses or documents, or merchants, prospering from these; receiving the application of the Moon, public scribes, instructors of kings or financial administrators, especially with a star in the tenth"),
                        _E(_R, 'unsplit', 'Ch. 57, the sixth, p. 76', "Ill-advised persons, thieves, those wanting other people's possessions, slanderers, incompetents, and those who get a bad reputation"),
                        _E(_F, 'by day', 'III.7, 26', "A morning star (the phase and sect words are the translator's supplement): the greatest fortune prepared from the learned facility of his own speech, from acting as an advocate, from business or by a grant of the gods, especially with another star in the tenth sign"),
                        _E(_F, 'by night', 'III.7, 27-30', "A morning star with another star in the tenth, interpreters, fishermen, bird-trappers and sculptors; a morning star with no star in the tenth, malign, malicious, the originators of bad counsel, thieves desiring others' things with an envious ardor of the mind, rejoicing in others' falls, informants, pursuing all their own relatives and neighbors with bitter hatreds, idle, blind in their thinking; an evening star, prudent, serious, secretive, in charge of accounts, banking, granaries, medicines, legal instruments or business deals, with great profits from these; taking up the conjunction of the approaching Moon, the scribes of judges, authorities or the Senate, teachers established with emperors, governing the actions of their prince, especially with the Moon or any other star in the tenth")],
                    'PN IV': _P('Received, not made unfortunate: not eager for collecting assets, withdrawing from business and advantage, and acquiring benefits from the underclass and every low work', 'II.21, 12 (shared with the 12th)',
                                'In a bad condition, made unfortunate, not received: illnesses of the nature of the planet making him unfortunate (Saturn, windiness, cold, pains of the joints, nerves and veins; Mars, blood and the like), seized for a crime he did not commit, evil reported about him for what he did not do, confinement and distresses', 'II.21, 13 (shared with the 12th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'unsplit', 'Ch. 57, the sixth, p. 75', "Understand of the mother what the Sun here says of the father -- the native oppressed by superiors and in subordination, the mother a slave, low-born or crippled, always a foreigner -- and the same from her ruler and her twelfth-part -- especially, as the Sun's sentence has it for the father, when the parent's ruler, Lot, twelfth-part or triplicity rulers are here too"),
                     _E(_R, 'unsplit', 'Ch. 57, the sixth, pp. 76-77', "Diseased in the spleen; with the Sun in conjunction with her here, madmen, epileptics, deranged persons (the spleen sentence follows one on the Sun, Mars and the Moon badly configured and situated here damaging the vision, and its 'also' leaves unsettled how much of that qualification carries over)")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    7: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by day', 'Ch. 57, the seventh, p. 82', "Success after long delay, long-lived persons, treasurers; injuries of the fundament or in secret places"),
                       _E(_R, 'by night', 'Ch. 57, the seventh, p. 82', "Worse: sickly persons, those suffering from a flux, and a sickness arising from heating"),
                       _E(_R, 'unsplit', 'Ch. 57, the seventh, p. 80', "Bad marriages or marrying widows, sicknesses of the fingers, hemorrhages or fluxes from the feet; the body always injured and the birth of bad children, especially out of sect; in sect in his own domicile or exaltation, long-lived and rich in old age, though distressed in youth"),
                       _E(_F, 'by day', 'III.2, 29', "A morning star, in a diurnal nativity: good, the greatest assets once the threshold of old age is crossed, the greatest period of old age, a guardian of monies; but hidden pains of the body -- hemorrhoids, or contracted by a pain of the nerves", portional=True),
                       _E(_F, 'by night', 'III.2, 30', "An evening star: fistulas around the anus and hemorrhoids, especially with Mars regarding him by any means -- inflammations, and frequently sores erupting; the misfortunes of the wives overwhelm him with the heavy bitterness of pain", portional=True)],
                   'PN IV': _P(_SAT_STAKE_GOOD, 'II.6, 1-2 (shared with the 1st, 4th and 10th)', _SAT_STAKE_BAD, 'II.6, 3 (shared with the 1st, 4th and 10th)')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'by day', 'Ch. 57, the seventh, p. 82', "Long-lived persons, with few children, rich towards the end of their lives"),
                        _E(_R, 'by night', 'Ch. 57, the seventh, p. 82', "Moderate in their circumstances, meeting with success in their middle years"),
                        _E(_R, 'unsplit', 'Ch. 57, the seventh, p. 80', "A fine old age free from toil and trouble, windfalls or inheritances abroad and professional activities, but youth more troublesome; unhappily married, especially in a tropical sign, and unfortunate in their children or childless"),
                        _E(_F, 'by day', 'III.3, 46', "Well-supplied, wealthy people, and a long and blessed old age; but it especially turns against the place of children and the spouse, for he necessarily loses a dearest and well-mannered wife, and sees the miserable death of a beloved son or daughter", portional=True),
                        _E(_F, 'by night', 'III.3, 47', "With increasing age the increases of patrimony, but of middling assets and such resources that he lacks for nothing", portional=True)],
                    'PN IV': _P(_JUP_STAKE_GOOD + ' -- in the seventh, because of women and antagonists', 'II.9, 1-2 (shared with the 1st, 4th and 10th)', _JUP_STAKE_BAD, 'II.9, 3 (shared with the 1st, 4th and 10th)')},
        'Mars': {'Rhetorius': [
                     _E(_R, 'by day', 'Ch. 57, the seventh, p. 82', "Hard-working, wicked, violent, murderous persons, torturers and traitors; some short-lived, especially aspecting the angular Moon"),
                     _E(_R, 'by night', 'Ch. 57, the seventh, p. 82', "Sorrows and agitations in everything that they do"),
                     _E(_R, 'unsplit', 'Ch. 57, the seventh, pp. 80-82', "A bad marriage, to an adulteress; injuries to the eyes, the fingers and the feet, and trembling; the ancestral property always destroyed, especially out of sect in the domiciles of others, and old age bad, but in sect in his own domicile or exaltation the evil is moderated; by day or by night those who die violently, especially in the signs of others, from pains and cuts or falls; burns or cuts by iron or hidden pains when he rules the time; professional activities from iron, fire, the military, or violence from tortures, obligations or the killing of men"),
                     _E(_F, 'by day', 'III.4, 38-41', "The greatest evils and immense dangers (the sect word is the translator's supplement): full of labors, murderers and rascals, criminals, inventors of crimes, torturers, executioners or traitors; regarding his own house from the diameter, no violent death, for Mars never destroys his own house, but short-lived if the Hour-marker and the Moon are defended by Jupiter; the Hour-marker not in a house of Mars, short-lived, especially regarding a Moon full of light in the Hour-marker or Midheaven by square or diameter", portional=True),
                     _E(_F, 'unsplit', 'III.4, 42-43', "By day and by night violent deaths according to the quality of the signs, more manifestly in alien signs -- pains and lacerations of the body, thrown down from high places or killed, or stuck in prison with perpetual chains and deadly calamities"),
                     _E(_F, 'by night', 'III.4, 44-45', "Pains and agitation from all actions, frequent wounds from iron cured by the burning of cauterization; the hidden and immense pains of bodies, especially as lord of the times, or holding the divided periods of the times from a full Moon"),
                     _E(_F, 'unsplit', 'III.4, 46-49', "With Saturn portionally in the Hour-marker or Midheaven, or the full Moon borne to Mars (or the waning Moon to Saturn): the greatest evils and lamentable calamity -- accusations, chains, prisons, convictions or capital sentences, thrown to wild beasts, flung from high places, crushed under ruins, torn to pieces in domestic plots, killed by robbers or dying in a shipwreck, by the signs (in earthy signs wild beasts, in solid ones ruins or high places, in watery ones shipwreck, in human ones the sword); the Sun in his diameter, with him or in another pivot, burned alive", conditional=True),
                     _E(_F, 'unsplit', 'III.4, 50-51', "Before his last day of life, involved in actions of fire, iron or some violence, tortures or murders; and he hounds the place of the marriage-partner and children most vehemently")],
                 'PN IV': _P(_MARS_STAKE_GOOD + '; in the other stakes, according to the indication of that stake', 'II.12, 1-2 (shared with the 1st, 4th and 10th)', _MARS_STAKE_BAD + '; in the west, ailments, illnesses, cutting by iron and different distresses, yet victorious over his enemies', 'II.12, 3-5 (shared with the 1st, 4th and 10th)')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the seventh, pp. 81-83', "Bad luck with marriage and children, but prosperity and wealth; the cause of sickness and injuries according to the nature of the star in conjunction or aspect with him"),
                    _E(_F, 'unsplit', 'III.5, 44', "The greatest defects and illnesses, especially with Mars and Saturn regarding him by any ray or with him -- defects and illnesses according to the nature of the malefic with him (Mars, hot and fiery ones; the phrase for Saturn is lost)"),
                    _E(_F, 'unsplit', 'III.5, 45-49', "With the Moon with him or in the seventh, or portionally in the Hour-marker or the Midheaven: great men, the powerful, nobles, and those to whom the greatest positions of authority are handed, especially in his own house or height or a house of Jupiter with the Lot of Fortune found portionally in a pivot; Jupiter holding a pivot too, emperors and kings, good and benevolent, powerful generals, strong, temperate and effective administrators; with all so placed, Mars and Saturn in the second or eighth, imperial rule lost, deposed from honors and administrations, oppressed by enemies, adversaries and rivals, death by their own hands, captives or exiles", conditional=True)],
                'PN IV': _P(_SUN_STAKE_GOOD + ' -- in the seventh, devoted to different managements, victorious over enemies, healthy in his body, and he sees what he loves from women', 'II.15, 1-2 (shared with the 1st, 4th and 10th)', _SUN_STAKE_BAD, 'II.15, 3 (shared with the 1st, 4th and 10th)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'unsplit', 'Ch. 57, the seventh, pp. 80-83', "Pleasing and youthful in old age (or living with younger women), a good old age, promiscuous and unstable in marriage; setting there without any aspect of the malefics, sensible persons; in her fall the same, the youth unstable, with intercourse with slave-girls and whores; by day or by night, with the passage of time marrying younger women and marrying late, good-dispositioned; impedited in tropical, scaly or servile signs, especially by Mars, lewd and passionate persons; in a woman's chart impedited by Saturn, indecent persons and whores, especially in Capricorn"),
                      _E(_F, 'by night', 'III.6, 33', "In her own sign: a lucky old age, but a wife more slowly, and children with difficulty"),
                      _E(_F, 'unsplit', 'III.6, 34-37', "Laboring with the greatest infamies from Venereal matters, according to the nature of the sign: in the tropical, scaly or erratic signs with Mars with her or in diameter or square by a powerful ray, impure, lustful, infamous; Saturn regarding a Venus so placed with Mars by any ray, women who have sex with women from an impure and shameless lust, and these vices stronger in Capricorn or Aries")],
                  'PN IV': _P(_VEN_STAKE_GOOD, 'II.18, 1-2 (shared with the 1st, 4th and 10th)', _VEN_STAKE_BAD, 'II.18, 3-4 (shared with the 1st, 4th and 10th)')},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'by day', 'Ch. 57, the seventh, p. 83', "Not good, with Venus mediating -- by passion it gives judgment against those born not from unknowns (as the translator renders it): lewd persons, brothel-keepers and the like; Mars in opposition or dominating, short-lived persons, consumptives, fugitives and condemned persons"),
                        _E(_R, 'by night', 'Ch. 57, the seventh, p. 83', "Managing the affairs of wealthy women, good fortune from sexual matters, from numbers or the fine arts, or finders of obscure writings"),
                        _E(_R, 'unsplit', 'Ch. 57, the seventh, p. 80', "Rich, educated, prudent persons, their wives meddlesome or beastly-minded or fond of poisoning"),
                        _E(_F, 'by day', 'III.7, 31-33', "Enveloped by serious vices and burning with the desires of miserable passions, very much the lovers of boys or of young girls, allotted defiled women as wives, and often taking up the care and support of infamous men; in a house of Mars or Saturn, or another sign where he is humbled, in charge of prostitutes, pimps, dirtied by the squalors of this infamy; Mars in the diameter, in the Midheaven or in the Setting with him, a short life, tuberculosis, fugitives, exiles, the condemned"),
                        _E(_F, 'by night', 'III.7, 34-35', "The managers of rich women, the greatest luckiness on the occasion of Venusian things, the inventors of calculations, music, documents or difficult letters; often jugglers")],
                    'PN IV': _P(_MERC_STAKE_GOOD, 'II.21, 1 (shared with the 1st, 4th and 10th)', _MERC_STAKE_BAD + '; in the seventh and fourth especially, quarreling and contention with his family; ' + _MERC_RETRO, 'II.21, 2-4 (shared with the 1st, 4th and 10th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'by day', 'Ch. 57, the seventh, p. 83', "Foreign travel and dangers either from wet places or from robbers, or ill-treated by slaves, or sickly persons, especially joined to a malefic as well, when the evils become unavoidable"),
                     _E(_R, 'by night', 'Ch. 57, the seventh, p. 83', "Changes of place and foreign travel, and increases in means with the passage of time"),
                     _E(_R, 'unsplit', 'Ch. 57, the seventh, p. 81', "Hospitable; in a feminine sign aspected by the Sun and Venus by conjunction or opposition, effeminate homosexuals and those having sexual relations with two sisters; in a woman's chart, in a masculine sign with Venus aspecting from masculine signs, women who love women"),
                     _E(_RD, 'by night', 'III.13 fn 285', "Changing house, foreign travel, and more means of subsistence over time"),
                     _E(_RD, 'by day', 'III.13 fn 285', "Foreign travel and danger either from wet places or robbers; mistreated by slaves or the sick, especially if the infortunes look")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    8: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by day', 'Ch. 57, the eighth, p. 86', "Acquiring assets with the passage of time, in order to assist others, but some acquiring assets from death"),
                       _E(_R, 'by night', 'Ch. 57, the eighth, p. 86', "Banished, consumptives, and those dying a bad death"),
                       _JOINT_EIGHTH_MALEFICS,
                       _E(_F, 'by day', 'III.2, 31', "Increases in the patrimony with the advancing time of age; in a sign or the bounds of Mars, monies from others' deaths"),
                       _E(_F, 'by night', 'III.2, 32-35', "The destruction of the patrimony; the Moon regarding him by square or diameter, evil (a lacuna follows), or the Moon in a house of Saturn regarding him with preceding rays; Mars regarding a Moon so placed with Saturn from either side by any ray, violent deaths; Mars not regarding, and Jupiter regarding the Moon and Saturn by a suitable ray (the trine or the sextile), good people of a good life and opinion, but always unstable with respect to the emotions of the wife")],
                   'PN IV': _P('In his own house or received: good from the dead; ' + _SAT_FALL_GOOD, 'II.6, 15',
                               'Not received, or retrograde: the decrease of his assets and their squandering, and distresses because of ancestors and destruction; ' + _SAT_FALL_BAD, 'II.6, 15')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the eighth, pp. 85-86', "In his own domicile or exaltation, adding to his numbers, profit from matters having to do with the dead, especially without Saturn and Mars; by day and by night, acquisition and inheritance with the passage of time"),
                        _JOINT_EIGHTH_BENEFICS,
                        _E(_F, 'unsplit', 'III.3, 48', "The patrimony frequently undermined, the greatest enemies, frequent seditions from the people, always busy with malicious acts; ignoble people spewing stupid wrath, backwards, whose furor proceeds into insanity, especially with a malefic in any pivot"),
                        _E(_F, 'unsplit', 'III.3, 49', "With Mercury with him or regarded by a trine, and the waxing Moon joined to him with a good ray: the administrators of kings, or accountants, or those granted the power of making announcements; and they always die at an early age", conditional=True)],
                    'PN IV': _P('Received, in a suitable condition: leisure and a scarcity of work, except that benefits are produced for him without seeking them, or because of the dead', 'II.9, 10 (shared with the 2nd)',
                                'Made unfortunate, not received: an abundance of spending without cheerfulness, distresses, and contention because of assets; ' + _JUP_FALL_AFFLICTED, 'II.9, 11 (shared with the 2nd)', shared=(_JUP_FALL_SHARED,))},
        'Mars': {'Rhetorius': [
                     _E(_R, 'by day', 'Ch. 57, the eighth, p. 86', "Wants and disorders and dangers"),
                     _E(_R, 'unsplit', 'Ch. 57, the eighth, p. 85', "He injures the vision, especially with the Sun or the Moon in the Ascendant; with Mercury there, forgers"),
                     _JOINT_EIGHTH_MALEFICS,
                     _E(_F, 'by day', 'III.4, 52-62', "The resources of patrimonies denied, or stripped away by miserable confiscation; the Sun and Moon in his diameter, blindness by the putting out of his eyes; alone here, want, difficulties, agitations, mobs, seditions and dangers; the Moon in the second with Jupiter joining them by no ray, estranged from them and not seeing the Hour-marker, violent deaths by the nature of the signs -- in human signs killed by men via the sword, by pirates, robbers or household members, or by a judge's sentence, and without the testimony of Venus or Jupiter gladiators perishing in public view; in terrestrial signs killed in desert places; in four-footed signs caught by beasts and mangled; in watery signs submerged in swamps or the sea or killed by pirates; in solid signs slipping down from high places; in fiery signs burned"),
                     _E(_F, 'by night', 'III.4, 63-64', "Hot-headed people, dangerous but bright, settled in public or illustrious acts, though a violent or sudden death is still decreed, as those with apoplectic seizures die; Jupiter regarding him by any ray or with him, these evils for the most part mitigated")],
                 'PN IV': _P('Received, not made unfortunate: benefit because of the conditions of the dead, ancestors and inheritances', 'II.12, 13',
                             'Made unfortunate, not received: something detestable for those reasons, quarrels, and his assets squandered', 'II.12, 14')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the eighth, p. 86', "The early death of the father, and some are madmen -- the stars with him or in aspect to him show the cause; opposing the Moon with the bond not yet broken, he injures the native"),
                    _E(_F, 'unsplit', 'III.5, 67-74', "The father dies quickly; Mars or Saturn with him, opposite him or in square, strange, crazy people, heart disorders or madness by the nature of the signs; the Moon opposite him with the conjunction not yet dissolved but full of light, or with him here, and Saturn and Mars likewise in the diameter, together or square, the greatest illnesses and weaknesses -- malicious humors burdening the head, people who fall and lunatics, constant seizures, strange words, shoulders dislocated, leprous marks, impetigoes, wasting, edema, elephantiasis, lameness, hunchbacks, paralysis, the greatest parts of the body amputated, all by the nature of the signs (and similar things in the second, sixth, twelfth and eighth); Jupiter and Venus joined by some ray, with them, in trine or joined to the Moon make these illnesses curable, though a malefic taking up the approaching Moon's conjunction makes the defect perpetual, mitigated by some divinity's providence; Mercury joined with them or in sextile restrains them by some remedy or incantations, especially with a waxing Moon joined to Jupiter or a waning one to Venus")],
                'PN IV': _P('Received, not made unfortunate: mild-temperedness and leisure time, benefits and revenue prepared for him in accordance with his spending, without labor', 'II.15, 8 (shared with the 2nd)',
                            'Made unfortunate, not received: leisure time but a scarcity of benefit, a bad condition in his way of life, negligence and laziness in what would benefit him', 'II.15, 9 (shared with the 2nd)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'by day', 'Ch. 57, the eighth, p. 86', "Those who marry late, and intercourse with lower-born women or widows or young girls; the native destroyed through gonorrhea or spasms or apoplexy"),
                      _E(_R, 'by night', 'Ch. 57, the eighth, pp. 86-87', "Wealthy and rich persons who benefit from the death of women; the death quick and painless and easy"),
                      _E(_R, 'unsplit', 'Ch. 57, the eighth, p. 84', "Miserable and shameful persons"),
                      _JOINT_EIGHTH_BENEFICS,
                      _E(_F, 'by day', 'III.6, 38-40', "A wife slowly, and then an unworthy one, or a widow, or sterile, or deformed with a serious foulness; Mercury with her and a malefic regarding them by square or diameter or with them, the whole patrimony squandered or stripped away by confiscation; death by gonorrhea, a contraction, a spasm or apoplexy"),
                      _E(_F, 'by night', 'III.6, 41', "Wealthy people, with luckiness conferred by the death of great women; a quick death, without pain or any torment")],
                  'PN IV': _P('Received, not made unfortunate: benefit from the underclass or from base work, and in the eighth especially an abundance of spending', 'II.18, 10 (shared with the 2nd)',
                              'Made unfortunate, not received: leisure time and a scarcity of benefit, contention regarding assets and corruption in them', 'II.18, 12')},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'by day', 'Ch. 57, the eighth, p. 87', "Vespertine: ineffective, unsuccessful, lazy, toilsome persons"),
                        _E(_R, 'by night', 'Ch. 57, the eighth, p. 87', "Vespertine: those inheriting from unrelated persons, deserving of windfalls, fortunate, but passive and easily becoming sick"),
                        _E(_R, 'unsplit', 'Ch. 57, the eighth, pp. 84-87', "Matutine, increases of money, aiming at great actions, some sharing in responsibilities, fiduciary relationships, managing or legal documents, or inheriting from unrelated persons; as ruler of the Lot of the Daemon, unintelligent, illiterate and lazy persons; as ruler of the eighth, twelfth or sixth, under the Sun's beams with Saturn and Mars, deaf and dumb; with Mars there, forgers"),
                        _E(_F, 'by day', 'III.7, 36-37', "An evening star: slow people who dare or desire to do nothing, always hindered by laborious actions, foolishly malicious, indispensable for no actions; in a house or the portions of Saturn, deaf, especially with Saturn regarding him by any ray"),
                        _E(_F, 'by night', 'III.7, 38', "An evening star: heirs of others' patrimony, discovering concealed resources by chance, equally lucky and blessed, but with defects, and easily exhausted by constant sluggishness"),
                        _E(_F, 'by day', 'III.7, 39', "A morning star (the sect word is the translator's supplement): increasing endlessly in monies and striving for great actions; others faithful and stable guardians of concerns, commendations, actions or legal instruments, or frequently the heirs of others' patrimony")],
                    'PN IV': _P('Received, not made unfortunate: benefit and profit from selling and buying, his associations with people praised, a good condition', 'II.21, 10 (shared with the 2nd)',
                                'In a bad condition, made unfortunate, not received: loss and a downturn in business, incriminated, blamed in his management, and he will quarrel because of assets', 'II.21, 11 (shared with the 2nd)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'by night', 'Ch. 57, the eighth, p. 84', "Adding to her numbers and light, especially with Jupiter in the eleventh: profit from matters having to do with the dead and with inheritances")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    9: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by day', 'Ch. 57, the ninth, pp. 65, 89', "Initiates, chiefs of the Magi, philosophers, prophesying the future and portents from the gods, some too involved with sacred duties or in charge of priests; but it surrounds some of them with hindrances"),
                       _E(_R, 'by night', 'Ch. 57, the ninth, pp. 65, 89', "Recluses, inventors of apothegms (or speaking from God), interpreters of dreams, philosophers, some of them long-haired"),
                       _E(_R, 'unsplit', 'Ch. 57, the ninth, p. 88', "Knowledgeable about the mysteries and the interpretation of dreams"),
                       _JOINT_NINTH_MALEFICS,
                       _E(_F, 'by day', 'III.2, 36-37', "Famous magicians or reputed philosophers, or priests of temples always famed for magic; by the quality of the signs haruspices, seers and mathematicians illustrious with true interpretations, their responses known as with a certain authority of divinity; some persevering in the support of temples or put in charge of religions, others divine interpreters of dreams, and frequently long-haired philosophers (the sect word is the translator's supplement)"),
                       _E(_F, 'by night', 'III.2, 38-39', "The anger of the gods and the hatred of emperors, especially if the Moon, diminished in light, is borne to him in any manner; greater evils from the gods or the Emperor if Mars regards him along with the Moon from any side, mitigated if Jupiter's ray approaches from a suitable place, or if he is in his own bounds, in the house of the Sun or in his height with Jupiter's trine")],
                   'PN IV': _P('In his own house or received, in a suitable condition: works in which he hopes for recompense and reward by God, benefit from some of the people, the management of a building for himself or someone in his care, a journey to a place he knows or toil multiplied for a hoped-for benefit, devoted to the conditions of relatives, brothers and foreigners; alien but received, the same, except that he goes on a distant journey and finds the benefit in it', 'II.6, 7-8 (shared with the 3rd)',
                               'Alien, not received: evil gossip about him because of religion and worship, evil ideas get the better of his motives, something detestable abroad, contention with family and relatives; retrograde, his opinions mixed up, doubts in his religion, a work attributed to corruption in religion; Mars looking at him harmfully adds something detestable from theft and fire', 'II.6, 9-11 (shared with the 3rd)')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'by day', 'Ch. 57, the ninth, pp. 65, 89', "Predicting the future with portents from the gods; some consecrated, some deemed worthy of the priesthood or given inalienable privileges"),
                        _E(_R, 'by night', 'Ch. 57, the ninth, pp. 65, 89', "Unstable in their dreams, speaking falsely of the gods, fond of gold (or of oracles), relying on the hope engendered by oracular responses and considering themselves happy"),
                        _E(_R, 'general benefic', 'Ch. 57, the ninth, p. 87', "The good stars here make good fortune in foreign places and pious persons, righteous in religious observance, especially if the ruler of the house is also well situated, in its own domicile or exaltation and not aspected by the malefics"),
                        _E(_F, 'by day', 'III.3, 50-51', "Predicting future things, interpreters of the gods; some attain priesthoods, on others something of gifts from temples is conferred, but sometimes losses and an agitation of the mind"),
                        _E(_F, 'by night', 'III.3, 52', "Dreaming false dreams, frequently telling some lie about the gods, promising themselves some hope of the good allotments of the gods, and always estimating themselves as flourishing from that")],
                    'PN IV': _P('Received, in a suitable condition: good things reported about him in his religion, a journey for the sake of piety, religion and good deeds, praised, delighting in brothers and reports', 'II.9, 8 (shared with the 3rd)',
                                'In a bad condition, or made unfortunate, not received: reports that distress him, harms and distresses in relation to brothers, negligent in his religion and with doubts about it, something detestable if he travels', 'II.9, 9 (shared with the 3rd)')},
        'Mars': {'Rhetorius': [
                     _E(_R, 'unsplit', 'Ch. 57, the ninth, pp. 65, 88-89', "By day and by night: the unpunished, sophists, fearful to the gods even when they swear falsely, always having an opportunity for deliverance, and fearful to demons; without Jupiter and Venus, blasphemers; with the Lot of Fortune not in the Ascendant but in the sixth or twelfth and Saturn with him here, very unfortunate, a wanderer, afflicted by demons; not good for a woman's marriage"),
                     _JOINT_NINTH_MALEFICS,
                     _E(_F, 'unsplit', 'III.4, 67-73', "Good for life and glory; Jupiter in the Hour-marker, lucky people obtaining everything easily, and by night the greatest authority of administrations, the power of capital punishment and the leading of an army; frequently troubles in foreign travel; in a house of Mercury or Venus, learned and famous orators; in his own house, a house of Jupiter or his own height with Jupiter not in the Hour-marker, people terrible to the gods, paying no attention to perjuries, fearsome to all demons, who flee at his command -- those the common people call exorcists, easily purging a man laboring under every malign spirit")],
                 'PN IV': _P('Received, not made unfortunate: he travels and gains good in it, strong and praised in his travel', 'II.12, 9 (shared with the 3rd)',
                             'In a bad condition, made unfortunate, not received: evil things reported about him, some true and some false; he travels, with loss and hardship, an illness from heat, fear, and misfortune from wild animals, especially in Leo or Scorpio', 'II.12, 10 (shared with the 3rd)')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the ninth, pp. 88, 90', "Builders of sacred things, those who make sacred inscriptions, adorners of the gods, hymn-singers and temple-founders, famous from these; some with duties in temples, privileges or positions of authority; with the Moon there, the parents pious or foreigners, sometimes the native pious, and always travelling a lot"),
                    _E(_F, 'unsplit', 'III.5, 77-80', "The fashioners of images of the gods, worshippers of divine images, adorners of the gods, constructors of temples, writers of hymns, endlessly chanting the praises of the gods with showy ostentation, with glory and honors from these; the father lucky, and various and manifold changes of life for him and his father; a benefic in the post-ascension of the Midheaven, settled in foreign places with the greatest increases of luckiness; a malefic there instead, many troubles on foreign journeys, and death abroad with pain")],
                'PN IV': _P('Received, not made unfortunate: he travels because of the Sultan, beautiful things said about him because of religion, good from brothers and relatives, and theirs from him', 'II.15, 6 (shared with the 3rd)',
                            'Made unfortunate, not received: ugly things reported about him because of religion, a journey without benefit and something detestable in it, distressed because of travelers, brothers and relatives', 'II.15, 7 (shared with the 3rd)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'out of sect', 'Ch. 57, the ninth, pp. 66, 90', "Afflicted by demons, or wearing rags in temples, shaggy-haired, announcing something divine from the gods or from dreams, or philosophers, especially aspected by Saturn (the paragraph's copy under the third, p. 65, gives this reading in sect)"),
                      _E(_R, 'in sect', 'Ch. 57, the ninth, pp. 66, 90', "Good prophets, priests, god-fearing persons; Saturn and Mars in conjunction with her or in square or opposition, stirred up by women, condemned, unlucky and mistreated by them, especially in the tropical signs, the signs of multiple marriage; Jupiter conjunct her or in trine or square, children by their wives, charming, successful, controlling the property of their wives, some business managers of women dwelling in royal palaces and advanced through the pleas of women (the paragraph's copy under the third, p. 66, gives this reading by night, out of sect)"),
                      _E(_R, 'unsplit', 'Ch. 57, the ninth, p. 88', "Not good for marriage for a man; the marriage unstable"),
                      _E(_R, 'general benefic', 'Ch. 57, the ninth, p. 87', "The good stars here make good fortune in foreign places and pious persons, righteous in religious observance, especially if the ruler of the house is also well situated, in its own domicile or exaltation and not aspected by the malefics"),
                      _E(_F, 'by day', 'III.6, 42-43', "Struck by the constant invasion of a certain demon, staying in temples dirty, always walking around so and never cutting their hair, announcing things to men as though said by the gods, accustomed to soothsaying, and frequently the interpreters of dreams; stronger with Saturn regarding her by any ray"),
                      _E(_F, 'by night', 'III.6, 44-46', "Divine men, worshippers of the gods, instructed in sacred things and religions, and on some official duties or gifts from temples; Mars and Saturn joined to her from the diameter or square or with her, women irritated, real unhappiness with women, women always unpleasant to him, especially in the tropical signs; Jupiter regarding her from a trine or sextile or with her, women love him deeply, handsome, charming, obtaining all things easily, dominant in the matter of the wife, some undertaking the care of women or administering for them, others persevering in the palaces of kings or the houses of powerful men for what women need, advanced and obtaining the greatest increases of patrimony from these matters")],
                  'PN IV': _P('Received, not made unfortunate: he travels, with status, rank and good on the journey, good social relationships with friends, well commended, benefit from brothers, the family and relatives, some of his women travel, well dressed, he prays and is kind to the people', 'II.18, 8 (shared with the 3rd)',
                              'Made unfortunate, not received: reports that distress him, a distant journey on which something detestable happens, defamed in his religion, contending with his brothers, assets ruined, something detestable from buying and selling', 'II.18, 9 (shared with the 3rd)', shared=(_VEN_FALL_SHARED,))},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the ninth, pp. 66, 87-90', "Matutine, haruspices, holy seers, augurs, physicians, astrologers, those inspired by the gods, with their livelihood from these; in opposition to Mars, sacrilegious persons or temple-robbers; vespertine, priests, mages, physicians, and craftsmen who contrive what they have not learned; in his own domicile or exaltation or matutine, not aspected by the malefics, established in making money after the first age, leaders of the people through business, some royal secretaries, and astronomers"),
                        _E(_F, 'unsplit', 'III.7, 40-44', "Without the ray of any star, argumentative, debating dialecticians professing to know what no mastery suggested to them, malicious, and able to fulfill nothing by effective thinking; a morning star, priests, divine people, haruspices, augurs, mathematicians, astrologers, medical doctors, with the supports of life from these arts, and stronger with a benefic's ray protecting him -- lucky, blessed, great, powerful; the malefics regarding him, or in his diameter or square, irreligious, sacrilegious, the despoilers of temples, condemned and condemnable, wandering as fugitives in foreign places or oppressed by the condemnations of exile; an evening star, priests, magicians, medical doctors, artisans living from these arts, with such cleverness that they teach themselves what no master handed down")],
                    'PN IV': _P('He travels and sees what he loves on his journey, good visions with a true interpretation, good spoken of him for his commitment to religion, increase in reason, good praised management, good for his brothers and acquaintances, praised in his situations, increase in knowledge and insight into things (II.21, 8: condition not explicitly stated; followed by an expressly adverse case in §9; placement in the suitable-condition column inferred from that contrast)', 'II.21, 8 (shared with the 3rd)',
                                'In a bad condition, made unfortunate, not received: he travels, and something detestable and damage afflict him on the journey, doubts in religion and ugly things reported about him because of it, bad visions, loss in buying, selling and business, and what is between him and his brothers and the people of his house corrupted', 'II.21, 9 (shared with the 3rd)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'by night', 'Ch. 57, the ninth, pp. 67, 90-91', "Notable persons who live abroad in honor, acquisitive; some benefit from the temples of the gods, some are entrusted with the business affairs of women, good businessmen, god-fearing"),
                     _E(_R, 'by day', 'Ch. 57, the ninth, pp. 67, 91', "Inglorious persons who live abroad in dishonor, their lives disturbed by wandering and dangers; some too with ignoble or servile duties in temples, temple-servants devoted to the gods, or those who seek sanctuary or confess their sins, especially if Mars and Saturn are in aspect or Venus is aspected by the malefics"),
                     _E(_R, 'unsplit', 'Ch. 57, the ninth, p. 88', "With the Sun there: the parents pious or foreigners, sometimes the native pious, and always travelling a lot", conditional=True),
                     _E(_F, 'by night', 'III.13, 23', "Increases of luckiness; compelled to cultivate the greatest religions, and the affairs of women handed over to them (the opening words of the sentence are the translator's supplement)"),
                     _E(_F, 'by day', 'III.13, 24', "Ignoble foreigners, their lives shaken by errors and dangers; for others ignoble duties in temples, or servile jobs in temples, especially with a malefic regarding her")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    10: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by day', 'Ch. 57, the tenth, pp. 91, 93', "In his own domicile or exaltation, a rich man in public service, happy in old age, though bad for marriage and children, with an easy death, and fond of farming; leaders, especially with the Sun in the Ascendant; becoming persons, fond of agriculture and of damp"),
                       _E(_R, 'by night', 'Ch. 57, the tenth, pp. 91, 93', "He destroys the ancestral property, bad for marriage and the children, lazy persons -- gardeners, water-carriers, sailors, fishermen, ferrymen, the really poor, and those dwelling around wet woods; bunglers put in damp places; not good for the wife nor useful for children if no benefic aspects him, and little help in the mother's giving birth; angular by night he is in general not good, with sorrows because of women"),
                       _JOINT_TENTH_MALEFICS,
                       _E(_F, 'unsplit', 'III.2, 40', "Emperors, leaders, the superintendents of the Praetorian guard", portional=True),
                       _E(_F, 'by day', 'III.2, 41-43', "In the signs where he is exalted, with the Sun in the portion of the Hour-marker: respectable farmers with respected moral qualities, rich, their possessions always adjacent to the sea, rivers or swamps; the greatest assets, great glory and inheritances from greater persons, especially with Mars joined to him by no ray -- less if Mars regards him by the strongest conjunction", portional=True),
                       _E(_F, 'by night', 'III.2, 44-48', "Unluckiness, the loss of the patrimony, nuptials denied and children denied, especially in the sign, place or bounds of a malign star; Jupiter or Venus regarding him by a good ray, some of what was denied bestowed; by night in all the pivots he decrees the greatest evils, destroying wives, striking down children, the bitter pains of orphanhood; taking up the conjunction of the waning Moon, worn out by the burden of poverty down to the provisions of daily life, especially with the Moon waning in a feminine sign; taking up the rays of the waxing Moon, the unevenness of life, turned around with changeable diversity", portional=True)],
                   'PN IV': _P(_SAT_STAKE_GOOD + ' (the Midheaven the strongest of the stakes for this)', 'II.6, 1-2 (shared with the 1st, 4th and 7th)', _SAT_STAKE_BAD, 'II.6, 3 (shared with the 1st, 4th and 7th)')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'by day', 'Ch. 57, the tenth, pp. 91, 93', "Rich persons with good children, fortunate marriages and distinguished friends, though aspecting the malefics he will harm; athletic contestants, directors of public affairs, those esteemed by the people, famous, well-known, entrusted by kings and magnates with the management of their affairs, or wearing crowns throughout their whole lives"),
                        _E(_R, 'by night', 'Ch. 57, the tenth, p. 94', "Handsome, dignified persons, but easily overthrown and deprived of their livelihood"),
                        _E(_F, 'by day', 'III.3, 53-56', "The leaders of public business deals, the ten leading citizens of the greatest cities, those on whom the greatest honors are conferred by the people, brilliant, wanting to disconnect themselves from the prestige of showing off yet luxuriant in life; some handling the business affairs of great men or emperors, others crowned for their whole lives; the greatest luckiness if no malefic put in the Lower Heaven resists him -- with one resisting, whatever dignity, honors or riches were gathered at an early age are wholly diminished, squandered and subverted at a later one, unless the Setting is protected by the light of another benefic", portional=True),
                        _E(_F, 'by night', 'III.3, 57', "Respectable in moral qualities, but quickly tricked, and the patrimony lost with frequent and quick squandering")],
                    'PN IV': _P(_JUP_STAKE_GOOD + ' -- in the Midheaven, because of those having importance', 'II.9, 1-2 (shared with the 1st, 4th and 7th)', _JUP_STAKE_BAD, 'II.9, 3 (shared with the 1st, 4th and 7th)')},
        'Mars': {'Rhetorius': [
                     _E(_R, 'by day', 'Ch. 57, the tenth, pp. 91, 94', "He scatters the possessions of the parents and either kills their children or separates them; wanderers and poor individuals, a wretched youth, and bad for marriage and the children; not good -- those who accomplish nothing, hindered in their activities and efforts, seizures of their livelihood, or even fugitives who meet their end abroad"),
                     _E(_R, 'by night', 'Ch. 57, the tenth, pp. 91, 94', "These evils are moderated; unstable persons prone to dangers, leaders, rulers of life and death or military commanders, frightful to the districts"),
                     _E(_R, 'unsplit', 'Ch. 57, the tenth, p. 94', "In the same angle with the Sun: kingdoms and rulerships handed down from the fathers to the children (the translator notes that Firmicus says this of the Sun)", conditional=True),
                     _JOINT_TENTH_MALEFICS,
                     _E(_F, 'by night', 'III.4, 74-79', "In a masculine sign, his own domicile or Jupiter's masculine one, or his own height: dangerous people, often deceived in various ways, but presidents, generals, lords of life and death, fearful tribunes over provinces and cities; accepting the conjunction of an approaching Moon with Jupiter in another pivot and Mars in his own house, height or bounds, powerful generals and lords over the whole world; Jupiter averse to him and in no pivot, still armed men, prefects, tribunes or leaders of a lesser place with the power of the sword; Saturn in another pivot besides, kings, emperors, powerful and fearsome, the overthrowers or builders of cities, but short-lived, some perishing abroad or under another's power; in a feminine sign (a lacuna) still glowing with respectability, but in a masculine sign by night or a feminine one by day, children denied", portional=True),
                     _E(_F, 'by day', 'III.4, 80-81', "Ineffective men hindered in all actions of various kinds; the greatest harms and the loss of the patrimony, confiscations and condemnations; on some the burden of foreign travel, on others the greatest necessities, flight, exile, or perishing abroad or in exile", portional=True)],
                 'PN IV': _P(_MARS_STAKE_GOOD + '; in the Midheaven, he sees what he loves from the Sultan', 'II.12, 1-2 (shared with the 1st, 4th and 7th)', _MARS_STAKE_BAD + '; harsher by day in the Ascendant or the Midheaven', 'II.12, 3-5 (shared with the 1st, 4th and 7th)')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the tenth, p. 92', "With the Moon in the Midheaven too, without any aspect of the malefics: distinguished fathers, and the natives with many friends, dear to great men (his own paragraph is lost)", conditional=True),
                    _E(_F, 'by day', 'III.5, 81-82', "In his own house, a house of Jupiter or the portion of his exaltation: kings to whom absolute rule is handed over from the father, or generals likewise with paternal honors, or administrators, consuls and proconsuls by the paternal favor of dignity; Mars in another pivot, kingdoms, generalships, administrations, consulates and proconsulates by means of dangers, fights and envy", portional=True),
                    _E(_F, 'by day', 'III.5, 83-85', "With Mars in the Setting and the Moon full of light in the Hour-marker or Midheaven regarding him, the king, general, consul, proconsul or administrator becomes an exiled captive; with Mars in the second or eighth and the Moon taking his rays by diameter or square, or with him full of light, violent death, exile or captivity; Mars estranged from those places, Jupiter in the pivots or their downward post-ascensions, and the Moon full of light in a pivot joined to Jupiter by a ray, these kings or generals extend their power over the whole world", portional=True, conditional=True),
                    _E(_F, 'unsplit', 'III.5, 86-88', "Broadly in the sign of the Midheaven: noble, respectable parents, especially with a benefic regarding him, diminished if a malefic hinders him; in all the pivots with Mercury, learned, eloquent, adorned by the various studies of letters, with varied action and the greatest education; Jupiter joined to them, settled with kings because of their letters and mastery")],
                'PN IV': _P(_SUN_STAKE_GOOD + ' -- in the Ascendant or the Midheaven, renowned, a voice among the people of his class, good from the Sultan', 'II.15, 1-2 (shared with the 1st, 4th and 7th)', _SUN_STAKE_BAD, 'II.15, 3 (shared with the 1st, 4th and 7th)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'unsplit', 'Ch. 57, the tenth, pp. 91-95', "By night and by day, especially not under the Sun's beams and not afflicted: entrusted with state affairs, negotiators in the marriages of rich women, loved by women, a pleasant old age and an easy death; honored, especially unaspected by Saturn and Mars by square, opposition or conjunction, in different activities, and oriental, popular persons, musicians honored by the people; Saturn present or so aspecting, reprehensible persons, not free agents in their honors but acting under others, or entrusting their own property to others; Mars also aspecting her, very indecent and notorious; a woman so placed promiscuous, a courtesan in whorehouses all her life -- so in servile and tropical signs, the signs of Saturn or fishy signs; a man very scandalous, a sodomite doing all sorts of lewd things; the Sun in a feminine sign in the same angle with the Moon, eunuchs or castrates or hermaphrodites performing the acts of women"),
                      _E(_F, 'unsplit', 'III.6, 50-52', "Illustrious and crowned people, with great glory and the greatest fortune; with Mercury, experts in the teaching of string instruments who easily obtain what they want; Saturn and Mars joined to her by no ray and she a morning star, public musicians honored by the people"),
                      _E(_F, 'unsplit', 'III.6, 53-58', "Saturn joined to her by some ray: bad reputations, of second tier in the arts they profess, never claiming the principal place, handing their own matters to another's protection; Mars regarding her with a strong ray or with her, famous with a bad reputation; a woman so placed impure, desirous, always in the actions of prostitutes, rented out or contracted to a pimp, in the erratic or tropical signs, the houses of Saturn or the scaly ones; a man so placed with a bad reputation forever, committing impure things in a manifold way; in a feminine sign with a Moon waxing in light in a feminine sign regarding Mars by square or diameter, eunuchs, a cut chief priest of Cybele, or hermaphrodites, stirred up by a preposterous heat of desire", conditional=True)],
                  'PN IV': _P(_VEN_STAKE_GOOD, 'II.18, 1-2 (shared with the 1st, 4th and 7th)', _VEN_STAKE_BAD, 'II.18, 3-4 (shared with the 1st, 4th and 7th)')},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the tenth, pp. 92, 95', "Resourceful persons, good at everything, sensible, having good children, honored in cities and called great, with many friends; matutine, admirable and splendid actions, and aspected house-wise by one of the stars of the sect, great rulers, entrusted with the affairs of cities, kingdoms or great men, trustworthy, good, discriminating; Mars opposing him, unsuccessful in advancement, imagining themselves promoted, with their share of flights, convictions or even violent death; vespertine, changes of place and diverse living abroad, and possibly their acts made public; Saturn aspecting by opposition or square, occupations and enterprises beside water, and moist illnesses"),
                        _E(_F, 'unsplit', 'III.7, 45-47', "A morning star: admirable and great in actions; Jupiter regarding him from a trine or sextile by day, or Venus by any ray by night, great, powerful people managing the business of the greatest cities, of emperors or of powerful men, faithful, benevolent, wise, serious"),
                        _E(_F, 'unsplit', 'III.7, 48-49', "Mars regarding him from a square or the diameter: those coming to promotion struck by various kinds of offenses, frequently exiles, or sustaining the burdens of some condemnation; the Moon joined to Mars by any ray, violent deaths", conditional=True),
                        _E(_F, 'unsplit', 'III.7, 50-51', "An evening star: foreign journeys and constant changes of place, and confiscations in all actions; Saturn regarding him from the diameter or square, always occupied in watery actions and duties, or settled near the sea, a river or swamps, with vices and illnesses from an abundance of humors")],
                    'PN IV': _P(_MERC_STAKE_GOOD + ' -- stronger in the Ascendant or the Midheaven', 'II.21, 1 (shared with the 1st, 4th and 7th)', _MERC_STAKE_BAD + '; ' + _MERC_RETRO, 'II.21, 2 (shared with the 1st, 4th and 7th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'in sect', 'Ch. 57, the tenth, p. 95', "With the Midheaven itself (partilely, as the translator reads it): great rulers, kings, rulers of life and death; merely in the sign of the Midheaven, great in their actions and in the efforts entrusted to them or done for their own benefit, receiving money; applying to Saturn, or with Saturn in another angle (the words are supplied from Firmicus), often hardships and drudgery"),
                     _E(_R, 'out of sect', 'Ch. 57, the tenth, pp. 95-96', "Middling in their livelihood and actions, unsteady persons without steady jobs; the Sun neither in the Ascendant nor in the Midheaven, great rulers, commanding, frightful to kings (the translator suspects 'either ... or' for 'neither ... nor')"),
                     _E(_R, 'unsplit', 'Ch. 57, the tenth, p. 92', "With the Sun in the Midheaven too, without any aspect of the malefics: distinguished fathers, and the natives with many friends, dear to great men", conditional=True),
                     _E(_F, 'by night', 'III.13, 25-27', "Portionally in the Midheaven, in the signs where she is exalted or rejoices, waxing in light, with Jupiter protecting her by suitable rays: the greatest emperors or powerful administrators entrusted with the power of life and death, and ordinary consulates; preceding or following the portion of the Midheaven in the neighboring portions, generals, tribunes and administrators; broadly in the sign, great in all actions and adorned with various kinds of luckiness; Saturn in another pivot (some manuscripts add: or she borne towards Saturn), frequent jealousies and unluckiness", portional=True),
                     _E(_F, 'by day', 'III.13, 28-30', "Middling in life and in all actions, and deceived in frequent ways; the Sun portionally in the Hour-marker in his own house or height or a house of Jupiter, great administrators of the largest provinces or greatest cities, proconsuls, but fearsome in that administration; Jupiter protecting both or one of the two by a prosperous ray, emperors all-powerful in luckiness and justice, looked up to with the trepidation of terror")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    11: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'general malefic', 'Ch. 57, the eleventh, p. 96', "The malefics here, having significance for the nativity as rulers of lots, houses or triplicities, without any evil position (under the Sun's beams, in their fall, in opposition or retrograde): they render the nativity good, increase the good things and lessen the bad", conditional=True),
                       _E(_F, 'unsplit', 'III.2, 54', "Middling goods; if anything great is bestowed, he comes to the place and rank his father held, but only after the thirtieth year of age -- whatever he seeks before it he loses, and neither dignity nor patrimony is his until the first thirty years have passed")],
                   'PN IV': _P('In his own house or received, in a suitable condition: he delights in friends, devoted to guarantees, building and expenditure on lands and things whose benefit he hopes for, and praised in that', 'II.6, 4 (shared with the 5th)',
                               'Alien, not received: distressed because of children and brothers, blamed, with a shortage in his possessions; retrograde as well, some of his dwellings or real estate destroyed, a loss in his assets, some crops and revenues corrupted; Mars looking at him harmfully adds damage in assets, the corruption of friends and distress over children', 'II.6, 5-6 (shared with the 5th)')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'general benefic', 'Ch. 57, the eleventh, p. 96', "The benefic stars here in their own faces, domiciles, exaltations or terms: great wealth and illustrious actions", conditional=True),
                        _E(_F, 'unsplit', 'III.3, 58', "The greatest luckiness and the greatest renown, famous authorities, even the fasces and proconsular rule -- if the Sun and Venus join him by a good ray, and the full Moon, withdrawing from the south, joins a Jupiter established in the north"),
                        _E(_F, 'by night', 'III.3, 59', "All of these are diminished and lose their effectiveness"),
                        _E(_F, 'unsplit', 'III.3, 60', "Mars contrary to him, put in the eighth place: for one established in great and powerful luckiness and styled in the greatest riches, whatever honors or dignity were conferred are changed by an attack of miseries and unluckiness", conditional=True)],
                    'PN IV': _P('Received, not made unfortunate: a good way of life, delighted, well commended, and his friends have good from him', 'II.9, 4',
                                'In a bad condition, not received, or made unfortunate: many worries, bad thoughts, no work prepared for him in which there is benefit, distressed because of friends, hopes and wishes', 'II.9, 5')},
        'Mars': {'Rhetorius': [
                     _E(_R, 'general malefic', 'Ch. 57, the eleventh, p. 96', "The malefics here, having significance for the nativity as rulers of lots, houses or triplicities, without any evil position (under the Sun's beams, in their fall, in opposition or retrograde): they render the nativity good, increase the good things and lessen the bad", conditional=True),
                     _E(_F, 'unsplit', 'III.4, 82', "Many goods, increases to the patrimony and love from the people; Jupiter regarding him with a good ray along with a waning Moon, or with him or joined in a triangular association, the trappings of the greatest dignity, judges having the power of capital punishment")],
                 'PN IV': _P('Received, not made unfortunate: delighting in the people, an increase in children, and rank and status from friends and brothers, generally from the masters of allies, wars and authorities, benefits from the working of fire and blood; time after time he plans to travel, though perhaps it is not prepared for him; if already away from his country, he returns to it', 'II.12, 6-7 (shared with the 5th)',
                             'Made unfortunate, not received: distress and accidents in the family and children, and feuding with friends and brothers', 'II.12, 8 (shared with the 5th)')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the eleventh, p. 99', "Alone here: happy individuals, born of a renowned and distinguished father, and coming with the passage of time into good fortune and happiness"),
                    _E(_F, 'unsplit', 'III.5, 89-91', "Lucky and noble men, whose fathers are adorned with the greatest splendor of honor, with increases of luckiness and dignity in the course of time; greater with Jupiter with him or regarding him by trine, or Venus a morning star with him or in sextile, when all these dignities are conferred because of friends; the malefics with him or joined by some ray hinder nothing in his actions or honors, but harm the place of children alone, by the perseverance of their bitter decree")],
                'PN IV': _P('Received, not made unfortunate: a suitable condition in his food and clothing, he sees what he loves from his brothers, delights in children and increases in them and in his crops and revenue, devoted to gifts and messengers', 'II.15, 4 (shared with the 5th)',
                            'Made unfortunate, not received: distressed because of friends who have authority, who undermine him, and he will contend with them, and distressed because of children', 'II.15, 5 (shared with the 5th)')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'unsplit', 'Ch. 57, the eleventh, p. 99', "Increasing in worldly goods with the passage of time, advanced by friends, charming and good-natured; especially in the matutine phase, and even more aspected by Saturn and Mars, those with no offspring or sterile, unhappily married, sodomites or lovers of actresses, especially in the cardinal signs"),
                      _E(_R, 'general benefic', 'Ch. 57, the eleventh, p. 96', "The benefic stars here in their own faces, domiciles, exaltations or terms: great wealth and illustrious actions", conditional=True),
                      _E(_F, 'unsplit', 'III.6, 59-62', "An evening star with Mars and Saturn regarding her from any portion: sterile, unable to generate children, reaching nuptials with difficulty, often the lovers of boys or actresses, or in charge of public goods, especially in the tropical signs -- yet increasing step by step in assets, with increases of honors or patrimonies under the protection of friends, charming and handsome; accepting the conjunction of the Moon coming towards her, rich in assets, more powerful in glory, entrusted with the care of great positions of authority, the ten leading citizens of noble cities; a morning star taking up the Moon's approaching conjunction, powerful from an early age and entrusted with power early, joined with great men or emperors in the trust of friendship, adorned with the grace of charm to the last day of life -- stronger with Mercury joined to Venus and the Moon by any ray or with them", conditional=True)],
                  'PN IV': _P('Received, not made unfortunate: increase in his friends, women and possessions, delighting in children and increasing in them, if the root of his nativity indicated that', 'II.18, 6 (shared with the 5th)',
                              'Made unfortunate, not received: distresses for no purpose, generally because of women and children (if he had them), and hostile to his friends', 'II.18, 7 (shared with the 5th)')},
        'Mercury': {'Rhetorius': [
                        _E(_F, 'unsplit', 'III.7, 52', "Ingenious people, indispensable in all actions, and to whom the acts of great accounts are handed over; greater duties if he is joined to Jupiter by a suitable ray")],
                    'PN IV': _P('Received, not made unfortunate: much good, he befriends the nobles and authorities, profits in business, selling and buying, delights in his children or has children (if the root of his nativity indicated that)', 'II.21, 5 (shared with the 5th)',
                                'In a bad condition, made unfortunate, not received: his friends hostile to him, slowness of understanding, an ailment and illness afflicting a child of his with death feared; retrograde, he is confused', 'II.21, 6-7 (shared with the 5th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'by night', 'Ch. 57, the eleventh, p. 100', "Very gracious: rulers, presumptive and settled in the first place, receiving good things from their parents, if she is unaspected by Saturn or Mars"),
                     _E(_R, 'by day', 'Ch. 57, the eleventh, p. 100', "Living abroad and estrangements from the parents, or separations or orphanhood, but with the passage of time happy, prosperous individuals"),
                     _E(_F, 'unsplit', 'III.13, 31', "She decrees here what she does in the fifth place (where Firmicus's own sentence is lost; the translator's note supplies Rhetorius's fifth-house reading)")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
    12: {
        'Saturn': {'Rhetorius': [
                       _E(_R, 'by night', 'Ch. 57, the twelfth, p. 46', "The loss of the inheritance, dangers and plots on account of slaves, and those who are mentally disturbed or perverts"),
                       _E(_R, 'by day', 'Ch. 57, the twelfth, p. 46', "More moderate in these matters"),
                       _E(_R, 'unsplit', 'Ch. 57, the twelfth, p. 46', "Squaring or opposing the Moon, inconstancy and decline, unless Jupiter aspecting either the Moon or Saturn causes a partial overturn of the rulers; and they experience severe illnesses, and have internal pains", conditional=True),
                       _E(_R, 'general malefic', 'Ch. 57, the twelfth, p. 43', "The malefics here make unfavorable births and bodily sicknesses, cuttings or injuries to the feet or the mouth or the toe, and the destruction of slaves and enemies"),
                       _E(_F, 'unsplit', 'III.2, 55-56', "The risks of servile insurrection or dangers on account of slaves, and he bound to slavery; the greatest illnesses and not-middling health problems, especially with the Moon joined to him from any side and no benefic in the pivots; a benefic in any pivot mitigates these evils to some extent -- all gentler and easier by day, stronger and more violent by night")],
                   'PN IV': _P('In his own house or received: victorious over his enemies, who befriend him and praise him well, but little occupied in works that would benefit him; ' + _SAT_FALL_GOOD, 'II.6, 19',
                               'Alien and not received, or retrograde: hardship from prison, confinement and enemies; alien and in a bad condition besides, or Mars looking at him with an aspect of corruption, torment and beating; ' + _SAT_FALL_BAD, 'II.6, 20-21')},
        'Jupiter': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the twelfth, pp. 43, 46', "Uprisings of enemies; fights and dangers and litigation against superiors; never benefic in these signs, especially by night"),
                        _E(_R, 'general benefic', 'Ch. 57, the twelfth, p. 43', "The benefics do injury in the twelfth and do not bestow their good"),
                        _E(_F, 'unsplit', 'III.3, 61-64', "The greatest unluckiness, always powerful enemies who frighten him with frequent uses of power, especially with a malefic portionally in the Hour-marker or Mars in the seventh from it -- with Mars in the Setting, a violent death without a doubt; the Sun and Saturn regarding him from the diameter, the calamity stronger; by night with the waning Moon opposite him, paupers full of troubles"),
                        _E(_F, 'unsplit', 'III.3, 65-66', "With neither the Sun nor Saturn in his diameter but in good places, and no Moon waning in light conveyed to him: often powerful in some subtle trade -- goldsmiths, those who paint or gild vestments with gold, mosaic workers, workers in silver -- and the art striven for and praised if Venus is joined to him with a suitable association", conditional=True)],
                    'PN IV': _P('Received, in a suitable condition: the lowest people praise him, benefit from those who are confined, and enemies make peace with him', 'II.9, 12 (shared with the 6th)',
                                'Not received, or made unfortunate: illnesses from windiness and other things, and distresses from enemies or confinement; ' + _JUP_FALL_AFFLICTED, 'II.9, 13 (shared with the 6th)', shared=(_JUP_FALL_SHARED,))},
        'Mars': {'Rhetorius': [
                     _E(_R, 'unsplit', 'Ch. 57, the twelfth, pp. 44, 46', "The cause of sicknesses and injuries; dangers and treacheries, and not few, on account of slaves or condemned persons and associated persons, especially by day, and by night the evils more moderate; with Venus found here too, wife-slayers"),
                     _E(_R, 'general malefic', 'Ch. 57, the twelfth, p. 43', "The malefics here make unfavorable births and bodily sicknesses, cuttings or injuries to the feet or the mouth or the toe, and the destruction of slaves and enemies"),
                     _E(_F, 'by day', 'III.4, 83-85', "The greatest illnesses and evil vices, frequent plots by slaves who always want to harm him and spread manifold scandals through all his actions; frequently agitations, anguish, worries, losses and dangers on account of certain people locked up or in custody, or the condemned -- all of this mitigated by night")],
                 'PN IV': _P('Received, not made unfortunate: little occupied in what would benefit him, he sees what he loves with respect to runaways and those who are confined, and is safe from his enemies', 'II.12, 15',
                             'Made unfortunate, not received: something detestable will affect him from those directions -- runaways, the confined, enemies', 'II.12, 16')},
        'Sun': {'Rhetorius': [
                    _E(_R, 'unsplit', 'Ch. 57, the twelfth, pp. 43, 46', "Born of a father who is a slave or an outcast or low-born, or expelled from his father's kindred, or the father injured; the parents lower-born or slaves or captives, and some experience injuries or sicknesses"),
                    _E(_F, 'unsplit', 'III.5, 92-94', "Ignoble people, frequently slaves and captives, the quick death of the father and the throwing away of the entire patrimony, defects and serious illnesses; a benefic with him there still makes the greatest obscurities; a malefic with him, perpetual illnesses, the greatest vices, and perpetual slavery")],
                'PN IV': _P('Received, not made unfortunate: beautiful things spoken about him, and safe from enemies', 'II.15, 12',
                            'Made unfortunate, not received: confinement, distresses because of confined people and enemies, or banished from his country', 'II.15, 13')},
        'Venus': {'Rhetorius': [
                      _E(_R, 'by night', 'Ch. 57, the twelfth, p. 46', "Distressed by the pretenses of women; aspected by Saturn and Mercury or Mars, mental illnesses of an erotic nature; some too marry slaves or courtesans, on which account they remain childless"),
                      _E(_R, 'by day', 'Ch. 57, the twelfth, pp. 46-47', "Badly and violently ruined by women, according to the physical nature of the signs, for the goddess has various and hard-to-find ways of doing business"),
                      _E(_R, 'unsplit', 'Ch. 57, the twelfth, pp. 43-44', "Bad marriages or marriages with slaves; with Mars found here too, wife-slayers"),
                      _E(_R, 'general benefic', 'Ch. 57, the twelfth, p. 43', "The benefics do injury in the twelfth and do not bestow their good"),
                      _E(_F, 'by night', 'III.6, 63-64', "Tormented with the constant pain of sorrow because of women; Mars and Mercury regarding her by a strong ray, frequent plots by slave-girls due to desire, the burning heat of the mind and the heat of love from an instinct of foul desire; some join in marriage with slave-girls, others associate with public prostitutes in the marriage-bed, because of which offspring are denied"),
                      _E(_F, 'by day', 'III.6, 65', "Death, or the cruelty of a bad death, because of women, according to the nature and quality of the signs"),
                      _E(_F, 'unsplit', 'III.6, 69', "Saturn joined to her by any ray: minds wrapped up in perverse vices and directed to no necessary actions, especially in a nocturnal nativity", conditional=True),
                      _E(_F, 'unsplit', 'III.6, 70-71', "Joined in a ray with Jupiter: freed from the attack of the preceding malice, for he bestows the greatest goods -- increases to the patrimony through their own actions, and excluded from every disgrace of vice, if the Moon does not regard a Venus so placed with Jupiter from the diameter", conditional=True)],
                  'PN IV': _P('Received, not made unfortunate: benefit because of the underclass, remedies, drugs, food and provisions', 'II.18, 13 (shared with the 6th)',
                              'Made unfortunate, not received: something detestable and distresses in relation to enemies, the confined, confinement and punishment', 'II.18, 15', shared=(_VEN_FALL_SHARED,))},
        'Mercury': {'Rhetorius': [
                        _E(_R, 'unsplit', 'Ch. 57, the twelfth, pp. 44, 47', "Malignant persons and evil-doers; those who take care of major affairs or even employees of the state, endangered by others' affairs but also happy in them; vespertine, a busybody; grammarians, orators, geometers, students of words, benefactors of men in distress through speech, considerably wiser than the rest; opposed by Mars, becoming evil, condemned on account of state secrets, forgeries, mismanagement or slaves, or forced into a judgment"),
                        _E(_F, 'unsplit', 'III.7, 53-57', "A morning star, ingenious people settled in great business affairs or the leaders of public business, with prosperous outcomes where others are endangered; an evening star, those agitating many affairs and the discoverers of many affairs; here (as in the sixth, where the rest of the stars have idle actions) learned people, grammarians, orators, geometers, teachers selling their own speeches and defenses in constant commerce, coming to the aid of laboring men through speech, wiser than the rest and with the increases of all teachings -- if he is joined to Jupiter or struck by no ray from the malefics"),
                        _E(_F, 'unsplit', 'III.7, 58-59', "Mars regarding him from the diameter or a square, or with him: hidden informants, sly, malicious, evildoers, criminals, poisoners, acting with the false tricks of malice, condemned as evildoers or forgers of records, transactions or public acts, and dying bad deaths; the greatest condemnations and the greatest dangers by slaves or on account of slaves", conditional=True)],
                    'PN IV': _P('Received, not made unfortunate: not eager for collecting assets, withdrawing from business and advantage, and acquiring benefits from the underclass and every low work', 'II.21, 12 (shared with the 6th)',
                                'In a bad condition, made unfortunate, not received: illnesses of the nature of the planet making him unfortunate (Saturn, windiness, cold, pains of the joints, nerves and veins; Mars, blood and the like), seized for a crime he did not commit, evil reported about him for what he did not do, confinement and distresses', 'II.21, 13 (shared with the 6th)')},
        'Moon': {'Rhetorius': [
                     _E(_R, 'unsplit', 'Ch. 57, the twelfth, p. 43', "She shows for the mother what the Sun here shows for the father: a slave or an outcast or low-born, or injured (her own paragraph, p. 47, breaks off after its first words)"),
                     _E(_F, 'by night', 'III.13, 32', "A short time and space of life; with Jupiter and Venus, or one of the two, portionally in the Hour-marker, the freedom of some authority, along with luckiness"),
                     _E(_F, 'unsplit', 'III.13, 33', "With Mars and Saturn, or one of the two, portionally in the Hour-marker: many and bad misfortunes, serious accidents, and illnesses and defects for him as well as the mother", conditional=True),
                     _E(_F, 'by day', 'III.13, 34', "Alone here, with nothing in the Hour-marker: the sacrificing of the patrimony, miserable actions, and foreign travel"),
                     _E(_F, 'by night', 'III.13, 35', "Miserable, humble people, or born of a humble or servile stock and full of labor, always occupied with inglorious actions, the body or mouth made nasty by a serious foulness of odor, frequently deceived or endangered, and with some defect from the troubles of foreign travel")],
                 'PN IV': _P(_MOON_POINTER, '', _MOON_POINTER, '')},
    },
}

# The Moon in the twelve houses by her transit -- Abu Ma'shar, PN IV VII.8,
# 1-12, this app's paraphrases, one reading per house. VII.8 reads her
# transit through the houses counted from the three positions (the
# Ascendant of the root, the Ascendant of the revolution and the sign of the
# terminal point), "so long as she is in it"; it supplies no condition
# split, so each house has one reading, mixed where the sentence is mixed,
# and the page prints it as a natal analogy in its own table with the natal
# Moon's house marked. "(from this indication)" is the text's own reservation
# (fn 96): the Moon's indication alone shows this, and another indication
# could show otherwise. fn 99, 101 and 110 read "conflicting" dreams or
# simply "different"; fn 106's reading of "takes away the same" is the
# translator's guess and is marked as his. Each house's locator is PN IV
# VII.8, <house>.
_MOON_VII8_TEXTS = {
    1: "Preserved from detestable things and troubles, lively, endearing himself to the people, benefiting from harmony with them and from the intervention of kings and leaders, the pursuit of lawsuits, selling and buying, and real estate and its purchase (the sentence's premise: the Ascendant of the root, the revolution and the sign of the terminal point a single sign, with the Moon in it)",
    2: "His revenue in accordance with his expenses, distressed because of old and new assets, what is taken from him not recovered (from this indication), quarrels in various things, devoted to the mountains, elevated places and deserts, various dreams, wronged by some of his relatives, something detestable for his parents or some of his relatives",
    3: "Devoted to messengers and reports, he will mock the people, associate with leaders and see what he loves from them and from travels, foreigners and earning money; what is taken from him recovered after trouble (from this indication); handling sales and purchases harms him; he quarrels with some of his brothers and relatives and is elevated above them; something detestable for some of him and his parents; detestable dreams",
    4: "His distresses multiply, absent from his home but often returning, benefit from associating with the nobles, dreams with a suitable interpretation, harm from handling purchases and sales and from acquiring homes and lands, what is taken from him recovered, people hostile to him, a disagreement between him and his parents",
    5: "Often distressed, he treats the people of his house well, benefits from female children, frees some slaves or treats them well; travels harm him, as does associating with nobles; a loss in buying and selling, conflicting (or different) dreams, devoted to messengers and reports, what is taken from him not recovered (from this indication)",
    6: "A high opinion of himself, mistrusting people, thinking about what is evil and idle, laboring and doing the servicing work of slaves, an ailment in his hands and legs, quarrels and benefit because of them, travel and the hunting of animals, sudden good yet a loss in buying and selling, harm from acquiring and cultivating lands, what is taken from him not recovered, frequent conflicting (or different) dreams",
    7: "A disagreement between his parents; devoted to friendliness and the authoring of maxims, he delights and rejoices in different things, benefits from buying and selling and from marriage, travels to the houses of worship and benefits in that, what is taken from him recovered after a delay, suitable dreams",
    8: "Humiliation and degradation afflict him, often distressed, in need of others, provisions and expenses necessary for him; pleasing reports reach him from faraway places; devoted to the conditions of ancestors, the dead and old affairs; benefit from handling lawsuits and from the use of farms and sowing",
    9: "Devoted to reports and messengers, the authoring of maxims and associating with foreign people; banquets and labor, lively and joyful, associating with nobles; if he quarreled, something detestable from it; benefit if he traveled, bought a riding animal or acquired real estate; what is taken from him not recovered (from this indication); suitable dreams",
    10: "He increases in his power, and takes away the same (fn 106, the translator's guess: he also loses some authority); sex with some of his relatives; devoted to business, waters and moist places; he earns money and goods, plants seedlings, makes use of gardens, builds buildings; good for his children and delight in them; benefit from travel and associating with kings, from buying and selling, lawsuits and their management, and marriage; he associates with the authorities; some of what is taken from him recovered",
    11: "Assets and goods come to him, a position of authority over his family's assets and those of his parents, benefit from travel and going out to towns and villages and from handling buying and selling, he takes on debts, property taken from him not found (from this indication), often conflicting (or different) dreams",
    12: "Distressed, with much labor, absent from his home, degradation afflicts him, yet victorious over his enemies; mixed in condition, of the good and the bad; quarrels with his family; money he loaned not returned (from this indication), and what he stood guarantor for as collateral paid from his own money; travels harmful to him, as are buying and selling and bringing riding animals into his home; something detestable for his mother from the people",
}
MOON_IN_HOUSES_VII8 = {h: _half(text, f"VII.8, {h}") for h, text in _MOON_VII8_TEXTS.items()}

def evaluate_essential_dignities(planetary_data, sect):
    """Full 5-fold essential dignity hierarchy (Domicile/Exaltation/
    Triplicity/Term/Face) plus major debilities (Detriment/Fall) and
    Peregrine. Reuses get_essential_rulers() so the exact same lordship
    logic backs both a planet's own dignity score and the syzygy-degree
    lookup used elsewhere."""
    triplicity_key = 'triplicity_day' if sect == 'Diurnal' else 'triplicity_night'
    results = {}
    for planet, data in planetary_data.items():
        if planet == 'North Node': continue

        lon = data['longitude']
        current_sign = get_zodiac_sign(lon)
        rulers = get_essential_rulers(lon)

        is_domicile = rulers['domicile'] == planet
        is_exalted = rulers['exaltation'] == planet
        is_triplicity = rulers[triplicity_key] == planet
        is_term = rulers['term'] == planet
        is_face = rulers['face'] == planet
        is_detriment = current_sign in DETRIMENTS.get(planet, [])
        is_fall = current_sign in FALLS.get(planet, [])

        has_positive = is_domicile or is_exalted or is_triplicity or is_term or is_face
        is_peregrine = not has_positive

        W = ESSENTIAL_DIGNITY_WEIGHTS
        score = (is_domicile * W['domicile'] + is_exalted * W['exaltation'] + is_triplicity * W['triplicity']
                 + is_term * W['term'] + is_face * W['face']
                 - is_detriment * 5 - is_fall * 4 - is_peregrine * 5)

        labels = []
        if is_domicile: labels.append(f"Dom (+{W['domicile']})")
        if is_exalted: labels.append(f"Exalt (+{W['exaltation']})")
        if is_triplicity: labels.append(f"Trip (+{W['triplicity']})")
        if is_term: labels.append(f"Term (+{W['term']})")
        if is_face: labels.append(f"Face (+{W['face']})")
        if is_detriment: labels.append("Detriment (-5)")
        if is_fall: labels.append("Fall (-4)")
        if is_peregrine: labels.append("Peregrine (-5)")

        results[planet] = {
            'Essential Score': score,
            'Domicile': is_domicile, 'Exalt': is_exalted, 'Triplicity': is_triplicity,
            'Term': is_term, 'Face': is_face, 'Detriment': is_detriment, 'Fall': is_fall,
            'Peregrine': is_peregrine, 'Essential Labels': labels,
        }
    return results

# --- Accidental dignity reference tables ---------------------------------
JOY_HOUSES = {'Mercury': 1, 'Moon': 3, 'Venus': 5, 'Mars': 6, 'Sun': 9, 'Jupiter': 11, 'Saturn': 12}
# Which author's domain (hayz) rule is in force. "Abu Ma'shar" is VII.1,
# 37 / VII.6, 13 (sign gender fixed to the planet's own). "Masha'allah" is
# On Nativities 1.23, 17: "in its own glow (and that is if the planet was
# male, by day above the earth in a male sign, and by night under the earth
# IN A FEMALE SIGN; and if it was feminine, by night it is above the earth
# and by day under the earth)" -- the sign's gender follows the hemisphere,
# and no sign is required of the feminine planets. Dykes' note there calls
# it "at odds with later accounts". Sidebar switch; Abu Ma'shar's is the
# default because the VII.6 table this feeds is his.
DOMAIN_RULE_OPTIONS = ("Abu Ma'shar", "Masha'allah")   # the sidebar radio and the test below share these
DOMAIN_RULE = DOMAIN_RULE_OPTIONS[0]
DIURNAL_SECT_PLANETS = {'Sun', 'Jupiter', 'Saturn'}
NOCTURNAL_SECT_PLANETS = {'Moon', 'Venus', 'Mars'}

def mercury_phase_sect(lon, sun_lon):
    """Mercury's sect by the selected phase rule of Gr. Intr. IV.9, 8.

    Normalized exact equality belongs to neither directional clause. There
    is deliberately no tolerance: two unrounded positions that merely print
    in the same degree retain their respective sides. The chapter's native
    diurnal indication is evidence recorded in the unresolved result, not a
    conjunction fallback.
    """
    mercury, sun = lon % 360.0, sun_lon % 360.0
    signed = ((mercury - sun + 180.0) % 360.0) - 180.0
    if mercury == sun:
        return UnresolvedResult(
            reason='exact conjunction',
            source='Abu Maʿshar, Great Introduction IV.9, 8; ITA fn 109',
            alternatives=(('native indication', 'Diurnal'),),
            status='unassigned',
            display_label='Unassigned by phase',
            display_alternatives=False,
        )
    return signed < 0.0


def planet_sect_is_diurnal(planet, lon, sun_lon):
    """The planet's own sect. Mercury uses ``mercury_phase_sect`` everywhere;
    its company under IV.9, 10 is reported separately because the text gives
    no precedence between the two statements."""
    if planet == 'Mercury':
        return mercury_phase_sect(lon, sun_lon)
    return planet in DIURNAL_SECT_PLANETS
MASCULINE_SIGNS = {'Aries', 'Gemini', 'Leo', 'Libra', 'Sagittarius', 'Aquarius'}
FEMININE_SIGNS = {'Taurus', 'Cancer', 'Virgo', 'Scorpio', 'Capricorn', 'Pisces'}
# Mean daily motions (deg/day), used only to gauge "swift" vs. an average pace
AVERAGE_DAILY_MOTION = {'Sun': 0.9856, 'Moon': 13.1764, 'Mercury': 1.383, 'Venus': 1.2, 'Mars': 0.524, 'Jupiter': 0.083, 'Saturn': 0.034}
# A planet counts as stationary at or under this speed, in deg/day. One
# tolerance for the whole file: the Chart page's Motion column, the
# accidental scoring and the VII.6 station tests (24, 32) all read it. The
# VII.6 test used to carry its own 0.02, so a planet could be Direct on the
# Chart page and "First station (32)" on Configurations at the same moment.
# Neither source gives a figure; this is the app's own.
STATION_SPEED_TOLERANCE = 0.003
ANGLE_HOUSES = {1, 4, 7, 10}
SUCCEDENT_HOUSES = {2, 5, 8, 11}
CADENT_HOUSES = {3, 6, 9, 12}
MALEFIC_HOUSES = {6, 8, 12}  # override to -5 regardless of their normal angularity group
HOUSE_ORDINAL = {1: '1st', 2: '2nd', 3: '3rd', 4: '4th', 5: '5th', 6: '6th', 7: '7th', 8: '8th', 9: '9th', 10: '10th', 11: '11th', 12: '12th'}

# --- Solar phase: the sources' own orbs ----------------------------------
# Abu Ma'shar walks the whole synodic cycle planet by planet (Great
# Introduction VII.2): seventeen conditions for the superiors (6-34),
# sixteen for the inferiors (35-57), sixteen for the Moon (58-74). Sahl's
# On Nativities Ch.1.22, 1-8 (and Dykes' table there, which reports
# al-Biruni SS481-82 agreeing) gives the UNDER-THE-RAYS figures
# independently -- 15 for Saturn and Jupiter, 18 east for Mars, 12 east /
# 15 west for Venus and Mercury -- so those are not one author's
# idiosyncrasy. Sahl gives no "burned" boundary at all (his 6 is the
# nine-day "considered eastern" floor of 1.22, 1). Dykes's chapter-head
# table for Sahl has Mars westernizing at 18, while Sahl's own sentences
# are silent on Mars west; the burn figures and Mars's 15 west are Abu
# Ma'shar's alone (process/tae_docs/synthesis/10_on_nativities_citation_audit.md):
#
#   Saturn, Jupiter  burned to 6 deg,  under the rays to 15 deg
#                    (VII.2, 11-13; the 15 also On Nativities 1.22, 1 and 6)
#   Mars             burned to 10 deg, under the rays to 18 deg east
#                    (VII.2, 11-13; the 18 also On Nativities 1.22, 3 and 6)
#   Venus, Mercury   burned to 7 deg,  under the rays to 12 deg east,
#                    15 deg west (VII.2, 40, 48, 51-52; the 12/15 also On
#                    Nativities 1.22, 7-8). VII.2, 37 is now legible in the OCR and
#                    states it outright: "called 'burned' until there comes
#                    to be up to 7 degrees between them and the Sun"; 40
#                    repeats it ("at a full 7 degrees in longitude, then
#                    they have passed beyond burning"). 44 gives 6 on the
#                    direct-eastern approach, which Dykes flags as probably
#                    an error for 7.
#   Moon             burned to 6 deg,  under the rays to 12 deg
#                    (VII.2, 60-61 and 72-74)
#
# Three things follow that the previous uniform 8.5 deg / 15 deg pair could
# not express. The orbs are PER PLANET, not one number for all. They are
# ASYMMETRIC about the Sun -- Mars reaches 18 deg easternizing but only
# 15 deg setting, Venus and Mercury 12 deg east against 15 deg west. And
# the burned band is much tighter than 8.5 deg for everything except Mars.
#
# Note what is deliberately NOT modelled. Abu Ma'shar's fifteenth condition
# for the superiors, "in the degrees of setting" (VII.2, 30-31), runs from
# 22 deg down to 15 deg, and Sahl 1.22, 2 likewise calls Saturn and Jupiter
# "considered western" from 22 deg. That is a seven-day allowance for a
# planet that is about to go under the rays, not a planet already under
# them: Abu Ma'shar only starts saying "under the rays" at 15 deg. It is
# reported as a label below, and scored at zero.
#
# (east, west) in degrees; the split is by which side of the Sun the planet
# stands, east = rising before him = a morning star.
SOLAR_BURNED_ORB = {
    'Saturn': (6.0, 6.0), 'Jupiter': (6.0, 6.0), 'Mars': (10.0, 10.0),
    'Venus': (7.0, 7.0), 'Mercury': (7.0, 7.0), 'Moon': (6.0, 6.0),
}
SOLAR_RAYS_ORB = {
    'Saturn': (15.0, 15.0), 'Jupiter': (15.0, 15.0), 'Mars': (18.0, 15.0),
    'Venus': (12.0, 15.0), 'Mercury': (12.0, 15.0), 'Moon': (12.0, 12.0),
}
# The Moon's 12 degrees is Abu Ma'shar's (VII.2, 61 and 72-73). Sahl gives
# 15 in On Nativities 1.19, 6: "if the Moon was under the rays, she will not
# be fit ... (and that is if there was 15 degrees between her and the Sun,
# in front of him and behind him)" -- On Nativities 1.22 covers only the
# five planets, so the Moon has two witnesses that disagree. Sidebar switch.
MOON_RAYS_ORB = 12.0
# Mars going under the rays on the WESTERN side: 15 degrees is Gr. Intr.
# VII.2, 31's ("in the degrees of setting until there come to be 15 degrees
# between them and the Sun"; the default). The 18 of the switch is DYKES'S
# chapter-head table in On Nativities 1.22 and his fn 175, which works
# VII.2, 30's westernizing boundary into an 18-degree "under the rays" --
# Sahl's own sentences are silent on Mars west (the label said "Sahl's
# table" until 2026-09-11; decision sheet row 10, DEC-D-15). Both give 18
# east. Chart-page switch like the Moon's. Rare: a 3-degree band on one planet.
MARS_WEST_RAYS_18 = False

def solar_rays_orb(planet):
    """(east, west) under-the-rays limits with both switches applied."""
    if planet == 'Moon':
        return (reading('MOON_RAYS_ORB'), reading('MOON_RAYS_ORB'))
    east, west = SOLAR_RAYS_ORB.get(planet, (15.0, 15.0))
    if planet == 'Mars' and reading('MARS_WEST_RAYS_18'):
        west = 18.0
    return (east, west)
# The seven-day setting allowance (VII.2, 30-31; On Nativities 1.22, 2-4).
# VII.2, 30 gives the two figures separately: the superiors do not cease
# to be "westernizing" "until there are 22 degrees between Saturn and
# Jupiter and [the Sun] in the west (AND 18 DEGREES BETWEEN MARS AND [THE
# SUN])", after which 31 puts them "in the degrees of setting" down to 15.
# Mars carried 22 here, which is his figure from Sahl's On Nativities 1.22
# table, not from the chapter this constant cites.
SOLAR_SETTING_DEGREES = {'Saturn': 22.0, 'Jupiter': 22.0, 'Mars': 18.0}

def solar_setting_degree(planet):
    """Outer edge of the zero-scored western setting band.

    The Mars 18-west reading takes both figures in Dykes's table: under the
    rays through 18 degrees and considered western at 22. The default keeps
    Abu Ma'shar's 18-to-15 setting band.
    """
    if planet == 'Mars' and reading('MARS_WEST_RAYS_18'):
        return 22.0
    return SOLAR_SETTING_DEGREES.get(planet)

# Whether an exact boundary belongs to the inner phase. The ordinary rule
# remains eastern completion (exclusive) and western entry (inclusive), but
# VII.2 gives exceptions where side alone is insufficient. The Moon
# approaches on the east and enters both conditions at 12/6 (73-74); the
# direct eastern inferiors likewise enter at 12/6 (44-45). A direct western
# inferior has passed beyond burning at 7 (47-48), while it remains under
# the rays through exactly 15 (48). The Moon's western and optional 15-degree
# boundaries remain inclusive under the ordinary western rule.
SOLAR_PHASE_ENDPOINT_INCLUSIVE = {
    ('Moon', 'eastern', 'burned'): True,
    ('Moon', 'eastern', 'rays'): True,
    ('direct inferior', 'eastern', 'burned'): True,
    ('direct inferior', 'eastern', 'rays'): True,
    ('direct inferior', 'western', 'burned'): False,
    ('direct inferior', 'western', 'rays'): True,
}

def _solar_phase_endpoint_inclusive(planet, side, band, speed_in_lon):
    profile = ('direct inferior'
               if planet in ('Venus', 'Mercury') and speed_in_lon is not None and speed_in_lon > 0
               else planet)
    return SOLAR_PHASE_ENDPOINT_INCLUSIVE.get((profile, side, band), side == 'western')
# How VII.6, 27 and 45 read "eastern/western relative to the Sun" for the
# superiors: 'hemisphere' (the half, excluding the rays) or 'VII.2 band'
# (the easternizing/westernizing bands VII.2, 14-21 and 29-31 name). Set
# from the sidebar; see the comment at the test.
EASTERN_RULE_OPTIONS = ('hemisphere', 'VII.2 band')   # the sidebar radio and the test share these
EASTERN_RULE = EASTERN_RULE_OPTIONS[0]
# "In the heart." Abu Ma'shar fixes this at 16', reasoning from the Sun's
# own apparent diameter of about 32' (VII.2, 7-9), and Dykes notes that
# al-Biruni has 16' as well. Sahl instead says "with him in one degree"
# (The Introduction Ch.3, 87; Fifty Aphorisms #40, 79), which is measured
# separately where Sahl's own testimony is scored. The 17' this once used
# is the later Lilly-era convention and belongs to neither.
CAZIMI_ORB = 16.0 / 60.0

SOLAR_HEART_ORBS = {"Sahl": 1.0, "Abu Ma'shar": CAZIMI_ORB}


def solar_separation(lon, sun_lon):
    """Shorter unrounded separation in longitude, including zodiac wrap."""
    distance = abs((lon % 360.0) - (sun_lon % 360.0))
    return min(distance, 360.0 - distance)


def in_solar_heart(planet, lon, sun_lon, *, source):
    """Sahl's inclusive degree (Introduction 3, 87); VII.2, 7-9's 16′."""
    limit = SOLAR_HEART_ORBS[source]
    return planet != 'Sun' and solar_separation(lon, sun_lon) <= limit


def sahl_under_rays(planet, lon, sun_lon, speed_in_lon=None):
    """Sahl's heart exception; retain the selected outer solar boundaries."""
    return solar_phase(planet, lon, sun_lon, speed_in_lon, source='Sahl')[0] in ('Burned', 'Under the rays')


def solar_phase(planet, lon, sun_lon, speed_in_lon=None, *, source="Abu Ma'shar"):
    """Where a planet stands relative to the Sun, per Gr. Intr. VII.2 and
    Sahl, On Nativities Ch.1.22. Returns (phase, side, elongation) where
    phase is one of 'Cazimi', 'Burned', 'Under the rays', 'Degrees of
    setting' or None, and side is 'eastern' (rising before the Sun, a
    morning star) or 'western'.

    `source` selects only the heart: Sahl's inclusive one-degree separation
    (Introduction 3, 87; Aphorisms 74, 79), or Abu Ma'shar's inclusive 16′.
    Outer radii, motion policy and reading switches remain unchanged.

    `speed_in_lon` selects the inferiors' direction-specific endpoints and,
    on the eastern side, their burned limit. VII.2 gives them TWO eastern
    burned limits -- 7 degrees while
    they leave the Sun after the conjunction (37, 40) and, AS PRINTED, 6
    degrees when they have stationed, "go direct in the east" (43) and
    close on him again (44: "simply under the rays until there are 6
    degrees between them and [the Sun]", then 45 "burned under the rays").
    Dykes fn 43: "To me it seems this should be 7 as in 40, but this is
    what the text says." The text's 6 is applied to a direct eastern
    inferior (order CONV-SOLAR_BURNED_ORB, 2026-09-11); a retrograde one
    keeps 37/40's 7, the western side 47's 7. Without a speed the 7
    stands and endpoint ownership falls back to the side, a reading
    solar_phase_note says on the row."""
    if planet == 'Sun':
        return None, None, 0.0
    signed = ((lon - sun_lon + 180.0) % 360.0) - 180.0
    elongation = solar_separation(lon, sun_lon)
    side = 'eastern' if signed < 0 else 'western'
    idx = 0 if side == 'eastern' else 1
    if in_solar_heart(planet, lon, sun_lon, source=source):
        return 'Cazimi', side, elongation          # the named source's inclusive heart, both sides
    # Endpoint ownership is per boundary and direction. Most eastern
    # boundaries are completion (exclusive) and most western boundaries are
    # entry (inclusive); the source-linked exception table above covers the
    # Moon and direct inferiors where those assumptions reverse.
    def within(value, limit, band):
        inclusive = _solar_phase_endpoint_inclusive(
            planet, side, band, speed_in_lon)
        return value <= limit if inclusive else value < limit

    burned = SOLAR_BURNED_ORB.get(planet, (8.5, 8.5))[idx]
    if planet in ('Venus', 'Mercury') and side == 'eastern' and speed_in_lon is not None and speed_in_lon > 0:
        burned = INFERIOR_DIRECT_EASTERN_BURNED   # VII.2, 44 as printed; see the docstring
    if within(elongation, burned, 'burned'):
        return 'Burned', side, elongation
    rays = solar_rays_orb(planet)[idx]
    if within(elongation, rays, 'rays'):
        return 'Under the rays', side, elongation
    setting = solar_setting_degree(planet)
    if side == 'western' and setting is not None and elongation <= setting:
        return 'Degrees of setting', side, elongation
    return None, side, elongation

# VII.2, 44 as printed: the direct eastern inferior's burned limit. fn 43
# reads it as an error for 40's 7; the engine applies the printed 6 and
# labels the band where the two differ (6-7 degrees). CONV-SOLAR_BURNED_ORB.
INFERIOR_DIRECT_EASTERN_BURNED = 6.0

def solar_phase_note(planet, side, speed_in_lon, elongation):
    """The label suffix for the one band where VII.2, 44's printed 6 and
    fn 43's 7 disagree: a direct eastern Venus or Mercury 6-7 degrees from
    the Sun. Empty elsewhere. Without a speed the row says the 7 of 37/40
    was used."""
    if planet not in ('Venus', 'Mercury') or side != 'eastern':
        return ''
    if not (INFERIOR_DIRECT_EASTERN_BURNED <= elongation < SOLAR_BURNED_ORB[planet][0]):
        return ''
    if speed_in_lon is None:
        return " -- motion unknown: 37/40's 7 degrees used, not 44's printed 6"
    if speed_in_lon > 0:
        return " -- VII.2, 44 as printed (6 degrees), not 40's 7; Dykes fn 43: 'should be 7 as in 40'"
    return " -- retrograde in the east: 37/40's 7 degrees, not 44's 6"

# Firmicus, Mathesis III.7 (Dykes), fn 194: Mercury's phase matching the
# sect of the chart -- morning star with a diurnal nativity, evening star
# with a nocturnal one -- is the case the successful examples share (III.7,
# 8, 26, 29); the other two pairings are the mismatch fn 186 names for
# the second place (7; and 9, the evening star by day) and 27-28 show for
# the sixth. The sect a morning star takes is Abu Ma'shar's rule, not only
# Firmicus's translator's: Gr. Intr. IV.9 in ITA V.11, rising before the Sun
# he is diurnal, setting after him nocturnal (planet_sect_is_diurnal; 3.14).
# A statement about every
# chart, so the row is always returned. Display only: nothing scores it.
MERCURY_PHASE_SECT_READING = {
    True: "the success that comes from Mercury's phase matching that of the chart",
    False: "less respected and independent uses of the intellect and skill",
}

def evaluate_mercury_phase_sect(planetary_data, sect):
    """Mathesis III.7, fn 194: does Mercury's phase match the sect? Morning
    star (eastern, rising before the Sun) with Diurnal, evening star
    (western) with Nocturnal. One row, always; the reading is fn 194's
    phrase for each case. Display only."""
    merc = planetary_data['Mercury']
    result = mercury_phase_sect(merc['longitude'], planetary_data['Sun']['longitude'])
    if isinstance(result, UnresolvedResult):
        phase = result
        match = result
        reading_text = ('The native indication is diurnal, but the adopted phase rule makes no assignment '
                        'at exact conjunction.')
    else:
        phase = 'morning star (eastern)' if result else 'evening star (western)'
        match = result == (sect == 'Diurnal')
        reading_text = MERCURY_PHASE_SECT_READING[match]
    return [{
        'Mercury': get_degree_string(merc['longitude']),
        'Phase': phase,
        'Sect': sect,
        'Match': match if isinstance(match, UnresolvedResult) else ('Yes' if match else 'No'),
        'Reading': reading_text,
    }]

# --- Mars in his own domicile, by the sect of the chart (Abu Bakr) --------
# Abu Bakr, On Nativities II.1.0 (PN II p. 142): one paragraph on Mars,
# read from the photograph. The condition it states is Mars IN HIS OWN
# DOMICILE, by night or by day -- not "of the sect" or "contrary to the
# sect" at large; the paragraph also has Mars in a domicile of Saturn, and
# a back-reference for Mars in the Midheaven "rejoicing in his own place".
# Each sentence is carried whole and verbatim, the footnote marker
# dropped (fn 652 on "unsound" is named in the page's notes). Read: own
# domicile = Aries or Scorpio; a domicile of Saturn = Capricorn or
# Aquarius; the Midheaven = the whole-sign tenth, "he would rejoice in his
# own place" quoted, not tested; the fortune's aspect on "a Mars so
# disposed" quoted in the notes, not tested. The other planets of the
# same lesson have no such witness here and are not built (decision 13).
# Supplement only; display only: nothing scores it. Pinned in
# tests/test_mars_abubakr_2026_09_15.py.
ABU_BAKR_MARS_II_1_0 = {
    'source': 'Abu Bakr, On Nativities II.1.0',
    'Nocturnal': ('in his own domicile, in a nocturnal nativity',
                  'Mars in a nocturnal nativity, appearing in his own domicile, signifies that the native will be one '
                  'master, and he will have others under himself, and he will be a good soldier, and fortunate in '
                  'slaughter and in wars, and always conquering, and wise in wars.'),
    'Diurnal': ('in his own domicile, in a diurnal nativity',
                'And if he were in his own domicile in a diurnal nativity, the native will be lazy in those things in '
                'which he ought to make money; infirm, greedy, an evil plunderer of strangers, a fornicator, esteeming '
                'murders and evils, violent and unsound.'),
    'Saturn': ('in a domicile of Saturn',
               'And if Mars were in a domicile of Saturn, the native will have a fatty liver, he will be quick in his '
               'acts, he will put the evil which is inflicted upon him into his own heart, and will retain it for a '
               'long time; and he will express his words hastily.'),
    'Midheaven': ('in the Midheaven (the whole-sign tenth)',
                  'And it was already stated that if Mars would appear in the Midheaven, and he would rejoice in his '
                  'own place, the native will be fortunate in war and slaughter, and a beautiful organizer of wars, '
                  'and wise in them.'),
    'Fortune': ('And if a fortune aspected a Mars so disposed, the native will acquire advantage and great fame from '
                'his wars.'),
    'fn652': ('Insanus, the root of our "insane." Nowadays this word has clinical overtones that are not present in '
              'the Latin, though Latins would have seen it as roughly equivalent to "crazy," which is a much looser '
              'notion.'),
}
ABU_BAKR_MARS_NO_SENTENCE = 'in none of his own domiciles, nor of Saturn\'s, nor in the Midheaven: no sentence of II.1.0 reaches him'

def evaluate_mars_abu_bakr(planetary_data, sect, ascendant_lon):
    """Abu Bakr, On Nativities II.1.0 on Mars: a row when he stands in Aries
    or Scorpio (his own domicile), with the sentence for the chart's sect;
    a row when he stands in Capricorn or Aquarius (a domicile of Saturn);
    a second row when he stands in the whole-sign tenth (the Midheaven
    sentence, its "rejoice in his own place" quoted, not tested); one row
    saying so when none of these reaches him. Display only."""
    lon = planetary_data['Mars']['longitude'] % 360.0
    sign = get_zodiac_sign(lon)
    src = ABU_BAKR_MARS_II_1_0['source']
    rows = []

    def row(case, text):
        rows.append({'Mars': get_degree_string(lon), 'Sign': sign, "Chart's sect": sect,
                     'Case': case, 'Source': src, 'Text': text})

    if sign in DOMICILES['Mars']:
        case, text = ABU_BAKR_MARS_II_1_0['Diurnal' if sect == 'Diurnal' else 'Nocturnal']
        if sect != 'Diurnal':
            row(case, text)
        else:
            row(case, text + ' (fn 652 on "unsound": ' + ABU_BAKR_MARS_II_1_0['fn652'] + ')')
    elif sign in DOMICILES['Saturn']:
        row(*ABU_BAKR_MARS_II_1_0['Saturn'])
    if get_wsh_house(lon, ascendant_lon) == 10:
        row(*ABU_BAKR_MARS_II_1_0['Midheaven'])
    if not rows:
        row(ABU_BAKR_MARS_NO_SENTENCE, '')
    return rows

# --- Affliction and fortification after Rhetorius (Holden) ----------------
# Chapters 26 (Dominance), 27 (Affliction and Ineffective Houses), 28
# (Effective Houses), 41 (Besieging) and 42 (Fortified Stars), each
# condition in the text's own words. Two degrees are in the text (Ch. 41:
# seven; Ch. 34, where Ch. 27's footnote sends the word kollesis: three);
# lunar application uses the Moon's daily motion (Ch. 35). Each condition
# keeps its own definition. Display only: nothing scores these rows.
RHETORIUS_INEFFECTIVE_HOUSES = (6, 3, 2, 8, 12)          # Ch. 27, in its order
RHETORIUS_EFFECTIVE_HOUSES = (1, 10, 7, 4, 5, 9, 11)     # Ch. 28: the angles, the two trines, the succedent of the MC
RHETORIUS_BESIEGING_DEGREES = 7.0                        # Ch. 41
RHETORIUS_KOLLESIS_DEGREES = 3.0                         # Ch. 34

RHETORIUS_CH26 = ('Dominance and superiority is when a star situated in the tenth house makes a square to '
                  'the left. Let Cancer be the ASC: then those [planets] in Aries are superior to one in '
                  'Cancer. Another kind of superiority is when some one being in a house on the right '
                  'beholds one by a left trine or square or sextile.')
RHETORIUS_CH27 = ('Affliction is said when one is aspected by malefics or besieged, or applying to a '
                  'destructive [star] or becomes in kollêsis, or is opposed or is disposed of by one badly '
                  'situated in the ineffective houses, which are the sixth from the ASC and the third and '
                  'the second and the eighth and the twelfth.')
RHETORIUS_CH28 = ('Effective houses are the four angles and the two trines on either side of the ASC and '
                  'the succedent of the MC.')
RHETORIUS_CH34 = ('Kollêsis is the most important application when a star moves towards a star, the swifter '
                  'to the slower, if it is not more than three degrees away.')
RHETORIUS_CH41 = ('Besieging is when two planets have another one between them according to some aspect '
                  'pattern, with no other casting a ray in between, within 7 degrees to the front or the rear.')
RHETORIUS_CH42 = ('Each one of the stars is said to be fortified when [it is] in its own domicile or '
                  'exaltation or terms or in proper phase or well-configured or in one of the stronger '
                  'houses of the chart.')

RHETORIUS_AFFLICTION_CONDITIONS = [
    # key; family; chapter; the condition in the text's words; the reading (how it is tested),
    # or `untested` with the reason when the chapters give nothing to test.
    {'key': 'aspected by malefics', 'family': 'Afflicted', 'chapter': 'Rhetorius Ch. 27 (Holden)',
     'text': 'aspected by malefics',
     'reading': 'Whole sign: Saturn or Mars in the second, third, fourth or sixth sign from the planet, on either side. '
                'The text gives no degree, so none is applied; a malefic in the same sign is not an aspect and is not counted here.'},
    {'key': 'besieged', 'family': 'Afflicted', 'chapter': 'Rhetorius Ch. 27 with Ch. 41 (Holden); ITA IV.4.2',
     'text': 'besieged',
     'reading': "Ch. 41 requires flanks within 7° and no third ray between them. Including bodies alongside rays in the geometry is this app's expansion. Relief follows the Latin Great Introduction and al-Qabīsī: a Sun, Jupiter, or Venus sextile or trine ray less than 7° from this planet, without an added between-the-flanks condition; bodily intervention is assessed separately."},
    {'key': 'enclosed by the fortunes', 'family': 'Fortified', 'chapter': 'Gr. Intr. VII.6 and BW VIII.76, in ITA IV.4.2',
     'text': 'if a planet or sign were besieged by the fortunes, this will be of the more worthy fortunes',
     'reading': 'The same 7-degree construction with Jupiter and Venus as the two besiegers: "if a planet or sign were besieged by '
                'the fortunes, this will be of the more worthy fortunes" (Gr. Intr. VII.6, in ITA IV.4.2); "if between fortunes, a '
                'good condition is going to come" (BW VIII.76, its brackets the translator\'s). Not a Rhetorius row: added from the ITA passage.'},
    {'key': 'applying to a destructive star', 'family': 'Afflicted', 'chapter': 'Rhetorius Ch. 27 (Holden)',
     'text': 'applying to a destructive [star]',
     'reading': "The Moon must approach a malefic's body or aspect degree within her day's motion; other approaching planets are confirmed here only within 3° as kollêsis, with their wider limit unspecified and no added sign-boundary exclusion."},
    {'key': 'in kollesis', 'family': 'Afflicted', 'chapter': 'Rhetorius Ch. 27 with Ch. 34 (Holden)',
     'text': 'becomes in kollêsis',
     'reading': 'The swifter by absolute daily motion approaches a body or aspect degree within 3° inclusive (Rhetorius Chs. 34–35, 109–110). Both directions of motion are tested, without a sign-boundary veto; exact contact and effectiveness are separate.'},
    {'key': 'opposed', 'family': 'Afflicted', 'chapter': 'Rhetorius Ch. 27 (Holden)',
     'text': 'is opposed',
     'reading': 'Whole sign: any planet in the seventh sign from the planet. The text names no opposer, so any planet counts; '
                'a malefic opposer also gives the "aspected by malefics" row.'},
    {'key': 'disposed by one in an ineffective house', 'family': 'Afflicted', 'chapter': 'Rhetorius Ch. 27 (Holden)',
     'text': 'is disposed of by one badly situated in the ineffective houses, which are the sixth from the ASC and the third '
             'and the second and the eighth and the twelfth',
     'reading': 'Whole sign: the lord of the planet\'s sign by domicile stands in the sixth, third, second, eighth or twelfth sign '
                'from the Ascendant. "Badly situated" is read as standing in one of those houses, since the sentence defines them and '
                'nothing else. A planet in its own domicile disposes itself and gives no row.'},
    {'key': 'dominated', 'family': 'Dominated', 'chapter': 'Rhetorius Ch. 26 (Holden)',
     'text': 'when a star situated in the tenth house makes a square to the left',
     'reading': 'Whole sign, from the example ("Let Cancer be the ASC: then those [planets] in Aries are superior to one in Cancer"): '
                'a planet in the tenth sign counted from another dominates it. "Another kind of superiority" adds the ninth and the '
                'eleventh sign ("a left trine or square or sextile"). The chapter says nothing of harm, so the row is neither an '
                'affliction nor a fortification and is shown on its own.'},
    {'key': 'in its own domicile', 'family': 'Fortified', 'chapter': 'Rhetorius Ch. 42 (Holden)',
     'text': 'in its own domicile', 'reading': 'Whole sign: the planet\'s sign is one it rules by domicile.'},
    {'key': 'in its own exaltation', 'family': 'Fortified', 'chapter': 'Rhetorius Ch. 42 (Holden)',
     'text': 'exaltation', 'reading': 'Whole sign: the planet\'s sign is its exaltation.'},
    {'key': 'in its own terms', 'family': 'Fortified', 'chapter': 'Rhetorius Ch. 42 (Holden)',
     'text': 'terms',
     'reading': 'By degree: the planet stands in a bound of its own. The chapter names no table; the Egyptian bounds this app uses '
                'everywhere are read.'},
    {'key': 'in proper phase', 'family': 'Fortified', 'chapter': 'Rhetorius Ch. 42 (Holden)',
     'text': 'in proper phase', 'reading': None,
     'untested': 'Not tested: none of these chapters says what "proper phase" is, and no reading is imported from elsewhere.'},
    {'key': 'well-configured', 'family': 'Fortified', 'chapter': 'Rhetorius Ch. 42 (Holden)',
     'text': 'well-configured', 'reading': None,
     'untested': 'Not tested: none of these chapters says what "well-configured" is, and no reading is imported from elsewhere.'},
    {'key': 'in one of the stronger houses', 'family': 'Fortified', 'chapter': 'Rhetorius Ch. 42 with Ch. 28 (Holden)',
     'text': 'in one of the stronger houses of the chart',
     'reading': 'Whole sign: the planet stands in one of Ch. 28\'s effective houses from the Ascendant -- the four angles (1, 10, 7, 4), '
                'the two trines on either side of the ASC (5, 9) and the succedent of the MC (11). Taking Ch. 42\'s "stronger houses" '
                'to be Ch. 28\'s effective ones is this app\'s equation; Ch. 42 names no chapter.'},
]

def _rhetorius_ordinal(n):
    return f"{n}{'th' if 10 <= n % 100 <= 20 else {1: 'st', 2: 'nd', 3: 'rd'}.get(n % 10, 'th')}"

def _rhetorius_signs_ahead(lon_from, lon_to):
    """How many signs lon_to lies ahead of lon_from in zodiacal order, 0-11."""
    return (int((lon_to % 360.0) // 30) - int((lon_from % 360.0) // 30)) % 12

def _rhetorius_applying(lon_p, v_p, lon_other, v_other, signs_apart):
    """Chs. 34–37: swifter by absolute speed, approaching either ray.

    The last argument selects an aspect angle in units of thirty degrees;
    it is not an admission gate on the planets' signs. Exactness is a
    separate fact, not an application. No rounding tolerance is applied.
    """
    target = 30.0 * signs_apart
    rel = (lon_p - lon_other) % 360.0
    targets = (target, (-target) % 360.0)
    delta = min((((rel - t + 180.0) % 360.0) - 180.0 for t in targets), key=abs)
    if delta == 0.0:
        return False, 0.0
    if v_p is None or v_other is None or abs(v_p) <= abs(v_other):
        return False, abs(delta)
    return delta * (v_p - v_other) < 0.0, abs(delta)


def _rhetorius_application(planet, data, other):
    """One nearest body/aspect contact; existence does not imply efficacy."""
    p, q = data[planet], data[other]
    candidates = []
    for n, name in ((0, 'conjunction'), (2, 'sextile'), (3, 'square'), (4, 'trine'), (6, 'opposition')):
        approaching, distance = _rhetorius_applying(
            p['longitude'], p.get('speed_in_lon'), q['longitude'], q.get('speed_in_lon'), n)
        candidates.append((distance, name, approaching))
    exact_contacts = [c for c in candidates if c[0] == 0.0]
    approaching_contacts = [c for c in candidates if c[2]]
    distance, aspect, approaching = min(exact_contacts or approaching_contacts or candidates, key=lambda item: item[0])
    exact = distance == 0.0
    if exact:
        result = False
    elif p.get('speed_in_lon') is None or q.get('speed_in_lon') is None:
        result = UnresolvedResult('daily motion is unavailable', 'Rhetorius Chs. 34–35')
    elif not approaching:
        result = False
    elif distance <= RHETORIUS_KOLLESIS_DEGREES:
        result = True
    elif planet == 'Moon':
        result = distance <= abs(p['speed_in_lon'])
    else:
        result = UnresolvedResult('the wider non-lunar application limit is unspecified',
                                  'Rhetorius Chs. 34–35', display_label='Limit unspecified')
    return {'result': result, 'aspect': aspect, 'distance': distance, 'exact': exact,
            'kollesis': approaching and distance <= RHETORIUS_KOLLESIS_DEGREES,
            'effectiveness': UnresolvedResult('application alone does not establish effectiveness',
                                               'Rhetorius Ch. 110')}


def _rhetorius_besiegers(planet, planetary_data, planets, group=None):
    """Ch. 41: the nearest body or ray behind the planet and the nearest
    ahead, each within 7 degrees, from two different planets -- of GROUP
    (the malefics for besieging, the fortunes for enclosure by them; ITA
    IV.4.2) when one is given. Returns (behind, gap, ahead, gap) or None."""
    lon = planetary_data[planet]['longitude'] % 360.0
    behind, ahead = None, None
    for other in planets:
        if other == planet or (group is not None and other not in group):
            continue
        for ray in _ray_degrees(planetary_data[other]['longitude'] % 360.0):
            gap_behind = (lon - ray) % 360.0
            gap_ahead = (ray - lon) % 360.0
            if 0.0 < gap_behind <= RHETORIUS_BESIEGING_DEGREES and (behind is None or gap_behind < behind[1]):
                behind = (other, gap_behind)
            if 0.0 < gap_ahead <= RHETORIUS_BESIEGING_DEGREES and (ahead is None or gap_ahead < ahead[1]):
                ahead = (other, gap_ahead)
    if behind is None or ahead is None or behind[0] == ahead[0]:
        return None
    return behind[0], behind[1], ahead[0], ahead[1]

def _rhetorius_loosener(planet, planetary_data, planets, breakers, friendly_only):
    """Friendly-ray relief (Latin Gr. Intr./al-Qabīsī), or the existing
    fortunes-enclosure alternative. A body is never a friendly ray."""
    lon = planetary_data[planet]['longitude'] % 360.0
    angles = (60.0, 120.0, 240.0, 300.0) if friendly_only else (0.0, 60.0, 90.0, 120.0, 180.0, 240.0, 270.0, 300.0)
    for other in planets:
        if other == planet or other not in breakers:
            continue
        o = planetary_data[other]['longitude'] % 360.0
        for a in angles:
            ray = (o + a) % 360.0
            gap = min((ray - lon) % 360.0, (lon - ray) % 360.0)
            if gap < RHETORIUS_BESIEGING_DEGREES:
                what = 'body' if a == 0.0 else {60.0: 'sextile', 120.0: 'trine', 90.0: 'square', 180.0: 'opposition'}.get(a % 360.0 if a <= 180.0 else 360.0 - a, 'ray')
                return f"{other}'s {what} {gap:.1f}° off"
    return None

def _rhetorius_siege_evidence(planet, data, planets, siege):
    """Keep Ch. 41's structure, friendly relief and body intervention apart.

    The body-or-ray geometry is the app's declared expansion; the primary
    structural interruption here tests a third ray. Dykes's alternative
    tests opposite-quality bodies/rays inside the flanked interval.
    """
    behind, bg, ahead, ag = siege
    lon = data[planet]['longitude']
    rays, bodies = [], []
    for other in planets:
        if other in (planet, behind, ahead):
            continue
        for angle in (0, 60, 90, 120, 180, 240, 270, 300):
            offset = ((data[other]['longitude'] + angle - lon + 180) % 360) - 180
            inside = -bg < offset < ag
            distance = float(abs(offset))
            friendly = angle in (60, 120, 240, 300)
            relief = other in FORTUNES | {'Sun'} and friendly and distance < 7.0
            if not inside and not relief:
                continue
            item = {'Donor': other, 'Aspect': {0:'body', 60:'sextile', 90:'square', 120:'trine',
                    180:'opposition', 240:'trine', 270:'square', 300:'sextile'}[angle],
                    'Distance': distance, 'Between flanks': inside,
                    'Relief': relief, 'Source': 'Latin Gr. Intr. VII.6 / al-Qabīsī III.28b' if relief
                    else ('Abbr. IV.25 / Dykes, ITA IV.4.2' if angle == 0 else 'Rhetorius Ch. 41')}
            (bodies if angle == 0 else rays).append(item)
    interruptions = [r for r in rays if r['Between flanks']]
    relief = [r for r in rays if r['Relief']]
    alternative = [r for r in rays + bodies if r['Between flanks'] and r['Donor'] in FORTUNES | {'Sun'}]
    return {'Structure intact': not interruptions, 'Friendly-ray relief': bool(relief),
            'Ray evidence': rays, 'Bodily intervention': bodies, 'Dykes interval break': bool(alternative)}


def evaluate_rhetorius_affliction(planetary_data, asc_lon, sect):
    """Rhetorius Chs. 26-28, 41-42 (Holden): a row per planet per condition
    tested, with unresolved results explicitly marked, each read as RHETORIUS_AFFLICTION_CONDITIONS says. `sect` is
    accepted for the page's uniform call and unused: none of the five
    chapters mentions sect. The seven planets only; display only."""
    planets = [p for p in ('Saturn', 'Jupiter', 'Mars', 'Sun', 'Venus', 'Mercury', 'Moon') if p in planetary_data]
    by_key = {c['key']: c for c in RHETORIUS_AFFLICTION_CONDITIONS}
    aspect_names = {2: 'sextile', 3: 'square', 4: 'trine', 6: 'opposition'}
    rows = []

    def add(planet, key, by):
        c = by_key[key]
        rows.append({'Planet': planet, 'Condition': f"{c['family']}: {c['key']}",
                     'By': by, 'Chapter': c['chapter'], 'Text': c['text'], 'Status': 'Present'})

    for planet in planets:
        lon = planetary_data[planet]['longitude'] % 360.0
        speed = planetary_data[planet].get('speed_in_lon')
        sign = get_zodiac_sign(lon)
        house = get_wsh_house(lon, asc_lon)

        # --- Chs. 27 and 26 --------------------------------------------
        malefic_aspects, opposers, applying, kollesis, dominators = [], [], [], [], []
        application_contacts, kollesis_contacts = [], []
        for other in planets:
            if other == planet:
                continue
            o_lon = planetary_data[other]['longitude'] % 360.0
            o_speed = planetary_data[other].get('speed_in_lon')
            ahead = _rhetorius_signs_ahead(lon, o_lon)
            apart = min(ahead, 12 - ahead)
            aspect_name = aspect_names.get(apart)
            if other in INFORTUNES and aspect_name:
                malefic_aspects.append(f'{other} by whole-sign {aspect_name}')
            if apart == 6:
                opposers.append(f'{other} by whole-sign opposition')
            if other in INFORTUNES:
                contact = _rhetorius_application(planet, planetary_data, other)
                result = contact['result']
                evidence = f"{other} by {contact['aspect']}, {round(contact['distance'], 2):g}° from exact"
                if result is True:
                    applying.append(evidence)
                    application_contacts.append({'Planet': other, 'Aspect': contact['aspect'], 'Distance': contact['distance']})
                elif is_unresolved(result):
                    add(planet, 'applying to a destructive star', evidence)
                    rows[-1].update(Status=result, **{'Exact contact': contact['exact'],
                        'Effectiveness': contact['effectiveness'], 'Distance': contact['distance']})
                if contact['kollesis']:
                    kollesis.append(evidence)
                    kollesis_contacts.append({'Planet': other, 'Aspect': contact['aspect'], 'Distance': contact['distance']})
                if contact['exact']:
                    rows.append({'Planet': planet, 'Condition': 'Exact contact', 'By': evidence,
                                 'Chapter': 'Rhetorius Chs. 37, 109–110', 'Text': '',
                                 'Exact contact': True, 'Status': 'Completed contact',
                                 'Effectiveness': contact['effectiveness']})
            # Ch. 26: the other stands in the ninth, tenth or eleventh sign
            # counted from the planet (its right-side trine, square, sextile).
            if ahead in (8, 9, 10):
                dominators.append(other + ' in ' + {9: 'the tenth sign, a square (the first kind)',
                                                    8: 'the ninth sign, a trine',
                                                    10: 'the eleventh sign, a sextile'}[ahead])
        if malefic_aspects:
            add(planet, 'aspected by malefics', '; '.join(malefic_aspects))
        siege = _rhetorius_besiegers(planet, planetary_data, planets, INFORTUNES)
        if siege:
            b, bg, a, ag = siege
            evidence = _rhetorius_siege_evidence(planet, planetary_data, planets, siege)
            add(planet, 'besieged', f'{b} ({round(bg, 2):g}° behind) and {a} ({round(ag, 2):g}° ahead), body-or-ray expansion')
            rows[-1].update(evidence)
            rows[-1]['Status'] = 'Present' if evidence['Structure intact'] else 'Interrupted — third ray (Ch. 41)'
        good = _rhetorius_besiegers(planet, planetary_data, planets, FORTUNES)
        if good:
            b, bg, a, ag = good
            broken = _rhetorius_loosener(planet, planetary_data, planets, INFORTUNES, friendly_only=False)
            add(planet, 'enclosed by the fortunes', f'{b} ({bg:.1f}° behind) and {a} ({ag:.1f}° ahead), by body or ray'
                + (f" -- broken by {broken} (Dykes, ITA IV.4.2 comment)" if broken else ''))
        if applying:
            add(planet, 'applying to a destructive star', '; '.join(applying))
            rows[-1].update({'Exact contact': False, 'Contact evidence': application_contacts,
                'Effectiveness': UnresolvedResult('application alone does not establish effectiveness', 'Rhetorius Ch. 110')})
        if kollesis:
            add(planet, 'in kollesis', '; '.join(kollesis))
            rows[-1].update({'Exact contact': False, 'Contact evidence': kollesis_contacts,
                'Effectiveness': UnresolvedResult('kollêsis alone does not establish effectiveness', 'Rhetorius Ch. 110')})
        if opposers:
            add(planet, 'opposed', '; '.join(opposers))
        lord = next((p for p, signs in DOMICILES.items() if sign in signs), None)
        if lord and lord != planet and lord in planetary_data:
            lord_house = get_wsh_house(planetary_data[lord]['longitude'], asc_lon)
            if lord_house in RHETORIUS_INEFFECTIVE_HOUSES:
                add(planet, 'disposed by one in an ineffective house',
                    f'{lord}, lord of {sign}, in the {_rhetorius_ordinal(lord_house)} house from the Ascendant')
        if dominators:
            add(planet, 'dominated', '; '.join(dominators))

        # --- Ch. 42 ------------------------------------------------------
        if sign in DOMICILES.get(planet, []):
            add(planet, 'in its own domicile', sign)
        if sign in EXALTATIONS.get(planet, []):
            add(planet, 'in its own exaltation', sign)
        for limit, term_lord in EGYPTIAN_TERMS[sign]:
            if lon % 30.0 < limit:
                if term_lord == planet:
                    add(planet, 'in its own terms', f'{get_degree_string(lon)}, its own bound')
                break
        if house in RHETORIUS_EFFECTIVE_HOUSES:
            add(planet, 'in one of the stronger houses', f'the {_rhetorius_ordinal(house)} house from the Ascendant')
    return rows

def evaluate_accidental_dignities(planetary_data, natal_houses, sect, jd=None,
                                  armc=None, obliquity=None, geo_lat=None):
    """Accidental dignity scoring: house angularity (Whole Sign, anchored to
    the Ascendant, with the 6/8/12 malefic-house override), planetary joys,
    domain/hayz, motion & speed, and solar phase.

    The point weights (+5 angular, -5 combust, and so on) are this app's own
    convenience for ranking, not anybody's doctrine -- no source in hand adds
    these conditions up. What IS sourced is the geometry each test uses, and
    each of those carries its citation at the point of use.

    `armc`, `obliquity` and `geo_lat` are the chart's horizon. With them the
    domain's "above the earth / below the earth" (Gr. Intr. VII.1, 37;
    VII.6, 13) is decided by the planet's ALTITUDE from its full ecliptic
    position (_sin_altitude, as sect and the syzygy already are); without
    them it falls back to the ecliptic proxy (lon - Ascendant) % 360 > 180,
    which misplaces a body within a degree or two of the horizon and says
    so in the row ('Hemisphere by'). Astra audit F04, 2026-09-11."""
    sun_lon = planetary_data['Sun']['longitude']
    ascendant = natal_houses[0]
    is_diurnal_chart = (sect == 'Diurnal')
    results = {}

    for planet, data in planetary_data.items():
        if planet == 'North Node': continue
        lon, speed = data['longitude'], data['speed_in_lon']
        labels = []
        score = 0

        # --- House angularity (Whole Sign) --------------------------------
        house_num = get_wsh_house(lon, ascendant)
        if house_num in MALEFIC_HOUSES:
            house_pts, house_label = -5, f"Malefic {HOUSE_ORDINAL[house_num]} (-5)"
        elif house_num in ANGLE_HOUSES:
            house_pts, house_label = 5, f"Angular {HOUSE_ORDINAL[house_num]} (+5)"
        elif house_num in SUCCEDENT_HOUSES:
            house_pts, house_label = 3, f"Succedent {HOUSE_ORDINAL[house_num]} (+3)"
        else:
            house_pts, house_label = -3, f"Cadent {HOUSE_ORDINAL[house_num]} (-3)"
        score += house_pts
        labels.append(house_label)

        # --- Planetary joy -------------------------------------------------
        is_joy = JOY_HOUSES.get(planet) == house_num
        if is_joy:
            score += 3
            labels.append("Joy (+3)")

        # --- Sect / Hayz -----------------------------------------------
        # All three must hold: the planet's own sect matches the chart's
        # sect, it's on its sect-favored side of the horizon, and it's in
        # a sign of its sect-favored gender. Mercury's own sect is derived
        # from its solar phase (morning riser = diurnal, evening star =
        # nocturnal) rather than being fixed -- Abu Ma'shar's rule, Gr. Intr.
        # IV.9 (ITA V.11); see planet_sect_is_diurnal.
        current_sign = get_zodiac_sign(lon)
        if armc is not None and obliquity is not None and geo_lat is not None:
            is_above_horizon = _sin_altitude(lon, data.get('latitude', 0.0), data.get('distance', 1.0),
                                             obliquity, armc, geo_lat) > 0.0
            hemisphere_by = 'altitude'
        else:
            is_above_horizon = (lon - ascendant) % 360 > 180.0
            hemisphere_by = 'ecliptic proxy (no horizon supplied)'

        planet_is_diurnal = planet_sect_is_diurnal(planet, lon, sun_lon)

        # Abu Ma'shar states the condition twice, the Mars exception only at VII.1, 37: "a male planet
        # by day is above the earth (AND BY NIGHT BELOW THE EARTH), in a male
        # sign; but if it was female, then by day it is below the earth (and
        # by night above the earth), in a female sign -- except for Mars
        # alone, because he is contrary to what we said" (Great Introduction
        # VII.1, 37), and again at VII.6, 13. Al-Qabisi I.78 has the
        # hemisphere-by-sect half only (Dykes' note 222 on VII.6, 10, glossing
        # halb; the note on 13 itself just points back to VII.1, 37-39). The
        # chart's sect does NOT have to match
        # the planet's: the HEMISPHERE requirement is what flips with it, so
        # a diurnal planet below the earth in a masculine sign is in its
        # domain in a nocturnal chart. An earlier version additionally
        # required the planet's own sect to match the chart's, which threw
        # away that entire second half of the rule.
        #
        # Mars's exception applies to the HEMISPHERE ONLY. He is masculine
        # in every source here, so the sign-gender half of the test is not
        # what "contrary to what we said" reverses -- and VII.6, 13 restates
        # the whole rule with no Mars exception at all. Dykes' note on
        # VII.1, 37 points the same way ("Mars will stand out because he is
        # a MALE, NOCTURNAL planet"), and the Course Glossary defines Domain
        # as "in a sign of ITS OWN GENDER and also in its preferred
        # hemisphere". Sahl's own glossary (Vol. I p. 777, Domain; p. 782,
        # Halb and Hayyiz) is a second witness in the corpus itself, and it
        # keeps the two conditions apart: HALB is "for diurnal planets, when
        # they are in the same hemisphere as the Sun ... for nocturnal
        # planets, when they are in the hemisphere opposite the Sun";
        # HAYYIZ/DOMAIN is "technically equivalent to halb, except that the
        # planet is also in a sign of its own gender". That split is what
        # DOMAIN_RULE_OPTIONS exposes. An earlier version flipped him on both
        # tests, which required him to be in feminine signs -- a reading no
        # text states.
        mars_is_masculine_but_nocturnal = (planet == 'Mars')
        def _domain_for(own_diurnal):
            if reading('DOMAIN_RULE') == DOMAIN_RULE_OPTIONS[1]:
                # On Nativities 1.23, 17 (see DOMAIN_RULE). Gender, not sect:
                # Mars is male, with no exception stated.
                planet_is_male = mars_is_masculine_but_nocturnal or own_diurnal
                if planet_is_male:
                    horizon_ok = is_above_horizon if is_diurnal_chart else not is_above_horizon
                    gender_ok = ((current_sign in MASCULINE_SIGNS) if is_diurnal_chart
                                 else (current_sign in FEMININE_SIGNS))
                else:
                    horizon_ok = not is_above_horizon if is_diurnal_chart else is_above_horizon
                    gender_ok = True
            else:
                if own_diurnal:
                    horizon_ok = is_above_horizon if is_diurnal_chart else not is_above_horizon
                else:
                    horizon_ok = not is_above_horizon if is_diurnal_chart else is_above_horizon
                # Sign gender follows the planet's own gender. It coincides
                # with sect for six planets; Mars remains masculine.
                if mars_is_masculine_but_nocturnal or own_diurnal:
                    gender_ok = current_sign in MASCULINE_SIGNS
                else:
                    gender_ok = current_sign in FEMININE_SIGNS
            return horizon_ok and gender_ok, (not horizon_ok) and (not gender_ok)

        is_hayz = contrary_domain = False
        domain_alternatives = None
        if isinstance(planet_is_diurnal, UnresolvedResult):
            day_domain, day_contrary = _domain_for(True)
            night_domain, night_contrary = _domain_for(False)
            domain_alternatives = (('if Mercury is diurnal', (day_domain, day_contrary)),
                                   ('if Mercury is nocturnal', (night_domain, night_contrary)))
            if day_domain == night_domain:
                is_hayz = day_domain
            else:
                is_hayz = UnresolvedResult(
                    reason="Mercury's domain depends on its unassigned phase sect",
                    source='Great Introduction IV.9, 8; VII.1, 37; VII.6, 13',
                    alternatives=(('if Mercury is diurnal', day_domain),
                                  ('if Mercury is nocturnal', night_domain)),
                    status='unassigned',
                )
            if day_contrary == night_contrary:
                contrary_domain = day_contrary
            else:
                contrary_domain = UnresolvedResult(
                    reason="Mercury's contrary-domain state depends on its unassigned phase sect",
                    source='Great Introduction IV.9, 8; VII.1, 37-39; VII.6, 13, 36',
                    alternatives=(('if Mercury is diurnal', day_contrary),
                                  ('if Mercury is nocturnal', night_contrary)),
                    status='unassigned',
                )
        elif planet_is_diurnal is not None:
            is_hayz, contrary_domain = _domain_for(planet_is_diurnal)
        if isinstance(is_hayz, UnresolvedResult):
            pass
        elif is_hayz:
            score += 3
            labels.append("Hayz/domain (+3)")
        elif contrary_domain:
            labels.append("Contrary to its domain")

        # --- Motion & speed -------------------------------------------
        # "Increasing in its rate of movement (with respect to the five
        # planets) is that it travels more than its mean motion" -- Great
        # Introduction VII.1, 29. But 30-31 immediately excepts the two
        # inferiors: "their mean motion in one day at a
        # [particular] time is not like their mean travel for the day" (30), so
        # for them the comparison is against the SUN's motion that day, not
        # their own mean. An earlier version measured all seven against
        # their own means.
        is_stationary = abs(speed) <= STATION_SPEED_TOLERANCE
        is_retrograde = speed < 0 and not is_stationary and planet not in ('Sun', 'Moon')
        if planet in ('Venus', 'Mercury'):
            pace = planetary_data['Sun']['speed_in_lon']
        else:
            pace = AVERAGE_DAILY_MOTION.get(planet, 1.0)
        is_swift = (not is_stationary) and (not is_retrograde) and speed > pace
        # Sahl separates the two stations sharply: stationing toward
        # retrogradation "indicates collapse in the matter, and disobedience
        # ... corruption, difficulty," while stationing toward direct motion
        # "indicates forward movement in that matter, with no difficulty ...
        # the suitability of the affair, and its strength" (Fifty Aphorisms
        # #48, 99-102). Abu Ma'shar likewise makes them separate conditions,
        # the seventh and the eleventh (VII.2, 23 and 27), and VII.6 counts
        # the first station a weakness (32) and the second a strength (24).
        # An earlier version scored both alike at -2.
        station = None
        if is_stationary and jd is not None and planet in PLANET_SWE_IDS:
            try:
                res_next, _ = swe.calc_ut(jd + 1.0, PLANET_SWE_IDS[planet])
                station = 'second' if res_next[3] > speed else 'first'
            except Exception:
                station = None
        if is_stationary:
            if station == 'second':
                score += 2
                labels.append("Second station, going direct (+2)")
            elif station == 'first':
                score -= 4
                labels.append("First station, turning retrograde (-4)")
            else:
                score -= 2
                labels.append("Stationary (-2)")
        elif is_retrograde:
            score -= 5
            labels.append("Retrograde (-5)")
        elif is_swift:
            score += 2
            labels.append("Swift (+2)")

        # --- Solar phase (Gr. Intr. VII.2; Sahl, On Nativities 1.22) --
        phase, side, elongation = solar_phase(planet, lon, sun_lon, speed)
        is_cazimi = phase == 'Cazimi'
        is_combust = phase == 'Burned'
        is_under_beams = phase == 'Under the rays'
        if is_cazimi:
            score += 5
            labels.append("Cazimi/in the heart (+5)")
        elif is_combust:
            score -= 5
            labels.append(f"Burned, {side} ({elongation:.1f} deg) (-5)" + solar_phase_note(planet, side, speed, elongation))
        elif is_under_beams:
            score -= 2
            labels.append(f"Under the rays, {side} ({elongation:.1f} deg) (-2)" + solar_phase_note(planet, side, speed, elongation))
        elif phase == 'Degrees of setting':
            labels.append(f"In the degrees of setting ({elongation:.1f} deg)")

        score_result = score
        if isinstance(is_hayz, UnresolvedResult):
            score_result = UnresolvedResult(
                reason=f'known subtotal {score}; the +3 domain contribution is unassigned',
                source=is_hayz.source,
                alternatives=(('if domain applies', score + 3), ('if domain does not apply', score)),
                status='unassigned',
            )
            labels.append('Domain/hayz unassigned (+3 excluded from the known subtotal)')
        results[planet] = {
            'Accidental Score': score_result, 'Known Accidental Score': score,
            'House': house_num, 'Joy': is_joy, 'Hayz': is_hayz,
            'PlanetSect': planet_is_diurnal,
            'Domain Alternatives': domain_alternatives,
            'Above the earth': is_above_horizon, 'Hemisphere by': hemisphere_by,
            'ContraryDomain': contrary_domain, 'Station': station,
            'Stationary': is_stationary, 'Retrograde': is_retrograde, 'Swift': is_swift,
            'Cazimi': is_cazimi, 'Combust': is_combust, 'UnderBeams': is_under_beams,
            'SolarPhase': phase, 'SolarSide': side, 'Elongation': elongation,
            'Accidental Labels': labels,
        }
    return results


def domain_description(accidental_row, short=False):
    """Describe domain without treating an unassigned sect as a failed test."""
    def wording(hayz, contrary):
        if hayz:
            return 'in its own domain' if short else 'in its own domain (hayz)'
        if contrary:
            return 'contrary to its domain'
        return 'neither' if short else 'neither in nor contrary to its domain'

    alternatives = accidental_row.get('Domain Alternatives')
    if alternatives:
        readings = []
        for name, (hayz, contrary) in alternatives:
            value = wording(hayz, contrary)
            readings.append((name, value))
        if all(value == readings[0][1] for _name, value in readings[1:]):
            return readings[0][1]
        return UnresolvedResult(
            reason="Mercury's domain description depends on its unassigned phase sect",
            source='Great Introduction IV.9, 8; VII.1, 37-39; VII.6, 13, 36',
            alternatives=tuple(readings),
            status='unassigned',
        )
    if accidental_row.get('Hayz') is True:
        return wording(True, False)
    if accidental_row.get('ContraryDomain') is True:
        return wording(False, True)
    return wording(False, False)

# Whole-sign configuration allowed by classical Ptolemaic doctrine: how many
# signs apart the two bodies must be for a given aspect, and its exact angle.
ASPECT_BY_SIGN_COUNT = {
    0: ('Conjunction', 0.0),
    2: ('Sextile', 60.0),
    3: ('Square', 90.0),
    4: ('Trine', 120.0),
    6: ('Opposition', 180.0),
}

# The two sign-distances left uncovered by ASPECT_BY_SIGN_COUNT: 1 sign
# apart (2nd/12th from each other) and 5 signs apart (6th/8th from each
# other). A place in either relationship to another is in Aversion to it --
# unconfigured, and unable to see it by the classical aspect scheme at all,
# regardless of how close the raw degree separation happens to land
# (Sahl, The Introduction Ch.2, 60).
AVERSION_SIGN_COUNTS = {1, 5}

# Each planet's own orb/"body" radius, front and behind (degrees) --
# Gr. Intr. VII.3, Fig. 105 ("bodies or orbs of
# planets"), identical to Sahl's "light" figures (The Introduction Ch.3,
# 12-18). One shared constant: used for Assembly-strength grading below,
# for the Connection engine, and for the Abu Ma'shar condition evaluator.
PLANETARY_ORBS = {'Sun': 15.0, 'Moon': 12.0, 'Saturn': 9.0, 'Jupiter': 9.0,
                   'Mars': 8.0, 'Venus': 7.0, 'Mercury': 7.0}

# The standing order of the planets by weight, heaviest (and so naturally
# slowest) first. This is the classical hierarchy the sources treat as
# fixed when they speak of "the light planet" and "the heavy planet", and
# it is the single definition of light/heavy used everywhere in this file:
# for the giver/accepter roles in _pairwise_configurations(), and for the
# heavier "collector" in evaluate_collections_of_light(). Instantaneous
# speed is a separate question, answered separately.
WEIGHT_ORDER = ['Saturn', 'Jupiter', 'Mars', 'Sun', 'Venus', 'Mercury', 'Moon']

def _format_orb(degrees):
    dd = int(degrees)
    mm = int(round((degrees - dd) * 60))
    if mm == 60:
        dd += 1
        mm = 0
    return f"{dd:02d}\u00b0 {mm:02d}'"

def _pairwise_configurations(planetary_data, encounter_history=None):
    """Shared per-pair raw data for every classical-planet combination:
    whole-sign gate (Sahl, The Introduction Ch.2, 50-60), the light/heavy
    roles, and (for configured pairs) the signed deviation from the exact
    (partile) aspect and its Applying/Separating motion. Consumed by
    evaluate_ptolemaic_aspects() and the Connection/Transfer/Collection
    evaluators so the gate & kinetics logic lives in exactly one place."""
    planets = [p for p in planetary_data.keys() if p != 'North Node']
    rows = []

    for p1, p2 in combinations(planets, 2):
        lon1, v1 = planetary_data[p1]['longitude'], planetary_data[p1]['speed_in_lon']
        lon2, v2 = planetary_data[p2]['longitude'], planetary_data[p2]['speed_in_lon']

        # --- 0. The light/heavy roles ------------------------------------
        # "Light" and "heavy" are the fixed classes both authors name as
        # nouns -- "the light planet", "the heavy planet" (Sahl Ch.3, 6-8,
        # 22, 67) -- so they are taken from the standing weight order,
        # NOT from instantaneous speed. The role decides who gives and who
        # accepts (67: "the one handing over is the light planet ... the
        # accepting one is the heavy planet"), whose light measures the
        # connection (19), and who casts the ray.
        #
        # An earlier version ranked by abs(speed_in_lon), which let a
        # transient reading flip the hierarchy: across an 860-year weekly
        # sample that disagrees with the standing order on 8% of pairs,
        # including Saturn/Jupiter on 23% and Saturn/Mercury on 2% --
        # i.e. it repeatedly made Saturn the LIGHT planet, which no source
        # allows. It also contradicted this same file, since Collection of
        # Light has always picked its heavier "collector" from WEIGHT_ORDER.
        #
        # Actual signed motion still decides everything kinetic below
        # (applying, separating, stationing, retrograde); the two are
        # simply no longer the same question. Note the Applying/Separating
        # result is invariant under this change: swapping the roles flips
        # the sign of both `s` and `delta_v`, and only their product is
        # used.
        if WEIGHT_ORDER.index(p1) > WEIGHT_ORDER.index(p2):
            light_name, fast_lon, light_speed = p1, lon1, v1
            heavy_name, slow_lon, heavy_speed = p2, lon2, v2
        else:
            light_name, fast_lon, light_speed = p2, lon2, v2
            heavy_name, slow_lon, heavy_speed = p1, lon1, v1

        # The kinetic facts, kept separate from the roles above.
        swifter_name = p1 if abs(v1) >= abs(v2) else p2
        light_retrograde = light_speed < 0
        heavy_retrograde = heavy_speed < 0

        raw_dist = abs(lon1 - lon2)
        dist = raw_dist if raw_dist <= 180.0 else 360.0 - raw_dist

        # --- 1. Whole-sign configuration gate ---------------------------
        # Reject any pair whose SIGNS aren't in a valid Ptolemaic relationship
        # (0/2/3/4/6 signs apart) -- this is what prohibits out-of-sign aspects
        # regardless of how close the raw degree separation happens to land.
        # A pair 1 or 5 signs apart is in Aversion instead: not configured,
        # and unable to form any aspect at all.
        sign_idx1 = int(lon1 // 30)
        sign_idx2 = int(lon2 // 30)
        raw_signs_apart = abs(sign_idx1 - sign_idx2)
        signs_apart = min(raw_signs_apart, 12 - raw_signs_apart)

        # --- 1b. Facts that are independent of the whole-sign gate -------
        # Each planet's body projects its own sphere of power outward
        # (Gr. Intr. VII.3, 6-11; VII.4, 5-8), so "is A inside B's
        # body" and "is B inside A's body" are separate questions with
        # separate answers, and the pair is only MUTUALLY merged when the
        # separation fits inside the smaller of the two. VII.4, 7 spells
        # this out: with Saturn and the Moon within 12 degrees, Saturn is
        # described thus: "Saturn would be in the power of the body of the Moon,
        # while the Moon would not be in the power of the body of Saturn,
        # until there is a little under 9° between them." (This is per-planet spheres, NOT a moiety --
        # nothing in either author averages the two orbs.)
        light_orb = PLANETARY_ORBS.get(light_name, 7.0)
        heavy_orb = PLANETARY_ORBS.get(heavy_name, 7.0)
        light_in_heavy_body = dist <= heavy_orb
        heavy_in_light_body = dist <= light_orb

        row = {
            'p1': p1, 'p2': p2, 'light_name': light_name, 'heavy_name': heavy_name,
            'light_speed': light_speed, 'heavy_speed': heavy_speed,
            'signs_apart': signs_apart, 'dist': dist,
            'swifter_name': swifter_name,
            'light_retrograde': light_retrograde,
            'heavy_retrograde': heavy_retrograde,
            # Gr. Intr. VII.5, 24: "sometimes at the assembly both of the
            # two planets will be retrograde, or one of them will be
            # retrograde and the other direct: the connection of one of
            # them with the other, and its separation from it, will be BY
            # RETROGRADATION." Neither author reassigns the light/heavy
            # roles in that case, so the roles stand and the fact is
            # surfaced instead of being silently resolved. Cf. the note on
            # VII.5, 130: Saturn "could never be received because he is too
            # slow to connect with anyone (unless by retrogradation)."
            'by_retrogradation': light_retrograde or heavy_retrograde,
            'assembly': signs_apart == 0,
            'light_orb': light_orb, 'heavy_orb': heavy_orb,
            'light_in_heavy_body': light_in_heavy_body,
            'heavy_in_light_body': heavy_in_light_body,
            'mutual_body': light_in_heavy_body and heavy_in_light_body,
            # Set by the post-pass below; only ever true on Aversion rows.
            'sahl_body_connection': False,
            # Directed agency, filled in for configured pairs at 2b below.
            # Distinct from light_name/heavy_name, which are the STANDING
            # ranks and never move.
            'applicant': None, 'receiver': None, 'application_cause': None,
            'applicant_is_heavier': False,
            'encounter': (encounter_history or {}).get(frozenset((p1, p2)))
                or planetary_data[p1].get('encounter_history', {}).get(p2)
                or planetary_data[p2].get('encounter_history', {}).get(p1),
            'positions': (lon1, lon2),
        }

        if signs_apart in AVERSION_SIGN_COUNTS:
            row.update(aspect_name='Aversion', deviation=None, motion=None, orientation=None)
            rows.append(row)
            continue

        aspect_name, target = ASPECT_BY_SIGN_COUNT[signs_apart]

        # --- 2. Kinetic direction: Applying (ittisal) vs Separating (insiraf)
        # s = signed position of the faster body relative to the receiver,
        # wrapped to (-180, 180]. deviation = dist - target is how far off
        # from the exact aspect we currently are; its time-derivative is
        # sign(s) * delta_v. Deviation shrinking toward zero => Applying.
        deviation = dist - target  # signed distance from partile (exact)
        s = ((fast_lon - slow_lon + 180.0) % 360.0) - 180.0
        sign_s = 1.0 if s >= 0 else -1.0
        delta_v = light_speed - heavy_speed
        rate = sign_s * delta_v
        motion = ('Exact' if deviation == 0 else 'No relative motion' if delta_v == 0
                  else 'Applying' if deviation * rate < 0 else 'Separating')

        # --- 2b. Who is actually approaching whom ------------------------
        # Natural rank (above) is one fact; DIRECTED AGENCY is another, and
        # the two come apart whenever the naturally heavier planet is the
        # one closing the gap.
        #
        # Sahl states the ordinary case: "connection is if a light, quick
        # star is GOING STRAIGHTAWAY TO a heavy star, and the light star is
        # FEWER IN DEGREES than the heavy one, as long as the [light] planet
        # is GOING TOWARDS the [heavy] planet" (Ch.3, 6), and "the one
        # handing over is the light planet ... the accepting one is the
        # heavy planet" (67). Both clauses assume forward motion, and in
        # forward motion the lighter planet is always the one that closes.
        #
        # Retrogradation breaks that assumption, and both authors say so
        # rather than leaving it to be inferred. Abu Ma'shar: "sometimes at
        # the assembly both of the two planets will be retrograde, or one of
        # them will be retrograde and the other direct: the connection of
        # one of them with the other, and its separation from it, will be BY
        # RETROGRADATION" (VII.5, 24). His Resistance turns on it --
        # "the light one IN MORE DEGREES goes retrograde and connects with
        # the heavy one through its retrogradation" (VII.5, 118), reversing
        # 6's fewer-degrees clause outright -- and Fig. 138's Resistance has
        # Venus retrograde connecting with Mercury, who is lighter than she
        # is. Dykes' note on VII.5, 130 makes the consequence explicit:
        # Saturn "could never be received because he is too slow to connect
        # with anyone (UNLESS BY RETROGRADATION)." And the note on Fifty
        # Aphorisms #19 says the same from the other side: "it would only be
        # possible for Saturn to be the one HANDING OVER if he is
        # retrograde."
        #
        # So the applicant is whichever planet's own motion is closing the
        # aspect, and the receiver is the one being approached. When both
        # move toward each other neither is fleeing, and 67's plain rule
        # stands: the naturally lighter planet is the one handing over.
        contrib_light = sign_s * light_speed   # d(deviation)/dt from the light planet alone
        contrib_heavy = -sign_s * heavy_speed  # ... and from the heavy one alone
        if deviation == 0:
            acting_light = acting_heavy = True
        elif motion == 'Applying':
            acting_light = deviation * contrib_light < 0
            acting_heavy = deviation * contrib_heavy < 0
        else:
            acting_light = deviation * contrib_light > 0
            acting_heavy = deviation * contrib_heavy > 0
        if acting_heavy and not acting_light:
            applicant, receiver = heavy_name, light_name
            cause = 'retrogradation' if heavy_retrograde else 'its own motion'
        elif acting_light and not acting_heavy:
            applicant, receiver = light_name, heavy_name
            cause = 'retrogradation' if light_retrograde else 'direct motion'
        else:
            applicant, receiver = light_name, heavy_name
            cause = 'mutual approach' if motion == 'Applying' else 'mutual recession'
        row.update(applicant=applicant, receiver=receiver, application_cause=cause,
                    applicant_is_heavier=(applicant == heavy_name))

        # --- 3. Dexter / Sinister orientation ----------------------------
        # Conjunction and Opposition have no handedness. Handedness is
        # reciprocal (if A's ray to B is dexter, B's to A is sinister), so
        # it needs a stated caster: here it is always the FASTER planet.
        # A dexter ("right") ray is cast against the order of signs, onto
        # EARLIER degrees; a sinister ("left") ray is cast with the order
        # of signs, onto LATER degrees. So a faster planet already ahead
        # of the receiver (s > 0) is casting backwards onto it = Dexter;
        # trailing it (s < 0) it casts forwards = Sinister. These two were
        # transposed in an earlier version.
        if aspect_name in ('Conjunction', 'Opposition'):
            orientation = "Direct"
        else:
            orientation = "Dexter" if s > 0 else "Sinister"

        row.update(aspect_name=aspect_name, target=target, deviation=deviation,
                    motion=motion, orientation=orientation, relative_speed=delta_v)
        rows.append(row)

    # --- 4. Sahl's out-of-sign connection by body (Ch.3, 20-21) ---------
    # "And if a planet was at the end of a sign, NOT CONNECTING WITH
    # ANYTHING, and it has already struck into the next sign with its own
    # light, then whatever planet was the first in that light, it is
    # connected with it. 21 And if it was not in the sign, then it will not
    # see it." The translator's note on 21 is explicit: such a pair does
    # not SEE each other (the next sign is in aversion), yet they are still
    # CONNECTED -- "the sentence is emphasizing the existence of
    # out-of-sign conjunctions by body. But it does not support out-of-sign
    # aspects." So this applies only to adjacent signs, only forward (the
    # light strikes INTO the next sign), and only when the striking planet
    # has no other connection at all. Abu Ma'shar denies this case outright
    # -- see _is_connected_abu_mashar().
    ordinary = {p: [] for p in planets}
    for row in rows:
        if row['aspect_name'] != 'Aversion':
            for p in (row['p1'], row['p2']):
                ordinary[p].append(row)
    strikes = {}
    for row in rows:
        if row['aspect_name'] != 'Aversion' or row['signs_apart'] != 1:
            continue
        a, b = row['p1'], row['p2']
        la, lb = planetary_data[a]['longitude'], planetary_data[b]['longitude']
        earlier, later = (a, b) if (lb - la) % 360 < (la - lb) % 360 else (b, a)
        gap = (planetary_data[later]['longitude'] - planetary_data[earlier]['longitude']) % 360
        ve, vl = planetary_data[earlier]['speed_in_lon'], planetary_data[later]['speed_in_lon']
        row.update(deviation=gap, target=0.0, relative_speed=ve-vl,
                   motion='No relative motion' if ve == vl else 'Applying' if ve > vl else 'Separating',
                   body_connection_from=earlier, body_connection_to=later,
                   applicant=later if vl < 0 and ve <= 0 else earlier,
                   receiver=earlier if vl < 0 and ve <= 0 else later,
                   application_cause='out-of-sign body (20–21)')
        row['applicant_is_heavier'] = WEIGHT_ORDER.index(row['applicant']) < WEIGHT_ORDER.index(row['receiver'])
        # The strike of 20-21 and its completed residue live inside the
        # striker's own light (the earlier body's): beyond it an adjacent-sign
        # pair is an aversion and nothing else -- 21 / fn 58 give it no
        # looking, and no ruling reaches it.
        if gap <= PLANETARY_ORBS.get(earlier, 7.0):
            strikes.setdefault(earlier, []).append((gap, later, row))
    for earlier, candidates in strikes.items():
        nearest = min(c[0] for c in candidates)
        first = [c for c in candidates if c[0] == nearest]
        occupied = doctrinal_or(*[(_is_connected_sahl(r) if reading('CONNECTION_PROFILE') == 'Sahl'
                                  else _sahl_connection_value(r)) for r in ordinary[earlier]])
        # A completed body that is not the first ahead has no residue: 20's
        # "first in that light" refuses it as it refuses a fresh strike.
        for gap, later, row in candidates:
            if row not in [c[2] for c in first] and _encounter_phase(row) == 'completed':
                row['residue_admission'] = False
        for gap, later, row in first:
            # A completed residue is the strike's own post-exact phase, so it
            # stands under 20's two conditions as the strike does: the striker
            # connecting with nothing else, and this body the one first in its
            # light -- recorded in residue_admission (True / False / unresolved)
            # so that membership, the label, the note and PN IV read ONE
            # decision. A completed nearest body still holds its place in the
            # ordering, so no farther body becomes the first one ahead.
            if _encounter_phase(row) == 'completed':
                if occupied is True:
                    row['residue_admission'] = False
                elif len(first) > 1:
                    # The same shared first-body choice as a fresh strike's tie,
                    # so two equally placed residues (or a residue beside a fresh
                    # candidate) are one exclusive choice, never two independent ones.
                    row['strike_tie'] = tuple(sorted((p, planetary_data[p]['longitude']) for _, p, _ in first))
                    row['residue_admission'] = _connection_decision(row, UnresolvedResult(
                        'equally placed first bodies; no priority is supplied', 'Sahl, The Introduction Ch. 3, 20–21'))
                    if row['residue_admission'] is True:
                        row['body_residue'] = True
                        row['sahl_body_connection'] = gap < 1.0
                elif is_unresolved(occupied):
                    row['residue_admission'] = UnresolvedResult(
                        'an ordinary connection is unresolved, so absence of other connections is not established',
                        'Sahl, The Introduction Ch. 3, 20–21')
                else:
                    row['residue_admission'] = True
                    row['body_residue'] = True
                    row['sahl_body_connection'] = gap < 1.0
                continue
            if occupied is True:
                continue
            # A strike is an approach (20: the light "reaches" the next sign
            # and the other planet is "first in that light"): a pair whose gap
            # is not closing is admitted to nothing.
            if row.get('motion') != 'Applying':
                continue
            if len(first) > 1:
                row['strike_tie'] = tuple(sorted((p, planetary_data[p]['longitude']) for _, p, _ in first))
                row['strike_admission'] = _connection_decision(row, UnresolvedResult(
                    'equally placed first bodies; no priority is supplied', 'Sahl, The Introduction Ch. 3, 20–21'))
            elif is_unresolved(occupied):
                row['strike_admission'] = UnresolvedResult(
                    'an ordinary connection is unresolved, so absence of other connections is not established',
                    'Sahl, The Introduction Ch. 3, 20–21')
            else:
                row['strike_admission'] = True
            row['sahl_body_connection'] = row['strike_admission'] is True

    return rows

# --- Connection profiles --------------------------------------------------
# Sahl and Abu Ma'shar agree that looking is sign-to-sign and connecting is
# degree-to-degree (Sahl Ch.3, 23), but they part company at the sign
# boundary and on what activates a connection. Rather than blend them into
# one Boolean, each author's rule is written out separately and one is made
# active; every downstream doctrine reads _is_connected(), which dispatches.
CONNECTION_PROFILE = 'Sahl'

def _encounter_phase(row):
    """Explicit encounter evidence, or ordinary separation under Sahl 3.22.

    The direct light planet beyond its selected contact and moving away
    establishes ordinary separation positionally. Exceptional motions still
    require evidence; widening alone does not establish completion.
    """
    history = row.get('encounter')
    if history:
        phase = history.get('phase')
        if phase == 'revoked':
            events = history.get('events', ())
            if tuple(events) != ('approach', 'retrograde turn before contact', 'departure'):
                return None
        return phase if phase in ('completed', 'uncompleted', 'revoked') else None
    if row.get('motion') != 'Separating':
        return None
    light_speed, heavy_speed = row.get('light_speed'), row.get('heavy_speed')
    if (light_speed is None or heavy_speed is None
            or light_speed <= 0 or light_speed <= heavy_speed):
        return None
    positions = dict(zip((row.get('p1'), row.get('p2')), row.get('positions', ())))
    light, heavy = row.get('light_name'), row.get('heavy_name')
    if light not in positions or heavy not in positions:
        return None
    signed_body_gap = (positions[light] - positions[heavy] + 180.0) % 360.0 - 180.0
    if row.get('aspect_name') == 'Aversion':
        # Only the adopted adjacent-sign bodily residue, never an aspect.
        if row.get('signs_apart') != 1:
            return None
        past_contact = signed_body_gap
    else:
        deviation = row.get('deviation')
        if deviation is None:
            return None
        # Select the same directed ray as the existing aspect geometry.
        # This handles dexter/sinister aspects and the 360-degree wrap.
        past_contact = deviation * (1.0 if signed_body_gap >= 0 else -1.0)
    return 'completed' if past_contact > 0 else None


def _unknown_encounter():
    return UnresolvedResult('the encounter history does not establish whether exact contact was completed',
                            'Sahl, The Introduction Ch. 3, 7–10; Gr. Intr. VII.5, 117')


def _sahl_connection_value(row):
    """Sahl Ch. 3, 6–21, with source-specific entry and departure tests.

    Same-sign application to the Sun uses his 15° (13); other approaches
    use the applicant's light (19). After established completion, retention
    is strictly inside the light planet's light in one sign (10), otherwise
    strictly inside one degree (9). Cross-sign bodily residue applies 9 by
    the declared inference; it is not the striker's whole light.
    """
    # A widening adjacent-sign pair inside one degree is connected only as
    # the strike's admitted residue: the strike loop's residue_admission is
    # the one decision, and 20's two conditions (the striker connecting with
    # nothing else, this body first in its light) refuse it as they refuse a
    # fresh strike -- membership, label, note and PN IV then agree.
    if (row['aspect_name'] == 'Aversion' and row['signs_apart'] == 1
            and row.get('motion') in ('Separating', 'No relative motion')):
        if abs(row['deviation']) >= 1.0:
            return False
        phase = _encounter_phase(row)
        if phase == 'completed':
            admission = row.get('residue_admission', False)
            return admission if is_unresolved(admission) else admission is True
        if phase in ('uncompleted', 'revoked'):
            return False
        return _unknown_encounter()
    aversion = row['aspect_name'] == 'Aversion'
    admission = True
    if aversion and not row.get('body_residue'):
        admission = row.get('strike_admission', row.get('sahl_body_connection', False))
        if admission is False:
            return False
    if row.get('deviation') is None:
        return False
    remaining = abs(row['deviation'])
    if remaining == 0.0:
        return True
    phase = _encounter_phase(row)
    if phase in ('revoked', 'uncompleted') and row['motion'] != 'Applying':
        return False
    if row['motion'] == 'Applying':
        if aversion:
            return admission
        actor = row.get('applicant') or row['light_name']
        limit = 15.0 if row['signs_apart'] == 0 and row.get('receiver') == 'Sun' else PLANETARY_ORBS.get(actor, 7.0)
        return remaining <= limit
    limit = PLANETARY_ORBS.get(row['light_name'], 7.0) if row['signs_apart'] == 0 else 1.0
    if remaining >= limit:
        return False
    return doctrinal_and(admission, True if phase == 'completed' else _unknown_encounter())

def _is_connected_sahl(row):
    return _connection_decision(row, _sahl_connection_value(row))


def _is_connected_abu_mashar(row):
    """Gr. Intr. VII.4-5. Two flat activation
    distances rather than Sahl's per-planet lights:

    - Assembly is same-sign (VII.4, 3), and its power begins "if there were
      15 degrees and less between one of them and the other."
    - "The beginning of the power of the connection by aspect is when there
      are 12 degrees between the two planets" (VII.5, 27) -- one figure for
      every pair, since (per the note there) "aspect rays are not given
      specific orbs, because they are not bodies with a glow of power
      around them."

    And no out-of-sign connection of any kind: VII.4, 13 says planets in
    two different signs are "NOT said to be united ... because of the
    difference of their signs," and VII.5, 14 that few degrees across a
    boundary "is not counted as a connection by assembly, but they are both
    mixing their natures in a weak way." That weak mixing is reported
    separately (see row['mutual_body']), never as a connection."""
    if row['aspect_name'] == 'Aversion':
        return False
    # 34, recovered with p. 452: "in all of this, WHEN ONE OF THEM GOES
    # BEYOND ITS ASSOCIATE BY 1' OR LESS, THEN IT HAS ALREADY SEPARATED
    # FROM IT -- except that they will both be BLENDING IN NATURE."
    #
    # So for Abu Ma'shar a connection ends AT the exact degree: VII.5, 16
    # says the same of the assembly ("if the light one passed by the slow
    # one by one minute or by less than that, then it has already
    # SEPARATED"). "By 1' or less" is the amount that already counts as
    # separated, not a grace interval after exactness -- an earlier version
    # kept a pair connected for up to a minute past exact, inverting the
    # sentence it quoted (Astra F09). His activation distances below are
    # approach windows, not two-sided orbs. The residue he does allow is a
    # mixing of natures, which this file reports separately as the body
    # overlap, never as a connection. The 1e-9 degree is machine tolerance
    # at exactness, nothing doctrinal.
    if row['motion'] in ('Separating', 'No relative motion') and abs(row['deviation']) > 0.0:
        return False
    if row['assembly']:
        return row['dist'] <= 15.0
    return abs(row['deviation']) <= 12.0

CONNECTION_PROFILES = {'Sahl': _is_connected_sahl, "Abu Ma'shar": _is_connected_abu_mashar}

def _is_connected(row):
    """Whether the pair is Connected under the doctrine currently in force."""
    return CONNECTION_PROFILES[reading('CONNECTION_PROFILE')](row)


@_connection_aware
def evaluate_mercury_company(planetary_data):
    """Abu Maʿshar IV.9, 10 shown beside, never applied over, phase sect.

    "Conjoins" uses the aspect table's same-sign assembly; "connects" uses
    the documented connection profile currently selected for dual-author
    tables. The Sun is excluded by the owner's adopted display policy.
    """
    if 'Mercury' not in planetary_data or 'Sun' not in planetary_data:
        return []
    phase = mercury_phase_sect(planetary_data['Mercury']['longitude'],
                               planetary_data['Sun']['longitude'])
    companions = []
    for relation in _pairwise_configurations(planetary_data):
        if 'Mercury' not in (relation['p1'], relation['p2']):
            continue
        other = relation['p2'] if relation['p1'] == 'Mercury' else relation['p1']
        if other in ('Mercury', 'Sun'):
            continue
        assembly = relation['aspect_name'] == 'Conjunction'
        connected = _is_connected(relation)
        if not assembly and not connected:
            continue
        companion_diurnal = planet_sect_is_diurnal(
            other, planetary_data[other]['longitude'], planetary_data['Sun']['longitude'])
        relationship = 'same-sign assembly' if assembly else (
            f"{relation['aspect_name'].lower()}, connected under the {reading('CONNECTION_PROFILE')} profile")
        companions.append((other, companion_diurnal, relationship))

    if not companions:
        return []
    company_sects = {value for _name, value, _relationship in companions}
    if len(company_sects) > 1:
        summary = 'Conflicting company indications; no supplied tie-break and no company override is applied'
    else:
        company_diurnal = next(iter(company_sects))
        company_name = 'Diurnal' if company_diurnal else 'Nocturnal'
        if isinstance(phase, UnresolvedResult):
            comparison = 'phase is unassigned'
        else:
            comparison = 'agrees with phase' if phase == company_diurnal else 'disagrees with phase'
        summary = f'{company_name} by company; {comparison}; no company override is applied'
    return [{
        'Companion': name,
        'Relationship': relationship,
        'Companion sect': 'Diurnal' if companion_diurnal else 'Nocturnal',
        'Company indication': summary,
        'Precedence': 'Unspecified in IV.9; displayed separately',
    } for name, companion_diurnal, relationship in companions]

def _rules_differ(row):
    """'Yes' where Sahl's and Abu Ma'shar's connection tests disagree on
    this pair -- the aspects table's way of making the rule switch visible."""
    sahl, abu = _is_connected_sahl(row), _is_connected_abu_mashar(row)
    return sahl if is_unresolved(sahl) else 'Yes' if sahl != abu else ''

def _is_separating_in_nature(row):
    """Abu Ma'shar's window for "separating from" a planet, which is NOT
    his connection window. VII.5, 34 ends a connection at 1' past exact, so
    under his profile _is_connected() is False for every separating pair --
    which left VII.6, 4 ("separating from a fortune and connecting with a
    fortune") and the separating/connecting form of VII.6, 57 unreachable:
    one hit in 1,680 sampled placements. His own text gives the window:
    after separating, "one of them will be 'in the nature' of its partner
    so long as it is in the sign in which they united" (VII.5, 17), and for
    rays, "the one of them will remain in the light of the other so long as
    the quick one is in that sign it is in" (VII.5, 35). A configured,
    separating pair at the chart moment is therefore still "separating
    from" until a sign changes, which is the same reading _sahl_enclosed()
    already takes for Sahl's Fig. 25."""
    return row['aspect_name'] != 'Aversion' and row['motion'] == 'Separating'

@contextmanager
def doctrine(author):
    """Pin the connection rule for the duration of one author's own table.

    The sidebar's Connection rule used to rewrite a module global that
    roughly thirty evaluators read, so a table headed "Sahl, The
    Introduction Ch.3, 24-27" could be running Abu Ma'shar's flat 12-degree
    orb -- the heading claiming one author while the arithmetic ran the
    other's. Under his profile Abu Ma'shar's 43 fired on 81% of placements
    against 8% under his own.

    An author's own evaluator now pins its own rule and restores whatever
    was in force. The sidebar selection still governs the genuinely dual
    tables -- the aspect grid, reception, blocking, cutting --
    which exist precisely to show both authors side by side, and which name
    the rule in force in their own headings.
    """
    previous = reading('CONNECTION_PROFILE')
    set_readings(CONNECTION_PROFILE=author)
    try:
        yield
    finally:
        set_readings(CONNECTION_PROFILE=previous)

SAHL, ABU_MASHAR = 'Sahl', "Abu Ma'shar"

def _body_overlap_label(row):
    """How the two bodies' spheres of power overlap, reported on its own
    rather than folded into the Connected verdict (VII.4, 5-8)."""
    if row['mutual_body']:
        return 'Mutual'
    if row['heavy_in_light_body']:
        return f"{row['heavy_name']} in {row['light_name']}'s body"
    if row['light_in_heavy_body']:
        return f"{row['light_name']} in {row['heavy_name']}'s body"
    return '–'

def _connection_state(row, connected):
    """Named state, distinct from geometry and from the presence of light."""
    if row.get('deviation') == 0.0 and row['aspect_name'] != 'Aversion':
        return 'Exact — connection completed'
    if is_unresolved(connected):
        return connected
    phase = _encounter_phase(row)
    if phase == 'revoked' and row.get('motion') != 'Applying':
        return 'Revoked — Abū Maʿshar, Gr. Intr. VII.5, 117'
    if row.get('motion') == 'No relative motion':
        return 'Under a single blanket' if connected and phase == 'completed' else 'No relative motion'
    if row.get('motion') == 'Applying':
        if connected:
            return 'Applying'
        return '–' if row['aspect_name'] == 'Aversion' else 'Not yet'
    if row['aspect_name'] == 'Aversion' and row.get('strike_admission', False) is False and not (
            row.get('body_residue') and reading('CONNECTION_PROFILE') == 'Sahl'):
        if is_unresolved(row.get('residue_admission')) and reading('CONNECTION_PROFILE') == 'Sahl':
            return row['residue_admission']   # a completed residue whose admission 20 leaves unresolved
        return '–'   # an adjacent-sign pair outside the strike's reach is an aversion and nothing else
    if phase == 'uncompleted':
        return 'Not applying — gap widening'
    if phase == 'completed':
        return 'Under a single blanket' if connected else 'Separated'
    # Being outside the retention window decides membership, but not history.
    if reading('CONNECTION_PROFILE') == 'Sahl':
        return _unknown_encounter()
    return 'Separated'

def evaluate_ptolemaic_aspects(planetary_data, encounter_history=None):
    """Aspects, Aversions & Connections per Sahl (The Introduction Ch.2,
    50-60) and Abu Ma'shar (Gr. Intr. VII.3-4): Sextile/Square/
    Trine/Opposition are fully formed once whole-sign configured (50-56) --
    no degree orb gates them. Union (Assembly) is unconditional same-sign
    co-presence, graded Strong/Partial/Co-present by each planet's own orb
    (VII.4, 5-8). Connection (Ch.3) is layered on top as a per-pair
    refinement of how close an applying/separating pair currently is, and
    its state is named by _connection_state()."""
    rows = _pairwise_configurations(planetary_data, encounter_history)
    aspects = []

    for row in rows:
        light_name, heavy_name = row['light_name'], row['heavy_name']

        if row['aspect_name'] == 'Aversion':
            # VII.4, 13-14: two planets in different (here, adjacent) signs
            # can still be "in the power of" each other's body by degree --
            # not an assembly (different signs), but a minor indication,
            # worth noting since it's the only case an Aversion pair can
            # still carry any classical significance at all.
            note = '\u2013'
            if row['signs_apart'] == 1 and (row['light_in_heavy_body'] or row['heavy_in_light_body']):
                note = 'In Power (out-of-sign)'
            if row.get('body_residue') and reading('CONNECTION_PROFILE') == 'Sahl':
                note = 'Completed bodily encounter; one-degree residue inferred from Sahl 9–10'
            elif _is_connected(row) is True:
                note = f"Body connection: {row['body_connection_from']} \u2192 {row['body_connection_to']} (Sahl 20-21)"
            has_body_state = (row.get('strike_admission', False) is not False or row.get('body_residue', False)
                              or (row['signs_apart'] == 1 and row.get('motion') in ('Separating', 'No relative motion')
                                  and row['dist'] < 1.0))
            # Same keys, same order, as the configured rows below: pandas
            # takes the first row's order, so a chart whose first pair was
            # an aversion used to get a different column order.
            aspects.append({
                'Light Planet': light_name,
                'Aspect': 'Aversion',
                'Heavy Planet': heavy_name,
                'Connecting planet': (f"{row['applicant']} → {row['receiver']}" if has_body_state else '–'),
                'Motion': (row.get('motion') or '–') if has_body_state else '–',
                'Orientation': '\u2013',
                'Exact Orb Dist': _format_orb(abs(row['deviation'])) if has_body_state else '–',
                'Bodies': _body_overlap_label(row),
                'Strength': note,
                'Connection': _connection_state(row, _is_connected(row)),
                'Rules differ': _rules_differ(row),
            })
            continue

        connected = _is_connected(row)

        if row['aspect_name'] == 'Conjunction':
            # Assembly strength (VII.4, 5-8): mutually strong if each
            # planet's own orb reaches the other; one-sided/weaker if only
            # the wider orb does; otherwise just nominal same-sign
            # co-presence. A shared bound (term) makes it more powerful still.
            if row['mutual_body']:
                strength = 'Strong'
            elif row['light_in_heavy_body'] or row['heavy_in_light_body']:
                strength = 'Partial'
            else:
                strength = 'Co-present'
            lon1 = planetary_data[row['p1']]['longitude']
            lon2 = planetary_data[row['p2']]['longitude']
            if get_essential_rulers(lon1)['term'] == get_essential_rulers(lon2)['term']:
                strength += ' (same bound)'
        else:
            # VII.5, 4 says only that a look grows weaker as it moves away
            # from the exact degree. It supplies no cutoffs and neither
            # author sums two planetary radii to create them. Exact Orb Dist
            # preserves the stated continuum; Strength is reserved for the
            # source-backed assembly grades above.
            strength = '–'

        aspects.append({
            'Light Planet': light_name,
            'Aspect': row['aspect_name'],
            'Heavy Planet': heavy_name,
            # Natural rank above, directed agency here. They coincide for
            # about 96% of configured pairs; the column exists for the rest,
            # where the naturally heavier planet is the one closing.
            'Connecting planet': (
                f"{row['applicant']} → {row['receiver']}"
                + (f" ({row['application_cause']})" if row['applicant_is_heavier'] else '')
            ) if row['applicant'] else '–',
            # VII.5, 24: with either planet retrograde the approach or
            # departure happens "by retrogradation" -- flagged rather than
            # allowed to quietly reassign the light/heavy roles.
            'Motion': row['motion'] + (' (retrogradation — Abū Maʿshar VII.5.24, 118)' if row['by_retrogradation'] else ''),
            'Orientation': row['orientation'],
            'Exact Orb Dist': _format_orb(abs(row['deviation'])),
            'Bodies': _body_overlap_label(row),
            'Strength': strength,
            'Connection': _connection_state(row, connected),
            'Rules differ': _rules_differ(row),
        })

    return aspects

# --- Transfer & Collection of light -- Sahl, The Introduction Ch.3, 24-30 ----

def _sahl_body_row(row):
    """Admitted Sahl bodily connection, including retained or unknown residue.

    Whole-sign aversion remains a separate fact. Consumers inside a complete
    judgment evaluate an unknown residue under both permitted histories.
    """
    if row['aspect_name'] != 'Aversion':
        return False
    return _is_connected_sahl(row)


def _sahl_has_separated(row):
    """Sahl Ch. 3, 22, 24–27, 119–123: departure after a meeting.

    This is not current connected membership: separation can remain true
    outside the retained blanket, as the transfer example two degrees past
    the aspect demonstrates. A known revoked approach is not a separation.
    """
    if row.get('motion') != 'Separating':
        return False
    phase = _encounter_phase(row)
    if phase == 'completed':
        return True
    if phase in ('uncompleted', 'revoked'):
        return False
    return _connection_decision(row, _unknown_encounter())


@_connection_aware
def evaluate_transfers_of_light(planetary_data):
    """Transfer of light: the core concept (a carrier separating from one
    planet and connecting to another) is Sahl's own (Ch.3, 24-27). The
    two-type breakdown below is Abu Ma'shar's addition (Great Introduction
    VII.5, 83-85, Fig. 126) -- Sahl does not distinguish a Type II.

    Type I: a "carrier" planet separates from one planet it is (or was) in
    aspect with, and connects with another, transferring the first
    planet's nature to the second. "Separates from" only requires the
    carrier to be the faster body, formerly-applying-now-past-exact
    (Motion == Separating) in a valid (non-Aversion) aspect -- not the
    tighter Connected threshold, which the book's own example (25-27)
    doesn't satisfy for Moon/Mercury (2 degrees past an exact Sextile,
    outside the 1-degree cross-sign Connected allowance) yet is still
    plainly described as "separating." "Connects to" does require
    Connected == True (24-25: "connecting with another... already
    connected with").

    Type II (84-85): a light planet connects with a slower one, which is
    itself already connecting onward with a third, even slower planet --
    the slow one shifts the light one's nature onto the third.

    The carrier must be the faster body in BOTH legs -- confirmed as a
    deliberate choice, not an oversight, after cross-checking a real chart
    (29 Oct 1990, 13:02 EST, Pontiac MI) against Janus's medieval module:
    Janus named the Sun as translating Mercury's light to Venus there, even
    though the Sun is slower than both (0.999 deg/day vs Mercury's 1.603
    and Venus's 1.254) -- the looser, more common modern/Lilly-style
    convention, where the carrier only needs its own angular gap to each
    to be independently shrinking/growing. Sahl's text ("the light, quick
    star... separates from the slow one, and connects with another")
    requires the SAME planet to be light/fast relative to both, which the
    Sun fails here on both counts -- so this implementation declines to
    name it a carrier, matching the sources this project is built on over
    the more permissive mainstream-software convention."""
    with doctrine(SAHL):
        rows = _pairwise_configurations(planetary_data)
        # The carrier is the planet that MOVES between the other two -- "the
        # light planet separates from the heavy planet, and is connecting with
        # another" (Ch.3, 24). Keyed on the directed applicant rather than on
        # natural rank, so that a planet carrying light by retrogradation is
        # recognised as the carrier rather than as the thing carried.
        by_fast = {}
        for row in rows:
            if row['aspect_name'] == 'Aversion' and not _sahl_body_row(row):
                continue
            by_fast.setdefault(row['applicant'] or row['light_name'], []).append(row)

        def _other(r):
            return r['receiver'] or r['heavy_name']

        applying_connected_to = {
            fast: {_other(r) for r in fast_rows if r['motion'] == 'Applying' and _is_connected(r)}
            for fast, fast_rows in by_fast.items()
        }

        transfers = []
        for carrier, carrier_rows in by_fast.items():
            separating_from = [_other(r) for r in carrier_rows if _sahl_has_separated(r)]
            connecting_to = applying_connected_to.get(carrier, set())
            for a in separating_from:
                for b in connecting_to:
                    if a != b:
                        transfers.append({'Type': 'I', 'Carrier': carrier, 'Separates From': a, 'Connects To': b})

        for light, mediums in applying_connected_to.items():
            for medium in mediums:
                for onward in applying_connected_to.get(medium, set()):
                    if onward != light:
                        transfers.append({'Type': 'II', 'Carrier': light, 'Separates From': f"(via {medium})", 'Connects To': onward})

        return transfers

COLLECTION_POLICY = "Both planets apply to a heavier collector; this display admits separating and averse pairs but, following an editorial convention, excludes pairs still applying to each other."


def _applying_connection(row):
    """The Configurations applying fact, also used by collection's exclusion."""
    return row['motion'] == 'Applying' and _is_connected(row)


def _collection_records(planetary_data):
    """Sahl Ch. 3, 28 / Gr. Intr. VII.5, 86, with the declared editorial exclusion.

    Called inside a complete connection judgment. Both collector legs and
    the exclusion read F6 membership and Motion, on the selected profile.
    """
    rows = _pairwise_configurations(planetary_data)
    pairs = {frozenset((r['p1'], r['p2'])): r for r in rows}
    applications = {}
    for r in rows:
        if _applying_connection(r):
            applications[(r['applicant'] or r['light_name'],
                          r['receiver'] or r['heavy_name'])] = True
    records = []
    for collector in WEIGHT_ORDER:
        lights = sorted(p for p in planetary_data if p in WEIGHT_ORDER
                        and WEIGHT_ORDER.index(p) > WEIGHT_ORDER.index(collector)
                        and applications.get((p, collector), False))
        for a, b in combinations(lights, 2):
            mutual = pairs[frozenset((a, b))]
            applying = applications.get((a, b), False) or applications.get((b, a), False)
            connected = _is_connected(mutual)
            records.append({'Collector': collector, 'Collects': f'{a} & {b}',
                            'Planets': (a, b), 'Profile': reading('CONNECTION_PROFILE'),
                            'Mutual sight': mutual['aspect_name'] != 'Aversion',
                            'Mutual application': applying,
                            'Residual connection': connected and mutual['motion'] not in ('Applying', 'Exact'),
                            'Applications to collector': ((a, collector), (b, collector)),
                            'Heavier collector': True, 'Editorial exclusion': applying,
                            'Admitted': not applying})
    return records


@_connection_aware
def evaluate_collections_of_light(planetary_data):
    """Each admitted pair, with the same evidence read by every consumer."""
    return [r for r in _collection_records(planetary_data) if r['Admitted']]

@_connection_aware
def evaluate_sahl_banishment(planetary_data):
    """Sahl's "banished" planet (The Introduction Ch.3, 64): "the banished
    planet is the planet which NONE OF THE PLANETS CONNECTS TO." That is a
    statement about connections, not about signs: a planet can share a
    trine with every other body and still be banished if no one is inside
    a live connection with it, and it can hold an out-of-sign body
    connection (20-21) without being banished at all. Connection here is
    Sahl's own (6-21, _is_connected_sahl): a state of the pair, so a planet
    applying to another within its light is connected and neither side is
    banished.

    This is NOT Abu Ma'shar's wildness (VII.5, 79-82), which is whole-sign
    aversion to every planet -- see evaluate_abu_wildness(). An earlier
    version ran the aversion rule under both names, so Sahl's table showed
    his rule's absence as a fact about his text. Dykes' note on 64 calls
    Sahl's an early, less precise form of the later definition; the two are
    kept apart so the student can see where they disagree.

    Each row says how close the planet came, so an empty table and a
    near-miss look different."""
    with doctrine(SAHL):
        rows = _pairwise_configurations(planetary_data)
        planets = [p for p in planetary_data.keys() if p != 'North Node']
        connected = set()
        for row in rows:
            if (row['aspect_name'] != 'Aversion' or _sahl_body_row(row)) and _is_connected(row):
                connected.update((row['p1'], row['p2']))
        results = []
        for p in planets:
            if p in connected:
                continue
            nearest = None
            for row in rows:
                if p not in (row['p1'], row['p2']) or row['aspect_name'] == 'Aversion':
                    continue
                if nearest is None or abs(row['deviation']) < abs(nearest['deviation']):
                    nearest = row
            if nearest is None:
                closest = 'in aversion to every planet'
            else:
                other = nearest['p2'] if nearest['p1'] == p else nearest['p1']
                # Name the test the near-miss fails: the applicant's own
                # light (19), half the light one's body in one sign (10),
                # or the full degree across signs (9).
                if nearest['motion'] == 'Applying':
                    actor = nearest['applicant'] or nearest['light_name']
                    why = f"outside {actor}'s light of {PLANETARY_ORBS.get(actor, 7.0):.0f}\u00b0 (19)"
                elif nearest['signs_apart'] == 0:
                    light = nearest['light_name']
                    why = f"past half of {light}'s body, {PLANETARY_ORBS.get(light, 7.0):.0f}\u00b0 (10)"
                else:
                    why = 'past the full degree of separation across signs (9)'
                closest = (f"{other}: {nearest['aspect_name'].lower()} by sign, {abs(nearest['deviation']):.1f}\u00b0 from exact "
                           f"and {nearest['motion'].lower()}, {why}")
            results.append({'Planet': p, 'Closest configured planet': closest})
        return results

def evaluate_abu_wildness(planetary_data):
    """Abu Ma'shar's wildness (Gr. Intr. VII.5, 79-82, Fig. 125):
    "if a planet is in a sign such that absolutely no planet looks at it,"
    which his own figure glosses as "in aversion to all other planets."
    Whole-sign, and independent of degree. Per 80-81, a wild planet still
    counts as connected with the lord of whatever bound it currently
    occupies, noted here rather than negating the flag.

    Distinct from Sahl's banishment (Ch.3, 64), which is about live
    connections and is in evaluate_sahl_banishment()."""
    rows = _pairwise_configurations(planetary_data)
    planets = [p for p in planetary_data.keys() if p != 'North Node']
    aversion_count = {p: 0 for p in planets}
    for row in rows:
        if row['aspect_name'] == 'Aversion':
            aversion_count[row['p1']] += 1
            aversion_count[row['p2']] += 1
    body_connected = {p for row in rows if row.get('sahl_body_connection')
                       for p in (row['p1'], row['p2'])}
    results = []
    for p in planets:
        if aversion_count[p] == len(planets) - 1:
            lon = planetary_data[p]['longitude']
            row = {'Planet': p, 'Bound Lord (residual connection)': get_essential_rulers(lon)['term']}
            # Whole-sign aversion to everything is not broken by an out-of-
            # sign body connection (Ch.3, 21: the two "will not see" each
            # other) -- but that connection means the planet is NOT banished
            # by Sahl's wording, and his own table will say so.
            if p in body_connected:
                row['Note'] = ("Wild by whole-sign aversion; holds an out-of-sign body connection "
                                "(Sahl Ch. 3, 20-21), so it is not banished in Sahl's table")
            results.append(row)
    return results

# --- Natural connections (Gr. Intr. VII.5, 53-77) ----------------------
# "Another type of connection and separation [even] without the planets'
# looking at each other is said to be a 'natural connection and
# separation'" (53), "and it is of two types" (55). Each is a family of
# SIGN PAIRS whose degrees correspond, so that a planet in one sign "is
# in the nature of the degree of" a planet in the other, and connects or
# separates with it as the degrees meet or part. The corresponding degree
# is the complement within the sign: "when a planet is in the first
# degree of Aries, then it is in the nature of a planet which is at the
# last degree of Pisces" (57); "the planet which is in 12° of Gemini is
# in the nature of the degree of the planet which is in 18° of
# Capricorn" (62).
#
# Both lists are exactly as Abu Ma'shar enumerates them here. The
# equal-daylight family is the antiscia (note 162), which as a complete
# scheme also pairs Aquarius with Scorpio -- but 67-75 does not say so,
# and the notes on 76-77 record two further pairs he "omits". None of
# those is added: the standing rule of this file is the text in hand over
# what the family implies, and the coverage note names them as unbuilt.
EQUAL_ASCENSION_PAIRS = {                                  # 56
    frozenset({'Aries', 'Pisces'}), frozenset({'Taurus', 'Aquarius'}),
    frozenset({'Gemini', 'Capricorn'}), frozenset({'Cancer', 'Sagittarius'}),
    frozenset({'Leo', 'Scorpio'}), frozenset({'Virgo', 'Libra'}),
}
EQUAL_DAYLIGHT_PAIRS = {                                   # 67-75
    frozenset({'Gemini', 'Cancer'}), frozenset({'Taurus', 'Leo'}),
    frozenset({'Aries', 'Virgo'}), frozenset({'Libra', 'Pisces'}),
    frozenset({'Sagittarius', 'Capricorn'}),
}
# 76-77 name four of each family's pairs as bridging an ordinary aversion:
# "the connection of the planet which is in Gemini with the degree of the
# planet which is in Capricorn ... is called a 'natural connection by
# opposition'" (76); "the one in Gemini with the one in Cancer ... is
# called the 'natural connection by sextile'" (77).
NATURAL_OPPOSITION_PAIRS = {
    frozenset({'Gemini', 'Capricorn'}), frozenset({'Sagittarius', 'Cancer'}),
    frozenset({'Aries', 'Virgo'}), frozenset({'Libra', 'Pisces'}),
}
NATURAL_SEXTILE_PAIRS = {
    frozenset({'Gemini', 'Cancer'}), frozenset({'Virgo', 'Libra'}),
    frozenset({'Sagittarius', 'Capricorn'}), frozenset({'Pisces', 'Aries'}),
}

def _natural_family(sign_a, sign_b):
    """The natural-connection family of a sign pair, or None."""
    pair = frozenset({sign_a, sign_b})
    if pair in EQUAL_ASCENSION_PAIRS:
        return 'Equal ascensions (56-66)'
    if pair in EQUAL_DAYLIGHT_PAIRS:
        return 'Equal daylight (67-75)'
    return None

def _counterpart_degree(lon):
    """The degree of the partner sign that corresponds to `lon`'s degree in
    its own: the complement within the sign (57, 62). 0.0 pairs with 30.0,
    the sign's last degree at its far edge."""
    return 30.0 - (lon % 30.0)

def evaluate_abu_natural_connections(planetary_data):
    """Abu Ma'shar's natural connections (VII.5, 53-77) -- a relation of its
    own, neither a Ptolemaic aspect nor a dignity. Every pair standing in
    one of the enumerated sign pairs is reported, with how far the second
    planet stands from the first's counterpart degree, and whether the
    two are closing or parting.

    Motion follows 62 -- "so when it passes beyond 12° of Gemini, then it
    has separated from it and comes to be in the nature of the planet
    which is in less than 18° of Capricorn" -- so the counterpart degree
    moves AGAINST its planet's own motion, and the gap between the second
    planet and it closes at the SUM of the two speeds. Two direct planets
    are therefore applying only while the second is still short of the
    counterpart, and separating once past it; a retrograde planet reverses
    its own contribution.

    Abu Ma'shar gives no orb here: a planet in Aries is always in the
    nature of SOME degree of Pisces (57-59), and the connection completes
    where the degrees coincide. The row reports the distance and the
    student judges; nothing is suppressed by an invented window. The
    ordinary whole-sign relation is repeated on the row precisely so that
    an Aries/Pisces pair reads as both in natural connection and in
    aversion: 53 says "without the planets' looking at each other", and no
    out-of-sign aspect is created anywhere from this."""
    rows = _pairwise_configurations(planetary_data)
    results = []
    for row in rows:
        a, b = row['p1'], row['p2']
        lon_a, lon_b = planetary_data[a]['longitude'], planetary_data[b]['longitude']
        sign_a, sign_b = get_zodiac_sign(lon_a), get_zodiac_sign(lon_b)
        if sign_a == sign_b:
            continue
        family = _natural_family(sign_a, sign_b)
        if family is None:
            continue
        counterpart = SIGN_ORDER.index(sign_b) * 30.0 + _counterpart_degree(lon_a)
        deviation = ((lon_b - counterpart + 180.0) % 360.0) - 180.0
        rate = planetary_data[a]['speed_in_lon'] + planetary_data[b]['speed_in_lon']
        if abs(deviation) <= 1.0 / 60.0:
            motion = 'Exact'
        elif deviation * rate < 0:
            motion = 'Applying'
        else:
            motion = 'Separating'
        pair = frozenset({sign_a, sign_b})
        affinity = ('natural opposition (76)' if pair in NATURAL_OPPOSITION_PAIRS
                    else 'natural sextile (77)' if pair in NATURAL_SEXTILE_PAIRS else '')
        results.append({
            'Pair': f'{a} & {b}', 'Family': family,
            'Degrees': (f"{a} {lon_a % 30:.1f}\u00b0 {sign_a[:3]} \u2194 {_counterpart_degree(lon_a):.1f}\u00b0 {sign_b[:3]}; "
                        f"{b} at {lon_b % 30:.1f}\u00b0 {sign_b[:3]}"),
            'From exact': f'{abs(deviation):.1f}\u00b0', 'Motion': motion,
            'Affinity (76-77)': affinity,
            'Ordinary aspect': row['aspect_name'],
            'Standing': 'direct: sign pairs and degree rule as enumerated',
        })
    return results

@_connection_aware
def evaluate_reflections_of_light(planetary_data, ascendant_lon):
    """VII.5, 87–89: Type I is the detected collection subset in aversion
    with onward rays. Type II permits aversion OR mutual separation;
    footnotes 170–171 give the narrower aversion-only editorial account.
    Looking-only arrangements outside detected collections are not tested.
    """
    with doctrine(ABU_MASHAR):
        rows = _pairwise_configurations(planetary_data)
        aversion_pairs = {frozenset({r['p1'], r['p2']}) for r in rows if r['aspect_name'] == 'Aversion'}

        separating_pairs = {frozenset((r['p1'], r['p2'])) for r in rows
                            if r['motion'] == 'Separating' and _sahl_has_separated(r)}
        reflections = []
        for t in evaluate_transfers_of_light(planetary_data):
            if t['Type'] == 'I' and frozenset({t['Separates From'], t['Connects To']}) in (aversion_pairs | separating_pairs):
                reflections.append({
                    'Reflection Type': 'II (Transfer)',
                    'Detail': f"{t['Carrier']} reflects between {t['Separates From']} and {t['Connects To']}",
                })

        for c in evaluate_collections_of_light(planetary_data):
            collected = c['Planets']
            if len(collected) == 2 and frozenset(collected) in aversion_pairs:
                collector = c['Collector']
                # The places the collector's OWN RAYS fall into. An earlier
                # version listed the whole-sign houses OCCUPIED BY OTHER
                # PLANETS that happened to aspect the collector, which is a
                # different set entirely -- it named where the collector is
                # looked at FROM, not where it looks TO, and it went silent
                # whenever no third planet happened to be configured to it.
                #
                # VII.5, 3: a planet "looks at every degree of the sign as well
                # as everything which is in it," so the reach is the seven
                # signs its rays fall in, independent of what occupies them.
                collector_lon = planetary_data[collector]['longitude']
                reached_houses = sorted({
                    get_wsh_house(d, ascendant_lon)
                    for d in _ray_degrees(collector_lon)[1:]   # skip its own body
                })
                detail = (f"{collector} collects {c['Collects']}, "
                           f"and its own rays reach houses {reached_houses}")
                reflections.append({'Reflection Type': 'I (Collection)', 'Detail': detail})
        return reflections

# How far outside the active author's own ray window Blocking Type II may
# still fire. Sahl's Fig. 14 / Abu Ma'shar's Fig. 131 needs one degree.
WORKED_FIGURE_TOLERANCE = 1.0

def _ray_activation_distance(row):
    """The active author's own approach window for this pair's ray: the
    applicant's light for Sahl (Ch.3, 13-19), a flat 12 degrees for Abu
    Ma'shar (VII.5, 27). The figure that connection would be measured
    against, before any tolerance is added."""
    if reading('CONNECTION_PROFILE') == SAHL:
        return PLANETARY_ORBS.get(row['applicant'] or row['light_name'], 7.0)
    return 12.0

@_connection_aware
def evaluate_blocking(planetary_data):
    """Blocking. Type I is Sahl's "Intervention" (The Introduction Ch.3,
    35-37), reused by Abu Ma'shar as his own Blocking Type I (Great
    Introduction VII.5, 91-92, footnote 172): three planets share a sign;
    the heaviest sits at the highest degree, and whichever of the other two
    sits at the middle degree blocks the lightest from reaching the
    heaviest until it passes by. Type II is Sahl's "Nullification" (The
    Introduction Ch.3, 38-48), reused by Abu Ma'shar as Blocking Type II
    (VII.5, 93-94, footnote 174): a light planet connects by aspect from
    another sign with a heavy one, while a third, lighter planet co-present
    with that heavy one is joining it by body and gets there first.

    Both types are reproduced against the authors' own worked figures --
    Fig. 13 (Moon 8, Mars 10, Saturn 12 Gemini: Mars intervenes) and
    Fig. 14 (Moon 10 Scorpio, Mars 15 and Saturn 23 Taurus: Mars cuts the
    Moon's aspect).

    An earlier version stated Type II as "a body connection categorically
    outranks an aspect connection regardless of relative degree closeness,"
    and so accepted ANY third planet sharing the heavy planet's sign, with
    no application and no exception. That conflated two different passages.
    Sahl 44-48 IS a flat precedence rule, but its subject is one planet
    holding both a union and an aspect of its own (45-48: the Moon uniting
    with Mars while aspecting Venus). The third-party blocking of 38-42 and
    VII.5, 93-94 is conditional, and both authors give it an escape: "if it
    goes beyond that, its connection is valid" (40), and "when the degrees
    of the one looking are closer to the connection than the degrees of the
    one joining by body, the connection belongs to the one looking" (94).
    Across ~3,600 sampled charts the correction removes 76% of reported
    blockings, nearly all of them third planets that were merely co-present
    and in fact SEPARATING from the heavy planet.

    Sahl's third blocking type, "Cutting the Light" (Ch.3, 31-34), is
    modeled as Type III of evaluate_cutting_the_light() rather than here,
    confirmed against Fig. 12's own numbers -- see that docstring.

    Note these are configurations, not verdicts about a matter: both
    authors are describing horary charts in which a querent and a quesited
    have already been identified. With no topical significators nominated,
    a row here says a blocking pattern exists between those three planets,
    not that any particular sought thing is obstructed."""
    rows = _pairwise_configurations(planetary_data)
    planets = [p for p in planetary_data.keys() if p != 'North Node']
    row_for = {frozenset({r['p1'], r['p2']}): r for r in rows}
    speed = {p: planetary_data[p]['speed_in_lon'] for p in planets}
    blocks = []

    def applying(a, b):
        """a is applying TO b, and the pair is actually connected under the
        active author's rule.

        Both halves were missing. Motion alone let a pair "block" while
        outside every activation window either author gives -- 12 or 15
        degrees for Abu Ma'shar, the actor's own light for Sahl -- and
        without checking direction, b applying to a counted as a applying
        to b."""
        r = row_for.get(frozenset({a, b}))
        if r is None or r['aspect_name'] == 'Aversion' or r['motion'] != 'Applying':
            return False
        if (r['applicant'] or r['light_name']) != a:
            return False
        return _is_connected(r)

    # --- Type I: Intervention (Sahl 35-37, Fig. 13; VII.5, 91-92) -------
    # "Three planets are in a single sign, IN DIFFERENT DEGREES, and the
    # heavy one has more degrees than [the other] two, so that the middle
    # one blocks the one with the fewest degrees from connecting with the
    # heavy one, UNTIL IT PASSES BY IT."
    by_sign = {}
    for p in planets:
        by_sign.setdefault(int(planetary_data[p]['longitude'] // 30), []).append(p)
    for group in by_sign.values():
        if len(group) < 3:
            continue
        for combo in combinations(group, 3):
            trio = sorted(combo, key=lambda p: planetary_data[p]['longitude'] % 30)
            blocked, blocker, target = trio
            if len({round(planetary_data[p]['longitude'] % 30, 6) for p in trio}) < 3:
                continue  # "in different degrees"
            if target != min(combo, key=WEIGHT_ORDER.index):
                continue  # the heavy one must be the one holding the most degrees
            # "Until it passes by it" only describes forward motion, and
            # VII.5, 10 frames the connection by assembly as belonging to
            # planets "direct in motion".
            if speed[blocked] <= 0 or speed[blocker] <= 0:
                continue
            # There is nothing to block unless the light planet is actually
            # on its way to the heavy one ...
            if not applying(blocked, target):
                continue
            # ... and nothing does the blocking unless the MIDDLE body is
            # itself joining the target. 35 has the middle planet standing
            # between the other two on its own way to the heavy one, so
            # that the light one cannot reach past it "UNTIL IT PASSES BY
            # IT". A middle planet already separating from the target is
            # moving out of the way, not standing in it -- this was
            # unchecked, so a separating body still emitted a block.
            if not applying(blocker, target):
                continue
            blocks.append({'Type': 'I (Intervention)', 'Blocked': blocked,
                            'Blocked By': blocker, 'From Reaching': target})

    # --- Type II: Nullification (Sahl 38-48, Fig. 14; VII.5, 93-94) -----
    # "Two planets are in a single sign, and the light one is connecting
    # with the heavy one, and another, [third] planet connects with that
    # heavy one BY LOOKING, but by degree it is less than the light one
    # which is uniting: thus the one with it in its sign blocks the one
    # looking." An earlier version treated ANY third planet co-present in
    # the heavy planet's sign as a blocker -- with no application, no
    # degree condition and no exception -- which is the bulk of the
    # over-reporting.
    #
    # VII.5, 94 supplies the exception: "when the degrees of the one
    # looking are closer to the connection than the degrees of the one
    # joining by body, the connection belongs to the one looking, because
    # it connects with it before the one joining with it" -- Sahl's own 40
    # says the same ("if it goes beyond that, its connection is valid").
    # The test is the REMAINING ARC IN DEGREES, not time-to-perfection,
    # even though 94 phrases the reason temporally. Sahl's worked Fig. 14
    # settles it: Moon 10 Scorpio, Mars 15 Taurus, Saturn 23 Taurus, and
    # his verdict is that Mars cuts the Moon's aspect. Mars has 8 degrees
    # left to Saturn against the Moon's 23, so by arc Mars wins -- but the
    # Moon covers her 23 degrees in under two days against Mars's fifteen,
    # so by elapsed time she would perfect first and the verdict would
    # invert. Arc it is.
    for row in rows:
        if row['aspect_name'] in ('Aversion', 'Conjunction') or row['motion'] != 'Applying':
            continue
        # The planet being blocked is the one APPLYING by ray, and the
        # target is what it applies to -- directed, not standing rank.
        looking, heavy = row['applicant'] or row['light_name'], row['receiver'] or row['heavy_name']
        remaining_ray = abs(row['deviation'])
        # The ray leg is NOT held to the live-connection test, and the
        # reason is the worked figure itself: Fig. 14 puts the Moon 13
        # degrees from her opposition to Saturn, one degree outside her
        # own 12-degree light (Ch.3, 13-17), and both authors still call
        # it a connection about to be cut. That is an inconsistency in the
        # source, not a bug, and it is honoured by exactly the one degree
        # it needs. An earlier version had no bound at all, so a ray 29
        # degrees from exact -- no connection by anyone's measure -- still
        # produced a nullification.
        if _is_connected(row):
            standing = 'live connection'
        elif remaining_ray <= _ray_activation_distance(row) + WORKED_FIGURE_TOLERANCE:
            standing = (f'worked-figure tolerance: ray {remaining_ray:.1f}\u00b0 from exact, within '
                        f'{WORKED_FIGURE_TOLERANCE:.0f}\u00b0 of the light (Fig. 14 sits 13\u00b0 against the Moon\'s 12\u00b0)')
        else:
            continue
        heavy_lon = planetary_data[heavy]['longitude']
        heavy_sign = int(heavy_lon // 30)
        for uniting in planets:
            if uniting in (looking, heavy):
                continue
            if int(planetary_data[uniting]['longitude'] // 30) != heavy_sign:
                continue
            if WEIGHT_ORDER.index(uniting) <= WEIGHT_ORDER.index(heavy):
                continue  # it is "the LIGHT one which is uniting" with the heavy one
            if not applying(uniting, heavy):
                continue  # co-presence alone is not a joining
            remaining_body = abs(heavy_lon - planetary_data[uniting]['longitude'])
            if remaining_body > remaining_ray:
                continue  # 40 / 94: the ray arrives first, so the connection is its own
            blocks.append({'Type': 'II (Nullification)', 'Blocked': looking,
                            'Blocked By': uniting, 'From Reaching': heavy, 'Standing': standing})
    return blocks

@_connection_aware
def evaluate_handing_over(planetary_data, sect):
    """Handing Over (Sahl, The Introduction Ch.3, 70-76): three grades of
    one phenomenon, not independent subtypes. Management (76, "in signs other
    than these two, she only hands over management"; fn. 90: "any application
    or connection hands over management") is the unconditional
    baseline for every Connected pair. Power (70-72) is additionally
    granted when the applying planet is itself in its own house,
    exaltation, or triplicity at the time of connecting. Nature (73-74,
    confirmed by the worked example's own parenthesis at 74 -- "that is, in
    reception") is additionally granted when the applying planet is
    connecting with the dispositor -- by house or exaltation only,
    matching Reception's own "perfect" scope (49-50), not triplicity -- of
    its own position. (Abu Ma'shar's own "Two Natures" subtype, Great
    Introduction VII.5, 97-100, doesn't appear anywhere in Sahl and isn't
    modeled here.)

    All three grades run in ONE direction only: "the one handing over" is
    always the light/applying planet, "the accepting one" is always the
    heavy planet it connects with (67) -- Power and Nature are graded
    elaborations of that same fixed direction, not independently
    reversible. An earlier version of this function also let the heavy
    (slow) side "hand power/nature back" to the applicant whenever ITS OWN
    position happened to qualify -- an addition with no basis in the
    text, caught during a source-fidelity review and removed."""
    with doctrine(SAHL):
        rows = _pairwise_configurations(planetary_data)
        triplicity_key = 'triplicity_day' if sect == 'Diurnal' else 'triplicity_night'
        results = []

        for r in rows:
            if (r['aspect_name'] == 'Aversion' and not _sahl_body_row(r)) or not _is_connected(r):
                continue
            # Handing over is directed: 67 makes the ONE HANDING OVER the planet
            # that connects, and 76 the one that accepts. This read natural rank
            # while the docstring described the applicant.
            fast, slow = r['applicant'] or r['light_name'], r['receiver'] or r['heavy_name']
            fast_lon = planetary_data[fast]['longitude']
            fast_rulers = get_essential_rulers(fast_lon)
            fast_power_claims = {fast_rulers['domicile'], fast_rulers['exaltation'], fast_rulers[triplicity_key]} - {'-'}
            fast_own_dispositors = {fast_rulers['domicile'], fast_rulers['exaltation']} - {'-'}

            results.append({'Type': 'Management', 'Planet': fast, 'Hands Over To': slow})

            # 70 names "house, exaltation, or triplicity"; 75-76 then restrict
            # the Moon: "if the Moon was in Taurus or Cancer, she hands over
            # power and management. And in signs other than these two, she
            # only hands over management." The narrower, later statement
            # governs her; the other six keep 70.
            moon_power_ok = (fast != 'Moon') or (fast in fast_own_dispositors)
            if fast in fast_power_claims and moon_power_ok:
                results.append({'Type': 'Power', 'Planet': fast, 'Hands Over To': slow})

            if slow in fast_own_dispositors:
                results.append({'Type': 'Nature', 'Planet': fast, 'Hands Over To': slow})

        return results

# --- Forward simulation -- Gr. Intr. VII.5's motion-dependent
# conditions (Emptiness of Course, Revoking, Resistance, Escape, Returning's
# retrograde case, Recompense) describe what happens as the chart moves
# forward, not the birth moment alone. This steps the ephemeris ahead and
# records each planet's sign-exit and station days, shared by every
# detector below rather than recomputed per-condition.

def _bisect_crossing(test_fn, day_lo, day_hi, tol=1e-4, max_iter=60):
    """Assumes exactly one transition of test_fn's boolean value inside
    [day_lo, day_hi]; returns the midpoint of the final bracket.

    tol is 1e-4 day (about nine seconds), the same order as the contact
    root-finder's, because an ingress found here is used as a HARD CUTOFF
    against contacts found there: at the old 0.02-day bracket (29 minutes)
    a sextile perfected three minutes before the Moon left her sign was
    placed after the recorded exit and lost (Astra F06; PN IV II.22, 1-4
    counts only what she connects with "so long as she is in her sign")."""
    val_lo = test_fn(day_lo)
    for _ in range(max_iter):
        if day_hi - day_lo < tol:
            break
        mid = (day_lo + day_hi) / 2.0
        if test_fn(mid) == val_lo:
            day_lo = mid
        else:
            day_hi = mid
    return (day_lo + day_hi) / 2.0

@st.cache_data(max_entries=32, show_spinner=False)
def _simulate_forward_pure(planet_names, jd, horizon_days, step_days):
    """The ephemeris half of _simulate_forward(), keyed only on values."""
    return _simulate_forward_uncached({p: None for p in planet_names}, jd,
                                       horizon_days, step_days)

FAVOR_SEARCH_DAYS = 200      # the forward simulation's horizon, named on the page where it finds nothing

def _simulate_forward(planetary_data, jd, horizon_days=FAVOR_SEARCH_DAYS, step_days=1.0):
    """Forward simulation, with the ephemeris work cached.

    _lon_at() memoises into the dict this returns, so the cached value is
    copied and given a fresh memo rather than handed out directly."""
    names = tuple(p for p in planetary_data if p != 'North Node')
    base = _simulate_forward_pure(names, jd, horizon_days, step_days)
    return {'series': base['series'], 'events': base['events'],
            'jd': base['jd'], 'horizon_days': base['horizon_days']}

def _simulate_forward_uncached(planetary_data, jd, horizon_days=FAVOR_SEARCH_DAYS, step_days=1.0):
    """Day-step swe.calc_ut for all 7 classical planets from jd out to
    horizon_days, returning per-planet daily (day, longitude, speed) series
    plus interpolated sign-exit and station event days. A near-term cutoff,
    not an indefinite search -- a condition not found within the horizon is
    reported as "none found," not chased further."""
    planets = [p for p in planetary_data.keys() if p != 'North Node']
    n_steps = int(horizon_days / step_days) + 1
    series = {p: {'day': [], 'lon': [], 'speed': []} for p in planets}
    for i in range(n_steps):
        day = i * step_days
        for p in planets:
            res, _ = swe.calc_ut(jd + day, PLANET_SWE_IDS[p])
            series[p]['day'].append(day)
            series[p]['lon'].append(res[0])
            series[p]['speed'].append(res[3])

    events = {p: {'sign_exits': [], 'stations': []} for p in planets}
    for p in planets:
        days, lons, speeds = series[p]['day'], series[p]['lon'], series[p]['speed']
        pid = PLANET_SWE_IDS[p]
        for i in range(len(days) - 1):
            # A station first: the longitude is monotonic on each side of
            # it and NOT across the whole step, so a planet can leave a sign
            # and come back inside one sample (Mercury stationing just past
            # 0 of the next sign) and show the same sign at both ends. The
            # step is split at the station and each monotonic piece searched
            # for its own crossing (Astra F07; Gr. Intr. VII.5, 117-119 and
            # Aphorism 48, 99-102 order stations and sign changes).
            pieces = [(days[i], lons[i]), (days[i + 1], lons[i + 1])]
            if (speeds[i] > 0) != (speeds[i + 1] > 0):
                kind = 'first' if speeds[i] > 0 else 'second'
                def _speed_test(day, _pid=pid, _positive=(speeds[i] > 0)):
                    res, _ = swe.calc_ut(jd + day, _pid)
                    return (res[3] > 0) == _positive
                station_day = _bisect_crossing(_speed_test, days[i], days[i + 1])
                events[p]['stations'].append((station_day, kind))
                station_lon = swe.calc_ut(jd + station_day, pid)[0][0]
                pieces.insert(1, (station_day, station_lon))
            for (d0, l0), (d1, l1) in zip(pieces, pieces[1:]):
                sign0 = int(l0 // 30)
                if int(l1 // 30) != sign0:
                    def _sign_test(day, _pid=pid, _sign0=sign0):
                        res, _ = swe.calc_ut(jd + day, _pid)
                        return int(res[0] // 30) == _sign0
                    events[p]['sign_exits'].append(_bisect_crossing(_sign_test, d0, d1))

    return {'series': series, 'events': events, 'jd': jd, 'horizon_days': horizon_days}

def _wrap180(degrees):
    """Fold an angle into (-180, 180]."""
    return ((degrees + 180.0) % 360.0) - 180.0

def _lon_at(sim, planet, day):
    """A planet's longitude at an arbitrary day inside the horizon, read
    straight from the ephemeris (memoized per simulation).

    Replaces an interpolation over the daily samples, which ran the
    straight line THROUGH the 0/360 seam: a planet at 359 deg followed by
    one at 1 deg interpolated backwards across 180 deg, so any event timed
    between those two samples was placed half a zodiac away."""
    cache = sim.setdefault('_lon_cache', {})
    key = (planet, round(day, 6))
    if key not in cache:
        res, _ = swe.calc_ut(sim['jd'] + day, PLANET_SWE_IDS[planet])
        cache[key] = res[0]
    return cache[key]

def _bisect_zero(fn, day_lo, day_hi, tol=1e-3, max_iter=60):
    """Bracketed root-find for a continuous fn known to change sign across
    [day_lo, day_hi]."""
    f_lo = fn(day_lo)
    for _ in range(max_iter):
        if day_hi - day_lo < tol:
            break
        mid = (day_lo + day_hi) / 2.0
        f_mid = fn(mid)
        if (f_mid < 0.0) == (f_lo < 0.0):
            day_lo, f_lo = mid, f_mid
        else:
            day_hi = mid
    return (day_lo + day_hi) / 2.0

def _configuration_target_at(sim, p1, p2, day):
    """The whole-sign aspect angle the pair is entitled to on a given day,
    or None if they are in aversion then."""
    s1 = int(_lon_at(sim, p1, day) // 30)
    s2 = int(_lon_at(sim, p2, day) // 30)
    raw_apart = abs(s1 - s2)
    apart = min(raw_apart, 12 - raw_apart)
    entry = ASPECT_BY_SIGN_COUNT.get(apart)
    return None if entry is None else entry[1]

def _sign_ingress_day(sim, planet, sign_idx, after_day=0.0, before_day=None):
    """Day the planet ENTERS the given sign, or None inside the horizon.

    sim['events'][p]['sign_exits'] records the moments a planet leaves the
    sign it was in; the sign it lands in is read just past the crossing."""
    for day in sim['events'][planet]['sign_exits']:
        if day <= after_day:
            continue
        if before_day is not None and day > before_day:
            break
        if int(_lon_at(sim, planet, day + 1e-3) // 30) == sign_idx % 12:
            return day
    return None

def _body_union_day(sim, a, b, after_day=0.0, before_day=None):
    """Day two planets conjoin BY DEGREE -- the "uniting" the sources treat
    as a distinct, stronger event than a ray perfecting (Sahl Ch.3, 44;
    VII.5, 121 and its note, which reads the verb as "conjoins" by degree
    "rather than the looser 'assembling'")."""
    return _perfection_day(sim, a, b, 0.0, after_day=after_day, before_day=before_day)

def _perfection_day(sim, p1, p2, target, before_day=None, after_day=None):
    """First day inside the horizon on which p1 and p2 actually perfect the
    given aspect, or None.

    Three things the previous implementation got wrong:

    (1) It tested `min_angular_distance - target`, which is confined to
    [0, 180]. For a conjunction (target 0) that residual is never negative
    and for an opposition (target 180) never positive, so NEITHER could
    ever cross zero -- perfection was silently undetectable for both, and
    callers that treat "no perfection found" as a positive result (notably
    Revoking) fired on every such pair. Here the pair's signed relative
    longitude is compared against +target and -target, each folded into
    (-180, 180], so every aspect including those two crosses zero cleanly.

    (2) It returned the sample day at which the sign flipped rather than
    the root, giving up to a full day of error; now bisected against the
    ephemeris directly.

    (3) It kept applying the natal aspect angle no matter how the chart had
    moved. A pair 3 signs apart at birth may be 4 apart by the time the
    90-degree hit arrives, and Abu Ma'shar permits no out-of-sign aspect
    (VII.5, 14: few degrees across a sign boundary is not a connection but
    a weak "mixing of natures"). The whole-sign configuration is now
    revalidated AT the moment of perfection, and a hit that no longer
    matches is discarded and the scan continues."""
    days = sim['series'][p1]['day']
    lons1, lons2 = sim['series'][p1]['lon'], sim['series'][p2]['lon']

    def residual(day, offset):
        return _wrap180(_lon_at(sim, p1, day) - _lon_at(sim, p2, day) - offset)

    for i in range(len(days) - 1):
        if before_day is not None and days[i] > before_day:
            break
        if after_day is not None and days[i + 1] < after_day:
            continue
        for offset in (target, -target):
            prev = _wrap180(lons1[i] - lons2[i] - offset)
            cur = _wrap180(lons1[i + 1] - lons2[i + 1] - offset)
            # A wrapped residual also "changes sign" when it rolls over the
            # +/-180 seam; a genuine root moves by far less than half a turn
            # in one step, so that guard rejects the seam artifact.
            if (prev < 0.0) == (cur < 0.0) or abs(cur - prev) >= 180.0:
                continue
            day = _bisect_zero(lambda d, _o=offset: residual(d, _o), days[i], days[i + 1])
            if before_day is not None and day > before_day:
                continue
            if after_day is not None and day < after_day:
                continue
            actual = _configuration_target_at(sim, p1, p2, day)
            if actual is not None and abs(actual - target) < 1e-9:
                return day
    return None

@_connection_aware
def evaluate_returning(planetary_data, accidental, ascendant_lon):
    """Returning (Sahl, The Introduction Ch.3, 65-69, Figs. 21-22): two
    distinct manners, not grades of one trigger, both present-tense --
    Sahl gives no forward-looking refinement for either.

    Manner I (65): a planet connects with a retrograde planet, or one
    under the rays -- either condition alone triggers it. "It returns to
    it what it accepted from it, and has already corrupted its
    management, and indicates that the question does not have a beginning
    nor end."

    Manner II (66-69): the light (handing-over, faster) planet is in a
    stake (angular house), connecting with a heavy (accepting) planet
    that is falling (cadent) -- "the sought thing has a beginning...
    but does not have an end because the accepting one is falling." NOTE:
    Sahl's own worked example (68: Moon in the 6th, Mars in the 12th --
    both cadent) doesn't actually satisfy this rule as stated (neither
    planet is angular); Dykes' own footnote there flags the same
    inconsistency in the source text, not a misreading here. Implemented
    per the rule as stated in 66-67, not the example."""
    with doctrine(SAHL):
        rows = _pairwise_configurations(planetary_data)
        results = []
        for r in rows:
            if (r['aspect_name'] == 'Aversion' and not _sahl_body_row(r)) or not _is_connected(r) or r['motion'] != 'Applying':
                continue
            # Returning is directed: 65 has "a planet ... CONNECTING WITH a
            # retrograde planet or one under the rays," and 67 names the mover
            # as the one handing over. So the subject is the applicant and the
            # planet that returns the management is the one being approached --
            # which for a retrograde connection is not the naturally heavier of
            # the two. (Manner I would otherwise report the retrograde planet
            # as returning management to itself whenever it was the applicant.)
            fast, slow = r['applicant'] or r['light_name'], r['receiver'] or r['heavy_name']
            acc_slow = accidental[slow]

            if acc_slow['Retrograde'] or sahl_under_rays(slow, planetary_data[slow]['longitude'], planetary_data['Sun']['longitude'], planetary_data[slow].get('speed_in_lon')):
                results.append({'Manner': 'I (65)', 'Planet': fast, 'Returned By': slow})

            fast_house = get_wsh_house(planetary_data[fast]['longitude'], ascendant_lon)
            slow_house = get_wsh_house(planetary_data[slow]['longitude'], ascendant_lon)
            # 66's "falling away from the Ascendant" is aversion, not cadency:
            # Dykes' note there reads "That is, in aversion to it," and the Course
            # Glossary makes the same equivalence under Cadent.
            slow_averse = _averse_to_ascendant(planetary_data[slow]['longitude'], ascendant_lon)
            if fast_house in ANGLE_HOUSES and slow_averse:
                results.append({'Manner': 'II (66-69)', 'Planet': fast, 'Returned By': slow})

        return results

def evaluate_revoking(planetary_data, sim):
    """Revoking (Gr. Intr. VII.5, 117, Fig. 137): "a planet is
    CONNECTING with a planet, but BEFORE IT REACHES IT, it retrogrades away
    from it, and its connection is nullified."

    NOT Sahl Ch.3, 117, which an earlier version of this docstring also
    cited: that paragraph is "know that Saturn, in nativities of the day
    ... decreases harm." The two works' paragraph numbers collide.

    "Before it reaches it" fixes the window: the only question is whether
    the aspect perfects between now and the applicant's first station. If
    it does, nothing was revoked; if it does not, the station is what
    stopped it. Searching the whole horizon instead, and accepting "no
    perfection found anywhere" as a positive, let a station on day 190
    revoke a connection that was never going to complete inside 200 days
    regardless -- a finding with no causal content."""
    with doctrine(ABU_MASHAR):
        if sim is None:
            return []
        rows = _pairwise_configurations(planetary_data)
        results = []
        for r in rows:
            if r['aspect_name'] == 'Aversion' or r['motion'] != 'Applying':
                continue
            # 117 revokes the connection of "a planet ... connecting with a
            # planet" when IT retrogrades away before arriving, so the station
            # that matters is the applicant's, not the naturally lighter one's.
            fast, slow = r['applicant'] or r['light_name'], r['receiver'] or r['heavy_name']
            first_station = next((s for s in sim['events'][fast]['stations'] if s[1] == 'first'), None)
            if not first_station:
                continue
            # "BEFORE IT REACHES IT, it retrogrades away from it." The only
            # window that matters is up to the station: if the aspect perfects
            # in it, nothing was revoked, and if it does not, the station is
            # what stopped it. An earlier version searched the whole 200-day
            # horizon and also accepted "no perfection found anywhere" as proof
            # -- so a station on day 190 revoked a connection that was never
            # going to complete inside the horizon in the first place, with the
            # station doing none of the work.
            station_day = first_station[0]
            if _perfection_day(sim, fast, slow, r['target'], before_day=station_day) is not None:
                continue
            results.append({
                'Planet': fast, 'Was Connecting To': slow,
                'Stations Retrograde In (days)': round(station_day, 1),
            })
        return results

def evaluate_resistance(planetary_data, sim):
    """Resistance (VII.5, 118, Fig. 138). The chapter prescribes an ordered
    sequence of events, and each step is now required rather than inferred:

    "RESISTANCE is if there was A LIGHT PLANET IN MANY DEGREES, and another
    planet HEAVIER THAN IT IN FEWER DEGREES, and A THIRD PLANET LIGHTER
    THAN THAT LIGHT ONE wanting a connection with the heavy one, so that
    the light one in more degrees GOES RETROGRADE and CONNECTS WITH THE
    HEAVY ONE THROUGH ITS RETROGRADATION -- and then GOES PAST IT and there
    is A CONNECTION OF THAT THIRD ONE with this retrograde one ... not with
    the heavy one."

    Dykes' note: "In Figure 138, Mercury wants to connect with Mars. But
    Venus, who is in a later degree, suddenly goes retrograde, passes by
    Mars, and connects with Mercury, resisting and obstructing his attempt
    to connect with Mars." So L = Venus, H = Mars, T = Mercury.

    Required, in order: T applying to H; L ahead of H by degree and lighter
    than it; L's first station; L's union with H after that station; T's
    connection with L after that; and T not reaching H first. An earlier
    version proved only the station and that some later connection existed,
    never that L actually reached H by retrogradation or that it did so
    before T arrived."""
    with doctrine(ABU_MASHAR):
        if sim is None:
            return []
        rows = _pairwise_configurations(planetary_data)
        row_for = {frozenset({r['p1'], r['p2']}): r for r in rows}
        planets = [p for p in planetary_data.keys() if p != 'North Node']
        results = []

        for heavy in planets:
            h_lon = planetary_data[heavy]['longitude']
            for light in planets:
                if light == heavy or WEIGHT_ORDER.index(light) <= WEIGHT_ORDER.index(heavy):
                    continue                       # L must be LIGHTER than H
                # "a light planet in many degrees, and another planet heavier
                # than it in fewer degrees" -- L ahead of H in the zodiac.
                if not 0.0 < (planetary_data[light]['longitude'] - h_lon) % 360.0 < 180.0:
                    continue
                station = next((s for s, k in sim['events'][light]['stations'] if k == 'first'), None)
                if station is None:
                    continue
                # "connects with the heavy one THROUGH ITS RETROGRADATION"
                union_lh = _body_union_day(sim, light, heavy, after_day=station)
                if union_lh is None:
                    continue
                for third in planets:
                    if third in (light, heavy):
                        continue
                    if WEIGHT_ORDER.index(third) <= WEIGHT_ORDER.index(light):
                        continue                   # T must be LIGHTER than L
                    r_th = row_for.get(frozenset({third, heavy}))
                    if r_th is None or r_th['aspect_name'] == 'Aversion' or r_th['motion'] != 'Applying':
                        continue                   # T must actually want H
                    # "and then goes past it and there is a connection of that
                    # third one with this retrograde one" -- after the union.
                    r_tl = row_for.get(frozenset({third, light}))
                    if r_tl is None or r_tl['aspect_name'] == 'Aversion':
                        continue
                    meet_tl = _perfection_day(sim, third, light, r_tl['target'], after_day=union_lh)
                    if meet_tl is None:
                        continue
                    # T must not have reached H first; otherwise nothing was
                    # obstructed.
                    reach_th = _perfection_day(sim, third, heavy, r_th['target'], before_day=meet_tl)
                    if reach_th is not None:
                        continue
                    results.append({
                        'Light Planet': light,
                        'Originally Heading To': heavy,
                        'Resisted, Now Connects With': third,
                        'Station (days)': round(station, 1),
                        'Reaches It Retrograde (days)': round(union_lh, 1),
                        'Third Planet Meets It (days)': round(meet_tl, 1),
                    })
        return results

def evaluate_escape(planetary_data, sim):
    """Escape (VII.5, 119, Fig. 139):

    "ESCAPE is if a planet is going towards the connection of a planet, but
    BEFORE IT REACHES IT, the one it is connecting with SHIFTS OVER TO THE
    NEXT SIGN, and WHEN THE ONE HANDING OVER CHANGES [to that next sign]
    there is one of the planets closer to it than [the first one], so its
    connection is with the other planet, and its connection with the first
    one is nullified."

    The second half is the part that was missing. Dykes' note spells out
    the picture: "Venus in Virgo had wanted to connect with Mercury, who
    was at the end of the sign. But before she could complete the
    connection, Mercury passed into Libra (and went past Saturn). BY THE
    TIME VENUS PASSES INTO LIBRA, SHE ENCOUNTERS THE BODY OF SATURN and
    connects with him, letting Mercury escape."

    So the applicant must itself follow the escapee into the new sign, and
    the capture is a body it meets there -- not merely whatever it happens
    to perfect with next anywhere in the chart. Both the ingress and the
    union are now required events with times, so a row asserts a capture
    only when one actually occurs.

    This makes Escape rare, which it should be: it needs two planets to
    cross the same sign boundary in sequence inside the horizon."""
    with doctrine(ABU_MASHAR):
        if sim is None:
            return []
        rows = _pairwise_configurations(planetary_data)
        results = []
        for r in rows:
            if r['aspect_name'] == 'Aversion' or r['motion'] != 'Applying':
                continue
            fast, slow = r['applicant'] or r['light_name'], r['receiver'] or r['heavy_name']
            sign_exits = sim['events'][slow]['sign_exits']
            if not sign_exits:
                continue
            exit_day = sign_exits[0]
            # "before it reaches it, the one it is connecting with shifts over"
            if _perfection_day(sim, fast, slow, r['target'], before_day=exit_day) is not None:
                continue
            new_sign = int(_lon_at(sim, slow, exit_day + 1e-3) // 30)
            # "when the one handing over changes [to THAT NEXT SIGN]" -- the
            # applicant follows it across the same boundary, which is what
            # Fig. 139 shows: Venus and Mercury are both in Virgo, Mercury
            # crosses into Libra, and Venus's own next crossing is into Libra
            # behind him. So it must be the applicant's NEXT sign change, not
            # any later arrival in that sign -- otherwise the Moon qualifies
            # against everything, since she re-enters every sign each month.
            next_exit = next((d for d in sim['events'][fast]['sign_exits'] if d > exit_day), None)
            if next_exit is None or int(_lon_at(sim, fast, next_exit + 1e-3) // 30) != new_sign:
                continue
            ingress_day = next_exit
            # "there is one of the planets closer to it" -- the body it meets
            # in that sign once it arrives.
            best, best_day = None, None
            for other in planetary_data:
                if other in (fast, slow, 'North Node'):
                    continue
                day = _body_union_day(sim, fast, other, after_day=ingress_day)
                if day is None:
                    continue
                if int(_lon_at(sim, other, day) // 30) != new_sign:
                    continue
                if best_day is None or day < best_day:
                    best, best_day = other, day
            if best is None:
                continue
            # "there is one of the planets CLOSER to it than [the first one],
            # so its connection is with the other planet, and its connection
            # with the first one is nullified": the capture must come BEFORE
            # the original connection would have perfected in the new sign.
            # If the applicant reaches the escapee first, nothing is
            # nullified and there is no Escape (Astra F05; Sahl, Introduction
            # 3, 57 fn 77 says the same from the other side: crossing over
            # and connecting with B "before there is a connection with
            # another planet" completes the ORIGINAL situation).
            original = _perfection_day(sim, fast, slow, r['target'], after_day=ingress_day)
            if original is not None and original <= best_day:
                continue
            results.append({
                'Planet': fast, 'Escaped': slow, 'Connected Instead With': best,
                'Escapee Leaves Sign (days)': round(exit_day, 1),
                'Applicant Follows In (days)': round(ingress_day, 1),
                'Meets Its Body (days)': round(best_day, 1),
            })
        return results

# Sahl's precedence among the three kinds of contact a planet can hold,
# stated at Ch.3, 44 and enumerated in Dykes' note there: "(1) 'uniting' or
# a conjunction by degree, (2) a connection by degree from another sign,
# (3) an aspect by sign. In other words, while degree-based connections can
# cut each other as in type #1, aspects by sign do not cut each other."
#
# The rule itself, at 44: "a connection does not nullify a uniting, but a
# uniting does NULLIFY a connection, while an aspect does not cut an aspect
# [but] hands over the sought thing, and a uniting cuts an aspect."
_CONTACT_NAMES = {0: 'a uniting', 1: 'a connection by degree', 2: 'an aspect by sign'}

def _contact_rank(row):
    if row['assembly'] and _is_connected(row):
        return 0
    if _is_connected(row):
        return 1
    return 2

@_connection_aware
def evaluate_cutting_the_light(planetary_data, sim):
    """Cutting the Light. Type III is Sahl's own Blocking #1, "Cutting the
    Light" (The Introduction Ch.3, 31-34, Fig. 12) -- confirmed against
    Fig. 12's own numbers (Ascendant Virgo, Mercury 10 Cancer applying by
    trine to Jupiter 15 Pisces, but Mars 13 Aries sits square to Mercury at
    only ~3 degrees from exact): Mercury completes the nearer connection to
    Mars before it ever reaches Jupiter, cutting off the original one --
    exactly this mechanism (among several planets a given one is applying
    to, it connects with whichever is nearest by degree first, cutting off
    the more distant, originally-favored connection). Abu Ma'shar reuses it
    as his own Type III when he moves "Cutting the Light" out of Sahl's
    three-way Blocking and into its own later, expanded category (Great
    Introduction VII.5, 120-125, Fig. 142). Types I-II (121-124, Figs.
    140-141, forward-sim) are Abu Ma'shar's own further elaboration, not
    found in Sahl: an intervening planet stations retrograde and enters the
    light planet's own sign before the light planet reaches its original
    heavy target.

    Contacts are ordered by Sahl's own PRECEDENCE (44 and its note, see
    _contact_rank()), not by nearness alone; nearness only breaks ties
    within a rank. But precedence decides which contact PREVAILS, and that
    is not the same question as which one is CUT.

    Two outcomes are therefore reported, not one:

    NULLIFICATION (44-48, Fig. 15). A single planet holds both a union and
    a connection. Sahl: "the Moon is in 10 degrees of Taurus, and Mars in
    20 degrees of Taurus, and the Moon is connecting with Venus (and Venus
    is in 15 degrees of Cancer). So her connection with Venus is PRIOR to
    her uniting with Mars, BUT the Moon is uniting [with Mars], and that is
    stronger than an aspect and a connection" (46-47). The note on 47:
    "EVEN THOUGH the connection by aspect may perfect first, the planet it
    is connecting to by body will still be the DOMINANT one." The note on
    48 then rules this out of the cutting category by name: "note that this
    is NOT A CASE OF 'CUTTING' ... and it is not exactly a case of blocking
    by nullification, because Venus is not one of the significators we want
    to join." An earlier version emitted it as "Moon cut off from Venus"
    and cited Fig. 15 in this docstring as confirmation of the sort order.
    The sort order was right; the verdict drawn from it was not.

    CUTTING (Sahl 31-34, Fig. 12; VII.5, 125, Fig. 142). A nearer
    degree-connection intercepts a more distant one. 32 defines it purely
    by order of arrival: "a planet between the lord of the Ascendant and
    the lord of the sought thing, IN FEWER DEGREES than one of them, so the
    connection with it is BEFORE the connection ... with the lord of the
    sought thing." Abu Ma'shar's Fig. 142 is the case the previous version
    could not detect: Mars 12 Aquarius wants Jupiter 29 Taurus by square,
    17 degrees off, and Saturn at 15 Taurus is 3 degrees off -- "he
    connects with Saturn first, WHO CUTS OFF HIS LIGHT from reaching
    Jupiter" (note on 125). A rank-2 exclusion, reasoned from 44's "an
    aspect does not cut an aspect", was discarding exactly this: 44
    withholds cutting from an ASPECT, and the note on 44 grants it to
    degree-based connections, which "can cut each other as in type #1".
    Only a bare sign-aspect cuts nothing.

    Note that 42-48 has been misread twice in this project. 42-43 (Fig. 14)
    is third-party blocking, handled in evaluate_blocking(); 45-48 (Fig.
    15) is the one-planet nullification above. They are consecutive."""
    rows = _pairwise_configurations(planetary_data)
    results = []

    by_fast = {}
    for r in rows:
        if r['aspect_name'] != 'Aversion':
            by_fast.setdefault(r['applicant'] or r['light_name'], []).append(r)
    for fast, candidates in by_fast.items():
        applying = sorted((r for r in candidates if r['motion'] == 'Applying'),
                           key=lambda r: (_contact_rank(r), abs(r['deviation'])))
        if len(applying) < 2:
            continue
        winner = applying[0]
        win_rank = _contact_rank(winner)
        if win_rank == 2:
            continue   # "an aspect does not cut an aspect" (44)
        for r in applying[1:]:
            lose_rank = _contact_rank(r)
            because = (f'{_CONTACT_NAMES[win_rank]} outranks {_CONTACT_NAMES[lose_rank]}'
                        if win_rank != lose_rank
                        else f'nearer by {abs(r["deviation"]) - abs(winner["deviation"]):.1f} deg')
            # A UNITING does not CUT: it DOMINATES. Dykes' note on 47 --
            # "EVEN THOUGH the connection by aspect may perfect first, the
            # planet it is connecting to by body will still be the DOMINANT
            # one" -- and his note on 48 rules the case out of this category
            # by name: "note that this is NOT A CASE OF 'CUTTING' which only
            # involves a connection from different signs; it is not a case
            # of 'intervention' ... and it is not exactly a case of blocking
            # by nullification, because Venus is not one of the
            # significators we want to join."
            #
            # An earlier version reported Fig. 15 as "Moon cut off from
            # Venus", and cited that figure in its own docstring as
            # CONFIRMATION of the sort order. The sort order is right; the
            # verdict drawn from it was not. 44's precedence says which
            # contact prevails, not which is severed.
            if win_rank == 0:
                results.append({
                    'Type': 'Nullification (44-48)',
                    'Planet': fast,
                    'Yields To': winner['receiver'] or winner['heavy_name'],
                    'Other Contact': r['receiver'] or r['heavy_name'],
                    'Because': because + '; the other perfects first but is not cut off',
                })
            else:
                results.append({
                    'Type': 'III',
                    'Planet': fast,
                    'Yields To': winner['receiver'] or winner['heavy_name'],
                    'Other Contact': r['receiver'] or r['heavy_name'],
                    'Because': because,
                })

    if sim is not None:
        for r in rows:
            if r['aspect_name'] == 'Aversion' or r['motion'] != 'Applying':
                continue
            light, heavy = r['applicant'] or r['light_name'], r['receiver'] or r['heavy_name']
            exact_day = _perfection_day(sim, light, heavy, r['target'])
            if exact_day is None:
                continue
            light_sign = int(planetary_data[light]['longitude'] // 30)

            # --- Type I (121-122, Fig. 140) ---------------------------
            # "a planet wants a connection with a planet heavier than
            # itself, and IN THE SECOND SIGN FROM THE LIGHT ONE is a
            # planet, but before the light one reaches the connection with
            # the heavy one, the planet which is in the second [sign] from
            # it GOES RETROGRADE AND ENTERS ITS SIGN, AND CONJOINS with
            # it." The note on 121 reads that last verb as conjoining by
            # degree, "rather than the looser 'assembling'".
            #
            # Two of those four clauses were unchecked: the intervener had
            # to start in the second sign, and it had to actually reach the
            # light planet's body. An earlier version accepted any planet
            # that stationed and crossed into the light planet's sign,
            # whatever it did once there and wherever it started.
            for candidate in planetary_data:
                if candidate in (light, heavy, 'North Node'):
                    continue
                if int(planetary_data[candidate]['longitude'] // 30) != (light_sign + 1) % 12:
                    continue
                station = next((s for s, k in sim['events'][candidate]['stations']
                                 if k == 'first' and s < exact_day), None)
                if station is None:
                    continue
                ingress = _sign_ingress_day(sim, candidate, light_sign,
                                             after_day=station, before_day=exact_day)
                if ingress is None:
                    continue
                union = _body_union_day(sim, candidate, light,
                                         after_day=ingress, before_day=exact_day)
                if union is None:
                    continue
                results.append({
                    'Type': 'I', 'Planet': light, 'Yields To': candidate, 'Other Contact': heavy,
                    'Because': f'stations day {station:.0f}, enters the sign day {ingress:.0f}, '
                                f'conjoins day {union:.0f}, before perfection on day {exact_day:.0f}',
                })

            # --- Type II (123-124, Fig. 141) --------------------------
            # "a light planet is connecting with a planet heavier than
            # itself, and THAT PLANET HANDS OVER TO A HEAVY PLANET, but
            # before the light one reaches the degree of the planet which
            # is heavier than itself, that planet CONNECTS WITH THE HEAVY
            # PLANET AND GOES PAST IT, so there is a connection of the
            # light one WITH THE HEAVY ONE, while it nullifies its
            # connection with the first one." The note: "Mercury wants to
            # connect with Venus. But before he can do that, she connects
            # with Mars and then continues on. Then Mercury is left with
            # the conjunction of Mars, which was not what he wanted."
            # Not implemented at all before this.
            for onward in planetary_data:
                if onward in (light, heavy, 'North Node'):
                    continue
                r_mid = next((x for x in rows if {x['p1'], x['p2']} == {heavy, onward}), None)
                if r_mid is None or r_mid['aspect_name'] == 'Aversion' or r_mid['motion'] != 'Applying':
                    continue
                if (r_mid['applicant'] or r_mid['light_name']) != heavy:
                    continue          # the middle planet must be the one handing on
                mid_day = _perfection_day(sim, heavy, onward, r_mid['target'], before_day=exact_day)
                if mid_day is None:
                    continue          # it must get there FIRST
                r_far = next((x for x in rows if {x['p1'], x['p2']} == {light, onward}), None)
                if r_far is None or r_far['aspect_name'] == 'Aversion':
                    continue
                far_day = _perfection_day(sim, light, onward, r_far['target'], after_day=mid_day)
                if far_day is None or far_day >= exact_day:
                    continue          # ... and the light one must land on it
                                      # BEFORE reaching the one it wanted,
                                      # which is what "nullifies its
                                      # connection with the first one" means
                results.append({
                    'Type': 'II', 'Planet': light, 'Yields To': onward, 'Other Contact': heavy,
                    'Because': f'{heavy} reaches {onward} on day {mid_day:.0f} and moves on; '
                                f'{light} lands on {onward} instead on day {far_day:.0f}',
                })
    return results

def _find_recompense_day(sim, helper, helped):
    """Recompense (VII.5, 127): "[the first planet] will not cease to have
    favor for it until the planet which had bestowed the favor on it FALLS
    INTO ITS OWN WELL OR FALL, and the other [planet] CONNECTS WITH IT (or
    it connects with the other), and PULLS IT OUT of its well or fall."

    Two events, both required: the helper arriving in its own well or fall,
    and a connection actually perfecting there. An earlier version scanned
    the daily samples and accepted any pair whose separation sat within 5
    degrees of an aspect angle on one of those days -- a proximity this
    project invented, on a one-day grid, standing in for a perfection that
    was never computed. Now the aspect is perfected against the ephemeris
    and the helper must still be in the well or fall when it lands.

    Returns the day, or None. Not found inside the horizon is omitted from
    the table, not reported as a negative finding."""
    helper_fall_signs = FALLS.get(helper, [])

    def in_well_or_fall(lon):
        sign = get_zodiac_sign(lon)
        return (sign in helper_fall_signs
                or (int(lon % 30) + 1) in WELLED_DEGREES.get(sign, []))

    days = sim['series'][helper]['day']
    lons_helper = sim['series'][helper]['lon']
    for day, lh in zip(days, lons_helper):
        if not in_well_or_fall(lh):
            continue
        target = _configuration_target_at(sim, helper, helped, day)
        if target is None:
            continue
        perf = _perfection_day(sim, helped, helper, target, after_day=day)
        if perf is None:
            continue
        if in_well_or_fall(_lon_at(sim, helper, perf)):
            return perf
    return None

@_connection_aware
def evaluate_favor_and_recompense(planetary_data, essential, sect, sim):
    """Favor and Recompense (VII.5, 126-128, Fig. 143): a planet in its own
    Fall or a Well, pulled out by a connecting dispositor (Favor);
    Recompense is the reciprocal payback later, found via the forward
    simulation when available."""
    with doctrine(ABU_MASHAR):
        rows = _pairwise_configurations(planetary_data)
        results = []
        for r in rows:
            if r['aspect_name'] == 'Aversion' or not _is_connected(r):
                continue
            for helper, helped in ((r['p1'], r['p2']), (r['p2'], r['p1'])):
                lon = planetary_data[helped]['longitude']
                sign = get_zodiac_sign(lon)
                degree_1_based = int(lon % 30) + 1
                in_well = degree_1_based in WELLED_DEGREES.get(sign, [])
                in_fall = essential[helped]['Fall']
                if not (in_well or in_fall):
                    continue
                if helper not in _dispositors(lon, sect):
                    continue
                entry = {'Planet': helped, 'Condition': 'Fall' if in_fall else 'Well', 'Favored By': helper}
                # The column stands whether or not the search found the
                # payback, and an empty search names its horizon (the
                # hostile pass of 2026-09-22, L3): a finite search is not
                # an absence.
                if sim is not None:
                    recompense_day = _find_recompense_day(sim, helper, helped)
                    horizon = int(sim.get('horizon_days', FAVOR_SEARCH_DAYS))
                    entry['Recompense (days)'] = (round(recompense_day, 1) if recompense_day is not None
                                                  else f'none within {horizon} days (the search ends there)')
                else:
                    entry['Recompense (days)'] = 'not searched'
                results.append(entry)
        return results

@_connection_aware
def evaluate_reception(planetary_data, sect, sim=None):
    """Reception, under whichever author profile is active. The two differ
    in scope, in direction, and in what counts as strong, so they are
    written out separately rather than blended.

    SAHL (The Introduction Ch.3, 49-55). One direction only: the connecting
    planet sits in a dignity belonging to the planet it connects with, and
    so is received by it (52: "if the Moon is in Aries and she is
    connecting with Mars, then he receives her because Aries is his
    house"). House or exaltation is "perfect reception, with truthful
    intention" (49); triplicity alone is expressly ranked "below this
    reception" (50); bound counts only together with triplicity, which
    Sahl attributes to Masha'allah (54-55, and Abu Ma'shar's own note on
    132 confirms the attribution). Face never appears. A connection is
    required throughout.

    Under Sahl's profile a non-reception of Kind II or III for the same
    pair suppresses the reception row (refusal wins), while Kind IV and V
    mark it brought down for the distinct reasons stated in 62; see the
    note at the end of the function and process/tae_docs/synthesis/13_open_decisions.md
    D-2 and D-22.

    Sahl gives two further forms after that, both previously unimplemented
    and both now here (they are his own, so they run under his profile
    only):

    56, RECEPTION AT ONE REMOVE. "If the Moon was connecting with a planet
    and that planet was connecting with the lord of the house of the Moon
    or its exaltation, then the Moon is received." The note glosses the
    second lord as "the exalted lord of the sign in which the Moon is," and
    calls the whole thing "like a transfer of light which indirectly allows
    for reception." Both legs are taken in Sahl's own directed sense of
    "connecting with" -- 6's "going straightaway to ... going towards," not
    mere proximity, since separating is his separate term at 22. On the
    loose reading this fired 1.93 times a chart, more often than direct
    reception; on the directed one, 0.20.

    57, RECEPTION OR UNDERMINING AFTER THE SIGN CHANGE. "If the Moon was
    empty in course, and then she passed over into the next sign and
    connected with the lord of her first sign (or its exaltation), IT IS
    JUST LIKE RECEPTION; and if she connected with a planet OTHER than the
    lord of her first sign or its exaltation, IT UNDERMINES HER." Both
    halves are reported: the second is a finding in its own right, not the
    absence of the first. Needs the forward simulation, which also means
    the slow planets exclude themselves -- Saturn takes years to change
    sign, well past the horizon.

    Sahl names the Moon in both 56 and 57. They are therefore evaluated for
    the Moon only, with the paragraph number on the row; the wider wording
    of 58 begins the following non-reception section and does not silently
    generalise these two reception rules.

    ABU MA'SHAR (Gr. Intr. VII.5, 129-133). Wider on every axis.
    All five dignities count (129). Reception also runs in REVERSE: "a
    planet connects with a planet, and the one accepting the connection is
    in the house of the one handing over" (130) -- his own note explains
    why, since "Saturn could never be received because he is too slow to
    connect with anyone." House or exaltation is strongest (131); a single
    minor dignity alone is weak "unless it brings together the bound and
    triplicity, or the bound and face, or the triplicity and face: for that
    will be a complete reception" (132). And reception can hold by looking
    with no connection at all, "except that reception by connection is more
    powerful" (133).

    Abu Ma'shar grades on TWO AXES, and they are returned as two columns.
    129-133 grade the LOCAL BASIS: "the strongest of them is the lord of
    the house or exaltation" (131), a lone minor dignity "is weak unless it
    brings together the bound and triplicity, or the bound and face, or the
    triplicity and face: for that will be a complete reception" (132).
    136-142 then class reception GLOBALLY as strong, middling, or below,
    and there house and exaltation sit with the rest: "a [2] middling
    reception is the planets' reception of each other from the house,
    exaltation, bound, triplicity, or face" (140); "if two met [together]
    from this, or each one of them received its associate, it is a strong
    reception" (141); the natural acceptances of 134-135 are "[3] below
    that" (142), and 137 and 139 are named strong forms. So a lone domicile
    reception is the strongest local basis AND globally middling -- both
    true, and an earlier version wrote only the first as its Grade. The
    rows carry 'Dignity quality' (129-133) and 'Overall class' (136-142);
    Sahl's rows keep his one 'Grade' (49-55), which is his own scale.

    Returns one row per reception found, naming the receiver, the planet
    received, which way round it runs, the dignities it rests on, its
    class(es) and whether it holds by connection or only by looking -- so
    the evidence is inspectable rather than reduced to a single flag.
    Mutual reception is reported as its own row.

    Absence of a row here is NOT Sahl's non-reception: that is a set of
    specific hostile configurations (58-62), computed separately in
    evaluate_non_reception(). A pair can easily be neither received nor
    non-received."""
    rows = _pairwise_configurations(planetary_data)
    triplicity_key = 'triplicity_day' if sect == 'Diurnal' else 'triplicity_night'
    sahl = reading('CONNECTION_PROFILE') == 'Sahl'
    results = []

    def claims(sitter, lord):
        """Which of `lord`'s dignities the `sitter` planet is standing in."""
        r = get_essential_rulers(planetary_data[sitter]['longitude'])
        found = []
        if r['domicile'] == lord: found.append('house')
        if r['exaltation'] == lord: found.append('exaltation')
        if r[triplicity_key] == lord: found.append('triplicity')
        if r['term'] == lord: found.append('bound')
        if not sahl and r['face'] == lord: found.append('face')   # Sahl never uses face
        return found

    def grade(found):
        """Sahl's single scale (49-55)."""
        if 'house' in found or 'exaltation' in found:
            return 'Perfect'
        # 50 ranks triplicity below perfect; bound only counts paired
        # with triplicity, on Masha'allah's authority (54-55).
        if 'triplicity' in found and 'bound' in found:
            return "Complete, triplicity with bound (54-55, Masha'allah)"
        if 'triplicity' in found:
            return 'Lesser, triplicity alone (50)'
        return None                                       # bound alone is not reception for Sahl

    def abu_axes(found):
        """Abu Ma'shar's two classifications of a dignity reception, as
        (dignity_quality per 129-133, overall_class per 136-142)."""
        majors = [d for d in found if d in ('house', 'exaltation')]
        minors = [d for d in found if d in ('bound', 'triplicity', 'face')]
        if majors:
            quality = f"Strongest basis, {' and '.join(majors)} (131)"
        elif len(minors) >= 2:
            quality = f"Complete, {' with '.join(minors)} (132)"
        else:
            quality = f"Weak, {minors[0]} alone (132)"
        # 140 lists all five dignities as middling; 141 promotes "two met
        # together from this" to strong. Which dignities makes no
        # difference to this axis -- that is what the other axis is for.
        overall = ('Strong (141): two dignities together' if len(found) >= 2
                   else 'Middling (140): one dignity')
        return quality, overall

    for row in rows:
        if row['aspect_name'] == 'Aversion' and not _sahl_body_row(row):
            continue
        connected = _is_connected(row)
        if sahl and not connected:
            continue                                      # Sahl requires the connection
        mode = 'By connection' if connected else 'By looking only (133)'
        # Reception is DIRECTED at the planet that is actually connecting,
        # not at the naturally lighter one. Sahl's own example is "the Moon
        # in Aries connecting with Mars, he receives her" -- she is received
        # because she is the one applying. Ordinarily the applicant is the
        # lighter planet and the two readings coincide; when the heavier is
        # closing (by retrogradation, or by overtaking a planet that is
        # slower still), they do not.
        #
        # Dykes' note on 130 makes this the explicit reason that paragraph
        # exists: "if the received planet always had to be the lighter
        # planet and connect with its own lord ... Saturn could never be
        # received because he is too slow to connect with anyone (UNLESS BY
        # RETROGRADATION)." Reading reception off natural rank threw away
        # the exception that note names. On a sample of ~8,000 configured
        # pairs the applicant is the heavier planet in 3.6% of them.
        light, heavy = row['light_name'], row['heavy_name']
        applicant = row['applicant'] or light
        accepter = row['receiver'] or heavy

        # 129: the connecting planet stands in the receiver's dignity.
        directions = [(accepter, applicant, 'Receives the connecting planet (129)')]
        if not sahl:
            # 130: the reverse, which Sahl does not have.
            directions.append((applicant, accepter, 'Receives the accepting planet (130)'))

        found_here = []
        for receiver, received, direction in directions:
            found = claims(received, receiver)
            if not found:
                continue
            row = {'Receiver': receiver, 'Received': received, 'Direction': direction,
                   'Via': ', '.join(found)}
            if sahl:
                g = grade(found)
                if not g:
                    continue
                row['Grade'] = g
            else:
                row['Dignity quality'], row['Overall class'] = abu_axes(found)
            row['Mode'] = mode
            found_here.append(receiver)
            results.append(row)
        if len(found_here) == 2:
            row = {'Receiver': f'{applicant} & {accepter}', 'Received': 'each other',
                   'Direction': 'Mutual', 'Via': '–'}
            if sahl:
                row['Grade'] = 'Mutual reception'           # Sahl does not name it
            else:
                # 141: "if ... each one of them received its associate, it
                # is a strong reception."
                row['Dignity quality'] = 'Each stands in a dignity of the other'
                row['Overall class'] = 'Strong (141): mutual'
            row['Mode'] = mode
            results.append(row)

    if not sahl:
        # --- 134-142: the recovered acceptance material ------------------
        # p. 477 was missing from the photographs until this revision. It
        # is Abu Ma'shar's OWN, and it is a different thing from 129-133:
        # the note on 134 says he "turns to a more general sense of the
        # Arabic word 'reception' which is NOT BASED ON DIGNITIES: namely,
        # that one planet will 'accept' the management of another if they
        # are in harmonious signs." Reported as its own basis so it cannot
        # be mistaken for a dignity reception.
        sign_of = {p: get_zodiac_sign(d['longitude']) for p, d in planetary_data.items()
                    if p != 'North Node'}
        for row in rows:
            a, b = row['p1'], row['p2']
            asp = row['aspect_name']

            # 137-138 come FIRST because they hold in aversion too: "he
            # receives her FROM ALL SIGNS, since her glow is from him". The
            # rest of this block needs a configuration; the Sun-Moon natural
            # reception does not, and skipping aversion rows before it lost
            # the row in 32% of charts.
            if {a, b} == {'Sun', 'Moon'}:
                if asp == 'Opposition':
                    # 137's own word, kept rather than forced onto the
                    # strong/middling/below ladder.
                    overall, via = 'Detestable (137): from the opposition', 'from the opposition'
                    quality = 'By nature, her glow is from him (137)'
                else:
                    sun_claims = _dispositors(planetary_data['Moon']['longitude'], sect) & {'Sun'}
                    doubled = bool(sun_claims)
                    overall = 'Strong (137)' + (', doubled by sign (138)' if doubled else '')
                    quality = 'By nature, her glow is from him (137)' + (' and by sign (138)' if doubled else '')
                    via = 'her glow is from him' + (', and he has a claim where she stands' if doubled else '')
                    if asp == 'Aversion':
                        via += ' (signs in aversion; 137 says from all signs)'
                results.append({
                    'Receiver': 'Sun', 'Received': 'Moon',
                    'Direction': 'Reception by nature (137-138)',
                    'Via': via, 'Dignity quality': quality, 'Overall class': overall,
                    'Mode': 'Natural, from all signs',
                })
            # 134: "if one of the two planets was in the TRINE of the other
            # (or in its SEXTILE), or in two signs of equal ascensions, or
            # in two signs whose length of the day is one [and the same],
            # or IN TWO SIGNS BELONGING TO ONE [and the same] PLANET, then
            # one of the two will 'receive' its associate due to the
            # agreement of the nature of these signs with each other."
            #
            # All four bases are computable: the equal-ascension and
            # equal-daylight sign pairs are enumerated in VII.5 itself (56
            # and 67-75; EQUAL_ASCENSION_PAIRS, EQUAL_DAYLIGHT_PAIRS). This
            # runs BEFORE the aversion skip, because 134 is acceptance
            # without looking -- the note on it: planets "in harmonious
            # signs" -- and most of these pairs do not look at each other:
            # Aries/Pisces, Gemini/Cancer and the same-lord pairs Aries/
            # Scorpio, Taurus/Libra and Capricorn/Aquarius are all in
            # aversion (note 157 on 53:
            # "some of the signs of equal ascensions below do look at each
            # other", i.e. most do not). An earlier version skipped aversion
            # first, which silently confined 134 to configured pairs.
            harmonious = []
            if asp in ('Trine', 'Sextile'):
                harmonious.append(f'{asp.lower()}')
            # "TWO signs belonging to one planet" -- Aries/Scorpio, Taurus/
            # Libra and so on. Equal lords with equal signs is an assembly,
            # which 134 does not list; without the inequality this fired on
            # every same-sign pair (26% of 134 rows in a sample).
            if (sign_of[a] != sign_of[b]
                    and SIGN_TO_DOMICILE.get(sign_of[a]) == SIGN_TO_DOMICILE.get(sign_of[b])):
                harmonious.append(f'both signs of {SIGN_TO_DOMICILE.get(sign_of[a])}')
            sign_pair = frozenset({sign_of[a], sign_of[b]})
            if sign_pair in EQUAL_ASCENSION_PAIRS:
                harmonious.append('signs of equal ascensions (56)')
            if sign_pair in EQUAL_DAYLIGHT_PAIRS:
                harmonious.append('signs of equal daylight (67-75)')
            if harmonious:
                results.append({
                    'Receiver': f'{a} & {b}', 'Received': 'each other',
                    'Direction': 'Acceptance by harmonious signs (134)',
                    'Via': ', '.join(harmonious),
                    'Dignity quality': 'Not a dignity basis: harmonious signs (134)',
                    'Overall class': 'Below middling (142)', 'Mode': 'Not a dignity reception',
                })
            if asp == 'Aversion':
                continue

            # 135: "the fortunes receive each other due to the moderation
            # of their natures, while Mars and Saturn each receive the
            # other from the assembly, sextile, and trine."
            if {a, b} == FORTUNES:
                results.append({
                    'Receiver': f'{a} & {b}', 'Received': 'each other',
                    'Direction': 'Acceptance by nature, the two fortunes (135)',
                    'Via': 'moderation of their natures',
                    'Dignity quality': 'Not a dignity basis: nature (135)',
                    'Overall class': 'Below middling (142)', 'Mode': 'Not a dignity reception',
                })
            if {a, b} == INFORTUNES and asp in ('Conjunction', 'Sextile', 'Trine'):
                results.append({
                    'Receiver': f'{a} & {b}', 'Received': 'each other',
                    'Direction': 'Acceptance, the two infortunes (135)',
                    'Via': f'{asp.lower()} only -- 135 allows assembly, sextile and trine',
                    'Dignity quality': 'Not a dignity basis: nature (135)',
                    'Overall class': 'Below middling (142)', 'Mode': 'Not a dignity reception',
                })

            # 137-138: "the majority of [strong reception] belongs to the
            # Moon relative to the Sun, because HE RECEIVES HER FROM ALL
            # SIGNS, since her glow is from him -- EXCEPT THAT HIS
            # RECEPTION OF HER FROM THE OPPOSITION IS DETESTABLE. But if
            # her connection with him is from a sign in which he has a
            # claim, that is TWO RECEPTIONS: a reception by nature, and a
            # reception by sign."
            # (137-138 handled at the top of the loop, before the aversion skip.)

            # 139: "if Mercury received a planet from out of Virgo, that is
            # also a strong reception." The note reads "from out of" as the
            # other planet being IN Virgo, where Mercury holds both house
            # and exaltation.
            for x, y in ((a, b), (b, a)):
                if x == 'Mercury' and sign_of[y] == 'Virgo':
                    results.append({
                        'Receiver': 'Mercury', 'Received': y,
                        'Direction': 'Reception in Virgo (139)',
                        'Via': 'house and exaltation together',
                        'Dignity quality': 'Strongest basis, house and exaltation (131)',
                        'Overall class': 'Strong (139)', 'Mode': 'By dignity',
                    })
        return results

    # --- 56: reception at one remove ---------------------------------
    # "And if the Moon was CONNECTING WITH A PLANET and THAT PLANET was
    # connecting with the lord of the house of the Moon or its exaltation,
    # then the Moon is received." The note on 56 glosses the second lord as
    # "the exalted lord of the sign in which the Moon is," and the note on
    # the sentence calls the whole thing "like a transfer of light which
    # indirectly allows for reception."
    #
    # Sahl names the Moon, as he does throughout 49-57; the mechanism has
    # nothing lunar in it, and 58 immediately widens the same shape to "the
    # Moon or the lord of the Ascendant," so it is applied to any planet
    # and the paragraph number is on the row.
    connected_pairs = {frozenset({r['p1'], r['p2']}): r for r in rows
                        if (r['aspect_name'] != 'Aversion' or _sahl_body_row(r)) and _is_connected(r)}

    def _connected(a, b):
        return frozenset({a, b}) in connected_pairs

    def _applies_to(a, b):
        """a is CONNECTING WITH b in Sahl's own directed sense: "a light,
        quick star GOING STRAIGHTAWAY TO a heavy star ... as long as the
        planet is GOING TOWARDS the [other] planet" (6). Separating is his
        separate term (22), so it does not satisfy "connecting with"."""
        r = connected_pairs.get(frozenset({a, b}))
        return (r is not None and r['motion'] == 'Applying'
                and (r['applicant'] or r['light_name']) == a)

    # 56 and 57 both name THE MOON, and both are restricted to her. 58's
    # subsequent "the Moon or the lord of the Ascendant" governs 58, not
    # the two paragraphs before it -- an earlier version generalised them
    # to every planet on the strength of that later sentence, which is the
    # weaker reading and was flagged as such at the time.
    for planet in ('Moon',):
        own_sign = get_zodiac_sign(planetary_data[planet]['longitude'])
        lords = {SIGN_TO_DOMICILE.get(own_sign)} | {
            p for p, s in EXALTATIONS.items() if s[0] == own_sign}
        lords.discard(None)
        lords.discard(planet)
        for middle in planetary_data:
            if middle in (planet, 'North Node') or middle in lords:
                continue          # middle == the lord is plain 49-52, not 56
            if not _applies_to(planet, middle):
                continue
            for lord in sorted(lords):
                if lord in planetary_data and _applies_to(middle, lord):
                    results.append({
                        'Receiver': lord, 'Received': planet,
                        'Direction': f'Received at one remove, via {middle} (56)',
                        'Via': f"lord of {own_sign}", 'Grade': 'Indirect reception (56)',
                        'Mode': 'By connection',
                    })

    # --- 57: reception, or undermining, after the sign change ---------
    # "And if the Moon was EMPTY IN COURSE, and then she PASSED OVER INTO
    # THE NEXT SIGN and connected with the lord of her first sign (or its
    # exaltation), IT IS JUST LIKE RECEPTION; and if she connected with a
    # planet OTHER than the lord of her first sign or its exaltation, IT
    # UNDERMINES HER." Both halves are reported: the second is a finding,
    # not the absence of one.
    if sim is not None:
        for planet in ('Moon',):
            if planet not in sim['events']:
                continue
            exits = sim['events'][planet]['sign_exits']
            if not exits:
                continue
            ingress = exits[0]
            # "EMPTY IN COURSE" is prospective, not a snapshot: no
            # perfection completes before she leaves the sign she is in.
            # The previous test asked only whether she was connected to
            # anything right now, which is Sahl's looser sense at 63 and
            # not what 57 needs.
            empty = True
            for other in planetary_data:
                if other in (planet, 'North Node'):
                    continue
                tgt = _configuration_target_at(sim, planet, other, 0.0)
                if tgt is None:
                    continue
                if _perfection_day(sim, planet, other, tgt, before_day=ingress) is not None:
                    empty = False
                    break
            if not empty:
                continue
            # And the search window is the NEXT SIGN ONLY -- "she passed
            # over into the next sign and connected with ..." An unbounded
            # search after the ingress could return a connection several
            # signs and months later and label it "after the sign change".
            second_exit = next((d for d in exits if d > ingress), sim['horizon_days'])
            first_lord = SIGN_TO_DOMICILE.get(get_zodiac_sign(planetary_data[planet]['longitude']))
            exalted = next((p for p, s in EXALTATIONS.items()
                             if s[0] == get_zodiac_sign(planetary_data[planet]['longitude'])), None)
            wanted = {first_lord, exalted} - {None, planet}
            best, best_day = None, None
            for other in planetary_data:
                if other in (planet, 'North Node'):
                    continue
                target = _configuration_target_at(sim, planet, other, ingress + 0.05)
                if target is None:
                    continue
                day = _perfection_day(sim, planet, other, target,
                                       after_day=ingress, before_day=second_exit)
                if day is not None and (best_day is None or day < best_day):
                    best, best_day = other, day
            if best is None:
                continue
            if best in wanted:
                results.append({
                    'Receiver': best, 'Received': planet,
                    'Direction': f'Just like reception, after the sign change (57)',
                    'Via': 'lord of the sign it left',
                    'Grade': f'Reached on day {best_day:.0f}', 'Mode': 'By connection',
                })
            else:
                results.append({
                    'Receiver': best, 'Received': planet,
                    'Direction': 'Undermined after the sign change (57)',
                    'Via': f"connects with {best}, not the lord of the sign it left",
                    'Grade': f'Reached on day {best_day:.0f}', 'Mode': 'By connection',
                })
    if sahl and results:
        # REFUSAL WINS (decision D-2, 2026-09-08; scope settled from the
        # text the same day). Sahl's profile only -- Abu Ma'shar's Figure
        # 143 reads the same configurations as favor (VII.5, 126-128).
        #
        # Kind II (59-60), the connection made FROM the receiver's fall:
        # "like one who comes to it from the house of its enemies, NOT
        # ACCEPTING IT nor approaching it." A refusal, so the reception the
        # same pair would otherwise earn is not listed. It can only ever be
        # a minor one (triplicity with bound, 54-55): no planet has its
        # house or exaltation in the sign of its own fall. Sahl's own
        # chart: Mercury leaving Gemini "was connecting with Mars, and he
        # does not accept [Mercury]" (Questions Ch. 1, 63; fn. 27 -> Ch. 1,
        # 40, "connecting with Mars from Cancer"), although Mars is
        # Cancer's night triplicity lord and holds its first bound; and
        # Ch. 3, 51 on the contrary of reception: "he does not acknowledge
        # it, he does not accept it."
        #
        # Kind IV (62), the receiver in ITS OWN fall, is worded as a
        # diminution, not a refusal: "it brings it down and diminishes what
        # comes to it from that." The receiver may hold its house or
        # exaltation at the applicant's place (Venus in Virgo receiving the
        # Moon from Taurus), and 49's "perfect reception" is not revoked by
        # 62 -- so the row stays, marked brought down. Kind V is likewise
        # a condition of the receiver, who stands in the applicant's fall;
        # it annotates the surviving reception with that distinct reason.
        #
        # Kind III (61) refuses too (decision D-22, 2026-09-08). The
        # applicant stands in its OWN fall and the receiver holds no house
        # or exaltation there: "it will not see it as fit for anything, as
        # though the one asking is offering defeat, and it will not be
        # recognized" (Ch. 3, 61), and in Sahl's other work "it DOES NOT
        # ACCEPT THEM ... his sought matter will not be accomplished"
        # (Questions Ch. 1, 41). Those are Kind II's verbs -- "not
        # recognized", "does not accept" -- not Kind IV's "brings it down",
        # so it suppresses rather than annotates. What it can suppress is
        # only ever minor: 61's own parenthesis exempts house and
        # exaltation, leaving the triplicity (50) with or without the bound
        # (54-55). Dykes' fn. 22 on Questions 1, 41 reads the exemption
        # wider -- "any dignity" -- on which Kind III would not fire here at
        # all; 61's parenthesis is followed instead, and the disagreement is
        # recorded in 13_open_decisions.md under D-22.
        non = evaluate_non_reception(planetary_data, sect)
        refused = {(r['Connecting'], r['With']) for r in non
                   if str(r['Kind']).startswith(('II ', 'III '))}
        brought_down = {
            (r['Connecting'], r['With']): (
                'the receiver in its own fall (62)'
                if str(r['Kind']).startswith('IV ')
                else "the receiver in the applicant's fall (62)"
            )
            for r in non if str(r['Kind']).startswith(('IV ', 'V '))
        }
        kept = []
        for r in results:
            pair = (r.get('Received'), r.get('Receiver'))
            if pair in refused:
                continue
            if pair in brought_down:
                reason = brought_down[pair]
                r = {**r, 'Grade': f"{r.get('Grade', '')}; brought down, {reason}".lstrip('; ')}
            kept.append(r)
        results = kept
    return results

@_connection_aware
def evaluate_non_reception(planetary_data, sect):
    """Non-reception (Sahl, The Introduction Ch.3, 58-62, Fig. 18): five
    named ways a connection is refused rather than received, using the
    book's own A -> B model (A = the connecting/faster planet, B = the one
    it connects with).

    Kind I (58): B holds no essential-dignity claim (domicile, exaltation,
    triplicity, term, or face) at A's own position -- "B is alien in A's
    sign." Not recognized, not received.

    Kind II (59-60): A is in B's own sign of fall -- "like one who comes
    to it from the house of its enemies." Confirmed against Sahl's own
    five examples (Aries->Saturn, Cancer->Mars, Virgo->Venus, Capricorn->
    Jupiter, Libra->Sun) -- all exactly A-in-B's-fall.

    Kind III (61): A is in A's OWN fall, and B holds no share -- house or
    exaltation ONLY, per 61's own parenthetical ("that is, by house or
    exaltation") -- at A's position. Narrower than Kind I's test (which
    also allows triplicity/term/face to count): a self-fall specifically
    needs A's two major dignities, not a minor one, to rescue it -- "as
    though the one asking is offering defeat."

    Kind IV (62): B is in B's OWN fall -- brings the connection down
    regardless of A's own condition.

    Kind V (62): B sits in A's own sign of fall -- the planet A is
    connecting with has landed in the very sign that would ruin A."""
    with doctrine(SAHL):
        rows = _pairwise_configurations(planetary_data)
        triplicity_key = 'triplicity_day' if sect == 'Diurnal' else 'triplicity_night'
        results = []

        for r in rows:
            if (r['aspect_name'] == 'Aversion' and not _sahl_body_row(r)) or not _is_connected(r):
                continue
            # A is "the connecting planet" (58: "if the Moon or the lord of the
            # Ascendant CONNECTED WITH a planet"), which is the directed
            # applicant, not the standing lighter one. Reception already keys
            # this way; keying non-reception the other way made the two
            # disagree on the ~3.6% of pairs where the heavier planet is the
            # one closing.
            a, b = r['applicant'] or r['light_name'], r['receiver'] or r['heavy_name']
            a_lon, b_lon = planetary_data[a]['longitude'], planetary_data[b]['longitude']
            a_sign, b_sign = get_zodiac_sign(a_lon), get_zodiac_sign(b_lon)
            a_rulers = get_essential_rulers(a_lon)
            b_alien_in_a_sign = b not in (a_rulers['domicile'], a_rulers['exaltation'], a_rulers[triplicity_key], a_rulers['term'], a_rulers['face'])
            b_no_major_share_in_a_sign = b not in (a_rulers['domicile'], a_rulers['exaltation'])
            a_in_own_fall = a_sign in FALLS.get(a, [])
            b_in_own_fall = b_sign in FALLS.get(b, [])
            a_in_b_fall = a_sign in FALLS.get(b, [])
            b_in_a_fall = b_sign in FALLS.get(a, [])

            if b_alien_in_a_sign:
                results.append({'Kind': 'I (58)', 'Connecting': a, 'With': b})
            if a_in_b_fall:
                results.append({'Kind': 'II (59-60)', 'Connecting': a, 'With': b})
            if a_in_own_fall and b_no_major_share_in_a_sign:
                results.append({'Kind': 'III (61)', 'Connecting': a, 'With': b})
            if b_in_own_fall:
                results.append({'Kind': 'IV (62)', 'Connecting': a, 'With': b})
            if b_in_a_fall:
                results.append({'Kind': 'V (62)', 'Connecting': a, 'With': b})

        return results

# --- Prenatal Lunation (Syzygy) — Abbasid / Medieval Method --------------

def get_house_number(longitude, cusps):
    """Given a longitude and 12 quadrant house cusps (in house-1..house-12
    order, as returned by swe.houses), return which house (1-12) it falls
    in. Each house spans from its own cusp forward to the next cusp."""
    for i in range(12):
        start = cusps[i]
        end = cusps[(i + 1) % 12]
        span = (end - start) % 360
        rel = (longitude - start) % 360
        if rel < span:
            return i + 1
    return 12

# Ptolemy's five-degree rule, in Sahl's own words. Fifty Aphorisms #44,
# 87-89: "the planet will NOT BE FALLING FROM THE STAKE unless it was 5
# degrees distant from its rear: I mean, if the stake was 10 degrees of
# Aries, then indeed every planet which has less than 5 degrees between it
# and the stake, is truly counted as BEING IN THE STAKE. And every planet
# which was in more than 5 degrees [from it] is not counted as being in the
# stake." Dykes' note there names it as Ptolemy's rule, "where the power of
# the stake or angle extends by 5 degrees beyond the cusp -- as measured in
# diurnal motion, hence Sahl's reference to the 'rear' of the stake."
#
# Sahl states it a second time, in a different work: "the planets will not
# fall from the stakes except after 5 degrees, and the planets do not become
# powerful in the sign [they are in] until they travel 5 degrees in it"
# (On Nativities Ch.1.22, 9, whose own footnote cross-references Aphorism
# #44). Two statements in Sahl, not independent witnesses; fn 176 cross-refers to the aphorism.
FIVE_DEGREE_CARRYOVER = 5.0
ANGLE_CUSP_INDICES = (0, 3, 6, 9)  # the four stakes, in swe.houses order
# The carry-over applies at the FOUR STAKES ONLY. The owner's canon of
# 2026-09-11 (OWNER_RULING_PLACES_VS_DYNAMICS): the five-degree rule is a
# DYNAMICS rule and nothing else -- a planet that has passed one of the four
# axial degrees (Asc, MC, Dsc, IC) by 0 to 5 degrees inclusive keeps the
# stake's strength; it never applies at the eight intermediate cusps, never
# moves a planet between whole-sign places, never touches aspects, and Lots
# have no dynamic angularity. Sahl states it for the stakes twice (Aphorism
# #44, 88; On Nativities 1.22, 9); 1.18, 19 -- "if there were 5 degrees
# between a planet and the degree of the Ascendant from behind it ... its
# strength will be in the Ascendant, and it will be fit for releasing; and
# likewise in all of the houses" -- is READ AS THE FOUR STAKES, the course's
# reading (Lesson 3 §4-5; Glossary s.v. Angles, succeedents, cadents), not
# as an all-cusps form. The `five_degree_all_cusps` preference that offered
# the all-cusps form was retired the same day; the constant stays for the
# `angles_only` parameter's default and is no longer a reading. A primary
# text of ANOTHER author reads the phrase literally: al-Qabisi IV.4 (ITA
# VIII.1.3, printed p. 355, read against the photograph 2026-09-17), inside his own releaser procedure,
# "every planet which was before the degree of the Ascendant or any house by
# five equal degrees and less, its strength will be valid in the house which
# follows it" -- and ITA's Introduction §6 (p. 12) gives the all-cusps form
# to Ptolemy. The owner's decision of 2026-09-15 (reconciliation decision 2,
# as recommended): the ruling stands for Sahl's three sentences, the Releaser
# tab names al-Qabisi's "any house" as the alternative, and no code
# alternative is restored -- it would import another author's procedure into
# Sahl's selector.
FIVE_DEGREE_ALL_CUSPS = False

def get_effective_house(longitude, cusps, angles_only=None):
    """Quadrant house with the five-degree carryover applied: a planet
    within 5 degrees before a cusp is counted as already in that house.

    MEASURED IN ECLIPTIC LONGITUDE -- a text's own unit, not a choice
    alone: al-Qabisi IV.4 states the rule "by five equal degrees" (ITA
    VIII.1.3, printed p. 355, read against the photograph 2026-09-17), and "equal degrees" is the Great
    Introduction's term for ecliptic measure (ITA VI.1.1 fn 2, p. 281).
    Against it, Dykes' note on Fifty Aphorisms #44 says the five degrees
    are reckoned "AS MEASURED IN DIURNAL MOTION, hence Sahl's reference to
    the 'rear' of the stake" -- that is, along the diurnal circle, not in
    zodiacal degrees. The two
    coincide only near the equinoctial points and diverge with latitude
    and with the obliquity of the rising sign. The oblique-ascension
    geometry exists in this file (_oblique_ascension, decision D-1) and is
    not used here: the five degrees stay in longitude, a proxy named as
    one wherever it is reported (the owner's ruling of 2026-09-11 keeps
    the proxy and its label). It affects Sahl 83 and Abu Ma'shar 39 and 42,
    and the releaser's places. Measured from the axial DEGREE, not "the
    last five degrees of the preceding sign": Ascendant 10 Aries, planet 28
    Pisces is 12 degrees away and not in the stake.

    Two of Sahl's three statements are about the stakes (Aphorism #44, 88;
    On Nativities 1.22, 9), and the transitions they describe (12th into
    1st, 3rd into 4th, 6th into 7th, 9th into 10th) are the cadent-to-
    angular ones, which is why the rule is phrased as not FALLING from the
    stake. The third, On Nativities 1.18, 19, ends "and likewise in all of
    the houses": 1.18, 19's "likewise in all of the houses" is read as the
    four stakes (the course's reading, adopted as canon 2026-09-11).
    angles_only defaults to True via FIVE_DEGREE_ALL_CUSPS; the all-cusps
    form is no longer offered as a reading.

    get_house_number() is deliberately left alone and still returns strict
    cusp membership. The two are separate facts and both are kept."""
    if angles_only is None:
        angles_only = not FIVE_DEGREE_ALL_CUSPS
    raw = get_house_number(longitude, cusps)
    if angles_only:
        # The allowance belongs to each axial degree, not to whichever cusp
        # happens to end the raw division. A narrow intermediate house can
        # therefore lie wholly inside the five degrees before a stake.
        # Measure to all four stakes directly and take the nearest eligible
        # one; do not extend the allowance to the intervening cusp.
        eligible = [((cusps[idx] - longitude) % 360.0, idx)
                    for idx in ANGLE_CUSP_INDICES
                    if (cusps[idx] - longitude) % 360.0 <= FIVE_DEGREE_CARRYOVER]
        if eligible:
            _distance, stake_idx = min(eligible)
            return stake_idx + 1
        return raw

    # The explicit legacy mode retains its old all-cusp behavior: only the
    # cusp ending the raw division can receive the allowance.
    next_idx = raw % 12
    to_next_cusp = (cusps[next_idx] - longitude) % 360.0
    if to_next_cusp <= FIVE_DEGREE_CARRYOVER:
        return next_idx + 1
    return raw


def quadrant_strength_position(longitude, cusps):
    """Return the actual division/quarter and adjusted strength house.

    The four angle cusps delimit quarter gender. The five-degree allowance
    changes only the strength classification at those angles.
    """
    division = get_house_number(longitude, cusps)
    strength_house = get_effective_house(longitude, cusps)
    return {
        'division': division,
        'strength_house': strength_house,
        'strength': ('stake or following'
                     if strength_house in ANGLE_HOUSES | SUCCEDENT_HOUSES else 'falling'),
        'quarter_gender': ('Masculine'
                           if division in MASCULINE_QUADRANT_HOUSES else 'Feminine'),
        'carried': division != strength_house,
    }

def get_wsh_house(longitude, ascendant_lon):
    """Whole Sign House: the Ascendant's sign is house 1 in its entirety,
    and each subsequent sign (in zodiacal order) is the next house — no
    quadrant cusp division within a sign."""
    asc_sign_idx = int((ascendant_lon % 360.0) // 30)
    target_sign_idx = int((longitude % 360.0) // 30)
    return ((target_sign_idx - asc_sign_idx) % 12) + 1

def get_essential_rulers(longitude):
    """Look up the classical essential dignities (domicile, exaltation,
    triplicity, term, face) ruling an arbitrary zodiacal degree — not tied
    to any specific planet's own position, unlike evaluate_essential_
    dignities(). Used to profile the prenatal syzygy degree itself."""
    longitude = longitude % 360.0
    sign = get_zodiac_sign(longitude)
    degree_in_sign = longitude % 30
    triplicity = triplicity_rulers(sign, table='Great Introduction')
    term_lord = next((lord for limit, lord in EGYPTIAN_TERMS.get(sign, []) if degree_in_sign < limit), '-')
    face_lord = CHALDEAN_ORDER[int(longitude // 10) % 7]
    return {
        'sign': sign,
        'domicile': SIGN_TO_DOMICILE.get(sign, '-'),
        'exaltation': SIGN_TO_EXALTATION.get(sign, '-'),
        'triplicity_day': triplicity['Day'],
        'triplicity_night': triplicity['Night'],
        # The Great Introduction table's partner (Mercury for Virgo, made in
        # triplicity_rulers: V.14, 7 with Figure 53 and fn 100).
        'triplicity_participating': triplicity['Participating'],
        'term': term_lord,
        'face': face_lord,
    }

def _sun_moon_signed_offset(jd, target_deg):
    """Sun/Moon longitudes and speeds at jd, plus the signed angular offset
    of (Moon - Sun) from target_deg, normalized to (-180, 180]. Positive
    means (Moon - Sun) has already passed target_deg going forward."""
    sun_res = swe.calc_ut(jd, swe.SUN)[0]
    moon_res = swe.calc_ut(jd, swe.MOON)[0]
    sun_lon, sun_speed = sun_res[0], sun_res[3]
    moon_lon, moon_speed = moon_res[0], moon_res[3]
    diff = (moon_lon - sun_lon) % 360
    offset = (diff - target_deg + 180) % 360 - 180
    rel_speed = moon_speed - sun_speed
    return sun_lon, moon_lon, offset, rel_speed

@st.cache_data(max_entries=32, show_spinner=False)
def calculate_prenatal_syzygy(jd_natal, lat, lon, natal_houses):
    """Find the most recent New or Full Moon before birth (the 'prenatal
    syzygy'), following the Abbasid/medieval method:

    - If the Moon is less than 180° ahead of the Sun at birth, the preceding
      syzygy was a conjunction (Coniunctio) — the birth is 'Conjunctional'.
    - Otherwise it was an opposition (Praeventio) — the birth is
      'Preventional', and the Syzygy degree is taken from whichever
      luminary was above the horizon at that prenatal Full Moon: Sahl, On
      Nativities 1.7, 2, "if it was after the opposition, then take the
      portion of whichever of the two luminaries was above the earth"
      (stated there for the Ascendant's degree; fn 33: the portion is the
      degree; Dykes's comment fixes "above the earth" at the lunation's
      moment). When both or neither is above the earth the Moon's degree
      is taken (FINAL-A6, 2026-09-11, when the line here said "per medieval
      practice" and cited nothing). Since 2026-09-15 the choice has its
      text: al-Qabisi IV.3 (ITA VIII.1.2, printed p. 352, read against the photograph 2026-09-17) reports
      three opinions on the fullness's degree -- Ptolemy's, "the degree of
      the luminary which was above the earth"; "certain ones of the
      sages'", when one luminary is on the eastern degree and the other on
      the western, "the degree of the east is the degree of the
      prevention"; and Valens's, "the degree in which the fullness is --
      wanting the degree of the Moon". The Moon default is Valens's; the
      sages' eastern-degree tie rule is named on the page and not adopted
      (decision 7, as recommended).

    The exact moment is located by a Newton-style root search on the
    Sun/Moon ephemeris (not just an average-synodic-month estimate), then
    the resulting degree is profiled for essential dignities and placed
    into the natal Whole Sign houses (anchored to the natal Ascendant).
    """
    sun_lon0 = swe.calc_ut(jd_natal, swe.SUN)[0][0]
    moon_lon0 = swe.calc_ut(jd_natal, swe.MOON)[0][0]
    diff0 = (moon_lon0 - sun_lon0) % 360

    if diff0 < 180.0:
        target = 0.0
        event_type = 'Conjunctional'
        event_label = 'Conjunctional (New Moon)'
    else:
        target = 180.0
        event_type = 'Preventional'
        event_label = 'Preventional (Full Moon)'

    # Initial guess via the average relative Moon-Sun speed (~12.19 deg/day),
    # then refine with Newton's method against the true ephemeris speed.
    AVG_REL_SPEED = 12.19075
    delta_back_deg = (diff0 - target) % 360
    jd_guess = jd_natal - delta_back_deg / AVG_REL_SPEED

    sun_lon, moon_lon = sun_lon0, moon_lon0
    for _ in range(15):
        sun_lon, moon_lon, offset, rel_speed = _sun_moon_signed_offset(jd_guess, target)
        if abs(offset) < 1e-6:
            break
        if abs(rel_speed) < 1e-6:
            rel_speed = AVG_REL_SPEED
        jd_guess -= offset / rel_speed

    jd_syzygy = jd_guess

    # Ascendant/houses at the syzygy moment (same natal location), needed
    # to determine (a) which luminary was above the horizon for a
    # Preventional birth, and (b) the sect of the syzygy chart itself for
    # triplicity assignment.
    # Above the horizon by ALTITUDE (_sin_altitude), from each luminary's
    # full ecliptic position at the syzygy moment; the ecliptic proxy this
    # replaced could invert near the poles and, for the Moon, whose
    # ecliptic latitude reaches five degrees, within a few degrees of the
    # horizon at any latitude (fixed 2026-09-08, with the chart's sect).
    _, ascmc_syzygy = swe.houses(jd_syzygy, lat, lon, b'B')
    obliquity_syzygy = swe.calc_ut(jd_syzygy, swe.ECL_NUT)[0][0]
    sun_res = swe.calc_ut(jd_syzygy, swe.SUN)[0]
    moon_res = swe.calc_ut(jd_syzygy, swe.MOON)[0]
    sun_above_horizon = _sin_altitude(sun_res[0], sun_res[1], sun_res[2], obliquity_syzygy, ascmc_syzygy[2], lat) > 0.0
    moon_above_horizon = _sin_altitude(moon_res[0], moon_res[1], moon_res[2], obliquity_syzygy, ascmc_syzygy[2], lat) > 0.0
    is_diurnal_syzygy = sun_above_horizon

    if event_type == 'Conjunctional':
        syzygy_lon = sun_lon  # sun_lon == moon_lon at convergence
    else:
        if sun_above_horizon and not moon_above_horizon:
            syzygy_lon = sun_lon
        elif moon_above_horizon and not sun_above_horizon:
            syzygy_lon = moon_lon
        else:
            syzygy_lon = moon_lon  # ambiguous/edge case: default to the Moon

    natal_house = get_wsh_house(syzygy_lon, natal_houses[0])
    rulers = get_essential_rulers(syzygy_lon)

    active_triplicity_lord = rulers['triplicity_day'] if is_diurnal_syzygy else rulers['triplicity_night']
    active_triplicity_label = 'Day' if is_diurnal_syzygy else 'Night'

    # Almuten by 5/4/3/2/1 points: weighted score across the essential
    # dignities ruling this degree (only the sect-appropriate triplicity
    # lord counts, not the Participating ruler, matching standard almuten
    # scoring). The course's technique; the weights are al-Qabisi's, ITA
    # I.18 (al-Qabisi I.22, printed p. 81, read against the photograph 2026-09-17): "the Lord of a domicile
    # has five strengths, and the Lord of the exaltation four, and the Lord
    # of the triplicity three, and the Lord of the bound two, and the Lord
    # of the face one", fn 210 "only the primary triplicity Lord receives
    # points"; the five places are his too (IV.7, ITA VIII.1.4). Relabelled
    # 2026-09-15 (decision 6); until then the page said "stated in no text
    # in hand". Sahl's own procedure for the governor of this degree (On
    # Nativities 1.7, 3-7) is sahl_syzygy_governor, shown beside it.
    scores = {}
    def _add_score(planet, pts):
        if planet and planet != '-':
            scores[planet] = scores.get(planet, 0) + pts
    _add_score(rulers['domicile'], ESSENTIAL_DIGNITY_WEIGHTS['domicile'])
    _add_score(rulers['exaltation'], ESSENTIAL_DIGNITY_WEIGHTS['exaltation'])
    _add_score(active_triplicity_lord, ESSENTIAL_DIGNITY_WEIGHTS['triplicity'])
    _add_score(rulers['term'], ESSENTIAL_DIGNITY_WEIGHTS['term'])
    _add_score(rulers['face'], ESSENTIAL_DIGNITY_WEIGHTS['face'])
    almuten = max(scores, key=scores.get) if scores else '-'
    almuten_score = scores.get(almuten, 0)

    return {
        'jd_syzygy': jd_syzygy,
        'event_type': event_type,
        'event_label': event_label,
        'syzygy_longitude': syzygy_lon,
        'sect_diurnal': is_diurnal_syzygy,
        'natal_house': natal_house,
        'rulers': rulers,
        'active_triplicity_lord': active_triplicity_lord,
        'active_triplicity_label': active_triplicity_label,
        'almuten': almuten,
        'almuten_score': almuten_score,
    }

# --- The governor of the syzygy degree, Sahl, On Nativities 1.7, 3-7 --------
# (CONV-ESSENTIAL_DIGNITY_WEIGHTS, 2026-09-11; rebuilt the same evening on
# the owner's ruling after Astra's blind reading, GOV-1.7 --
# process/astra_2026-09-11/1.7_governor_ruling.md.) Sahl states his own
# procedure for the one in charge of the degree of the meeting or
# opposition; the 5/4/3/2/1 almuten above is the course's technique (the
# weights al-Qabisi's, ITA I.18) and is kept beside it under its own name. 1.7, 3: "you will know the one in
# charge of that portion from five things: the lord of the house,
# triplicity, exaltation, bound, and image, and the eastern one of them --
# if [one] had superior claims over the rest of them". 1.7, 4: "Then see
# which of them is stronger in its [own] place, and is direct in course,
# looking at the sign of the meeting or opposition". 1.7, 7: "if they were
# both in power equally, [then] whichever of them was in a stake or in its
# own house, triplicity, bound, or exaltation, and had superiority over its
# associate in this respect, that is the governor."
#
# TWO RESULTS, both on the page. (1) THE VERDICT, Astra's conservative
# procedure: a name only where the text's clear subcases decide, otherwise
# "unresolved" with the profiles printed and the unmodelled stages named --
# 4's "stronger in its [own] place", 5-6 (the lord of the best; the one
# changed more quickly), and 7's ranking among candidates that each hold a
# listed advantage. The Sun is a claim-holder whose solar side is NOT
# APPLICABLE: 3's eastern preference neither prefers nor sets him aside.
# 3's preference is a preference among claim-holders, not a veto: a candidate
# not qualifying as eastern is set aside only by an eastern one with at least as many
# claims on the degree ("if [one] had superior claims over the rest").
# 7 keeps a PROFILE, not a score: a listed advantage (a stake; own house,
# exaltation, triplicity, bound -- the image is not in 7's list) beats
# none; two candidates each holding one are left unresolved; 1.20, 2-4's
# ranking of the lords is not imported. (2) THE MODEL, this app's
# approximation as first built: the eastern pool first (the Sun having no
# side), then one point a listed condition, highest wins -- disclosed on
# the page as arithmetic of this app's, not a rule Sahl states.
# "In a stake": strength language, the DIVISION under the canon
# (OWNER_RULING_PLACES_VS_DYNAMICS), with Astra's notation on the row that
# for the stakes is the counted sign (Introduction 2,
# 31). Eastern preference now reads the adopted Sahl 1.22 level 1;
# the lunar criterion is unresolved. Gr. Intr. VII.2, 4 supplies lunar
# side, not that criterion; al-Qabisi III.8a, ITA II.10.1 p. 98 supplies
# the opposition boundary convention for the superiors. Every condition
# is evaluated in the natal
# chart, declared. Al-Qabisi's own tie-breaks for his governor (IV.5, ITA
# VIII.1.3 p. 356: equal claimants that all aspect, "the one which was
# stronger in [its own] place"; equal there, "the one which was closer to
# the degree of the releaser", printed p. 356, read against the photograph 2026-09-17) are a different technique of
# a different author, named in SAHL_1_7_UNMODELLED and not borrowed
# (decision 15).
SAHL_1_7_LOOKING_SIGN_COUNTS = (0, 2, 3, 4, 6)   # the same sign or a whole-sign aspect to it
SAHL_1_7_UNMODELLED = ("4's \"stronger in its [own] place\", 5-6 (the lord of the best; the one changed more quickly "
                       "into the superior condition) and 7's ranking among candidates that each hold a listed advantage "
                       "are not modelled; al-Qabisi's tie-breaks for his own governor -- the claimant stronger in its place, "
                       "then the one nearer the releaser's degree (ITA VIII.1.3, al-Qabisi IV.5) -- are a different author's "
                       "procedure, named here and not borrowed")
SAHL_1_7_MODEL_DISCLOSURE = ("this app's arithmetic, not a rule Sahl states: the eastern candidates taken first (the Sun "
                             "having no side), then one point for a stake (by the division) and one each for own house, "
                             "exaltation, triplicity and bound, the highest total the pick; 4's comparative place strength "
                             "and 5-6 omitted; equal totals are model ties")

@dataclass(frozen=True)
class SahlEasternLevel:
    """One explicitly selected level of the adopted Sahl 1.22 reading."""

    level: int
    status: str
    threshold: float | None
    reason: str
    source: str

    @property
    def value(self):
        if self.status == 'unresolved':
            return UnresolvedResult(self.reason, self.source)
        if self.status == 'not applicable':
            return None
        return self.status == 'yes'

    @property
    def display(self):
        if self.status == 'unresolved':
            return self.value
        if self.status == 'not applicable':
            return 'not applicable — Sun'
        if self.status == 'yes':
            return 'yes (considered eastern)' if self.level == 1 else 'yes (easternizes)'
        return 'no — ' + self.status


@dataclass(frozen=True)
class SahlEasternResult:
    """Both levels share one unrounded elongation and motion observation."""

    planet: str
    elongation: float
    speed: float
    level1: SahlEasternLevel
    level2: SahlEasternLevel


def sahl_eastern_1_22(planet, longitude, sun_longitude, speed):
    """Cross-group B: a declared interpretation, separate from solar_phase.

    1.22, 1/fn 172 supplies the inclusive 6-degree allowance; 3/fn 174
    distinguishes Dykes's Mars allowance from the textual 18 degrees;
    6 gives full easternization; 7 gives the inferior direct/12 condition.
    Al-Qabisi III.8a-9 informs the opposition-inclusive upper convention.
    This is not a replacement for another sentence's own phase definition.
    """
    elongation = (sun_longitude - longitude) % 360.0
    bounds = {'Saturn': (6.0, 15.0), 'Jupiter': (6.0, 15.0),
              'Mars': (15.0, 18.0), 'Venus': (12.0, 12.0), 'Mercury': (12.0, 12.0)}
    if planet not in (*bounds, 'Sun', 'Moon'):
        raise ValueError(f'no Sahl 1.22 reading for {planet!r}')
    levels = []
    for level in (1, 2):
        threshold = bounds[planet][level - 1] if planet in bounds else None
        source = ('Sahl, On Nativities 1.22, 7' if planet in ('Venus', 'Mercury') else
                  'Sahl, On Nativities 1.22, 3; Dykes fn 174' if planet == 'Mars' and level == 1 else
                  'Sahl, On Nativities 1.22, 1; fn 172' if level == 1 else
                  'Sahl, On Nativities 1.22, 6')
        if planet == 'Sun':
            status, reason = 'not applicable', 'the Sun has no solar preference relative to himself'
        elif planet == 'Moon':
            status, reason = 'unresolved', 'lunar criterion not supplied'
            source = 'Sahl, On Nativities 1.7, 3 and 1.22; lunar criterion not supplied'
        elif elongation > 180.0:
            status, reason = 'evening side', 'beyond the adopted morning interval'
        elif elongation < threshold:
            status, reason = 'below adopted threshold', 'morning distance below the adopted allowance'
        elif planet in ('Venus', 'Mercury') and speed <= 0.0:
            status, reason = 'unresolved', '1.22, 7 does not settle a retrograde or stationary morning inferior'
        else:
            status, reason = 'yes', 'meets the adopted threshold and motion condition'
        levels.append(SahlEasternLevel(level, status, threshold, reason, source))
    return SahlEasternResult(planet, elongation, speed, *levels)


def _sahl_governor_branch(rows, which):
    """Adjudicate one complete assignment, retaining actual eliminators."""
    rows = [dict(row) for row in rows]
    eliminations = {}
    stage = {row['Planet']: ('eligible' if row['_eligible'] else 'dropped by 1.7, 4') for row in rows}
    eligible = [row for row in rows if row['_eligible']]
    unresolved = False
    if not eligible:
        governor, how = '-', 'no lord of the degree is both direct and looking at its sign (1.7, 4)'
    else:
        # 1.7, 3's preference: a candidate not qualifying as eastern is set
        # aside by an eastern one holding at least as many claims; the Sun,
        # with no side, neither sets aside nor is set aside.
        remaining = list(eligible)
        if len(remaining) > 1:
            easterns = [row for row in remaining if row['_eastern']]
            for row in list(remaining):
                if not row['_eastern'] and not row['_sun'] and any(e['_claims'] >= row['_claims'] for e in easterns):
                    stage[row['Planet']] = "set aside by 1.7, 3's preference (an eastern candidate with claims at least equal)"
                    eliminations[row['Planet']] = tuple(e['Planet'] for e in easterns if e['_claims'] >= row['_claims'])
                    remaining.remove(row)
        # 1.7, 7's clear subcase: a listed advantage beats none.
        if len(remaining) > 1 and any(row['_advantages'] for row in remaining):
            for row in list(remaining):
                if not row['_advantages']:
                    stage[row['Planet']] = 'set aside by 1.7, 7 (no listed advantage against a candidate holding one)'
                    remaining.remove(row)
        if len(remaining) == 1:
            governor = remaining[0]['Planet']
            steps = [f'direct, looking at the sign of the {which} (1.7, 4)']
            if len(eligible) > 1:
                by3 = [p for p, s in stage.items() if s.startswith("set aside by 1.7, 3")]
                if by3:
                    for displaced in by3:
                        actors = ', '.join(eliminations[displaced])
                        steps.append(f"1.7, 3's preference for the eastern {actors} set aside {displaced}")
                by7 = [p for p, s in stage.items() if s.startswith('set aside by 1.7, 7')]
                if by7:
                    steps.append(f"1.7, 7: a listed advantage against none ({', '.join(by7)} set aside)")
            how = f"{governor}: " + '; '.join(steps)
            if len(eligible) == 1:
                how += ' (the only eligible lord)'
        else:
            unresolved = True
            governor = 'unresolved between ' + ' and '.join(row['Planet'] for row in remaining)
            how = ('each direct and looking' + (', neither set aside by 1.7, 3' if len(eligible) > 1 else '')
                   + '; ' + '; '.join(f"{row['Planet']}: {row['Stake or own dignity (1.7, 7)']}" for row in remaining)
                   + f' -- {SAHL_1_7_UNMODELLED}')
            for row in remaining:
                stage[row['Planet']] = 'unresolved'
    # The model: as first built, disclosed as this app's arithmetic.
    model_pick, model_points, model_how = '-', 0, 'no eligible lord'
    if eligible:
        pool = [row for row in eligible if row['_eastern']] or eligible
        best = max(row['Model points'] for row in pool)
        top = [row for row in pool if row['Model points'] == best]
        model_pick = ' / '.join(row['Planet'] for row in top) if len(top) > 1 else top[0]['Planet']
        model_points = best
        model_how = (f"{model_pick} ({best} point{'s' if best != 1 else ''}" + (', a model tie' if len(top) > 1 else '') + ') -- '
                     + SAHL_1_7_MODEL_DISCLOSURE)
    for row in rows:
        row['Verdict'] = 'The governor' if row['Planet'] == governor else stage[row['Planet']]
        row['Model'] = 'the pick' if row['Planet'] in ([model_pick] if ' / ' not in model_pick else model_pick.split(' / ')) else ''
        for k in ('_eligible', '_eastern', '_sun', '_claims', '_advantages', '_qualification'):
            del row[k]
    return {'governor': governor, 'how': how, 'unresolved': unresolved, 'rows': rows,
            'preference_eliminations': eliminations, 'model_pick': model_pick, 'model_points': model_points, 'model_how': model_how}


def sahl_syzygy_governor(syzygy, p_data, cusps, sect):
    """Evaluate 1.7 once; preserve complete routes for consequential unknowns.

    The natal triplicity claimant is specific to this procedure and its
    approximation. The separate weighted almuten retains its convention.
    Candidate facts are shared by the verdict, model, rows and explanation.
    """
    r = syzygy['rulers']
    triplicity_lord = r['triplicity_day'] if sect == 'Diurnal' else r['triplicity_night']
    lords = [r['domicile'], r['exaltation'], triplicity_lord, r['term'], r['face']]
    claims = {}
    for kind, lord in zip(('house', 'exaltation', 'triplicity', 'bound', 'image'), lords):
        if lord and lord != '-' and lord in p_data:
            claims.setdefault(lord, []).append(kind)
    sun_lon = p_data['Sun']['longitude']
    syz_sign_idx = int((syzygy['syzygy_longitude'] % 360.0) // 30)
    which = 'meeting' if syzygy['event_type'] == 'Conjunctional' else 'opposition'
    rows = []
    for planet, kinds in claims.items():
        d = p_data[planet]
        lon = d['longitude']
        speed = d.get('speed_in_lon', 0.0)
        direct = (UnresolvedResult('exactly zero speed is neither direct nor retrograde',
                                   'Gr. Intr. VII.2, 23; this app’s exact-point convention',
                                   status='unassigned') if speed == 0.0 else speed > 0.0)
        apart = (int((lon % 360.0) // 30) - syz_sign_idx) % 12
        apart = min(apart, 12 - apart)
        looking = apart in SAHL_1_7_LOOKING_SIGN_COUNTS
        qualification = sahl_eastern_1_22(planet, lon, sun_lon, speed)
        eastern = qualification.level1.value
        division = get_effective_house(lon, cusps)
        own = get_essential_rulers(lon)
        own_trip = own['triplicity_day'] if sect == 'Diurnal' else own['triplicity_night']
        own_dignities = [name for name, holder in (('house', own['domicile']), ('exaltation', own['exaltation']),
                                                    ('triplicity', own_trip), ('bound', own['term'])) if holder == planet]
        in_stake = division in (1, 4, 7, 10)
        advantages = (['a stake'] if in_stake else []) + [f'own {n}' for n in own_dignities]
        rows.append({
            'Planet': planet,
            'Claim on the degree (1.7, 3)': ', '.join(kinds),
            'Direct (1.7, 4)': direct if is_unresolved(direct) else ('yes' if direct else 'no (retrograde)'),
            'Looking at the sign (1.7, 4)': ('yes (' + ('the same sign' if apart == 0 else ASPECT_BY_SIGN_COUNT[apart][0].lower()) + ')')
                                          if looking else f'no (in aversion, {apart} sign{"s" if apart != 1 else ""} off)',
            'Eastern (1.7, 3)': qualification.level1.display,
            'Stake or own dignity (1.7, 7)': f'division {division}' + ('; ' + ', '.join(advantages) if advantages else '; none listed'),
            'Model points': (1 if in_stake else 0) + len(own_dignities),
            '_eligible': doctrinal_and(direct, looking), '_eastern': eastern, '_sun': planet == 'Sun',
            '_qualification': qualification,
            '_claims': len(kinds), '_advantages': len(advantages),
        })

    from itertools import product

    # At a stationary inferior, the direct-course uncertainty is the same
    # condition used by 1.22, 7. Do not invent independent truth assignments
    # that admit it as direct yet refuse easternness at a qualifying distance.
    linked = {i for i, row in enumerate(rows)
              if row['Planet'] in ('Venus', 'Mercury')
              and is_unresolved(row['_eligible']) and is_unresolved(row['_eastern'])}
    variables = [(i, key) for i, row in enumerate(rows) for key in ('_eligible', '_eastern')
                 if is_unresolved(row[key]) and
                 (key == '_eligible' or (row['_eligible'] is not False and i not in linked))]
    routes = []
    for values in product((False, True), repeat=len(variables)):
        assigned = [dict(row) for row in rows]
        labels = []
        for (i, key), value in zip(variables, values):
            assigned[i][key] = value
            if i in linked and key == '_eligible':
                assigned[i]['_eastern'] = value
            condition = 'direct eligibility' if key == '_eligible' else 'eastern preference'
            labels.append(f"{rows[i]['Planet']} {condition} {'met' if value else 'not met'}")
        routes.append(('if ' + ', '.join(labels) if labels else 'established conditions',
                       _sahl_governor_branch(assigned, which)))

    def merge(values, reason):
        first = values[0][1]
        if all(value == first for _label, value in values):
            return first
        return UnresolvedResult(reason, 'Sahl, On Nativities 1.7, 3-7; adopted 1.22 reading', tuple(values))

    result = {}
    names = {'governor': 'governor', 'how': 'explanation', 'model_pick': 'approximation',
             'model_points': 'approximation’s point total', 'model_how': 'approximation’s explanation',
             'preference_eliminations': 'preference decisions'}
    for key in ('governor', 'how', 'model_pick', 'model_points', 'model_how', 'preference_eliminations'):
        result[key] = merge([(label, route[key]) for label, route in routes],
                            'the ' + names[key] + ' depends on an unassigned candidate condition')
    result['unresolved'] = is_unresolved(result['governor']) or any(route['unresolved'] for _, route in routes)
    result['rows'] = []
    for i, row in enumerate(rows):
        public = {key: value for key, value in row.items() if not key.startswith('_')}
        for key in ('Verdict', 'Model'):
            public[key] = merge([(label, route['rows'][i][key]) for label, route in routes],
                                'this candidate’s ' + key.lower() + ' depends on an unassigned condition')
        result['rows'].append(public)
    result['summary'] = merge([(label, f"{route['governor']} -- {route['how']}") for label, route in routes],
                              'the governor judgment depends on an unassigned candidate condition')
    if not result['unresolved'] and is_unresolved(result['summary']):
        summary = result['summary']
        result['summary'] = UnresolvedResult(
            'the governor is settled; the explanation depends on an unassigned condition',
            summary.source, summary.alternatives,
            display_label=f"{result['governor']} — explanation conditional")
    result['triplicity_lord'] = triplicity_lord
    result['triplicity_sect'] = 'day' if sect == 'Diurnal' else 'night'
    result['eastern_qualifications'] = {row['Planet']: row['_qualification'] for row in rows}
    result['alternative_routes'] = tuple(routes) if variables else ()
    return result


# --- Planetary Day & Hour (Chronocrats) ----------------------------------

DAY_LORD_BY_WEEKDAY = {0: 'Moon', 1: 'Mars', 2: 'Mercury', 3: 'Jupiter', 4: 'Venus', 5: 'Saturn', 6: 'Sun'}  # Python's date.weekday(): Monday=0..Sunday=6
# The Chaldean order of the planetary hours is the standing weight order,
# heaviest first -- the same list, under the name the chronocrator doctrine
# uses for it.
CHALDEAN_HOUR_ORDER = WEIGHT_ORDER

class _CircumpolarSunError(Exception):
    """Raised when swe.rise_trans reports no sunrise/sunset event exists
    for this date/location (res == -2: the Sun is circumpolar — polar day
    or polar night)."""
    pass

def _find_sun_event(jd_start, lat, lon, want_rise):
    """Wraps swe.rise_trans to find the next sunrise/sunset at or after
    jd_start. Signature confirmed directly against a real pyswisseph
    install (20230604): rise_trans(tjdut, body, rsmi, geopos, atpress=0.0,
    attemp=0.0, flags=FLG_SWIEPH) -> (res, tret), tret[0] = JD of event.
    res == -2 means the body is circumpolar (no event that day) -- this is
    a real condition for extreme-latitude locations, not just a hypothetical."""
    rsmi = swe.CALC_RISE if want_rise else swe.CALC_SET
    geopos = (lon, lat, 0.0)
    res, tret = swe.rise_trans(jd_start, swe.SUN, rsmi, geopos)
    if res != 0:
        raise _CircumpolarSunError(
            f"No {'sunrise' if want_rise else 'sunset'} event found near jd={jd_start:.4f} "
            f"at lat={lat}, lon={lon} (res={res}) — the Sun is circumpolar there on this date."
        )
    return tret[0]

def _weekday_from_jd(jd_ut, utc_offset_hours):
    """Local weekday (Monday=0, matching DAY_LORD_BY_WEEKDAY) taken from the
    Julian Day rather than from datetime.weekday().

    This matters for every chart before the Gregorian reform. The chart is
    computed with swe.JUL_CAL for pre-1582 dates, so the same digits denote
    a Julian-calendar date -- but datetime.weekday() reads those digits as
    PROLEPTIC GREGORIAN, and the two calendars had drifted ten days apart by
    1582. For 1582-10-04, a Thursday in the Julian calendar, datetime says
    Monday: a three-planet error in the Lord of the Day, and through the
    Chaldean order an error in the Lord of the Hour as well.

    The Julian Day count is continuous across the reform and knows nothing
    of either calendar, so it is the reliable source. The weekday wanted is
    the LOCAL one, so the UT day is shifted by the local offset first."""
    return int(math.floor(jd_ut + (utc_offset_hours / 24.0) + 0.5)) % 7

@st.cache_data(max_entries=32, show_spinner=False)
def calculate_chronocrats(jd_utc, lat, lon, local_hour, utc_offset_hours=0.0):
    """Planetary Day (from the astrological day, which begins at Sunrise —
    not the calendar weekday) and Planetary Hour (from the unequal/temporal
    hour system, bracketed by real sunrise/sunset times for this date and
    location). Falls back to calendar weekday + equal 2-hour divisions if
    the location has no sunrise/sunset that day (circumpolar)."""
    approximate = False
    try:
        # Bracket the birth moment with the nearest sunrise/sunset before
        # and after it. Forward-search from jd_utc for the "after" events,
        # then forward-search from just under 1.2 days before each "after"
        # event to find the immediately preceding one (a safe margin:
        # consecutive solar events are ~1.0 day apart, so 1.2 days
        # guarantees we land on exactly the previous one, not two cycles back).
        sunrise_after = _find_sun_event(jd_utc, lat, lon, True)
        sunset_after = _find_sun_event(jd_utc, lat, lon, False)
        sunrise_before = _find_sun_event(sunrise_after - 1.2, lat, lon, True)
        sunset_before = _find_sun_event(sunset_after - 1.2, lat, lon, False)

        # The astrological day strictly begins at Sunrise, not midnight. A
        # birth between midnight and sunrise still belongs to the PRECEDING
        # day's astrological date — e.g. 3:00 AM Wednesday is still ruled
        # by Tuesday's Day Lord. Step the local clock backward by the exact
        # elapsed time since the most recent sunrise to sample the correct
        # weekday.
        time_since_sunrise = jd_utc - sunrise_before
        day_lord = DAY_LORD_BY_WEEKDAY[_weekday_from_jd(jd_utc - time_since_sunrise, utc_offset_hours)]

        is_diurnal_hour = sunrise_before > sunset_before
        if is_diurnal_hour:
            period_start, period_end = sunrise_before, sunset_after
        else:
            period_start, period_end = sunset_before, sunrise_after

        temporal_hour = (period_end - period_start) / 12.0
        elapsed = jd_utc - period_start
        hour_number = int(elapsed / temporal_hour) + 1 if temporal_hour > 0 else 1
        hour_number = min(max(hour_number, 1), 12)

        # The 24 planetary hours (12 day + 12 night) are one continuous
        # cycle of 7, starting with the Day Lord at day-hour 1; night-hour
        # 1 is the 13th step in that same cycle.
        cycle_offset = (hour_number - 1) if is_diurnal_hour else (12 + hour_number - 1)
    except _CircumpolarSunError:
        # No real sunrise or sunset exists here on this date (polar day or
        # polar night). The temporal hour is DEFINED by the interval
        # between them, so it has no value at all in that case -- there is
        # no canonical technique to fall back on, and none of the sources
        # in hand contemplates the situation.
        #
        # What is returned is an explicitly modern approximation, flagged
        # as such: the civil day divided into 24 equal hours, continuing
        # the same Chaldean 7-cycle. An earlier version wrote
        # `min(hour // 2, 11)` -- twelve two-hour bins, so the cycle
        # advanced twelve steps across the day where the temporal-hour
        # branch above advances twenty-four. It did cover the whole day,
        # but at half the rate, so it was not "continuing the same 7-cycle"
        # as its comment claimed: the same civil moment landed on a
        # different lord depending on which branch had run.
        approximate = True
        day_lord = DAY_LORD_BY_WEEKDAY[_weekday_from_jd(jd_utc, utc_offset_hours)]
        cycle_offset = int(local_hour)        # 0-23, one step per civil hour

    start_index = CHALDEAN_HOUR_ORDER.index(day_lord)
    hour_lord = CHALDEAN_HOUR_ORDER[(start_index + cycle_offset) % 7]

    return {
        'Day Lord': day_lord, 'Hour Lord': hour_lord, 'Approximate': approximate,
        'Hour Basis': 'Equal civil hours (modern approximation; the Sun is '
                       'circumpolar here, so the temporal hour is undefined)'
                       if approximate else 'Temporal (unequal) hours',
    }

# --- Classical Lots (Arabic Parts) ---------------------------------------

def calculate_classical_lots(asc, sun, moon, sect):
    """The four Lots this app has always shown, each computed from its own
    LOT_DEFINITIONS row, which carries the provenance, so this table cannot
    disagree with the Topical Lots table below it. Fortune and Exaltation
    are stated in Sahl; Spirit is stated at Gr. Intr. VIII.3, 28-29 and
    named by Sahl; Basis is stated at Gr. Intr. VIII.4, 22-24 (order
    LOT-BASIS, 2026-09-12: until then it was the unsigned shorter arc
    between Fortune and Spirit, a construction of this app's own, and was
    labelled unattested)."""
    luminaries = {'Sun': {'longitude': sun}, 'Moon': {'longitude': moon}}
    classical_ids = {'Lot of Fortune': 'fortune', 'Lot of Spirit': 'spirit',
                     'Lot of Exaltation': 'exaltation', 'Lot of Basis': 'basis'}
    lots = {name: lot_by_id(lot_id, luminaries, asc, None, sect) for name, lot_id in classical_ids.items()}
    standing_by_id = {d['id']: d['confidence'] for d in LOT_DEFINITIONS}
    result = []
    for name, lon_val in lots.items():
        result.append({
            'Lot Name': name,
            'Standing': standing_by_id[classical_ids[name]],
            'Position': get_degree_string(lon_val),
            'WS place': get_wsh_house(lon_val, asc),
            'Sign Dispositor': SIGN_TO_DOMICILE.get(get_zodiac_sign(lon_val), '-'),
        })
    return result

# --- Topical Lots ---------------------------------------------------------
# Every Lot carries its own provenance. Sahl's Nativities gives many of
# these more than once, with formulas that genuinely conflict, and Dykes'
# apparatus does not silently reconcile them -- so neither does this table.
# His editorial position is recorded on each row, in his own words where he
# states one, and the rivals stay visible beside the default.
#
# Four kinds of case, all of them his:
#
#   SAHL HIMSELF RULES. Of the two sibling Lots: "and they are both
#   applied ... And both of the Lots are correct, SO WORK WITH THEM BOTH
#   TOGETHER" (Ch. 3.11, 1-4). Neither is subordinate.
#
#   DYKES NAMES HIS CHOICE. On the three witnesses to Masha'allah's Lot of
#   enemies (his Fig. 71): "I HAVE USED M HERE, which adopts the 'Hermetic'
#   Lot of necessity ... the problem is that the end of 48 equates it with
#   the Lot of slaves, and that is not true." On the night reversal of the
#   Saturn-Moon Lot: "Paul instructs us to reverse it by night, but Abu
#   Ma'shar says not to. WE SHOULD FOLLOW PAUL."
#
#   DYKES MARKS ONE STANDARD. On children: "this view belongs to
#   Theophilus, whereas THE USUAL CALCULATION (from Jupiter to Saturn by
#   day, and reversed by night) is that of Hermes."
#
#   DYKES ONLY TABULATES. Three Lots for work (his Fig. 63 in Sahl's On
#   Nativities -- not Abu Ma'shar's Fig. 63, the V.22 degrees), after: "Sahl
#   quietly switches to Masha'allah's treatise on Lots ... and now he is
#   substituting another one WITHOUT TELLING US that the formula is
#   different! So we now have three different Lots, ostensibly for the same
#   topic." All three are shown.
#
# NOTE ON THE SOURCE TEXT. Every formula here is taken from the running
# prose or a footnote, never from one of the summary tables. An earlier
# OCR mangled their glyph columns; the current one (read 2026-09-08) has
# Sahl's Fig. 63 (On Nativities, Lots of action or work) reading
# Mercury->Mars, Saturn->Moon and Sun->Saturn, all marked
# (R), so the table agrees with the prose on the bodies and differs only
# in marking the Saturn-Moon Lot reversed where 10.2.5, 1 says "by day
# and night". Where prose and table disagree, the prose is used and the
# disagreement is noted.
#
# A point may be a planet, 'Ascendant', 'cuspN' (the Nth place -- see
# LOT_HOUSE_CUSP), 'lordN' (the domicile lord of the sign of that construction's cusp),
# or another Lot by id. Lots that feed other Lots are listed before them.
#
# "The second place", "the ninth" (2.15, 1; Ch. 9, 9): Sahl specifies
# no degree construction explicitly; Dykes' note 207 on 4.14 glosses assets as
# "from the lord of the second to the second". In whole signs the degree of
# the Nth place is 0° of the Nth sign, the app's fallback convention and its default; the
# calculated cusp is prescribed where a source says "by equation" (the Lot of death). Sidebar switch for the rows whose text
# names no construction. ONE ROW DECLARES ITS OWN: the Lot of death's eighth
# is "by equation" (Gr. Intr. VIII.4, 226; VIII.6, 69; VIII.3, 14-15), the
# calculated cusp -- Sahl's 8.6, 1 "the degree of the eighth place" names no
# construction, and an earlier line here cited it for "whole-sign
# throughout", which it does not say (owner, 2026-09-11; see the row).
LOT_HOUSE_CUSP_OPTIONS = ('whole-sign place', 'quadrant cusp')   # the sidebar radio and _lot_point share these
LOT_HOUSE_CUSP = LOT_HOUSE_CUSP_OPTIONS[0]
LOT_DEFINITIONS = [
    dict(id='fortune', topic='Fortune', name='Lot of Fortune',
         start='Sun', end='Moon', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 1.37, 1 (and throughout)',
         confidence='settled',
         note='"The Ascendant of the Moon" (1.37, 1); the Sun-to-Moon formula, reversed at '
              "night, is Dykes's note 494 on 1.37, 3 -- Sahl's own sentence 2 gives an "
              'hour-based construction instead.'),
    dict(id='spirit', topic='Spirit', name='Lot of Spirit',
         start='Moon', end='Sun', project='Ascendant', reverse_at_night=True,
         source='Gr. Intr. VIII.3, 28-29 (the Lot of the Invisible); named in Sahl, On Nativities Ch. 11.2, 4-6',
         confidence='stated (Gr. Intr. VIII.3, 28-29); named in Sahl',
         note='"They began [2] the Lot of the Invisible by day from her up to the Sun in degrees of equality, '
              'and by night from the Sun to the Moon ... cast out from the beginning of the sign of the Ascendant" '
              '(Gr. Intr. VIII.3, 28-29). Sahl names it the Lot of the Invisible, later Spirituality (notes on '
              'Ch. 9.5, 73 and On Nativities Ch. 11, 5 -- the chapter preamble -- confirm the identity) but nowhere states the formula.'),
    # [9] of the Great Introduction, the Greek Basis (fn 67): Fortune to the
    # Invisible from the Ascendant, reversed at night -- the same construction
    # as Sahl's Lot of passion (7.1, 141) and Abu Ma'shar's Lot of Venus [6],
    # with which VIII.4, 24 and VIII.7, 5 say it coincides. Order LOT-BASIS,
    # 2026-09-12: until then this app computed Basis as the UNSIGNED shorter
    # arc between Fortune and Spirit from the Ascendant, a construction stated
    # in no text, and called the Lot unattested.
    dict(id='basis', topic='Basis', name='Lot of Basis',
         start='fortune', end='spirit', project='Ascendant', reverse_at_night=True,
         source="Gr. Intr. VIII.4, 22-24 ([9] the Lot of firmness and survival, \"the Lot of the Ascendant's support\"; fn 67: "
                "the Greek Lot of Basis); VIII.7, 5; the name is ITA VI.1.4's heading, \"The Lot of Basis\"",
         confidence='stated (Gr. Intr. VIII.4, 22-24; VIII.7, 5); not in Sahl under this name',
         note='"Is taken by day from the Lot of Fortune to the Lot of the Invisible, and by night the contrary, and it is '
              'added to what the degrees of the Ascendant come to, and it is cast out from the beginning of the sign of the '
              'Ascendant. 24 And this Lot matches [6] the Lot of Venus" (VIII.4, 23-24; VIII.7, 5 the same). The same '
              'construction as the Lot of passion (Eros) below, Sahl 7.1, 141, with which it coincides.'),
    # The night formula is not the day formula reversed: it changes BOTH
    # ends. "By day from the degree of the Sun to the degree of HIS
    # exaltation ... and by night from the degree of the MOON to the degree
    # of HER exaltation" (4.1, 6). The generic reversal produced
    # Asc + (Sun - 33) at night, using the wrong body and the wrong
    # direction, and disagreed with the Classical Lots table two rows above
    # it on every nocturnal chart. reverse_at_night is False because
    # _lot_point already swaps the luminary by sect.
    dict(id='exaltation', topic='Exaltation', name='Lot of Exaltation',
         start='sect_light', end='exaltation_degree', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 4.1, 6 (Theophilus)',
         confidence='settled',
         note='"By day from the degree of the Sun to the degree of his exaltation ... '
              'by night from the degree of the Moon to the degree of her exaltation."'),
    dict(id='assets_lord2', topic='Assets', name='Lot of assets (lord of the 2nd)',
         start='lord2', end='cusp2', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 2.15, 1',
         confidence='attested',
         note='"Count from the lord of the second to the second place, and add on top of '
              'that the degrees of the Ascendant."'),
    dict(id='assets_jupsat', topic='Assets', name='Lot of assets (Jupiter-Saturn)',
         start='Jupiter', end='Saturn', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 2.15, 17',
         confidence='variant',
         note='Sahl gives a second, unrelated formula for the same topic in the same '
              'chapter: "count from Jupiter to Saturn by day, and by night the reverse."'),
    dict(id='siblings_hermes', topic='Siblings', name='Lot of siblings (Hermes)',
         start='Saturn', end='Jupiter', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 3.11, 2',
         confidence='settled, used alongside the other',
         note='"For one who was born by day and night" -- no reversal. Sahl: "both of the '
              'Lots are correct, so work with them both together" (3.11, 4).'),
    dict(id='siblings_valens', topic='Siblings', name='Lot of siblings (Valens)',
         start='Mercury', end='Jupiter', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 3.11, 3',
         confidence='settled, used alongside the other',
         note='"Taken by night and day." Dykes\' note 121 on Ch. 3.11, 3 adds that in Dorotheus '
              'this one is specifically for the number of siblings.'),
    dict(id='father', topic='Father', name='Lot of the father',
         start='Sun', end='Saturn', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 4.14, 1',
         confidence='settled',
         note='"By day from the Sun to Saturn and by night from Saturn to the Sun."'),
    dict(id='father_burnt', topic='Father', name='Lot of the father (Saturn under the rays)',
         start='Mars', end='Jupiter', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 4.14, 2',
         confidence='conditional -- see the Status column',
         note='"Now if Saturn was under the rays, then count from Mars to Jupiter, and project it from the Ascendant." This '
              'replaces the ordinary father Lot only while Saturn is actually under the '
              "rays; the row reports whether that condition holds in this chart. Sahl leaves the night order unstated; "
              "this app retains Mars to Jupiter at both times. Abu Ma'shar, Gr. Intr. VIII.4, 74 reverses it "
              "by night; Dykes's fn 92 conjectures a reversal and Jupiter to Mars by day. Those variants "
              "are not applied here."),
    # Abu Ma'shar's own preference for the same case, shown only under
    # "Course text and supplement" (owner, 2026-09-13): VIII.4, 74 gives the
    # Mars-Jupiter form to "some of the people" (fn 92: Dorotheus), and 75
    # prefers Hermes' -- Saturn's indication gone, the Sun's stands, so the
    # Lot is taken from the Sun to Jupiter by day and the contrary by night.
    dict(id='father_burnt_abu', topic='Father', name="Lot of the father (Saturn under the rays), Hermes' form preferred by Abu Ma'shar",
         start='Sun', end='Jupiter', project='Ascendant', reverse_at_night=True, supplement=True,
         source="Abu Ma'shar, Gr. Intr. VIII.4, 75",
         confidence='conditional -- see the Status column; the supplement, beside Sahl\'s row; Dykes\'s fn 93 '
                    'objects: "The obvious flaw in this logic is that being under the rays should nullify '
                    'Jupiter\'s indication as well."',
         note='"But what Hermes said is more correct, because Jupiter is more indicative for fathers than '
              'Mars is; moreover, if Saturn\'s indication was nullified by his being under the Sun\'s rays, '
              'the indication of the Sun would still stand, so if Saturn was under the rays it would be '
              'necessary for it to be taken by day from the Sun to Jupiter (and by night the contrary), and '
              'that be cast out from the Ascendant, just as Hermes said." Compared under '
              'Sahl\'s condition profile; an applicable comparison, never selected here.'),
    dict(id='mother', topic='Mother', name='Lot of the mother',
         start='Venus', end='Moon', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 4.14 (Dykes's note 198)",
         confidence='attested',
         note='"Taken by day from Venus to the Moon (and by night the contrary), and is '
              'projected from the Ascendant."'),
    dict(id='children_theophilus', topic='Children', name='Lot of children (Jupiter-Saturn, unreversed)',
         start='Jupiter', end='Saturn', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 5.1, 91',
         confidence='variant',
         note='Sahl gives it "by night and by day". The note: "According to Abu Ma\'shar, '
              'this view belongs to Theophilus."'),
    dict(id='children_hermes', topic='Children', name='Lot of children (Jupiter-Saturn, reversed)',
         start='Jupiter', end='Saturn', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 5.1, 91 (Dykes's note 51)",
         confidence='the usual calculation',
         note='"the usual calculation (from Jupiter to Saturn by day, and reversed by '
              'night) is that of Hermes." The only difference from the row above is the '
              'night reversal, which Sahl\'s text omits.'),
    dict(id='children_mercury', topic='Children', name='Lot of children (Mercury-Saturn)',
         start='Mercury', end='Saturn', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 5.1, 92',
         confidence='variant, attribution disputed',
         note='"And according to the method of Hermes, it is taken from Mercury to '
              'Saturn." Sahl assigns this to Hermes; the note on 91 assigns Hermes the '
              'Jupiter-Saturn form instead. The conflict is in the sources.'),
    dict(id='children_timing', topic='Children', name='Lot of the timing of children',
         start='Mars', end='Jupiter', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 5.3, 2 and 8',
         confidence='attested',
         note='A separate Lot for when, not how many: "when Jupiter reaches this Lot in '
              'his course and transit."'),
    dict(id='marriage_men', topic='Marriage', name="Lot of men's marriage",
         start='Saturn', end='Venus', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 7.1, 223 and Ch. 7.2, 44',
         confidence='settled', note='Stated twice, identically.'),
    dict(id='marriage_women', topic='Marriage', name="Lot of women's marriage",
         start='Venus', end='Saturn', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 7.1, 224 and Ch. 7.2, 44',
         confidence='settled', note='Stated twice, identically.'),
    dict(id='passion', topic='Marriage', name='Lot of passion (Eros)',
         start='fortune', end='spirit', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 7.1, 141 (Dykes's note 11)",
         confidence='attested',
         note='"The Lot of Eros or love according to Valens, taken by day from Fortune to '
              'Spirit (and reversed at night)." Sahl\'s text says Fortune to the Lot of '
              'the Invisible, which is the Lot of Spirit.'),
    dict(id='chronic_illness', topic='Health', name='Lot of chronic illness',
         start='Saturn', end='Mars', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 6.3.4, 2 and Ch. 6.3.5, 1',
         confidence='settled',
         note='"Taken from Saturn to Mars by day, and by night the contrary." Stated twice.'),
    dict(id='slaves', topic='Slaves', name='Lot of slaves',
         start='Mercury', end='Moon', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 6.10, 20',
         confidence='settled',
         note='"Taken from Mercury to the Moon by day, and by night the reverse."'),
    dict(id='death', topic='Death', name='Lot of death',
         start='Moon', end='cusp8', project='Saturn', reverse_at_night=False,
         # This Lot's eighth degree is "by equation" (Gr. Intr. VIII.4, 226; VIII.6,
         # 69): the calculated cusp, whatever the shared LOT_HOUSE_CUSP switch says
         # for the Lots whose texts name no construction. Owner, 2026-09-11 (decision
         # sheet row 4 / FINAL-A12); the whole-sign 0° comparison is the row below.
         cusp_rule='quadrant cusp',
         source="Gr. Intr. VIII.4, 226 with fn 128; VIII.6, 69; VIII.3, 11 and 14-15 (\"by equation\"); "
                "Sahl, On Nativities 8.6, 1 as printed, Saturn being Dykes's reading for the MSS' \"Ascendant\" (fn 89)",
         confidence="stated (Gr. Intr. VIII.4, 226; VIII.6, 69); Sahl 8.6, 1 as printed agrees, its manuscripts read "
                    "the Ascendant (fn 89); the eighth's degree \"by equation\" is the Alchabitius cusp -- a declared "
                    "convention, no text naming the algorithm",
         note='"The Lot of death is taken by day and night from the degree of the Moon to the degree of the '
              'eighth house by equation, and to it is added what Saturn has traveled in his sign, and it is '
              'cast out from the beginning of Saturn\'s sign" (Gr. Intr. VIII.4, 226, Hermes\'s Lot, which 228 '
              'calls "more correct" than the Persians\'; VIII.6, 69 the same) -- projection from Saturn\'s degree, '
              'as VIII.3, 11 and fn 36 spell out. Sahl 8.6, 1 as printed: "taken by night and day from the Moon '
              'to the degree of the eighth place, and cast out from Saturn"; that "Saturn" is Dykes\'s emendation '
              'of Sahl\'s manuscripts -- fn 89: "Reading with the Masha\'allah MSS for \'Ascendant\'. This is the '
              'Lot as reported by Dorotheus (Carmen IV.3, 16)" -- so the emendation is a fact about Sahl\'s '
              'transmission, not about the rule. "By equation" is Abu Ma\'shar\'s own word for the cusp (VIII.3, '
              '14: "by counting is one of the signs, and by equation ... another house"; 15\'s example cannot be '
              'produced by a carried Ascendant degree); Sahl\'s "the degree of the eighth place" names no '
              'construction. The Saturn and Ascendant projections differ in sign on about 93% of charts; the '
              'whole-sign comparison uses 0° of the eighth sign, not a carried Ascendant degree.'),
    dict(id='death_ws', topic='Death', name='Lot of death (variant: whole-sign eighth)',
         start='Moon', end='cusp8', project='Saturn', reverse_at_night=False,
         cusp_rule='whole-sign place',
         source="The app's comparison convention: 0° of the eighth sign from the rising sign",
         confidence="not prescribed in any supplied passage -- Sahl 8.6, 1 leaves the construction unspecified, "
                    "Gr. Intr. VIII.4, 226 prescribes the cusp; shown for comparison",
         note='Same arc and projection as the row above; only the eighth\'s degree differs. Kept as a labelled '
              'variant so the whole-sign reading stays visible beside it.'),
    dict(id='killer', topic='Death', name='Lot of the killer',
         start='lord1', end='Moon', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 8.2, 17',
         confidence='attested',
         note='"Taken from the lord of the Ascendant to the Moon by day (and by night the '
              'reverse), and is cast out from the Ascendant."'),
    dict(id='travel', topic='Travel', name='Lot of travel',
         start='lord9', end='cusp9', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 9, 9 (the chapter preamble; restated at 9.3, 4 and 9.4, 37)',
         confidence='settled',
         note='"Taken by night and day from the lord of the ninth to the ninth."'),
    dict(id='work_action', topic='Work', name='Lot of work (action / praxis)',
         start='Mercury', end='Mars', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 10.1.1, 14',
         confidence='variant (one of three)',
         note="The Greek Lot of action. Dykes's Fig. 63 in Sahl's On Nativities names it Work in Sahl and BA, "
              "\"managers, viziers, and Sultans\" in Gr. Intr. VIII.4."),
    dict(id='work_expedition', topic='Work', name='Lot of work (expedition)',
         start='Saturn', end='Moon', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 10.2.5, 1',
         confidence='variant (one of three); Sahl\'s text, unreversed',
         note='Sahl: "Calculate by day and night from Saturn to the Moon" -- no reversal, and '
              'Abu Ma\'shar agrees. Dykes\' note prefers Paul ("Paul instructs us to reverse it '
              'by night, but Abu Ma\'shar says not to. We should follow Paul"); the row below '
              'gives that reading, so both are visible.'),
    dict(id='work_expedition_paul', topic='Work', name='Lot of work (expedition, reversed by night -- Paul)',
         start='Saturn', end='Moon', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 10.2.5, 1 (Dykes's note 165)",
         confidence="Dykes's preference against Sahl's text",
         note='Paul, chapter 22, as reported by Dykes in Sahl, On Nativities 10.2.5, note 165: '
              '"Paul instructs us to reverse it by night, but Abū Ma\'shar says not to." '
              'This is Dykes\'s report, not a direct quotation of Paul. A comparison, not the PN IV authority Lot.' ),
    dict(id='work_authority', topic='Work', name="Lot of authority, work and craft (Masha'allah)",
         start='Sun', end='Saturn', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 10.2.5, 4-14 (Masha'allah); Dykes's fn 166",
         confidence='variant (one of three); reversed at night (Gr. Intr. VIII.4, 288; VIII.6, 86)',
         note='Sahl switches treatises mid-chapter without saying so, and his text at 10.2.5, 4-14 '
              'gives no formula: the Sun-Saturn identification is Dykes\' fn 166, Masha\'allah '
              '"defines this in the same way as the Lot of fathers (Sun-Saturn)". The father Lot '
              'reverses at night (4.14, 1) and Sahl\'s Fig. 63 (On Nativities) marks this one (R), '
              'so it reverses here too; Gr. Intr. VIII.4, 288 and VIII.6, 86 state the night reversal.'),
    dict(id='friends', topic='Friends', name='Lot of friends',
         start='Moon', end='Mercury', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 11, 5 (the chapter preamble) and Ch. 11.1, 29',
         confidence="reversed at night after al-Andarzaghar (al-Qabisi V.14a, in ITA VI.2.45), Masha'allah (Book of "
                    "Aristotle III.12.1) and Dykes's note 72; Abu Ma'shar unreversed (Abbr. VI.53 and the Latin Gr. "
                    "Intr. VIII.4, in ITA VI.2.45: from the Moon to Mercury by day and by night)",
         note='Dykes\' note 72 on Ch. 9.5, 3 identifies it: \"the Moon-Mercury Lot (projected from '
              'the Ascendant, reversed by night), which is identical to Dorotheus\'s Lot '
              'of friendship." In the Ch. 11 preamble the Moon-to-Mercury words are Dykes\' '
              'bracketed supplement; Sahl states the formula in his own words at 11.1, 29.'),
    dict(id='desire', topic='Friends', name='Lot of desire',
         start='fortune', end='spirit', project='Ascendant', reverse_at_night=True,
         source='Sahl, On Nativities Ch. 11.2, 4-5',
         confidence='attested; identical in form to the Lot of passion',
         note='"By day from the Lot of Fortune to the Lot of Spirituality ... and by night '
              'the converse."'),
    dict(id='necessity', topic='Friends', name='Lot of necessity',
         start='spirit', end='fortune', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 11.4, 18 (Dykes's note 62)",
         confidence='attested',
         note='"This is the opposite of the Lot of Eros: by day from the Lot of Spirit to '
              'the Lot of Fortune (and by night the reverse)."'),
    dict(id='enemies_necessity', topic='Enemies', name='Lot of enemies (M: Mercury to Fortune)',
         start='Mercury', end='fortune', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 12.1, 48 (Dykes's Fig. 71)",
         confidence="Dykes's own choice of the three",
         note='"I have used M here, which adopts the \'Hermetic\' Lot of necessity (from '
              'Mercury to Fortune or \'the Lot of the Moon\', reversed by night). This Lot '
              'does have to do with enmity, but the problem is that the end of 48 equates '
              'it with the Lot of slaves, and that is not true."'),
    dict(id='enemies_slaves', topic='Enemies', name='Lot of enemies (E: Mercury to Moon)',
         start='Mercury', end='Moon', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 12.1, 49 (Dykes's Fig. 71)",
         confidence='variant; really the Lot of slaves',
         note="The second manuscript's reading. Dykes's table names its real identity as the "
              'Lot of slaves, which is why he did not adopt it.'),
    dict(id='enemies_hermes', topic='Enemies', name='Lot of enemies (Latin: lord of the 12th)',
         start='lord12', end='cusp12', project='Ascendant', reverse_at_night=False,
         source="Vat. Pal. lat. 1892, f. 103r (Dykes's Fig. 71)",
         confidence='variant; the Latin witness',
         note="Dykes's table names this one Enemies (Hermes) -- the only one of the three "
              'whose real identity is the topic it is used for. Cusp construction applies the rule of '
              'Gr. Intr. VIII.3, 14–15; this is an application, not verification of the absent Latin witness.'),
    dict(id='courage', topic='Courage', name='Lot of courage (Hermetic)',
         start='Mars', end='fortune', project='Ascendant', reverse_at_night=True,
         source="Sahl, On Nativities Ch. 10.3 (Dykes's note 214)",
         confidence='conjectural identification',
         note='"There seem to be two Lots used here ... for either of these the author '
              'might mean the Hermetic Lot of courage: by day from Mars to the Lot of '
              'Fortune (by night the reverse). ... However, I suspect that one of them -- '
              'probably the Lot of valor -- is originally Dorotheus\'s Lot of expedition."'),
    # Abu Ma'shar's Lots of Jupiter and Saturn, the two of his seven planetary
    # Lots (VIII.3, 17-51; the list at VIII.6, 7-14) this file did not carry:
    # Fortune, the Invisible (Spirit), Venus (desire), Mercury (necessity) and
    # Mars (courage) are above. Supplement rows, like the father Lot's VIII.4,
    # 75 form: Sahl's Nativities has neither (coverage list, 2026-09-14).
    dict(id='jupiter_prosperity', topic='Fortune', name="Lot of Jupiter (prosperity, aid, victory), Abu Ma'shar",
         start='spirit', end='Jupiter', project='Ascendant', reverse_at_night=True, supplement=True,
         source="Abu Ma'shar, Gr. Intr. VIII.3, 40-41; VIII.6, 13; ITA VI.1.7's heading names it \"The Lot of Victory or Jupiter\"",
         confidence='attested; the supplement (not in Sahl)',
         note='"The Lot of Jupiter (comprising prosperity and aid) is taken by day from the Lot of the '
              'Invisible (indicative of good fortune) to Jupiter (indicative of prosperity and aid), and '
              'by night the contrary, and to it is added the degrees of the Ascendant, and it is cast out '
              'from the Ascendant" (VIII.3, 40). "Its indication is for might, victory, aid, prosperity, '
              'generosity, praised outcomes, uprightness, and seeking religion ... hope, and everything a '
              'man obtains pertaining to good deeds" (41).'),
    dict(id='saturn_burdensome', topic='Fortune', name='Lot of Saturn (the burdensome), Abu Ma\'shar',
         start='Saturn', end='fortune', project='Ascendant', reverse_at_night=True, supplement=True,
         source="Abu Ma'shar, Gr. Intr. VIII.3, 37-38; VIII.6, 14; ITA VI.1.8's heading names it \"The Lot of Nemesis or Saturn\"",
         confidence='attested; the supplement (not in Sahl)',
         note='"The Lot of Saturn (\'the burdensome\') is taken by day from the degree of Saturn to the '
              'degree of the Lot of Fortune, and by night the contrary, and to it is added the degrees of '
              'the Ascendant, and it is projected from the beginning of the Ascendant" (VIII.3, 37). "Its '
              'indication is for preservation, depth of thought, religion, piety ... everything which has '
              'gone astray, was stolen, ran away ... the condition of the dead, and how the death will be '
              '... old age, and burdens, and every thing which is in fetters or prison, and its rescue" '
              '(38); VIII.6, 14 names it "the Lot of fetters and prison, and whether one would be rescued '
              'from it or not". The same construction as the Hermetic Lot of courage with Saturn for Mars.'),
    dict(id='deception_men', topic='Deception', name="Lot of men's deception",
         start='Sun', end='Venus', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 7.1, 220',
         confidence='attested', note='"By day and by night from the Sun to Venus."'),
    dict(id='deception_women', topic='Deception', name="Lot of women's deception",
         start='Moon', end='Mars', project='Ascendant', reverse_at_night=False,
         source='Sahl, On Nativities Ch. 7.1, 221',
         confidence='attested', note='"By day and by night from the Moon to Mars."'),
]

LOT_WHOLE_SIGN_HELP = "Whole-sign places — 0° convention. Use 0°00′ of the Nth sign from the rising sign. This is an app convention: the cited Sahl passages do not specify a whole-sign degree. Abū Maʿshar's quoted assets, travel, and death formulas use calculated cusps."
LOT_CUSP_HELP = "Measure from the ruler of the sign containing the calculated house cusp to that cusp. When its sign differs from the counted house sign, the starting ruler changes too. This follows Abū Maʿshar, VIII.3, 14–15; its use for Sahl's formulas is an interpretation of his unspecified house/place."
LOT_CONSTRUCTION_STANDING = ("Where a source prescribes the construction it is used whatever this control says: the Lot of death measures to the "
                             "eighth's calculated cusp (Gr. Intr. VIII.4, 226, \"by equation\"). Where the source is silent — Sahl's house "
                             "and place formulas — this control is the app's fallback convention, whole-sign places by default.")


def _lot_house_endpoint(house, asc, cusps, cusp_rule=None):
    rule = reading('LOT_HOUSE_CUSP') if cusp_rule is None else cusp_rule
    if rule == LOT_HOUSE_CUSP_OPTIONS[1]:
        return cusps[house - 1] if cusps is not None and len(cusps) >= house else None
    return (math.floor(asc / 30.0) * 30.0 + 30.0 * (house - 1)) % 360.0


def _lot_formula_point(name, asc, cusps, cusp_rule=None):
    if name.startswith('lord'):
        house = int(name[4:])
        sign = get_zodiac_sign(_lot_house_endpoint(house, asc, cusps, cusp_rule))
        return f"{SIGN_TO_DOMICILE[sign]} ({sign}'s ruler)"
    if name.startswith('cusp'):
        house = int(name[4:])
        return f"house {house} endpoint ({get_degree_string(_lot_house_endpoint(house, asc, cusps, cusp_rule))})"
    return name


def lot_visible(definition, supplement=False):
    """Shared reading-depth policy; formula availability is unaffected."""
    return supplement or not definition.get('supplement', definition.get('Supplement', False))


LOT_COMPARISON_IDS = frozenset(('death_ws', 'work_expedition_paul'))


def lot_operative_id(definition, father):
    """Stable formula selection, independent of the wording of Status."""
    identity = definition['id']
    if identity in ('father', 'father_burnt', 'father_burnt_abu'):
        return not isinstance(father, UnresolvedResult) and identity == father['formula_id']
    return identity not in LOT_COMPARISON_IDS


def _lot_point(name, planetary_data, asc, cusps, sect, resolved, cusp_rule=None):
    """Resolve one end of a Lot formula to a longitude, or None if the
    chart cannot supply it."""
    if name == 'Ascendant':
        return asc
    if name in planetary_data:
        return planetary_data[name]['longitude']
    if name in resolved:
        return resolved[name]
    if name == 'sect_light':
        return planetary_data['Sun' if sect == 'Diurnal' else 'Moon']['longitude']
    if name == 'exaltation_degree':
        # 4.1, 6 pairs the sect light with ITS OWN exaltation degree: the
        # Sun's is 19 Aries, the Moon's 3 Taurus.
        return 19.0 if sect == 'Diurnal' else 33.0
    if name.startswith('cusp'):
        return _lot_house_endpoint(int(name[4:]), asc, cusps, cusp_rule)
    if name.startswith('lord'):
        endpoint = _lot_house_endpoint(int(name[4:]), asc, cusps, cusp_rule)
        if endpoint is None:
            return None
        lord = SIGN_TO_DOMICILE.get(get_zodiac_sign(endpoint))
        return planetary_data[lord]['longitude'] if lord in planetary_data else None
    return None

def _lot_longitude(d, planetary_data, asc, cusps, sect, resolved):
    """One LOT_DEFINITIONS row at this chart: (longitude, start, end), or
    None if the chart cannot supply a point. The day formula reverses to
    end -> start at night where the row says so."""
    start, end = d['start'], d['end']
    if d['reverse_at_night'] and sect != 'Diurnal':
        start, end = end, start
    rule = d.get('cusp_rule')
    a = _lot_point(start, planetary_data, asc, cusps, sect, resolved, rule)
    b = _lot_point(end, planetary_data, asc, cusps, sect, resolved, rule)
    p = _lot_point(d['project'], planetary_data, asc, cusps, sect, resolved, rule)
    if a is None or b is None or p is None:
        return None
    return (p + b - a) % 360.0, start, end

FATHER_SUBSTITUTION_TEXT = "Now if Saturn was under the rays, then count from Mars to Jupiter, and project it from the Ascendant."
FATHER_NIGHT_POLICY = "night reversal unstated in Sahl; stated order retained"
FATHER_CONDITION_READING = ("Sahl's heart through 1° excluded; Saturn's rays to 15°, "
                            "outside at 15° on the morning side and inside on the evening side")


def father_lot_selection(planetary_data, asc, cusps, sect):
    """One operative Lot under Sahl 4.14, 1-2, with its natal provenance.

    Formula rows remain independent reference calculations. This selection
    is reused by the reading and by 4.20, 31-36, never reselected in transit.
    """
    def unavailable(reason):
        return UnresolvedResult(reason=reason, source='Sahl, On Nativities 4.14, 1-2', status='unavailable')
    if asc is None or any(p not in planetary_data or planetary_data[p].get('longitude') is None for p in ('Sun', 'Saturn')):
        return unavailable("the father's Lot needs the Ascendant, Sun and Saturn")
    sun, sat = planetary_data['Sun']['longitude'], planetary_data['Saturn']['longitude']
    phase, side, distance = solar_phase('Saturn', sat, sun, planetary_data['Saturn'].get('speed_in_lon'), source='Sahl')
    substitute = phase in ('Burned', 'Under the rays')
    formula_id = 'father_burnt' if substitute else 'father'
    definition = next(d for d in LOT_DEFINITIONS if d['id'] == formula_id)
    calculated = _lot_longitude(definition, planetary_data, asc, cusps, sect, {})
    if calculated is None:
        return unavailable("Saturn meets the substitution condition, but Mars or Jupiter is unavailable")
    degree, start, end = calculated
    return {'longitude': degree, 'formula_id': formula_id, 'substituted': substitute,
            'formula': f"Ascendant + ({end} - {start})",
            'order_note': FATHER_NIGHT_POLICY if substitute else f"{'day' if sect == 'Diurnal' else 'night'} order",
            'source': definition['source'], 'condition_profile': FATHER_CONDITION_READING,
            'condition': ("Saturn is under the rays; the ordinary Sun–Saturn formula is replaced" if substitute else
                          "Saturn is in the heart; the substitution does not apply" if phase == 'Cazimi' else
                          "Saturn is outside the substitution's ray interval"),
            'heart': phase == 'Cazimi', 'distance': distance, 'side': side}


def operative_lot_definitions():
    """The father's comparison formulas are not additional operative Lots."""
    return (d for d in LOT_DEFINITIONS if d['id'] not in ('father_burnt', 'father_burnt_abu'))


def lot_by_id(lot_id, planetary_data, asc, cusps, sect):
    """A single Lot by its LOT_DEFINITIONS id, resolving the rows it feeds
    on first. The one place the chart-level Lot of Fortune and the
    Classical Lots table get their arithmetic -- the formula used to be
    written out three times, and the two Lots tables once disagreed on
    every night chart."""
    if lot_id == 'father':
        selection = father_lot_selection(planetary_data, asc, cusps, sect)
        return None if isinstance(selection, UnresolvedResult) else selection['longitude']
    resolved = {}
    for d in LOT_DEFINITIONS:
        got = _lot_longitude(d, planetary_data, asc, cusps, sect, resolved)
        if got is not None:
            resolved[d['id']] = got[0]
        if d['id'] == 'father':
            selection = father_lot_selection(planetary_data, asc, cusps, sect)
            resolved['father'] = None if isinstance(selection, UnresolvedResult) else selection['longitude']
        if d['id'] == lot_id:
            return resolved.get(lot_id)
    raise KeyError(lot_id)

def calculate_topical_lots(planetary_data, asc, cusps, sect):
    """Every Lot in LOT_DEFINITIONS, computed with its provenance attached.

    A Lot is projected_point + (end - start), and the day formula reverses
    to end -> start at night where the source says so. Lots that feed other
    Lots (Fortune and Spirit feed passion, desire, necessity, courage and
    one of the enemy variants) are resolved in table order."""
    is_diurnal = (sect == 'Diurnal')
    resolved = {}
    rows = []
    father = father_lot_selection(planetary_data, asc, cusps, sect)
    for d in LOT_DEFINITIONS:
        got = _lot_longitude(d, planetary_data, asc, cusps, sect, resolved)
        if got is None:
            continue
        lon, start, end = got
        resolved[d['id']] = lon
        arc = 'day' if is_diurnal else 'night'
        status = 'Comparison — not operative' if d['id'] in LOT_COMPARISON_IDS else 'Available'
        if d['topic'] == 'Father':
            if isinstance(father, UnresolvedResult):
                status = father
            elif d['id'] == 'father':
                status = 'Replaced — reference calculation' if father['substituted'] else 'Selected — read and directed'
            elif father['substituted']:
                status = 'Selected — read and directed' if d['id'] == 'father_burnt' else 'Applicable alternative — not selected'
            else:
                status = ('Not applicable — heart exception under the selected reading' if father['heart'] else
                          'Not applicable' if d['id'] == 'father_burnt' else 'Not applicable under this condition profile')
        # Dependencies see the operative degree; this row retains its raw reference arithmetic.
        if d['id'] == 'father':
            resolved['father'] = None if isinstance(father, UnresolvedResult) else father['longitude']
        rows.append({
            'Topic': d['topic'],
            'Lot': d['name'],
            'Status': status,
            'Id': d['id'],
            'Operative': lot_operative_id(d, father),
            'Longitude': lon,
            'Position': get_degree_string(lon),
            'WS place': get_wsh_house(lon, asc),
            'Lord': SIGN_TO_DOMICILE.get(get_zodiac_sign(lon), '-'),
            'Formula': (f"{_lot_formula_point(d['project'], asc, cusps, d.get('cusp_rule'))} + ({_lot_formula_point(end, asc, cusps, d.get('cusp_rule'))} - {_lot_formula_point(start, asc, cusps, d.get('cusp_rule'))})"
                        + (f'; {FATHER_NIGHT_POLICY}' if d['id'] == 'father_burnt' else '')
                        + ('' if not d['reverse_at_night'] else f'  [{arc} order]')
                        + ((f"  [{d['cusp_rule']}, this Lot's own rule]" if d.get('cusp_rule') else f'  [{reading("LOT_HOUSE_CUSP")}]')
                           if 'cusp' in (start, end, d['project']) or start.startswith('cusp') or end.startswith('cusp') else '')),
            'Standing': d['confidence'],
            'Source': d['source'],
            'Editor’s note': d['note'],
            'Supplement': bool(d.get('supplement')),
        })
    return rows

def operative_lot_rows(planetary_data, asc, cusps, sect, supplement=False):
    """The image and its wheel share the Lots page's selection and visibility."""
    return [r for r in calculate_topical_lots(planetary_data, asc, cusps, sect)
            if r['Operative'] and lot_visible(r, supplement)]


# --- Coverage: what these sources contain that this file does not ---------
# Named explicitly rather than left as silence, so the absence is a stated
# scope limit and not an implied claim of completeness. Each entry is a
# doctrine present in the corpus and not implemented here.
NOT_IMPLEMENTED_COVERAGE = [
    ("Gr. Intr. VII.5, 29-31", "Priority among several planets connecting from a "
     "single degree and minute, decided by claims in the handing-over sign, with the "
     "bound lord breaking ties (pp. 450-452). Computable, not yet built."),
    ("Gr. Intr. VII.5, 38-52", "Connection by latitude, in three kinds -- assembly "
     "at equal latitude with one eclipsing the other, and two further kinds (pp. 452-453). "
     "Latitude is available in the chart data but unused for connection."),
    ("Gr. Intr. VII.5, 67-77, the pairs the notes say he omits", "The natural "
     "connections are built from the sign pairs 56 and 67-75 enumerate. Three pairs "
     "the complete schemes contain are not in his lists and are not added: "
     "Aquarius-Scorpio (the antiscia family; 67-75 stops at five pairs), and the "
     "'agreeing in manner' affinities Aries-Scorpio, Taurus-Libra (note 163) and "
     "Aquarius-Capricorn (note 164). Adding them would be inference from the family, "
     "not from the text in hand."),
    ("Gr. Intr. VII.5, 97-100", "Handing over two natures. The Sahl handing-over "
     "table is not a substitute."),
    ("Gr. Intr. VII.5, 104-116", "The full returning tree, with its suitability and "
     "corruption grades. Only Sahl's two manners (Ch. 3, 65-69) are implemented."),
    ("Gr. Intr. VII.6, 13 and 36", "The masculine and feminine degrees, alongside "
     "the signs. The table is Fig. 59 (V.19, p. 304 of Abu Ma'shar's volume); fn 108 "
     "there says he takes no stance among three schemes. Not implemented: the table disagrees with "
     "the sign's gender on half the zodiac, the three schemes agree on a third of it, and "
     "reading 'or male degrees' into 13 and 36 would widen a vote the author would not "
     "commit to."),
    ("Sahl, On Questions Ch. 1, 18-20", "Advancement matched to the nature of the matter: "
     "'if the question was about the nature of retreating, such as travel, moving, a detained "
     "person's exit from his prison, and being released from sorrows, then look for these "
     "matters from the place of retreat and withdrawal.' Stated for questions and never "
     "restated for nativities; testimony 83 stays unconditional (Introduction Ch. 3, 83). "
     "A note, not a topical modifier."),
    ("Sahl, On Questions Ch. 6, 2 and 7.7, 90-101 (Figures 37-41)", "Per-topic reassignment "
     "of the angles -- 'the Ascendant indicates the doctor, the Midheaven indicates the sick "
     "person, the seventh sign indicates the illness, and the fourth sign indicates the "
     "medicine' -- and a twelve-house scheme for war. Horary, and in tension with the fixed "
     "house meanings of Introduction Ch. 2, 4-29, which no text reconciles. A caveat only: "
     "this app's house meanings are one topic's assignment."),
    ("Gr. Intr. VII.3, 2 / VI.26, 3", "The advancing and withdrawing quadrants as a "
     "condition in its own right (ASC to MC and DSC to IC advancing: primary motion "
     "toward the meridian). Figure 90 (Gr. Intr.) with Dykes's note on On Nativities 10.3; "
     "distinct from Sahl 83, which is his own Ch. 3, 4. Recorded; neither computed for the chart nor scored. "
     "Sahl's own natal timing uses "
     "the quadrants (On Nativities 7.4, 17, marriage: \"the two eastern quarters, in what is "
     "between the Ascendant and the Midheaven, and what is opposite that\"; 5.3, 11-12, children) "
     "and a clockwise scheme for chronic illness (6.5, 1); On Times 1, 14's preference for "
     "Masha'allah's hemispheres (On Choices 6, 16-17) is On Times 1's alone."),
    ("Gr. Intr. VII.5, 32-33", "Mixing of natures by ray across a sign boundary "
     "(Fig. 121: the Moon at 29 59' Aquarius, Saturn at 1 Cancer casting his trine ray to 1 Pisces). Only the "
     "body-to-body case of VII.4, 13-14 is reported."),
    ("Sahl, On Nativities Ch. 1.20, 13-15", "Two sentences of the house-master's years: 13 is illegible "
     "in part, 14-15 unclear in sense (fn 156); both are printed on the Releaser tab and not applied. 'Umar "
     "al-Tabari, Book of Nativities I.4.3, reads 14-15 by degree -- one such condition (peregrination, setting, "
     "retrogradation, burning) does not harm a superior planet as it does an inferior; a superior under a "
     "serious impediment gives its lesser years as months or days -- and 'serious' is defined in no text, so "
     "the reading rides in the printed note and is not coded."),
    ("ITA VIII.1.3 (al-Qabisi IV.4-6), with its directions at VIII.2.2", "Al-Qabisi's own releaser and house-master "
     "procedure, whole: eleven places for each luminary with sign-gender gates, the Moon's rays, the twelve houses "
     "by the degrees of the Ascendant's hours with the five degrees at every cusp, three orderings of the lords, "
     "the tie-breaks (own place, the nearer degree, the Sun's relations), the four-sign exception, and the "
     "directions by the hours of the distance from the angle (VIII.2.2). A second longevity procedure beside "
     "Sahl's, from a text in hand; Sahl's is the one built, and the Releaser tab names al-Qabisi where he "
     "differs (the five degrees at any house; quadrant houses for the places)."),
    ("Sahl, On Nativities Ch. 2.2", "The chapter's own judgment of eminence from the fixed stars. The "
     "28 stars Sahl carries are placed on the Timing page for PN IV's I.6, 7 and III.8, 9; 2.2's "
     "delineation is not built."),
    ("Sahl, On Nativities Ch. 2.6 and 4.9", "Uses of the twelfth-parts beyond the "
     "Moon's (luminaries, Ascendant, infortunes)."),
    ("Sahl, On Nativities: further Lots", "Constitution (1.34, 13, p. 358, "
     "'foundation' uncertain); male/female (3.13, 20); Venus to the 7th place "
     "(7.1, 10 and 145); Sun to Moon projected from Venus (7.4, 8); religion "
     "(9.5, 3, identity disputed); riding animals (12.2 fn 18)."),
]

# --- Special Degrees & Conditions ----------------------------------------

# Classical "wells" (pitted degrees) by sign, per Abu Ma'shar, The Great
# Introduction to Astrology V.21, Fig. 62 (Dykes translation, p. 308 of Abu
# Ma'shar's own volume -- the pagination is not Sahl's). Ordinal degrees as
# the figure prints them ("the 6th, 11th, 17th ..."); the code tests
# int(lon % 30) + 1 against them. Corrected 2026-09-08 against the owner's
# photographs of V.21 (p. 308), not the OCR capture: Aries gained 29,
# Gemini's 13 became 12, Pisces gained 28; confirmed a third time the same
# day against Fig. 62's own page image, all 64 cells -- PN IV's Figure
# 98 has 62, differing in three (CORPUS_MANIFEST). (The earlier text
# cited Fig. 98, which in Abu Ma'shar's volume is "Speed relative to
# apogee" -- but Persian Nativities IV's Fig. 98 IS the wells table, so
# name the volume before "correcting" this.) Pinned cell by cell in
# tests/test_base_tables.py.
WELLED_DEGREES = {
    'Aries': [6, 11, 17, 23, 29],
    'Taurus': [5, 13, 18, 24, 25, 26],
    'Gemini': [2, 12, 17, 26, 30],
    'Cancer': [12, 17, 23, 26, 30],
    'Leo': [6, 13, 15, 22, 23, 28],
    'Virgo': [8, 13, 16, 21, 25],
    'Libra': [1, 7, 20, 30],
    'Scorpio': [9, 10, 17, 22, 23, 27],
    'Sagittarius': [7, 12, 15, 24, 27, 30],
    'Capricorn': [2, 7, 17, 22, 24, 28],
    'Aquarius': [1, 12, 17, 23, 29],
    'Pisces': [4, 9, 24, 27, 28],
}

# Sahl's two sign categories, stated identically in two works (C-04):
# "of them are signs which are said to be dark, and they are Libra and
# Capricorn" (Introduction Ch. 1, 18; On Nativities 1.38, 8 the same), and
# "a place called the 'burned place,' and it is the end of Libra and the
# beginning of Scorpio" (Introduction Ch. 1, 19; On Nativities 1.38, 9).
# Sahl gives the burned place NO degrees. The 15 Libra-15 Scorpio span this
# app once flagged is the definition "often" given (Dykes, Carmen p. 258 fn
# 104) and al-Qabisi's own, III.29 in ITA IV.3 (p. 222; OCR, unverified):
# "the burnt path (which is the last half of Libra and the first half of
# Scorpio)" -- a primary text in hand, cited since 2026-09-15 in place of
# the Course Glossary (3.13) -- and is not applied to Sahl; the 19 Libra-3
# Scorpio span is Abu Ma'shar's own harsher band (VII.6, 40,
# HARSH_BURNED_PATH below), applied where the table is his and BORROWED,
# labelled as borrowed, for Sahl's condition 110 (Introduction 3, 110: "the
# end of Libra and the beginning of Scorpio"). Decision D-5, 2026-09-08, as
# implemented by the owner's sheet row 9 (2026-09-11); comment rewritten
# after the build check (D5).
DARK_SIGNS = {'Libra', 'Capricorn'}
BURNED_PLACE_SIGNS = ('Libra', 'Scorpio')

# --- Sahl's sign categories where his two works disagree (C-11 / D-7) ----
# One reading per work, never a union: the union is in neither witness
# (process/tae_docs/synthesis/03_changes.md #2; decision D-7, 2026-09-08). Introduction =
# The Introduction Ch. 1; Nativities = On Nativities Ch. 1.38.
#   Four-footed. Intro 1, 13: "Aries, Taurus, and the beginning of
#   Capricorn, and the end of Sagittarius." Nat. 1.38, 1: "Aries, Leo,
#   Taurus have four feet; and the <first> half of Sagittarius has two
#   feet, and the other has four feet." Leo is in one, Capricorn in the
#   other. Values give the extent of the sign the text names.
FOUR_FOOTED = {
    'Introduction':  {'Aries': 'whole', 'Taurus': 'whole', 'Capricorn': 'the beginning', 'Sagittarius': 'the end'},
    'On Nativities': {'Aries': 'whole', 'Leo': 'whole', 'Taurus': 'whole', 'Sagittarius': 'the second half'},
}
#   Voice. Intro 1, 20-22, three classes; Nat. 1.38, 25-28, four. Virgo
#   is "half a voice" in one and "powerful voice" in the other.
VOICE = {
    'Introduction': {
        'full voice': ['Aries', 'Taurus', 'Gemini', 'Leo', 'Libra', 'Sagittarius'],
        'half a voice': ['Capricorn', 'Aquarius', 'Virgo'],
        'no voice': ['Cancer', 'Scorpio', 'Pisces'],
    },
    'On Nativities': {
        'powerful voice': ['Gemini', 'Virgo', 'Libra'],
        'balanced voice': ['Aries', 'Taurus', 'Leo', 'Sagittarius'],
        'weak voice': ['Capricorn', 'Aquarius'],
        'no voice': ['Cancer', 'Scorpio', 'Pisces'],       # "Cancer and its triplicity"
    },
}
#   Barren. Intro 1, 23: "signs of barrenness, few in children: and they
#   are Aries, Leo, and Virgo." Nat. 1.38, 16: "The barren ones: Leo,
#   Virgo, and Sagittarius"; 17: "Some scholars said that Capricorn and
#   Aquarius are barren" (a reported opinion, kept apart). Many children
#   agrees in both (Intro 1, 24; Nat. 1.38, 14): Cancer, Scorpio, Pisces.
BARREN = {
    'Introduction': ['Aries', 'Leo', 'Virgo'],
    'On Nativities': ['Leo', 'Virgo', 'Sagittarius'],
    'On Nativities (some scholars, 1.38, 17)': ['Capricorn', 'Aquarius'],
}
MANY_CHILDREN = ['Cancer', 'Scorpio', 'Pisces']
SIGN_CATEGORY_SOURCES = {
    'Four-footed': ('Introduction Ch. 1, 13', 'On Nativities 1.38, 1'),
    'Voice': ('Introduction Ch. 1, 20-22', 'On Nativities 1.38, 25-28'),
    'Barren': ('Introduction Ch. 1, 23', 'On Nativities 1.38, 16-17'),
    'Many children': ('Introduction Ch. 1, 24', 'On Nativities 1.38, 14'),
    'Dark': ('Introduction Ch. 1, 18', 'On Nativities 1.38, 8'),
    'Burned place': ('Introduction Ch. 1, 19', 'On Nativities 1.38, 9'),
}

def sign_categories(sign):
    """Every category label a sign carries, one column per work where the
    works disagree, so a reader sees both readings and no merged one."""
    def voice_class(work):
        return next((cls for cls, signs in VOICE[work].items() if sign in signs), '-')
    return {
        'Four-footed (Intro)': FOUR_FOOTED['Introduction'].get(sign, '-'),
        'Four-footed (Nat.)': FOUR_FOOTED['On Nativities'].get(sign, '-'),
        'Voice (Intro)': voice_class('Introduction'),
        'Voice (Nat.)': voice_class('On Nativities'),
        'Barren (Intro)': 'yes' if sign in BARREN['Introduction'] else '-',
        'Barren (Nat.)': ('yes' if sign in BARREN['On Nativities']
                          else 'some scholars' if sign in BARREN['On Nativities (some scholars, 1.38, 17)'] else '-'),
        'Many children (both)': 'yes' if sign in MANY_CHILDREN else '-',
        'Dark (both)': 'yes' if sign in DARK_SIGNS else '-',
        'Burned place, no degrees (both)': 'yes' if sign in BURNED_PLACE_SIGNS else '-',
    }

# --- Dignity orderings, kept apart by context (C-20 / D-8) ---------------
# "the triplicity is below the house, and likewise the bound below the
# triplicity, and the face below the bound" (Questions Ch. 13, 7 -- a
# planet's rank; exaltation is not placed). "the stronger of them is the
# lord of the bound, then the lord of the house, then the lord of the
# exaltation, then the lord of the triplicity, then the lord of the image"
# (On Nativities 1.20, 2 -- house-master selection). Glossary p. 777 lists
# domicile > exaltation > triplicity > bound > face. The bound is first in
# one and third in another; they answer different questions and are never
# merged (decision D-8). VICTOR_WEIGHTS (the almuten) is a fourth thing.
DIGNITY_ORDER = {
    'Questions Ch. 13, 7 (a planet\'s rank)': ['house', 'triplicity', 'bound', 'face'],
    'On Nativities 1.20, 2 (house-master selection)': ['bound', 'house', 'exaltation', 'triplicity', 'image'],
    'Glossary p. 777 (general listing)': ['house', 'exaltation', 'triplicity', 'bound', 'face'],
}

# --- Casting the rays by ascensions: Ptolemy's method as reported by Abu
# Ma'shar (Gr. Intr. VII.7, 1-22) -- decision D-1, 2026-09-08 ----
#
# A STATIC chart quantity: where a planet's sextile, square and trine rays
# fall at the moment of the chart once the ascensions of the birth latitude
# are taken into account, beside the zodiacal aspect VII.5 uses. Nothing
# here advances a point through time. The distributions are implemented
# separately, from Persian Nativities IV (section 3b below, D-3 closed
# 2026-09-10); the RELEASER is still deferred, because PN IV turned out
# not to state how one is chosen (III.3, 1 lists the candidates; IX.8, 123 defers their years). VII.7, 1-2 says the
# tradition disagrees and that this is Ptolemy's account ("we will state
# what Ptolemy ... said"), so it is labelled his, not Abu Ma'shar's own.
#
# The chapter presupposes tables it does not give (fn. 250: the hourly
# times, "special tables in the Almagest"; fn. 251: the ascension-to-degree
# inverses), all computable from spherical astronomy and computed here:
# right ascension and declination of an ecliptic degree (swe.cotrans), the
# ascensional difference for the latitude, the oblique ascension RA - AD,
# and the diurnal hourly time (90 + AD) / 6 in degrees of right ascension
# per seasonal hour; the nocturnal hourly time is (90 - AD) / 6, derived
# from the SAME ascensional difference so the two semi-arcs close to 180
# exactly (computing the nocturnal one from the opposite degree is not
# exactly antisymmetric in floating point, and the sliver it left open
# once divided by a zero semi-arc; fixed 2026-09-08).
#
# Domain. The ascension-to-degree inverse is closed-form and unique only
# where every ecliptic degree rises and sets, |latitude| + obliquity < 90.
# Beyond that some degrees are circumpolar, the oblique ascension is not
# injective (at 70N three longitudes share OA = 0 and two of them never
# cross the horizon), and at the critical latitude itself the mapping has a
# flat interval. There _lon_with_oblique_ascension() returns None and the
# rays table says the method does not apply, in the file's [UNCERTAIN --]
# style, rather than printing a fabricated ray. Owner's call to overturn
# (2026-09-08).
#
# One thing the text leaves open: 18 adds the correction to the candidate
# position NEAREST the planet (left rays), 21 to the more DISTANT (right
# rays). Whether that flip is geometry or a copyist's is not said
# (01_abu_mashar_vii_7_to_9.md B5), so cast_rays_by_ascension() takes an
# anchor argument -- 'as written' (the default), 'nearest', 'distant' --
# and the tests hold all three. 22: the opposition needs none of this,
# "in the same degree and minute".
RAY_ASPECTS = (('Left sextile', 60.0), ('Left square', 90.0), ('Left trine', 120.0),
               ('Right sextile', -60.0), ('Right square', -90.0), ('Right trine', -120.0))
RAY_ANCHOR_OPTIONS = ('as written', 'nearest', 'distant')

def _ra_decl(lon, obliquity):
    """Right ascension and declination of an ecliptic degree (latitude 0)."""
    ra, decl, _r = swe.cotrans((lon % 360.0, 0.0, 1.0), -obliquity)
    return ra % 360.0, decl

def _ascensional_difference(decl, geo_lat):
    x = math.tan(math.radians(geo_lat)) * math.tan(math.radians(decl))
    return math.degrees(math.asin(max(-1.0, min(1.0, x))))

def _oblique_ascension(lon, obliquity, geo_lat):
    ra, decl = _ra_decl(lon, obliquity)
    return (ra - _ascensional_difference(decl, geo_lat)) % 360.0

def _semiarcs(lon, obliquity, geo_lat):
    """Diurnal and nocturnal semi-arcs of the degree, in degrees of right
    ascension, from ONE ascensional difference: 90 + AD and 180 - (90 + AD),
    so that they close to 180 exactly in floating point. A sixth of each is
    the hourly time (fn. 250), in degrees of right ascension per seasonal
    hour."""
    _ra, decl = _ra_decl(lon, obliquity)
    diurnal = 90.0 + _ascensional_difference(decl, geo_lat)
    return diurnal, 180.0 - diurnal

def _ascensional_method_applies(obliquity, geo_lat):
    """Where every ecliptic degree rises and sets, so the oblique ascension
    has a unique inverse (see the domain note above RAY_ASPECTS)."""
    return abs(geo_lat) + obliquity < 90.0

def _lon_with_right_ascension(ra, obliquity):
    """The ecliptic degree whose right ascension is ra (fn. 251's inverse)."""
    r = math.radians(ra % 360.0)
    return math.degrees(math.atan2(math.sin(r), math.cos(r) * math.cos(math.radians(obliquity)))) % 360.0

def _lon_with_oblique_ascension(oa, obliquity, geo_lat):
    """The ecliptic degree whose oblique ascension at this latitude is oa
    (fn. 251's inverse), in closed form:
        tan(lon) = sin(oa) / (cos(eps) cos(oa) - sin(eps) tan(phi)),
    exact to 2e-13 degrees over |phi| <= 65 (tests/test_spherical_math.py).
    Returns None outside the domain |phi| + eps < 90, where the inverse is
    not unique and sometimes does not exist. The scan-and-bisect this
    replaced assumed the 1-degree cell with the smallest sampled residual
    bracketed a sign change; above the polar circle it can miss a narrow
    branch and return a degree six degrees off (found 2026-09-08)."""
    if not _ascensional_method_applies(obliquity, geo_lat):
        return None
    a, e, p = math.radians(oa % 360.0), math.radians(obliquity), math.radians(geo_lat)
    return math.degrees(math.atan2(math.sin(a), math.cos(e) * math.cos(a) - math.sin(e) * math.tan(p))) % 360.0

def _hours_from_stake(lon, armc, obliquity, geo_lat):
    """VII.7, 3-13: the planet's quadrant and its distance in seasonal hours
    from the stake the text measures from. Returns (quadrant, stake, hours)."""
    ra, _decl = _ra_decl(lon, obliquity)
    # The day and night semi-arcs from one ascensional difference: "the
    # portions of the hours of the degree of the planet" and "of the degree
    # of the opposition of the planet", which are 90 + AD and 90 - AD. The
    # classification uses the semi-arcs themselves as boundaries.
    arc_day, arc_night = _semiarcs(lon, obliquity, geo_lat)
    h_day, h_night = arc_day / 6.0, arc_night / 6.0
    d = (ra - armc) % 360.0                                       # right circle of the planet less that of the Midheaven
    if d >= 360.0:                                                 # a tiny negative remainder rounds to exactly 360.0
        d = 0.0

    def hours(arc, hourly):
        # A zero semi-arc (a degree that never rises, or never sets, at this
        # latitude) is a quadrant of no extent: a planet classified into it
        # stands at its stake. Degenerate, and said so rather than divided.
        return 0.0 if hourly == 0.0 else arc / hourly

    if d < arc_day:                                                # 4-5: between the Midheaven and the Ascendant
        return 'Midheaven to Ascendant', 'Midheaven', hours(d, h_day)
    if d < 180.0:                                                  # 6-8: between the Ascendant and the stake of the earth
        return 'Ascendant to stake of the earth', 'Ascendant', hours(d - arc_day, h_night)
    d2 = d - 180.0                                                 # measured from the stake of the earth
    if d2 < arc_night:                                             # 9-10: between the stake of the earth and the setting
        return 'Stake of the earth to setting', 'Stake of the earth', hours(d2, h_night)
    return 'Setting to Midheaven', 'Stake of the setting', hours(d2 - arc_night, h_day)   # 11-13

def _circular_distance(a, b):
    return abs(((a - b + 180.0) % 360.0) - 180.0)

def cast_rays_by_ascension(lon, armc, obliquity, geo_lat, anchor='as written'):
    """VII.7, 14-22 for one planet. Each aspect gives a dict with the
    zodiacal ray, the two candidate positions (from the right ascensions,
    14, and from the ascensions of the city, 15), the hours of distance
    from the stake, and the ascensional ray: the two candidates when they
    agree (16), otherwise the anchor moved towards the other candidate by
    a sixth of their excess for every hour of distance (17-19, 21)."""
    quadrant, stake, hours = _hours_from_stake(lon, armc, obliquity, geo_lat)
    ra, _decl = _ra_decl(lon, obliquity)
    oa = _oblique_ascension(lon, obliquity, geo_lat)
    out = {}
    for name, arc in RAY_ASPECTS:
        from_ra = _lon_with_right_ascension(ra + arc, obliquity)
        from_oa = _lon_with_oblique_ascension(oa + arc, obliquity, geo_lat)
        if from_oa is None:
            # Outside the method's domain (see the note above RAY_ASPECTS):
            # no second candidate, so no ray. None, never a guessed degree.
            ray = None
        elif (excess := _circular_distance(from_ra, from_oa)) < 1e-9:
            ray = from_ra
        else:
            near, far = sorted((from_ra, from_oa), key=lambda x: _circular_distance(x, lon))
            use_nearest = {'as written': arc > 0, 'nearest': True, 'distant': False}[anchor]
            base, other = (near, far) if use_nearest else (far, near)
            step = (excess / 6.0) * hours
            direction = 1.0 if ((other - base) % 360.0) <= 180.0 else -1.0
            ray = (base + direction * min(step, excess)) % 360.0
        out[name] = {'zodiacal': (lon + arc) % 360.0, 'from right ascensions (14)': from_ra,
                     'from the city\'s ascensions (15)': from_oa, 'ascensional': ray,
                     'hours': hours, 'quadrant': quadrant, 'stake': stake}
    out['Opposition'] = {'zodiacal': (lon + 180.0) % 360.0, 'ascensional': (lon + 180.0) % 360.0,
                         'from right ascensions (14)': None, 'from the city\'s ascensions (15)': None,
                         'hours': hours, 'quadrant': quadrant, 'stake': stake}
    return out

RAYS_OUT_OF_DOMAIN = ("[Uncertain -- Ptolemy's ascensional method does not apply at this latitude: "
                      "|latitude| + obliquity is 90 or more, so some ecliptic degrees never rise or set and "
                      "the ascensions of the city (VII.7, 15) have no unique inverse; no ray is given]")

def evaluate_rays_by_ascension(planetary_data, armc, obliquity, geo_lat, anchor='as written'):
    """One row per planet and aspect: the zodiacal ray beside Ptolemy's
    ascensional one, and how far apart they are. Outside the method's
    domain the ascensional cell carries RAYS_OUT_OF_DOMAIN; the opposition
    row (22, "in the same degree and minute") needs no ascensions and is
    given at every latitude."""
    rows = []
    for planet, data in planetary_data.items():
        if planet == 'North Node':
            continue
        cast = cast_rays_by_ascension(data['longitude'], armc, obliquity, geo_lat, anchor)
        for name, _arc in RAY_ASPECTS + (('Opposition', 180.0),):
            r = cast[name]
            if r['ascensional'] is None:
                ascensional, apart = RAYS_OUT_OF_DOMAIN, '--'
            else:
                ascensional = get_degree_string(r['ascensional'])
                apart = f"{_circular_distance(r['zodiacal'], r['ascensional']):.2f}°"
            rows.append({'Planet': planet, 'Ray': name, 'Zodiacal ray': get_degree_string(r['zodiacal']),
                         'Ascensional ray (VII.7)': ascensional, 'Apart': apart,
                         'Hours from stake': f"{r['hours']:.2f} from the {r['stake']}"})
    return rows

# --- Abu Ma'shar's two V.22 degree tables (decisions D-20, D-21) ----------
# Display only, labelled a supplement: none of VII.6's conditions reads
# either table, so neither enters any verdict, and they are shown only
# under "Course text and supplement" (owner, 2026-09-13). Sahl has his OWN
# table for the second rule, NOBILITY_DEGREES below (On Nativities 1.38,
# 39-41, Figure 57 of his volume), eight signs against Figure 64's twelve
# with six of the eight disagreeing; that one is course text and always
# shown. Ordinal
# degrees as the figures print them, tested with int(lon % 30) + 1 like
# the wells. Pinned cell by cell in tests/test_base_tables.py.
#   Figure 63, V.22, 1-2: "when planets indicate the native's good fortune
#   by means of their positions, and the Moon or the Lot of Fortune is in
#   these degrees, or [these degrees] are exactly on the Ascendant, then
#   they will increase in the native's good fortune. And if they indicate
#   downfall, then these will instigate some motion towards high rank and
#   power." Seven degrees, 1.9% of the zodiac.
GOOD_FORTUNE_DEGREES = {'Taurus': [15, 27, 30], 'Leo': [3, 5], 'Scorpio': [7], 'Aquarius': [20]}
#   Figure 64, V.22, 4: "if the Ascendant was one of these degrees ... or
#   the Sun by day or the Moon by night was in one of them, and they were
#   in an excellent position of the circle, and the planets of the root of
#   the nativity indicated good fortune, then they will make him attain
#   nobility and the houses of kings." Thirty-one degrees, 8.6% of the
#   zodiac. The two preconditions cannot be tabulated and are shown as a
#   caveat. Aquarius 17 is also a well (V.21, Fig. 62); Leo 5 and
#   Aquarius 20 are in both tables. The text reconciles none of this.
ELEVATION_DEGREES = {
    'Aries': [19], 'Taurus': [3], 'Gemini': [11], 'Cancer': [1, 2, 3, 14, 15],
    'Leo': [5, 7, 17], 'Virgo': [2, 12, 20], 'Libra': [3, 5, 21], 'Scorpio': [12, 20],
    'Sagittarius': [13, 20], 'Capricorn': [12, 13, 14, 20], 'Aquarius': [7, 16, 17, 20], 'Pisces': [12, 20],
}

def evaluate_book_v_degrees(planetary_data, ascendant_lon, fortune_lon, sect):
    """The points each V.22 table names, checked against its degrees:
    Figure 63 for the Moon, the Lot of Fortune and the Ascendant; Figure 64
    for the Ascendant and the luminary of the sect. A row per hit, and no
    row is a verdict -- see GOOD_FORTUNE_DEGREES above."""
    luminary = 'Sun' if sect == 'Diurnal' else 'Moon'
    checks = [
        ('Moon', planetary_data['Moon']['longitude'], GOOD_FORTUNE_DEGREES, 'Increasing in good fortune (V.22, 1-2; Fig. 63)'),
        ('Lot of Fortune', fortune_lon, GOOD_FORTUNE_DEGREES, 'Increasing in good fortune (V.22, 1-2; Fig. 63)'),
        ('Ascendant', ascendant_lon, GOOD_FORTUNE_DEGREES, 'Increasing in good fortune (V.22, 1-2; Fig. 63)'),
        ('Ascendant', ascendant_lon, ELEVATION_DEGREES, 'Elevation and power (V.22, 4; Fig. 64)'),
        (f'{luminary} (luminary of the sect)', planetary_data[luminary]['longitude'], ELEVATION_DEGREES,
         'Elevation and power (V.22, 4; Fig. 64)'),
    ]
    results = []
    for point, lon, table, label in checks:
        sign = get_zodiac_sign(lon)
        degree_1_based = int(lon % 30) + 1
        if degree_1_based in table.get(sign, []):
            caveat = ('the text adds "in an excellent position of the circle" and a fortunate root, neither tabulated'
                      if table is ELEVATION_DEGREES else 'an amplifier of a good fortune already shown, not a testimony')
            if table is ELEVATION_DEGREES and degree_1_based in WELLED_DEGREES.get(sign, []):
                caveat += '; this degree is also a well (V.21)'
            results.append({'Point': point, 'Position': get_degree_string(lon), 'Table': label, 'Caveat': caveat})
    return results

# Sahl's degrees of nobility and rank -- On Nativities 1.38, 39-41 and
# Figure 57 of his volume (p. 378): "39 A section on the knowledge of the
# degrees in which the native will reach nobility and rank … 40 If it
# happened that a native was born and his Ascendant was one of these
# degrees, or the Moon and Sun were in the equivalent of these degrees
# (and that is superior if it was the Sun by day and by night the Moon),
# then he will reach exaltation and power, or he will rule many lands …"
# The comparison below is a separate witness, not the source of the preceding quotation.
# The same rule as Gr. Intr. V.22, 4 (ELEVATION_DEGREES) with a table
# of its own: eight signs, no degree in Libra, Scorpio, Sagittarius or
# Pisces, and Gemini 13 for his 11, Cancer 13 for his 2-3, Virgo 13 where
# he has 12, Capricorn without his 14. Both luminaries qualify; the sect
# light is "superior". The figure prints bare degrees ("19°", "3°"); they
# are READ as ordinals, which is how Figure 64 prints the same rule's
# degrees and makes the shared cells coincide -- the app's reading. Read
# off the page photograph 2026-09-14 (the corpus transcription had
# Scorpio for the seventh row's Capricorn, corrected the same day);
# pinned in tests/test_base_tables.py.
# Course text: always shown, unlike the V.22 tables it stands beside.
NOBILITY_DEGREES = {
    'Aries': [19], 'Taurus': [3], 'Gemini': [13], 'Cancer': [1, 13, 14, 15],
    'Leo': [5, 7], 'Virgo': [2, 13, 20], 'Capricorn': [12, 13, 20], 'Aquarius': [12, 20],
}

def evaluate_nobility_degrees(planetary_data, ascendant_lon, sect):
    """On Nativities 1.38, 40-41: the Ascendant, the Sun and the Moon
    against Sahl's degrees of nobility and rank. A row per hit; the
    luminary of the sect is marked as the "superior" case the sentence
    names. Display only, like the V.22 tables: no verdict reads it."""
    luminary = 'Sun' if sect == 'Diurnal' else 'Moon'
    checks = [('Ascendant', ascendant_lon), ('Sun', planetary_data['Sun']['longitude']),
              ('Moon', planetary_data['Moon']['longitude'])]
    results = []
    for point, lon in checks:
        sign = get_zodiac_sign(lon)
        degree_1_based = int(lon % 30) + 1
        if degree_1_based in NOBILITY_DEGREES.get(sign, []):
            if point == luminary:
                note = 'the luminary of the sect -- "superior if it was the Sun by day and by night the Moon"'
            elif point == 'Ascendant':
                note = 'the Ascendant itself'
            else:
                note = 'the luminary out of sect (the sentence calls the sect light "superior")'
            results.append({'Point': point, 'Position': get_degree_string(lon),
                            'Degree': f'{get_zodiac_sign(lon)} {degree_1_based}', 'Note': note})
    return results

# Valens, Anthologies II.36 (Riley): the eleven phases of the Moon. Ten are
# listed with a degree from the Sun (the crescent at 45, the quarter at 90,
# the gibbous at 135, the full at 180, the second gibbous at 225, the second
# quarter at 270, the second crescent at 315, the final visibility at 360;
# the new moon and the first visibility carry none) and an eleventh, "when
# it first begins to wane", carries none. The chapter then gives what each
# indicates and the planet that adds its influence to a day of the Moon's
# motion. The text gives moments, not spans: a phase here runs from its own
# degree to the next one's, and the boundaries the text does not give are
# Abu Ma'shar's markers of the Moon's phases applied to Valens's phases --
# Abbr. II.27-31 in ITA II.10.5 (printed p. 108 with Figure 52, read against the photograph 2026-09-17): the conjunction,
# "if she receded from him by 12°", 90°, "distant from the opposite of the
# Sun by 12°", the opposition, "if she would add 12° on top of the
# opposite", 90°, "distant from the Sun by 12°". Used: 12 degrees after the
# conjunction for the new moon and 12 before it for the final visibility,
# 12 after the opposition for the full moon (the waning phase from there to
# the second gibbous at 225). Not used: his fourth marker, 12 before the
# opposition -- Valens's gibbous runs to 180. Until 2026-09-15 the tags and
# the page called these boundaries the app's own (3.9). Display only:
# nothing scores it. Each 'indicates' is quoted as Riley has it.
VALENS_MOON_PHASES = [
    # (phase, lower bound, upper bound, bound source, indicates, ruler, to day)
    ('New moon', 0.0, 12.0, "Abu Ma'shar (12 after the conjunction)",
     'indicative of rank and power, of kingly and despotic dispositions, of all public business '
     'concerning cities, of parents, marriages, religion, and of all universal, cosmic matters. The '
     'rulers of the new moon, of the latitude, and of the motion are indicative of the same things.',
     None, None),
    ('First visibility', 12.0, 45.0, "Abu Ma'shar (lower), Valens (upper)",
     'indicative of life, occupation, and future wealth; in addition, it strengthens the matters '
     'influences by the now moon. The ruler of the “light” indicates the overall influences in the '
     'same way that the monthly cycles and the universal cycles are observed by means of the first '
     'visibility.',
     'Mercury', 4),
    ('Crescent', 45.0, 90.0, 'Valens',
     'indicative of nurture and expectations in life, of wives and mothers.',
     'Mercury', 8),
    ('Quarter', 90.0, 135.0, 'Valens',
     'indicative of injuries, diseases, and violent accidents; also of children, status, and good '
     'things to come.',
     'Venus', 12),
    ('Gibbous', 135.0, 180.0, 'Valens',
     'indicative of prosperity, future success, travel, and the affinity of relatives.',
     'Sun', 14),
    ('Full moon', 180.0, 192.0, "Valens (lower), Abu Ma'shar (upper: 12 after the opposition)",
     'indicative of fame and infamy, of travel and violent events, of those who fall from '
     'pre-eminence as well as those who rise from a humble state, of affinities, passions, political '
     'opposition, and the affinity of parents. This phase has the color of the sign in the Descendant.',
     None, None),
    ('First waning of the light', 192.0, 225.0, "Abu Ma'shar (lower), Valens (upper)",
     'Valens gives this to the ruler of the phase; the ruler is not computed here. Indicative of the diminishing of resources, of the chilling of occupations, of those who grow '
     'humble and lowly, and of sudden falls. This phase has the same influence as the sign which '
     'just follows the Descendant.',
     'Mars', 21),
    ('Second gibbous', 225.0, 270.0, 'Valens',
     'indicative of travel abroad, of great activities, and of prosperity. It has the same influence '
     'as <the IX Place of> the God.',
     'Jupiter', 25),
    ('Second quarter', 270.0, 315.0, 'Valens',
     'indicative of old affairs, of chronic diseases, and of children.',
     'Saturn', 30),
    ('Second crescent', 315.0, 348.0, "Valens (lower), Abu Ma'shar (upper: 12 before the conjunction)",
     'Valens gives this to the ruler of the phase; the ruler is not computed here. Indicative of a wife’s death, of unemployment or robbery.',
     None, None),
    ('Final visibility', 348.0, 360.0, "Abu Ma'shar (lower), Valens (upper)",
     'indicative of chains, imprisonment, secrets, condemnation, and infamy.',
     None, None),
]

def evaluate_moon_phase_valens(planetary_data):
    """Anthologies II.36: the chart's Moon placed in one of Valens's eleven
    phases by its waxing angle from the Sun, (Moon - Sun) mod 360, with the
    phase's 'indicates' and the planet that adds its influence to the day
    the text names. One row always; display only -- see VALENS_MOON_PHASES
    for which boundaries are the text's and which are Abu Ma'shar's 12-degree
    markers (Abbr. II.27-31) applied to it."""
    angle = (planetary_data['Moon']['longitude'] - planetary_data['Sun']['longitude']) % 360.0
    for phase, lo, hi, _src, indicates, ruler, day in VALENS_MOON_PHASES:
        if lo <= angle < hi:
            break
    else:                                         # 360.0 cannot occur after the modulo; guard anyway
        phase, indicates, ruler, day = VALENS_MOON_PHASES[-1][0], VALENS_MOON_PHASES[-1][4], None, None
    return [{'Moon': get_degree_string(planetary_data['Moon']['longitude']),
             'Angle from Sun': f"{angle:.2f}°",
             'Phase': phase,
             'Indicates': indicates,
             'Ruler (to day)': f"{ruler} (to day {day})" if ruler else '--'}]
# --- Morin's rules for aspects into good and bad houses --------------------
# Astrologia Gallica 21.II.X (Holden, pp. 105-106). Display only, like the
# nobility degrees above: no verdict reads it. The chapter names trine and
# sextile as the favorable rays and opposition and square as the adverse
# ones (the semi-sextile and quincunx it calls weak, and p. 110 sets them
# aside unless partile; this app's aspect table has neither). "Unfortunate
# houses" it never lists; the 6th, 8th and 12th are this app's reading --
# the equation with Morin's phrase is the app's, the three houses the
# tradition's difficult averse places: Dykes on al-Qabisi III.28c, ITA
# IV.4.1 fn 43 (p. 224, photographed) on "cadent from the Ascendant": "That
# is, being in aversion to it; in a sign which does not aspect the rising
# sign, particularly the twelfth, eighth, and sixth; the second sign is also
# cadent from the Ascendant but is not considered as difficult" (3.8,
# 2026-09-15).

MORIN_FORTUNATE_HOUSES = (1, 2, 3, 4, 5, 7, 9, 10, 11)
MORIN_UNFORTUNATE_HOUSES = (6, 8, 12)
MORIN_FAVORABLE_ASPECTS = ('Trine', 'Sextile')
MORIN_ADVERSE_ASPECTS = ('Square', 'Opposition')
MORIN_BENEFICS = ('Jupiter', 'Venus')
MORIN_MALEFICS = ('Saturn', 'Mars')

# (planet kind, ray kind, house kind) -> Morin's phrase, always his words:
# its own clause where the text gives the case one, and the same clause
# marked "one clause for both kinds of house" where it does not.
MORIN_ASPECT_RULES = {
    ('Fortune', 'favorable', 'fortunate'):
        '"produce good with ease and in abundance, and cause good in the fortunate houses"',
    ('Fortune', 'favorable', 'unfortunate'):
        '"prevent or mitigate evil in the unfortunate ones"',
    ('Fortune', 'adverse', 'fortunate'):
        '"bring difficulties, hindrances, or misfortunes to be surmounted" (one clause for both kinds of house)',
    ('Fortune', 'adverse', 'unfortunate'):
        '"bring difficulties, hindrances, or misfortunes to be surmounted" (one clause for both kinds of house)',
    ('Infortune', 'adverse', 'fortunate'):
        '"extremely harmful ... preventing or spoiling the good in the fortunate ones, unless it rules over the location where the adverse aspect falls"',
    ('Infortune', 'adverse', 'unfortunate'):
        '"extremely harmful, causing evil in the unfortunate houses"',
    ('Infortune', 'favorable', 'fortunate'):
        '"indicate something good gained by difficult means" (one clause for both kinds of house)',
    ('Infortune', 'favorable', 'unfortunate'):
        '"indicate something good gained by difficult means" (one clause for both kinds of house)',
}

def evaluate_morin_aspects(planetary_data, houses, asc):
    """A row for every trine, sextile, square or opposition the app's own
    aspect table already holds between a Fortune (Jupiter, Venus) or an
    Infortune (Saturn, Mars) and any other planet, with Morin's phrase for
    the ray's kind and the house it falls into -- the whole-sign house of
    the aspected planet. A pair of Fortune and Infortune gives two rows,
    one from each. `houses` (the quadrant cusps) is accepted for the
    evaluators' common signature and not read: the chapter speaks of
    houses as places, and the app's unit for places is the whole sign."""
    rows = []
    for row in _pairwise_configurations(planetary_data):
        aspect = row['aspect_name']
        if aspect in MORIN_FAVORABLE_ASPECTS:
            ray = 'favorable'
        elif aspect in MORIN_ADVERSE_ASPECTS:
            ray = 'adverse'
        else:
            continue
        for caster, other in ((row['p1'], row['p2']), (row['p2'], row['p1'])):
            if caster in MORIN_BENEFICS:
                kind = 'Fortune'
            elif caster in MORIN_MALEFICS:
                kind = 'Infortune'
            else:
                continue
            house = get_wsh_house(planetary_data[other]['longitude'], asc)
            house_kind = 'unfortunate' if house in MORIN_UNFORTUNATE_HOUSES else 'fortunate'
            rows.append({
                'Planet': f'{caster} ({kind})',
                'Aspect': f'{aspect} ({ray})',
                'To': other,
                'House': f'{house} ({house_kind})',
                'Rule': MORIN_ASPECT_RULES[(kind, ray, house_kind)],
            })
    order = {'Saturn': 0, 'Jupiter': 1, 'Mars': 2, 'Venus': 3}
    rows.sort(key=lambda r: (order[r['Planet'].split(' ')[0]], r['To']))
    return rows
# --- Places harming the eyesight: Sahl 6.2, 48-75 and Gr. Intr. VI.20 ---------
# Sahl's chapter on chronic illness of the eyesight ends with the "degrees
# of chronic illness in the signs" (item 5 of al-Andarzaghar's five, 6.2,
# 6): places named for the nebulous stars in them -- the Pleiades, the
# cloud of Cancer, the forehead and sting of the Scorpion, the arrow, the
# spines, the rope -- in four lists that overlap but do not agree (his
# own, 49-55; Rhetorius's, 60-68; the Bizidaj's, 69-72; Nawbakht's, 74).
# Abu Ma'shar states the same places with his own measured longitudes
# (VI.20, 4-9), a degree or more away from Sahl's, and says they move
# (VI.20, 10). Every row is carried as its text prints it; nothing is
# reconciled between the lists or the two authors. READINGS, the app's:
# a bare degree or a degree written as an ordinal ("the ninth degree",
# "from 6 to 9") is the ordinal degree, as Figures 57 and 64 are read, so
# "the ninth to the fifteenth" is 8°00'-15°00' and the lo/hi pair below is
# half-open; a longitude measured in minutes (Abu Ma'shar's, the Bizidaj's
# 27°36'-28°) is taken at its face and the pair is closed, a single
# measured longitude being read as the whole degree it falls in. Two spans
# are the app's reading of a phrase: Sahl 49's "having already passed half
# [of it] until she completes 18°" as 15°00'-18°00', and Nawbakht's
# "the middle of Taurus" (74) as the 15th and 16th degrees. 73 (the first
# degrees of Aries, the last of Capricorn) is left out: it says the child
# will be sickly, not that the eyes are harmed. Display only: nothing
# scores it, and the conditions each rule attaches (the infortunes'
# aspect, the Moon's light, by day the Sun) are carried in the row's
# text, not tested. Shown at the supplement depth only, Sahl's rows with
# Abu Ma'shar's. Pinned in tests/test_eyesight_places_2026_09_14.py.
# A third list beside them (2026-09-15, decision 14 of the PN
# reconciliation): Abu Bakr, On Nativities II.7.3 (PN II p. 238), "the
# same list as Masha'allah's" (fn 1024, Book of Aristotle III.6.2), from
# which Dykes says Sahl's is "somewhat different"; carried as its own
# source, never reconciled with Sahl's. Its ordinals are read as Sahl's
# are, and contiguous ordinals are one span ("the ninth, tenth" is
# 8°00'-10°00'), as Rhetorius's "seventh, eighth" is above. Dorotheus's
# Scorpio degrees, which Abu Bakr gives as a second opinion, are their
# own rows and say so. NOT built: Hugo's own variants of the same list
# (BA III.6.2, fns 36-43; known only from unverified OCR of PN I, so not
# quoted), and Abu Bakr's rule sentences on pp. 237-238 (the luminaries
# besieged, the Moon decreased in the sixth, the Tail on the Ascendant
# degree, and the rest), which the evaluator does not compute. The
# `sentence` field is the page, the chapter having no sentence numbers.
# Pinned in tests/test_eye_degrees_abubakr_2026_09_15.py.
_EYE_SAHL = 'Sahl, On Nativities 6.2'
_EYE_ABU = 'Gr. Intr. VI.20'
_EYE_ABUBAKR = 'Abu Bakr, On Nativities II.7.3'
_EYE_RULE_48 = ('48: "if you found the Moon in the degrees of chronic illness in the signs, and the infortunes '
                'looked at her and their bound, <it indicates> a defect of the eyesight generally, or in the rest of '
                'the body: because in the signs are positions which if the Moon is made unfortunate in them, or the '
                'lord of the Ascendant, it indicates the corruption of the eye"')
_EYE_RULE_60 = '60: "The [degrees] indicative of chronic illness are:"'
_EYE_RULE_69 = ('69: "Now as for the degrees which indicate the corruption of vision especially, if the Moon was with '
                'them by night and the Sun by day, made unfortunate, that is in the conjunction of:"')
_EYE_RULE_74 = ('74: "for the native will have darkness in his eyes"')
_EYE_RULE_ABU = ('1: "The positions in the signs which indicate an ailment of the eyes"')
_EYE_RULE_ABUBAKR = ('p. 238: "And it must be known that in some signs are some degrees which destroy vision:"')

def _eye(source, sign, lo, hi, printed, place, sentence, quote, rule, closed=False, reading=''):
    return {'source': source, 'sign': sign, 'lo': lo, 'hi': hi, 'closed': closed, 'printed': printed,
            'place': place, 'sentence': sentence, 'quote': quote, 'rule': rule, 'reading': reading}

EYESIGHT_PLACES = (
    # al-Andarzaghar's list (48-55), under the rule of 48
    _eye(_EYE_SAHL, 'Leo', 15.0, 18.0, 'Leo, past half until she completes 18°', 'the mane of the Lion', 49,
         '"If the Moon was in Leo, having already passed half [of it] until she completes 18°, and that is around the mane of the Lion, which is called al-Dafārah."',
         _EYE_RULE_48, reading='read as 15°00\'-18°00\''),
    _eye(_EYE_SAHL, 'Scorpio', 7.0, 10.0, 'Scorpio, the eighth, ninth and tenth degree', 'the forehead of the Scorpion', 50,
         '"And in Scorpio, in the eighth, ninth, and tenth degree (and in 23), and it is the forehead of the Scorpion."',
         _EYE_RULE_48, reading='ordinal degrees, 7°00\'-10°00\''),
    _eye(_EYE_SAHL, 'Scorpio', 22.0, 23.0, 'Scorpio, in 23', 'the sting of the Scorpion (23, "the sting of Scorpio, as opposed to the others which form the forehead", fn 75)', 50,
         '"And in Scorpio, in the eighth, ninth, and tenth degree (and in 23), and it is the forehead of the Scorpion."',
         _EYE_RULE_48, reading='read as the 23rd degree, 22°00\'-23°00\''),
    _eye(_EYE_SAHL, 'Sagittarius', 5.0, 9.0, 'Sagittarius from 6° to 9°', 'the place of the arrow', 51,
         '"And in Sagittarius from 6° to 9°, and that is the place of the arrow."',
         _EYE_RULE_48, reading='ordinal degrees, 5°00\'-9°00\''),
    _eye(_EYE_SAHL, 'Aquarius', 9.0, 10.0, 'Aquarius, the tenth', 'the place of the rope ("Or rather, the Pitcher", fn 76)', 52,
         '"And in Aquarius, the tenth, eighteenth, and nineteenth, and that is due to the place of the rope which is in it."',
         _EYE_RULE_48, reading='ordinal degree, 9°00\'-10°00\''),
    _eye(_EYE_SAHL, 'Aquarius', 17.0, 19.0, 'Aquarius, the eighteenth and nineteenth', 'the place of the rope ("Or rather, the Pitcher", fn 76)', 52,
         '"And in Aquarius, the tenth, eighteenth, and nineteenth, and that is due to the place of the rope which is in it."',
         _EYE_RULE_48, reading='ordinal degrees, 17°00\'-19°00\''),
    _eye(_EYE_SAHL, 'Capricorn', 25.0, 29.0, 'Capricorn from 26° to 29°', 'the spines', 53,
         '"And in Capricorn from 26° to 29°, and that is because of the spines."',
         _EYE_RULE_48, reading='ordinal degrees, 25°00\'-29°00\''),
    _eye(_EYE_SAHL, 'Taurus', 5.0, 10.0, 'Taurus from 6° to 10°', 'the position of the Pleiades', 54,
         '"And in Taurus from 6° to 10°, because of the position of the Pleiades."',
         _EYE_RULE_48, reading='ordinal degrees, 5°00\'-10°00\''),
    _eye(_EYE_SAHL, 'Cancer', 8.0, 15.0, 'Cancer, the ninth degree to the fifteenth', 'the place of the cloud', 55,
         '"And in Cancer the ninth degree to the fifteenth, and that is due to the place of the cloud."',
         _EYE_RULE_48, reading='ordinal degrees, 8°00\'-15°00\''),
    # Rhetorius's list (60-68): "these overlap with, but are not identical
    # to, the degrees harming the eyes" (fn 80)
    _eye(_EYE_SAHL, 'Leo', 17.0, 19.0, 'Leo, 18° and 19°', 'Rhetorius\'s degrees of chronic illness', 61,
         '"<In Leo>, 18° and 19° and 28°."', _EYE_RULE_60, reading='ordinal degrees, 17°00\'-19°00\''),
    _eye(_EYE_SAHL, 'Leo', 27.0, 28.0, 'Leo, 28°', 'Rhetorius\'s degrees of chronic illness', 61,
         '"<In Leo>, 18° and 19° and 28°."', _EYE_RULE_60, reading='ordinal degree, 27°00\'-28°00\''),
    _eye(_EYE_SAHL, 'Scorpio', 18.0, 19.0, 'Scorpio, 19°', 'Rhetorius\'s degrees of chronic illness', 62,
         '"In Scorpio, in 19° and 29°."', _EYE_RULE_60, reading='ordinal degree, 18°00\'-19°00\''),
    _eye(_EYE_SAHL, 'Scorpio', 28.0, 29.0, 'Scorpio, 29°', 'Rhetorius\'s degrees of chronic illness', 62,
         '"In Scorpio, in 19° and 29°."', _EYE_RULE_60, reading='ordinal degree, 28°00\'-29°00\''),
    _eye(_EYE_SAHL, 'Sagittarius', 0.0, 1.0, 'Sagittarius, the first degree', 'Rhetorius\'s degrees of chronic illness', 63,
         '"And in Sagittarius the first degree, the seventh, eighth, 18, and 19."', _EYE_RULE_60, reading='ordinal degree, 0°00\'-1°00\''),
    _eye(_EYE_SAHL, 'Sagittarius', 6.0, 8.0, 'Sagittarius, the seventh and eighth', 'Rhetorius\'s degrees of chronic illness', 63,
         '"And in Sagittarius the first degree, the seventh, eighth, 18, and 19."', _EYE_RULE_60, reading='ordinal degrees, 6°00\'-8°00\''),
    _eye(_EYE_SAHL, 'Sagittarius', 17.0, 19.0, 'Sagittarius, 18 and 19', 'Rhetorius\'s degrees of chronic illness', 63,
         '"And in Sagittarius the first degree, the seventh, eighth, 18, and 19."', _EYE_RULE_60, reading='ordinal degrees, 17°00\'-19°00\''),
    _eye(_EYE_SAHL, 'Taurus', 5.0, 8.0, 'Taurus, the sixth, seventh and eighth', 'Rhetorius\'s degrees of chronic illness', 64,
         '"And in Taurus the sixth, seventh, eighth, and tenth."', _EYE_RULE_60, reading='ordinal degrees, 5°00\'-8°00\''),
    _eye(_EYE_SAHL, 'Taurus', 9.0, 10.0, 'Taurus, the tenth', 'Rhetorius\'s degrees of chronic illness', 64,
         '"And in Taurus the sixth, seventh, eighth, and tenth."', _EYE_RULE_60, reading='ordinal degree, 9°00\'-10°00\''),
    _eye(_EYE_SAHL, 'Cancer', 8.0, 15.0, 'Cancer, the ninth to the fifteenth degree', 'Rhetorius\'s degrees of chronic illness', 65,
         '"And in Cancer the ninth to the fifteenth degree."', _EYE_RULE_60, reading='ordinal degrees, 8°00\'-15°00\''),
    _eye(_EYE_SAHL, 'Capricorn', 25.0, 29.0, 'Capricorn, the twenty-sixth to the twenty-ninth', 'Rhetorius\'s degrees of chronic illness', 66,
         '"And in Capricorn the twenty-sixth to the twenty-ninth."', _EYE_RULE_60, reading='ordinal degrees, 25°00\'-29°00\''),
    _eye(_EYE_SAHL, 'Aquarius', 9.0, 10.0, 'Aquarius, the tenth', 'Rhetorius\'s degrees of chronic illness', 67,
         '"Aquarius: the tenth, twelfth, and in the nineteenth."', _EYE_RULE_60, reading='ordinal degree, 9°00\'-10°00\''),
    _eye(_EYE_SAHL, 'Aquarius', 11.0, 12.0, 'Aquarius, the twelfth', 'Rhetorius\'s degrees of chronic illness', 67,
         '"Aquarius: the tenth, twelfth, and in the nineteenth."', _EYE_RULE_60, reading='ordinal degree, 11°00\'-12°00\''),
    _eye(_EYE_SAHL, 'Aquarius', 18.0, 19.0, 'Aquarius, the nineteenth', 'Rhetorius\'s degrees of chronic illness', 67,
         '"Aquarius: the tenth, twelfth, and in the nineteenth."', _EYE_RULE_60, reading='ordinal degree, 18°00\'-19°00\''),
    _eye(_EYE_SAHL, 'Libra', 5.0, 8.0, 'Libra, the sixth, seventh and eighth', 'Rhetorius\'s degrees of chronic illness ("This may be an error", fn 89)', 68,
         '"And in Libra the sixth, seventh, eighth, and tenth."', _EYE_RULE_60, reading='ordinal degrees, 5°00\'-8°00\''),
    _eye(_EYE_SAHL, 'Libra', 9.0, 10.0, 'Libra, the tenth', 'Rhetorius\'s degrees of chronic illness ("This may be an error", fn 89)', 68,
         '"And in Libra the sixth, seventh, eighth, and tenth."', _EYE_RULE_60, reading='ordinal degree, 9°00\'-10°00\''),
    # the Bizidaj (69-72)
    _eye(_EYE_SAHL, 'Libra', 27.6, 28.0, 'Libra, from 27° 36\' to 28°', 'the Bizidaj\'s degrees of the corruption of vision', 70,
         '"Libra, from 27° 36\' to 28°."', _EYE_RULE_69, closed=True, reading='measured, 27°36\'-28°00\''),
    _eye(_EYE_SAHL, 'Sagittarius', 0.0, 1.0, 'Sagittarius, the first degree', 'the Bizidaj\'s degrees of the corruption of vision', 71,
         '"And likewise the first degree of Sagittarius, and the ninth and tenth <of Scorpio>."', _EYE_RULE_69, reading='ordinal degree, 0°00\'-1°00\''),
    _eye(_EYE_SAHL, 'Scorpio', 8.0, 10.0, 'Scorpio, the ninth and tenth', 'the Bizidaj\'s degrees of the corruption of vision', 71,
         '"And likewise the first degree of Sagittarius, and the ninth and tenth <of Scorpio>."', _EYE_RULE_69, reading='ordinal degrees, 8°00\'-10°00\''),
    _eye(_EYE_SAHL, 'Cancer', 14.0, 19.0, 'Cancer, from 15° to 19°', 'the blemish in Cancer', 72,
         '"And likewise the blemish in Cancer, and it is from 15° to 19°."', _EYE_RULE_69, reading='ordinal degrees, 14°00\'-19°00\''),
    # Nawbakht (74)
    _eye(_EYE_SAHL, 'Taurus', 14.0, 16.0, 'the middle of Taurus', 'Nawbakht\'s degrees of darkness in the eyes', 74,
         '"And likewise if the Moon was in the middle of Taurus, or in the ninth degree of Cancer, or in the first degree of Sagittarius, for the native will have darkness in his eyes."',
         _EYE_RULE_74, reading='read as the 15th and 16th degrees, 14°00\'-16°00\''),
    _eye(_EYE_SAHL, 'Cancer', 8.0, 9.0, 'the ninth degree of Cancer', 'Nawbakht\'s degrees of darkness in the eyes', 74,
         '"And likewise if the Moon was in the middle of Taurus, or in the ninth degree of Cancer, or in the first degree of Sagittarius, for the native will have darkness in his eyes."',
         _EYE_RULE_74, reading='ordinal degree, 8°00\'-9°00\''),
    _eye(_EYE_SAHL, 'Sagittarius', 0.0, 1.0, 'the first degree of Sagittarius', 'Nawbakht\'s degrees of darkness in the eyes', 74,
         '"And likewise if the Moon was in the middle of Taurus, or in the ninth degree of Cancer, or in the first degree of Sagittarius, for the native will have darkness in his eyes."',
         _EYE_RULE_74, reading='ordinal degree, 0°00\'-1°00\''),
    # Abu Ma'shar, Gr. Intr. VI.20, 4-9: measured longitudes "in our time period" (10)
    _eye(_EYE_ABU, 'Taurus', 13.6, 14.5, 'from 13° 36\' Taurus to 14° 30\'', 'the Pleiades', 4,
         '"Now as for [1] the Pleiades, they are from 13° 36\' Taurus to 14° 30\'; their latitude in the north is from 3° to 5°."',
         _EYE_RULE_ABU, closed=True, reading='measured, 13°36\'-14°30\''),
    _eye(_EYE_ABU, 'Cancer', 21.0, 22.0, 'Cancer 21° 08\'', 'the nebula which is in Cancer', 5,
         '"As for [2] the nebula which is in Cancer, it is in it at 21° 08\'; its latitude in the north is 40\'."',
         _EYE_RULE_ABU, reading='one measured longitude, read as its whole degree, 21°00\'-22°00\''),
    _eye(_EYE_ABU, 'Scorpio', 20.0, 21.0, 'Scorpio 20°', 'the leg of the Scorpion (the first of two)', 6,
         '"As for [3-4] the leg of the Scorpion, there are two (and both of them are in Scorpio): one of them is in it at 20°, and the other in it at 21° 10\'; their latitude in the south is 6°."',
         _EYE_RULE_ABU, reading='one measured longitude, read as its whole degree, 20°00\'-21°00\''),
    _eye(_EYE_ABU, 'Scorpio', 21.0, 22.0, 'Scorpio 21° 10\'', 'the leg of the Scorpion (the second of two)', 6,
         '"As for [3-4] the leg of the Scorpion, there are two (and both of them are in Scorpio): one of them is in it at 20°, and the other in it at 21° 10\'; their latitude in the south is 6°."',
         _EYE_RULE_ABU, reading='one measured longitude, read as its whole degree, 21°00\'-22°00\''),
    _eye(_EYE_ABU, 'Sagittarius', 15.0, 16.0, 'Sagittarius 15° 20\'', 'the arrows', 7,
         '"The place of [5] the arrows in Sagittarius is 15° 20\', its latitude in the south 6° 20\'."',
         _EYE_RULE_ABU, reading='one measured longitude, read as its whole degree, 15°00\'-16°00\''),
    _eye(_EYE_ABU, 'Capricorn', 22.0, 23.0, 'Capricorn 22°', 'the spines of Capricorn', 8,
         '"The [6] spines of Capricorn are 22°, its latitude in the north 39° 15\'."',
         _EYE_RULE_ABU, reading='one measured longitude, read as its whole degree, 22°00\'-23°00\''),
    _eye(_EYE_ABU, 'Aquarius', 20.0 + 10.0 / 60.0, 24.0 + 20.0 / 60.0, 'from 20° 10\' Aquarius to 24° 20\'', 'the pour of water of Aquarius', 9,
         '"The [7] pour of water of Aquarius is four stars, and they are from 20° 10\' Aquarius to 24° 20\' of it; its latitude in the north is from 8° 10\' to 10° 20\'."',
         _EYE_RULE_ABU, closed=True, reading='measured, 20°10\'-24°20\''),
    # Abu Bakr, On Nativities II.7.3 (p. 238): Masha'allah's list (fn 1024), carried whole beside Sahl's, not reconciled
    _eye(_EYE_ABUBAKR, 'Taurus', 5.0, 6.0, 'Taurus, the sixth', 'the place of Thurayya, the Pleiades (fn 1026)', 'p. 238',
         '"in Taurus, the place of Thurayyā, the sixth, ninth, and tenth degrees."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 5°00\'-6°00\''),
    _eye(_EYE_ABUBAKR, 'Taurus', 8.0, 10.0, 'Taurus, the ninth and tenth', 'the place of Thurayya, the Pleiades (fn 1026)', 'p. 238',
         '"in Taurus, the place of Thurayyā, the sixth, ninth, and tenth degrees."',
         _EYE_RULE_ABUBAKR, reading='ordinal degrees, 8°00\'-10°00\''),
    _eye(_EYE_ABUBAKR, 'Cancer', 8.0, 15.0, 'Cancer, from the ninth degree up to the fifteenth', 'the nebula in Cancer (fn 1027)', 'p. 238',
         '"In Cancer, from the ninth degree up to the fifteenth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degrees, 8°00\'-15°00\''),
    _eye(_EYE_ABUBAKR, 'Leo', 17.0, 18.0, 'Leo, the eighteenth', 'the place of Dafira (Adhafera, fn 1028)', 'p. 238',
         '"In Leo, the place of Dafira, the eighteenth degree, the twenty-seventh, and twenty-eighth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 17°00\'-18°00\''),
    _eye(_EYE_ABUBAKR, 'Leo', 26.0, 28.0, 'Leo, the twenty-seventh and twenty-eighth', 'the place of Dafira -- fn 1029 names the stars near the Lion\'s tail for these two', 'p. 238',
         '"In Leo, the place of Dafira, the eighteenth degree, the twenty-seventh, and twenty-eighth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degrees, 26°00\'-28°00\''),
    _eye(_EYE_ABUBAKR, 'Scorpio', 18.0, 19.0, 'Scorpio, the nineteenth', 'Scorpio (no place named)', 'p. 238',
         '"In Scorpio, the nineteenth and twenty-eighth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 18°00\'-19°00\''),
    _eye(_EYE_ABUBAKR, 'Scorpio', 27.0, 28.0, 'Scorpio, the twenty-eighth', 'Scorpio (no place named)', 'p. 238',
         '"In Scorpio, the nineteenth and twenty-eighth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 27°00\'-28°00\''),
    _eye(_EYE_ABUBAKR, 'Scorpio', 7.0, 10.0, 'Scorpio, the eighth, ninth and tenth, according to Dorotheus', 'Dorotheus\'s degrees: the face and sting of the Scorpion (fn 1031)', 'p. 238',
         '"And according to Dorotheus, in Scorpio the eighth degree, the ninth, tenth, and twenty-second."',
         _EYE_RULE_ABUBAKR, reading='ordinal degrees, 7°00\'-10°00\''),
    _eye(_EYE_ABUBAKR, 'Scorpio', 21.0, 22.0, 'Scorpio, the twenty-second, according to Dorotheus', 'Dorotheus\'s degrees: the face and sting of the Scorpion (fn 1031)', 'p. 238',
         '"And according to Dorotheus, in Scorpio the eighth degree, the ninth, tenth, and twenty-second."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 21°00\'-22°00\''),
    _eye(_EYE_ABUBAKR, 'Sagittarius', 0.0, 1.0, 'Sagittarius, the first degree', 'the point of the arrow (fn 1032)', 'p. 238',
         '"In Sagittarius, the first, seventh, eighth, and ninth degree."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 0°00\'-1°00\''),
    _eye(_EYE_ABUBAKR, 'Sagittarius', 6.0, 9.0, 'Sagittarius, the seventh, eighth and ninth', 'the point of the arrow (fn 1032)', 'p. 238',
         '"In Sagittarius, the first, seventh, eighth, and ninth degree."',
         _EYE_RULE_ABUBAKR, reading='ordinal degrees, 6°00\'-9°00\''),
    _eye(_EYE_ABUBAKR, 'Capricorn', 25.0, 29.0, 'Capricorn, from the twenty-sixth up to the twenty-ninth', 'the spine of Capricorn (fn 1033)', 'p. 238',
         '"In Capricorn, from the twenty-sixth up to the twenty-ninth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degrees, 25°00\'-29°00\''),
    _eye(_EYE_ABUBAKR, 'Aquarius', 5.0, 6.0, 'Aquarius, the sixth', 'the Pitcher (fn 1034)', 'p. 238',
         '"In Aquarius, the sixth degree, tenth, and nineteenth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 5°00\'-6°00\''),
    _eye(_EYE_ABUBAKR, 'Aquarius', 9.0, 10.0, 'Aquarius, the tenth', 'the Pitcher (fn 1034)', 'p. 238',
         '"In Aquarius, the sixth degree, tenth, and nineteenth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 9°00\'-10°00\''),
    _eye(_EYE_ABUBAKR, 'Aquarius', 18.0, 19.0, 'Aquarius, the nineteenth', 'the Pitcher (fn 1034)', 'p. 238',
         '"In Aquarius, the sixth degree, tenth, and nineteenth."',
         _EYE_RULE_ABUBAKR, reading='ordinal degree, 18°00\'-19°00\''),
)

def eyesight_place_in_span(row, longitude):
    """True when longitude stands in the row's sign and inside its span:
    half-open for ordinal degrees, closed for a measured span."""
    if get_zodiac_sign(longitude) != row['sign']:
        return False
    d = longitude % 30.0
    if row['closed']:
        return row['lo'] - 1e-9 <= d <= row['hi'] + 1e-9
    return row['lo'] <= d < row['hi']

def evaluate_eyesight_places(planetary_data, ascendant_lon):
    """The Moon and the lord of the Ascendant (6.2, 48), the Sun (69), and
    the Ascendant degree itself (this app's addition) against
    EYESIGHT_PLACES: a row per point per span it stands in, each text's
    span under its own source. The lord of the Ascendant is the domicile
    lord of the rising sign. Display only; nothing scores it, and the
    rule's further conditions ride in the 'Text' column."""
    asc_sign = get_zodiac_sign(ascendant_lon)
    asc_lord = next(pl for pl, signs in DOMICILES.items() if asc_sign in signs)
    checks = [('Sun', planetary_data['Sun']['longitude']), ('Moon', planetary_data['Moon']['longitude']),
              ('Ascendant', ascendant_lon)]
    if asc_lord in ('Sun', 'Moon'):
        # one row, both roles named
        checks = [(f'{pt} (the lord of the Ascendant)' if pt == asc_lord else pt, lon) for pt, lon in checks]
    elif asc_lord in planetary_data:
        checks.append((f'Lord of the Ascendant ({asc_lord})', planetary_data[asc_lord]['longitude']))
    results = []
    for point, lon in checks:
        for row in EYESIGHT_PLACES:
            if eyesight_place_in_span(row, lon):
                results.append({'Point': point, 'Position': get_degree_string(lon),
                                'Place': f"{row['place']} -- {row['printed']} ({row['reading']})",
                                'Source': f"{row['source']}, {row['sentence']}",
                                'Text': f"{row['sentence']}: {row['quote']} Rule -- {row['rule']}"})
    return results

def evaluate_special_degrees(planetary_data):
    """Flags planets in Sahl's dark signs, in the two signs of his burned
    place (no degrees -- see DARK_SIGNS above), in a classical welled
    degree of their current sign, or in one of Sahl's two sign-boundary
    conditions.

    The boundary pair is the other half of the five-degree rule and its
    mirror at the far end of the sign:

    ENTERING -- "every planet which is at the beginning of a sign is weak
    until it is firmly established in it and comes to be 5 degrees within
    it" (Fifty Aphorisms #44, 87), repeated as "the planets do not become
    powerful in the sign [they are in] until they travel 5 degrees in it"
    (On Nativities Ch.1.22, 9).

    LEAVING -- "if a planet came to be in the LAST degree of the sign, then
    its strength has already gone away from that sign, and its strength is
    in the next sign ... like a man putting his foot on the threshold of
    [his] door. And if a planet was in the twenty-ninth degree, then indeed
    the strength of the planet IS in that sign" (Fifty Aphorisms #15,
    31-33). So the 29th degree still counts, and only the 30th has left."""
    results = []
    for planet, data in planetary_data.items():
        if planet == 'North Node': continue
        lon = data['longitude']
        conditions = []

        sign = get_zodiac_sign(lon)
        if sign in DARK_SIGNS:
            conditions.append("Dark sign (Intro Ch. 1, 18; Nat. 1.38, 8)")
        if sign in BURNED_PLACE_SIGNS:
            conditions.append('In the burned place\'s signs -- "the end of Libra and the beginning of '
                              'Scorpio", no degrees given (Intro Ch. 1, 19; Nat. 1.38, 9)')
        degree_1_based = int(lon % 30) + 1
        if degree_1_based in WELLED_DEGREES.get(sign, []):
            conditions.append("Welled Degree")

        deg_in_sign = lon % 30.0
        if deg_in_sign < 5.0:
            conditions.append("Not yet established in the sign (Aph. #44, 87)")
        elif deg_in_sign >= 29.0:
            conditions.append("On the threshold; strength already in the next sign (Aph. #15, 31)")

        if conditions:
            results.append({
                'Planet': planet,
                'Position': get_degree_string(lon),
                'Condition': ", ".join(conditions),
            })
    return results

# --- Abu Ma'shar's Planetary Condition -- Gr. Intr. VII.6 -------
# (with supporting data/mechanics from VII.3-4 and V.20)

FORTUNES = {'Jupiter', 'Venus'}
INFORTUNES = {'Saturn', 'Mars'}

# Choices Ch. 1, 12 names three grounds for possible suitability. The
# complete sentence is FITTING_INFORTUNE_QUOTE below. The natal-only switch
# and its whole softening at the existing affliction sites are this app's
# optional convention, not an exhaustive implementation of the sentence.
# Choices 1, 16-17 describes the infortunes' nature; it is not used to erase
# the observation in 12. Natural-malefic tests (135; Head/Tail 51) stay so.
FITTING_INFORTUNE_QUOTE = (
    "And perhaps you have seen the indicator being connected to an infortune from a square, assembly, or opposition, "
    "and someone had gains: that infortune was good for him, because the infortunes are perhaps more fitting for him, "
    "since [one] may be the lord of the original Ascendant, and the lord of the distribution, or the lord of the Ascendant of the year.")
FITTING_INFORTUNE_STANDING = (
    "Sahl names three grounds for an infortune's possible suitability. This optional convention uses only lordship of the natal Ascendant "
    "and extends it to the listed affliction tests. Suitability through the distribution's lord or the annual Ascendant's lord is not evaluated here.")
FITTING_INFORTUNE = False
SOFTENED_INFORTUNE = None

def fitting_infortune(ascendant_lon):
    """The malefic that rules the Ascendant sign, or None."""
    lord = SIGN_TO_DOMICILE.get(get_zodiac_sign(ascendant_lon))
    return lord if lord in INFORTUNES else None

def effective_infortunes():
    """INFORTUNES less the fitting one, when the switch has named it."""
    softened = reading('SOFTENED_INFORTUNE')
    return INFORTUNES - {softened} if softened else set(INFORTUNES)

# Gr. Intr. V.20, Figs. 60-61: bright/dusky/empty/dark degrees by
# sign, transcribed as (start, end, category) ranges (degree-in-sign, 0-29).
BRIGHTNESS_DEGREES = {
    'Aries':       [(0, 2, 'Dusky'), (3, 7, 'Dark'), (8, 15, 'Dusky'), (16, 19, 'Bright'), (20, 23, 'Dark'), (24, 28, 'Bright'), (29, 29, 'Dark')],
    'Taurus':      [(0, 2, 'Dusky'), (3, 9, 'Dark'), (10, 11, 'Empty'), (12, 19, 'Bright'), (20, 24, 'Empty'), (25, 27, 'Bright'), (28, 29, 'Dusky')],
    'Gemini':      [(0, 6, 'Bright'), (7, 9, 'Dusky'), (10, 14, 'Bright'), (15, 16, 'Empty'), (17, 22, 'Bright'), (23, 29, 'Dusky')],
    'Cancer':      [(0, 6, 'Dusky'), (7, 11, 'Bright'), (12, 13, 'Dusky'), (14, 17, 'Bright'), (18, 19, 'Dark'), (20, 27, 'Bright'), (28, 29, 'Dark')],
    'Leo':         [(0, 6, 'Bright'), (7, 9, 'Dusky'), (10, 15, 'Dark'), (16, 20, 'Empty'), (21, 29, 'Bright')],
    'Virgo':       [(0, 4, 'Dusky'), (5, 8, 'Bright'), (9, 10, 'Empty'), (11, 16, 'Bright'), (17, 20, 'Dark'), (21, 27, 'Bright'), (28, 29, 'Empty')],
    'Libra':       [(0, 4, 'Bright'), (5, 9, 'Dusky'), (10, 17, 'Bright'), (18, 20, 'Dusky'), (21, 27, 'Bright'), (28, 29, 'Empty')],
    'Scorpio':     [(0, 2, 'Dusky'), (3, 7, 'Bright'), (8, 13, 'Empty'), (14, 19, 'Bright'), (20, 21, 'Dark'), (22, 26, 'Bright'), (27, 29, 'Dusky')],
    'Sagittarius': [(0, 8, 'Bright'), (9, 11, 'Dusky'), (12, 18, 'Bright'), (19, 22, 'Dark'), (23, 29, 'Dusky')],
    'Capricorn':   [(0, 6, 'Dusky'), (7, 9, 'Bright'), (10, 14, 'Dark'), (15, 18, 'Bright'), (19, 20, 'Dusky'), (21, 24, 'Empty'), (25, 29, 'Bright')],
    'Aquarius':    [(0, 3, 'Dark'), (4, 8, 'Bright'), (9, 12, 'Dusky'), (13, 20, 'Bright'), (21, 24, 'Empty'), (25, 29, 'Bright')],
    'Pisces':      [(0, 5, 'Dusky'), (6, 11, 'Bright'), (12, 17, 'Dusky'), (18, 21, 'Bright'), (22, 24, 'Empty'), (25, 27, 'Bright'), (28, 29, 'Dusky')],
}

def _brightness_category(lon):
    sign = get_zodiac_sign(lon)
    degree_in_sign = int(lon % 30)
    for start, end, category in BRIGHTNESS_DEGREES[sign]:
        if start <= degree_in_sign <= end:
            return category
    return 'Bright'  # unreachable if the table is complete, kept as a safe default

# Gr. Intr. VII.6, 19-20: for the five non-luminaries, the domicile
# in which their nature is "moderated" (simply fortunate) versus the other,
# "contrary" domicile (suitable, but of a lesser grade).
PREFERRED_DOMICILE = {'Saturn': 'Aquarius', 'Jupiter': 'Sagittarius', 'Mars': 'Scorpio', 'Venus': 'Taurus', 'Mercury': 'Virgo'}

# Gr. Intr. VI.26, 3-4, Fig. 90: quadrants alternate Advancing/
# Masculine/Eastern vs. Withdrawing/Feminine/Western, diagonally opposite
# pairs sharing a designation. In Whole-Sign-House terms:
# Quadrant genders, VERIFIED against Abu Ma'shar's Figure 90 (VI.26, 3,
# "Advancing and withdrawing quadrants"). Reading the figure with the
# Ascendant on the left, the Midheaven on top and primary motion running
# clockwise, its four quadrants map onto house numbers as:
#
#   upper-left,  MC to Asc  = 10, 11, 12 -> Advancing, MALE, eastern, on right
#   upper-right, Desc to MC =  7,  8,  9 -> Withdrawing, FEMININE, western, on left
#   lower-left,  Asc to IC  =  1,  2,  3 -> Withdrawing, FEMININE, western, on left
#   lower-right, IC to Desc =  4,  5,  6 -> Advancing, MALE, eastern, on right
#
# which is exactly the assignment below. It was previously carried as an
# unsourced convention, because VII.6, 28 names the quadrants but defines
# them at IV.8, 16, outside this corpus, and the figure's image was
# missing until it was rephotographed.
MASCULINE_QUADRANT_HOUSES = {4, 5, 6, 10, 11, 12}
FEMININE_QUADRANT_HOUSES = {1, 2, 3, 7, 8, 9}
QUADRANT_GENDER_READER = ("Quadrant gender follows the planet's position between the four angular degrees; the "
                            "five-degree allowance counts it as angular for strength without changing its quadrant.")
SUN_NINTH_EXCEPTION_READER = ("This exception uses the ninth quadrant division before the angular-strength "
                               "allowance, following VI.26's local house definitions.")

# RESOLVED (audit of 2026-09-06). Fig. 90 also labels the two masculine
# quadrants ADVANCING and the two feminine ones WITHDRAWING, which looked
# like a conflict with Sahl 83 (angular or succedent: 1, 2, 4, 5, 7, 8, 10,
# 11). It is not one; they are two different conditions sharing an English
# word.
#
# The reshoot of Fig. 90 is rotated 90 degrees, and its right margin carries
# the line-beginnings of VI.26, 3, which read (top to bottom): "3 Now |
# rants whic | Ascendan | heaven, | setting to | called "a | eastern, |
# while th | which ar | to the set | fourth t | are calle | feminine |
# left." 4 (" -- i.e. the quadrants from the Ascendant to midheaven and
# from the setting to the fourth are called "advancing ... eastern", those
# from the midheaven to the setting and from the fourth to the Ascendant
# "... feminine ... left". The figure's dashed arrows run ASC -> MC and
# DSC -> IC: primary motion carrying a planet TOWARD THE MERIDIAN. Dykes
# states the same criterion at On Nativities 10.3 fn. 231: "the eastern
# quarter from the Ascendant to the Midheaven is advancing, while the
# quarter from the Midheaven to the Descendant is declining or retreating."
#
# Abu Ma'shar lists the two as separate conditions himself -- VII.3, 2-5:
# "The first is if one is in the advancing and withdrawing quadrants of the
# circle. The second is if it is in one of the houses ... which are the
# stakes. The third ... follows a stake. The fourth ... the withdrawing
# houses." Sahl defines HIS word at Ch.3, 4, "advancement, it is if a
# planet was in a stake or what follows a stake", and his Figure 9 shades
# exactly sectors 1, 2, 4, 5, 7, 8, 10, 11 with the axes as boundaries. So
# 83 is right as implemented, and the quadrant set below is Abu Ma'shar's
# own condition (VII.3, 2), recorded for when it is scored in its own right.
#
# Sahl knew the quadrant scheme too, and names a rival to it. On Times
# Ch. 1, 10-11 gives the quadrants -- "a planet is in what is between the
# Ascendant and the Midheaven, and what is opposite that (and that is what
# is between the setting and the stake of the earth): and this position of
# the circle indicates quickness ... [the other two] slowness and delay,
# ACCORDING TO THE STATEMENT OF THE ANCIENTS" -- then 12-13 gives
# Masha'allah's HEMISPHERES, "he differs from them in that: he makes the
# quick place of the circle be what begins its rise ... (and it is what is
# from the degree of the fourth), [then] turned back towards the Midheaven",
# and 14 takes Masha'allah's side: "And that is the closest of the two
# statements, in [its] analogy" (fn. 7: "most appropriate, most resembling
# the truth"). Figure 44 draws both wheels. Sahl then USES the hemisphere
# division in another work, On Choices Ch. 6, 16-17: treat from the head
# to the navel "when the Moon is in what is between the stake of the earth,
# so rising up to the Midheaven ... the highest region of the circle", and
# below the navel when she is "between the tenth, declining towards the
# stake of the earth ... the lowest part of the circle". (Ch. 6, 30's
# "declining from the Midheaven toward the stake of the earth" shares the
# vocabulary only.)
#
# None of that moves 83 or the set below. On Times Ch. 1 is about the
# QUICKNESS OF TIMING and On Choices Ch. 6 about a bodily correspondence;
# neither says the hemisphere division governs advancing-as-strength, so
# it is recorded here as a third scheme in the sources, not scored. Where
# the 2026-09-07 review (process/tae_docs/synthesis/05, Tier 3) and the report it reviewed
# (process/tae_docs/synthesis/04, A4) differ, it is only on whether Ch. 6, 30 counts as a
# use of the scheme; this comment follows the review and cites 16-17 alone.
#
# And Sahl's OWN natal timing uses the quadrants that On Times 1, 14 sets
# aside: On Nativities 7.4, 17 (marriage: the Moon "in the two eastern
# quarters, in what is between the Ascendant and the Midheaven, and what is
# opposite that"), 5.3, 11-12 (children), and a clockwise scheme for chronic
# illness at 6.5, 1. So "Sahl prefers Masha'allah's hemispheres" is true of
# On Times 1 alone; any future build of those three rules uses the scheme its
# own sentence names (order DIS-9, 2026-09-11). Nothing here is scored.
ADVANCING_BY_QUADRANT_FIG90 = {4, 5, 6, 10, 11, 12}   # Gr. Intr. VII.3, 2 / VI.26, 3; not Sahl 83

# Approximate geocentric distance range (AU) per planet, used only as a
# modern proxy for "rising up in the circle of their apogee" (VII.6, 23) --
# swisseph doesn't expose the classical deferent/epicycle apogee for
# non-lunar bodies, so this substitutes "farther from Earth than usual" for
# the true Ptolemaic concept. An approximation, flagged as such.
GEOCENTRIC_DISTANCE_RANGE = {
    'Moon': (0.0024, 0.0027), 'Mercury': (0.53, 1.45), 'Venus': (0.27, 1.73),
    'Sun': (0.983, 1.017), 'Mars': (0.37, 2.68), 'Jupiter': (3.95, 6.45), 'Saturn': (8.0, 11.1),
}

PLANET_SWE_IDS = {'Sun': swe.SUN, 'Moon': swe.MOON, 'Mercury': swe.MERCURY,
                   'Venus': swe.VENUS, 'Mars': swe.MARS, 'Jupiter': swe.JUPITER, 'Saturn': swe.SATURN}

def _dispositors(lon, sect):
    """The rulers of all five essential dignities at a degree -- used for
    the Received test (VII.6, 12)."""
    rulers = get_essential_rulers(lon)
    triplicity_key = 'triplicity_day' if sect == 'Diurnal' else 'triplicity_night'
    return {rulers['domicile'], rulers['exaltation'], rulers[triplicity_key], rulers['term'], rulers['face']} - {'-'}

def _in_last_bound(lon):
    """Whether the degree lies in its sign's final Egyptian bound, which the
    table makes an infortune's in every sign -- the test both Moon lists
    share (Sahl Ch.3, 108; VII.6, 72)."""
    terms = EGYPTIAN_TERMS.get(get_zodiac_sign(lon), [])
    return bool(terms) and (lon % 30.0) >= (terms[-2][0] if len(terms) > 1 else 0)

def _current_bound_width(lon):
    """The width in degrees of the Egyptian bound the degree falls in --
    the measure VII.6, 48 uses for "less than the bound of [a single]
    planet between them and the infortunes"."""
    sign = get_zodiac_sign(lon)
    degree_in_sign = lon % 30
    lower = 0.0
    for limit, _lord in EGYPTIAN_TERMS.get(sign, []):
        if degree_in_sign < limit:
            return float(limit) - lower
        lower = float(limit)
    return 30.0 - lower

# VII.6, 58 says an encloser counts as present in the 2nd or 12th sign "by
# its body or rays." True (the default) is the literal text. False selects
# this project's own conservative variant -- see the note in
# _abu_mashar_enclosed().
SIGN_ENCLOSURE_BODIES_ONLY = False

def _ray_degrees(lon):
    """A planet's body plus the degrees into which it casts its rays."""
    return [(lon + a) % 360.0 for a in (0.0, 60.0, 90.0, 120.0, 180.0, 240.0, 270.0, 300.0)]

def _abu_mashar_enclosed(planet, enclosing_set, planetary_data, rows, orb=7.0):
    """Enclosure per Abu Ma'shar's own VII.6, 56-62 -- which is NOT simply
    Sahl's concept borrowed. He gives two types of his own:

    Type 1 by degree (57): the planet has one enclosing planet's body OR
    RAYS within orb in front of it and the other's behind it; or it is
    separating from one by assembly or aspect and connecting with the
    other (that second form IS Sahl's mechanism). The 7-degree orb is from
    the note on 57: "the rays or bodies of the enclosing planets must be 7
    degrees or less on either side of the enclosed planet."

    Confirmed against Figure 145: the Moon at 13 Libra is besieged by
    Saturn's opposition ray from 18 Aries (5 degrees ahead) and Mars's
    sextile ray from 10 Leo (3 degrees behind).

    Type 2 by sign (58): one enclosing planet, by body OR RAYS, is in the
    SECOND sign from the planet and the other is in the twelfth. Taken
    literally this fires on about 43 percent of placements, since a planet's
    rays reach eight of the twelve signs; SIGN_ENCLOSURE_BODIES_ONLY selects
    a bodies-only variant at about 2 percent. The literal text is the
    default -- a rule being common is not evidence that it was meant to be
    rare.

    Dissolution (60-61): "if the Sun or one of the fortunes looked at the
    enclosed planet, and there was less than 7 degrees between the planet
    and those rays, then it indicates the dissolving of that misfortune."
    An earlier version of this project removed a dissolution rule as an
    unsourced invention. It is sourced -- this is it.

    Returns (enclosed, kind, dissolved_by) with dissolved_by naming the
    planet whose rays break it, or None."""
    members = [m for m in enclosing_set if m != planet and m in planetary_data]
    if len(members) != 2:
        return False, None, None
    lon = planetary_data[planet]['longitude']
    a_rays = _ray_degrees(planetary_data[members[0]]['longitude'])
    b_rays = _ray_degrees(planetary_data[members[1]]['longitude'])

    # 57 confines the degree type to the planet's OWN SIGN: "a planet is in
    # a sign, and WITH IT IN ITS SIGN is an infortune or its rays in front
    # of it, and an infortune or its rays behind it." The windows below were
    # pure degree arithmetic and so reached across the sign boundary, where
    # 20% of type-1 hits were coming from. The by-sign type at 58 is the
    # construction that is allowed to look outside the sign, and it does so
    # on its own terms.
    own_sign = int(lon // 30)

    def ahead(rays):
        return any(0.0 < (d - lon) % 360.0 <= orb and int(d // 30) == own_sign
                    for d in rays)

    def behind(rays):
        return any(0.0 < (lon - d) % 360.0 <= orb and int(d // 30) == own_sign
                    for d in rays)

    kind = None
    if (ahead(a_rays) and behind(b_rays)) or (ahead(b_rays) and behind(a_rays)):
        kind = 'by degree (57)'
    else:
        # The second form of 57: separating from one, connecting with the
        # other -- Sahl's own shape, which Abu Ma'shar folds in here.
        for sep_t, con_t in ((members[0], members[1]), (members[1], members[0])):
            sep = any((r['applicant'] or r['light_name']) == planet and (r['receiver'] or r['heavy_name']) == sep_t
                       and _is_separating_in_nature(r) for r in rows)
            con = any((r['applicant'] or r['light_name']) == planet and (r['receiver'] or r['heavy_name']) == con_t
                       and r['motion'] == 'Applying' and _is_connected(r) for r in rows)
            if sep and con:
                kind = 'separating/connecting (57)'
                break
    if kind is None:
        # Type 2 by sign (58): one encloser in the second sign from the
        # planet "by its body or rays," the other in the twelfth.
        #
        # A previous pass restricted this to BODIES because the literal
        # reading fires on 41-44% of placements over 3,654 planet-checks
        # (a planet's rays reach eight of the twelve signs), against
        # 1.4-2.5% for bodies alone. That frequency argument is real but it
        # is not textual authority, and the text says "or rays" twice. The
        # literal reading is the default; SIGN_ENCLOSURE_BODIES_ONLY selects
        # the conservative variant, which is this project's own and is
        # labelled as such wherever it is shown.
        sign_idx = int(lon // 30)
        second = (sign_idx + 1) % 12
        twelfth = (sign_idx - 1) % 12
        if SIGN_ENCLOSURE_BODIES_ONLY:
            a_signs = {int(planetary_data[members[0]]['longitude'] // 30)}
            b_signs = {int(planetary_data[members[1]]['longitude'] // 30)}
            variant = 'bodies only, project variant'
        else:
            a_signs = {int(d // 30) for d in a_rays}
            b_signs = {int(d // 30) for d in b_rays}
            variant = 'body or rays'
        if ((second in a_signs and twelfth in b_signs)
                or (second in b_signs and twelfth in a_signs)):
            kind = f'by sign, 2nd and 12th, {variant} (58)'
    if kind is None:
        return False, None, None

    # 60: "in BOTH TYPES, if the Sun or one of the fortunes looked at the
    # enclosed planet, and there was less than 7 degrees between the planet
    # and those rays, then it indicates the dissolving of that misfortune."
    # Dykes' note on 60 restricts it: "Actually this only refers to the
    # degree-based type of enclosure, in 56."
    #
    # Both are honoured, because the disagreement is about the MEASURE, not
    # about whether a sign-based enclosure can be dissolved at all -- 61
    # gives a dissolution in the same breath for an enclosed sign, with no
    # degree condition ("if the enclosed thing was itself a sign, and the
    # fortunes or Sun looked at it, they will dissolve that misfortune").
    # So the degree type takes 60's 7-degree ray test, and the sign type
    # takes 61's bare look. Applying the 7-degree test to the sign type, as
    # an earlier version did, is the one reading the note rules out.
    by_sign = kind is not None and 'by sign' in kind
    for breaker in ({'Sun'} | FORTUNES) - set(members) - {planet}:
        if breaker not in planetary_data:
            continue
        breaker_lon = planetary_data[breaker]['longitude']
        if by_sign:
            raw = abs(int(breaker_lon // 30) - own_sign)
            if min(raw, 12 - raw) not in AVERSION_SIGN_COUNTS:
                return True, kind, breaker
        elif any(abs(((d - lon + 180.0) % 360.0) - 180.0) < orb
                  for d in _ray_degrees(breaker_lon)):
            return True, kind, breaker
    return True, kind, None

def _sahl_enclosed(planet, enclosing_set, rows, blocking_pairs):
    """Enclosure (Sahl, The Introduction Ch.3, 119-123, Fig. 25). The
    planet is separating from ONE member of enclosing_set and connecting
    with the OTHER, with neither leg itself intercepted by a third
    planet's rays -- "without another planet casting its rays between the
    two" (121), implemented by excluding either leg if it's already
    Blocked (evaluate_blocking()'s own output, passed in as
    blocking_pairs to avoid recomputing it per planet). Graded "more
    powerful and more unfortunate" when both legs are within 7 degrees of
    exact (121, confirmed against Fig. 25's own numbers: Moon separating
    from Mars by square, connecting to Saturn by opposition).

    Replaces an earlier version built from Abu Ma'shar's own VII.6, 56-59
    (a same-sign front/behind Connected test plus a separate sign-based
    2nd/12th-house test) which turned out not to match this mechanism at
    all -- Abu Ma'shar reuses Sahl's own concept rather than defining a
    new one.

    Returns (is_enclosed, is_severe, separating_from, connecting_to), or
    (False, False, None, None)."""
    members = [m for m in enclosing_set if m != planet]
    if len(members) != 2:
        return False, False, None, None
    for sep_target, con_target in ((members[0], members[1]), (members[1], members[0])):
        # The separating leg is NOT gated on the connection still being
        # live. 121 gives the base condition as "separating from one of
        # them [and] connecting with the other, without another planet
        # casting its rays between the two", and makes 7 degrees the
        # SEVERE grade, not the entry condition. Sahl's own Fig. 25 proves
        # it: "Mars is in 10 degrees of Cancer, and Saturn in 18 degrees of
        # Aries, and the Moon in 13 degrees of Libra. So the Moon is
        # separating from Mars from [his] second square, connecting with
        # Saturn from the opposition: at this time she is enclosed" (122).
        # She is 3 degrees past Mars ACROSS A SIGN BOUNDARY, and 9's
        # cross-sign separation window is a single degree -- so requiring
        # _is_connected here made the chapter's own worked figure return
        # nothing, and suppressed 82% of enclosures besides.
        #
        # This is the same distinction the Transfer evaluator already draws
        # for its own past leg (see its docstring): separating needs a
        # valid configuration and past-exact motion, not the tighter
        # Connected threshold.
        sep_row = next((r for r in rows if r['aspect_name'] != 'Aversion'
                         and (r['applicant'] or r['light_name']) == planet and (r['receiver'] or r['heavy_name']) == sep_target
                         and _sahl_has_separated(r)), None)
        con_row = next((r for r in rows if r['aspect_name'] != 'Aversion'
                         and (r['applicant'] or r['light_name']) == planet and (r['receiver'] or r['heavy_name']) == con_target
                         and r['motion'] == 'Applying' and _is_connected(r)), None)
        if not sep_row or not con_row:
            continue
        if (planet, sep_target) in blocking_pairs or (planet, con_target) in blocking_pairs:
            continue
        severe = abs(sep_row['deviation']) <= 7.0 and abs(con_row['deviation']) <= 7.0
        return True, severe, sep_target, con_target
    return False, False, None, None


def _third_day_intervening_ray(planetary_data, rows, separating_from, connecting_to):
    """Name an extra planet whose aspect ray lies between an enclosure's legs.

    This is the complete ray clause of Sahl, Introduction Ch. 3, 121,
    applied only to F10's third-day Saturn/Mars profile. ``_sahl_enclosed``
    deliberately keeps the established generic connection/blocking
    behavior used elsewhere. Each selected row's whole-sign aspect chooses
    one angle through ``ASPECT_BY_SIGN_COUNT``; of that aspect's two
    directed rays, the one nearest the Moon is the exact malefic endpoint.
    This direction cannot be recovered from ``deviation``, which records
    unsigned angular distance minus the aspect angle. Every sextile, square,
    trine, or opposition ray cast by another planet is then tested strictly
    between those endpoints. The intervening planet's body is not itself a
    ray, and either endpoint belongs to the enclosing malefic rather than
    lying between them.
    """
    moon_lon = planetary_data['Moon']['longitude'] % 360.0

    def leg(target, motion):
        return next((row for row in rows
                     if row['aspect_name'] != 'Aversion'
                     and (row['applicant'] or row['light_name']) == 'Moon'
                     and (row['receiver'] or row['heavy_name']) == target
                     and row['motion'] == motion), None)

    separating = leg(separating_from, 'Separating')
    connecting = leg(connecting_to, 'Applying')
    if separating is None or connecting is None:
        return None

    def offset(longitude):
        return ((longitude - moon_lon + 180.0) % 360.0) - 180.0

    def selected_ray(row, target):
        _aspect_name, angle = ASPECT_BY_SIGN_COUNT[row['signs_apart']]
        target_lon = planetary_data[target]['longitude'] % 360.0
        candidates = ((target_lon + angle) % 360.0,
                      (target_lon - angle) % 360.0)
        return min(candidates, key=lambda ray: abs(offset(ray)))

    first = offset(selected_ray(separating, separating_from))
    second = offset(selected_ray(connecting, connecting_to))
    low, high = sorted((first, second))
    excluded = {'Moon', separating_from, connecting_to}
    for other in PLANET_SWE_IDS:
        if other in excluded or other not in planetary_data:
            continue
        data = planetary_data[other]
        # _ray_degrees begins with the body's own degree; 121 asks for a
        # planet casting a ray, so only its aspect directions count.
        for ray in _ray_degrees(data['longitude'])[1:]:
            if low < offset(ray) < high:
                return other
    return None

@_connection_aware
def evaluate_enclosure(planetary_data):
    """Enclosure (Sahl, The Introduction Ch.3, 119-123, Fig. 25): every
    planet enclosed between the two infortunes (misfortune), or the two
    fortunes (Abu Ma'shar's own extension of the same shape to a good-
    fortune direction, Gr. Intr. VII.6, 5)."""
    with doctrine(SAHL):
        rows = _pairwise_configurations(planetary_data)
        blocking_pairs = {(row['Blocked'], row['From Reaching']) for row in evaluate_blocking(planetary_data)}
        results = []
        for planet in planetary_data:
            if planet == 'North Node':
                continue
            for label, enclosing_set in (('Infortunes', effective_infortunes()), ('Fortunes', FORTUNES)):
                is_enc, severe, sep, con = _sahl_enclosed(planet, enclosing_set, rows, blocking_pairs)
                if is_enc:
                    results.append({
                        'Planet': planet, 'Enclosed By': label,
                        'Separating From': sep, 'Connecting To': con,
                        'Severity': 'More powerful/unfortunate (within 7°)' if severe else 'Standard',
                    })
        return results

FIXED_SIGNS = {'Taurus', 'Leo', 'Scorpio', 'Aquarius'}

# Sahl's narrow burned path, "the end of Libra and the beginning of Scorpio"
# (Ch.3, 110, with the 19 Libra to 3 Scorpio band footnoted there), which
# VII.6, 40 also uses as its harsh band inside the two whole signs.
HARSH_BURNED_PATH = (199.0, 213.0)

# Traditional planetary gender, for Strength/Weakness (87). Mercury is
# common/neutral and Sahl's own text doesn't address it here, so it's left
# unassigned rather than guessed.
PLANET_GENDER = {'Sun': 'Masculine', 'Moon': 'Feminine', 'Mercury': None,
                  'Venus': 'Feminine', 'Mars': 'Masculine', 'Jupiter': 'Masculine', 'Saturn': 'Masculine'}

def _averse_to_ascendant(lon, ascendant_lon):
    """Whether a degree's sign is in Aversion to the Ascendant's sign (2nd/
    12th or 6th/8th from it, Sahl, The Introduction Ch.2, 50-60) -- the
    planet-to-point analogue of the Aversion test _pairwise_configurations()
    already applies planet-to-planet."""
    sign_idx = SIGN_ORDER.index(get_zodiac_sign(lon))
    asc_idx = SIGN_ORDER.index(get_zodiac_sign(ascendant_lon))
    raw_apart = abs(sign_idx - asc_idx)
    signs_apart = min(raw_apart, 12 - raw_apart)
    return signs_apart in AVERSION_SIGN_COUNTS

# Sahl, The Introduction Ch.3, 78: the stakes and what follows them, but
# only "of the places which look at the Ascendant" -- which drops the 2nd
# and 8th (both in aversion to the Ascendant), leaving six. The author's
# own footnote to 78 confirms the count: "the definition here allows only
# six good places." Distinct from bare advancement (4, 83), which is every
# stake and succeedent place with no visibility qualifier.
EXCELLENT_PLACES = {1, 4, 5, 7, 10, 11}

# --- The good-place schemes, each labelled for what it is FOR (C-09 / D-9)
# Eight places, "for a planet in itself" (glossary p. 771): "Four of them
# are called the 'stakes' ... 33 four of them are said to be what follows
# the stakes ... 35 four of them are said to be falling from the stakes"
# (Introduction Ch. 2, 31-36, Fig. 5). Seven praised places, ranked, "for
# the native": Ascendant, Midheaven, seventh, fourth, eleventh, ninth,
# fifth (Ch. 2, 37-44, Figs. 6-7). Six excellent places: EXCELLENT_PLACES
# above (Ch. 3, 78). And Choices Ch. 9, 12: "in an excellent place, in the
# Ascendant, eleventh, or tenth" -- corroborating the glossary's hedge and
# demoting the 7th and 4th. Never merged into one house-strength number.
GOOD_PLACE_SCHEMES = {
    'Eight places, for a planet in itself (Intro Ch. 2, 31-36)': {
        'stakes': [1, 10, 7, 4], 'what follows the stakes': [2, 5, 8, 11], 'falling from the stakes': [3, 6, 9, 12]},
    'Seven praised places, ranked, for the native (Intro Ch. 2, 37-44)': [1, 10, 7, 4, 11, 9, 5],
    'Six excellent places (Intro Ch. 3, 78)': sorted(EXCELLENT_PLACES),
    'Excellent places for the Sun (Choices Ch. 9, 12)': [1, 11, 10],
}
# The tail of the ranking is a conflation (fn. 42): manuscript B reads
# "... 11, 5, 9"; H and L read "... 11, 9, 5"; the printed text takes H/L's
# order plus B's note that the ninth is the Sun's joy (42, itself B-only).
# Kept as printed, labelled (decision D-9, low-medium confidence).
SEVEN_PLACE_RANKING_NOTE = ("Printed order (manuscripts H and L: ... 11, 9, 5). Manuscript B reads ... 11, 5, 9; "
                            "the printed text takes H/L's order plus B's note that the ninth is the Sun's joy "
                            "(Introduction Ch. 2, 42, fn 42) -- Dykes's conflation, kept as printed.")

def _fact(name, value):
    """One fact of a testimony's row detail, "<name>: <value>", from a value
    the evaluator already holds when it decides the testimony. Booleans
    print yes/no, degrees to two places with the unrounded value beside them, sets in zodiac or numeric order,
    an empty collection or None as "none". The name never contains ": ",
    which is where the page splits it into Fact and Value."""
    if isinstance(value, bool):
        text = 'yes' if value else 'no'
    elif value is None:
        text = 'none'
    elif isinstance(value, float):
        text = f'{value:.2f} (unrounded: {value!r})'
    elif isinstance(value, (set, frozenset, list, tuple)):
        items = list(value)
        if not items:
            text = 'none'
        elif all(isinstance(v, str) and v in SIGN_ORDER for v in items):
            text = ', '.join(sorted(items, key=SIGN_ORDER.index))
        elif all(isinstance(v, int) for v in items):
            text = ', '.join(str(v) for v in sorted(items))
        else:
            text = ', '.join(str(v) for v in items)
    else:
        text = str(value)
    return f'{name}: {text}'


@_connection_aware
def evaluate_strength_of_planets(planetary_data, essential, accidental, ascendant_lon, sect, natal_houses):
    """Strength of the Planets (Sahl, The Introduction Ch.3, 78-88): the
    eleven testimonies of a planet's strength at the time of judgment that
    77 announces, cross-checked against the author's own summary table
    (Fig. 24). An earlier version found only ten, having misread 87 (the
    tenth, "in the heart of the Sun") and renumbered 88 (the eleventh, the
    gender-matching quadrant and sign) as the tenth; the count discrepancy
    against 77 was noted at the time but resolved the wrong way.

    Note that 78 and 83 are NOT the same test, though an earlier version
    computed both from the same eight whole-sign houses. 78's "excellent
    place" is whole-sign and is narrowed by which places LOOK at the
    Ascendant (six of them, per its own footnote). 83's "advancing" is
    dynamic -- measured against the quadrant cusps, per the note on 83 and
    Sahl's glossary (Vol. I p. 771, Advancement: "dynamically angular or
    succeedent, i.e. moving by primary motion toward an axial degree"; the
    course glossary has the same entry) -- and takes this function's
    natal_houses argument.
    On a sample of 414 charts the two readings of advancement disagree for
    a third of all planet placements.

    Distinct from the existing Gr. Intr. VII.6-based Planetary Condition
    table, which scores a broader, differently-sourced strength/weakness
    scheme (21-46) built earlier in this project -- this is Sahl's own,
    narrower list, kept as its own table rather than merged into a later
    author's scheme (the project's standing practice: retain the original
    source's own definition when two sources diverge)."""
    with doctrine(SAHL):
        rows = _pairwise_configurations(planetary_data)
        results = []
        for planet, data in planetary_data.items():
            if planet == 'North Node':
                continue
            lon = data['longitude']
            sign = get_zodiac_sign(lon)
            house = get_wsh_house(lon, ascendant_lon)
            # 83 is measured against the real angular axes rather than by sign
            # -- see the comment there. Both readings of the quadrant place are
            # kept: strict cusp membership, and the same with Sahl's own
            # five-degree carryover (Fifty Aphorisms #44, 87-89; On Nativities
            # Ch.1.22, 9). A planet a few degrees short of an angle is not
            # "falling from the stake" by either of those passages.
            quadrant_position = quadrant_strength_position(lon, natal_houses)
            quadrant_house_strict = quadrant_position['division']
            quadrant_house = quadrant_position['strength_house']
            ess, acc = essential[planet], accidental[planet]
            labels = []
            # Each label also as a structure -- the paragraph number, the
            # label's own words, and the chart values the branch tested --
            # for the row detail under the grid. The label list is what it
            # was; the structure restates it and adds no test.
            testimonies = []

            def _add(n, label, *facts):
                labels.append(label)
                testimonies.append({'n': n, 'sentence': label, 'facts': list(facts)})

            # (78) In an excellent place: a stake or what follows one, limited
            # to those that look at the Ascendant (see EXCELLENT_PLACES). An
            # earlier version used all eight angular+succeedent houses, which
            # wrongly admitted the 2nd and 8th.
            if house in EXCELLENT_PLACES:
                _add('78', 'In an excellent place from the Ascendant (78)',
                     _fact('Whole-sign place', house), _fact('Excellent places', EXCELLENT_PLACES))

            # (79) In something of its own share: house, exaltation, triplicity, bound, face, or joy.
            if ess['Domicile'] or ess['Exalt'] or ess['Triplicity'] or ess['Term'] or ess['Face'] or acc['Joy']:
                _add('79', 'In its own share of dignity (79)',
                     _fact('Share of dignity', [k for k in ('Domicile', 'Exalt', 'Triplicity', 'Term', 'Face') if ess[k]]
                           + (['Joy'] if acc['Joy'] else [])))

            # (80) Direct in course.
            if not acc['Retrograde']:
                _add('80', 'Direct in course (80)',
                     _fact('Retrograde (accidental)', acc['Retrograde']),
                     _fact('Speed in longitude', data['speed_in_lon']))

            # (81) No infortune "with it in its sign, connecting with it, or
            # looking at it FROM A SQUARE OR OPPOSITION" -- Fig. 24 renders
            # this as "not in whole-sign angles of infortune," so assembly,
            # square and opposition count and sextile/trine do not. (An
            # earlier version counted any non-Aversion configuration.)
            # The infortune must be the OTHER planet in the pair. Testing
            # "either member is an infortune" made Mars and Saturn fail 81
            # against any partner at all, since each is itself an infortune
            # -- 59% of their placements lost a testimony Sahl grants them.
            infortune_contact = any(
                r['aspect_name'] in ('Conjunction', 'Square', 'Opposition')
                and (({r['p1'], r['p2']} - {planet}) & effective_infortunes())
                for r in rows if planet in (r['p1'], r['p2'])
            )
            if not infortune_contact:
                _others81 = effective_infortunes() - {planet}
                _add('81', 'Not in the whole-sign angles of an infortune (81)',
                     _fact('Infortunes', sorted(_others81)),
                     *[_fact(f'Configuration with {_inf}', _r['aspect_name'])
                       for _inf in sorted(_others81)
                       for _r in rows if {_r['p1'], _r['p2']} == {planet, _inf}])

            # (82) Not connecting with a planet falling from the ASC or in its
            # own fall, and not itself in its own fall.
            connects_weak_target = False
            _seen82 = []
            for r in rows:
                if r['aspect_name'] == 'Aversion' or not _is_connected(r) or planet not in (r['light_name'], r['heavy_name']):
                    continue
                other = r['heavy_name'] if r['light_name'] == planet else r['light_name']
                other_house = get_wsh_house(planetary_data[other]['longitude'], ascendant_lon)
                other_sign = get_zodiac_sign(planetary_data[other]['longitude'])
                _seen82.append((other, other_house, other_sign))
                # Sahl's "falling away from the Ascendant" is AVERSION, not
                # cadency. Sahl's glossary (Vol. I p. 774, Cadent) says so:
                # "3rd, 6th, 9th, 12th. But see also FALLING AWAY FROM, WHICH
                # IS EQUIVALENT TO AVERSION" -- and its Aversion entry is the
                # 2nd, 6th, 8th and 12th (the Course Glossary carries the same
                # two entries verbatim). Sahl keeps the two apart himself at
                # 91, "falling from the stakes AND not looking at the
                # Ascendant: and that is in the sixth and the twelfth", which
                # this file already reads correctly. Reading it as cadency
                # admits the 3rd and 9th, which DO look at the Ascendant, and
                # misses the 2nd and 8th, which do not.
                if _averse_to_ascendant(planetary_data[other]['longitude'], ascendant_lon) \
                        or other_sign in FALLS.get(other, []):
                    connects_weak_target = True
                    break
            if not connects_weak_target and not ess['Fall']:
                _add('82', 'Not connecting with a falling/fallen planet, nor itself in its fall (82)',
                     _fact('Connected with', [o for o, _, _ in _seen82]),
                     *[_fact(o, f'whole-sign place {h}, {s}') for o, h, s in _seen82],
                     _fact('Fall (essential)', ess['Fall']))

            # (83) Advancing -- measured DYNAMICALLY, against the quadrant
            # cusps, not by whole sign. The note on 83 is explicit that the
            # word is the active participle of Form IV and so "means that it
            # is dynamically angular or succeedent, i.e. by primary motion
            # with respect to the angular axes, and not by whole sign"; Sahl's
            # own Figure 9 for 4-5 is captioned "understood dynamically," and
            # Sahl's glossary (Vol. I p. 771, Advancement) defines it as
            # "dynamically angular or succeedent, i.e. moving by primary
            # motion toward an axial degree" -- the course glossary's entry
            # is the same text.
            #
            # Primary motion carries a planet 1 -> 12 -> 11 -> 10: out of an
            # angle into the cadent house, moving AWAY from the axis it just
            # crossed, then into the succedent house, approaching the next
            # one. So "dynamically angular or succeedent" is the same set of
            # house numbers as the whole-sign test uses -- the difference is
            # entirely in which house system assigns the number, and on a
            # typical chart it moves most of the planets.
            #
            # Note this is deliberately NOT the same predicate as 78's
            # "excellent place", which is whole-sign: that one is defined by
            # which places LOOK at the Ascendant, and its own footnote counts
            # the six resulting places.
            if quadrant_house in ANGLE_HOUSES | SUCCEDENT_HOUSES:
                _facts83 = [_fact('Quadrant division', quadrant_house),
                            _fact('Angular and succedent divisions', ANGLE_HOUSES | SUCCEDENT_HOUSES)]
                if quadrant_house != quadrant_house_strict:
                    _add('83', f'Advancing, by the five-degree rule into the {HOUSE_ORDINAL[quadrant_house]} (83)',
                         *_facts83, _fact('Quadrant division, strict', quadrant_house_strict))
                else:
                    _add('83', 'Advancing (83)', *_facts83)

            # (84) A masculine planet (Saturn, Jupiter, Mars) eastern, arising at dawn.
            # 84 is "eastern, ARISING AT DAWN" -- visible, not merely on the
            # eastern side. Sahl gives the floors himself in On Nativities 1.22,
            # 1: with 6 degrees between Saturn or Jupiter and the Sun "they are
            # considered to be eastern ... BUT IF THEY WERE LESS THAN THAT, THEY
            # WILL NOT BE FIT"; Mars at 15 degrees (1.22, 3 and its note).
            # Sahl's glossary (Vol. I p. 778, Eastern and western) sense (2)
            # is "to be outside the Sun's rays and visible (eastern) or under
            # them and invisible (western)"; the Course Glossary's Eastern (2)
            # is the same entry. An earlier version tested only which side of the Sun the
            # planet stood on, so 11.5% of eastern superiors took this testimony
            # while burned or under the rays.
            if planet in ('Saturn', 'Jupiter', 'Mars'):
                sun_lon = planetary_data['Sun']['longitude']
                signed_from_sun = ((lon - sun_lon + 180.0) % 360.0) - 180.0
                visible_floor = 15.0 if planet == 'Mars' else 6.0
                if signed_from_sun < 0 and abs(signed_from_sun) >= visible_floor:
                    # Not "risen": 6 degrees is On Nativities 1.22, 1's nine-day
                    # allowance ("considered to be eastern"), inside the 15
                    # degrees this same table calls under the rays (93). Mars's
                    # 15 is Dykes' inference at 1.22 fn. 174, not text.
                    _add('84', f'Masculine planet, eastern, {visible_floor:.0f}+ deg from the Sun '
                               f'(84; "considered eastern", On Nativities 1.22, 1)',
                         _fact('Signed distance from the Sun (negative is eastern)', signed_from_sun),
                         _fact('Visible floor', visible_floor))

            # (85) "In their own glow: that is, a masculine planet in the day,
            # and a feminine planet in the night." The translator's footnote
            # calls the gendered wording an error for DIURNAL/NOCTURNAL (Mars
            # being masculine but nocturnal), and Fig. 24 renders the row as
            # simply "of the sect" -- so this is bare sect agreement. An
            # earlier version reused accidental[]['Hayz'], which additionally
            # demands the right side of the horizon and a sign of matching
            # gender, and so under-reported the testimony.
            planet_is_diurnal = planet_sect_is_diurnal(planet, lon, planetary_data['Sun']['longitude'])
            if isinstance(planet_is_diurnal, UnresolvedResult):
                testimonies.append({
                    'n': '85',
                    'sentence': planet_is_diurnal,
                    'facts': [_fact('Sect', sect), 'Native indication: diurnal'],
                    'result': planet_is_diurnal,
                })
            elif planet_is_diurnal == (sect == 'Diurnal'):
                _add('85', 'In its own glow, i.e. of the sect (85)',
                     _fact('Planet is diurnal', planet_is_diurnal), _fact('Sect', sect))

            # (86) In a fixed sign.
            if sign in FIXED_SIGNS:
                _add('86', 'In a fixed sign (86)', _fact('Sign', sign), _fact('Fixed signs', FIXED_SIGNS))

            # (87) In the heart of the Sun -- "when they are with him in one
            # degree." Sahl's own one-degree window, not the later 16-17
            # arcminute cazimi convention that accidental[]['Cazimi'] uses, so
            # it is measured here rather than reusing that flag.
            if planet != 'Sun':
                sun_lon = planetary_data['Sun']['longitude']
                if in_solar_heart(planet, lon, sun_lon, source='Sahl'):
                    _add('87', 'In the heart of the Sun (87)',
                         _fact('Distance from the Sun', abs(((lon - sun_lon + 180.0) % 360.0) - 180.0)),
                         _fact('Window', 1.0))

            # (88) Masculine/feminine QUADRANT and sign matching the planet's
            # own gender. This is Sahl's ELEVENTH testimony, not the tenth --
            # an earlier version numbered it (87) and omitted the heart of the
            # Sun entirely, leaving only ten of the eleven that 77 announces.
            #
            # The quadrant clause is measured against the QUADRANT CUSPS. It
            # read the whole-sign house, which is a different place for 52.3% of
            # placements and flips the masculine/feminine verdict for 18.6% of
            # them. A quadrant is the span between two angles; a whole sign is
            # not, and the file computes the real one a few lines above.
            #
            # 88 announces ONE testimony with two clauses. Both are reported,
            # but as a single entry -- appending them separately gave one
            # paragraph two votes.
            gender = PLANET_GENDER.get(planet)
            _q88 = gender == quadrant_position['quarter_gender']
            _s88 = ((gender == 'Masculine' and sign in MASCULINE_SIGNS)
                     or (gender == 'Feminine' and sign in FEMININE_SIGNS))
            if _q88 and _s88:
                _add('88', f"In a matching-gender ({gender.lower()}) quadrant and sign (88)",
                     _fact('Gender', gender),
                     _fact('Quadrant division, strict', quadrant_house_strict),
                     _fact('Quadrant gender', quadrant_position['quarter_gender']),
                     _fact('Sign', sign),
                     _fact('Sign gender', 'Masculine' if sign in MASCULINE_SIGNS else 'Feminine'))

            if labels or testimonies:
                item = {'Planet': planet, 'Strength Testimonies': ', '.join(labels), 'Count': len(labels),
                        'Labels': labels, 'Testimonies': testimonies}
                unresolved_count = sum(isinstance(t.get('result'), UnresolvedResult) for t in testimonies)
                if unresolved_count:
                    item['Unresolved Count'] = unresolved_count
                results.append(item)
        return results

@_connection_aware
def evaluate_weakness_of_planets(planetary_data, essential, accidental, ascendant_lon, sect):
    """Weakness of the Planets (Sahl, The Introduction Ch.3, 91-100): the
    ten testimonies of weakness Sahl names as item [15] of his sixteen-item
    scheme (90: "the weakness of the planets, and their harms in nativities
    and questions, indeed that is in ten ways"), cross-checked against the
    author's own summary table (Fig. 24).

    Distinct from the existing Gr. Intr. VII.6-based Planetary Condition
    table's own, differently-sourced weakness scheme (30-46) -- kept
    separate per the project's standing practice of retaining the original
    source's own definition.

    94 and 95 are separate testimonies, not one: 94 is an ordinary harmful
    connection with a single infortune by assembly/square/opposition, 95 is
    enclosure between both of them. An earlier version merged the two into
    the enclosure test and so never flagged a single-infortune contact.

    97's two clauses ("connecting with a planet falling away from the
    Ascendant, AND it is separating from a planet receiving it") are
    reported independently, since the translator's own footnote says he is
    "not sure that these conditions must both exist at once" -- an open
    question in the source, so the permissive reading is used and flagged
    rather than silently settled."""
    with doctrine(SAHL):
        rows = _pairwise_configurations(planetary_data)
        blocking_pairs = {(row['Blocked'], row['From Reaching']) for row in evaluate_blocking(planetary_data)}
        results = []
        for planet, data in planetary_data.items():
            if planet == 'North Node':
                continue
            lon, lat = data['longitude'], data['latitude']
            sun_lon = planetary_data['Sun']['longitude']
            house = get_wsh_house(lon, ascendant_lon)
            ess, acc = essential[planet], accidental[planet]
            labels = []
            # The same structure as the strength table's: number, the
            # label's words, the values tested. See evaluate_strength_of_planets.
            testimonies = []

            def _add(n, label, *facts):
                labels.append(label)
                testimonies.append({'n': n, 'sentence': label, 'facts': list(facts)})

            # (91) Falling from the stakes, and not looking at (averse to) the Ascendant.
            if house in CADENT_HOUSES and _averse_to_ascendant(lon, ascendant_lon):
                _add('91', 'Falling from the stakes, averse to the Ascendant (91)',
                     _fact('Whole-sign place', house), _fact('Cadent places', CADENT_HOUSES),
                     _fact('Averse to the Ascendant', True))

            # (92) Retrograde.
            if acc['Retrograde']:
                _add('92', 'Retrograde (92)',
                     _fact('Retrograde (accidental)', acc['Retrograde']),
                     _fact('Speed in longitude', data['speed_in_lon']))

            # (93, 99) Under the rays of the Sun. 99's first clause -- western,
            # "the Sun having already overtaken it (that is, if it was in front
            # of the Sun)" -- belongs to this testimony, not to 98; Fig. 24
            # pairs them as one "(93, 99) Under the rays" row.
            if sahl_under_rays(planet, lon, sun_lon, data.get('speed_in_lon')):
                signed_from_sun = ((lon - sun_lon + 180.0) % 360.0) - 180.0
                western = signed_from_sun > 0
                _add('93', 'Under the rays of the Sun' + (', western/overtaken (93, 99)' if western else ' (93)'),
                     _fact('Solar phase (Sahl)', solar_phase(planet, lon, sun_lon, data.get('speed_in_lon'), source='Sahl')[0]),
                     _fact('Heart limit (degrees, inclusive)', 1.0),
                     _fact('Signed distance from the Sun (positive is western)', signed_from_sun))

            # (94) Connecting with the infortunes from an assembly, opposition,
            # or square -- an ordinary harmful connection, and a testimony in
            # its own right. An earlier version folded this into 95's enclosure
            # and so never reported a single-infortune contact at all.
            # ONE testimony however many infortunes match -- 94 announces a
            # single item, and appending a label per matching planet let one
            # numbered testimony vote twice.
            _hit94 = []
            _facts94 = []
            for r in rows:
                if r['aspect_name'] not in ('Conjunction', 'Square', 'Opposition') or planet not in (r['p1'], r['p2']):
                    continue
                other = r['p2'] if r['p1'] == planet else r['p1']
                if other in effective_infortunes() and _is_connected(r):
                    _hit94.append(other)
                    _facts94.append(_fact(f'Connecting with {other}', r['aspect_name']))
            if _hit94:
                _add('94', f"Connecting with {' and '.join(sorted(_hit94))} by assembly, square, or opposition (94)",
                     *_facts94)

            # (95) Enclosed between the two infortunes -- separating from one,
            # connecting with the other (Sahl's own Enclosure, 119-123).
            is_enc, severe, sep, con = _sahl_enclosed(planet, effective_infortunes(), rows, blocking_pairs)
            if is_enc:
                _add('95', f'Enclosed between the infortunes, separating from {sep} and connecting to {con} (95, 119-123)',
                     _fact('Separating from', sep), _fact('Connecting to', con))

            # (96) In its own fall.
            if ess['Fall']:
                _add('96', 'In its own fall (96)', _fact('Fall (essential)', ess['Fall']))

            # (97) Connecting with a planet falling from the Ascendant, or
            # separating from a planet that would have received it.
            # Likewise ONE testimony for 97, whose two clauses and any number of
            # matching planets previously produced a label each.
            _averse97, _sep97 = [], []
            _rulers97 = None
            for r in rows:
                if r['aspect_name'] == 'Aversion' or planet not in (r['light_name'], r['heavy_name']):
                    continue
                other = r['heavy_name'] if r['light_name'] == planet else r['light_name']
                if _is_connected(r) and _averse_to_ascendant(planetary_data[other]['longitude'], ascendant_lon):
                    _averse97.append(other)
                if (r['applicant'] or r['light_name']) == planet and r['motion'] == 'Separating' and _is_connected(r):
                    # "separating from a planet RECEIVING IT": the departed
                    # planet receives THIS one, so it must own the house or
                    # exaltation of THIS planet's degree (3, 52: Mars receives
                    # the Moon in Aries "because [Aries] is his house"). The
                    # earlier test looked the other way round -- whether this
                    # planet ruled the departed one's degree (Astra F08). fn 99
                    # ("I am not sure that these conditions must both exist
                    # at once") leaves the two clauses' conjunction open; the
                    # engine applies each clause on its own, a reading.
                    my_rulers = get_essential_rulers(planetary_data[planet]['longitude'])
                    _rulers97 = my_rulers
                    if other in (my_rulers['domicile'], my_rulers['exaltation']):
                        _sep97.append(other)
            if _averse97 or _sep97:
                _parts = []
                _facts97 = []
                if _averse97:
                    _parts.append(f"connecting with {' and '.join(sorted(_averse97))}, averse to the Ascendant")
                    _facts97.append(_fact('Connecting with, averse to the Ascendant', sorted(_averse97)))
                if _sep97:
                    _parts.append(f"separating from {' and '.join(sorted(_sep97))}, which would have received it")
                    _facts97.append(_fact('Separating from', sorted(_sep97)))
                    _facts97.append(_fact('Domicile lord of its degree', _rulers97['domicile']))
                    _facts97.append(_fact('Exaltation lord of its degree', _rulers97['exaltation']))
                _add('97', _pn4_sentence_case('; '.join(_parts)) + ' (97)', *_facts97)

            # (98) "In a house in which it did not have testimony (neither
            # house nor exaltation nor triplicity)" -- alien/peregrine, and a
            # standalone testimony. Two corrections here: the scope is those
            # three dignities only, so a bare term or face claim does NOT
            # rescue it (essential[]['Peregrine'] counts all five and is
            # therefore too lenient); and it carries no solar condition at all
            # -- an earlier version additionally required the planet to be
            # under the rays AND western, which is 99's clause, not 98's.
            alien_rulers = get_essential_rulers(lon)
            triplicity_key_local = 'triplicity_day' if sect == 'Diurnal' else 'triplicity_night'
            if planet not in (alien_rulers['domicile'], alien_rulers['exaltation'], alien_rulers[triplicity_key_local]):
                _add('98', 'Alien: no house, exaltation, or triplicity where it sits (98)',
                     _fact('Domicile lord where it sits', alien_rulers['domicile']),
                     _fact('Exaltation lord where it sits', alien_rulers['exaltation']),
                     _fact(f'Triplicity lord where it sits ({triplicity_key_local})', alien_rulers[triplicity_key_local]))

            # (99) With the Head or Tail, without latitude.
            north_node_lon = planetary_data['North Node']['longitude']
            south_node_lon = (north_node_lon + 180.0) % 360.0
            node_dist = min(abs(((lon - north_node_lon + 180) % 360) - 180), abs(((lon - south_node_lon + 180) % 360) - 180))
            if node_dist < 12.0 and abs(lat) < 1.0:
                _add('99', 'With the Head or Tail, without latitude (99; this app reads |latitude| < 1°; 12 deg orb for either node, Ch. 3, 107 and VII.6, 52)',
                     _fact('Distance from the nearer node', node_dist), _fact('Latitude', lat))

            # (100) Inverted: in the seventh sign from its own house (Detriment).
            if ess['Detriment']:
                _add('100', 'Inverted, in the seventh sign from its own house (100)',
                     _fact('Detriment (essential)', ess['Detriment']))

            if labels:
                results.append({'Planet': planet, 'Weakness Testimonies': ', '.join(labels), 'Count': len(labels),
                                'Labels': labels, 'Testimonies': testimonies})
        return results

@_connection_aware
def evaluate_abu_mashar_condition(planetary_data, natal_houses, sect, essential, accidental, jd, ascendant_lon, sim=None):
    """Planetary condition per Gr. Intr. VII.6: good
    fortune (1-20), strength (21-29), weakness (30-46), misfortune (47-62)
    -- plus, for the Moon only, Abu Ma'shar's OWN eleven corruptions of her
    (63-74, via _abu_mashar_moon_corruption()). Sahl's ten (The Introduction
    Ch.3, 103-112) are a different list, not a variant reading of this one,
    and have their own table, Corruption of the Moon, in the Sahl view.

    One reading to flag: 8's "while the Moon is made fortunate" is gated on
    zero of Abu Ma'shar's own eleven corruptions (63-74). Reading
    "fortunate" as "uncorrupted" is this app's own -- in this chapter's
    vocabulary being made fortunate means satisfying 1-14 -- and the Moon
    has no corruptions in under 1.5% of charts, so 8 almost never fires.
    Left as-is rather than replaced by another guess. The Rhetorius/PN4
    delineations downstream show both readings and take this table's Net
    only as a lean; the older Hellenistic net dignity score in
    evaluate_essential_dignities()/evaluate_accidental_dignities() is
    retained separately.

    This chapter was built early in the project from photographed pages
    that were later lost, and stayed unverifiable through several passes.
    It has now been checked line by line against the text. Corrected here:
    2 names its aspects (sextile, square, trine, or assembly) and excludes
    the opposition, which was being counted; 8 places no aspect restriction
    on the Moon's inspection, which had inherited 7's trine/sextile; 19's
    "merely fortunate" is the moderated own house (Saturn in Aquarius,
    Jupiter in Sagittarius, and so on), not any single dignity claim, so a
    bare face no longer earns it; 48 carries a degree condition ("less than
    the bound of a planet between them and the infortunes") that was absent
    entirely; 50 is the tenth or eleventh sign, where the ninth was also
    being admitted; and enclosure is now Abu Ma'shar's own two types with
    the dissolution of 60-61 -- see _abu_mashar_enclosed(), and note that a
    dissolution rule deleted earlier in this project as unsourced turns out
    to be exactly what 60 states.

    A second pass, once the OCR of the chapter replaced the photographs,
    closed every gap the first pass had recorded, since all of them were
    parentheses that the photographs had cropped or blurred: 14's reverse
    case (the fortunes in the luminaries' shares); 27's extra strength when
    a superior looks at the Sun from the sextile; the Sun-specific clauses
    in 28 and 45, including his exception for the ninth, "for it is his
    joy"; 33's harsher retrogradation for the inferiors, especially when
    also burned; 44's harsher form (empty in course, no fortune looking, not
    received); and 53's tighter 4-degree node orb for the Sun against the
    Moon's full 12.

    That pass also corrected three tests that had been reading only half of
    what the text says. 36 is the exact mirror of 13 and needs BOTH the
    sign's gender and the hemisphere inverted, where this had been firing on
    any planet not in its domain -- about 82 percent of placements, against
    28 percent for the real condition. 22 and 38 each name two states
    ("rising up in the north OR is northern"), now told apart by latitude
    speed. And 46's "beginning of easternization" is VII.2, 40-41's 12
    degrees for the inferiors, not a flat 15.

    Not implemented BY DECISION D-19 (2026-09-08; kept by the owner
    2026-09-11, decision sheet row 15): the masculine and feminine DEGREES
    that 13 and 36 name alongside the signs -- the table is in the corpus
    as Gr. Intr. V.19, Figure 59 (V.19, 7 with fn 108), and D-19's ground is
    that it disagrees with the sign's gender on half the zodiac and the
    three schemes agree on a third of it, so reading "or male degrees"
    into 13 and 36 would widen a vote the author does not commit to. (An
    earlier line here said "for want of a source in hand rather than by
    choice", which contradicted NOT_IMPLEMENTED_COVERAGE's D-19 entry.)
    52's "their own Dragons" -- each planet's own nodes -- IS read below
    since 2026-09-11 (order GAP-39), from the ephemeris's MEAN nodes: 52
    does not say mean or true, a reading declared on the label."""
    with doctrine(ABU_MASHAR):
        rows = _pairwise_configurations(planetary_data)
        reception_rows = evaluate_reception(planetary_data, sect, sim)
        connected_lookup = {}
        configured_lookup = {}  # frozenset -> aspect name, for non-Connected "look"/assembly checks
        for row in rows:
            pair = frozenset({row['p1'], row['p2']})
            is_conn = row['aspect_name'] != 'Aversion' and _is_connected(row)
            connected_lookup[pair] = is_conn
            configured_lookup[pair] = row['aspect_name']
        blocking_pairs = {(row['Blocked'], row['From Reaching']) for row in evaluate_blocking(planetary_data)}

        def connected_to(planet, targets, aspects=None):
            for t in targets:
                if t == planet or not connected_lookup.get(frozenset({planet, t}), False):
                    continue
                if aspects is None or configured_lookup.get(frozenset({planet, t})) in aspects:
                    return True
            return False

        def configured_to(planet, targets, aspects=None):
            for t in targets:
                if t == planet:
                    continue
                name = configured_lookup.get(frozenset({planet, t}))
                if name and name != 'Aversion' and (aspects is None or name in aspects):
                    return True
            return False

        def averted_from(planet, targets):
            return all(configured_lookup.get(frozenset({planet, t})) == 'Aversion' for t in targets if t != planet)

        # Moon's own corruption count is needed (for §8's "Moon fortunate" test)
        # before the main per-planet loop, computed with no dependency on this
        # function's own output, to avoid recursion.
        # 8's "while the Moon is made fortunate" is judged on ABU MA'SHAR's
        # own eleven corruptions (63-74), not Sahl's ten. An Abu Ma'shar table
        # reaching into Sahl's list for its own paragraph was the last place
        # the two authors were still crossed. Note this is still a reading:
        # "made fortunate" in this chapter's vocabulary most naturally means
        # satisfying 1-14, and "uncorrupted" is the app's proxy for it.
        moon_corruption_count = len(_abu_mashar_moon_corruption(planetary_data, ascendant_lon, jd))

        results = {}
        for planet, data in planetary_data.items():
            if planet == 'North Node':
                continue
            lon, lat, speed = data['longitude'], data['latitude'], data['speed_in_lon']
            sign = get_zodiac_sign(lon)
            sign_idx = int(lon // 30)
            house = get_wsh_house(lon, ascendant_lon)
            quadrant_position = quadrant_strength_position(lon, natal_houses)
            quadrant_house_strict = quadrant_position['division']
            strength_house = quadrant_position['strength_house']
            in_masculine_quadrant = quadrant_position['quarter_gender'] == 'Masculine'
            ess, acc = essential[planet], accidental[planet]

            positive, negative = [], []
            unresolved_positive = []
            unresolved_negative = []

            # --- Good fortune (VII.6, 1-14) -----------------------------------
            # 2 names its aspects: "in the inspection of the fortunes from the
            # sextile, square, or trine, or are assembled with them" -- the
            # opposition is not among them, though an earlier version counted
            # any connected aspect. The note on 2 glosses "inspection" as an
            # aspect exact by degree, so the degree-based connection test is
            # the right gate.
            if connected_to(planet, FORTUNES, {'Conjunction', 'Sextile', 'Square', 'Trine'}):
                positive.append('Aspect/assembly with a fortune (2)')
            if averted_from(planet, effective_infortunes()):
                positive.append('Infortunes averted (3)')
            # 4: "Or they are separating from a FORTUNE and connecting with a
            # fortune" -- surrounded by benefics in time. An earlier version
            # tested separation from an INFORTUNE, which is the shape of 57's
            # enclosure (separating from one infortune, connecting with the
            # other) and belongs to the misfortune list, not this one.
            separating_fortune = any(
                (r['applicant'] or r['light_name']) == planet and (r['receiver'] or r['heavy_name']) in FORTUNES
                and _is_separating_in_nature(r)
                for r in rows
            )
            if separating_fortune and connected_to(planet, FORTUNES):
                positive.append('Separating fortune, connecting fortune (4)')
            # 5 / 62: "if the planet or sign was enclosed by the fortunes, then
            # that is of superior good fortune."
            is_enc_f, enc_kind_f, _diss = _abu_mashar_enclosed(planet, FORTUNES, planetary_data, rows)
            if is_enc_f:
                positive.append(f'Enclosed between the two fortunes, {enc_kind_f} (5, 62)')
            if acc['Cazimi']:
                positive.append('Cazimi (6)')
            # 7, like 2 and 8, says "in the INSPECTION of" -- and the note on 2
            # glosses inspection as an aspect exact by degree. This was the one
            # of the three still testing bare whole-sign configuration.
            if connected_to(planet, {'Sun'}, {'Trine', 'Sextile'}):
                positive.append('Trine/sextile the Sun (7)')
            # 8 puts no aspect restriction on the Moon's inspection, unlike 2
            # and 7 which name theirs; an earlier version limited it to the
            # trine and sextile borrowed from 7.
            if planet != 'Moon' and connected_to(planet, {'Moon'}) and moon_corruption_count == 0:
                positive.append('Aspects the Moon, uncorrupted by 63-74 (8)')
            # 9 is a conjunction, not a synonym: "quick in motion, INCREASING IN
            # LIGHT and number." VII.1, 19-21 defines increasing in light as
            # falling from the apogee toward the earth, so geocentric distance
            # decreasing. "Number" is the equation-table term of VII.1, 23-25,
            # which Dykes' note there says has no direct astrological import, so
            # it is not required. An earlier version accepted swiftness alone.
            #
            # For the MOON "increasing in light" is her glow, not her distance:
            # VII.2, 62-71 measure her conditions by "one-fourth of [her] glow",
            # and Sahl Ch.3, 112 has "decreasing in light (and that is [at] the
            # end of the [lunar] month)". The distance test agreed with waxing
            # in 47.5% of sampled charts -- chance. (VII.1, 22 allows the same
            # synodic sense for the superiors "in the manner that is said about
            # the Moon", but calls the apogee sense "the one agreed upon", so
            # the planets keep it.)
            if planet == 'Moon':
                increasing_in_light = 0.0 < (lon - planetary_data['Sun']['longitude']) % 360.0 < 180.0
            else:
                increasing_in_light = data.get('speed_in_dist', 0.0) < 0
            if acc['Swift'] and increasing_in_light:
                positive.append('Swift and increasing in light (9)')
            halb = ess['Domicile'] or ess['Exalt'] or ess['Triplicity'] or ess['Term'] or ess['Face'] or acc['Joy']
            if halb:
                positive.append('Halb (10)')
            brightness = _brightness_category(lon)
            if brightness == 'Bright':
                positive.append('Bright degree (11)')
            # Reception here is Abu Ma'shar's own. evaluate_reception() reads
            # the doctrine in force, and this function pins ABU_MASHAR above,
            # so the sidebar's Connection rule cannot reach it: all five
            # dignities count (VII.5, 129), reception runs in both directions
            # (130), and it holds by looking as well as by connection (133).
            # Under Sahl's narrower rule 43's "not received" fired on about
            # 81% of placements against about 8% under his own -- the case
            # that motivated doctrine().
            received = any(rec['Received'] == planet or (rec['Direction'] == 'Mutual'
                                                          and planet in rec['Receiver'].split(' & '))
                            for rec in reception_rows)
            for rec in reception_rows:
                if rec['Direction'] == 'Mutual':
                    if planet in rec['Receiver'].split(' & '):
                        positive.append(f"Mutual reception with {[x for x in rec['Receiver'].split(' & ') if x != planet][0]}")
                elif rec['Received'] == planet:
                    positive.append(f"Received by {rec['Receiver']} ({rec['Overall class'].split(':')[0].lower()}, via {rec['Via']})")
                elif rec['Receiver'] == planet:
                    positive.append(f"Receives {rec['Received']} into its own {rec['Via']}")
            if acc['Hayz'] is True:
                positive.append('Domain/hayz (13)')
            elif isinstance(acc['Hayz'], UnresolvedResult):
                unresolved_positive.append(acc['Hayz'])
            # 14 runs in both directions: "if the luminaries were in the shares
            # of the two fortunes, for it is as though [the luminaries] are in
            # their own shares (AND LIKEWISE, IF THE TWO FORTUNES WERE IN THE
            # SHARES OF THE LUMINARIES)." The second clause was unreadable in
            # the photographs and went unimplemented; it is plain in the OCR.
            if planet in ('Sun', 'Moon'):
                fortune_dispositors = _dispositors(lon, sect) & FORTUNES
                if fortune_dispositors:
                    positive.append(f"Luminary in a fortune's share (14): {', '.join(sorted(fortune_dispositors))}")
            elif planet in FORTUNES:
                luminary_dispositors = _dispositors(lon, sect) & {'Sun', 'Moon'}
                if luminary_dispositors:
                    positive.append(f"Fortune in a luminary's share (14): {', '.join(sorted(luminary_dispositors))}")

            # Good-fortune grade (15-20), three types. [1] "doubled" is two or
            # more claims at once (16-18, Mercury in Virgo having house and
            # exaltation, and a third if also in his bound). [2] "merely
            # fortunate" is specifically the one of its own houses "in which
            # its nature is moderated and agrees with it" -- Saturn in
            # Aquarius, Jupiter in Sagittarius, Mars in Scorpio, Venus in
            # Taurus, and either luminary in its own house (19). [3] is the
            # other of its two houses (20).
            #
            # An earlier version awarded [2] for ANY single dignity claim, so a
            # bare face counted as being "merely fortunate"; 19 is about
            # domicile placement, and a lone minor claim fits none of the three
            # grades.
            claims = sum([ess['Domicile'], ess['Exalt'], ess['Triplicity'], ess['Term'], ess['Face']])
            if claims >= 2:
                grade = 'Doubled good fortune (16-18)'
            elif ess['Domicile'] and planet in PREFERRED_DOMICILE:
                grade = ('Fortunate (19)' if sign == PREFERRED_DOMICILE[planet]
                         else 'Below that / suitable (20)')
            elif ess['Domicile']:
                grade = 'Fortunate (19)'          # the luminaries have one house each
            else:
                grade = None
            if grade:
                positive.append(grade)

            n_good_fortune = len(positive)

            # --- Strength (VII.6, 21-29) --------------------------------------
            # 22: "RISING UP in the north or ARE northern" -- the stronger form
            # is northern latitude still increasing (VII.1, 34-35 puts the
            # maximum 90 deg past the planet's own Head), the weaker is simply
            # being on the northern side of its nodes.
            if lat > 0:
                if data.get('speed_in_lat', 0.0) > 0:
                    positive.append('Rising up in the north (22)')
                else:
                    positive.append('Northern latitude (22)')
            dist_range = GEOCENTRIC_DISTANCE_RANGE.get(planet)
            if dist_range and data['distance'] >= sum(dist_range) / 2.0:
                positive.append('Apogee circle, approximated (23)')
            station = None
            if planet in PLANET_SWE_IDS and abs(speed) <= STATION_SPEED_TOLERANCE:
                res_next, _ = swe.calc_ut(jd + 1.0, PLANET_SWE_IDS[planet])
                if res_next[3] > speed:
                    station = 'second'
                    positive.append('Second station (24)')
                elif res_next[3] < speed:
                    station = 'first'
            # 25 is "GOING OUT of the rays of the Sun" -- a specific station in
            # the synodic cycle, not "anywhere outside them". VII.2, 12 names it:
            # at the burnt limit "they have already gone past burning and shift
            # over to the third condition, and they are said to be simply 'under
            # the rays', and FROM THERE THEY BEGIN IN [THEIR] ADVANCEMENT TOWARDS
            # EASTERNIZATION, AND THEY ARE SUITABLE FOR GRANTING THEIR GREATER
            # YEARS AS WELL AS SPEAR-BEARING." VII.2 itself credits that band
            # with a benefit, and 32 denies the same benefit to the band on the
            # setting side ("they [are] not suitable for granting their greater
            # years"), so the departure is what carries the merit.
            #
            # So this is the under-the-rays band WITH the elongation opening.
            # It does overlap 34's weakness, necessarily -- both chapters say
            # what they say, and VII.6 lists strengths and weaknesses in
            # separate categories that can both hold. An earlier version
            # credited every planet anywhere outside the rays whose elongation
            # was opening, which is the whole eastern half of a superior's
            # cycle: 35% of placements, and a second vote alongside 27 for
            # every eastern superior.
            _signed_sun = ((lon - planetary_data['Sun']['longitude'] + 180.0) % 360.0) - 180.0
            elongation_opening = (
                planet != 'Sun'
                and (speed - planetary_data['Sun']['speed_in_lon']) * _signed_sun > 0
            )
            if acc['UnderBeams'] and elongation_opening:
                positive.append('Going out of the rays (25)')
            # 26 and 39 are opposing sides of VI.26's divided-house strength
            # classification. The five-degree allowance is applied once, at
            # the four stakes, and both labels derive from that same result.
            # Dykes's fn 231 leaves a whole-sign reading of 39 open; the page
            # preserves that editorial alternative.
            stake_or_following = strength_house in ANGLE_HOUSES | SUCCEDENT_HOUSES
            if stake_or_following:
                positive.append('Stake or following (26)')
            is_superior = planet in {'Saturn', 'Jupiter', 'Mars'}
            # "THE THREE inferior planets" (29, 46), against "the three
            # superiors" (27, 45). Venus and Mercury are only two: the third
            # inferior is the Moon, who is below the Sun in the same sense the
            # other two are. An earlier version left her out of both clauses,
            # so she was tested by neither the superior nor the inferior rule.
            is_inferior = planet in {'Venus', 'Mercury', 'Moon'}
            sun_lon = planetary_data['Sun']['longitude']
            signed_from_sun = ((lon - sun_lon + 180.0) % 360.0) - 180.0
            is_eastern_of_sun = signed_from_sun < 0  # rises before the Sun
            # 27 carries a bonus that was unreadable in the photographs: "the
            # three superiors are eastern relative to the Sun (AND IF THEY LOOK
            # AT HIM FROM THE SEXTILE IT IS STRONGER FOR THEM)" -- cross-cited
            # to VII.2, 17-18, where the sextile is exactly the outer edge of
            # "proper, strong easternization."
            #
            # "Eastern relative to the Sun" is read as the hemisphere (VII.2, 2
            # calls the whole half "on the right side"), but never while under
            # the rays: VII.2, 14 reserves "easternizing" for after the 15/18
            # degrees are complete, and VII.6, 34 already counts the rays as
            # weakness. EASTERN_RULE = 'VII.2 band' narrows both 27 and 45 to
            # the bands VII.2 actually names (15/18 to 90 degrees eastern;
            # 90 down to 15 degrees western, 29-31). Hemisphere fires on 52% of
            # superior placements, the band on 25%.
            _e_lim, _w_lim = solar_rays_orb(planet)
            _elong = abs(signed_from_sun)
            if reading('EASTERN_RULE') == EASTERN_RULE_OPTIONS[1]:
                eastern_27 = is_eastern_of_sun and _e_lim <= _elong <= 90.0
                western_45 = (not is_eastern_of_sun) and _w_lim <= _elong <= 90.0
            else:
                eastern_27 = is_eastern_of_sun and _elong >= _e_lim
                western_45 = (not is_eastern_of_sun) and _elong >= _w_lim
            if is_superior and eastern_27:
                if configured_to(planet, {'Sun'}, {'Sextile'}):
                    positive.append('Superior, eastern of the Sun, by sextile (27)')
                else:
                    positive.append('Superior, eastern of the Sun (27)')
            # 28, 29, 45 and 46 use quarters bounded by the four actual
            # angular degrees. The strength allowance does not move a planet
            # across one of those boundaries.
            if is_superior and in_masculine_quadrant:
                positive.append('Superior, in a masculine quadrant (28)')
            # 28's parenthesis covers the Sun, who is neither superior nor
            # inferior and so fell through both clauses: "and if the Sun was in
            # these two quadrants or in the male signs, then he is also strong,
            # UNLESS HE IS IN LIBRA" -- his own sign of fall.
            #
            # Note the shape: these are OR-clauses, so both halves matching is
            # still ONE condition satisfied. Each such paragraph contributes a
            # single entry, with the evidence named inside it, rather than one
            # vote per matching sub-clause.
            if planet == 'Sun' and sign != 'Libra':
                sun_28 = ([ 'masculine quadrant' ] if in_masculine_quadrant else []) + \
                         ([ 'masculine sign' ] if sign in MASCULINE_SIGNS else [])
                if sun_28:
                    positive.append(f"Sun, {' and '.join(sun_28)} (28)")
            inf_29 = ([ 'western of the Sun' ] if not is_eastern_of_sun else []) + \
                     ([ 'feminine quadrant' ] if not in_masculine_quadrant else [])
            if is_inferior and inf_29:
                positive.append(f"Inferior, {' and '.join(inf_29)} (29)")

            # --- Weakness (VII.6, 30-46) --------------------------------------
            # (negative starts here; everything before was good fortune/strength)
            # 31's "slow in course" takes VII.1, 30-31's exception like every
            # other speed test in this file: for Venus and Mercury the pace is
            # the SUN's motion that day, not their own mean. Measured against
            # their own means, Mercury read slow 32.2% of the time and Venus
            # 44.0%, against 18.3% and 17.1% correctly.
            _pace = (planetary_data['Sun']['speed_in_lon'] if planet in ('Venus', 'Mercury')
                     else AVERAGE_DAILY_MOTION.get(planet, 1.0))
            is_slow = 0 <= speed < _pace
            if is_slow:
                negative.append('Slow in course (31)')
            if station == 'first':
                negative.append('First station (32)')
            # 33's parenthesis, unreadable in the photographs: "(and the more
            # harmful retrogradation is the retrogradation of the two inferior
            # planets -- AND ESPECIALLY IF IN ADDITION TO THEIR RETROGRADATION
            # THEY ARE BURNED)."
            if acc['Retrograde']:
                if is_inferior and acc['Combust']:
                    negative.append('Retrograde, inferior and burned (33)')
                elif is_inferior:
                    negative.append('Retrograde, the harsher inferior kind (33)')
                else:
                    negative.append('Retrograde (33)')
            if acc['Combust'] or acc['UnderBeams']:
                negative.append(f"Under the rays, {acc['SolarSide']} (34)")
            # 35 names the dark degrees and nothing else. The dusky and empty
            # bands come from the separate brightness scheme of V.20 (Fig. 61);
            # an earlier version voted them here as extra "minor" negatives,
            # which is this app's invention, not 35's.
            if brightness == 'Dark':
                negative.append('Dark degree (35)')
            # 36 is the exact mirror of 13, and needs BOTH halves wrong: "the
            # male ones are in a FEMALE sign, or in the female degrees, by day
            # UNDER the earth, and by night above the earth." VII.1, 39 keeps a
            # single failure as the milder middle case ("it takes away from the
            # nature of balance"), reserving "the contrary of its domain" for
            # "if it was contrary to ALL of this." An earlier version fired this
            # on any planet merely not in its domain, which is most of them.
            if acc['ContraryDomain'] is True:
                negative.append('Contrary to its domain (36)')
            elif isinstance(acc['ContraryDomain'], UnresolvedResult):
                unresolved_negative.append(acc['ContraryDomain'])
            if ess['Fall']:
                negative.append('Sign of fall (37)')
            # 38: "going DOWN in the south or IS southern" -- two conditions, as
            # 22 gives two on the northern side. Latitude speed separates them.
            if lat < 0:
                if data.get('speed_in_lat', 0.0) < 0:
                    negative.append('Going down in the south (38)')
                else:
                    negative.append('Southern latitude (38)')
            # 39 is the cadent side of the same allowance-adjusted division
            # used by 26. It cannot coexist with 26 for one planet.
            cadent_no_override = strength_house in CADENT_HOUSES
            if cadent_no_override:
                negative.append('Falling from the stake (39)')
            in_burned_path = 180.0 <= lon < 240.0
            in_harsh_burned_path = HARSH_BURNED_PATH[0] <= lon < HARSH_BURNED_PATH[1]
            if in_harsh_burned_path:
                negative.append('Burned path, harsh band (40)')
            elif in_burned_path:
                negative.append('Burned path (40)')
            if ess['Detriment']:
                negative.append('Opposition of own house / detriment (41)')
            # 42: "if it connects with a planet [that is] retrograde, corrupted,
            # in its own fall, or FALLING OR WITHDRAWING." Two separate words for
            # cadency, so both senses count -- dynamically cadent against the
            # quadrant cusps, or cadent by whole sign. An earlier version tested
            # whole sign alone.
            connects_debilitated = any(
                connected_lookup.get(frozenset({planet, other}), False)
                and (essential[other]['Fall'] or accidental[other]['Retrograde']
                     or get_wsh_house(planetary_data[other]['longitude'], ascendant_lon) in CADENT_HOUSES
                     or get_effective_house(planetary_data[other]['longitude'], natal_houses) in CADENT_HOUSES)
                for other in planetary_data if other not in (planet, 'North Node')
            )
            if connects_debilitated:
                negative.append('Connects to a debilitated planet (42)')
            if not received:
                negative.append('Not received (43)')
            # Emptiness of Course. Sahl's own base concept (The Introduction
            # Ch.3, 63, Fig. 19 -- illustrated there for the Moon, but item [10]
            # of his own 16-item list isn't restricted to her) is present-tense:
            # not currently connecting or uniting with any planet. Abu Ma'shar
            # sharpens this into the real, prospective definition used here --
            # no connection completes with ANY planet before this one leaves
            # its current sign (Gr. Intr. VII.5, 78, Fig. 124),
            # confirmed as his own addition by Dykes' footnote on 63 -- via the
            # forward simulation when available; falls back to Sahl's own
            # present-tense proxy (no currently-Applying-and-Connected pair)
            # when sim wasn't supplied.
            # A planet with no sign exit inside the horizon cannot be judged
            # prospectively -- the search is censored, not negative -- so it
            # takes the present-tense proxy instead of being called empty
            # because nothing perfected in 200 days.
            if sim is not None and sim['events'][planet]['sign_exits']:
                sign_exits = sim['events'][planet]['sign_exits']
                exit_day = sign_exits[0]
                empty_of_course = True
                for other in planetary_data:
                    if other in (planet, 'North Node'):
                        continue
                    raw_apart = abs(sign_idx - int(planetary_data[other]['longitude'] // 30))
                    signs_apart_other = min(raw_apart, 12 - raw_apart)
                    if signs_apart_other not in ASPECT_BY_SIGN_COUNT:
                        continue
                    target_angle = ASPECT_BY_SIGN_COUNT[signs_apart_other][1]
                    exact_day = _perfection_day(sim, planet, other, target_angle, before_day=exit_day)
                    if exact_day is not None and exact_day <= exit_day:
                        empty_of_course = False
                        break
            else:
                empty_of_course = not any(
                    ((row['applicant'] or row['light_name']) == planet and row['motion'] == 'Applying' and _is_connected(row))
                    for row in rows
                )
            # 44's parenthesis, unreadable in the photographs: "(and harsher
            # than that is if it was empty [in course], WITH NO FORTUNE LOOKING
            # AT IT OR PLANET BEING FAVORABLE TO IT)" -- three conditions
            # stacked, not just emptiness. "Favorable to it" is read as being
            # received by somebody, the same favor 43 measures.
            if ess['Peregrine']:
                unaided = empty_of_course and not configured_to(planet, FORTUNES) and not received
                if unaided:
                    negative.append('Exile, empty of course and unaided (44)')
                elif empty_of_course:
                    negative.append('Exile/peregrine, empty of course (44)')
                else:
                    negative.append('Exile/peregrine (44)')
            if is_superior and western_45:
                negative.append('Superior, western of the Sun (45)')
            if is_superior and not in_masculine_quadrant:
                negative.append('Superior, in a feminine quadrant (45)')
            # 45's parenthesis, the counterpart of 28's: "(and the weakness of
            # the Sun is if he is in the feminine signs or in these two quadrants
            # as well, UNLESS HE IS IN THE NINTH: FOR IT IS HIS JOY)." The Sun
            # previously fell through both the superior and inferior clauses and
            # so was never tested here at all. One entry per paragraph, as at 28.
            # The local antecedent of the Sun's ninth-place exception is the
            # geometrical ninth division. It is read before the angular
            # allowance and independently of the whole-sign Joy flag.
            if planet == 'Sun' and quadrant_house_strict != 9:
                sun_45 = ([ 'feminine quadrant' ] if not in_masculine_quadrant else []) + \
                         ([ 'feminine sign' ] if sign in FEMININE_SIGNS else [])
                if sun_45:
                    negative.append(f"Sun, {' and '.join(sun_45)} (45)")
            # "The beginning of their easternization" is where VII.2, 40-41 puts
            # it: from leaving the burned band until strong easternization at
            # 12 deg, not the flat 15 deg an earlier version used.
            # VII.2, 40: the beginning is AFTER burning -- "when they are
            # distant from [the Sun] at a full 7 degrees ... they come to the
            # beginning of their advancement towards easternization" -- so the
            # burned band is excluded.
            inf_46 = ([ 'beginning of easternization' ]
                      if is_eastern_of_sun and SOLAR_BURNED_ORB[planet][0] <= abs(signed_from_sun) < solar_rays_orb(planet)[0] else []) + \
                     ([ 'masculine quadrant' ] if in_masculine_quadrant else [])
            if is_inferior and inf_46:
                negative.append(f"Inferior, {' and '.join(inf_46)} (46)")

            n_weakness = len(negative)

            # --- Misfortune (VII.6, 47-55) ------------------------------------
            # 48 attaches a degree condition: the planet is with the infortunes
            # by assembly, opposition, square, trine or sextile "and there are
            # LESS THAN THE BOUND OF [a single] PLANET between them and the
            # infortunes." The width of the bound the planet itself occupies is
            # used as that measure -- the text does not say whose bound, so this
            # is an interpretive choice, but it is at least derived from the
            # chart rather than assumed. An earlier version applied no degree
            # condition at all.
            bound_width = _current_bound_width(lon)
            # The infortune must be the OTHER planet in the pair. Testing
            # "either member is an infortune" made Mars and Saturn satisfy this
            # against any partner at all, since each is itself an infortune --
            # so both malefics were permanently near an infortune by definition.
            close_to_infortune = any(
                r['aspect_name'] != 'Aversion' and planet in (r['p1'], r['p2'])
                and ({r['p1'], r['p2']} - {planet}) & effective_infortunes()
                and abs(r['deviation']) < bound_width
                for r in rows
            )
            if close_to_infortune:
                negative.append(f'Connected to an infortune, within a bound ({bound_width:.0f} deg) (47-48)')
            term_lord = get_essential_rulers(lon)['term']
            if SIGN_TO_DOMICILE.get(sign) in effective_infortunes() or term_lord in effective_infortunes():
                negative.append('In the bound/house of an infortune (49)')
            for other in planetary_data:
                if other in (planet, 'North Node'):
                    continue
                other_lon = planetary_data[other]['longitude']
                other_idx = int(other_lon // 30)
                forward = (other_idx - sign_idx) % 12 + 1
                # 50: "elevated above them from the TENTH OR ELEVENTH from
                # their place (and it is bad for that in all of this IF THE
                # INFORTUNES WERE NOT RECEPTIVE OF THEM)" -- an earlier version
                # admitted the ninth, and read the reception clause as reception
                # by anybody, so being received by a fortune elsewhere in the
                # chart suppressed an overcoming by Saturn. The escape is that
                # THIS infortune receives it.
                if other in effective_infortunes() and forward in (10, 11):
                    received_by_other = any(
                        rec['Receiver'] == other and rec['Received'] == planet
                        or (rec['Direction'] == 'Mutual'
                            and {planet, other} <= set(rec['Receiver'].split(' & ')))
                        for rec in reception_rows
                    )
                    if not received_by_other:
                        negative.append(f'Overcome by {other}, unreceived (50)')
                        break
            if configured_to(planet, {'Sun'}, {'Conjunction', 'Square', 'Opposition'}):
                negative.append('Assembly/square/opposition to the Sun (51)')
            north_node_lon = planetary_data['North Node']['longitude']
            south_node_lon = (north_node_lon + 180.0) % 360.0
            head_dist = abs(((lon - north_node_lon + 180) % 360) - 180)
            tail_dist = abs(((lon - south_node_lon + 180) % 360) - 180)
            with_head = head_dist <= tail_dist
            node_dist = min(head_dist, tail_dist)
            # 52 sets the general orb at 12 deg. 53 then grades it: "the most
            # harmful they can be for the SUN is if there were 4 deg between him
            # and them ... and the most harmful they can be for the MOON is if
            # there were 12 deg between her and one of them." So the Sun's
            # damage concentrates in a narrow 4 deg, the Moon's spans the whole
            # 12 deg. An earlier version applied one flat 12 deg to everything.
            #
            # 54-55 add a polarity, which the earlier version collapsed by
            # taking the nearer node without recording which it was: "the Head's
            # nature is that of increase ... the nature of the Tail is decrease,"
            # so the Head is a fortune with the fortunes and an infortune with
            # the infortunes, and the Tail the reverse. Abu Ma'shar reports this
            # as the ancients' view rather than asserting it ("some of the
            # ancients claimed"), so it is named in the label and not scored
            # separately.
            if node_dist <= 12.0:
                node_name = 'Head' if with_head else 'Tail'
                if planet in FORTUNES:
                    polarity = 'increasing its good' if with_head else 'taking from its good'
                elif planet in INFORTUNES:
                    polarity = 'increasing its evil' if with_head else 'taking from its evil'
                else:
                    polarity = 'increase' if with_head else 'decrease'
                worst = ((planet == 'Sun' and node_dist <= 4.0)
                         or (planet == 'Moon' and node_dist <= 12.0))
                if planet == 'Sun' and not worst:
                    negative.append(f'With the {node_name}, {polarity} (52, 54-55)')
                elif worst:
                    negative.append(f'With the {node_name} at its worst, {polarity} (52-55)')
                else:
                    negative.append(f'With the {node_name}, {polarity} (52-55)')
            # 52's FIRST clause: "Or they are with the Heads of their OWN Dragons,
            # or with their Tails ... and between them are 12 degrees or less" --
            # each planet's own nodes, read from the ephemeris at the moment
            # (swe.nod_aps_ut). MEAN nodes: 52 does not say mean or true, a
            # reading said on the label; the Moon's node above is the chart's
            # TRUE node (owner, 2026-09-07). The luminaries have no nodes of
            # their own in this sense. Order GAP-39, 2026-09-11.
            if jd is not None and planet in PLANET_SWE_IDS and planet not in ('Sun', 'Moon'):
                try:
                    own = swe.nod_aps_ut(jd, PLANET_SWE_IDS[planet], swe.NODBIT_MEAN)
                    own_head, own_tail = own[0][0] % 360.0, own[1][0] % 360.0
                except Exception:
                    own_head = own_tail = None
                if own_head is not None:
                    d_head = abs(((lon - own_head + 180) % 360) - 180)
                    d_tail = abs(((lon - own_tail + 180) % 360) - 180)
                    if min(d_head, d_tail) <= 12.0:
                        negative.append(f"With its own {'Head' if d_head <= d_tail else 'Tail'} within 12 degrees "
                                        f"(52, \"their own Dragons\"; the mean node, a reading -- 52 does not say mean or true)")

            # --- Enclosure by the infortunes (VII.6, 56-62) -------------------
            # Abu Ma'shar's OWN two types, not Sahl's borrowed: see
            # _abu_mashar_enclosed(). The standalone Enclosure table elsewhere
            # remains Sahl's (Ch.3, 119-123); this is the VII.6 table, so it
            # uses VII.6's version, including the dissolution of 60-61 that an
            # earlier pass of this project deleted as unsourced.
            #
            # The dissolution is a positive entry belonging to the MISFORTUNE
            # section (56-62), not to Strength (21-29). It is appended after the
            # strength count has already been taken, so the boundary is recorded
            # here rather than inferred from list length -- an earlier version
            # inferred it, and filed every dissolved enclosure under Strength.
            n_positive_through_strength = len(positive)
            is_enc, enc_kind, dissolver = _abu_mashar_enclosed(planet, effective_infortunes(), planetary_data, rows)
            if is_enc:
                if dissolver:
                    positive.append(f'Enclosure by the infortunes dissolved by {dissolver} (60-61)')
                else:
                    negative.append(f'Enclosed by the infortunes, {enc_kind} (56-58)')

            # --- Corruption of the Moon (VII.6, 63-74), Moon only -------------
            # This is Abu Ma'shar's OWN eleven, which is what belongs in a table
            # that is otherwise wholly VII.6. Sahl's ten (The Introduction Ch.3,
            # 103-112) have their own table in the Sahl view, under their own numbering --
            # the two lists overlap only partly, and neither is a variant
            # reading of the other.
            moon_defects = _abu_mashar_moon_corruption(planetary_data, ascendant_lon, jd) if planet == 'Moon' else []

            # --- The Good/Bad verdict -----------------------------------------
            # NOT Abu Ma'shar's. He enumerates these conditions; he nowhere adds
            # them up, and no weighting or tie rule appears anywhere in VII.6.
            # Counting the labels and subtracting is this app's own convenience,
            # kept because the Rhetorius/PN4 delineations downstream have to
            # choose one of two readings, and labelled as a heuristic everywhere
            # it is shown.
            #
            # Two distortions in the raw count are corrected here, both of which
            # let one underlying fact vote many times:
            #
            # (1) The Moon was the only planet handed a SECOND full checklist,
            # her corruptions, each as its own vote. Over 325 sampled charts
            # that pushed her average negative tally to 10.1 against 6.5-7.9 for
            # every other planet, and she came out "Good" 24.9% of the time
            # against 40-61% for the rest -- an artifact of the arithmetic, not
            # a judgement about the Moon. Her defects still show in full below;
            # they now contribute one entry to the tally.
            #
            # (2) Reception can yield several rows for one planet (more so under
            # Abu Ma'shar's wider profile, which counts all five dignities and
            # runs in both directions), and each was voting separately. Received
            # or not received is one fact, so it counts once.
            reception_labels = [l for l in positive if l.startswith(('Received', 'Receives', 'Mutual reception'))]
            positive_votes = [l for l in positive if l not in reception_labels]
            if reception_labels:
                positive_votes.append(reception_labels[0])
            negative_votes = list(negative)
            if moon_defects:
                negative_votes.append(f'Corruption of the Moon ({len(moon_defects)} defects)')

            net_known = len(positive_votes) - len(negative_votes)

            def _condition_class(net):
                return 'Indeterminate' if abs(net) <= 1 else ('Good' if net > 0 else 'Bad')

            def _alternative_result(reason, alternatives, source):
                values = [value for _name, value in alternatives]
                if all(value == values[0] for value in values[1:]):
                    return values[0]
                return UnresolvedResult(reason=reason, source=source,
                                        alternatives=tuple(alternatives), status='unassigned')

            net = net_known
            condition = _condition_class(net_known)
            good_fortune = n_good_fortune
            weakness = n_weakness
            positive_score = len(positive)
            negative_score = len(negative) + len(moon_defects)
            domain_alternatives = acc.get('Domain Alternatives')
            if unresolved_positive or unresolved_negative:
                source = acc['PlanetSect'].source
                states = domain_alternatives or ()
                net_options = [(name, net_known + (1 if hayz else 0) - (1 if contrary else 0))
                               for name, (hayz, contrary) in states]
                net = _alternative_result(
                    f'known subtotal {net_known}; the domain and contrary-domain testimonies depend on Mercury sect',
                    net_options, source)
                condition = _alternative_result(
                    'the heuristic class is evaluated under both Mercury-sect alternatives',
                    [(name, _condition_class(value)) for (name, _state), (_same, value)
                     in zip(states, net_options)], source)
                good_fortune = _alternative_result(
                    f'known subtotal {n_good_fortune}; the domain testimony depends on Mercury sect',
                    [(name, n_good_fortune + (1 if hayz else 0))
                     for name, (hayz, _contrary) in states], source)
                weakness = _alternative_result(
                    f'known subtotal {n_weakness}; the contrary-domain testimony depends on Mercury sect',
                    [(name, n_weakness + (1 if contrary else 0))
                     for name, (_hayz, contrary) in states], source)
                positive_score = _alternative_result(
                    f'known subtotal {len(positive)}; the domain testimony depends on Mercury sect',
                    [(name, len(positive) + (1 if hayz else 0))
                     for name, (hayz, _contrary) in states], source)
                negative_score = _alternative_result(
                    f'known subtotal {len(negative) + len(moon_defects)}; the contrary-domain testimony depends on Mercury sect',
                    [(name, len(negative) + len(moon_defects) + (1 if contrary else 0))
                     for name, (_hayz, contrary) in states], source)
            results[planet] = {
                # The four sections VII.6 itself is organised into, reported
                # separately so the picture doesn't collapse to one number.
                'Good Fortune': good_fortune,
                'Strength': n_positive_through_strength - n_good_fortune,
                'Weakness': weakness,
                'Misfortune': (len(negative) - n_weakness) + (len(positive) - n_positive_through_strength),
                'Moon Defects': len(moon_defects),
                'Positive Score': positive_score,
                'Negative Score': negative_score,
                'Net': net,
                # Owner's ruling (2026-09-16, after Astra's F06): a net of zero is
                # 'Indeterminate' here as the Dignities page's Lean already says,
                # not 'Good' -- a tie is not a favourable judgment.
                # ... and (the owner's second ruling, 2026-09-16, after the hostile
                # pass's M1) inside a margin of one as well, the width of a single
                # testimony -- the Dignities page's Lean's own margin, so the two
                # pages name one Net one way.
                'Condition': condition,
                'Positive Labels': positive,
                'Unresolved Positive Labels': tuple(unresolved_positive),
                'Negative Labels': negative + moon_defects,
                'Unresolved Negative Labels': tuple(unresolved_negative),
            }
        return results

def _twelfth_part_sign(lon):
    """The sign holding a point's twelfth-part. Each sign's 30 degrees are
    mapped onto the twelve signs in order, 2.5 degrees apiece, beginning
    with the sign itself.

    PROVENANCE: Gr. Intr. V.18, 1-3 (Figure 57) STATES the construction --
    each sign "divided ... into twelve divisions, so that each division is
    2 1/2 degrees" (1), "the first division of it is like the nature of the
    sign itself" (2), and the short calculation: take the degree and minute
    from the beginning of the sign, "multiply it by 12, and ... cast out
    what it amounts to from the beginning of that sign, 30 for every sign"
    (3). Both authors use it: the Moon's twelfth-part falling to Saturn or
    Mars is her fifth corruption (VII.6, 68), assembly with them is one of
    the ways planets meet (VII.4, 2), and Sahl devotes On Nativities 2.6 to
    them. An earlier note here said no text in hand stated the formula; it
    was written before Book V of the Great Introduction entered the corpus
    (Astra audit F14; work order PN4R-4n-2, 2026-09-11)."""
    sign_idx = int(lon // 30)
    step = int((lon % 30.0) // 2.5)
    return get_zodiac_sign(((sign_idx + step) % 12) * 30.0 + 15.0)

def _abu_mashar_moon_corruption(planetary_data, ascendant_lon, jd=None):
    """The ELEVEN corruptions of the Moon, Abu Ma'shar's own (Great
    Introduction VII.6, 63-74) -- announced at 63 as "in 11 ways" and
    enumerated one per paragraph through 74.

    Kept deliberately separate from Sahl's ten (The Introduction Ch.3,
    103-112, in _corruption_of_the_moon_labels()). They are not two
    transcriptions of one list: Abu Ma'shar has eclipse, the twelfth-part of
    Saturn or Mars, southern latitude and the ninth house, none of which
    appear in Sahl; Sahl has her own fall, connection with a fallen planet,
    and wildness, none of which appear here. The VII.6 condition table uses
    this list because that table is otherwise wholly VII.6; Sahl's tables
    keep Sahl's.

    64's "harsher" clause -- eclipsed in the natal sign or its trine or
    square -- needs a radix separate from the chart being judged and is not
    implemented for a natal chart, which IS the root."""
    with doctrine(ABU_MASHAR):
        moon = planetary_data['Moon']
        lon, lat, speed = moon['longitude'], moon['latitude'], moon['speed_in_lon']
        sun_lon = planetary_data['Sun']['longitude']
        sign = get_zodiac_sign(lon)
        rows = _pairwise_configurations(planetary_data)
        labels = []

        signed_sun = ((lon - sun_lon + 180.0) % 360.0) - 180.0
        elongation = abs(signed_sun)

        # [1] (64) "If she is eclipsed." A LUNAR eclipse in progress at the
        # birth moment, asked of the ephemeris globally: lun_eclipse_when()
        # gives the next eclipse's contact times, and the Moon is eclipsed
        # while the moment lies inside them. The earlier version asked
        # lun_eclipse_how() at geoposition (0, 0, 0), which reports 0 whenever
        # the eclipse is below the horizon AT GREENWICH, and then fell back to
        # a 12-degree node/syzygy window that also counted SOLAR eclipses
        # (where she eclipses rather than is eclipsed) -- it labelled 3 of 240
        # sampled charts against 0 real eclipses. A penumbral-only passage is
        # reported as such rather than silently equated with the umbral kind.
        north_node_lon = planetary_data['North Node']['longitude']
        node_dist = min(abs(((lon - north_node_lon + 180) % 360) - 180),
                        abs(((lon - (north_node_lon + 180.0) + 180) % 360) - 180))
        if jd is not None:
            try:
                _r, _t = swe.lun_eclipse_when(jd - 3.0, swe.FLG_SWIEPH, 0, False)
                _umbral = bool(_t[2] and _t[3] and _t[2] <= jd <= _t[3])
                _penumbral = bool(_t[6] and _t[7] and _t[6] <= jd <= _t[7])
                if _umbral:
                    labels.append('Eclipsed (64)')
                elif _penumbral:
                    labels.append('Eclipsed, penumbral only (64)')
            except Exception:
                pass

        # [2] (65) Under the rays: 12 degrees between her and the Sun's body,
        # "in the front or in the rear."
        if elongation <= 12.0:
            labels.append('Within 12 degrees of the Sun (65)')

        # [3] (66) The same 12 degrees around the minute of his opposition,
        # "going towards his opposition or flowing away from it."
        if abs(elongation - 180.0) <= 12.0:
            labels.append('Within 12 degrees of the Sun\'s opposition (66)')

        # [4] (67) "With the infortunes or they were looking at her" -- assembly
        # or aspect, and the looking half is whole-sign by its own wording.
        for infortune in sorted(effective_infortunes()):
            row = next((r for r in rows if {r['p1'], r['p2']} == {'Moon', infortune}), None)
            if row and row['aspect_name'] != 'Aversion':
                labels.append(f"With or looked at by {infortune} (67)")
                break

        # [5] (68) In the twelfth-part of Saturn or Mars -- the twelfth-part
        # falling in a sign those two rule.
        tp_sign = _twelfth_part_sign(lon)
        tp_lord = SIGN_TO_DOMICILE.get(tp_sign)
        if tp_lord in effective_infortunes():
            labels.append(f'In the twelfth-part of {tp_lord} ({tp_sign}) (68)')

        # [6] (69) With the Head or Tail within 12 degrees.
        if node_dist <= 12.0:
            labels.append('With the Head or Tail, within 12 degrees (69)')

        # [7] (70) "Southern OR going down in the south" -- two states, as at 38.
        if lat < 0:
            labels.append('Going down in the south (70)' if moon.get('speed_in_lat', 0.0) < 0
                          else 'Southern in latitude (70)')

        # [8] (71) In the burned path, "and that is Libra and Scorpio" -- the
        # whole two signs here, wider than VII.6, 40's harsher 19 Libra to
        # 3 Scorpio band (HARSH_BURNED_PATH).
        if sign in ('Libra', 'Scorpio'):
            labels.append('In the burned path, Libra or Scorpio (71)')

        # [9] (72) At the end of the signs, "because at that time she will be in
        # the bounds of the infortunes" -- the reason names the test, so this is
        # the sign's LAST bound rather than a fixed number of degrees.
        if _in_last_bound(lon):
            labels.append('In the last bound of the sign (72)')

        # [10] (73) Slow, "when she goes at less than her mean motion."
        if 0 <= speed < AVERAGE_DAILY_MOTION['Moon']:
            labels.append('Slow in motion (73)')

        # [11] (74) In the ninth house from the Ascendant.
        if get_wsh_house(lon, ascendant_lon) == 9:
            labels.append('In the ninth house (74)')

        return labels

MOON_DEFECT_IDS = (103, 104, 105, 106, 107, 108, 109, 110, 111, 112)

@_connection_aware
def evaluate_corruption_of_the_moon(planetary_data, ascendant_lon, sect):
    """The ten defects of the Moon (Sahl, The Introduction Ch.3, 103-112).
    Replaces an earlier version built from Abu Ma'shar's own, differently-
    numbered eleven-item list (Gr. Intr. VII.6, 63-74) without
    cross-checking it against Sahl's own item [16] -- four of that
    version's items (eclipsed, the twelfth-part of an infortune, southern
    latitude, ninth house from the Ascendant) don't appear anywhere in
    Sahl's ten and have been dropped; several genuine gaps against Sahl's
    own list (Moon in her own fall, connecting with a planet in its own
    fall, falling from the stakes or connecting with a falling planet,
    wild/empty of course, waning in light) are added here for the first
    time.

    Re-verified against the source's full paragraph text (rather than
    partial photo transcriptions): items [5] and [6] were corrected --
    (107) requires the Node to be in the SAME sign as the Moon, not just
    within 12 degrees regardless of sign; (108) requires the LAST bound of
    the sign specifically (every sign's final Egyptian term is an
    infortune's, but not every infortune-ruled bound is the sign's last
    one -- the earlier version matched any of them). Item [7] (109) is a
    plain disjunction in the source and is implemented as one. Item [9]'s
    "wild" (111) is confirmed, on the full text, to be glossed there as
    "not connecting with any of the planets" -- the same present-tense
    wording as Emptiness of Course (63) -- so its existing implementation
    was left as-is.

    Shown as its own table, Corruption of the Moon, in the Sahl view of the
    Configurations page.

    Returns one record per numbered testimony, 103-112, so the count is
    "how many of Sahl's ten" and never "how many clauses matched". 104
    ("in her own fall, OR connecting with a star in its fall") and 109
    ("falling from the stakes, OR connecting with a planet falling") can
    each be satisfied by several planets at once, and 112 by slowness and
    waning together; an earlier version appended one label per match and
    counted the labels, so a list announced as ten defects could exceed
    ten and one paragraph could vote three times. Every clause is still
    reported, under its paragraph:

        {'testimonies': {103: {'matched': False, 'clauses': []},
                         104: {'matched': True, 'clauses': ['In her own fall, Scorpio',
                                                             'Connecting with Venus, itself in its own fall']},
                         ...},
         'unique_testimony_count': 3,      # of ten -- the only number to score with
         'matching_instances': 5,          # every clause that matched, for the record
         'labels': ['In her own fall, Scorpio (104)', ...]}"""
    with doctrine(SAHL):
        moon = planetary_data['Moon']
        lon, speed = moon['longitude'], moon['speed_in_lon']
        sun_lon = planetary_data['Sun']['longitude']
        sign = get_zodiac_sign(lon)
        rows = _pairwise_configurations(planetary_data)
        testimonies = {n: {'matched': False, 'clauses': []} for n in MOON_DEFECT_IDS}
        labels = []

        def hit(n, clause, cite=None):
            """Record one matching clause under testimony n. The label keeps
            the paragraph number for the table; the count does not read it."""
            testimonies[n]['matched'] = True
            testimonies[n]['clauses'].append(clause)
            labels.append(f'{clause} ({cite or n})')

        def connected_row(other):
            return next((r for r in rows if {r['p1'], r['p2']} == {'Moon', other}), None)

        # [1] (103) Burned, within 12 degrees of the Sun, front or behind.
        sun_dist = abs(((lon - sun_lon + 180) % 360) - 180)
        if sun_dist <= 12.0:
            hit(103, 'Burned, within 12 degrees of the Sun')

        # [2] (104) In the degrees of her own fall (Scorpio), or connecting
        # with a planet in ITS own fall.
        if sign in FALLS.get('Moon', []):
            hit(104, 'In her own fall, Scorpio')
        for other in planetary_data:
            if other in ('Moon', 'North Node'):
                continue
            r = connected_row(other)
            if r and r['aspect_name'] != 'Aversion' and _is_connected(r):
                other_sign = get_zodiac_sign(planetary_data[other]['longitude'])
                if other_sign in FALLS.get(other, []):
                    hit(104, f'Connecting with {other}, itself in its own fall')

        # [3] (105) Opposed to the Sun, within 12 degrees, not yet having
        # reached the exact opposition (still approaching, not past it).
        opp_target = (sun_lon + 180.0) % 360.0
        signed_to_opp = ((opp_target - lon + 180) % 360) - 180
        if 0 <= signed_to_opp <= 12.0:
            hit(105, "Approaching the Sun's opposition, within 12 degrees")

        # [4] (106) Assembled with an infortune, or looking at it from a
        # square or opposition (sextile/trine don't count here) -- or enclosed
        # between the two infortunes (separating from one, connecting with the
        # other -- Sahl's own Enclosure test, 119-123).
        if any(row['aspect_name'] in ('Conjunction', 'Square', 'Opposition')
               and 'Moon' in (row['p1'], row['p2'])
               and (row['p1'] in effective_infortunes() or row['p2'] in effective_infortunes())
               for row in rows):
            hit(106, 'Assembled with, square, or opposed by an infortune')
        blocking_pairs = {(row['Blocked'], row['From Reaching']) for row in evaluate_blocking(planetary_data)}
        is_enc, severe, _sep, _con = _sahl_enclosed('Moon', effective_infortunes(), rows, blocking_pairs)
        if is_enc:
            hit(106, 'Enclosed between the two infortunes' + (', severe' if severe else ''), cite='106, 119-123')

        # [5] (107) With the Head or Tail, IN ONE SIGN, less than 12 degrees
        # between them -- same-sign co-presence (Sahl's own "connection" shape)
        # plus the Moon's own light-radius, not merely raw closeness in degree
        # regardless of sign boundary.
        north_node_lon = planetary_data['North Node']['longitude']
        south_node_lon = (north_node_lon + 180.0) % 360.0
        for node_lon in (north_node_lon, south_node_lon):
            if get_zodiac_sign(node_lon) == sign and abs(((lon - node_lon + 180) % 360) - 180) < 12.0:
                hit(107, 'With the Head or Tail, in one sign and under 12 degrees')
                break

        # [6] (108) In the twelfth sign from her own house (Gemini, since her
        # house is Cancer), or in the LAST degrees of the sign specifically --
        # the final Egyptian-term division, which (per the table) is always
        # ruled by one of the two infortunes, not just any infortune-ruled
        # bound elsewhere in the sign (e.g. Aries' 20-25 degree bound is
        # Mars's but isn't the sign's last one).
        if sign == 'Gemini':
            hit(108, "In Gemini, the twelfth sign from her own house")
        if _in_last_bound(lon):
            hit(108, "In the last degrees of the sign, the infortunes' bound")

        # [7] (109) "Falling from the stakes, or connecting with a planet
        # falling from the stakes" -- a plain disjunction, verbatim. (A prior
        # edit replaced this with a cadent + averse-to-Ascendant + separating-
        # from-an-infortune predicate, which is Weakness 91/97 material and
        # appears nowhere in 109; reverted.)
        moon_house = get_wsh_house(lon, ascendant_lon)
        if moon_house in CADENT_HOUSES:
            hit(109, 'Falling from the stakes')
        for other in planetary_data:
            if other in ('Moon', 'North Node'):
                continue
            r = connected_row(other)
            if r and r['aspect_name'] != 'Aversion' and _is_connected(r):
                if get_wsh_house(planetary_data[other]['longitude'], ascendant_lon) in CADENT_HOUSES:
                    hit(109, f'Connecting with {other}, itself falling from the stakes')

        # [8] (110) "in the burned path, and that is at the end of Libra and
        # the beginning of Scorpio". SAHL GIVES NO DEGREES. The span tested,
        # 19 Libra to 3 Scorpio, is Abu Ma'shar's harsher band (Gr. Intr.
        # VII.6, 40: "harsher than that is if it was from 19 Libra up to 3
        # Scorpio"), the one fn 120 reports, BORROWED here as the only
        # author-stated region of the shape Sahl describes. Firing on the two
        # whole signs would over-read Sahl (early Libra is not "the end of
        # Libra"). Owner, 2026-09-11 (decision sheet row 9, DEC-D-5 as
        # implemented); an earlier comment claimed Sahl's wording "matches"
        # the band, which it does not state. Readings, not tests: 15 Libra to
        # 15 Scorpio is the definition "often" given (Dykes, Carmen p. 258 fn
        # 104) and al-Qabisi's (III.29, ITA IV.3: the last half of Libra and
        # the first half of Scorpio); Dorotheus's own (Carmen p. 258, 6: the
        # equinoctial point taken southward) is a different construction.
        # Both readings are ON THE PAGE, in the clause (review D5).
        if HARSH_BURNED_PATH[0] <= lon < HARSH_BURNED_PATH[1]:
            hit(110, 'In the burned path, "the end of Libra and the beginning of Scorpio" -- Sahl gives no degrees; '
                     "the 19 Libra-3 Scorpio span is Gr. Intr. VII.6, 40's harsher band (fn 120), borrowed. "
                     'READINGS, not tests: 15 Libra-15 Scorpio is the definition "often" given (Dykes, Carmen p. 258 fn 104) '
                     "and al-Qabisi's own, the last half of Libra and the first half of Scorpio (ITA IV.3, al-Qabisi III.29), "
                     "so it is not true that no source in hand states it; Dorotheus's own definition "
                     "(Carmen p. 258, 6: the equinoctial point taken southward) is a different construction")

        # [9] (111) Wild -- empty of course, not connecting with any planet.
        # Sahl's own present-tense definition (not Abu Ma'shar's later,
        # prospective sharpening used elsewhere in this file).
        if not any((row['applicant'] or row['light_name']) == 'Moon' and row['motion'] in ('Applying', 'Exact') and _is_connected(row) for row in rows):
            hit(111, 'Wild, empty of course')

        # [10] (112) Slow in course, or waning in light (past full, heading
        # back toward new).
        if 0 <= speed < AVERAGE_DAILY_MOTION['Moon']:
            hit(112, 'Slow in course')
        if 180.0 < ((lon - sun_lon) % 360.0) < 360.0:
            hit(112, 'Waning in light')

        return {
            'testimonies': testimonies,
            'unique_testimony_count': sum(t['matched'] for t in testimonies.values()),
            'matching_instances': len(labels),
            'labels': labels,
        }

def _corruption_of_the_moon_labels(planetary_data, ascendant_lon, sect):
    """The flat label list, one per matching clause. For display; the
    testimony count is evaluate_corruption_of_the_moon()['unique_testimony_count']."""
    return evaluate_corruption_of_the_moon(planetary_data, ascendant_lon, sect)['labels']


MASHAALLAH_ASSESSMENT_COLUMN = 'Stated topic condition — app assessment'
MASHAALLAH_CONDITION_SOURCES = {
    3: '3.10, 14', 4: '4.11, 24', 6: '6.3.4, 24',
    7: '7.1, 217 — the lord of the seventh; fn 135: \'Omitting "in the seventh"\'',
    9: '9.4, 35', 10: '10.2.4, 13', 11: '11.1, 28', 12: '12.1, 47',
}
MASHAALLAH_UNSTATED = {
    1: 'No section-wide neutrality condition stated (1.36, 78–97)',
    2: 'No matching neutrality condition stated; introductory testimony qualification at 2.14, 1',
    5: 'No explicit neutrality gate for the fifth-lord list; 5.1, 91 conditionally turns to the Lot of children (fnn 49–50)',
    8: 'No section-wide neutrality condition stated; local qualification at 8.5, 2',
}


def mashaallah_condition(house_i, lord, planetary_data, ascendant_lon):
    """Assess only the eight stated topic conditions. The seventh names its
    lord alone (7.1, 217 fn 135); the other seven name the place and lord.
    Whole-sign affliction by assembly/square/opposition and benefic testimony
    by any aspect are this app's convention, also applied to 11.1, 28's
    distinct wording. The lord never testifies against itself. The four
    unstated conditions are unresolved source scope, not missing chart data.
    Returns (met/not met, reason), or the shared UnresolvedResult.
    This column never filters or reverses the source delineation.
    """
    if house_i in MASHAALLAH_UNSTATED:
        return UnresolvedResult(MASHAALLAH_UNSTATED[house_i], "Sahl, On Nativities")
    asc_idx = int(ascendant_lon // 30)
    house_idx = (asc_idx + house_i - 1) % 12
    lord_idx = int(planetary_data[lord]['longitude'] // 30)
    ordinal = {1: 'st', 2: 'nd', 3: 'rd'}.get(house_i if house_i < 20 else house_i % 10, 'th')
    house_name = f"the {house_i}{ordinal}"
    targets = ((lord_idx, f'its lord {lord}'),) if house_i == 7 else ((house_idx, house_name), (lord_idx, f'its lord {lord}'))
    reasons = []
    for other, data in planetary_data.items():
        if other == 'North Node' or other == lord:
            continue
        other_idx = int(data['longitude'] // 30)
        if other in INFORTUNES:
            for target, name in targets:
                rel = (other_idx - target) % 12
                if rel in (0, 3, 6, 9):
                    how = {0: 'with', 3: 'square', 6: 'opposite', 9: 'square'}[rel]
                    reasons.append(f'{other} {how} {name}')
        elif other in FORTUNES:
            for target, name in targets:
                rel = (other_idx - target) % 12
                if rel not in (1, 5, 7, 11):
                    how = {0: 'with', 2: 'sextile', 3: 'square', 4: 'trine', 6: 'opposite', 8: 'trine', 9: 'square', 10: 'sextile'}[rel]
                    reasons.append(f'{other} witnesses {name} ({how})')
    return ('met', '') if not reasons else ('not met', '; '.join(reasons))

def evaluate_house_lords(planetary_data, ascendant_lon):
    """For each Whole Sign topical house (1-12), find its domicile lord and
    the WSH house that lord is physically placed in, then look up
    Masha'allah's delineation for that [placed_in][ruled_house] pairing --
    the cell's text with Sahl's locator in parentheses, e.g. "... (1.36,
    83)" -- with his own operating condition (mashaallah_condition) beside
    it."""
    asc_idx = int(ascendant_lon // 30)
    results = []
    for house_i in range(1, 13):
        sign_idx = (asc_idx + house_i - 1) % 12
        cusp_sign = get_zodiac_sign(sign_idx * 30 + 15.0)  # mid-sign probe, sign is constant across the whole 30 deg
        domicile_lord = SIGN_TO_DOMICILE.get(cusp_sign)

        if domicile_lord not in ('Sun', 'Moon') and domicile_lord not in planetary_data:
            continue

        lord_lon = planetary_data[domicile_lord]['longitude']
        placed_in = get_wsh_house(lord_lon, ascendant_lon)
        cell = MASHAALLAH_LORDS.get(placed_in, {}).get(house_i)
        text = '-' if cell is None else (f"{cell['text']} ({cell['cite']})" if cell['cite'] else cell['text'])
        assessment = mashaallah_condition(house_i, domicile_lord, planetary_data, ascendant_lon)
        if not isinstance(assessment, UnresolvedResult):
            status, why = assessment
            assessment = 'Met under app test' if status == 'met' else f'Not met under app test: {why}'
        if cell and cell.get('note'):
            text += ' ' + cell['note']

        results.append({
            'Topical House': house_i,
            'Cusp Sign': cusp_sign,
            'Domicile Lord': domicile_lord,
            'Placed in (WS place)': placed_in,
            MASHAALLAH_ASSESSMENT_COLUMN: assessment,
            'Condition source': MASHAALLAH_CONDITION_SOURCES.get(house_i, MASHAALLAH_UNSTATED.get(house_i, '')),
            'Source detail': ('5.1, 91 directs inspection of the Lot of children, not a gate on this list. '
                              'Fn 49: attribution to Masha\'allah or Sahl uncertain; fn 50: the reading of witness is tentative and E virtually illegible. '
                              if house_i == 5 else '') + (cell.get('detail', '') if cell else ''),
            "Masha'allah Signification": text,
        })
    return results

# --- Victors (Almutens) of significant points -- ibn Ezra (1485/1537),
# TNAC Handy Tables Lesson 20 -- generalizing the same weighted essential-
# dignity lookup already used below for the Prenatal Syzygy's Almuten to
# the Ascendant, Sun, Moon, and Lot of Fortune. Two parallel weighting
# traditions are given directly in the source, disagreeing on whether
# Bound or Triplicity ranks higher, so both are computed side by side
# rather than silently picking one. The "older" label is the course's,
# kept as printed (owner, 2026-09-15): the one passage in the corpus that
# attributes weights to 'Umar -- Abu Bakr, On Nativities II.5.14 (via
# al-'Anbas; PN II) -- gives triplicity 3, bound 2, the "newer" order, and
# Dykes's introduction there says the weighted victor is not found in Sahl
# or Masha'allah. The bound-before-triplicity ORDER itself is known to
# al-Qabisi -- ITA I.22 (p. 81): "certain people put the bound before the
# triplicity" -- so only its attribution to 'Umar and Masha'allah is
# unwitnessed. Said on the Victors page.
VICTOR_WEIGHTS = {
    "Older (al-Tabari/Masha'allah)": {'domicile': 5, 'exaltation': 4, 'triplicity': 2, 'term': 3, 'face': 1},
    "Newer (Al-Qabisi/Abu Ma'shar)": dict(ESSENTIAL_DIGNITY_WEIGHTS),
}

# "Places" bonus wheels (Handy Tables Lesson 20): a candidate planet's own
# Whole-Sign-House placement adds this many points to its total, on top of
# its essential-dignity claim at the point being profiled. Both are
# permutations of 1-12. Which wheel goes with which weighting scheme is not
# stated in the source, so evaluate_victors() computes all four combinations
# and marks the two same-tradition pairings (ibn Ezra's wheel with the
# newer/Al-Qabisi weights, Masha'allah's with the older) as the interpretive
# presets they are.
VICTOR_PLACES_VALUES = {
    "Older (al-Tabari/Masha'allah)": {1: 12, 2: 3, 3: 5, 4: 7, 5: 8, 6: 1, 7: 9, 8: 4, 9: 6, 10: 11, 11: 10, 12: 2},
    "Newer (Al-Qabisi/Abu Ma'shar)": {1: 12, 2: 6, 3: 3, 4: 9, 5: 7, 6: 1, 7: 10, 8: 4, 9: 5, 10: 11, 11: 8, 12: 2},
}

def evaluate_victors(planetary_data, ascendant_lon, lot_of_fortune, syzygy_lon, sect, chronocrats):
    """The victor of the chart, per ibn Ezra's victor #1 worksheet
    (1485/1537, Handy Tables Lesson 20).

    The worksheet is ONE table: the seven planets are its columns, and its
    rows are the five points -- Sun, Moon, Ascendant, Lot of Fortune, and
    the prenatal New/Full Moon -- followed by Lord of the Day (+7), Lord of
    the Hour (+6) and Places. Every column is then summed into a single
    Totals row, and the highest total is the victor. So this produces one
    aggregate victor per weighting scheme, and the output mirrors the
    worksheet grid so a student can check it cell by cell against a
    hand-filled sheet.

    Rebuilt from an earlier version that computed a SEPARATE victor for
    each of four points, omitted the prenatal syzygy row entirely, and
    added the Lord of the Day, Lord of the Hour and Places bonuses afresh
    to every one of those four tallies -- so the auxiliary rows, which
    appear once on the sheet, were counted four times over, and the four
    winners were reported where the sheet asks for one.

    The two axes are independent and are kept that way. The five dignity
    weights come from the older (Umar al-Tabari / Masha'allah: bound 3,
    triplicity 2) or newer (al-Qabisi / Abu Ma'shar: triplicity 3, bound 2)
    tradition; the Places wheel comes from ibn Ezra's own or Masha'allah's.
    Which pairing to use is not stated in the source, so all four
    combinations are computed, and the two same-tradition pairings are
    marked as presets.

    Dignity claims are read AT EACH POINT'S degree, not at the candidate
    planet's own position; the Places value is keyed the other way, by the
    CANDIDATE's own Whole-Sign house. Masha'allah's wheel gives two
    competing values on four wedges (Masha'allah vs. Dorotheus); the
    Masha'allah value is used, matching the scheme it is paired with.

    Ibn Ezra's victor #2 (1507) is not implemented: it drops the two
    chronocrator rows and adds a "Superiors" row scored only for Saturn,
    Jupiter and Mars (the worksheet blacks out the other four cells), but
    no available course document gives that row's weight, and guessing it
    would make the totals meaningless."""
    triplicity_key = 'triplicity_day' if sect == 'Diurnal' else 'triplicity_night'
    points = {
        'Sun': planetary_data['Sun']['longitude'],
        'Moon': planetary_data['Moon']['longitude'],
        'Ascendant': ascendant_lon,
        'Lot of Fortune': lot_of_fortune,
        'Prenatal Syzygy': syzygy_lon,
    }
    columns = [p for p in WEIGHT_ORDER]           # worksheet column order
    day_lord = chronocrats.get('Day Lord')
    hour_lord = chronocrats.get('Hour Lord')

    # The two axes are genuinely independent, so all four combinations are
    # computed rather than the two diagonal pairings alone. Which wheel
    # goes with which weighting is nowhere stated in the source; pairing
    # each with the wheel of its own named tradition is a reading, and
    # hard-coding it hid the two off-diagonal cases entirely. The diagonal
    # pairings are marked as the interpretive presets they are.
    results = {}
    for (weight_name, weights), (places_name, places_values) in product(
            VICTOR_WEIGHTS.items(), VICTOR_PLACES_VALUES.items()):
        preset = ' (matched preset)' if weight_name == places_name else ''
        scheme_name = (f"{weight_name.split(' (')[0]} weights + "
                       f"{places_name.split(' (')[0]} places{preset}")
        totals = {p: 0 for p in columns}
        grid = []

        for point_name, lon in points.items():
            rulers = get_essential_rulers(lon)
            claims = {p: 0 for p in columns}
            for key, weight_key in (('domicile', 'domicile'), ('exaltation', 'exaltation'),
                                     (triplicity_key, 'triplicity'), ('term', 'term'), ('face', 'face')):
                lord = rulers[key]
                if lord in claims:
                    claims[lord] += weights[weight_key]
            for p in columns:
                totals[p] += claims[p]
            grid.append({'Row': point_name, **{p: str(claims[p]) if claims[p] else '' for p in columns}})

        # The three auxiliary rows appear ONCE on the sheet, not per point.
        for label, lord, pts in (('Lord of the Day (7)', day_lord, 7),
                                  ('Lord of the Hour (6)', hour_lord, 6)):
            row = {p: '' for p in columns}
            if lord in totals:
                totals[lord] += pts
                row[lord] = str(pts)
            grid.append({'Row': label, **row})

        places_row = {}
        for p in columns:
            if p in planetary_data:
                bonus = places_values[get_wsh_house(planetary_data[p]['longitude'], ascendant_lon)]
                totals[p] += bonus
                places_row[p] = str(bonus)
            else:
                places_row[p] = ''
        grid.append({'Row': 'Places', **places_row})
        grid.append({'Row': 'Totals', **{p: str(totals[p]) for p in columns}})

        victor = max(columns, key=lambda p: totals[p])
        tied = [p for p in columns if totals[p] == totals[victor]]
        # The runner-up is the first planet STRICTLY below the top score.
        # sorted()[1] used to hand back a co-winner whenever the top was
        # tied, so a tie read as "Saturn / Jupiter, runner-up Jupiter".
        lower = [p for p in sorted(columns, key=lambda p: -totals[p]) if totals[p] < totals[victor]]
        results[scheme_name] = {
            'grid': grid,
            'victor': ' / '.join(tied) if len(tied) > 1 else victor,
            'total': totals[victor],
            'runner_up': f"{lower[0]} ({totals[lower[0]]})" if lower else '-',
            'tied': len(tied) > 1,
        }
    return results

def evaluate_planets_in_houses(planetary_data, abu_mashar_condition, ascendant_lon):
    """Each planet's Whole Sign house placement with the readings for that
    pairing: shared PN IV passages outside the split, plus the two PN IV
    halves in the page's condition columns, 'If in a
    suitable condition' and 'If in a bad condition', each printed as
    "<text> (<locator>)", and the Rhetorius/Firmicus testimony list in a
    third column, 'Rhetorius and Firmicus, as the texts state it', each
    entry printed as "Rhetorius (by day): <text> [<locator>]" and the
    entries separated by a middle dot (rhetorius_entries_cell()). 'Entries'
    carries every entry of the list, printed in full, for the row's detail
    and the export.

    Both PN IV halves are shown rather than one being chosen for the
    reader. The only thing available to choose with is the Net in the
    Planetary Condition table, and that number is this app's own
    arithmetic: Abu Ma'shar enumerates the VII.6 conditions and nowhere
    totals them, supplies no weighting, and gives no rule for ties. Letting
    an invented score silently pick one of two classical delineations
    turned a convenience into a verdict, and hid the fact that a chart can
    sit one label away from the opposite reading.

    The Net still appears, as a LEAN rather than a selection, and goes
    'Indeterminate' inside a margin of one -- the width of a single
    testimony, so the cases that could flip on one more source paragraph
    say so instead of committing."""
    results = []
    for planet, data in planetary_data.items():
        if planet == 'North Node': continue

        wsh_house = get_wsh_house(data['longitude'], ascendant_lon)
        condition_data = abu_mashar_condition[planet]
        net = condition_data['Net']
        if isinstance(net, UnresolvedResult):
            if isinstance(condition_data['Condition'], UnresolvedResult):
                lean = condition_data['Condition']
            else:
                lean = f"leans {condition_data['Condition']} under both alternatives"
        elif abs(net) <= 1:
            lean = 'Indeterminate'
        else:
            lean = f"leans {condition_data['Condition']}"

        results.append({
            'Planet': planet,
            'Placed in (WS place)': wsh_house,
            'Net': net,
            'Lean': lean,
            'Standing': 'app arithmetic, not a source verdict',
            'If in a suitable condition': planets_in_houses_cell(wsh_house, planet, 'Good'),
            'If in a bad condition': planets_in_houses_cell(wsh_house, planet, 'Bad'),
            'Rhetorius and Firmicus, as the texts state it': rhetorius_entries_cell(wsh_house, planet),
            'Shared PN IV passages': ' · '.join(_printed_half(h) for h in PLANETS_IN_HOUSES[wsh_house][planet]['PN IV']['Shared']) or '—',
            'Joint testimony IDs': [e['testimony_id'] for e in PLANETS_IN_HOUSES[wsh_house][planet]['Rhetorius'] if 'testimony_id' in e],
            'Entries': [rhetorius_entry_text(e) for e in PLANETS_IN_HOUSES[wsh_house][planet]['Rhetorius']],
        })
    return results


def topical_testimony_count(planetary_data, ascendant_lon):
    """Count indexed source passages, not satisfied predictions. The four
    joint passages indexed under either member count once, by identity."""
    seen = set()
    count = 0
    for planet, data in planetary_data.items():
        if planet == 'North Node':
            continue
        house = get_wsh_house(data['longitude'], ascendant_lon)
        for entry in PLANETS_IN_HOUSES[house][planet]['Rhetorius']:
            identity = entry.get('testimony_id')
            if identity is not None:
                if identity in seen:
                    continue
                seen.add(identity)
            count += 1
    return count


def planets_in_houses_cell(wsh_house, planet, half):
    """The printed form of one PN IV half for one planet in one house:
    "<text> (<locator>)". A half with no locator (the Moon's pointer to her
    own table) prints its text alone."""
    h = PLANETS_IN_HOUSES[wsh_house][planet]['PN IV'][half]
    return _printed_half(h)


def _printed_half(h):
    return h['text'] if h['cite'] == '' else f"{h['text']} ({h['cite']})"


def rhetorius_entry_text(entry):
    """One testimony entry as the page prints it: the author, his own axis
    in parentheses, the text, "(portionally)" where Firmicus states the
    placement so, and the locator in square brackets."""
    portional = ' (portionally)' if entry['portional'] else ''
    axis = JOINT_CONFIGURATION_LABEL if entry['axis'] == 'joint' else entry['axis']
    return f"{entry['author']} ({axis}): {entry['text']}{portional} [{entry['cite']}]"


# A cell whose entries' texts run past this many words prints only the
# entries that are not conditional (those whose whole reading rests on a
# stated configuration go to the row's detail, whole -- never trimmed of
# their conditions to fit) and says how many it sent there.
RHETORIUS_CELL_WORDS = 60

def rhetorius_entries_cell(wsh_house, planet):
    """The printed form of the third column for one planet in one house:
    the entries in order, " · "-separated, a dash for an empty list. Over
    RHETORIUS_CELL_WORDS the conditional entries are left to the row's
    detail (all entries if every one is conditional) and the cell ends with
    the count it sent there."""
    entries = PLANETS_IN_HOUSES[wsh_house][planet]['Rhetorius']
    if not entries:
        return '—'
    shown = entries
    if sum(len(e['text'].split()) for e in entries) > RHETORIUS_CELL_WORDS:
        shown = [e for e in entries if not e['conditional']] or entries
    text = ' · '.join(rhetorius_entry_text(e) for e in shown)
    deferred = len(entries) - len(shown)
    if deferred:
        text += f" · {deferred} conditional {'entry' if deferred == 1 else 'entries'} in the row's detail"
    return text


def evaluate_moon_in_houses(planetary_data, ascendant_lon):
    """The Moon in the twelve houses by her transit, PN IV VII.8, 1-12, as a
    natal analogy: one unsplit row per house (House, Reading, Locator), the
    natal Moon's own whole-sign house marked 'Yes' in 'Natal Moon here'."""
    moon = planetary_data.get('Moon')
    moon_house = get_wsh_house(moon['longitude'], ascendant_lon) if moon else None
    return [{'House': h,
             'Reading': MOON_IN_HOUSES_VII8[h]['text'],
             'Locator': MOON_IN_HOUSES_VII8[h]['cite'],
             'Natal Moon here': 'Yes' if h == moon_house else ''}
            for h in range(1, 13)]

# --- Chronocrator Matrix (Time Lords): Profections & Distributions -------

# --- The planetary years (Gr. Intr. VII.8, Figure 146) -- DISPLAY ONLY --
# Decision D-3 (2026-09-08): On Times stays under the Revolutions deferral
# for implementation, because every rule it gives is contradicted by
# another passage and PN IV is the only source that could break the ties.
# The one thing admitted now is this table with the two placement rules
# beside it, both labelled, neither applied to anything. Figure 146 was
# rebuilt 2026-09-07 and is doubly verified: every cell matches the prose
# restatement at VII.8, 3-8, and the fardar column sums to the 75 years
# the text itself totals (VII.8, 3). Pinned in tests/test_base_tables.py.
#
# THE MIDDLE COLUMN USES TWO CONSTRUCTIONS, which is why the luminaries look
# anomalous and must not be "corrected" to a single rule. The five planets take
# the ordinary mean of least and great -- Saturn (30+57)/2 = 43.5, Jupiter
# (12+79)/2 = 45.5, Mars (15+66)/2 = 40.5, Venus (8+82)/2 = 45, Mercury
# (20+76)/2 = 48. The two luminaries halve the great year first:
# (least + great/2)/2, giving the Sun (19+60)/2 = 39.5 and the Moon
# (25+54)/2 = 39.5, not the 69.5 and 66.5 an ordinary mean would give. The
# second construction is stated outright at Valens VII.5, now read in Riley's
# translation ("The sun has half of 120 years and hence receives 60; its
# minimum period is 19. The total is 79, half of which is 39 years, 6 months";
# the Moon likewise; his Venus is a "complete period of 84", not 82); first
# established by an independent source reconstruction, 2026-09-09, after two
# earlier passes here had assumed one rule for all seven and read the
# luminaries as an error.
# NOT CLOSED BY THAT: the witnesses split four and three. For 39 1/2 --
# Valens VII.5 (Riley, read 2026-09-15); Gr. Intr. VII.8, 3-8 with Figure 146 (the 2020 Arabic volume, photographed);
# Abu Bakr, On Nativities I.16 (PN II printed p. 133, read against the photograph 2026-09-16: "by the halving
# of their greater years and the addition of their lesser years, and the
# halving of all of them assembled together"); the course's table; PN IV
# I.8, 12 is Abu Ma'shar again. For the ordinary mean, Sun 69 1/2 / Moon
# 66 1/2 -- Masha'allah, Book of Aristotle III.1.8 (PN I printed p. 60, the years table, read against the photograph 2026-09-16);
# Abu 'Ali, Judgments of Nativities Ch. 4 (PN I printed p. 235, read against the photograph 2026-09-16);
# the Latin Gr. Intr. Figure 108 as ITA VII.2 prints it (p. 332,
# read on the page by the owner). Dykes calls 69 1/2 "traditional" at PN II
# fn 625. Owner's decision of 2026-09-15 (reconciliation decision 1, as
# recommended): keep 39 1/2, and say all six on the Reference page.
PLANETARY_YEARS = {
    'Saturn':  {'fardar': 11, 'lesser': 30, 'middle': 43.5, 'greater': 57,  'mighty': 265},
    'Jupiter': {'fardar': 12, 'lesser': 12, 'middle': 45.5, 'greater': 79,  'mighty': 427},
    'Mars':    {'fardar': 7,  'lesser': 15, 'middle': 40.5, 'greater': 66,  'mighty': 284},
    'Sun':     {'fardar': 10, 'lesser': 19, 'middle': 39.5, 'greater': 120, 'mighty': 1461},
    'Venus':   {'fardar': 8,  'lesser': 8,  'middle': 45,   'greater': 82,  'mighty': 1151},
    'Mercury': {'fardar': 13, 'lesser': 20, 'middle': 48,   'greater': 76,  'mighty': 480},
    'Moon':    {'fardar': 9,  'lesser': 25, 'middle': 39.5, 'greater': 108, 'mighty': 520},
}
NODE_FARDAR_YEARS = {'Head': 3, 'Tail': 2}

def reference_planetary_years_rows():
    """The planetary years as the course's Handy Tables print them (Lesson
    5: lesser, middle, greater, mighty) with the fardar period (PN IV IV.1,
    2), for the Reference tables page. DISPLAY ONLY, and a table rather
    than a grant: nothing here applies a planet's years to a judgment,
    which is what D-3 forbids -- see D3_GRANT_READERS in the tests."""
    return [{'Planet': p, 'Lesser': y['lesser'], 'Middle': y['middle'], 'Greater': y['greater'],
             'Mighty': y['mighty'], 'Fardar (years)': y['fardar']} for p, y in PLANETARY_YEARS.items()]

def evaluate_planetary_years_display(planetary_data, cusps, ascendant_lon, sect, essential, supplement=False):
    """Figure 146 beside each planet's placement, with the grade On
    Nativities 1.20, 7-34 would give it AS HOUSE-MASTER (the corpus's one
    natal grant, FINAL-A1; placed by the DIVISION, the POWER unit of the
    owner's ruling of 2026-09-11 -- this column had read whole signs and
    only 10, 11, 16 and 17), and what On Times 4, 7 would give it -- a
    rule for a QUESTION chart ("in the hour of the question", 4, 2), shown
    for comparison only. The grant is applied to one planet, the
    house-master the Timing page names from 1.15; here every planet is
    shown as if it were.

    On Times 4, 7 (of the ruler of the question's victor): "if the ruler
    was in a stake, eastern, it grants its greater years; or if it was in
    what follows the stakes, it grants its middle years; and if it was
    falling, it grants its lesser years." Stake/succedent/falling by the
    division with the five-degree carry-over, as elsewhere.

    `supplement`: add the column of Abu 'Ali's ladder (JN Ch. 3, with 'Umar
    TBN I.4.3), filled only beside a "1.20 silent" cell -- reconciliation
    decision 9, the supplement depth only."""
    rows = []
    sun_lon = planetary_data['Sun']['longitude']
    for planet in ('Saturn', 'Jupiter', 'Mars', 'Sun', 'Venus', 'Mercury', 'Moon'):
        lon = planetary_data[planet]['longitude']
        years = PLANETARY_YEARS[planet]
        ws = get_wsh_house(lon, ascendant_lon)
        q = get_effective_house(lon, cusps)
        ess = essential.get(planet, {})
        in_share = any(ess.get(k) for k in ('Domicile', 'Exalt', 'Triplicity', 'Term', 'Face'))
        if planet == 'Sun':
            phase_side = '-'
        else:
            phase_side = solar_phase(planet, lon, sun_lon, planetary_data[planet].get('speed_in_lon'))[1] or '-'
        eastern = phase_side == 'eastern'
        side = ("not defined for the Moon in these years rules" if planet == 'Moon' else phase_side)
        # On Times 4, 7 -- the question-chart rule, for comparison
        if q in ANGLE_HOUSES:
            times = 'greater (in a stake, eastern; 4, 7)' if eastern else 'in a stake but not eastern: 4, 7 gives no value'
        elif q in SUCCEDENT_HOUSES:
            times = 'middle (follows the stakes; 4, 7)'
        else:
            times = 'lesser (falling; 4, 7)'
        # On Nativities 1.20, 7-34 in full, by the division
        g = sahl_house_master_years(planet, planetary_data, cusps, sect, essential)
        if isinstance(g['grade'], UnresolvedResult):
            nat = g['result']
        elif g.get('indication') == 'void':
            nat = g['text']
        elif g['grade'] is None:
            nat = '1.20 silent'
        elif g['years'] is not None:
            nat = f"{g['grade']} ({g['sentence']})"
        else:
            nat = f"{g['grade']} ({g['sentence']}; count not restated)"
        if g['flags']:
            nat += ' -- 14-15 printed, not applied'
        row = {
            'Planet': planet, 'Lesser': years['lesser'], 'Middle': years['middle'], 'Greater': years['greater'],
            'Mighty': years['mighty'], 'Fardar': years['fardar'],
            'WS place': ws, 'Division (5 deg at the stakes)': q, 'Side of the Sun': side, 'In a share': 'yes' if in_share else 'no',
            'On Nativities 1.20 grants (as house-master)': nat,
            'On Times 4, 7 (a question chart, 4, 2): for comparison': times,
        }
        if supplement:
            j = (jn_years_fallback(planet, planetary_data, cusps, sect, essential)
                 if g['grade'] is None or isinstance(g['grade'], UnresolvedResult) else None)
            if j and isinstance(j['result'], UnresolvedResult):
                fallback = j['result']
            elif j:
                fallback = (f"{j['class']}" + (f", {j['count']:g}" if j['unit'] == 'years' else f" ({j['count']:g})")
                            + (" -- " + ", ".join(st_ for st_, _ in j['steps']) if j['steps'] else ""))
            else:
                fallback = '-'
            row[f"Where 1.20 is silent or conditional: {JN_YEARS_CITATION}; the supplement"] = fallback
        rows.append(row)
    return rows

# =========================================================================
# 3b. PERSIAN NATIVITIES IV -- THE TIMING APPARATUS  (D-3 closed 2026-09-10)
# =========================================================================
#
# Abu Ma'shar, On the Revolutions of the Years of Nativities (Persian
# Nativities IV, tr. Dykes), read 2026-09-10 and answered in
# process/tae_docs/synthesis/04_timing_answers_2026-09-10.md. Citations are Book.chapter,
# sentence -- "III.1, 13" is Book III, chapter 1, sentence 13 -- and were
# re-checked against the corpus at blob 295eb22 before this code was
# written (see process/tae_docs/synthesis/15_pn4_implementation_2026-09-10.md).
#
# WHAT IS DELIBERATELY ABSENT, and why.
#
# The releaser/house-master/cutter chain is NOT implemented. PN IV names
# the five releasers -- "the Sun, Moon, Ascendant, Lot of Fortune, or the
# degree of the meeting or degree of the opposition" (III.3, 1) -- and
# gives the frame (the house-master is the distributor of the releaser's
# natal position, Dykes' Figure 50), but it never states how to CHOOSE
# among the five, how many years the house-master grants, or how
# increasers and decreasers are counted. Abu Ma'shar says so himself:
# those "who look into it are wandering around in the dark; but a
# statement of the truth of that ... is found in the book which we worked
# on concerning nativities" (IX.8, 123) -- a book outside this corpus.
#
# Nothing below depends on that choice. The distributions implemented here
# are the one taken FROM THE ASCENDANT, which II.2, 6-7 lists as an
# indicator separate from the one taken from the longevity releaser, and
# which III.1, 14 says the Persians alone called the "jar bakhtar"; and,
# since 2026-09-10, the ones taken FROM THE MERIDIAN -- the degrees of the
# Midheaven and the fourth, "directed by the ascensions of the right
# sphere" (III.1, 12; fn 14: "or rather, the IC itself"). What PN IV does
# NOT say about the meridian distribution, and the page says instead: it
# gives it no topic (the "profession" reading is fn 4, al-Qabisi IV.12,
# an editor's note), it does not list it among the year's indicators
# (II.2), it works no example of it (the III.1, 19-45 example directs the
# Ascendant only), and its partner-at-birth rule (III.1, 23-25) is worded
# for the Ascendant and carried to the meridian by analogy. A planet ON the
# degree of the Midheaven or the fourth (or the Ascendant), which III.1, 12
# also assigns to that ascension, is directed as the degree is since
# 2026-09-11 (GAP-37, the owner's ruling (e): the degree itself, numerical
# tolerance, no orb -- PN4_ASCENSION_RULE, pn4_axis_of).
#
# The third case of III.1, 12 -- everything that is neither the Ascendant
# nor the meridian, directed "according to what we stated in our book [on
# that topic]" -- was listed as unavailable until 2026-09-15 (never as a
# prohibition: III.1, 5 directs all planets and Lots). PN IV defers the
# method to a book it does not reproduce; Dykes' fn 16 identifies it as
# Ptolemy's proportional semi-arcs (VI.2, 21 fn 33 the same). Built under
# reconciliation decision 5 from al-Qabisi IV.11-12 (ITA VIII.2.2) and
# Dykes's Appendix E (ITA pp. 402-407), whose worked example is the
# fixture: semi_arc_direction and pn4_distribution_by_semi_arcs, below the
# meridian distribution.

PN4_SEVEN = ('Saturn', 'Jupiter', 'Mars', 'Sun', 'Venus', 'Mercury', 'Moon')

# The order of the spheres, highest first -- "according to the succession
# of their spheres" (IV.1, 3). CHALDEAN_ORDER above is the same cycle
# rotated to start at Mars for the planetary hours; written out again here
# because the fardar and its sub-periods both index into it by position,
# and a rotation whose starting point is incidental to another doctrine is
# not a safe thing to index.
PN4_DESCENDING_SPHERES = ('Saturn', 'Jupiter', 'Mars', 'Sun', 'Venus', 'Mercury', 'Moon')

PN4_NODE_ORDER = ('Head', 'Tail')

# Quadruplicity under PN IV's names. "Convertible" is Dykes' rendering of
# the cardinal signs; "having two bodies" of the common ones (IX.1, 26-30).
PN4_QUADRUPLICITY = {
    'Aries': 'convertible', 'Cancer': 'convertible', 'Libra': 'convertible', 'Capricorn': 'convertible',
    'Taurus': 'fixed', 'Leo': 'fixed', 'Scorpio': 'fixed', 'Aquarius': 'fixed',
    'Gemini': 'double-bodied', 'Virgo': 'double-bodied', 'Sagittarius': 'double-bodied', 'Pisces': 'double-bodied',
}

# --- III.1, 13: the rate ladder ------------------------------------------
# 1 deg = 1 year, 5' = 1 month, 1' = 6 days, 10" = 1 day, 25''' = 1 hour,
# on an idealised year of twelve 30-day months (fn 17). Matching Sahl,
# On Nativities 1.18, 21 exactly and extending it by one rung.
#
# The ladder is ONE conversion, not five: a degree is 360 idealised days
# and every rung follows from that. 25''' -- twenty-five THIRDS, a
# sixtieth of a second of arc -- is the rung that closes it, because 10"
# is a day and 10"/24 = 25''' exactly.
#
# The corpus READ `every 25" one hour` until 2026-09-11, which would make
# an hour two and a half days long; it now reads 25''' as the page prints
# (photo-verified, p. 288: PN4_READTHROUGH_FINDINGS_2026-09-10.md, D-07;
# repaired in PN4_REPAIRS_2026-09-11.md). The arithmetic settles it without
# the photograph, and the tests assert both halves.
PN4_IDEALISED_DAYS_PER_YEAR = 360.0
PN4_IDEALISED_DAYS_PER_MONTH = 30.0

def pn4_arc_to_time(arc_degrees):
    """III.1, 13. An arc of direction in degrees -> Abu Ma'shar's years,
    months, days and hours on the idealised 360-day year."""
    total = float(arc_degrees) * PN4_IDEALISED_DAYS_PER_YEAR
    years, rem = divmod(total, PN4_IDEALISED_DAYS_PER_YEAR)
    months, rem = divmod(rem, PN4_IDEALISED_DAYS_PER_MONTH)
    days, rem = divmod(rem, 1.0)
    return {'years': int(years), 'months': int(months), 'days': int(days), 'hours': rem * 24.0}

def pn4_format_arc_time(arc_degrees):
    t = pn4_arc_to_time(arc_degrees)
    return f"{t['years']}y {t['months']}m {t['days']}d {t['hours']:.1f}h"

# III.1, 6: the unit of a directed degree is keyed to the LEVEL OF THE
# CHART, not to sign type, planetary strength, quadruplicity or speed.
# This is orthogonal to corpus disagreement #5, which asks a different
# question and stays open; PN IV must not be cited on any side of it.
PN4_DIRECTION_UNITS = {
    'root': 'years',
    'revolution of the year': 'months and days',
    'revolution of the month': 'days and hours',
}

def pn4_direction_unit(chart_level):
    """III.1, 6."""
    return PN4_DIRECTION_UNITS.get(chart_level)

# --- III.1, 12: which ascensions measure which point ----------------------
# III.1, 12, and the STATE of each of its three cases in this engine. The
# table must not flatten them: the first two cases are built, each for the
# DEGREE of its point and for a planet ON that degree itself; the third
# case's method is not in PN IV at all, being deferred to a book he does
# not reproduce (fn 16: Ptolemy's proportional semi-arcs; VI.2, 21 fn 33
# "That is, by normal proportional semi-arcs"), and since 2026-09-15
# (reconciliation decision 5) is built from al-Qabisi (ITA VIII.2.2) and
# Dykes's Appendix E, for the DEGREE of every other point (see
# semi_arc_direction). The owner's ruling of 2026-09-11 (evening; GAP-37 /
# PN4R-4b-4, reading (e)): "in the Ascendant / Midheaven / fourth" means
# ON the axial degree, recognised with a NUMERICAL tolerance
# (PN4_AXIS_TOLERANCE, floating-point equality -- not an astrological orb:
# no 3, no 5, no band); get_effective_house and the five-degree carry-over
# have no role in method selection; every planet not on an axis is
# directed by proportional semi-arcs (the third case), with no fall-back
# to RA or OA. The Descendant stays out (fn 15: omitted by the author).
# The state strings are asserted verbatim by
# test_pn4_printed_reference_tables_derive_from_the_rules.
PN4_AXIS_TOLERANCE = 1e-9   # degrees: floating-point equality with the axial degree, documented, not an orb
PN4_SEMIARCS_SOURCES = 'al-Qabisi, Introduction IV (ITA VIII.2.2); Dykes, ITA Appendix E'
PN4_SEMIARCS_REFUSED = ('Refused at this latitude: some degrees never rise or set there, and a degree without a '
                        'semi-arc has nothing to proportion.')
PN4_ASCENSION_RULE = {
    'Ascendant': ('oblique ascensions of the birth latitude',
                  'applied to the degree of the Ascendant and to a planet on the degree itself (numerical tolerance, no orb)'),
    'Midheaven': ('right ascensions',
                  'applied to the degrees of the Midheaven and the fourth and to a planet on the degree itself (numerical tolerance, no orb)'),
    'Fourth (IC)': ('right ascensions',
                    'applied to the degrees of the Midheaven and the fourth and to a planet on the degree itself (numerical tolerance, no orb)'),
    'anything else': ('proportional semi-arcs',
                      "applied to the degree of every point on none of the three axial degrees (III.1, 5 directs all "
                      "planets and Lots): the significator's distance from the meridian in proportion to its semi-arc, "
                      "carried to the promittor's semi-arc -- Ptolemy's method as Dykes identifies it (III.1, 12 fn 16; "
                      "VI.2, 21 fn 33), stated by al-Qabisi (ITA VIII.2.2) and worked by Dykes (ITA Appendix E)"),
}

def pn4_ascension_measure(point):
    """III.1, 12. Returns (measure, state) -- see PN4_ASCENSION_RULE."""
    return PN4_ASCENSION_RULE.get(point, PN4_ASCENSION_RULE['anything else'])

def pn4_axis_of(lon, ascendant_lon, mc_lon, tolerance=PN4_AXIS_TOLERANCE):
    """III.1, 12 under the ruling (e): which of the three axial degrees the
    longitude is ON -- 'Ascendant', 'Midheaven', 'Fourth (IC)' -- or None.
    The wrapped signed offset from each axis is compared with the numerical
    tolerance; the Descendant is not an axis of III.1, 12 (fn 15)."""
    def off(a, b):
        return abs(((a - b + 180.0) % 360.0) - 180.0)
    if off(lon, ascendant_lon) <= tolerance:
        return 'Ascendant'
    if off(lon, mc_lon) <= tolerance:
        return 'Midheaven'
    if off(lon, mc_lon + 180.0) <= tolerance:
        return 'Fourth (IC)'
    return None

def _pn4_sentence_case(text):
    """Upper-case the first letter only. str.capitalize() would lower the
    rest and turn "PN IV" into "pn iv"."""
    return text[:1].upper() + text[1:]

# The three reference tables the Timing page prints beside the working
# ones, so a number on the page can be traced to the sentence that fixes
# it without leaving the app. Each is DERIVED from the rule above it
# rather than restating it, so the printed table cannot drift from the
# rule the engine applies.
PN4_LADDER_ROWS = [
    {'Arc': label, 'Is': f"{v} {unit}" if v != 1 else f"1 {unit.rstrip('s')}"}
    for label, arc, unit, v in (
        ('1\u00b0', 1.0, 'years', 1), ("5\u2032", 5 / 60, 'months', 1),
        ("1\u2032", 1 / 60, 'days', 6), ('10\u2033', 10 / 3600, 'days', 1),
        ('25\u2034', 25 / 216000.0, 'hours', 1))
    if pn4_arc_to_time(arc)[unit] == v            # each rung checked as it is printed
]
PN4_UNIT_ROWS = [{'Directed in the': label, 'A degree is': pn4_direction_unit(key)}
                 for key, label in (('root', 'Root of the nativity'),
                                    ('revolution of the year', 'Revolution of the year'),
                                    ('revolution of the month', 'Revolution of the month'))]
PN4_ASCENSION_ROWS = [
    {'Point directed': label, 'Measured in': _pn4_sentence_case(pn4_ascension_measure(point)[0]),
     'In this app': pn4_ascension_measure(point)[1]}
    for point, label in (('Ascendant', 'Ascendant, and things in it'),
                         ('Midheaven', 'Midheaven, or the fourth'),
                         ('anything else', 'Anything else'))
]

def pn4_bound_lord(lon):
    """III.1, 11: the lord of the bound the directed degree stands in is
    the distributor, 'whether it looked at [the bound] or not'."""
    sign = get_zodiac_sign(lon)
    degree_in_sign = lon % 30.0
    return next((lord for limit, lord in EGYPTIAN_TERMS.get(sign, []) if degree_in_sign < limit), '-')

def pn4_bound_starts():
    """The longitude at which every Egyptian bound begins, once round the
    circle: (longitude, lord, sign). EGYPTIAN_TERMS stores each bound's
    END degree, so a bound starts where the previous one ended."""
    out = []
    for i, sign in enumerate(SIGN_ORDER):
        base, start = i * 30.0, 0.0
        for limit, lord in EGYPTIAN_TERMS[sign]:
            out.append((base + start, lord, sign))
            start = float(limit)
    return out

# I.7, 12 and III.1, 15: "the positions of the planets and their rays."
# The ray is a POINT, not a body with an orb -- the partner holds "until
# it encounters another planet by its body or rays" (III.1, 16), so there
# is always exactly one partner and never a contested overlap. Transit
# orbs (Figure 83, p. 397) belong to a different technique and must not be
# carried in here.
PN4_RAY_OFFSETS = ((60.0, 'sextile'), (90.0, 'square'), (120.0, 'trine'), (180.0, 'opposition'))

def pn4_bodies_and_rays(planetary_data):
    """Every body and ray a directed degree can meet, as
    (longitude, kind, planet, aspect). Both sides of each aspect, "right or
    left"; the opposition is one point, not two."""
    out = []
    for planet in PN4_SEVEN:
        row = planetary_data.get(planet)
        if not row:
            continue
        lon = row['longitude'] % 360.0
        out.append((lon, 'body', planet, 'body'))
        for offset, name in PN4_RAY_OFFSETS:
            out.append(((lon + offset) % 360.0, 'ray', planet, name))
            if offset != 180.0:
                out.append(((lon - offset) % 360.0, 'ray', planet, name))
    return out

# III.2, 103-104: the strength of a partner. Body first, then the rays in
# the order opposition > square > trine > sextile -- HARD ASPECTS ABOVE
# SOFT ONES, which is the reverse of the usual benefic intuition and is
# easy to get backwards.
PN4_PARTNER_RANK = {'body': 0, 'opposition': 1, 'square': 2, 'trine': 3, 'sextile': 4}

def pn4_partner_strength(aspect):
    return PN4_PARTNER_RANK.get(aspect)

PN4_DISTRIBUTION_SPAN_YEARS = 120.0

PN4_OPENING_SOURCE = 'PN IV III.1, 23–25; opening-partner convention'


def pn4_result_text(value, absent='none'):
    """Render a distribution fact without giving an unknown a truth value."""
    if is_unresolved(value):
        alternatives = '; '.join(f"{name}: {pn4_result_text(v)}" for name, v in value.alternatives)
        return f"Unresolved — {value.reason}" + (f" ({alternatives})" if value.display_alternatives and alternatives else '')
    if value is None:
        return absent
    if isinstance(value, (list, tuple)):
        return '; '.join(pn4_result_text(v) for v in value)
    if isinstance(value, dict):
        return '; '.join(f'{k}: {pn4_result_text(v)}' for k, v in value.items())
    return str(value)


def _pn4_opening_aware(function):
    """Evaluate all tied opening alternatives, never select a partner.

    Partner, aspect and origin share contact labels. A complete consumer is
    evaluated for each coherent alternative; changing results remain typed
    unknowns. The original trajectory and its partner are never rewritten.
    """
    @wraps(function)
    def run(*args, **kwargs):
        groups = {}
        def collect(value):
            if is_unresolved(value) and value.source == PN4_OPENING_SOURCE:
                groups[tuple(n for n, _ in value.alternatives)] = value
            elif isinstance(value, dict):
                for v in value.values(): collect(v)
            elif isinstance(value, (tuple, list)):
                for v in value: collect(v)
        collect((args, kwargs))
        if not groups:
            return function(*args, **kwargs)
        def resolve(value, choices):
            if is_unresolved(value) and value.source == PN4_OPENING_SOURCE:
                return dict(value.alternatives)[choices[tuple(n for n, _ in value.alternatives)]]
            if isinstance(value, dict): return {k: resolve(v, choices) for k, v in value.items()}
            if isinstance(value, list): return [resolve(v, choices) for v in value]
            if isinstance(value, tuple): return tuple(resolve(v, choices) for v in value)
            return value
        outcomes, labels = [], []
        for options in product(*groups):
            choices = dict(zip(groups, options))
            outcomes.append(function(*resolve(args, choices), **resolve(kwargs, choices)))
            labels.append(' / '.join(options))
        def merge(values):
            if all(repr(v) == repr(values[0]) for v in values[1:]): return values[0]
            if all(isinstance(v, tuple) for v in values) and len({len(v) for v in values}) == 1:
                return tuple(merge(list(v)) for v in zip(*values))
            if all(isinstance(v, dict) for v in values):
                keys = list(dict.fromkeys(k for v in values for k in v))
                return {k: merge([v.get(k) for v in values]) for k in keys}
            if all(isinstance(v, list) for v in values) and all(isinstance(r, dict) for v in values for r in v):
                stable = ('From age', 'From day', '#', 'Test', 'kind',
                          'In the bound, in the revolution', 'Planet')
                if len({len(v) for v in values}) == 1 and all(
                        any(k in rows[0] and all(r.get(k) == rows[0][k] for r in rows) for k in stable)
                        for rows in zip(*values)):
                    return [merge(list(rows)) for rows in zip(*values)]
                # Existing mechanism A preserves conditional row membership.
                def retag(value):
                    if is_unresolved(value):
                        return UnresolvedResult('the opening partner is tied', PN4_OPENING_SOURCE,
                            value.alternatives, display_alternatives=value.display_alternatives)
                    if isinstance(value, dict): return {k: retag(v) for k, v in value.items()}
                    if isinstance(value, list): return [retag(v) for v in value]
                    return value
                return retag(_connection_merge(values))
            return UnresolvedResult('the opening partner is tied', PN4_OPENING_SOURCE,
                                    tuple(zip(labels, values)))
        return merge(outcomes)
    return run


def _pn4_distribute(planetary_data, start_lon, measure, span_years, point_label,
                    opening_window='sign', epoch='birth', significator=None):
    """The bound-by-bound direction of III.1, 7-16, shared by every point
    this engine distributes: `start_lon` directed through the Egyptian
    bounds, naming at every moment a distributor -- the lord of the bound
    reached (III.1, 11) -- and a partner, the most recent body or ray the
    direction has met (III.1, 15-16). `measure` maps an ecliptic degree
    to the ascension the point is directed in (III.1, 12): the oblique
    ascension of the birth latitude for the Ascendant, the right ascension
    for the meridian. Callers choose it and own its domain.

    Returns a list of segments in age order, each
    {from, to, from_lon, distributor, partner, partner_aspect, ...}, where
    `from_lon` is the zodiacal degree the direction stands on as the
    segment opens.

    `span_years` is in units of the measure (degrees of arc); the caller
    converts to time. `opening_window` is where the partner already in
    place is looked for: 'sign' (III.1, 23-25, back to the beginning of the
    sign) or 'bound' (IX.7, 30, "in the bounds of the degree"). `epoch`
    names the moment the direction starts from in the row text.

    Time comes from the arc: one degree of ascension is one year
    (III.1, 13). Order comes from the longitudes: both measures increase
    with the longitude wherever they are defined, so "the next bound round
    the zodiac" and "the next arc of direction" are the same sequence.
    """
    start_lon %= 360.0
    m0 = measure(start_lon)
    meetings = pn4_bodies_and_rays(planetary_data)

    # III.1, 23-25: the partner AT BIRTH. Look back from the directed
    # degree to the beginning of its sign; the nearest body or ray behind
    # it is already the partner. "But since I did not find a planet nor
    # its rays from the beginning of the sign up to the degree of the
    # Ascendant, Venus became the distributor without a planet partnering
    # with her" -- the search does NOT run back past the start of the
    # sign. Worded for the Ascendant; the meridian gets it by analogy, and
    # the page says so.
    #
    # IX.7, 30 (the small days) names a narrower window, "in the bounds of
    # the degree of the Ascendant of the revolution": the same look-back,
    # stopping at the beginning of the BOUND. The sentence does not say
    # whether a body ahead of the degree within its bound manages from the
    # first day or from the day the degree reaches it; this reads it the
    # way III.1, 23-25 is worked, and the page says so.
    if opening_window == 'sign':
        window_start = (start_lon // 30.0) * 30.0
        alone_cite = 'III.1, 25'
    elif opening_window == 'bound':
        window_start = max(lon for lon, _lord, _sign in pn4_bound_starts() if lon <= start_lon)
        alone_cite = 'IX.7, 24 and 30'
    else:
        raise ValueError(f"opening_window must be 'sign' or 'bound', not {opening_window!r}")
    behind = [m for m in meetings if window_start <= m[0] <= start_lon and m[2] != significator]
    latest = max((m[0] for m in behind), default=None)
    openings = sorted(set(m for m in behind if m[0] == latest))
    opening = openings[0] if len(openings) == 1 else None

    # Everything the direction will meet, by arc from the starting point.
    events = [(lon, 'bound', lord, 'bound') for lon, lord, _sign in pn4_bound_starts()]
    events.extend(meetings)

    dated = []
    for lon, kind, who, aspect in events:
        arc = (measure(lon) - m0) % 360.0
        if 0.0 < arc <= span_years:
            dated.append((arc, lon, kind, who, aspect))
    dated.sort(key=lambda e: e[0])

    distributor = pn4_bound_lord(start_lon)
    partner = opening[2] if opening else None
    partner_aspect = opening[3] if opening else None
    partner_from = f'none: the distributor acts alone ({alone_cite})'
    opened_by = (f'at {epoch}: {partner} by {partner_aspect}' if opening
                 else f'at {epoch}: the distributor alone')

    if opening:
        # One source for the opening contact's words: "at the starting
        # degree" on an exact endpoint, "behind" for an earlier contact.
        position = 'at the starting degree' if opening[0] == start_lon else f'behind the {point_label}'
        partner_from = f"{opening[2]} by {opening[3]} at {get_degree_string(opening[0])}, {position}"
    elif openings:
        def description(m):
            position = 'at the starting degree' if m[0] == start_lon else f'behind the {point_label}'
            return f"{m[2]} by {m[3]} at {get_degree_string(m[0])}, {position}"
        def tied(values):
            return UnresolvedResult('equally placed eligible opening contacts; no precedence is supplied',
                PN4_OPENING_SOURCE, tuple((description(m), value) for m, value in zip(openings, values)))
        partner = tied([m[2] for m in openings])
        partner_aspect = tied([m[3] for m in openings])
        partner_from = tied([description(m) for m in openings])
        opened_by = tied([f'at {epoch}: {description(m)}' for m in openings])

    segments, cursor, from_lon = [], 0.0, start_lon
    for arc, lon, kind, who, aspect in dated:
        if arc - cursor > 1e-9:
            segments.append({
                'from': cursor, 'to': arc, 'from_lon': from_lon, 'distributor': distributor,
                'partner': partner, 'partner_aspect': partner_aspect,
                'partner_from': partner_from, 'opened_by': opened_by,
            })
            cursor = arc
        if kind == 'bound':
            distributor = who
            opened_by = f"bound of {who} at {get_degree_string(lon)}"
        else:
            partner, partner_aspect = who, aspect
            partner_from = (f"{who} by body at {get_degree_string(lon)}" if aspect == 'body'
                            else f"{who} by {aspect} at {get_degree_string(lon)}")
            opened_by = partner_from
        from_lon = lon
    if cursor < span_years:
        segments.append({
            'from': cursor, 'to': span_years, 'from_lon': from_lon, 'distributor': distributor,
            'partner': partner, 'partner_aspect': partner_aspect,
            'partner_from': partner_from, 'opened_by': opened_by,
        })
    return segments

def pn4_distribution_from_ascendant(planetary_data, ascendant_lon, obliquity, geo_lat,
                                    span_years=PN4_DISTRIBUTION_SPAN_YEARS, significator=None):
    """The *jar bakhtar* (III.1, 14): the degree of the Ascendant directed
    through the bounds by the oblique ascensions of the birth latitude
    (III.1, 12). Segments as _pn4_distribute returns them, or None where
    the method has no domain.

    ABOVE THE POLAR CIRCLE THIS REFUSES, returning None, on the domain of
    D-23: where |latitude| + obliquity >= 90 some degrees never rise, the
    oblique ascension has no unique inverse, and an arc of direction from
    the Ascendant is not defined. Refusing is the decided behaviour for
    every method in this file that rests on the oblique ascension. The
    meridian distribution below does not rest on it, and does not refuse.
    """
    if not _ascensional_method_applies(obliquity, geo_lat):
        return None
    return _pn4_distribute(planetary_data, ascendant_lon,
                           lambda lon: _oblique_ascension(lon, obliquity, geo_lat),
                           span_years, 'Ascendant', significator=significator)

PN4_MERIDIAN_POINTS = ('Midheaven', 'Fourth (IC)')

def pn4_distribution_from_meridian(planetary_data, mc_lon, obliquity, point='Midheaven',
                                   span_years=PN4_DISTRIBUTION_SPAN_YEARS, start_lon=None, label=None, significator=None):
    """III.1, 12: "what is in the Midheaven or the fourth is directed by
    the ascensions of the right sphere" -- the degree of the Midheaven, or
    of the fourth (fn 14: "or rather, the IC itself", the point opposite
    it), directed through the bounds by RIGHT ASCENSION, one degree to a
    year (III.1, 13), with the same distributor and partner as any other
    direction (III.1, 10-11, 15-16). Built 2026-09-10.

    This never refuses. Right ascension has no latitude in it and the
    meridian crosses the ecliptic at every latitude, so the arc is defined
    everywhere; D-23's refusal is about inverting the OBLIQUE ascension,
    which this does not do.

    What the source does not supply, and the page admits: no worked
    example of a meridian direction exists in PN IV (the check is
    arithmetic, plus the editor's four-minutes-a-degree animation of
    Appendix A, p. 673); the partner-at-birth rule is worded for the
    Ascendant; the distribution is given no topic by Abu Ma'shar and is
    not among the year's indicators (II.2); planets IN the Midheaven are
    not directed, only its degree."""
    if start_lon is not None:
        # III.1, 12: "what is in the Midheaven or the fourth is directed by the
        # ascensions of the right sphere" -- a PLANET'S degree, by right
        # ascension, labelled by its name (orders GAP-37 / PN4R-4b-4).
        return _pn4_distribute(planetary_data, start_lon % 360.0, lambda lon: _ra_decl(lon, obliquity)[0],
                               span_years, label or f"{get_degree_string(start_lon)} by right ascension", significator=significator)
    if point not in PN4_MERIDIAN_POINTS:
        raise ValueError(f"point must be one of {PN4_MERIDIAN_POINTS}, not {point!r}")
    start = mc_lon if point == 'Midheaven' else mc_lon + 180.0
    return _pn4_distribute(planetary_data, start, lambda lon: _ra_decl(lon, obliquity)[0],
                           span_years, point)

# --- III.1, 12's third case: proportional semi-arcs -----------------------
# "What is not in these three positions is directed according to what we
# stated in our book" (III.1, 12): PN IV defers the method to a book it
# does not reproduce, and Dykes identifies it as Ptolemy's proportional
# semi-arcs (fn 16; VI.2, 21 fn 33, "by normal proportional semi-arcs").
# Built (reconciliation decision 5, 2026-09-15) from the two texts in hand
# that state it: al-Qabisi, Introduction IV.11-12 (ITA VIII.2.2b-e, pp.
# 362-364) -- the significator's distance from the angle in hours of its
# own semi-arc, carried to the promittor's semi-arc -- and Dykes's Appendix
# E (ITA pp. 402-407), which states Gansten's form of the same rule,
#     PromMD - (SigMD / SigSA) * PromSA = Arc,
# shows that al-Qabisi's "equation" (the sixth of the difference between
# the significator of the right circle and the significator of the region,
# multiplied by the hours from the angle) is that formula with the 6
# cancelled, and works it on a chart of 4 September 2010, 2:15:09 PM CDT,
# Minneapolis (93w15'49", 44n58'48"): Venus (promittor) directed to Saturn
# (significator), with the planets' latitudes (A) and without (B). That
# worked example is the fixture, tests/test_semiarcs_2026_09_15.py: every
# printed term and both arcs reproduce to the arc-second from the chart
# cast here.
#
# What Appendix E leaves unsaid and this engine chooses (docstring of
# semi_arc_direction): the sign of the meridian distance, the meridian to
# measure from when the two points are not in one quadrant, and the
# promittor's semi-arc in that case. Al-Qabisi's own rule for a promittor
# in another quarter (IV.12c: direct to the angle between, then from the
# angle, and add) is a different, approximate join and is NOT built; the
# single formula with signed distances is exact for every quadrant.
# The page directs the DEGREE of each point, latitude 0, as the two built
# cases direct degrees and as fn 27 says al-Qabisi's tables did; the
# engine takes latitude, and the fixture holds both of Dykes's forms.

def _wrap_180(x):
    """A signed arc in [-180, 180)."""
    return ((x + 180.0) % 360.0) - 180.0

def semi_arc_terms(lon, lat, ramc, geo_lat, obliquity):
    """The equatorial terms of one point (Appendix E, pp. 403-404): right
    ascension and declination from its longitude and latitude; the
    ascensional difference; the diurnal and nocturnal semi-arcs, 90 + AD and
    90 - AD in degrees of right ascension (closed to 180 from one AD, as
    _semiarcs does); the signed meridian distance from the upper meridian,
    RA - RAMC in [-180, 180), positive on the eastern side (the point has
    yet to culminate), and from the lower meridian, positive on the western
    side (it has yet to reach the fourth) -- primary motion DECREASES both;
    and whether the point is above the horizon, |MD from the upper
    meridian| <= diurnal semi-arc. A point that never rises or sets at this
    latitude (|tan(phi) tan(delta)| >= 1) has 'circumpolar' True and no
    semi-arc to proportion."""
    ra, decl, _r = swe.cotrans((lon % 360.0, lat, 1.0), -obliquity)
    ra %= 360.0
    x = math.tan(math.radians(geo_lat)) * math.tan(math.radians(decl))
    circumpolar = abs(x) >= 1.0
    ad = math.degrees(math.asin(max(-1.0, min(1.0, x))))
    dsa = 90.0 + ad
    md_upper = _wrap_180(ra - ramc)
    return {
        'longitude': lon % 360.0, 'latitude': lat, 'ra': ra, 'declination': decl,
        'ascensional_difference': ad, 'diurnal_sa': dsa, 'nocturnal_sa': 180.0 - dsa,
        'md_upper': md_upper, 'md_lower': _wrap_180(ra - ramc - 180.0),
        'above': abs(md_upper) <= dsa, 'circumpolar': circumpolar,
    }

def semi_arc_direction(sig_lon, sig_lat, prom_lon, prom_lat, ramc, geo_lat, obliquity):
    """The arc of direction, in degrees of right ascension, of a promittor
    to a significator by proportional semi-arcs, exactly as ITA Appendix E
    states it (p. 404):

        Arc = PromMD - (SigMD / SigSA) * PromSA

    -- the significator's meridian distance divided by its semi-arc is the
    share of the semi-arc it has travelled; that share of the promittor's
    semi-arc, taken from the promittor's meridian distance, is what the
    promittor has still to travel by primary motion to reach the place
    proportional to the significator's (al-Qabisi IV.11-12, ITA VIII.2.2:
    the hours from the angle, applied to the other degree's hours).

    Each point's RA and declination come from its longitude AND latitude
    (Appendix E part A directs the bodies; part B, with latitude 0, the
    zodiacal degrees; pass 0 for the second). Returns a dict: 'arc' (None
    when refused), the two points' terms under 'significator' and
    'promittor' (semi_arc_terms), the meridian and semi-arc actually used
    ('meridian': 'upper' or 'lower'; 'sig_md', 'sig_sa', 'prom_md',
    'prom_sa'), the significator's share 'fraction', and 'refused', a
    sentence, where a point never rises or sets at this latitude.

    Where the text is silent, and what is chosen here:
    * SIGN. Appendix E works one quadrant, both points east of the upper
      meridian, and subtracts the smaller RA from the larger. Here the
      meridian distance is SIGNED: positive before the meridian in primary
      motion (east of the Midheaven; west of the fourth), negative after,
      so one formula serves every quadrant and Dykes's numbers come out
      unchanged (+24 23' 27" and +5 43' 16").
    * WHICH MERIDIAN, WHICH SEMI-ARC. The significator's side of the
      horizon decides: above it, both distances are from the upper meridian
      and both semi-arcs are diurnal; below it, from the lower meridian and
      nocturnal (fn 24: below the horizon, "one of the two nocturnal
      semi-arcs"). The promittor's distance is from THAT meridian even when
      it currently stands on the other side of the horizon, and its
      semi-arc is the one of the hemisphere it will be in when it arrives;
      for a promittor below the horizon coming to a significator above it
      this equals the promittor's rise (its oblique ascension less the
      Ascendant's) plus its share of its own diurnal arc. A point ON the
      horizon (|MD| = SA) counts as above.
    * DIRECT ONLY. The arc is taken modulo 360, so a promittor that has
      already passed the proportional place comes round after a
      revolution; converse directions are not built, no text in hand
      asking for them (ITA's glossary, p. 382, has them as an allowance
      of "some later astrologers", a derivation beside the classical
      direct ones).
    * REFUSAL. A point that never rises or sets at this latitude has no
      semi-arc to proportion (the significator's would divide by zero);
      'arc' is None and 'refused' says so, in the file's style.
    """
    sig = semi_arc_terms(sig_lon, sig_lat, ramc, geo_lat, obliquity)
    prom = semi_arc_terms(prom_lon, prom_lat, ramc, geo_lat, obliquity)
    out = {'significator': sig, 'promittor': prom, 'arc': None, 'refused': None}
    if sig['circumpolar'] or prom['circumpolar']:
        who = 'significator' if sig['circumpolar'] else 'promittor'
        out['refused'] = (f"the {who} never rises or sets at this latitude, so it has no semi-arc to proportion")
        return out
    if sig['above']:
        meridian, sig_md, sig_sa, prom_md, prom_sa = 'upper', sig['md_upper'], sig['diurnal_sa'], prom['md_upper'], prom['diurnal_sa']
    else:
        meridian, sig_md, sig_sa, prom_md, prom_sa = 'lower', sig['md_lower'], sig['nocturnal_sa'], prom['md_lower'], prom['nocturnal_sa']
    fraction = sig_md / sig_sa
    out.update({'meridian': meridian, 'sig_md': sig_md, 'sig_sa': sig_sa, 'prom_md': prom_md, 'prom_sa': prom_sa,
                'fraction': fraction, 'raw': prom_md - fraction * prom_sa})
    out['arc'] = out['raw'] % 360.0
    return out

def pn4_distribution_by_semi_arcs(planetary_data, start_lon, obliquity, geo_lat, ramc,
                                  span_years=PN4_DISTRIBUTION_SPAN_YEARS, label=None, significator=None):
    """III.1, 12's third case: the DEGREE of a point that is on none of the
    three axial degrees, directed through the bounds by proportional
    semi-arcs (semi_arc_direction, the point's degree at latitude 0 as
    significator, every bound start, body and ray as a promittor at
    latitude 0), one degree of the arc to a year (III.1, 13), with the
    same distributor and partner as any other direction (III.1, 10-11,
    15-16). Segments as _pn4_distribute returns them, or None where the
    method has no domain: above the polar circle (|latitude| + obliquity
    >= 90, where some degrees never rise or set), as the Ascendant's
    distribution refuses."""
    if not _ascensional_method_applies(obliquity, geo_lat):
        return None
    start_lon %= 360.0
    if semi_arc_terms(start_lon, 0.0, ramc, geo_lat, obliquity)['circumpolar']:
        return None

    def measure(lon):
        return semi_arc_direction(start_lon, 0.0, lon, 0.0, ramc, geo_lat, obliquity)['raw']

    return _pn4_distribute(planetary_data, start_lon, measure, span_years,
                           label or f"{get_degree_string(start_lon)} by proportional semi-arcs", significator=significator)

def pn4_semi_arc_terms_rows(sig_lon, obliquity, geo_lat, ramc, segments, current):
    """The arc and its terms, as the Timing page prints them beside a
    semi-arc distribution: one row for the significator (the point's
    degree), one for the promittor that opened the period now running --
    or the degree itself when the period is the one open at birth -- and
    one for the promittor the direction reaches next. Right ascension,
    declination, the signed meridian distance from the meridian used, the
    semi-arc used, and the arc in degrees and in III.1, 13's time."""
    def fmt(v):
        return f"{v:.2f}°"

    def row(point, d, promittor):
        t = d['promittor'] if promittor else d['significator']
        md, sa = (d['prom_md'], d['prom_sa']) if promittor else (d['sig_md'], d['sig_sa'])
        return {'Point': point, 'Longitude': get_degree_string(t['longitude']),
                'Right ascension': fmt(t['ra']), 'Declination': fmt(t['declination']),
                'Meridian distance': f"{md:+.2f}° from the {'Midheaven' if d['meridian'] == 'upper' else 'fourth'}",
                'Semi-arc': f"{fmt(sa)} ({'diurnal' if d['meridian'] == 'upper' else 'nocturnal'})",
                'Arc': f"{fmt(d['arc'])} = {pn4_format_arc_time(d['arc'])}" if promittor else '-'}

    own = semi_arc_direction(sig_lon, 0.0, sig_lon, 0.0, ramc, geo_lat, obliquity)
    if own['arc'] is None:
        return []
    rows = [row('significator: the degree itself', own, False)]
    if not segments:
        return rows
    idx = segments.index(current) if current in segments else 0
    for seg, what in ((segments[idx], 'opened the period now running'),
                      (segments[idx + 1] if idx + 1 < len(segments) else None, 'reached next')):
        if seg is None:
            continue
        if seg['from'] <= 0.0:
            rows.append({**rows[0], 'Point': 'the period open at birth: the degree itself', 'Arc': fmt(0.0)})
            continue
        d = semi_arc_direction(sig_lon, 0.0, seg['from_lon'], 0.0, ramc, geo_lat, obliquity)
        rows.append(row(f"promittor that {what}: {seg['opened_by']}", d, True))
    return rows

def pn4_semi_arc_stand(planetary_data, natal_lon, obliquity, geo_lat, ramc, age, significator=None):
    """VI.2, 1, "directed from its degree (a year for every degree)": where
    a point's direction by proportional semi-arcs stands at an age -- the
    sign it has reached (every sign starts a bound, so the period's opening
    degree is in the sign the direction stands in), the distributor and
    the partner of the period, and its span. VI.2, 21 sends a displaced
    cusp's degree to "the portions of the hours and the right circle" (fn
    33: proportional semi-arcs), the same operation. A sentence, for the
    turning table's column."""
    segs = pn4_distribution_by_semi_arcs(planetary_data, natal_lon, obliquity, geo_lat, ramc, significator=significator)
    if segs is None:
        return 'by proportional semi-arcs: refused at this latitude, where some degrees never rise or set'
    cur = pn4_distribution_at_age(segs, age)
    if cur is None:
        return f"by proportional semi-arcs: age {age} is past the {PN4_DISTRIBUTION_SPAN_YEARS:g}-year table"
    return (f"by proportional semi-arcs: in {get_zodiac_sign(cur['from_lon'])}, standing from "
            f"{get_degree_string(cur['from_lon'])}; distributor {cur['distributor']}, partner "
            f"{pn4_result_text(cur['partner'])} (age {cur['from']:.2f} to {cur['to']:.2f})")

# --- IX.7, 29-31: "the small days" ---------------------------------------
# "you look at the degree of the Ascendant of the revolution of the year,
# so that you direct from it (for the knowledge of the conditions of the
# days), a day for every 59' 08", until it returns to the degree of the
# Ascendant at the end of the year" (IX.7, 29). The management is the
# distribution's -- a body or ray already in the bound, else the bound
# lords "in the way we have stated" until a planet or ray is reached
# (IX.7, 30; 24) -- and IX.7, 31 names it the small days.
#
# ZODIACAL, on purpose. The sentence gives a rate in degrees of the
# zodiac and promises a return to the same degree at the year's end, and
# 360 / 59'08" is 365.28 days -- the year to within an hour. Abu Ma'shar
# grades it himself: "there is an approximation in it, but the correct
# [approach] is that this way of directing is like the direction of the
# Sun every day ... between this sense which is by approximation and the
# exact one, the second one is easy, [with] no harm in the work"
# (IX.7, 32). The exact form he names -- the degree advancing by the
# Sun's real motion each day -- is not built; nor is Dykes' fn 178, which
# would have it by ascensions (an editor's view). Decided by the owner
# 2026-09-10 between the three, with the stated rate chosen.
#
# What is read into the sentence and said on the page: the bodies and
# rays are the REVOLUTION'S (the sentence sits inside the revolution);
# the days count from the moment of the revolution (fn 161 says a "day"
# is not defined); only the revolution's Ascendant is directed, though
# IX.7, 31 extends the method to "everything of the planets, Lots, and
# houses". No worked example of it exists in PN IV.
PN4_SMALL_DAYS_RATE = (59.0 * 60.0 + 8.0) / 3600.0      # degrees of the zodiac per day, IX.7, 29

def pn4_small_days_arc_to_days(arc_degrees):
    """IX.7, 29: a day for every 59' 08"."""
    return float(arc_degrees) / PN4_SMALL_DAYS_RATE

def pn4_day_point_origins(natal, revolution, supplement=False):
    """IX.7, 27/29-31: natal profection for mighty days, revolution for small days."""
    points = {}
    def add(key, label, root, rev):
        points[key] = {'label': label, 'natal': root, 'revolution': rev,
                       'significator': key.split(':', 1)[1] if key.startswith('planet:') else None}
    for planet in PN4_SEVEN:
        if planet in natal['planetary_data'] and planet in revolution['planetary_data']:
            add('planet:' + planet, planet, natal['planetary_data'][planet]['longitude'],
                revolution['planetary_data'][planet]['longitude'])
    for i, (root, rev) in enumerate(zip(natal['houses'][:12], revolution['houses'][:12]), 1):
        add(f'house:{i}', f'house {i}', root, rev)
    for definition in operative_lot_definitions():
        if definition.get('supplement') and not supplement:
            continue
        def lot(chart):
            return lot_by_id(definition['id'], chart['planetary_data'], chart['ascendant'],
                             chart['houses'], chart['sect'])
        add('lot:' + definition['id'], definition['name'], lot(natal), lot(revolution))
    return points


def pn4_small_days(sr_planetary_data, sr_ascendant_lon, point_label='Ascendant of the revolution', significator=None):
    """The revolution's Ascendant distributed round the revolution chart
    for one year (IX.7, 29-31). Segments in DAYS from the revolution, each
    {from, to, from_lon, distributor, partner, partner_aspect, ...}; the
    last ends at the full circuit, 360 / (59' 08"), about 365.28 days.
    Never refuses: the measure is the zodiac itself."""
    # IX.7, 31: "you work like that with everything of the planets, Lots, and
    # houses, of whatever you want the direction of" -- any start point, by
    # its label (order PN4R-4c-4; the revolution's Ascendant alone until 2026-09-11).
    segments = _pn4_distribute(sr_planetary_data, sr_ascendant_lon, lambda lon: lon % 360.0,
                               360.0, point_label,
                               opening_window='bound', epoch='the revolution', significator=significator)
    for seg in segments:
        seg['from'] = pn4_small_days_arc_to_days(seg['from'])
        seg['to'] = pn4_small_days_arc_to_days(seg['to'])
    return segments

# --- IX.7, 23-28: "the mighty days" --------------------------------------
# "you look in the revolution of the year at the degree of the sign which
# the year terminated at, from the Ascendant of the root of the nativity"
# (IX.7, 23) -- the terminal point, the natal Ascendant's degree carried
# into the sign of the year -- and direct it: a body or ray already "in
# the bounds of that degree" manages until another meets it, else the
# lord of the bound and the bounds that follow (IX.7, 23-24). The rate
# (IX.7, 25): "multiply by 12 days, <4 hours>, 10 minutes, and 30
# seconds ... from the first day of the revolution", because thirty of
# them "comes to 365 1/4 days, approximately the number of days of the
# year" (IX.7, 28) -- the profected thirty degrees treated as the year.
#
# THE AUTHOR'S OWN FRACTION. IX.7, 25 prints "12 days, <4 hours>, 10
# minutes, and 30 seconds (and that is 1/6 of a day and half a sixth of a
# tenth of a day)". Three figures stand in that sentence: (1) the
# manuscript's 12;10,30 days -- "10 minutes and 30 seconds" as sexagesimal
# fractions OF A DAY, 10/60 + 30/3600 = 0.175 d = 4 h 12 m; (2) the author's
# parenthetical, 1/6 + 1/120 = 0.175 d, the same number stated in words,
# thirty of which are 365 1/4 days EXACTLY (IX.7, 28's "approximately");
# (3) Dykes's hybrid 12 d 4 h 10 m 30 s = 12.17396 d, his pointed-bracket
# "<4 hours>" supplied and the manuscript's minutes and seconds read as
# clock time on top of it, thirty of which are 365 d 5 h 15 m -- fn 177
# then gives 12 d 4 h 12 m for a 365 1/4-day year, which is (1) and (2)
# again. APPLIED: (2), 12 + 1/6 + 1/120 = 12.175 d a degree. The owner's
# decision of 2026-09-11 (decision sheet row 13; order PN4R-4d-1),
# replacing the hybrid applied 2026-09-10 as "the printed rate". The page
# prints all three and says which is applied. Zodiacal by construction --
# there is no ascension anywhere in the sentence; fn 175's report that
# Birchfield would prefer ascensions is an editor's note.
#
# The direction does NOT stop at the end of the sign of the year: it
# starts at the terminal degree and runs thirty degrees, so its last
# part is in the bounds of the NEXT sign, which is what IX.7, 24's "then
# to the lord of the bound which follows it" describes. The same three
# readings as the small days are made and said on the page: the
# revolution's bodies and rays; days from the moment of the revolution;
# the opening partner behind the degree within its bound. IX.7, 27's
# extension to the Lots of the parents and every house and Lot is not
# built. No worked example exists in PN IV.
PN4_MIGHTY_DAYS_PER_DEGREE = 12.0 + 1.0 / 6.0 + 1.0 / 120.0   # IX.7, 25's parenthetical: 12.175 d; thirty = 365.25 d
PN4_MIGHTY_DAYS_SPAN_DEGREES = 30.0                                                # IX.7, 28: thirty degrees, the year

def pn4_mighty_days_arc_to_days(arc_degrees):
    """IX.7, 25's own fraction: 12 + 1/6 + 1/120 days a degree (12.175 d);
    see PN4_MIGHTY_DAYS_PER_DEGREE for the three figures in that sentence."""
    return float(arc_degrees) * PN4_MIGHTY_DAYS_PER_DEGREE

def pn4_mighty_days(sr_planetary_data, terminal_lon, point_label='terminal point of the year', significator=None):
    """The terminal degree of the year directed through the revolution
    chart for the year (IX.7, 23-28). Segments in DAYS from the
    revolution, each {from, to, from_lon, distributor, partner, ...}; the
    last ends at thirty degrees, 365.22 days at the printed rate. Never
    refuses: the measure is the zodiac itself."""
    # IX.7, 27: the same direction from the profected degree of any point
    # (order PN4R-4c-4; the terminal point alone until 2026-09-11).
    segments = _pn4_distribute(sr_planetary_data, terminal_lon, lambda lon: lon % 360.0,
                               PN4_MIGHTY_DAYS_SPAN_DEGREES, point_label,
                               opening_window='bound', epoch='the revolution', significator=significator)
    for seg in segments:
        seg['from'] = pn4_mighty_days_arc_to_days(seg['from'])
        seg['to'] = pn4_mighty_days_arc_to_days(seg['to'])
    return segments

def pn4_distribution_at_age(segments, age_years):
    """The segment covering an age, or None past the end of the span."""
    if not segments:
        return None
    for seg in segments:
        if seg['from'] <= age_years < seg['to']:
            return seg
    return None


# =========================================================================
# The releaser and the house-master, from SAHL (built 2026-09-10)
# =========================================================================
# NOT PN IV. Abu Ma'shar lists the candidates (III.3, 1) and defers the
# matter to "the book which we worked on concerning nativities" (IX.8,
# 123 -- said there of deriving the lifespan indicator's years), a book
# this corpus does not hold; the engine refused the step on that ground. The owner decided
# on 2026-09-10, on process/TIMING_SOURCES_REPORT_2026-09-10.md, to build
# it from Sahl instead: the RELEASER per Nawbakht, On Nativities 1.15
# (with 1.16 and the ranking of 1.20, 1-4), and the HOUSE-MASTER DIRECTED
# per Masha'allah, On Nativities 1.23, 2. The years the house-master
# grants are, since 2026-09-11 (FINAL-A1, the owner), granted from On
# Nativities 1.20, 7-34 read in full (sahl_house_master_years): On Times 4
# is a question-chart chapter and 1.23, 68 points to 1.20, so corpus
# disagreement #2 was a tie between a horary rule and a natal one. The D-3
# control admits that one reader by name.
#
# 1.15, 6-9 (day): "look ... at the Sun and the meeting: because if you
# found the Sun in the Ascendant, the Midheaven, the house of hope, or in
# the stake of the west, or in the eighth, then he will have a releaser
# ... But if you do not find the Sun in any of these places, or he was in
# one of them but the lord of his bound was not looking at him, nor that
# of his house, exaltation, triplicity, or face, then the Sun will not be
# the releaser. And with that you must look at the meeting ... if the
# meeting was in any of these five places, then the meeting is the
# releaser. But if the meeting and the Sun were both falling, then the
# releaser at that time will be the Ascendant." Fn 109: "The inclusion of
# the eighth suggests that these are quadrant divisions, not whole signs."
# 1.15, 10-14 (night): the Moon "in a stake or what follows a stake, with
# the lord of the bound, house, exaltation, triplicity, or image looking
# at her"; else the fullness, same test; else the Lot of Fortune, same
# test. 1.15, 13: "that one -- of any of these indicators -- which is
# looking at the releaser, is the house-master." 1.15, 15-16: else the
# Ascendant, "being looked at by the fortunes, and the lord of the
# Ascendant in its own house or exaltation, or in its own bound,
# triplicity, or image, in good places"; "and if there was nothing of
# what I have mentioned, then know that the native does not have a
# foundation for his lifespan." 1.16, 1-2: the Sun in Aries or Leo, the
# Moon in Taurus or Cancer, "becomes both the releaser and the
# house-master". 1.20, 2-4: bound, then house, exaltation, triplicity,
# image; "the one having two shares is stronger than the lord of only a
# single one"; the bound lord "in the Ascendant with the releaser, it is
# stronger than the others". 1.20, 5: a house-master under the rays "is
# deceptive, subtractive, corrupting" (a flag, not a disqualification).
#
# READINGS MADE HERE, each said on the page (rewritten 2026-09-11 after the
# owner's ruling OWNER_RULING_PLACES_VS_DYNAMICS and the final adjudication):
# (1) THE PLACES. "A stake or what follows a stake" (1.15, 6-16) is read as a
# POWER test and counted by the Alcabitius divisions with the five-degree
# allowance at the four axial degrees only (get_effective_house): a planet
# 0-5 degrees past the Ascendant, Midheaven, setting degree or fourth into
# the cadent division keeps the stake's power, measured from the axial
# DEGREE, in longitude (a proxy). The warrant for the UNIT is the
# translator's course convention adopted by the owner as canon -- Lesson 3
# "A Chart Tour" §4-5, the Course Glossary s.v. Advancement, Carmen p. 108
# fn 187 ("Dorotheus ... is using dynamic divisions to speak of the planets'
# power, because one can only move from a stake to a decline by primary
# motion") -- with fn 109 on 1.15, 6 agreeing ("quadrant divisions, not
# whole signs"); and the dispatch is Dykes's own published proposal, ITA
# Introduction §6, pp. 15-20 ("(1) adopt whole signs for topics; ... (3)
# apply Nechepso's eight-place scheme to quadrant-based dynamical
# divisions", ITA Introduction printed pp. 19-20, read against the photograph 2026-09-17), so the page cites him (3.7, 2026-09-15).
# Al-Qabisi IV.4 (ITA VIII.1.3) uses quadrant houses in his own releaser
# procedure with the five degrees at "any house" -- named on the tab as a
# different author's method (decisions 2 and 17). The corpus's own
# vocabulary counts SIGNS: Introduction 2,
# 31-35 defines the stakes, "what follows the stakes" and the falling
# places as counted signs, and 1.20, 10 says "the sign of the west"; so the
# division reading is the translator's, not Sahl's or Nawbakht's. 1.18, 19
# ("its STRENGTH will be in the Ascendant ... and likewise in all of the
# houses") is about strength and is read as the four stakes; Aphorism 44
# and 1.22, 9 witness the five degrees, not the house system. The Lot of
# Fortune, a candidate by night (1.15, 14), has no dynamic angularity and
# is tested by its WHOLE-SIGN place (the canon; a change of 2026-09-11).
# The meeting's and the fullness's degrees (1.15, 6-8, 12) are neither
# planet nor Lot: the division is used for them, an OPEN reading the owner
# has not ruled on. The day list is the five places 1.15, 6 names, the
# night list every stake and succedent. (2) "Looking" is the whole-sign
# aspect, and a lord in the candidate's own sign counts as looking (1.20, 4
# treats the bound lord in the Ascendant with the releaser as the strongest
# of those looking). (3) A candidate is not its own house-master except in
# 1.16's four signs; a luminary that is its own bound, triplicity or face
# lord elsewhere is not counted as "looking at" itself. (4) The triplicity
# lord is the lord of the sect (day lord by day, night lord by night), as
# the engine reads it everywhere. (5) The meeting is the last New Moon and
# the fullness the last Full Moon before birth; the fullness's degree is
# the luminary above the earth at that moment -- On Nativities 1.7, 2,
# "take the portion of whichever of the two luminaries was above the
# earth", stated for the Ascendant's degree and applied here by the shared
# word "portion" (1.15, 12); when both or neither is up the engine takes
# the Moon's degree (FINAL-A6) -- Valens's opinion of the three al-Qabisi
# IV.3 reports (ITA VIII.1.2: Ptolemy's the luminary above the earth; the
# sages' the eastern degree when one is rising and the other setting;
# Valens's the Moon's; see the docstring of calculate_prenatal_syzygy);
# none of the other two adopted (decision 7). (6) "In good places" for
# the Ascendant's lord (1.15, 16) is Sahl's seven praised places
# (Introduction 2, 37-44: "praised, powerful"; 1.30, 71 names them; fn 372
# "good or advantageous"), counted by whole-sign place; no sentence defines
# 16's phrase, an interpretation (owner, sheet row 12) -- Dykes identifies
# the seven as Timaeus's and Dorotheus's busy places, "good" and "strong"
# (ITA pp. 18, 121; reconciliation 3.17). (7) The day chart
# consults the Sun, the meeting, then the Ascendant -- 1.15, 9: "if the
# meeting and the Sun were both falling, then the releaser at that time
# will be the Ascendant" closes the day list -- and the night chart the
# Moon, the fullness, the Lot, then the Ascendant (10-14); 1.15, 15 lists
# all five before the Ascendant and is read as the summary of the two
# lists; read as a procedure it would consult the other sect's candidates
# first, which changes the releaser in about one day chart in seven
# (FINAL-A5, owner, sheet row 6). The meeting's gate (1.15, 8) states a
# place test only; the looking-lord test applied to it is supplied from
# 15's general wording, a reading.
# APPLIED since 2026-09-11 (the BLOCKED orders, unblocked by the owner's
# ruling on row 3): 1.19, 6 (the Moon within 15 degrees of the Sun "will
# not be fit" -- the fullness is consulted; REL-5-1), 1.20, 6 (an eastern
# lord with a share in the Ascendant is house-master without looking;
# REL-5-2). SHOWN, not applied: 1.18, 8-10 (the short-life testimonies,
# counted beside the releaser; REL-5-3). NOT APPLIED: Dorotheus's feminized
# seventh (1.15, 5, which Nawbakht rejects) -- each is named on the page
# where it would bite. APPLIED since 2026-09-11 (FINAL-A7): 1.32, 11-13's
# stand-in in the empty case -- al-Andarzaghar's chapter "On the matter of
# survival for one who does not live" answers the question Nawbakht's "no
# foundation" leaves, a complement not a contradiction; "the first of them
# is the Ascendant, then the Moon" is 1.32, 13 as printed across the p.
# 346/347 break, so the Ascendant's distribution is labelled the stand-in
# and the Moon is directed after it (1.32, 12: whichever first connects
# with an infortune kills; 14: if no fortune looks).

SAHL_RELEASER_DAY_PLACES = (1, 10, 11, 7, 8)          # 1.15, 6 and 8, "these five places"
SAHL_RELEASER_NIGHT_PLACES = (1, 4, 7, 10, 2, 5, 8, 11)  # 1.15, 11-14, "a stake or what follows a stake"
SAHL_DIGNITY_RANK = ('bound', 'house', 'exaltation', 'triplicity', 'face')   # 1.20, 2
# 1.15, 16's "in good places" for the Ascendant's lord: Sahl's SEVEN praised
# places -- Introduction 2, 37-44 ranks the Ascendant, tenth, seventh,
# fourth, eleventh, ninth and fifth and closes "these seven places are
# praised, powerful"; 1.30, 71 names "the seven places (which are the stakes
# and the trines of the Ascendant, and the eleventh)"; fn 372 calls them
# "good or advantageous". No sentence defines 16's phrase, so the
# identification is an interpretation (the engine had used the eight-place
# stake-or-succedent class of 1.15, 11 as reading 6); Dykes says the seven
# are the busy places of Timaeus and Dorotheus, which Sahl "explicitly uses"
# in Introduction §4 (ITA III.4 comment, p. 121) and Dorotheus calls "good"
# and "strong" (ITA Introduction §6, p. 18) (ITA printed p. 121 and the Introduction's §6 pp. 15-18, read against the photograph 2026-09-17; 3.17). Counted by WHOLE-SIGN
# PLACE: topic language, the canon of 2026-09-11. Owner, decision sheet row
# 12 (REL-2-6), corroborated blind (astra_2026-09-11/1.15_readings_ruling Q2).
SAHL_GOOD_PLACES = (1, 10, 7, 4, 11, 9, 5)
SAHL_BOTH_AT_ONCE = {'Sun': ('Aries', 'Leo'), 'Moon': ('Taurus', 'Cancer')}  # 1.16, 1-2
SAHL_RELEASER_NOT_APPLIED = (
    ('1.16, 4', 'al-Andarzaghar keeps a luminary "powerful in the places of the releaser" as releaser "even if a house-master '
                'is not looking"; Nawbakht\'s 1.15, 7 gate (no lord looking, not the releaser) is applied instead -- two '
                'chapters of one book, opposite rules'),
    ('1.18, 8-10', 'one short-life testimony makes the releaser "weak and not fit, except through reception"; two, with no releaser, "one will not know his lifespan except by revolving his years" -- counted and shown beside the releaser, not disqualifying it'),
    ('1.15, 5', "Dorotheus's releaser in the seventh in a feminine sign, which Nawbakht says he \"exonerated\" (1.15, 5)"),
)

def sahl_prenatal_meeting_and_fullness(jd_natal, lat, lon):
    """The last New Moon ("the meeting") and the last Full Moon ("the
    fullness") before birth, each found by the same Newton search as
    calculate_prenatal_syzygy, both always -- 1.15 consults the meeting by
    day and the fullness by night, whichever was nearer. The meeting's
    degree is the luminaries' common longitude; the fullness's is the
    luminary above the horizon at that moment -- On Nativities 1.7, 2,
    "take the portion of whichever of the two luminaries was above the
    earth", stated for the Ascendant's degree and applied to 1.15, 12 by
    the shared word "portion" (FINAL-A6) -- else the Moon's, the engine's
    choice where both or neither is up. Returns {'meeting': {...},
    'fullness': {...}} with 'longitude', 'jd' and 'degree_of'."""
    AVG_REL_SPEED = 12.19075
    sun0 = swe.calc_ut(jd_natal, swe.SUN)[0][0]
    moon0 = swe.calc_ut(jd_natal, swe.MOON)[0][0]
    diff0 = (moon0 - sun0) % 360.0
    out = {}
    for name, target in (('meeting', 0.0), ('fullness', 180.0)):
        jd_guess = jd_natal - ((diff0 - target) % 360.0) / AVG_REL_SPEED
        sun_lon, moon_lon = sun0, moon0
        for _ in range(15):
            sun_lon, moon_lon, offset, rel_speed = _sun_moon_signed_offset(jd_guess, target)
            if abs(offset) < 1e-6:
                break
            if abs(rel_speed) < 1e-6:
                rel_speed = AVG_REL_SPEED
            jd_guess -= offset / rel_speed
        if name == 'meeting':
            out[name] = {'longitude': sun_lon % 360.0, 'jd': jd_guess, 'degree_of': 'the luminaries together'}
        else:
            _, ascmc = swe.houses(jd_guess, lat, lon, b'B')
            obl = swe.calc_ut(jd_guess, swe.ECL_NUT)[0][0]
            s, m = swe.calc_ut(jd_guess, swe.SUN)[0], swe.calc_ut(jd_guess, swe.MOON)[0]
            sun_up = _sin_altitude(s[0], s[1], s[2], obl, ascmc[2], lat) > 0.0
            moon_up = _sin_altitude(m[0], m[1], m[2], obl, ascmc[2], lat) > 0.0
            if sun_up and not moon_up:
                out[name] = {'longitude': sun_lon % 360.0, 'jd': jd_guess, 'degree_of': 'the Sun, above the horizon'}
            else:
                out[name] = {'longitude': moon_lon % 360.0, 'jd': jd_guess,
                             'degree_of': ('the Moon (both above, the default)' if sun_up and moon_up else
                                           'the Moon, above the horizon' if moon_up else 'the Moon (neither above, the default)')}
    return out

def _sahl_dignity_lords(lon, sect):
    """The five lords of a degree in 1.20, 2's order, as (rank, planet);
    a rank with no lord (exaltation in a sign that has none) is left
    out. Triplicity by the sect (reading 4)."""
    r = get_essential_rulers(lon)
    pairs = (('bound', r['term']), ('house', r['domicile']), ('exaltation', r['exaltation']),
             ('triplicity', r['triplicity_day'] if sect == 'Diurnal' else r['triplicity_night']),
             ('face', r['face']))
    return [(rank, planet) for rank, planet in pairs if planet in PN4_SEVEN]

def _sahl_looks(from_lon, to_lon):
    """Whole-sign aspect from a planet's sign to a degree's sign: the
    aspect's name, 'in it' for the same sign, or None in aversion."""
    apart = (int(to_lon % 360.0 // 30) - int(from_lon % 360.0 // 30)) % 12
    apart = min(apart, 12 - apart)
    entry = ASPECT_BY_SIGN_COUNT.get(apart)
    if entry is None:
        return None
    return 'in it' if apart == 0 else entry[0].lower()

def _sahl_examine_candidate(label, lon, planetary_data, cusps, sect, places, self_planet=None, unit='division'):
    """One candidate of 1.15 against its two tests: the place, and a
    dignity lord looking. Returns the facts and the verdict. `unit` is
    'division' (the Alcabitius division with the five-degree allowance at
    the stakes -- the POWER reading of "a stake or what follows a stake",
    the canon of 2026-09-11) or 'place' (the whole-sign place, for the Lot
    of Fortune, which has no dynamic angularity)."""
    place = get_effective_house(lon, cusps) if unit == 'division' else get_wsh_house(lon, cusps[0])
    in_places = place in places
    sign = get_zodiac_sign(lon)
    both = bool(self_planet and sign in SAHL_BOTH_AT_ONCE.get(self_planet, ()))
    lords, looking = [], []
    for rank, planet in _sahl_dignity_lords(lon, sect):
        if planet == self_planet:
            lords.append((rank, planet, 'itself' if both else 'itself -- not counted (reading 3)'))
            continue
        row = planetary_data.get(planet)
        aspect = _sahl_looks(row['longitude'], lon) if row else None
        lords.append((rank, planet, aspect or 'in aversion'))
        if aspect:
            looking.append((rank, planet, aspect))
    fit = in_places and (bool(looking) or both)
    # 1.20, 6 (order REL-5-2, applied): "if you found the releaser in a good
    # position, and its lord eastern and having a share in the Ascendant (even
    # if it was in the second from it, not looking at it), then it will assume
    # the responsibility of being the house-master with it, unless in that
    # nativity there was a planet preferable to it" -- read: a candidate in the
    # places with no lord looking keeps its standing when one of its dignity
    # lords is eastern of the Sun and holds a share (any of the five) at the
    # Ascendant's degree; that lord is the house-master "by 1.20, 6"; a looking
    # lord, being "preferable", takes precedence (the ranking runs as before).
    sharer_1_20_6 = None
    if in_places and not looking and not both:
        asc_rulers = get_essential_rulers(cusps[0])
        asc_shares = {asc_rulers[k] for k in ('domicile', 'exaltation', 'triplicity_day' if sect == 'Diurnal' else 'triplicity_night', 'term', 'face')}
        for rank, planet, _aspect in lords:
            row = planetary_data.get(planet)
            if not row or planet == self_planet or planet == 'Sun':
                continue
            side = solar_phase(planet, row['longitude'], planetary_data['Sun']['longitude'], row.get('speed_in_lon'))[1]
            if side == 'eastern' and planet in asc_shares:
                sharer_1_20_6 = (rank, planet)
                break
        if sharer_1_20_6:
            fit = True
    if not in_places:
        why = f"falling: house {place} is not among the places (1.15, {'6' if places is SAHL_RELEASER_DAY_PLACES else '11-14'})"
    elif both:
        why = f"{self_planet} in {sign}: both releaser and house-master (1.16, 1-2)"
    elif looking:
        why = f"house {place}; looked at by " + ', '.join(f"{p} ({r}, {a})" for r, p, a in looking)
    elif sharer_1_20_6:
        why = (f"house {place}; no lord looks, but {sharer_1_20_6[1]}, its {sharer_1_20_6[0]} lord, is eastern with a share in "
               f"the Ascendant -- house-master by 1.20, 6 (\"even if it was in the second from it, not looking at it\"; "
               f"\"a share\" read as any of the five dignities at the Ascendant's degree)")
    else:
        why = f"house {place}, but no lord of its bound, house, exaltation, triplicity or face looks at it ({'1.15, 15' if label == 'the meeting (the last New Moon)' else '1.15, 7'}, Nawbakht)"
        if self_planet in ('Sun', 'Moon'):
            # 1.16, 4 (al-Andarzaghar): a luminary "powerful in the places of
            # the releaser ... (even if a house-master is not looking, and it
            # [itself] is not the house-master)" is still directed as the
            # releaser. Two chapters of one book state opposite gates for this
            # case; Nawbakht's is applied, the other named (order REL-2-3).
            why += ("; al-Andarzaghar's 1.16, 4 would keep it as releaser \"even if a house-master is not "
                    "looking\" -- not applied")
    # 1.19, 6 (order REL-5-1, applied): "if the Moon was under the rays, she
    # will not be fit to take up [the role of the] manager (and that is if
    # there was 15 degrees between her and the Sun, in front of him and behind
    # him)" -- Dorotheus in Sahl; manager = releaser (1.30, 35). Sahl's own 15
    # for THIS gate, separate from the Chart page's 12/15 solar-phase switch.
    # Applied: the fullness is consulted next.
    under_1_19_6 = False
    if self_planet == 'Moon' and 'Sun' in planetary_data:
        elong = abs(((lon - planetary_data['Sun']['longitude'] + 180.0) % 360.0) - 180.0)
        if elong <= 15.0:   # the sentence's own interval, 0 degrees included; no heart exception is stated for this gate
            under_1_19_6 = True
            fit = False
            why += (f"; within 15 degrees of the Sun ({elong:.1f}): \"not fit to take up [the role of the] manager\" "
                    f"(Dorotheus, 1.19, 6; manager = releaser, 1.30, 35) -- applied, the fullness is consulted")
    return {'candidate': label, 'longitude': lon % 360.0, 'place': place, 'in_places': in_places,
            'lords': lords, 'looking': looking, 'both_at_once': both, 'self_planet': self_planet,
            'fit': fit, 'why': why, 'sharer_1_20_6': sharer_1_20_6, 'under_1_19_6': under_1_19_6}

def _sahl_ascendant_candidate(planetary_data, ascendant_lon, cusps, sect):
    """1.15, 15-16: the Ascendant "being looked at by the fortunes, and
    the lord of the Ascendant in its own house or exaltation, or in its
    own bound, triplicity, or image, in good places"."""
    cand = _sahl_examine_candidate('the Ascendant', ascendant_lon, planetary_data, cusps, sect,
                                   SAHL_RELEASER_DAY_PLACES if sect == 'Diurnal' else SAHL_RELEASER_NIGHT_PLACES)
    fortunes = [(p, _sahl_looks(planetary_data[p]['longitude'], ascendant_lon))
                for p in ('Jupiter', 'Venus') if p in planetary_data]
    fortunes_looking = [(p, a) for p, a in fortunes if a]
    lord = SIGN_TO_DOMICILE.get(get_zodiac_sign(ascendant_lon))
    lord_row = planetary_data.get(lord)
    own = []
    lord_place = None
    if lord_row:
        own = [rank for rank, planet in _sahl_dignity_lords(lord_row['longitude'], sect) if planet == lord]
        lord_place = get_wsh_house(lord_row['longitude'], ascendant_lon)      # a PLACE, whole sign (see SAHL_GOOD_PLACES)
    good_place = lord_place in SAHL_GOOD_PLACES
    fit = bool(fortunes_looking) and bool(own) and good_place
    cand.update({
        'in_places': True, 'fit': fit,
        'why': (f"looked at by {', '.join(f'{p} ({a})' for p, a in fortunes_looking) or 'no fortune'}; its lord {lord} "
                f"{'in its own ' + ' and '.join(own) if own else 'in none of its own shares'}, whole-sign place {lord_place} "
                f"({'one of' if good_place else 'not one of'} Sahl's seven praised places, Introduction 2, 37-44; 1.30, 71 -- "
                f"an interpretation of 16's 'good places') (1.15, 16)"),
    })
    return cand

def _sahl_rank_house_master(cand, planetary_data, cusps):
    """1.15, 13 and 1.20, 2-5 over the lords looking at the chosen
    releaser: grouped by planet, ordered by shares then by the best rank,
    the bound lord in the Ascendant with the releaser first of all.
    Returns rows in order; the first is the house-master."""
    if cand['both_at_once']:
        return [{'Planet': cand['self_planet'], 'Shares': 'its own house or exaltation, and triplicity',
                 'Looks by': 'is the releaser', 'Rank': 'both releaser and house-master (1.16, 1-2)', 'Under the rays': '-'}]
    if not cand['looking'] and cand.get('sharer_1_20_6'):
        rank, planet = cand['sharer_1_20_6']
        phase = solar_phase(planet, planetary_data[planet]['longitude'], planetary_data['Sun']['longitude'],
                            planetary_data[planet].get('speed_in_lon'), source='Sahl')[0]
        return [{'Planet': planet, 'Shares': rank, 'Looks by': 'not looking',
                 'Rank': 'house-master by 1.20, 6: eastern, with a share in the Ascendant, "not looking at it"',
                 'Under the rays': (f"{phase}: \"deceptive, subtractive, corrupting\" (1.20, 5)"
                                    if phase in ('Burned', 'Under the rays') else 'no' if phase != 'Cazimi' else 'Cazimi')}]
    by_planet = {}
    for rank, planet, aspect in cand['looking']:
        d = by_planet.setdefault(planet, {'ranks': [], 'aspect': aspect})
        d['ranks'].append(rank)
    rows = []
    for planet, d in by_planet.items():
        best = min(SAHL_DIGNITY_RANK.index(r) for r in d['ranks'])
        in_asc_with = ('bound' in d['ranks']
                       and get_zodiac_sign(cand['longitude']) == get_zodiac_sign(cusps[0])
                       and get_zodiac_sign(planetary_data[planet]['longitude']) == get_zodiac_sign(cusps[0]))
        phase = solar_phase(planet, planetary_data[planet]['longitude'], planetary_data['Sun']['longitude'],
                            planetary_data[planet].get('speed_in_lon'), source='Sahl')[0]
        rows.append({'Planet': planet, 'Shares': ', '.join(d['ranks']), 'Looks by': d['aspect'],
                     '_key': (0 if in_asc_with else 1, -len(d['ranks']), best),
                     'Rank': ('the bound lord in the Ascendant with the releaser: "stronger than the others" (1.20, 4)'
                              if in_asc_with else
                              f"{len(d['ranks'])} share{'s' if len(d['ranks']) != 1 else ''}; best {SAHL_DIGNITY_RANK[best]} (1.20, 2-3)"),
                     'Under the rays': (f"{phase}: \"deceptive, subtractive, corrupting\" (1.20, 5)"
                                        if phase in ('Burned', 'Under the rays') else 'no' if phase != 'Cazimi' else 'Cazimi')})
    rows.sort(key=lambda r: r['_key'])
    for r in rows:
        r.pop('_key')
    return rows

# On Nativities 1.20, 7-34: the sentences applied, in the reading FINAL-A1
# set out and the owner adopted (2026-09-11), each with the clause it turns on.
SAHL_1_20_SENTENCES = {
    10: ("greater", "in the Ascendant or in the Midheaven, or in the sign of the west, or the eleventh, enhanced by what I explained ... the <greater> years"),
    11: ("greater", "under the earth, eastern, in one of its shares, enhanced ... its greater years (and by night in the fourth and fifth, it indicates the greater years)"),
    16: ("middle", "in the second or eighth ... its middle years"),
    17: ("middle", "in the house of hope or the fifth, and was not in something of its shares, and was not eastern ... its middle years"),
    18: ("middle", "under the rays of the Sun in a stake ... its middle years"),
    19: ("middle as months and days", "alien, westernizing in the places ... its middle years as months and days"),
    20: ("middle", "in the stakes, easternizing, or not easternizing so long as it is not retrograde and not burned ... its middle years"),
    21: ("lesser", "under the rays of the Sun, retrograde in a stake ... its lesser years"),
    22: ("lesser", "alien, westernizing, retrograde ... the lesser ones"),
    23: ("lesser", "in the eleventh or fifth, retrograde ... its lesser years"),
    "23b": ("months", "[in the eleventh or fifth, retrograde,] and if it was under the rays of the Sun ... months"),
    24: ("days and hours", "and if along with that it was in its fall ... days and hours"),
    25: ("months", "in the second and eighth, retrograde, under the rays of the Sun ... months"),
    26: ("middle", "in the ninth, in one of its shares, eastern ... its middle years"),
    27: ("lesser", "[in the ninth,] if it does not have a share nor easternization ... its lesser years"),
    28: ("lesser", "in the <other> [places] falling from the stakes ... its lesser years"),
    29: ("months", "[in the ninth,] under the rays of the Sun, retrograde ... months"),
    30: ("middle", "in the third, [uncertain] in its house or share ... its middle years"),
    31: ("lesser", "[in the third,] if it does not have a share nor easternization ... its lesser years"),
    32: ("months", "[in the third,] if it was retrograde ... months"),
    33: ("days", "[in the third,] if it was retrograde under the rays ... days"),
    34: ("hours", "[in the third,] and if with that it was in its fall ... hours"),
}
SAHL_1_20_READINGS = (
    "Readings of 1.20 made here: the house-master is placed by "
    "the Alchabitius division with the five-degree allowance at the four axial degrees -- 1.20's grades are a power "
    "judgment -- and 10's \"the sign of the west\" is the texts' own sign vocabulary, not a house system (the same "
    "attribution as the releaser's places); \"enhanced\" (7-9) = in a share, eastern, direct, not under the rays, 8's "
    "sect aspects not tested; \"a share\" = any of its five dignities at its own degree; for the five planets, \"eastern\" and "
    "\"westernizing\" follow the solar phase's side; the supplied passages do not define the Moon's orientality, so "
    "both assignments are carried through the ordered rules; \"under the rays\" = burned or under the rays; \"alien\" = in no "
    "share; 10 and 20 as fn 158 reads them (an enhanced stake = the greater years, a bare stake direct and unburned = the "
    "middle); \"under the earth\" (11) = the fourth division, and the fifth by night (11's parenthesis), 11's \"enhanced\" "
    "required of both (a retrograde or under-the-rays planet there is not 11's greater); 12 is "
    "subsumed by 10 (the house of Good Fortune is the eleventh, fn 153) -- 12 does not say direct or unburned, so for a "
    "diurnal planet in the eleventh, eastern, in a share and retrograde, 12 (greater) and 23 (lesser) conflict as "
    "printed, and 23 is applied; 13 is illegible in part and not applied; 14-15 are printed "
    "and not applied (fn 156: the sense is unclear); 19 and 22 (alien, westernizing) are applied only where no placement "
    "sentence reaches; where a sentence names months, days or hours without a count, none is invented. Placements no "
    "sentence reaches print \"1.20 silent\". On Times 4, 7 is a rule for a question chart (\"in the hour of the "
    "question\", 4, 2) and is shown on the Fardar and ages page for comparison only. 1.23, 53 and 61: these years are what the "
    "infortunes may cut off."
)

def _sahl_1_20_grade(q, share, east, west, retro, rays, fall, night):
    """1.20, 10-34 as a decision on the facts; q is the DIVISION (1-12).
    Returns (sentence key, grade) or (None, None) where no sentence reaches."""
    enhanced = share and east and not retro and not rays
    if q in (1, 4, 7, 10):
        if rays and retro:
            return 21, 'lesser'
        if q in (1, 10, 7) and enhanced:
            return 10, 'greater'
        if q == 4 and enhanced:                 # 11: "under the earth, eastern, in one of its shares, ENHANCED"
            return 11, 'greater'
        if rays:
            return 18, 'middle'
        if not retro:
            return 20, 'middle'
        return None, None
    if q == 11:
        if enhanced:
            return 10, 'greater'
        if retro:
            if rays and fall:
                return 24, 'days and hours'
            if rays:
                return '23b', 'months'
            return 23, 'lesser'
        if not share and not east:
            return 17, 'middle'
        return None, None
    if q in (2, 8):
        if retro and rays:
            return 25, 'months'
        return 16, 'middle'
    if q == 5:
        if night and enhanced:                  # 11's parenthesis carries 11's "enhanced"
            return 11, 'greater'
        if retro:
            if rays and fall:
                return 24, 'days and hours'
            if rays:
                return '23b', 'months'
            return 23, 'lesser'
        if not share and not east:
            return 17, 'middle'
        return None, None
    if q == 9:
        if rays and retro:
            return 29, 'months'
        if share and east:
            return 26, 'middle'
        if not share and not east:
            return 27, 'lesser'
        return None, None
    if q == 3:
        if retro and rays and fall:
            return 34, 'hours'
        if retro and rays:
            return 33, 'days'
        if retro:
            return 32, 'months'
        if share:
            return 30, 'middle'
        if not east:
            return 31, 'lesser'
        return None, None
    if q in (6, 12):
        return 28, 'lesser'
    return None, None


def _sahl_house_master_years_branch(planet, planetary_data, cusps, sect, essential, moon_eastern=None):
    """One Sahl 1.20 route with lunar orientality fixed when supplied."""
    row = planetary_data.get(planet)
    if not row:
        return None
    lon = row['longitude'] % 360.0
    q = get_effective_house(lon, cusps)
    ess = essential.get(planet, {})
    share = any(ess.get(k) for k in ('Domicile', 'Exalt', 'Triplicity', 'Term', 'Face'))
    fall, exile = bool(ess.get('Fall')), bool(ess.get('Detriment'))
    burned = False
    if planet == 'Sun':
        east = west = rays = False
    else:
        phase, side = solar_phase(planet, lon, planetary_data['Sun']['longitude'], row.get('speed_in_lon'), source='Sahl')[:2]
        if planet == 'Moon' and moon_eastern is not None:
            east, west = moon_eastern, not moon_eastern
        else:
            east, west = side == 'eastern', side == 'western'
        rays = phase in ('Burned', 'Under the rays')
        # 1.21, 13 says "burned", and 1.20 grants years to a house-master
        # "under the rays" (18, 21, 23b, 25, 33, 34): the two words are kept
        # apart, the refusal keyed on the inner band alone -- the app's
        # Burned phase, a declared operational interpretation of the word,
        # not a definition 1.21, 13 supplies.
        burned = phase == 'Burned'
    retro = (row.get('speed_in_lon') or 0.0) < 0.0
    night = sect != 'Diurnal'
    key, grade = _sahl_1_20_grade(q, share, east, west, retro, rays, fall, night)
    if key is None and not share and west:
        key, grade = (22, 'lesser') if retro else (19, 'middle as months and days')
    years = None
    unit = None
    if grade in ('greater', 'middle', 'lesser'):
        years, unit = PLANETARY_YEARS[planet][grade], 'years'
    elif grade == 'middle as months and days':
        years, unit = PLANETARY_YEARS[planet]['middle'], 'months and days'
    n = str(key).rstrip('b') if key is not None else None
    if key is None:
        text = f"1.20 silent: no sentence of 10-34 reaches {planet} in division {q} with these facts"
    elif years is not None:
        text = (f"the {grade} years, {years:g} ({planet}) -- 1.20, {n}: \"{SAHL_1_20_SENTENCES[key][1]}\"")
    else:
        text = (f"{grade} -- 1.20, {n}: \"{SAHL_1_20_SENTENCES[key][1]}\" (the count is not restated by the "
                f"sentence; none is invented)")
    flags = []
    if planet in ('Saturn', 'Jupiter', 'Mars') and (exile or west or retro or rays):
        flags.append('1.20, 14-15, printed and not applied (fn 156: "to me the meaning is still unclear"): "Know that exile, '
                     'setting, retrogradation, and burning do not stick to the superior planets, but they do stick to the '
                     'inferior planets. And if a superior one was in the condition which sticks to it, it is corrupting, for it '
                     'indicates its lesser years as months or days." \'Umar al-Tabari reads the pair by degree (Book of '
                     'Nativities I.4.3): one such condition does not harm a superior planet as it does an inferior, and a '
                     'superior under a serious impediment gives its lesser years as months or days; what makes an impediment '
                     'serious no text states, so the reading is carried here and not applied.')
    if q == 5 and planet == 'Venus':
        flags.append('1.20, 13 (Venus in her own shares in the fifth, eastern, the governor) is illegible in part and not applied.')
    # 1.21, 13 is a positive refusal, not 1.20's silence. Apply in the
    # shared route so lunar alternatives and the JN fallback cannot bypass it;
    # keyed on the burned band only, and 1.20's own indication kept visible
    # beside the qualification (both witnesses stand; one qualifies the result).
    indicated_1_20 = text
    if burned:
        grade, years, unit = 'no indication', None, None
        text = ('1.21, 13: burned, no indication — "And if the house-master was burned, '
                'it will not indicate anything." The JN fallback is not consulted: Sahl is not silent. '
                f'1.20 would have indicated: {indicated_1_20}. Burned here is this app\'s inner band of the rays '
                '(the Burned phase of its Sahl reading), a declared operational interpretation of the word; a house-master '
                'under the rays but not burned keeps 1.20\'s grant.')
        outcome = YearsOutcome('1.21, 13', grade, years, unit, text)
    else:
        outcome = YearsOutcome(f"1.20, {n}" if n else None, grade, years, unit, text)
    return {'grade': grade, 'sentence': outcome.sentence, 'years': years, 'text': text, 'flags': flags,
            'division': q, 'facts': {'share': share, 'eastern': east, 'westernizing': west, 'retrograde': retro,
                                     'under the rays': rays, 'burned': burned, 'fall': fall}, 'readings': SAHL_1_20_READINGS,
            'result': outcome, 'indicated_1_20': indicated_1_20,
            'indication': 'void' if burned else 'available' if grade else 'silent'}


def _lunar_orientation_evidence(source):
    """Unknown lunar orientality represented as inverse Boolean predicates."""
    eastern = UnresolvedResult(
        reason="the supplied passages do not define the Moon's orientality",
        source=source,
        alternatives=(('if the Moon is eastern', True), ('if the Moon is not eastern', False)),
    )
    westernizing = UnresolvedResult(
        reason="the supplied passages do not define the Moon's orientality",
        source=source,
        alternatives=(('if the Moon is eastern', False), ('if the Moon is not eastern', True)),
    )
    return eastern, westernizing


def sahl_house_master_years(planet, planetary_data, cusps, sect, essential):
    """The years On Nativities 1.20, 7-34 grants a house-master (Nawbakht;
    the section 1.23, 68 -- Masha'allah -- sends the reader to: "according
    to what I explained to you in the section on the house-master"). The
    corpus's ONE natal grant: On Times 4, 7 is stated for a question chart
    ("in the hour of the question", 4, 2) and is no natal rival (FINAL-A1,
    owner 2026-09-11, decision sheet row 1). The placement is the DIVISION
    (the POWER unit of the owner's ruling). Returns a dict: 'grade', 'sentence',
    'years' (the count where the grade names one), 'text' (the grade with
    its sentence quoted), 'flags' (14-15 printed where it would apply; 13),
    'division', 'readings'; None if the planet is absent. Not the Sun's
    grant only in the sense that the Sun has no solar phase."""
    if planet != 'Moon':
        return _sahl_house_master_years_branch(planet, planetary_data, cusps, sect, essential)
    eastern = _sahl_house_master_years_branch(planet, planetary_data, cusps, sect, essential, True)
    western = _sahl_house_master_years_branch(planet, planetary_data, cusps, sect, essential, False)
    if eastern is None:
        return None
    eastern_evidence, western_evidence = _lunar_orientation_evidence(
        "Sahl, On Nativities 1.20; Abu 'Ali, Judgments of Nativities Ch. 3")
    if _same_years_outcome(eastern['result'], western['result']):
        resolved = dict(eastern)
        resolved['facts'] = dict(eastern['facts'], eastern=eastern_evidence, westernizing=western_evidence)
        return resolved
    result = UnresolvedResult(
        reason="the supplied passages do not define the Moon's orientality",
        source='Sahl, On Nativities 1.20, 10-34',
        alternatives=(('if the Moon is eastern', eastern['result']),
                      ('if the Moon is not eastern', western['result'])),
    )
    return {'grade': result, 'sentence': result, 'years': result, 'text': result,
            'flags': list(dict.fromkeys(eastern['flags'] + western['flags'])),
            'division': eastern['division'],
            'facts': dict(eastern['facts'], eastern=eastern_evidence, westernizing=western_evidence),
            'readings': SAHL_1_20_READINGS, 'result': result,
            'orientation_alternatives': (('if the Moon is eastern', eastern),
                                         ('if the Moon is not eastern', western))}

# --- Sahl, On Nativities 1.29, 11-12 and 1.26, 7: the Moon on the third day; 1.8-1.9: the fetus's stay ---
# Two course-text findings (reconciliation decision 18, 2026-09-15), DISPLAY
# ONLY: nothing scores either. Sahl is canon, so neither is gated by the
# reading depth.
#
# THE THIRD DAY. "The third day of the Moon" (1.29, 11-12; 1.26, 7) is
# read as the Moon's position two days after the birth moment, the birth
# day counted as the first and the birth hour kept: Sahl's own words
# elsewhere are "the position of the Moon, where she is on the third day
# from the nativity" (On Nativities Ch. 9, 3) and "the position of the Moon on the
# third day, the seventh, and the fortieth day" (1.30, 22); no sentence
# of his fixes the count or the hour. The count is read from Firmicus's
# worked chart (Mathesis II.29, 34, the nativity of Albinus; III.14,
# 17-19) by degree in Dykes's approximate Figure 34, the owner's ruling
# of 2026-09-15 -- see MOON_THIRD_DAY_DAYS. "Corrupted" is
# read in 1.29's own terms, 1.29, 3: "safe from the infortunes, burning,
# and falling" -- an infortune looking at her by whole sign (the
# infortunes where they stand on that day), burned within the twelve
# degrees the app already uses for the Moon (The Introduction Ch. 3,
# 103), or falling from the stakes by the whole-sign place from the
# Ascendant OF THE NATIVITY (1.30, 33 asks "how is her position relative
# to the Ascendant"). 1.26, 7's four-footed sign is 1.38, 1's list
# (FOUR_FOOTED['On Nativities']; the second half of Sagittarius). 1.29,
# 12's "the two infortunes were in the Ascendant or seventh" is read as
# both natal infortunes in the whole-sign first or seventh place; 1.29,
# 11's antecedents (the triplicity lords corrupted, a fortune in a
# stake) are not tested here -- the Timing page carries the upbringing
# lords -- and the row says so.
#
# THE FETUS'S STAY. 1.8, 5-6 are computable: the meeting before the birth
# (the last New Moon; Dykes's fn 40 allows the lunation generally, so the
# nearer opposition is shown when that is what preceded the birth) and
# the Ascendant erected for its hour at the birthplace. 1.8, 3-4's three
# divisions are framed "from the degree of the Ascendant" of a chart the
# sentence does not name; Dykes reads it as the pre-conception lunation
# (fn 38 and his comment), which no sentence of 1.8 shows how to find, so
# the divisions and 7-13 are not computed and the rows say so. 1.9, 1-10
# are computed as written: the Moon of the nativity, the Moon a year
# before ("the past Moon") and a year after ("the renewed Moon"), the
# year read as the calendar anniversary at the birth hour (the app's
# reading; a Julian year of 365.25 days only where the anniversary's
# digits name no day), the aspects whole-sign. 1.9, 11 needs the meeting
# of the conception and 12-13 the stay itself, neither in hand before
# 1.10: presented, not computed.
SAHL_1_29_3 = ("For if they were safe from the infortunes, burning, and falling, then the native will be brought up "
               "and will reach [his] lifespan; and if they were corrupted, it destroys and he will not be brought up.")
SAHL_1_29_11 = ("And if you found the lords of the triplicities of the Ascendant in the stakes and what follows them, "
                "then the native will be brought up; now if the lords of its triplicity were corrupted and there was "
                "a fortune in a stake, and the third day of the Moon not corrupted, then he will be brought up.")
SAHL_1_29_12 = ("And if the two infortunes were in the Ascendant or seventh, and the third day of the Moon corrupted, "
                "he will not be brought up; and likewise if you found the two infortunes in the Ascendant and the "
                "seventh, and the lords of the triplicities [of the Ascendant] withdrawing from the stakes, and they "
                "are <not> looking at the Ascendant, he will not be brought up.")
SAHL_1_29_13 = ("And if the lords of the triplicity of the Ascendant were corrupted and the Moon safe, as well as her "
                "third, and the Lot of Fortune and the third lord of the triplicity, he will be brought up.")
SAHL_1_29_FN303 = ("This could possibly also be translated as, \"...in a stake, and not corrupted on the third day,\" "
                   "in other words, it is the fortune which is not corrupted on that day. But it is more likely that "
                   "the Moon's own condition is meant, as in Carmen I.12, and in Ch. 1.30, 28 below.")
SAHL_1_29_FN304 = "This probably means her third day."
SAHL_1_26_7 = ("And if the third day of the Moon she reached a sign having four feet, and the infortunes are looking "
               "at her, then that is an indicator that the native will have four feet or what resembles that.")
SAHL_1_38_1 = ("The signs having two feet and four: Gemini, Libra, and Aquarius have two feet; Aries, Leo, Taurus "
               "have four feet; and the <first> half of Sagittarius has two feet, and the other has four feet.")
SAHL_9_3 = ("The first item is that you look at the position of the Moon, where she is on the third day from the "
            "nativity, and who is looking at her (of the fortunes and infortunes), and in which sign she is, and "
            "which bound, and with whom she is connecting and who is looking at her, and how her condition is, and "
            "that of her lord.")
SAHL_1_30_22 = "Then, look at the position of the Moon on the third day, the seventh, and the fortieth day."
# Two days, the birth day counted as the first. The witness is Firmicus's
# worked chart: in the nativity of Albinus (Mathesis II.29, 21-22, 34; Figure
# 34 -- Mar 14 303 AD JC, 10:43:13 PM LMT, Rome, the Moon 14 58' Cancer, Mars
# 11 15' Aquarius) "on the third day the Moon, being established in Leo, full
# of light, flung herself into the rays of Mars" (II.29, 34; III.14, 17-19).
# By the ephemeris at the corrected Figure 34 instant (UT 21:53:17), the
# Moon is still at 29 Cancer one day after the birth, at 13 Leo two days
# after (on Mars's opposition ray), and at 28 Leo three days after (past
# it). Sahl's fn 95 and Holden's fn 2 on Rhetorius both witness the inclusive
# count: the birth day is the first, so the third is birth + 2.
# Owner's ruling of 2026-09-15.
MOON_THIRD_DAY_DAYS = 2.0
MOON_BURNED_DEGREES = 12.0    # The Introduction Ch. 3, 103, as evaluate_corruption_of_the_moon reads it
MOON_THIRD_DAY_COLUMNS = ['Item', 'Value', 'Text']
THIRD_DAY_CORRUPTION_READER = ("This limited reading checks whole-sign co-presence, square or opposition with an "
                               "infortune, enclosure, burning and falling; a trine or sextile alone does not count, "
                               "and other lunar defects are not assessed.")

SAHL_1_8_1 = ("Now indeed the indicators will be corrupted if the native was one of seven months (and likewise "
              "nativities having four feet): the amount of the stay in the belly will not be adequate, because "
              "animals are those whose stay is one month, five months, and more than that and less, and the "
              "indicator will not be suitable unless for one born in nine months.")
SAHL_1_8_3 = ("Know that the degrees of the circumference of the circle are 360, and they are divided into three "
              "sections; you start with the first division from the degree of the Ascendant up to the degree of "
              "the Midheaven, to the degree of the eighth, [then] to the degree of the stake of the earth, [then] "
              "to the degree of the Ascendant.")
SAHL_1_8_4 = ("And this dividing is made according to equal degrees which belong to the belt [of the zodiac]: so "
              "the first division is 150°, the second division 120°, and the third division 90°.")
SAHL_1_8_5 = ("Then calculate the meeting of the Sun and Moon which was before the native was born, in the month "
              "in which he was born, until you know in which sign it was, and which degree and minute.")
SAHL_1_8_6 = ("Then erect the degree of the Ascendant for that hour, and the Ascendant of the meeting will be at "
              "that time.")
SAHL_1_8_FN38 = "That is, of the pre-natal conception (according to Hephaistion)."
SAHL_1_8_FN40 = ("Or rather the lunation, whether it was a conjunction or opposition (assuming that Hephaistion does "
                 "not insist on only a conjunction).")
SAHL_1_8_COMMENT = ("It also omits to say in which chart the template is applied, but we will assume it is the "
                    "pre-conception lunation, as with Hephaistion")
SAHL_1_9_1 = ("Look at the Moon on the day the native was born, and erect her [position] by one year before that "
              "and a year after it; then look at these three stakes:")
SAHL_1_9_FN45 = ("This will yield three Moons: the rough Moon on the day of birth, the Moon exactly one year "
                 "before, and the Moon exactly one year after (the \"renewed\" Moon).")
SAHL_1_9_RULES = (
    (2, "For if the past Moon and the renewed Moon are looking at the Moon of the nativity from the trine, then "
        "the native will be complete at nine months."),
    (3, "And if the two Moons are looking at the Moon of the nativity from a square, then the native will be born "
        "at the shorter period, which is 258 days."),
    (4, "And if the past Moon was in its trine and the renewed Moon in its square, then the nativity belongs to "
        "the middle period; and likewise if [the situation] was reversed."),
    (5, "And if the past Moon was in the square of the Moon of the nativity, and the renewed Moon is not looking "
        "at the Moon of the nativity, then the native will be one of eight months, and will not survive, and "
        "will come out dead."),
    (6, "And if the first Moon opposed it and the second Moon was turned away from it, it indicates the shortness "
        "of [its] survival."),
    (7, "And if the two Moons were not looking at the Moon of the nativity, then the native will come out dead, "
        "and harsh misfortune will afflict the mother."),
    (8, "And if each one of the two Moons were in the seventh from the Moon of the nativity, then the native will "
        "be one of seven months."),
    (9, "And if the past Moon was in the seventh from the Moon of the nativity, and the renewed one in the trine "
        "of the Moon or Ascendant [of the nativity], then it will also be one of seven months."),
    (10, "And likewise [it is seven months] if the first Moon was in its trine or square, and the second Moon in "
         "its opposite."),
)
SAHL_1_9_11 = "And likewise if the Sun was in the seventh from the sign of the meeting of the conception."
SAHL_1_9_12 = ("And know that a native whose stay in the womb is greater than the second stay, up to the third, "
               "will be superior in his body and growth: for example, growing fleshier in the face and body.")
SAHL_1_9_13 = ("And those whose stay is more than the first stay up to the second one, their bodies will have "
               "decrease and weakness.")
SAHL_1_9_14 = ("Now once you knew that the native was one of nine months, then you ought to know the day and hour "
               "in which the planting [of the sperm] takes place in the womb, and how long the stay is in the "
               "belly of its mother, by the day and hour; and the knowledge of that is according to what I will "
               "explain to you.")
SAHL_1_9_FN47 = "Valens specifies 269 days."
SAHL_1_9_FN49 = "Valens has the trine here."
SAHL_1_9_FN51 = ("In this sentence Sahl or his source has combined two thoughts in Valens that indicate seven "
                 "months. The first has the past Moon opposing, the renewed Moon squaring the natal Moon and "
                 "Ascendant (as a continuation of 9). The second has the past Moon squaring, the renewed Moon "
                 "opposing.")
SAHL_1_9_FN53 = "Valens does not specify which meeting it is, but this seems to be the only option."
SAHL_1_9_FN54 = "That is, between a normal term (273 days) and the greater term (288 days)."
SAHL_1_9_FN55 = "That is, early term (258) to normal term (273)."
GESTATION_COLUMNS = ['Item', 'Value', 'Text']
GESTATION_NOT_COMPUTED = "Not computed"


def _ordinal_place(n):
    return f"{n}{'th' if 11 <= n % 100 <= 13 else {1: 'st', 2: 'nd', 3: 'rd'}.get(n % 10, 'th')}"


def sign_has_four_feet(lon):
    """1.38, 1's list (FOUR_FOOTED['On Nativities']): the whole sign, or the
    second half of Sagittarius (15° on). Returns the list's word or None."""
    sign = get_zodiac_sign(lon)
    part = FOUR_FOOTED['On Nativities'].get(sign)
    if part is None:
        return None
    if part == 'the second half' and (lon % 30.0) < 15.0:
        return None
    return part


def third_day_positions(jd_natal):
    """The seven planets two days after birth, with motion for enclosure."""
    jd = jd_natal + MOON_THIRD_DAY_DAYS
    out = {'jd': jd, 'planetary_data': {}}
    for name, planet_id in PLANET_SWE_IDS.items():
        position, _flag = swe.calc_ut(jd, planet_id)
        row = {'longitude': position[0] % 360.0, 'latitude': position[1], 'distance': position[2],
               'speed_in_lon': position[3], 'speed_in_lat': position[4], 'speed_in_dist': position[5]}
        out['planetary_data'][name] = row
        out[name] = row['longitude']
    return out


def _third_day_lon(third, planet):
    value = third[planet]
    return (value['longitude'] if isinstance(value, dict) else value) % 360.0


@_connection_aware
def third_day_enclosure(third):
    """Sahl enclosure for a complete third-day snapshot, or typed absence."""
    planetary_data = third.get('planetary_data')
    required = tuple(PLANET_SWE_IDS)
    if (not isinstance(planetary_data, dict)
            or any(name not in planetary_data for name in required)
            or any(not isinstance(planetary_data[name], dict)
                   or 'longitude' not in planetary_data[name]
                   or 'speed_in_lon' not in planetary_data[name]
                   for name in required)):
        return UnresolvedResult(
            reason=('enclosure was not evaluated because the third-day snapshot lacks all seven planets '
                    'with longitude and motion'),
            source='Sahl, The Introduction Ch. 3, 106 and 119-123',
            status='unavailable',
        )
    with doctrine(SAHL):
        configurations = _pairwise_configurations(planetary_data)
        blocking_pairs = {(row['Blocked'], row['From Reaching']) for row in evaluate_blocking(planetary_data)}
        enclosed, severe, separating, connecting = _sahl_enclosed(
            'Moon', {'Saturn', 'Mars'}, configurations, blocking_pairs)
        if (enclosed
                and _third_day_intervening_ray(
                    planetary_data, configurations, separating, connecting) is not None):
            enclosed, severe, separating, connecting = False, False, None, None
    return {'enclosed': enclosed, 'severe': severe, 'separating_from': separating,
            'connecting_to': connecting}


@_connection_aware
def moon_third_day_rows(third, natal_positions, ascendant_lon):
    """Separate 1.26 looking from the limited 1.29 corruption profile."""
    moon = _third_day_lon(third, 'Moon')
    sign = get_zodiac_sign(moon)
    place = get_wsh_house(moon, ascendant_lon)
    rows = []
    rows.append({'Item': 'The Moon on the third day',
                 'Value': f"{get_degree_string(moon)}, the {_ordinal_place(place)} place from the Ascendant of the nativity "
                          f"(two days after the birth, the birth day counted as the first, the birth hour kept)",
                 'Text': f'On Nativities 9, 3: "{SAHL_9_3}"'})
    feet = sign_has_four_feet(moon)
    rows.append({'Item': 'A sign having four feet (1.26, 7)',
                 'Value': (f"Yes: {sign}, {feet}" if feet else f"No: {sign} is not in 1.38, 1's list"
                           + (" (the first half of Sagittarius has two feet)" if sign == 'Sagittarius' else "")),
                 'Text': f'On Nativities 1.38, 1: "{SAHL_1_38_1}"'})
    looking = []
    copresent = []
    malefic_relations = []
    for inf in ('Saturn', 'Mars'):
        inf_lon = _third_day_lon(third, inf)
        how = _sahl_looks(inf_lon, moon)
        relation = f"{inf} by {how} from {get_zodiac_sign(inf_lon)}"
        if how in ('sextile', 'square', 'trine', 'opposition'):
            looking.append(relation)
        if how == 'in it':
            copresent.append(f"{inf} with her in {sign}")
        if how in ('in it', 'square', 'opposition'):
            malefic_relations.append(f"{inf} {'with her in ' + sign if how == 'in it' else 'by ' + how + ' from ' + get_zodiac_sign(inf_lon)}")
    rows.append({'Item': 'The infortunes looking at her (1.26, 7)',
                 'Value': ("Yes: " + "; ".join(looking) if looking
                           else f"No intersign look: Saturn in {get_zodiac_sign(_third_day_lon(third, 'Saturn'))} and "
                                f"Mars in {get_zodiac_sign(_third_day_lon(third, 'Mars'))}")
                          + " (whole-sign sextile, square, trine or opposition; co-presence is separate)",
                 'Text': f'On Nativities 1.26, 7: "{SAHL_1_26_7}"'})
    rows.append({'Item': 'Co-presence with an infortune',
                 'Value': "Yes: " + "; ".join(copresent) if copresent else f"No infortune shares {sign} with the Moon",
                 'Text': 'The Introduction Ch. 3, 106 and 119 distinguish an infortune being with the Moon from looking at her.'})
    enclosure = third_day_enclosure(third)
    if isinstance(enclosure, UnresolvedResult):
        enclosure_value = enclosure
        enclosure_state = enclosure
    elif enclosure['enclosed']:
        enclosure_value = (f"Yes: separating from {enclosure['separating_from']} and connecting with "
                           f"{enclosure['connecting_to']}"
                           + ("; both legs within 7°" if enclosure['severe'] else ""))
        enclosure_state = True
    else:
        enclosure_value = 'No: the required separation, application, and unobstructed rays are not all present'
        enclosure_state = False
    rows.append({'Item': 'Enclosure (The Introduction Ch. 3, 106 and 121)',
                 'Value': enclosure_value,
                 'Text': 'Evaluated by the same Sahl enclosure profile used in the Connections finding.'})
    sun_dist = abs(((moon - _third_day_lon(third, 'Sun') + 180.0) % 360.0) - 180.0)
    burned = sun_dist <= MOON_BURNED_DEGREES
    rows.append({'Item': 'Burning (1.29, 3)',
                 'Value': (f"Yes: {sun_dist:.1f}° from the Sun, within {MOON_BURNED_DEGREES:.0f}°" if burned
                           else f"No: {sun_dist:.1f}° from the Sun (burned within {MOON_BURNED_DEGREES:.0f}°, The Introduction Ch. 3, 103)"),
                 'Text': f'On Nativities 1.29, 3: "{SAHL_1_29_3}"'})
    falling = place in CADENT_HOUSES
    rows.append({'Item': 'Falling (1.29, 3)',
                 'Value': (f"Yes: the {_ordinal_place(place)} place from the Ascendant of the nativity, falling from the stakes"
                           if falling else f"No: the {_ordinal_place(place)} place from the Ascendant of the nativity"),
                 'Text': 'On Nativities 1.30, 33 asks "how is her position relative to the Ascendant"; the place is whole-sign.'})
    reasons = ((["infortune relation (" + "; ".join(malefic_relations) + ")"] if malefic_relations else [])
               + ([f"enclosed between {enclosure['separating_from']} and {enclosure['connecting_to']}"]
                  if enclosure_state is True else [])
               + (["burned"] if burned else []) + (["falling"] if falling else []))
    corrupted = doctrinal_or(bool(reasons), enclosure_state)
    if corrupted is True:
        corruption_value = "Corruption found in these checks: " + "; ".join(reasons)
    elif corrupted is False:
        corruption_value = "No corruption found in these checks"
    else:
        corruption_value = UnresolvedResult(
            reason='no evaluated corruption is positive, but enclosure is unavailable',
            source='On Nativities 1.29, 3; The Introduction Ch. 3, 106 and 121',
            alternatives=(('evaluated checks', 'no corruption found'), ('enclosure', enclosure_state)),
            status='unavailable',
        )
    rows.append({'Item': 'Third-day Moon: corruption checks (limited reading of 1.29, 3)',
                 'Value': corruption_value,
                 # Keep the quoted source alone in this fixed-schema field.
                 # The app renders THIRD_DAY_CORRUPTION_READER as a separate
                 # qualification and cites 1.29, 31 in its explanatory note.
                 'Text': f'On Nativities 1.29, 3: "{SAHL_1_29_3}"'})
    rows.append({'Item': '1.26, 7, the native with four feet',
                 'Value': ("Met: a sign having four feet, and " + "; ".join(looking) + " -- 1.26, 7's indicator, one among the chapter's"
                           if feet and looking
                           else "Not met: " + ("no infortune looking at her" if feet else "the sign has no four feet")),
                 'Text': f'On Nativities 1.26, 7: "{SAHL_1_26_7}"'})
    if corrupted is False:
        day_11 = 'Third-day component holds: no corruption found in these checks'
    elif corrupted is True:
        day_11 = 'Third-day component does not hold: corruption found in these checks'
    else:
        day_11 = UnresolvedResult('the third-day component depends on unevaluated enclosure',
                                  'On Nativities 1.29, 11', (('corruption profile', corrupted),),
                                  status='unavailable')
    rows.append({'Item': '1.29, 11, the third-day component',
                 'Value': (day_11 if isinstance(day_11, UnresolvedResult)
                           else day_11 + '; the triplicity lords and fortune in a stake are separate conditions not tested here'),
                 'Text': f'On Nativities 1.29, 11: "{SAHL_1_29_11}" (fn 303: "{SAHL_1_29_FN303}")'})
    in_asc_or_seventh = [inf for inf in ('Saturn', 'Mars') if get_wsh_house(natal_positions[inf], ascendant_lon) in (1, 7)]
    both = len(in_asc_or_seventh) == 2
    nat_places = ", ".join(f"{inf} in the {_ordinal_place(get_wsh_house(natal_positions[inf], ascendant_lon))}" for inf in ('Saturn', 'Mars'))
    day_12 = doctrinal_and(both, corrupted)
    if day_12 is True:
        day_12_value = (f"The first clause's two components hold: both infortunes are in the Ascendant or seventh "
                        f"({nat_places}), and corruption was found; the sentence's other conditions remain separate")
    elif day_12 is False:
        day_12_value = (f"Not met: the two infortunes {'are' if both else 'are not both'} in the Ascendant or seventh "
                        f"({nat_places}); " + ("corruption was found" if corrupted is True else "no corruption was established"))
    else:
        day_12_value = UnresolvedResult('the natal placement holds, but the third-day component depends on unevaluated enclosure',
                                        'On Nativities 1.29, 12', (('corruption profile', corrupted),),
                                        status='unavailable')
    rows.append({'Item': '1.29, 12, its first-clause components',
                 'Value': day_12_value,
                 'Text': f'On Nativities 1.29, 12: "{SAHL_1_29_12}"'})
    return rows


@_connection_aware
def evaluate_moon_third_day(chart_data):
    """The Moon on the third day (Sahl, On Nativities 1.29, 11-12; 1.26, 7):
    rows for her sign and condition two days after the birth, the birth
    day counted as the first. Display only."""
    third = third_day_positions(chart_data['julian_day'])
    p = chart_data['planetary_data']
    natal = {'Saturn': p['Saturn']['longitude'], 'Mars': p['Mars']['longitude']}
    return moon_third_day_rows(third, natal, chart_data['ascendant'])


def _calendar_date_is_valid(year, month, day, calendar):
    if not (1 <= int(year) <= 9999 and 1 <= int(month) <= 12 and 1 <= int(day) <= 31):
        return False
    y, m, d, _hour = swe.revjul(swe.julday(int(year), int(month), int(day), 0.0, calendar), calendar)
    return (int(y), int(m), int(d)) == (int(year), int(month), int(day))


def _moment_from_jd_in_calendar(jd, calendar):
    # Round the clock to a whole second relative to civil midnight. A value
    # that rounds to 24:00 belongs to the next date; clamping it to 23:59:59
    # silently changes the represented instant.
    day_start = math.floor(float(jd) + 0.5) - 0.5
    seconds = int(round((float(jd) - day_start) * 86400.0))
    if seconds >= 86400:
        day_start += 1.0
        seconds -= 86400
    elif seconds < 0:
        day_start -= 1.0
        seconds += 86400
    year, month, day, _hour = swe.revjul(day_start, calendar)
    return CivilMoment(year, month, day, seconds // 3600, (seconds % 3600) // 60, seconds % 60)


def anniversary_moment(local_birth, years, *, time_standard='fixed', zone_name=None, utc_offset_hours=0.0,
                       birth_calendar=None):
    """Repeat the local birth month/day/clock in the birth calendar, then UT.

    Named zones resolve the target date under that date's own rules. Fixed
    offsets, including LMT, retain the supplied offset. Invalid calendar
    dates and ambiguous or nonexistent named-zone clocks return a typed
    unavailable result; no elapsed-year substitute is invented.
    """
    local_birth = (local_birth if isinstance(local_birth, CivilMoment)
                   else CivilMoment(local_birth.year, local_birth.month, local_birth.day,
                                    local_birth.hour, local_birth.minute, local_birth.second))
    calendar = birth_calendar if birth_calendar is not None else civil_calendar(
        local_birth.year, local_birth.month, local_birth.day)
    target_year = local_birth.year + int(years)
    source = 'Sahl, On Nativities 1.9, 1; Valens, Anthologies I.22'
    if not _calendar_date_is_valid(target_year, local_birth.month, local_birth.day, calendar):
        return UnresolvedResult(
            reason=(f"{target_year:04d}-{local_birth.month:02d}-{local_birth.day:02d} is unavailable under the "
                    "same-date convention in the birth calendar"),
            source=source, status='unavailable')
    target = CivilMoment(target_year, local_birth.month, local_birth.day,
                         local_birth.hour, local_birth.minute, local_birth.second)
    named_zone = time_standard == 'named' or str(time_standard).startswith('Standard time')
    if named_zone:
        if not zone_name:
            return UnresolvedResult(reason='no named time zone is available for the anniversary',
                                    source=source, status='unavailable')
        try:
            zone = pytz.timezone(zone_name)
            # pytz takes Python's proleptic-Gregorian datetime as a carrier
            # for the local clock digits. The resulting offset is then
            # applied to the Julian Day made in `calendar` below, preserving
            # the target date in the birth calendar. This is the same policy
            # as the chart-input path. A Julian-only date has no datetime
            # carrier and is therefore unavailable under a named zone.
            naive = datetime(target.year, target.month, target.day, target.hour, target.minute, target.second)
            localized = zone.localize(naive, is_dst=None)
        except ValueError:
            return UnresolvedResult(
                reason=f"{target.isoformat()} is a Julian-calendar date that {zone_name} cannot represent",
                source=source, status='unavailable')
        except pytz.exceptions.AmbiguousTimeError:
            return UnresolvedResult(
                reason=f"{target.isoformat()} happens twice in {zone_name}; no occurrence is selected",
                source=source, status='unavailable')
        except pytz.exceptions.NonExistentTimeError:
            return UnresolvedResult(
                reason=f"{target.isoformat()} does not exist in {zone_name}",
                source=source, status='unavailable')
        offset = localized.utcoffset().total_seconds() / 3600.0
        standard_label = zone_name
    else:
        offset = float(utc_offset_hours)
        standard_label = 'LMT' if str(time_standard).startswith('LMT') else f"UTC{offset:+g}"
    local_jd = swe.julday(target.year, target.month, target.day, target.hour_decimal(), calendar)
    jd_ut = local_jd - offset / 24.0
    return {'jd': jd_ut, 'local': target, 'ut': _moment_from_jd_in_calendar(jd_ut, calendar),
            'offset_hours': offset, 'standard': standard_label,
            'calendar': 'Julian' if calendar == swe.JUL_CAL else 'Gregorian'}


def gestation_moons(jd_natal, birth_context=None):
    """1.9, 1's Moons on strict local-date anniversaries."""
    context = dict(birth_context or {})
    local_birth = context.get('local_moment') or CivilMoment.from_jd(jd_natal)
    kwargs = {'time_standard': context.get('time_standard', 'fixed'),
              'zone_name': context.get('timezone'),
              'utc_offset_hours': context.get('utc_offset_hours', 0.0),
              'birth_calendar': context.get('birth_calendar')}
    # The natal instant is already known and calculated. Anniversary date
    # resolution cannot erase it, even when a target date cannot be carried
    # by datetime or a named-zone clock is ambiguous.
    out = {'natal': {'jd': jd_natal,
                     'longitude': swe.calc_ut(jd_natal, swe.MOON)[0][0] % 360.0,
                     'result': None}}
    for key, years in (('past', -1), ('renewed', 1)):
        moment = anniversary_moment(local_birth, years, **kwargs)
        if isinstance(moment, UnresolvedResult):
            out[key] = {'jd': moment, 'longitude': moment, 'result': moment}
            continue
        jd = moment['jd']
        out[key] = {'jd': jd, 'longitude': swe.calc_ut(jd, swe.MOON)[0][0] % 360.0,
                    'result': moment}
    return out


def _anniversary_description(moment):
    return (f"local {moment['local'].isoformat()} ({moment['calendar']}, {moment['standard']}, "
            f"UTC offset {moment['offset_hours']:+g}); UT {moment['ut'].isoformat()}")


def _looks_phrase(how):
    if how is None:
        return 'not looking (in aversion)'
    if how == 'in it':
        return 'in her sign'
    return f'from the {how}' if how != 'opposition' else 'from the seventh (the opposition)'


def _gestation_tail_rows():
    return [
        {'Item': '1.9, 11, the meeting of the conception',
         'Value': f"{GESTATION_NOT_COMPUTED}: the sign of the meeting of the conception is not in hand before 1.10",
         'Text': f'On Nativities 1.9, 11: "{SAHL_1_9_11}" (fn 53: "{SAHL_1_9_FN53}")'},
        {'Item': '1.9, 12-13, the three stays',
         'Value': f"{GESTATION_NOT_COMPUTED}: the stay itself is found by 1.10; the terms are fn 54\'s and fn 55\'s",
         'Text': f'On Nativities 1.9, 12-13: "{SAHL_1_9_12} {SAHL_1_9_13}" (fn 54: "{SAHL_1_9_FN54}"; fn 55: "{SAHL_1_9_FN55}"). 1.9, 14: "{SAHL_1_9_14}"'},
    ]


def gestation_1_9_rows(natal_moon, past_moon, renewed_moon, ascendant_lon, anniversary_results=None):
    """1.9, 1-10 read from three longitudes and the Ascendant of the
    nativity, the aspects whole-sign. Pure, so a test can hand-build a case."""
    moments = anniversary_results or {}
    if isinstance(past_moon, UnresolvedResult) or isinstance(renewed_moon, UnresolvedResult):
        unknowns = [value for value in (past_moon, renewed_moon) if isinstance(value, UnresolvedResult)]
        result = UnresolvedResult(
            reason='the anniversary Moon pair is unavailable under the strict same-date convention',
            source='Sahl, On Nativities 1.9, 1-10',
            alternatives=tuple((f'anniversary {n + 1}', value) for n, value in enumerate(unknowns)),
            status='unavailable')
        rows = [
            {'Item': 'The Moon of the nativity (1.9, 1)', 'Value': f"{get_degree_string(natal_moon)}",
             'Text': f'On Nativities 1.9, 1: "{SAHL_1_9_1}" (fn 45: "{SAHL_1_9_FN45}")'},
            {'Item': 'The past Moon, a year before (1.9, 1)',
             'Value': past_moon if isinstance(past_moon, UnresolvedResult) else get_degree_string(past_moon),
             'Text': f'fn 45: "{SAHL_1_9_FN45}"'},
            {'Item': 'The renewed Moon, a year after (1.9, 1)',
             'Value': renewed_moon if isinstance(renewed_moon, UnresolvedResult) else get_degree_string(renewed_moon),
             'Text': f'fn 45: "{SAHL_1_9_FN45}"'},
            {'Item': '1.9, 2-10', 'Value': result,
             'Text': 'No aspect pair is inferred when a strict same-date anniversary is unavailable.'},
        ]
        return rows + _gestation_tail_rows()
    past, renewed = _sahl_looks(past_moon, natal_moon), _sahl_looks(renewed_moon, natal_moon)
    renewed_asc = _sahl_looks(renewed_moon, ascendant_lon)
    natal_sign = get_zodiac_sign(natal_moon)
    rows = [
        {'Item': 'The Moon of the nativity (1.9, 1)',
         'Value': f"{get_degree_string(natal_moon)}",
         'Text': f'On Nativities 1.9, 1: "{SAHL_1_9_1}" (fn 45: "{SAHL_1_9_FN45}")'},
        {'Item': 'The past Moon, a year before (1.9, 1)',
         'Value': f"{get_degree_string(past_moon)}, {_looks_phrase(past)} at {natal_sign} "
                  f"(the local same-date anniversary at the birth clock time"
                  + (f"; {_anniversary_description(moments['past'])}" if moments.get('past') else "") + ")",
         'Text': f'fn 45: "{SAHL_1_9_FN45}"'},
        {'Item': 'The renewed Moon, a year after (1.9, 1)',
         'Value': f"{get_degree_string(renewed_moon)}, {_looks_phrase(renewed)} at {natal_sign}"
                  + (f"; from the trine of the Ascendant ({get_zodiac_sign(ascendant_lon)})" if renewed_asc == 'trine' else "")
                  + (f"; {_anniversary_description(moments['renewed'])}" if moments.get('renewed') else ""),
         'Text': f'fn 45: "{SAHL_1_9_FN45}"'},
    ]
    hits = {
        2: past == 'trine' and renewed == 'trine',
        3: past == 'square' and renewed == 'square',
        4: (past == 'trine' and renewed == 'square') or (past == 'square' and renewed == 'trine'),
        5: past == 'square' and renewed is None,
        6: past == 'opposition' and renewed is None,
        7: past is None and renewed is None,
        8: past == 'opposition' and renewed == 'opposition',
        9: past == 'opposition' and (renewed == 'trine' or renewed_asc == 'trine'),
        10: past in ('trine', 'square') and renewed == 'opposition',
    }
    verdicts = {2: 'complete at nine months', 3: 'the shorter period, which is 258 days',
                4: 'the middle period (fn 47: "' + SAHL_1_9_FN47 + '")',
                5: 'one of eight months, and will not survive, and will come out dead',
                6: 'the shortness of [its] survival (fn 49: "' + SAHL_1_9_FN49 + '")',
                7: 'will come out dead, and harsh misfortune will afflict the mother',
                8: 'one of seven months', 9: 'one of seven months',
                10: 'seven months (fn 51: "' + SAHL_1_9_FN51 + '")'}
    matched = False
    for n, text in SAHL_1_9_RULES:
        if hits[n]:
            matched = True
            rows.append({'Item': f'1.9, {n}', 'Value': f"Met: {verdicts[n]}", 'Text': f'On Nativities 1.9, {n}: "{text}"'})
    if not matched:
        rows.append({'Item': '1.9, 2-10',
                     'Value': f"No sentence names this pair: the past Moon {_looks_phrase(past)}, the renewed Moon {_looks_phrase(renewed)}",
                     'Text': 'On Nativities 1.9, 2-10 name the trine, the square, the seventh and not looking; the sextile and her own sign are not named.'})
    return rows + _gestation_tail_rows()


def evaluate_gestation(chart_data, lat=None, lon=None, birth_context=None):
    """The fetus's stay (Sahl, On Nativities 1.8-1.9): what the two chapters
    let the app state. Display only."""
    jd = chart_data['julian_day']
    lat = chart_data.get('geo_lat') if lat is None else lat
    lon = chart_data.get('geo_lon') if lon is None else lon
    lunations = sahl_prenatal_meeting_and_fullness(jd, lat, lon)
    meeting = lunations['meeting']
    _, ascmc = swe.houses(meeting['jd'], lat, lon, b'B')
    rows = [
        {'Item': 'The meeting before the birth (1.8, 5)',
         'Value': f"The last New Moon before the birth: {get_degree_string(meeting['longitude'])}",
         'Text': f'On Nativities 1.8, 5: "{SAHL_1_8_5}"'},
        {'Item': 'The Ascendant of the meeting (1.8, 6)',
         'Value': f"{get_degree_string(ascmc[0])}, erected for the hour of the meeting at the birthplace",
         'Text': f'On Nativities 1.8, 6: "{SAHL_1_8_6}"'},
    ]
    fullness = lunations['fullness']
    if fullness['jd'] > meeting['jd']:
        _, ascmc_f = swe.houses(fullness['jd'], lat, lon, b'B')
        rows.append({'Item': 'The lunation nearer the birth, an opposition (fn 40)',
                     'Value': f"The Full Moon after that meeting: {get_degree_string(fullness['longitude'])} "
                              f"(the degree of {fullness['degree_of']}), its Ascendant {get_degree_string(ascmc_f[0])}",
                     'Text': f'Dykes, fn 40: "{SAHL_1_8_FN40}"'})
    rows.append({'Item': 'The three divisions (1.8, 3-4) and their reading (1.8, 7-13)',
                 'Value': f"{GESTATION_NOT_COMPUTED}: the divisions are framed from the degree of the Ascendant of a chart "
                          f"the sentence does not name; Dykes reads it as the pre-conception lunation, which no sentence "
                          f"of 1.8 shows how to find, so 7-13 are not read",
                 'Text': f'On Nativities 1.8, 3-4: "{SAHL_1_8_3} {SAHL_1_8_4}" (fn 38: "{SAHL_1_8_FN38}"; Dykes\'s comment: "{SAHL_1_8_COMMENT}")'})
    rows.append({'Item': 'What the indicators presume (1.8, 1)',
                 'Value': 'The chapter\'s indicators are for one born in nine months',
                 'Text': f'On Nativities 1.8, 1: "{SAHL_1_8_1}"'})
    moons = gestation_moons(jd, birth_context)
    rows.extend(gestation_1_9_rows(moons['natal']['longitude'], moons['past']['longitude'],
                                   moons['renewed']['longitude'], chart_data['ascendant'],
                                   {key: value['result'] for key, value in moons.items()
                                    if isinstance(value.get('result'), dict)}))
    return rows
# --- Abu 'Ali's years ladder (JN Ch. 3-4), with 'Umar (TBN I.4.3): a SUPPLEMENT fallback where 1.20 is silent ---
# Reconciliation decision 9 (owner, 2026-09-15). Sahl 1.20 is the canon and
# stays as graded above; this ladder is shown at the supplement depth only,
# and only for a house-master no sentence of 1.20, 10-34 reaches. The
# sentences are quoted from Persian Nativities I, pp. 233-235 (photographed
# 2026-09-15) and Persian Nativities II, pp. 13-14; the test
# tests/test_years_ladder_2026_09_15.py holds them to the page. Ch. 4's
# additions and subtractions are listed, display only, by
# evaluate_jn_years_additions below (2026-09-15; the app still declares
# Sahl's 1.21 additions not applied); the one place JN and Sahl contradict each other
# -- the fortunes' squares and oppositions -- is quoted on the page, both
# sides. 'Umar's reading of the superiors (I.4.3, "not so much ... a
# serious impediment") rides in 1.20, 14-15's note (decision 10) and is not
# repeated here.
JN_YEARS_LADDER = {
    # place -> the class of years, JN Ch. 3 (each sentence verbatim)
    'places': (
        ('angle', 'greater',
         "But when you knew the kadukhudhāh, look to see if it were in the angles, in its own domicile or exaltation or "
         "triplicity, and oriental, free from the bad ones and retrogradation and burning up: it signifies its own "
         "greater years for the native."),
        ('succedent', 'middle',
         "And if it were in a follower of the angles, in the aforesaid likeness [as was said] in the angles, it decrees "
         "its own middle years to the native."),
        ('cadent', 'lesser',
         "If however it were in the cadents from the angles, with these conditions which we wrote before in the angles, "
         "it grants its own lesser years."),
    ),
    'preface': ("And know that if the kadukhudhāh were diminished by some one of these ways which I have recited, it will "
                "subtract from its own years according to the place of the circle, in this way which I will have stated:"),
    # the demotions, in the order the chapter states them (each sentence verbatim)
    'demotions': (
        ('not oriental',
         "For if it were in the angles (but what I said, with the exception that it is not oriental), it will be "
         "transformed from the greater years to the middle ones."),
        ('occidental and peregrine',
         "And if it were occidental and peregrine, it will go down from the middle years to the lesser years."),
        ('occidental, peregrine, retrograde and burned up',
         "And if it were occidental, peregrine, retrograde and burned up, it will be transformed from the quality of the "
         "lesser years and months, to a likeness of the same quality of them to days."),
        ('one rule',
         "And you will observe it likewise for the rest of the planets' impediments, because it is one rule."),
    ),
    'ranks': ('greater', 'middle', 'lesser', 'months', 'days'),
}
# JN Ch. 4's table, "The years which the individual planets decree when they
# are the kadukhudhāh": (an angle: greater, a succeedent: middle, a cadent: lesser).
JN_YEARS_TABLE = {
    'Saturn': (57, 43.5, 30), 'Jupiter': (79, 45.5, 12), 'Mars': (66, 40.5, 15), 'Sun': (120, 69.5, 19),
    'Venus': (82, 45, 8), 'Mercury': (76, 48, 20), 'Moon': (108, 66.5, 25),
}
# Where Ch. 4's count differs from this app's table (the luminaries' middle years).
JN_YEARS_TABLE_DIFFERS = {p: (JN_YEARS_TABLE[p][1], PLANETARY_YEARS[p]['middle'])
                          for p in JN_YEARS_TABLE if JN_YEARS_TABLE[p][1] != PLANETARY_YEARS[p]['middle']}
JN_CH4_ADDITIONS = ("Nevertheless it must be known [that] the square or opposite rays of the fortunes add or subtract nothing "
                    "from the kadukhudhāh, just as even the sextiles and trigons of the infortunes make no addition nor diminution.")
SAHL_1_21_8 = ("And if the fortunes looked from a square or opposition, or they were with the house-master, then they will "
               "<not> withhold years, but will even add the equivalent of its lesser years—if they were not retrograde nor "
               "burned: for if [the fortune] was retrograde or burned, it adds the equivalent of its lesser years, in months.")
# 'Umar al-Tabari, Book of Nativities I.4.3: the ladder as he states it (verbatim).
TBN_YEARS_RULE = (
    "After these things, we will look to the place of this planetary mubtazz for knowing the years [of the native]: which "
    "if it were oriental and in an angle, and in addition it were in its own ḥayyiz (that is, a masculine planet in the day "
    "in a masculine sign above the earth, and so on), and it were in its own dignity (like in the domicile and exaltation, "
    "in the triplicity and the bound), and it came about that it is in [its own] ḥayyiz and in the angles which are the "
    "Ascendant and the Midheaven, it will give its own greater years. But if it were oriental in its own dignity and in "
    "the followers of the angles, and especially in [its own] ḥayyiz, if it were not in the degrees of the angle, free, it "
    "will signify its own middle years. And if it were free in the same way which we have said, but it were cadent from "
    "an angle, it signifies its own lesser years. But if [this] happened to it with fall, retrogradation, and "
    "peregrination, or descension, it will signify hours according to the number of its own lesser years.")
# I.4.3's further sentences, where 'Umar differs from Abu 'Ali (verbatim).
TBN_YEARS_DIFFERENCES = {
    'angle': ("'Umar's greater years ask the Ascendant or the Midheaven and the planet's own ḥayyiz: \"and it came about "
              "that it is in [its own] ḥayyiz and in the angles which are the Ascendant and the Midheaven, it will give "
              "its own greater years\"; Abu 'Ali's ladder names the angles without distinction."),
    'succedent': ("'Umar counts the eleventh by day and the fifth by night among the greater-years places: \"Which if it "
                  "were in its own domicile or exaltation or its own triplicity, in the Midheaven or in the Ascendant or in "
                  "the 11th (if it were diurnal, that is, in the figure of the day), and in such a strength in the fourth "
                  "and in the fifth (in the night), it signifies its own greater years\" -- \"greater\" is Dykes's emendation "
                  "(fn 82: reading maiores for minores). Abu 'Ali's follower of the angles is the middle years. And 'Umar's "
                  "next sentence (I.4.4) has an angular planet give its greater years \"oriental or not oriental (if, however, "
                  "not retrograde nor burned up)\" -- no step for orientality, where Abu 'Ali's ladder takes one."),
    'cadent': ("'Umar drops a cadent planet under impediment to hours, not by steps: \"But if [this] happened to it with "
               "fall, retrogradation, and peregrination, or descension, it will signify hours according to the number of "
               "its own lesser years.\""),
    'peregrine': ("'Umar takes no step for peregrination in an angle or a follower: \"But if the kadukhudhāh were outside "
                  "(that is, [outside] the domicile and the rest of these places), in the angles or the succeedents of the "
                  "angles, it signifies its own years whether it were peregrine or whatever its condition was—unless it "
                  "were burned up, because burning up signifies a scarcity of life.\""),
}
JN_YEARS_CITATION = "Abu 'Ali, Judgments of Nativities Ch. 3 (with 'Umar, TBN I.4.3)"
JN_YEARS_NOTE = (
    "This ladder is Abu 'Ali's, not Sahl's. This app shows it only where no sentence of On Nativities 1.20, 10-34 reaches "
    "the planet; Sahl's grade, where he gives one, is never overridden by it. Read: the place by the division, as 1.20 is "
    "placed; one step of the ladder for each impediment the chapter names -- not oriental, peregrine, retrograde, burned "
    "up -- in the ranks greater, middle, lesser, months, days, because \"you will observe it likewise for the rest of the "
    "planets' impediments, because it is one rule\" (the chapter states the angular cases with one, two and four impediments; the single steps between are this app's reading of \"one rule\"), and no step below days (the count of months or days is not the chapter's -- \"the number of the lesser years\" is 'Umar's and Sahl 1.20, 15's); \"peregrine\" is a planet in none of its five "
    "shares (the chapter's own condition names the domicile, exaltation and triplicity); \"burned up\" is this app's "
    "under-the-rays fact; the Sun takes no step for orientality; \"free from the bad ones\" is not tested here: this is an unassessed Ch. 3 rank condition. Ch. 4's "
    "additions and subtractions are a separate count, listed below planet by planet and not applied. Where Ch. 4's count differs from this app's table it is printed as Ch. 4 "
    "has it. On the additions Abu 'Ali and Sahl disagree: Ch. 4, \"" + JN_CH4_ADDITIONS + "\"; On Nativities 1.21, 8 as "
    "printed, \"" + SAHL_1_21_8 + "\" (fn 160: 8-14 \"do match TBN I.4.4\"). Neither is applied.")


def _jn_years_ladder_branch(planet, q, facts, eastern):
    """One JN Ch. 3 route with orientality fixed to a Boolean value."""
    place = 'angle' if q in ANGLE_HOUSES else ('succedent' if q in SUCCEDENT_HOUSES else 'cadent')
    place_row = next(r for r in JN_YEARS_LADDER['places'] if r[0] == place)
    ranks = JN_YEARS_LADDER['ranks']
    demotions = dict(JN_YEARS_LADDER['demotions'])
    steps = []
    if planet != 'Sun' and not eastern:
        steps.append(('not oriental', demotions['not oriental']))
    if not facts['share']:
        steps.append(('peregrine', demotions['occidental and peregrine']))
    if facts['retrograde']:
        steps.append(('retrograde', demotions['occidental, peregrine, retrograde and burned up']))
    if facts['under the rays']:
        steps.append(('burned up', demotions['occidental, peregrine, retrograde and burned up']))
    rank = min(ranks.index(place_row[1]) + len(steps), len(ranks) - 1)
    cls = ranks[rank]
    years = JN_YEARS_TABLE[planet]
    if rank <= 2:
        count, unit = years[rank], 'years'
    else:
        count, unit = years[2], cls
    umar = [TBN_YEARS_DIFFERENCES[place]]
    if place != 'cadent' and not facts['share']:
        umar.append(TBN_YEARS_DIFFERENCES['peregrine'])
    if place == 'cadent' and not steps:
        umar = []                                     # both give a free cadent planet its lesser years
    table_note = None
    if rank <= 2 and planet in JN_YEARS_TABLE_DIFFERS and rank == 1:
        jn_v, app_v = JN_YEARS_TABLE_DIFFERS[planet]
        table_note = f"Ch. 4's table gives {planet}'s middle years as {jn_v:g}; this app's table reads {app_v:g}."
    stepped = (" -- " + ", ".join(s for s, _ in steps) + f" ({len(steps)} step{'s' if len(steps) > 1 else ''} down)") if steps else ""
    if unit == 'years':
        text = f"the {cls} years, {count:g} ({planet}), {place} by the division ({q}){stepped}"
    else:
        text = f"{cls}, the number of the lesser years ({count:g}, {planet}), {place} by the division ({q}){stepped}"
    outcome = YearsOutcome(f"JN Ch. 3, {place}", cls, count, unit, text)
    return {'class': cls, 'count': count, 'unit': unit, 'place': place, 'division': q,
            'facts': dict(facts, eastern=eastern, westernizing=not eastern), 'steps': steps,
            'jn': place_row[2], 'umar': umar, 'text': text, 'citation': JN_YEARS_CITATION,
            'table_note': table_note, 'result': outcome}


def _conditional_years_rows(eastern, western, reason, source):
    """Combine two complete years routes without collapsing either value."""
    eastern_evidence, western_evidence = _lunar_orientation_evidence(source)
    if _same_years_outcome(eastern['result'], western['result']):
        outcome = eastern['result']
        common_outcome = YearsOutcome(
            outcome.sentence, outcome.grade, outcome.number, outcome.unit,
            f"the common result under both lunar-orientality alternatives: {outcome.grade}, "
            f"{outcome.number:g} {outcome.unit}",
        )
        combined = dict(eastern)
        combined.update({'text': common_outcome.text, 'result': common_outcome, 'steps': [],
                         'facts': dict(eastern['facts'], eastern=eastern_evidence,
                                       westernizing=western_evidence),
                         'alternative_routes': (('if the Moon is eastern', eastern),
                                                ('if the Moon is not eastern', western))})
        return combined
    result = UnresolvedResult(
        reason=reason,
        source=source,
        alternatives=(('if the Moon is eastern', eastern['result']),
                      ('if the Moon is not eastern', western['result'])),
    )
    combined = dict(eastern)
    combined.update({'class': result, 'count': result, 'unit': result, 'text': result, 'result': result,
                     'place': result, 'steps': result, 'jn': result, 'umar': result, 'table_note': result,
                     'facts': dict(eastern['facts'], eastern=eastern_evidence,
                                   westernizing=western_evidence),
                     'alternative_routes': (('if the Moon is eastern', eastern),
                                            ('if the Moon is not eastern', western))})
    return combined


def jn_years_ladder(planet, division, facts, eastern=None):
    """Evaluate JN Ch. 3 alone, preserving both lunar-orientality routes."""
    if planet not in JN_YEARS_TABLE:
        return None
    if planet == 'Moon' and eastern is None:
        return _conditional_years_rows(
            _jn_years_ladder_branch(planet, division, facts, True),
            _jn_years_ladder_branch(planet, division, facts, False),
            "the supplied passages do not define the Moon's orientality",
            "Abu 'Ali, Judgments of Nativities Ch. 3",
        )
    if eastern is None:
        eastern = True if planet == 'Sun' else facts['eastern']
    return _jn_years_ladder_branch(planet, division, facts, eastern)


def _jn_solar_facts(planet, planetary_data, facts):
    """Keep JN's existing solar convention separate from Sahl's heart."""
    row = planetary_data[planet]
    phase = solar_phase(planet, row['longitude'], planetary_data['Sun']['longitude'], row.get('speed_in_lon'))[0]
    return dict(facts, **{'under the rays': phase in ('Burned', 'Under the rays')})


def jn_years_fallback(planet, planetary_data, cusps, sect, essential):
    """The JN Ch. 3 supplement where a complete Sahl 1.20 route is silent.

    For the Moon, each permitted orientality assignment traverses Sahl first
    and reaches JN only if that same route is silent. This prevents a
    conditional Sahl result from being mistaken for blanket source silence.
    """
    if planet not in JN_YEARS_TABLE:
        return None
    if planet != 'Moon':
        g = sahl_house_master_years(planet, planetary_data, cusps, sect, essential)
        if g is None or g['grade'] is not None:
            return None
        return jn_years_ladder(planet, g['division'], _jn_solar_facts(planet, planetary_data, g['facts']))

    routes = []
    for label, eastern in (('if the Moon is eastern', True), ('if the Moon is not eastern', False)):
        sahl = _sahl_house_master_years_branch(planet, planetary_data, cusps, sect, essential, eastern)
        if sahl is None:
            return None
        if sahl['grade'] is not None:
            route = {'class': sahl['grade'], 'count': sahl['years'], 'unit': sahl['result'].unit,
                     'place': 'Sahl 1.20', 'division': sahl['division'], 'facts': sahl['facts'], 'steps': [],
                     'jn': None, 'umar': [], 'text': sahl['text'], 'citation': 'Sahl, On Nativities 1.20',
                     'table_note': None, 'result': sahl['result'], 'route': 'Sahl 1.20'}
        else:
            route = jn_years_ladder(planet, sahl['division'], _jn_solar_facts(planet, planetary_data, sahl['facts']), eastern=eastern)
            route = dict(route, route='JN Ch. 3 after Sahl 1.20 is silent')
        routes.append((label, route))
    eastern, western = routes[0][1], routes[1][1]
    if _same_years_outcome(eastern['result'], western['result']):
        if all(route['route'] == 'Sahl 1.20' for _, route in routes):
            return None
        return eastern
    result = _conditional_years_rows(
        eastern, western,
        "the supplied passages do not define the Moon's orientality; each alternative follows Sahl first",
        "Sahl, On Nativities 1.20; Abu 'Ali, Judgments of Nativities Ch. 3",
    )
    result['alternative_routes'] = tuple(routes)
    result['citation'] = "Sahl, On Nativities 1.20; " + JN_YEARS_CITATION
    return result

# --- Abu 'Ali's additions and subtractions to the house-master's years (JN Ch. 4), DISPLAY ONLY, supplement ---
# The chapter's second half, built 2026-09-15 on the owner's work order and
# amended 2026-09-17 on the owner's JN-CH4 ruling (Astra's reading adopted
# whole: labels, witness cells, two luminary rows, the note; no row's
# arithmetic changed). Sahl 1.20 stays the grant of the years and the app's
# 1.21 declaration ("the 1.21 additions" not applied) stands: this lists what
# each planet WOULD add or subtract by Ch. 4, one row a planet, and forms no
# sum. Sentences quoted from Persian Nativities I, p. 235 (photographed
# 2026-09-15, read against the page 2026-09-16), fn 27-28; Abu Bakr I.15 (PN
# II pp. 129-131) and 'Umar I.4.4 (PN II pp. 15-16) as witnesses, each
# witness's conditions attached to its own result and gating no row of Abu
# 'Ali's. The test tests/test_jn_years_additions_2026_09_15.py holds the
# sentences to the page.
JN_CH4_SENTENCES = {
    'fortune': ("If therefore you examined how much the kadukhudhāh signified (of years, months and days), and you wished "
                "to know what the planets added to it or subtracted from it, do thusly: consider diligently if a fortune "
                "were joined with it or aspected it by a trine or sextile aspect: it will add its own lesser years to [the "
                "kadukhudhāh]; and if [the fortune] were middling in strength, [it will give] so many months; and if it "
                "were more unsound, days or hours."),
    'infortune': ("But if a bad one were joined to [the kadukhudhāh] or aspected it by a square [or] opposite aspect, it "
                  "will subtract its own lesser years from [the kadukhudhāh]."),
    'nothing': JN_CH4_ADDITIONS,
    'mercury': ("Mercury however, if he were with fortunes (which add), he will add his own lesser years to the "
                "kadukhudhāh; and if he were with bad ones, he will subtract them."),
    'mars': "But of all of those which impede the kadukhudhāh more, it is Mars.",
    'fn27': "Reading quadrati vel oppositi...radii for quadratos vel oppositos...radios.",
    'fn28': ("This probably means that if Mercury were with or in aspect to a benefic, and he himself were in a sextile "
             "or trine relationship to the kadukhudhāh, he would add years; but if he were with or in aspect to a "
             "malefic, and he himself were in a square or opposition to the kadukhudhāh, he would subtract. Nativities "
             "§4 is much briefer."),
}
# The three grades of the fortune's addition, in the chapter's words; no
# text defines "middling in strength" or "more unsound", so no grade is chosen.
JN_CH4_GRADES = (("its own lesser years", "years"),
                 ("if [the fortune] were middling in strength, [it will give] so many months", "months"),
                 ("if it were more unsound, days or hours", "days or hours"))
# Witnesses beside Abu 'Ali (verbatim).
ABU_BAKR_I15_ADDITIONS = {
    'method': ("Therefore operate according to this method by adding with the fortunes and diminishing with the "
               "infortunes. And the fortunes add to the native's years by conjunction or the sextile or trine aspect, "
               "but the infortunes subtract by their conjunction or square aspect or the opposition."),
    'grades': ("Which if planets aspecting the kadukhudhāh were in the Ascendant, the 10th or 7th house, they will give "
               "their own lesser years. If they were in the 5th, in the 10th or 4th, and in their own domiciles, "
               "exaltations, triplicities or bounds, they will confer their own lesser years. But if they were "
               "retrograde, burned up or unfortunate, they will give months for their own lesser years. And if they "
               "were in angles or succeedents, and also retrograde, burned up or impeded, or in their own fall, as was "
               "said, they will give days or hours for the lesser years."),
    'differs': ("If an infortune aspected the significator of life from the square or opposite aspect, it will subtract "
                "from it its own years from the given years. And if the said infortune aspected the said significator by "
                "a trine or sextile aspect, and from a good place, it will add its own lesser years with the given "
                "years. And if a fortune aspected it by any aspect, it will subtract nothing from the given years, but "
                "it will always add."),
    'mercury': ("But this is not to be said about Mercury: for if he were with an infortune, he will increase the evil "
                "and misfortune."),
    'luminaries': ("If the Sun and Moon were made unfortunate, they destroy, and they add to the infortunes in evil and "
                   "destruction. Which if it were in their dignity or with fortunes, they will remove evil from the "
                   "infortunes."),
}
TBN_I44_ADDITIONS = {
    'fortunes': ("Know that if fortunes aspected the kadukhudhāh, and it were not under the rays of the Sun, they will "
                 "increase for it, and they will give it their own lesser years."),
    'grades': ("Which if the good ones gave something to the kadukhudhāh by means of an aspect, [the good one] will "
               "increase for it [by] months or days according to its own lesser years, if [the good one] were impeded "
               "or seized."),
    'squares': ("Know that the fortunes, if they aspected from out of a square aspect or the opposition, or they were "
                "with the kadukhudhāh, they do not cut off anything from him—that is, it does not take away from it, "
                "but they increase the lesser years if they were not retrograde or burned up or impeded by the bad ones."),
    'infortunes': ("And if the bad ones aspected the kadukhudhāh from out of the opposition or the square aspect, or "
                   "were with it in one sign, they will subtract from it according to their own lesser years, whether "
                   "they received it or not."),
    'seized': ("If however a bad one aspected the kadukhudhāh by a trine or sextile aspect, and they seized it without "
               "the aspect of the fortunes, then the bad one which was so [placed] will subtract according to its own "
               "lesser years."),
    'fn87': "Again, this is probably besiegement.",
    'sun': ("If the Sun were with the kadukhudhāh by conjunction or opposition and the square aspect, he likewise cuts "
            "off [according to the number of] his lesser years. If he aspected the kadukhudhāh from out of a trine or "
            "sextile, he adds his own lesser years to it."),
    'sun_reception': ("And if he were conjoined to or opposite it, or he were in its square aspect, and there were "
                      "reception in the same place, he will subtract months or days [according to] a like [number] of "
                      "his own lesser years, if God wills."),
}
JN_CH4_ADDITIONS_CITATION = "Abu 'Ali, Judgments of Nativities Ch. 4 (fn 27-28), with Abu Bakr I.15 and 'Umar, TBN I.4.4"
JN_CH4_ADDITIONS_NOTE = (
    "Display only: no total is formed and these rows do not change the Sahl-based grant of the years above (Sahl's "
    "own 1.21 additions stay not applied). The principal rows state Abu 'Ali, Ch. 4. A fortune's square or "
    "opposition and a bad one's sextile or trine explicitly contribute zero in that chapter. Abu Bakr and 'Umar are "
    "separate witnesses with different conditions and, in some cases, different effects; each witness's conditions "
    "('Umar's house-master under the rays and his fortune retrograde, burned up or impeded; Abu Bakr's reception and "
    "binding) are not assessed in the Witnesses column, which gives their reported amounts; they gate no row of Abu 'Ali's. Fortune strength "
    "grades are conditional alternatives; the chapter does not operationally define their selection, so no grade is "
    "chosen and none defaults to years. Mercury results derived from Dykes fn 28 are conjectural interpretations: "
    "his addition requires his sextile or trine to the house-master and a benefic companion whose own Ch. 4 row "
    "actually adds; a positive sub-year grade still qualifies. Unmatched, unresolved, or mixed cases are not silently "
    "treated as zero. A solar modifier shown under 'Umar belongs to "
    "that witness; no explicit numerical lunar modifier is supplied here. Whole-sign aspects and any same-sign or "
    "bodily-conjunction definition used by this display are declared implementation conventions except where a "
    "cited clause expressly specifies sharing a sign ('Umar's \"or were with it in one sign\"): \"joined\" is read by "
    "whole sign, as this app's other readings of Abu 'Ali are, and Mercury \"with\" a fortune or a bad one is this "
    "app's convention, whole sign -- in one sign or in any whole-sign aspect, as fn 28 has \"with or in aspect to\" -- "
    "the house-master itself not counted as his company. The fortunes are Jupiter and "
    "Venus, the bad ones Saturn and Mars; the lesser years are Ch. 4's own column, which this app's table matches.")


JN_CH4_ZERO = "0 -- explicitly neither adds nor subtracts (Abu 'Ali, Ch. 4)"
JN_CH4_NO_LUMINARY = "no explicit luminary modifier specified here"
JN_CH4_FN28_LABEL = "Dykes fn 28: conjectural interpretation"
# The witness cells, one a case: what Abu Bakr I.15 and 'Umar I.4.4 say of the
# same configuration, each with its own conditions, gating nothing of Abu 'Ali's.
JN_CH4_WITNESSES = {
    'fortune adds': "'Umar I.4.4: months if retrograde or burned up",
    'fortune square': "'Umar I.4.4: adds its lesser years if not retrograde, burned up or impeded",
    'infortune trine': ("Abu Bakr I.15: adds its lesser years from a good place (to the significator of life, his term) "
                        "· 'Umar I.4.4: subtracts if it seizes the house-master without a fortune's aspect (fn 87 reads "
                        "'seized' as besieged)"),
    'mercury': ("Abu Bakr I.15: \"But this is not to be said about Mercury: for if he were with an infortune, he will "
                "increase the evil and misfortune.\""),
    'sun': ("'Umar I.4.4: subtracts his lesser years by conjunction, square or opposition; adds them by trine or "
            "sextile; with reception in those three, months or days (no unit chosen) -- the 19 is Ch. 4's table reused"),
    'moon': ("Abu Bakr I.15: made unfortunate, the luminaries destroy and add to the infortunes in evil; in their "
             "dignity or with fortunes they remove evil (the antecedent of 'it' unclear as printed)"),
}


def _jn_positive_contribution(row):
    """Whether a benefic row contributes a positive amount under Ch. 4.

    Every listed grade of an ``adds`` row is positive, including months,
    days, or hours, so an unchosen strength grade does not make this gate
    unknown. Explicit zero and absence are false; a typed unknown survives.
    """
    if row is None:
        return False
    effect = row.get('effect')
    if effect == 'adds':
        return True
    if effect == 'nothing':
        return False
    if isinstance(effect, UnresolvedResult):
        return effect
    return False


def mercury_jn_addition_gate(permitted_aspect, companion_contributions):
    """Three-valued fn 28 gate: Mercury's aspect AND an actual giver."""
    return doctrinal_and(permitted_aspect, doctrinal_or(*companion_contributions))


def _jn_mercury_indeterminate(status, reason, alternatives=()):
    label = {'unresolved': 'Unresolved', 'not decided': 'Not decided',
             'unavailable': 'Not specified'}[status]
    return UnresolvedResult(
        reason=reason,
        source="Abu 'Ali, Judgments of Nativities Ch. 4; Dykes fn 28",
        alternatives=alternatives,
        status=status,
        display_label=f"{JN_CH4_FN28_LABEL} — {label}",
    )


def evaluate_jn_years_additions(house_master, planetary_data):
    """JN Ch. 4's additions and subtractions to the house-master's years,
    DISPLAY ONLY: one row for each other planet joined to the house-master
    or aspecting it by whole sign -- a fortune joined, trine or sextile
    adds its lesser years (three grades, none chosen and none defaulting
    to years), a bad one joined, square or opposite subtracts its lesser
    years, a fortune's square or opposition and a bad one's sextile or
    trine are the chapter's explicit zero, Mercury by fn 28 (Dykes's
    conjecture, labelled so; the cases fn 28 does not pair are not
    decided under it, with the sentence's literal reading beside). A
    planet in aversion gets no row. After the five, the luminaries, always
    (unless the house-master): the chapter gives them no modifier; the
    Sun's row carries 'Umar's rule attributed to him, the Moon's Abu
    Bakr's qualitative sentence. Returns a list of dicts (empty when there
    is no house-master); each carries 'planet', 'aspect' (None in
    aversion, luminaries only), 'effect' ('adds', 'subtracts', 'nothing',
    'not decided', 'not specified', 'unresolved'), 'lesser_years',
    'grades' (count and unit at each of JN_CH4_GRADES, or None where the
    chapter grades nothing), 'sentence', 'reading', 'witnesses', and for
    Mercury 'literal' (the sentence read literally: 'adds' / 'subtracts' /
    None), for the Sun 'umar' ('adds' / 'subtracts' / None). No sum."""
    if not house_master or house_master not in planetary_data:
        return []
    hm_lon = planetary_data[house_master]['longitude']
    rows = []
    for planet in ('Saturn', 'Jupiter', 'Mars', 'Venus', 'Mercury'):
        if planet == house_master or planet not in planetary_data:
            continue
        look = _prosperity_looks(planetary_data[planet]['longitude'], hm_lon)
        if look is None:
            continue
        aspect = 'joined' if look == 'with' else look
        lesser = JN_YEARS_TABLE[planet][2]   # Ch. 4's own lesser column, as jn_years_fallback reads it
        reading, witnesses, literal = None, "-", None
        if planet in ('Jupiter', 'Venus'):
            if look in ('with', 'trine', 'sextile'):
                effect, sentence = 'adds', JN_CH4_SENTENCES['fortune']
                reading = (f"{lesser:g} years; if middling, {lesser:g} months; if more unsound, {lesser:g} days or "
                           f"{lesser:g} hours -- strength grade not determined here")
                witnesses = JN_CH4_WITNESSES['fortune adds']
            else:
                effect, sentence = 'nothing', JN_CH4_SENTENCES['nothing']
                reading, witnesses = JN_CH4_ZERO, JN_CH4_WITNESSES['fortune square']
        elif planet in SAHL_INFORTUNES:
            if look in ('with', 'square', 'opposition'):
                effect, sentence = 'subtracts', JN_CH4_SENTENCES['infortune']
            else:
                effect, sentence = 'nothing', JN_CH4_SENTENCES['nothing']
                reading, witnesses = JN_CH4_ZERO, JN_CH4_WITNESSES['infortune trine']
        else:                                                   # Mercury, fn 28
            company = {p: _prosperity_looks(planetary_data[planet]['longitude'], planetary_data[p]['longitude'])
                       for p in ('Jupiter', 'Venus', 'Saturn', 'Mars') if p != house_master and p in planetary_data}
            good = [p for p in ('Jupiter', 'Venus') if company.get(p)]
            bad = [p for p in ('Saturn', 'Mars') if company.get(p)]
            sentence, witnesses = JN_CH4_SENTENCES['mercury'], JN_CH4_WITNESSES['mercury']
            prior = {r['planet']: r for r in rows}
            contributions = [(p, _jn_positive_contribution(prior.get(p))) for p in good]
            if good and bad:
                effect = _jn_mercury_indeterminate(
                    'unresolved', 'mixed benefic and malefic company; the source supplies no combined result',
                    tuple((f"{p} company", prior.get(p, {}).get('effect', 'no Ch. 4 row')) for p in good + bad),
                )
                reading = effect
            elif good:
                gate = mercury_jn_addition_gate(look in ('sextile', 'trine'), [value for _, value in contributions])
                literal = 'adds'
                if gate is True:
                    effect = 'adds'
                    reading = (f"Dykes fn 28 (conjectural interpretation): with or aspecting {', '.join(good)}, himself {look} to the "
                               f"house-master; {', '.join(p for p, value in contributions if value is True)} adds under Ch. 4 "
                               f"-- adds {lesser:g} years")
                elif isinstance(gate, UnresolvedResult):
                    effect = _jn_mercury_indeterminate(
                        'unresolved', 'the only qualifying benefic companion has an unresolved Ch. 4 contribution',
                        tuple((p, value) for p, value in contributions),
                    )
                    reading = effect
                else:
                    effect = _jn_mercury_indeterminate(
                        'not decided',
                        ('Mercury lacks the required sextile or trine to the house-master'
                         if look not in ('sextile', 'trine')
                         else 'no benefic companion actually adds under JN Ch. 4'),
                        tuple((p, value) for p, value in contributions),
                    )
                    reading = effect
            elif bad and look in ('square', 'opposition'):
                effect, literal = 'subtracts', 'subtracts'
                reading = (f"Dykes fn 28 (conjectural interpretation): with or aspecting {', '.join(bad)}, himself {look} to the "
                           f"house-master -- subtracts {lesser:g} years")
            elif not good and not bad:
                effect = _jn_mercury_indeterminate(
                    'unavailable', 'no benefic or malefic company supplies a case under fn 28')
                reading = effect
            else:
                literal = 'subtracts'
                effect = _jn_mercury_indeterminate(
                    'not decided', 'Mercury and the malefic company do not form fn 28\'s stated pairing')
                reading = effect
        grades = None
        if effect == 'adds' and planet != 'Mercury':
            grades = tuple((text, lesser, unit) for text, unit in JN_CH4_GRADES)
        row = {'planet': planet, 'aspect': aspect, 'effect': effect, 'lesser_years': lesser, 'grades': grades,
               'sentence': sentence, 'reading': reading, 'witnesses': witnesses}
        if planet == 'Mercury':
            row['literal'] = literal
        rows.append(row)
    # The luminaries, out of the loop: Ch. 4 names them in its table as
    # house-master and gives them no modifier. The Sun's row is 'Umar's,
    # attributed to him in the cell; the Moon's is Abu Bakr's, qualitative.
    for planet in ('Sun', 'Moon'):
        if planet == house_master or planet not in planetary_data:
            continue
        look = _prosperity_looks(planetary_data[planet]['longitude'], hm_lon)
        aspect = None if look is None else 'joined' if look == 'with' else look
        row = {'planet': planet, 'aspect': aspect, 'effect': 'not specified', 'lesser_years': JN_YEARS_TABLE[planet][2],
               'grades': None, 'sentence': JN_CH4_NO_LUMINARY, 'witnesses': JN_CH4_WITNESSES[planet.lower()]}
        if planet == 'Sun':
            n = row['lesser_years']
            if look in ('with', 'square', 'opposition'):
                row['umar'] = 'subtracts'
                row['reading'] = (f"'Umar: subtracts {n:g} years ({aspect}); with reception, months or days (no unit "
                                  f"chosen) -- reception not tested here")
            elif look in ('trine', 'sextile'):
                row['umar'] = 'adds'
                row['reading'] = f"'Umar: adds {n:g} years ({aspect})"
            else:
                row['umar'] = None
                row['reading'] = "'Umar: none of his cases applies (in aversion)"
        else:
            row['reading'] = "not specified"
        rows.append(row)
    return rows


def jn_years_additions_rows(house_master, planetary_data):
    """The Ch. 4 rows as the Releaser tab prints them (evaluate_jn_years_additions in words)."""
    out = []
    for r in evaluate_jn_years_additions(house_master, planetary_data):
        n = r['lesser_years']
        if r['planet'] in ('Sun', 'Moon'):
            effect = JN_CH4_NO_LUMINARY
        elif r['planet'] == 'Mercury':
            if isinstance(r['effect'], UnresolvedResult):
                effect = r['effect']
            else:
                effect = JN_CH4_FN28_LABEL + " -- " + {
                    'adds': f"adds his lesser years ({n:g})",
                    'subtracts': f"subtracts his lesser years ({n:g})",
                }[r['effect']]
        else:
            effect = {'adds': f"adds its lesser years ({n:g})", 'subtracts': f"subtracts its lesser years ({n:g})",
                      'nothing': "adds or subtracts nothing"}[r['effect']]
        if r['grades']:
            cells = [f"{n:g} {unit}" for _, _, unit in r['grades']]
            grade = "not determined: no text defines \"middling in strength\" or \"more unsound\""
        elif not isinstance(r['effect'], UnresolvedResult) and r['effect'] in ('adds', 'subtracts'):
            cells = [f"{n:g} years", "-", "-"]
            grade = "the chapter grades the fortune's addition only"
        else:
            cells, grade = ["-", "-", "-"], "-"
        looks = f"{r['aspect']} (whole sign)" if r['aspect'] else "in aversion (whole sign)"
        out.append({'Planet': r['planet'], 'Looks at the house-master': looks, 'Ch. 4': effect,
                    'Its own lesser years': cells[0], 'If middling in strength': cells[1], 'If more unsound': cells[2],
                    'Grade': grade,
                    'Reading': r['reading'] if r['reading'] is not None else "the chapter's sentence",
                    'Witnesses': r['witnesses']})
    return out

# --- Spear-bearing: two stated definitions, DISPLAY ONLY (owner, 2026-09-11, decision sheet row 11 / DEC-D-18) ---
# A. Right-sidedness, Sahl, On Nativities 2.5, 2-3 ("what is called the
#    'spear-bearing' of the planets", the chapter head): 2 "If you found one
#    of the two planets in square or sextile to its companion, and they were
#    both in their exaltations or their houses, or one of them was in its
#    exaltation and the other in its house, or one of its shares, and each one
#    of the two was casting rays upon its companion, then that is a strong
#    right-sidedness." 3 "And if they were not in their houses nor exaltations,
#    but they were both of the sect of the day or the sect of the night, then
#    that is also called right-sidedness (though it is below [the first
#    version])." 1: "especially if the diurnal planets were right-siding by
#    day, and the nocturnal ones by night".
# B. The honor-guard, Ptolemy in Sahl, On Nativities 10.2.1, 10-15: 10 "the
#    planets formed an honor-guard for them (and that is if the planets were
#    eastern from the Sun and western from the Moon)"; 13 "no honor-guard ...
#    and they do not look at them"; 14 "the luminaries were not in the stakes,
#    and there was an honor-guard (and it is spear-bearing) in the stakes";
#    15 neither in the stakes.
# Neither enters a score; each table names its source. Readings, the engine's:
# in A, "casting rays upon its companion" = the pair is Connected under the
# Configurations page's connection rule; "one of its shares" = a share
# (triplicity, bound or face) held by the partner of a planet in its house or
# exaltation; "of the sect of the day or ... night" = both planets of one sect
# (Mercury has none of his own here and is not counted). In B, "eastern from
# the Sun" = rising before him (the solar phase's side), "western from the
# Moon" = rising after her (the shorter arc); "in the stakes" = the whole-sign
# places 1, 4, 7, 10 (a topic of rank, the canon's places); "look at" = the
# whole-sign aspect. The course material named as a possible arbiter of the
# definition has not been opened; both stated forms are shown.
@_connection_aware
def evaluate_right_sidedness(planetary_data, sect):
    """On Nativities 2.5, 2-3, per pair in whole-sign sextile or square. Display only."""
    ess = evaluate_essential_dignities(planetary_data, sect)
    pairs = {frozenset((r['light_name'], r['heavy_name'])): r for r in _pairwise_configurations(planetary_data)
             if r['aspect_name'] != 'Aversion'}
    names = [p for p in PN4_SEVEN if p in planetary_data]
    rows = []
    for i, a in enumerate(names):
        for b in names[i + 1:]:
            apart = (int(planetary_data[b]['longitude'] % 360 // 30) - int(planetary_data[a]['longitude'] % 360 // 30)) % 12
            apart = min(apart, 12 - apart)
            if apart not in (2, 3):
                continue
            aspect = 'sextile' if apart == 2 else 'square'
            r = pairs.get(frozenset((a, b)))
            rays = bool(r) and _is_connected(r)
            def dign(p):
                e = ess.get(p, {})
                strong = [k for k in ('Domicile', 'Exalt') if e.get(k)]
                share = [k for k in ('Triplicity', 'Term', 'Face') if e.get(k)]
                return strong, share
            sa, sha = dign(a)
            sb, shb = dign(b)
            strong = bool(sa) and bool(sb) or (bool(sa) and bool(shb)) or (bool(sb) and bool(sha))
            same_sect = ((a in DIURNAL_SECT_PLANETS and b in DIURNAL_SECT_PLANETS)
                         or (a in NOCTURNAL_SECT_PLANETS and b in NOCTURNAL_SECT_PLANETS))
            in_sect = (sect == 'Diurnal' and a in DIURNAL_SECT_PLANETS and b in DIURNAL_SECT_PLANETS) or                       (sect != 'Diurnal' and a in NOCTURNAL_SECT_PLANETS and b in NOCTURNAL_SECT_PLANETS)
            if strong and rays:
                grade = 'strong right-sidedness (2.5, 2)'
            elif same_sect and not (sa or sb):
                grade = 'right-sidedness by sect, "below the first" (2.5, 3)'
            else:
                grade = '-'
            rows.append({'Pair': f"{a} and {b}", 'Aspect (whole sign)': aspect,
                         'Casting rays on each other': "yes (Connected under the page's rule)" if rays else 'no',
                         'Dignities': f"{a}: {', '.join(sa + sha) or 'none'}; {b}: {', '.join(sb + shb) or 'none'}",
                         'One sect': 'yes' if same_sect else 'no',
                         'In the sect of the chart (2.5, 1)': 'yes' if in_sect else 'no',
                         'Grade': grade, 'Source': 'Sahl, On Nativities 2.5, 1-3'})
    return rows

def evaluate_honor_guard(planetary_data, ascendant_lon):
    """On Nativities 10.2.1, 10-15 (Ptolemy in Sahl): per planet, eastern
    from the Sun and western from the Moon, in the stakes, looking at the
    luminaries; the luminaries' signs and places. Display only; which of
    10-15 the configuration matches is stated as facts, not pronounced."""
    sun, moon = planetary_data['Sun']['longitude'] % 360.0, planetary_data['Moon']['longitude'] % 360.0
    rows = []
    for lum in ('Sun', 'Moon'):
        lon = planetary_data[lum]['longitude'] % 360.0
        sign = get_zodiac_sign(lon)
        place = get_wsh_house(lon, ascendant_lon)
        rows.append({'Planet': lum, 'Role': 'luminary', 'Sign': f"{sign} ({'male' if sign in MASCULINE_SIGNS else 'female'})",
                     'Whole-sign place': place, 'In a stake': 'yes' if place in (1, 4, 7, 10) else 'no',
                     'Eastern from the Sun': '-', 'Western from the Moon': '-', 'Looks at the luminaries': '-',
                     'Source': '10.2.1, 10-15'})
    for p in ('Saturn', 'Jupiter', 'Mars', 'Venus', 'Mercury'):
        if p not in planetary_data:
            continue
        lon = planetary_data[p]['longitude'] % 360.0
        east_of_sun = ((lon - sun + 180.0) % 360.0) - 180.0 < 0
        west_of_moon = ((lon - moon + 180.0) % 360.0) - 180.0 > 0
        place = get_wsh_house(lon, ascendant_lon)
        looks = [l for l in ('Sun', 'Moon') if _sahl_looks(lon, planetary_data[l]['longitude'])]
        rows.append({'Planet': p, 'Role': 'honor-guard' if east_of_sun and west_of_moon else '-',
                     'Sign': get_zodiac_sign(lon), 'Whole-sign place': place,
                     'In a stake': 'yes' if place in (1, 4, 7, 10) else 'no',
                     'Eastern from the Sun': 'yes' if east_of_sun else 'no',
                     'Western from the Moon': 'yes' if west_of_moon else 'no',
                     'Looks at the luminaries': ', '.join(looks) or 'neither', 'Source': '10.2.1, 10'})
    return rows

# --- Sahl, On Nativities 2.13, 48-51: the sect light's first triplicity lord by ascensional band ---
# 48: "of the more powerful indications of good fortune is if the first lord of
# the triplicity of the glowing one [fn 190: the sect light] is in a stake or
# what follows it, and that is the 15 degrees which follows it, by degrees of
# ascensions: for if it was like that, it indicates praise and good fortune
# (and what is less [than that] in degrees is preferable). 49 Now if it was in
# the second 15 degrees, it indicates his good fortune is below the first
# [type]. 50 And if it was in the third 15 degrees, it indicates [what is in]
# the middle of assets. 51 And what is after that in degrees, up to the next
# stake, is of the nativities of the poor." Read in the corpus 2026-09-11 (the
# owner's addendum required it) before this was built. fn 189: Carmen I.28,
# 1-6; the printed Carmen (p. 108, the owner's photograph) has the same four
# parts, its third 15 "middling in assets", then "needy" to the next stake, and
# says "the lord" where Sahl says "the first lord" -- cite 2.13 for "first".
# 2.3, 4 with fns 82-83 is the same rule misreported from the sign's start.
#
# A DIFFERENT RULE from Aphorism #45 (FINAL-A4, CLOSED; owner, sheet row 5):
# the aphorism speaks of EVERY planet and of being "in the situation of one
# who is in the stake" (90-92, with a zodiacal example, 10 to 25 Aries); fn 57
# calls it "misstated" and measured in ascensions. The aphorism stays as
# printed and is NOT applied; 2.13 is built under its own name for one planet;
# the per-planet table is the ENGINE'S GENERALISATION of 2.13 and is labelled
# so (DIS-12 addendum). The 2026-09-07 policy "implement the editor's
# ascensional correction of Aphorism #45" is retired by the same ruling.
#
# CONVENTIONS, the engine's, none a sentence of any text: the interval is the
# one between consecutive ACTUAL stakes in zodiacal order (Asc, IC, Dsc, MC),
# measured from the stake the planet FOLLOWS (zodiacally after it -- 2.13
# "what follows it"; Intro 2, 33 "rising up to them"; Aph. 45's 10 to 25
# Aries); oblique ascension at the horizon (the Descendant by the oblique
# descension, i.e. the OA of the opposite degree) and RIGHT ascension at the
# meridian (Carmen gives one rising instruction for all four; the split is a
# declared engine convention); the planet's ECLIPTIC degree, latitude ignored;
# bands end-inclusive, 0-15, 15-30, 30-45, the remainder to the next stake; a
# planet on the next stake re-anchors at zero; the five-degree allowance of
# Aphorism #44 lies on the OTHER side of the stake and is NOT inherited here
# (51's "up to the next stake" stands). Refused where the ascension has no
# inverse (D-23), as every ascensional measure here is.
SAHL_2_13_BANDS = (
    (15.0, 'first 15 degrees of ascension', 'praise and good fortune (48; "what is less [than that] in degrees is preferable")'),
    (30.0, 'second 15 degrees', 'good fortune below the first type (49)'),
    (45.0, 'third 15 degrees', 'the middle of assets (50; Carmen I.28, 5 "middling in assets and good fortune")'),
    (None, 'the remainder, up to the next stake', 'of the nativities of the poor (51; Carmen I.28, 6 "needy [and] wretches")'),
)

def evaluate_ascensional_bands(planetary_data, ascendant_lon, mc_lon, obliquity, geo_lat, sect):
    """Every planet's ascensional distance past the stake it follows, in
    2.13's three bands and remainder -- the ENGINE'S GENERALISATION, display
    only, an ordinal preference and no score -- with 2.13, 48-51's own
    judgment applied to the one planet the text names, the sect light's
    first triplicity lord; and Aphorism #45 AS PRINTED (15 zodiacal degrees)
    beside it, not applied. Returns {'rows', 'judged', 'first_lord',
    'refused'}; 'refused' carries the reason at the poles."""
    if not _ascensional_method_applies(obliquity, geo_lat):
        return {'rows': [], 'judged': None, 'first_lord': None,
                'refused': f"refused at latitude {geo_lat:.1f}: the ascensions have no unique inverse here"}
    asc, mc = ascendant_lon % 360.0, mc_lon % 360.0
    stakes = [('Ascendant', asc), ('fourth (IC)', (mc + 180.0) % 360.0), ('setting degree (Dsc)', (asc + 180.0) % 360.0), ('Midheaven', mc)]

    def measure(stake_name, stake_lon, lon):
        """The directed distance from the stake to the degree, in the stake's ascension."""
        if stake_name == 'Ascendant':
            return (_oblique_ascension(lon, obliquity, geo_lat) - _oblique_ascension(stake_lon, obliquity, geo_lat)) % 360.0
        if stake_name.startswith('setting'):
            return (_oblique_ascension((lon + 180.0) % 360.0, obliquity, geo_lat)
                    - _oblique_ascension((stake_lon + 180.0) % 360.0, obliquity, geo_lat)) % 360.0
        return (_ra_decl(lon, obliquity)[0] - _ra_decl(stake_lon, obliquity)[0]) % 360.0

    light = 'Sun' if sect == 'Diurnal' else 'Moon'
    light_sign = get_zodiac_sign(planetary_data[light]['longitude'])
    first_lord = _triplicity_lords_in_sect_order(light_sign, sect, table='Sahl')[0]
    rows, judged = [], None
    for planet in PN4_SEVEN:
        if planet not in planetary_data:
            continue
        lon = planetary_data[planet]['longitude'] % 360.0
        # the stake the planet follows: the nearest one zodiacally behind it
        name, s_lon = min(stakes, key=lambda st: (lon - st[1]) % 360.0)
        nxt_name, nxt_lon = min(((n, l) for n, l in stakes if n != name), key=lambda st: (st[1] - lon) % 360.0)
        x = measure(name, s_lon, lon)
        to_next = measure(name, s_lon, nxt_lon)
        if x > to_next:                      # numerical guard: the planet cannot be past the next stake
            x = to_next
        band = next((label for limit, label, _j in SAHL_2_13_BANDS if limit is not None and x <= limit), SAHL_2_13_BANDS[-1][1])
        judgment = next((j for limit, label, j in SAHL_2_13_BANDS if label == band))
        printed = (lon - s_lon) % 360.0 <= 15.0
        row = {'Planet': planet, 'Follows the stake': f"{name} ({get_degree_string(s_lon)})",
               'Ascensional distance': f"{x:.2f} deg ({'oblique' if name in ('Ascendant',) or name.startswith('setting') else 'right'} ascension)",
               'To the next stake': f"{to_next:.2f} deg ({nxt_name})",
               'App grade (generalised from 2.13, 48-51)': band,
               'Aphorism #45 as printed (15 zodiacal degrees; not applied)': 'within' if printed else 'beyond',
               '2.13, 48-51 (the sect light\'s first triplicity lord only)': judgment if planet == first_lord else '-',
               'Triplicity table': TRIPLICITY_TABLE_SOURCES['Sahl']}
        rows.append(row)
        if planet == first_lord:
            judged = {'planet': planet, 'band': band, 'judgment': judgment, 'distance': x, 'stake': name}
    return {'rows': rows, 'judged': judged, 'first_lord': first_lord, 'refused': None}

def sahl_releaser(planetary_data, ascendant_lon, cusps, sect, lot_of_fortune, meeting_lon, fullness_lon):
    """Nawbakht's selection (On Nativities 1.15, 6-16) with 1.16's
    exception and 1.20, 2-5's ranking of the house-master. Returns
    {'releaser', 'longitude', 'house_master', 'ranking', 'candidates',
    'verdict', 'sect'}; 'releaser' and 'house_master' are None when
    nothing qualifies ("the native does not have a foundation for his
    lifespan", 1.15, 16)."""
    day = sect == 'Diurnal'
    if day:
        order = [('the Sun', planetary_data['Sun']['longitude'], 'Sun'),
                 ('the meeting (the last New Moon)', meeting_lon, None)]
        places = SAHL_RELEASER_DAY_PLACES
    else:
        order = [('the Moon', planetary_data['Moon']['longitude'], 'Moon'),
                 ('the fullness (the last Full Moon)', fullness_lon, None),
                 ('the Lot of Fortune', lot_of_fortune, None)]
        places = SAHL_RELEASER_NIGHT_PLACES
    cands = [_sahl_examine_candidate(label, lon, planetary_data, cusps, sect, places, self_planet,
                                     unit='place' if label == 'the Lot of Fortune' else 'division')
             for label, lon, self_planet in order]
    cands.append(_sahl_ascendant_candidate(planetary_data, ascendant_lon, cusps, sect))
    chosen = next((c for c in cands if c['fit']), None)
    ranking = _sahl_rank_house_master(chosen, planetary_data, cusps) if chosen else []
    house_master = ranking[0]['Planet'] if ranking else None
    rows = []
    for i, c in enumerate(cands):
        consulted = chosen is None or i <= cands.index(chosen)
        rows.append({'Candidate': c['candidate'], 'Degree': get_degree_string(c['longitude']),
                     'House (division, 5 deg at the stakes; the Lot by whole-sign place)': c['place'],
                     'Lords of its degree': '; '.join(f"{r} {p}: {a}" for r, p, a in c['lords']),
                     'Verdict': ('The releaser -- ' if c is chosen else '' if consulted else 'not consulted, an earlier candidate qualified -- ')
                                + c['why'],
                     'Source': ('1.15, 6-9' if day else '1.15, 10-14') if c['candidate'] != 'the Ascendant' else '1.15, 15-16'})
    if chosen is None:
        verdict = ('No releaser by Nawbakht\'s rule (1.15, 16: "the native does not have a foundation for his '
                   'lifespan"). For one who does not live, al-Andarzaghar directs the strongest of the four anyway '
                   '(1.32, 11), "the first of them is the Ascendant, then the Moon" (13): the Ascendant\'s distribution '
                   '(the tab "from the Ascendant") is that stand-in, and the Moon is directed after it, below; '
                   'whichever first connects with an infortune kills (12), if no fortune looks (14).')
    elif house_master is None:
        verdict = f"The releaser is {chosen['candidate']}; no house-master, nothing looks at it."
    else:
        verdict = (f"The releaser is {chosen['candidate']} at {get_degree_string(chosen['longitude'])}; the house-master is "
                   f"{house_master}" + (' (1.16: the luminary is both)' if chosen['both_at_once'] else
                                        f", {ranking[0]['Rank']}") + '.')
    return {'releaser': chosen['candidate'] if chosen else None,
            'longitude': chosen['longitude'] if chosen else None,
            'house_master': house_master, 'ranking': ranking, 'candidates': rows,
            'chosen': chosen, 'verdict': verdict, 'sect': sect}

def sahl_releaser_distribution(planetary_data, releaser_lon, obliquity, geo_lat,
                               span_years=PN4_DISTRIBUTION_SPAN_YEARS, significator=None):
    """1.15, 22: "if you did find a releaser, then direct from it and from
    the degree in which it was"; 1.18, 20-21: "the lord of the bound of
    that releaser is the distributor of time ... direct the degrees from
    the degree in which the releaser is, by degrees of the ascensions of
    the signs in that city, for each degree ... a year". The same
    bound-by-bound direction as the Ascendant's, by the oblique ascension
    of the birth latitude; refuses at the poles as that does (D-23)."""
    if releaser_lon is None or not _ascensional_method_applies(obliquity, geo_lat):
        return None
    return _pn4_distribute(planetary_data, releaser_lon,
                           lambda lon: _oblique_ascension(lon, obliquity, geo_lat), span_years, 'releaser', significator=significator)

SAHL_INFORTUNES = ('Saturn', 'Mars')

def sahl_house_master_direction(planetary_data, house_master, obliquity, geo_lat,
                                span_years=PN4_DISTRIBUTION_SPAN_YEARS, origin_jd=None,
                                start_lon=None, target_planets=None, sun_target=True,
                                target_policy='house-master'):
    """Masha'allah, On Nativities 1.23, 2: "look at the position of the
    governor [fn 181: the house-master] ... then direct it to the
    conjunction of the infortunes and the degree of burning, and its
    opposition and its square, a year for every degree of ascensions."
    The house-master's natal degree is directed forward by the oblique
    ascension of the birth latitude -- "the ascensions in that city",
    1.15, 17; 1.16, 4; 1.18, 21 -- to the bodies, squares and oppositions
    of Saturn and Mars and to the Sun's degree, read as "the degree of
    burning". One row per target within the span, in age order. Refuses
    at the poles (D-23). 1.23, 3-11's check of that year's revolution is
    sahl_house_master_in_revolution; 4.12, 6's converse direction of a
    retrograde planet is not applied."""
    # `start_lon` directs any degree by the same operation (1.23, 13-14: the lord
    # of the Ascendant, the Ascendant's degree; 4.20, 32: the Lot of the father,
    # the Sun or Saturn); `target_planets` replaces Saturn and Mars (4.20, 31's
    # harmers); `sun_target` keeps or drops "the degree of burning".
    if start_lon is None and (house_master not in planetary_data):
        return None
    if not _ascensional_method_applies(obliquity, geo_lat):
        return None
    start = (start_lon if start_lon is not None else planetary_data[house_master]['longitude']) % 360.0
    oa0 = _oblique_ascension(start, obliquity, geo_lat)
    targets = []
    for p in (target_planets if target_planets is not None else SAHL_INFORTUNES):
        if p not in planetary_data:
            continue
        own = p == house_master
        if own and (target_policy != 'house-master' or p not in SAHL_INFORTUNES):
            continue
        lon = planetary_data[p]['longitude'] % 360.0
        if not own:
            targets.append((f"{p}'s body", lon, '1.23, 2 "the conjunction of the infortunes"'))
        targets.append((f"{p}'s opposition", (lon + 180.0) % 360.0, '1.23, 2 "its opposition"'))
        targets.append((f"{p}'s square (right)", (lon - 90.0) % 360.0, '1.23, 2 "its square"'))
        targets.append((f"{p}'s square (left)", (lon + 90.0) % 360.0, '1.23, 2 "its square"'))
    if sun_target and target_policy != 'stand-in' and 'Sun' in planetary_data and house_master != 'Sun':
        targets.append(("the Sun's degree (burning)", planetary_data['Sun']['longitude'] % 360.0,
                        '1.23, 2 "the degree of burning" (reading: the Sun\'s natal degree)'))
    rows = []
    for label, lon, cite in targets:
        arc = (_oblique_ascension(lon, obliquity, geo_lat) - oa0) % 360.0
        if arc <= 1e-9 or arc > span_years:
            continue
        row = {'Target': label, 'Degree': get_degree_string(lon), 'Arc (years)': f"{arc:.2f}",
               'In the year of age': int(arc), 'Source': ("4.20, 31-32: the father's harmers and direction; Mercury's harm is conditional in 31, not certified here; 4.12, 6's converse direction is not applied" if target_policy == 'father' else cite), '_arc': arc}
        if origin_jd is not None:
            row['Date'] = f"{pn4_datetime_from_jd(origin_jd + arc * PN4_DIRECTION_YEAR_DAYS):%Y-%m-%d}"
        rows.append(row)
    rows.sort(key=lambda r: r['_arc'])
    for r in rows:
        r.pop('_arc')
    return rows

def sahl_house_master_in_revolution(house_master, chart_data, sr):
    """1.23, 3-4: "calculate for the revolution of that year ... for if
    your calculation of this [by direction] and the revolution both
    indicate burning, and then the governor is burned at the revolution,
    the native will be destroyed; and if it is not burned at the
    revolution but it is burned in one of the stakes of the Ascendant of
    the year, it indicates that [as well]; and it is worse for that in the
    Ascendant [itself]." The facts for the house-master in the revolution:
    where it stands, its solar phase, its house from the revolution's
    Ascendant, and which infortune shares its sign. Judgment is not
    built; 1.23, 5-11's further witnesses are the II.3 examination."""
    rev = sr['planetary_data']
    if house_master not in rev:
        return []
    lon = rev[house_master]['longitude']
    phase, side, elong = solar_phase(house_master, lon, rev['Sun']['longitude'], rev[house_master].get('speed_in_lon'), source='Sahl')
    house = get_wsh_house(lon, sr['ascendant'])
    with_infortune = [p for p in SAHL_INFORTUNES if p != house_master and p in rev
                      and get_zodiac_sign(rev[p]['longitude']) == get_zodiac_sign(lon)]
    natal_lon = chart_data['planetary_data'][house_master]['longitude']
    return [
        {'Fact': f"{house_master} in the revolution", 'Reads': f"{get_degree_string(lon)} (natal {get_degree_string(natal_lon)})", 'Source': '1.23, 3'},
        {'Fact': 'Burned at the revolution', 'Reads': (f"{phase}, {side}, {elong:.1f} from the Sun" if phase else f"no: {elong:.1f} from the Sun, {side}"), 'Source': '1.23, 4'},
        {'Fact': "In a stake of the Ascendant of the year", 'Reads': f"house {house} from the revolution's Ascendant" + (' -- a stake' if house in (1, 4, 7, 10) else '') + (' -- the Ascendant itself, "worse"' if house == 1 else ''), 'Source': '1.23, 4'},
        {'Fact': 'With an infortune in its sign', 'Reads': ', '.join(with_infortune) or 'none', 'Source': '1.23, 2-4'},
    ]

@_connection_aware
def sahl_short_life_testimonies(chart_data, lot_fortune_lon):
    """Sahl, On Nativities 1.18, 1-10 (Masha'allah, fn 125): the testimonies
    of a short upbringing, counted, and 8-10's sentence for the count --
    shown beside the releaser and NOT disqualifying it (order REL-5-3).
    Counted (1-4): 1 the Moon's connection "with a retrograde planet not
    receiving her"; 2 "the handing over of her management to the lord of
    the eighth, or the eighth [from] her house"; 3 "to a planet under the
    earth while she is above the earth by day"; 4 "the retrogradation of
    the lord of the Lot of Fortune, and the presence of an infortune in a
    stake ... if it was inimical to the Ascendant or did not have a
    testimony in the Ascendant". Not counted, "equivalent" (5-7; fn 126,
    129): 5 the lord of the Ascendant connecting with the lord of the
    eighth; 6 the lord of the Ascendant retrograde, "unless it was Mercury"
    (fn 127: inverts); 7 its connection with a <retrograde> planet "or one
    in its first slowness ... (unless it receives it)". Readings: the
    Moon's "connection" and "handing over" = her first perfection before
    she leaves her sign (the natal chart followed forward); "receiving" per
    evaluate_reception; "under the earth" by altitude where the horizon is
    known; "in a stake" the whole-sign places 1, 4, 7, 10; "inimical to the
    Ascendant" is not read, "no testimony in the Ascendant" = no dignity of
    its own at the Ascendant's degree; 4's two clauses are read as one
    testimony (fn 130 makes 8-10 count 1, 2, 3)."""
    p = chart_data['planetary_data']
    asc = chart_data['ascendant']
    sect = chart_data['sect']
    asc_sign = get_zodiac_sign(asc)
    eighth = SIGN_ORDER[(SIGN_ORDER.index(asc_sign) + 7) % 12]
    moon_sign = get_zodiac_sign(p['Moon']['longitude'])
    eighth_from_moon = SIGN_ORDER[(SIGN_ORDER.index(moon_sign) + 7) % 12]
    lord_asc = SIGN_TO_DOMICILE.get(asc_sign)
    lord_8 = SIGN_TO_DOMICILE.get(eighth)
    rows = []
    # the Moon's first perfection in her sign
    try:
        conns = _pn4_luminary_connections(p, chart_data['julian_day'], 'Moon', 6, 0.25, applying_only=False)['connections']
    except Exception:
        conns = []
    first = conns[0] if conns else None
    received = set()
    with doctrine(SAHL):
        for r in evaluate_reception(p, sect):
            if r.get('Received') == 'Moon' and r.get('Receiver'):
                received.add(r['Receiver'])
    t1 = bool(first) and p.get(first['planet'], {}).get('speed_in_lon', 1.0) < 0 and first['planet'] not in received
    rows.append({'Testimony': '1. The Moon connects with a retrograde planet not receiving her', 'Met': 'yes' if t1 else 'no',
                 'Fact': (f"her first perfection in {moon_sign}: {first['planet']} by {first['aspect']} on day {first['day']:.1f}, "
                          f"{'retrograde' if p.get(first['planet'], {}).get('speed_in_lon', 1.0) < 0 else 'direct'}, "
                          f"{'receiving her' if first['planet'] in received else 'not receiving her'}") if first else 'no perfection before she leaves her sign',
                 'Counted': 'yes', 'Source': '1.18, 1'})
    t2 = bool(first) and first['planet'] in (lord_8, SIGN_TO_DOMICILE.get(eighth_from_moon))
    rows.append({'Testimony': '2. She hands her management to the lord of the eighth, or of the eighth from her house',
                 'Met': 'yes' if t2 else 'no',
                 'Fact': f"lord of the eighth ({eighth}): {lord_8}; of the eighth from the Moon ({eighth_from_moon}): "
                         f"{SIGN_TO_DOMICILE.get(eighth_from_moon)}; her first perfection: {first['planet'] if first else 'none'}",
                 'Counted': 'yes', 'Source': '1.18, 2'})
    def above(planet):
        row = p[planet]
        if chart_data.get('armc') is not None and chart_data.get('geo_lat') is not None:
            return _sin_altitude(row['longitude'], row.get('latitude', 0.0), row.get('distance', 1.0),
                                 chart_data['obliquity'], chart_data['armc'], chart_data['geo_lat']) > 0
        return (row['longitude'] - asc) % 360.0 > 180.0
    t3 = bool(first) and sect == 'Diurnal' and above('Moon') and not above(first['planet'])
    rows.append({'Testimony': '3. By day, she above the earth hands over to a planet under the earth', 'Met': 'yes' if t3 else 'no',
                 'Fact': (f"{'day' if sect == 'Diurnal' else 'night'} chart; the Moon {'above' if above('Moon') else 'below'} the earth; "
                          + (f"{first['planet']} {'above' if above(first['planet']) else 'below'}" if first else 'no perfection')),
                 'Counted': 'yes', 'Source': '1.18, 3'})
    lord_lot = SIGN_TO_DOMICILE.get(get_zodiac_sign(lot_fortune_lon))
    lot_retro = lord_lot in p and p[lord_lot].get('speed_in_lon', 1.0) < 0
    asc_r = get_essential_rulers(asc)
    asc_dign = {asc_r['domicile'], asc_r['exaltation'], asc_r['triplicity_day'] if sect == 'Diurnal' else asc_r['triplicity_night'], asc_r['term'], asc_r['face']}
    bad = [q for q in SAHL_INFORTUNES if q in p and get_wsh_house(p[q]['longitude'], asc) in (1, 4, 7, 10) and q not in asc_dign]
    t4 = lot_retro and bool(bad)
    rows.append({'Testimony': "4. The lord of the Lot of Fortune retrograde, and an infortune in a stake with no testimony in the Ascendant",
                 'Met': 'yes' if t4 else 'no',
                 'Fact': f"lord of the Lot {lord_lot}: {'retrograde' if lot_retro else 'direct'}; infortunes in a stake without a dignity at the Ascendant's degree: {', '.join(bad) or 'none'} (\"inimical to the Ascendant\" not read)",
                 'Counted': 'yes', 'Source': '1.18, 4'})
    # 5-7: equivalent, not counted
    pairs = {frozenset((r['light_name'], r['heavy_name'])): r for r in _pairwise_configurations(p) if r['aspect_name'] != 'Aversion'}
    def connected(a, b):
        r = pairs.get(frozenset((a, b)))
        with doctrine(SAHL):
            return bool(r) and _is_connected(r)
    rows.append({'Testimony': '5. The lord of the Ascendant connects with the lord of the eighth', 'Met': 'yes' if lord_asc and lord_8 and lord_asc != lord_8 and connected(lord_asc, lord_8) else 'no',
                 'Fact': f"{lord_asc} and {lord_8}" + (' (the same planet)' if lord_asc == lord_8 else ''), 'Counted': 'no -- "equivalent" (fn 126)', 'Source': '1.18, 5'})
    rows.append({'Testimony': '6. The lord of the Ascendant retrograde (unless Mercury)',
                 'Met': 'yes' if lord_asc in p and p[lord_asc].get('speed_in_lon', 1.0) < 0 and lord_asc != 'Mercury' else 'no',
                 'Fact': f"{lord_asc}: {'retrograde' if lord_asc in p and p[lord_asc].get('speed_in_lon', 1.0) < 0 else 'direct'}",
                 'Counted': 'no -- "inverts it" (fn 127)', 'Source': '1.18, 6'})
    retro_partners = [q for q in PN4_SEVEN if q in p and q != lord_asc and p[q].get('speed_in_lon', 1.0) < 0 and connected(lord_asc, q)] if lord_asc in p else []
    rows.append({'Testimony': '7. The lord of the Ascendant connects with a <retrograde> planet, unless it receives it',
                 'Met': 'yes' if retro_partners else 'no', 'Fact': (', '.join(retro_partners) or 'none') + ' (reception not tested here)',
                 'Counted': 'no -- "equivalent" (fn 129)', 'Source': '1.18, 7'})
    count = sum(1 for r in rows[:4] if r['Met'] == 'yes')
    sentence = {0: 'no testimony of a short upbringing (1.18, 1-4)',
                1: '"the releaser will be weak and not fit, except through the reception of that releaser, [which indicates] fitness" (1.18, 8)',
                2: '"if ... there were two testimonies of what I mentioned, and the native did not have a releaser, one will not know his lifespan except by revolving [his] years" (1.18, 9; fn 130)'}.get(
                    count, '"if ... there were three testimonies of what I mentioned, then he will not be brought up" (1.18, 10)')
    return {'rows': rows, 'count': count, 'sentence': sentence}

def sahl_father_lot_harmers(sect, planetary_data, lot_lon, sun_lon):
    """Sahl, On Nativities 4.20, 31: "if the nativity was by day, the
    infortunes which harm his Lot are Mars and Saturn (and Mercury, if he
    was unfortunate); and if it was by night, the infortunes which harm
    them are Mars and Mercury (if he was unfortunate)". One row per harmer
    with its whole-sign aspect to the Sun and to the Lot; Mercury's "if he
    was unfortunate" is a judgment and is shown as such, not made; 36's
    Saturn ("if Saturn looked at the degree of the Lot of the father from
    hostility, it is more harmful for some of the injuries") is a separate
    row by night, when he is not a harmer. fn 288 (Dykes: Mars the main
    malefic in both sects, Saturn barred by night because he indicates the
    father) is the editor's reading and is quoted, not applied (order
    DIS-10, 2026-09-11)."""
    day = sect == 'Diurnal'
    harmers = [('Mars', 'stated'), ('Saturn', 'stated')] if day else [('Mars', 'stated')]
    harmers.append(('Mercury', '"if he was unfortunate" -- a judgment, not made here'))
    rows = []
    for p, note in harmers:
        if p not in planetary_data:
            continue
        lon = planetary_data[p]['longitude']
        rows.append({'Harmer': p, 'Named by 31': note,
                     'Looks at the Sun': _sahl_looks(lon, sun_lon) or 'in aversion',
                     'Looks at the Lot': _sahl_looks(lon, lot_lon) or 'in aversion', 'Source': '4.20, 31'})
    if not day and 'Saturn' in planetary_data:
        a = _sahl_looks(planetary_data['Saturn']['longitude'], lot_lon)
        rows.append({'Harmer': 'Saturn', 'Named by 31': 'not a harmer by night (he indicates the father, 32); 36 applies',
                     'Looks at the Sun': _sahl_looks(planetary_data['Saturn']['longitude'], sun_lon) or 'in aversion',
                     'Looks at the Lot': (f"{a} -- \"from hostility\" (square or opposition) is 36's harm" if a in ('square', 'opposition')
                                          else (a or 'in aversion')), 'Source': '4.20, 36'})
    return rows

@_pn4_opening_aware
def sahl_turning_reaches_partner(segments, age, ascendant_lon, planetary_data):
    """Sahl, On Nativities 1.24, 4-5 with 1.23, 23: "if you came to the year
    of the turning before the departure of its light, it is preferable; and
    likewise the infortunes are worse" -- the year of the turning (the
    profection of the Ascendant, a sign a year) reaching the sign that holds
    the NATAL BODY of the partner of the Ascendant's current distribution,
    while that partner still holds the bound. Body only; the ray reading is
    not applied. Returns None when there is no current segment or partner
    (order GAP-2, 2026-09-11)."""
    seg = pn4_distribution_at_age(segments, float(age)) if segments else None
    partner = (seg or {}).get('partner')
    if not partner or partner not in planetary_data:
        return None
    year = pn4_sign_of_the_year(ascendant_lon, int(age))
    partner_sign = get_zodiac_sign(planetary_data[partner]['longitude'])
    holds = year['sign'] == partner_sign
    nature = pn4_nature(partner)
    verdict = ('"preferable" (1.23, 23)' if nature == 'fortune' else '"the infortunes are worse" (1.23, 23)'
               if nature == 'infortune' else 'neither a fortune nor an infortune: 1.23, 23 gives no verdict') if holds else '-'
    return {'partner': partner, 'partner_sign': partner_sign, 'sign_of_year': year['sign'], 'holds': holds,
            'distributor': seg['distributor'], 'verdict': verdict,
            'text': (f"The year of the turning ({year['sign']}) {'reaches' if holds else 'does not reach'} the sign of the "
                     f"partner's natal body ({partner} in {partner_sign}) while {partner} partners {seg['distributor']}"
                     + (f": {verdict}" if holds else '') + " -- Sahl, On Nativities 1.24, 4-5; 1.23, 23 (body only; the ray "
                     "reading is not applied; 1.23, 23's \"in an excellent position relative to the Ascendant\" -- 1.24, 5 the "
                     "same -- is not judged)")}

def sahl_house_master_turning(house_master, planetary_data, span_years=PN4_DISTRIBUTION_SPAN_YEARS):
    """PN IV IX.8, 30: "if the turning of the years from any of the five
    releasers (or from the indicator of the lifespan) reached their bodies,
    oppositions, or squares, then they also kill" -- the indicator of the
    lifespan (Sahl's house-master, 1.30, 35) TURNED a year a sign from its
    natal sign, which IX.8, 32 says is the only operation for it ("the
    indicator of the lifespan alone is turned in the signs, sign-by-sign,
    and is not directed degree-by-degree"). One row per year of age in
    which the turned sign holds a cutter's body or is its opposition or
    square sign; "their" is the cutters' -- Saturn and Mars, the infortunes
    the direction table already targets (IX.8, 6-19 name the cutters more
    widely; the two bodies and their rays are the ones both authors share).
    Whole-sign turning, as VI.2, 1's. PN IV's own rule, shown beside
    Sahl's direction (FINAL-A2, decision sheet row 2, 2026-09-11)."""
    if house_master not in planetary_data:
        return []
    start_idx = int(planetary_data[house_master]['longitude'] % 360.0 // 30)
    cutters = []
    for p in SAHL_INFORTUNES:
        if p in planetary_data and p != house_master:
            idx = int(planetary_data[p]['longitude'] % 360.0 // 30)
            cutters += [(idx, f"{p}'s body"), ((idx + 6) % 12, f"{p}'s opposition"),
                        ((idx + 3) % 12, f"{p}'s square (left)"), ((idx - 3) % 12, f"{p}'s square (right)")]
    rows = []
    for year in range(int(span_years)):
        sign_idx = (start_idx + year) % 12
        hits = [label for idx, label in cutters if idx == sign_idx]
        if hits:
            rows.append({'Year of age': year, 'Turned sign': SIGN_ORDER[sign_idx],
                         'Reaches': ', '.join(hits), 'Source': 'PN IV IX.8, 30'})
    return rows

def sahl_house_master_flags(house_master, planetary_data, cusps):
    """1.23, 12: "perhaps one will not be able to be guided by the
    governor ... if the governor is one of the infortunes, or it is the
    lord of the eighth"; 1.23, 53: "one should fear for the governor if it
    was retrograde or in its fall". Flags, shown; nothing redirected."""
    if not house_master or house_master not in planetary_data:
        return []
    flags = []
    eighth_sign = SIGN_ORDER[(SIGN_ORDER.index(get_zodiac_sign(cusps[0])) + 7) % 12]
    if house_master in SAHL_INFORTUNES:
        flags.append(f"{house_master} is an infortune: \"perhaps one will not be able to be guided by\" it (1.23, 12; fn 190)")
    if SIGN_TO_DOMICILE.get(eighth_sign) == house_master:
        flags.append(f"{house_master} is the lord of the eighth ({eighth_sign}) (1.23, 12)")
    row = planetary_data[house_master]
    if row.get('speed_in_lon', 1.0) < 0:
        flags.append(f"{house_master} is retrograde: \"one should fear for the governor\" (1.23, 53)")
    if get_zodiac_sign(row['longitude']) in FALLS.get(house_master, ()):
        flags.append(f"{house_master} is in its fall: \"one should fear for the governor\" (1.23, 53)")
    return flags


# --- III.7, 32-42: when a natal indication comes out ----------------------
# A planet may distribute or manage more than once in a lifetime (III.7,
# 32), and this chapter asks how often what it promised in the root
# actually manifests, and at what ages.
#
# HOW OFTEN, by the quadruplicity of the sign it holds in the ROOT:
#   fixed        -- "in [only] a single time" (III.7, 35)
#   convertible  -- "in [only] one of the times" (III.7, 39)
#   double-bodied-- "on an occasional basis" (III.7, 38)
# III.7, 36 raises this to "whenever it distributes" when the planet looks
# at the position of the distribution AND "is strong in [its] indication
# for that thing". The looking is computable; the strength is not defined
# anywhere in the chapter, so the engine reports the quadruplicity rule
# and does not silently promote a row. III.7, 37 exempts the MANAGER (the
# partner): not being an indicator in its own right, it "will produce its
# indication" whenever it manages.
#
# AT WHAT AGE -- III.7, 34 and 42 name two measures and gesture at a third:
#   (a) "the number of ascensions of the sign in which it was in the root"
#   (b) "the amount of one of its own years" (fn 190: "For example, its
#       lesser years"; III.7, 35 says "its greater, middle, or lesser")
#   (c) "the rest of the times which one employs as models" -- unquantified;
#       fn 191 guesses the fardars. Not invented here.
#
# WHAT ABU MA'SHAR ADDS, and the only part that is his: the effect is
# "strong, evident, notable" when such an age falls in a period where that
# same planet is the distributor or the manager (III.7, 42). That is what
# the "Confirmed by" column computes, against the jar bakhtar above.
#
# NOT IMPLEMENTED, deliberately, both from Dykes' fn 191:
#   * The SUM of (a) and (b), and 1/3, 1/2 and 2/3 of that sum, are
#     introduced with "IF WE FOLLOW VALENS". They are not Abu Ma'shar's
#     and no sentence of III.7 contains them. Corpus disagreement #4 over
#     Valens's year-tables is untouched and stays open.
#   * fn 191's worked figure for Taurus at 45N, 20.17 ascensional times,
#     is not reproduced: the exact computation gives 20.09, and Dykes'
#     own thirds are internally inconsistent with his 20.17 anyway
#     (he prints 2/3 = 26.75 where 20.17 gives 26.78). The engine computes
#     ascensions directly, as it does everywhere else.
#
# WHICH of the greater, middle and lesser years applies is chosen "in
# accordance with what its position in the rotation of the circle
# indicated in the root" (III.7, 35) -- a placement rule PN IV
# presupposes and never states (III.3, 1 lists the candidates; IX.8, 123 defers the years). Since 2026-09-11 the
# house-master's own years come from Sahl, On Nativities 1.20 (FINAL-A1);
# III.7's ages are a different use of the table, so ALL THREE are still
# shown here and none is chosen.

PN4_MANIFESTATION_BY_QUADRUPLICITY = {
    'fixed': ('once in the lifespan', 'III.7, 35'),
    'convertible': ('in one of the times', 'III.7, 39'),
    'double-bodied': ('on an occasional basis', 'III.7, 38'),
}

def pn4_sign_ascensions(sign, obliquity, geo_lat):
    """The ascensional times of a whole sign at the birth latitude: the
    arc of the equator that rises with it (III.7, 34, "the ascensions of
    its sign"). Returns None above the polar circle, where a sign may not
    rise at all -- the domain of D-23."""
    if not _ascensional_method_applies(obliquity, geo_lat):
        return None
    start = SIGN_ORDER.index(sign) * 30.0
    return (_oblique_ascension(start + 30.0, obliquity, geo_lat)
            - _oblique_ascension(start, obliquity, geo_lat)) % 360.0

@_pn4_opening_aware
def pn4_activation_ages(planetary_data, obliquity, geo_lat, segments=None,
                        max_age=PN4_DISTRIBUTION_SPAN_YEARS, distributions=None, ascendant_lon=None):
    """III.7, 32-42, one row per planet.

    `distributions` is a mapping {label: segments} -- the Ascendant's, the
    Midheaven's, the fourth's and the releaser's -- and each candidate age
    is checked against EVERY one of them for Abu Ma'shar's confirmation
    (III.7, 42): is this planet the distributor or the manager at that age,
    and in which distribution (order PN4R-4a-2; it had been checked against
    the Ascendant's alone). `segments` alone is the Ascendant's, kept for
    older callers. `ascendant_lon`, when given, lets III.7, 35's own
    condition be read: the "once" is for a fixed-sign distributor that is
    "NOT looking at the position of the distribution"; a fixed-sign planet
    that looks falls under 36, "whenever it distributes" if strong (order
    PN4R-4a-1). The position of the distribution is read as the
    Ascendant's sign at birth, "looking" as the whole-sign aspect, a planet
    in the sign itself counted as looking -- three readings; whether the
    position is the current bound's sign or its degree is a further
    silence, said on the page."""
    if distributions is None and segments is not None:
        distributions = {"the Ascendant's distribution": segments}
    distributions = distributions or {}
    rows = []
    for planet in PN4_SEVEN:
        row = planetary_data.get(planet)
        if not row:
            continue
        sign = get_zodiac_sign(row['longitude'])
        kind = PN4_QUADRUPLICITY[sign]
        manifests, cite = PN4_MANIFESTATION_BY_QUADRUPLICITY[kind]
        if kind == 'fixed' and ascendant_lon is not None:
            aspect = _sahl_looks(row['longitude'], ascendant_lon)
            if aspect:
                manifests, cite = (f"looks at the position of the distribution (whole-sign {aspect} to the Ascendant's sign): "
                                   f"III.7, 36 applies if strong, which the chapter does not define; III.7, 35's \"once\" is for "
                                   f"the non-looking case"), 'III.7, 35-36'
            else:
                manifests, cite = 'once in the lifespan (not looking at the position of the distribution)', 'III.7, 35'
        ascensions = pn4_sign_ascensions(sign, obliquity, geo_lat)
        years = PLANETARY_YEARS[planet]

        candidates = {'ascensions of the sign': ascensions}
        for grade in ('lesser', 'middle', 'greater'):
            candidates[f'{grade} years'] = float(years[grade])

        confirmed = []
        for label, age in candidates.items():
            if age is None or not (0.0 < age <= max_age):
                continue
            for dist_label, segs in distributions.items():
                seg = pn4_distribution_at_age(segs, age) if segs else None
                if seg and planet in (seg['distributor'], seg['partner']):
                    role = 'distributor' if seg['distributor'] == planet else 'manager'
                    confirmed.append(f"{label} ({age:.2f}, as {role}, {dist_label})")

        rows.append({
            'Planet': planet, 'Natal sign': sign, 'Quadruplicity': kind,
            'Manifests': f"{manifests} ({cite})",
            'Ascensions of the sign': '-' if ascensions is None else f"{ascensions:.2f}",
            'Lesser': f"{years['lesser']:g}", 'Middle': f"{years['middle']:g}",
            'Greater': f"{years['greater']:g}",
            'Confirmed by the distribution': '; '.join(confirmed) if confirmed else 'none',
        })
    return rows

# --- IV.1 and IV.7: the fardar -------------------------------------------

def pn4_fardar_sequence(sect):
    """IV.1, 2-4 with IV.7, 24. The order is the descending order of the
    spheres, begun from the light of the sect and wrapped: by day from the
    Sun, by night from the Moon. The Head (3 years) and the Tail (2) come
    LAST IN BOTH SECTS -- "whether the native was diurnal or nocturnal"
    (IV.7, 24) -- which is the point the later tradition got wrong.

    Seven planets sum to 70 and the Nodes carry it to 75 (IV.1, 2, 8)."""
    light = 'Sun' if sect == 'Diurnal' else 'Moon'
    i = PN4_DESCENDING_SPHERES.index(light)
    order = [PN4_DESCENDING_SPHERES[(i + k) % 7] for k in range(7)]
    seq = [(p, float(PLANETARY_YEARS[p]['fardar'])) for p in order]
    seq += [(n, float(NODE_FARDAR_YEARS[n])) for n in PN4_NODE_ORDER]
    return seq

PN4_FARDAR_CYCLE_YEARS = 75.0

def pn4_fardar_subperiods(lord, years):
    """IV.1, 5-6: each planetary fardar divides into seven equal parts,
    the lord itself first, then "the planet which is below it in the
    celestial circle" and so on down the spheres.

    IV.1, 8: the Head and the Tail have NO sub-periods -- they "stand
    alone in the management of their years ... because they do not have
    houses". Returns []."""
    if lord in NODE_FARDAR_YEARS:
        return []
    i = PN4_DESCENDING_SPHERES.index(lord)
    part = float(years) / 7.0
    return [(PN4_DESCENDING_SPHERES[(i + k) % 7], part) for k in range(7)]

# --- VI.1: the lord of the orb -------------------------------------------
# "you look at the lord of the hour in which the native was born, and
# assign it to the Ascendant and to the first year from his birth"
# (VI.1, 4); "the lord of the second hour from it to the second house
# ... and to the second year" (5), and so on, "so that the lord of the
# twelfth hour from the lord of the hour in which the native was born,
# belongs to the twelfth house ... and the lord of the thirteenth hour
# from it belongs to the Ascendant of the root and the thirteenth year"
# (VI.1, 8) -- a CONTINUOUS loop of the seven hour lords against the
# twelve-year cycle of the profection, so the pairing changes every
# twelve years. Judged "just as you judge by means of the lord of the
# year" (VI.1, 12). Indicator #5 of the year (II.1, 10).
#
# The hour lords run down the spheres, "the planet which is below it in
# the circle" (IX.7, 3-5 and Dykes' Figure 45): after the natal hour lord
# comes the next in PN4_DESCENDING_SPHERES, cyclically. VI.1, 10 names
# each by the house its number matches -- "the lord of the hour of the
# house of assets" is the SECOND hour lord from the natal one -- and
# VI.1, 18-19 use those names for six fixed positions: the Ascendant,
# Midheaven and house of hope of the root, and the sign of the year with
# the tenth and eleventh from it. Those are read here by VI.1, 10's
# naming, hour k for house k, without the twelve-year "reset" Dykes
# proposes in Intro Sect. 13 and calls "my idea".
#
# What PN IV presupposes and does not state: the planetary hours
# themselves. The sequence from the day lord at sunrise is Dykes' Figure
# 45 (Intro Sect. 13), and the engine's calculate_chronocrats follows it,
# with real sunrise and sunset and a flagged equal-hour approximation
# where the Sun is circumpolar. Dykes floats a single-cycle alternative
# (each house keeping its first hour lord for life, Intro Figure 48) on
# the thought that the loop is Abu Ma'shar's own error; VI.1, 8 states
# the loop, and the owner chose it 2026-09-10. The delineations of VI.1,
# 12-17 are not built; the seven-day grant of IX.7, 7-9 IS -- method 2 of the
# days tab, pn4_ix7_weeks_from_orb (an earlier line here said it was not; order GAP-27).

def pn4_hour_lord_from_natal(natal_hour_lord, steps):
    """The lord of the hour `steps` hours after the natal one, down the
    spheres and round again (VI.1, 5-8)."""
    if natal_hour_lord not in PN4_DESCENDING_SPHERES:
        return None
    i = PN4_DESCENDING_SPHERES.index(natal_hour_lord)
    return PN4_DESCENDING_SPHERES[(i + int(steps)) % 7]

def pn4_lord_of_the_orb(natal_hour_lord, completed_years):
    """VI.1, 4-8: the lord of the orb for the year -- the natal hour lord
    at age 0, and one hour lord further down the spheres for every
    completed year, without reset."""
    return pn4_hour_lord_from_natal(natal_hour_lord, completed_years)

def pn4_hour_lord_of_house(natal_hour_lord, house):
    """VI.1, 10: "the lord of the hour of the house of X" is the hour lord
    numbered as the house, counted from the natal hour as the first."""
    return pn4_hour_lord_from_natal(natal_hour_lord, int(house) - 1)

def pn4_named_lords_of_the_orb(natal_hour_lord, completed_years):
    """VI.1, 18-19: the six named lords of the orb -- the hour lords of
    the Ascendant, Midheaven and house of hope of the root (18), and of
    the sign of the terminal point and the tenth and eleventh from it
    (19), each by VI.1, 10's naming."""
    k = int(completed_years) % 12 + 1              # the house of the sign of the year from the natal Ascendant
    positions = (
        ('Ascendant of the root', 1, 'VI.1, 18'),
        ('Midheaven of the root', 10, 'VI.1, 18'),
        ('House of hope of the root', 11, 'VI.1, 18'),
        ('Sign of the terminal point', k, 'VI.1, 19'),
        ('Tenth from the sign of the year', (k - 1 + 9) % 12 + 1, 'VI.1, 19'),
        ('Eleventh from the sign of the year', (k - 1 + 10) % 12 + 1, 'VI.1, 19'),
    )
    age = int(completed_years)
    rows = []
    for label, house, cite in positions:
        # VI.1, 8's assignment: "the lord of the thirteenth hour from it belongs
        # to the Ascendant of the root and the thirteenth year" -- the hour lord
        # this house RECEIVED when the profection last reached it, which is a
        # different planet from VI.1, 10's first-cycle naming from age 12 on
        # (order PN4R-4e-2). '-' while the loop has not yet reached the house.
        step = age - ((age - (house - 1)) % 12)
        rows.append({'Position': label, 'House': house,
                     "By VI.1, 10's naming (first cycle): hour": house,
                     "Lord of the hour (VI.1, 10)": pn4_hour_lord_of_house(natal_hour_lord, house) or '-',
                     "By VI.1, 8's assignment (the hour lord the house received when the profection last reached it)":
                         (pn4_hour_lord_from_natal(natal_hour_lord, step) or '-') if step >= 0 and natal_hour_lord else '-',
                     'Source': cite})
    return rows

# --- VI.2, 1-26: the turning of the houses of the root ---------------------
# "every one of the seven planets, the twelve houses, and the twelve Lots,
# is turned at the revolutions of years from its own position (a year for
# every sign), and is directed from its degree (a year for every degree);
# and when any of them, by turning or by direction, reaches a sign or
# planetary fortune or infortune, it produces the indication of that sign
# or planet" (VI.2, 1). VI.2, 2-17 assign the points to topics; 18-20
# generalise to any indicator; 21-26 handle a quadrant cusp that falls in
# a different sign from its whole-sign house -- turned "in two ways",
# from the house by counting and from the sign the degree falls in
# (VI.2, 22), directed from its actual degree (21).
#
# The turning is whole-sign profection from each point's own natal
# position, one sign a year, exactly as the Ascendant's. The direction "a
# year for every degree" is III.1, 12's third case for planets and Lots
# and, for cusps, VI.2, 21's "portions of the hours and the right circle"
# (fn 33: proportional semi-arcs): since 2026-09-15 each row carries where
# that direction stands at the age (pn4_semi_arc_stand). The Ascendant's
# and the meridian's directions are the two distributions on the page and
# the rows for houses 1, 10 and 4 point to them.
#
# What is read in and said on the page: which "twelve Lots" is not
# stated; the formulas in fn 12-31 are Dykes' identifications from Sahl
# and the Great Introduction, and the engine's Lots are paired to them
# below with the three places they differ on the night reversal named.
# "whichever of them had the shift in the root" (VI.2, 8; compare 6) is the sect planet,
# per fn 16 and 19. The triplicity lords of VI.2, 4-5 are pn4_turning_triplicity_lords (2026-09-11); all
# the delineation are not built. No worked example exists; Figures 90-91
# are Dykes' diagrams.

PN4_TURNING_HOUSES = (
    (1, 'the body, with the Moon', 'VI.2, 3'),
    (2, 'assets, with the Lots of Fortune and assets', 'VI.2, 4'),
    (3, 'siblings, with its Lot', 'VI.2, 5'),
    (4, 'the fathers, with the Lot of the father', 'VI.2, 6'),
    (5, 'children, with its Lot', 'VI.2, 9'),
    (6, 'slaves, and illnesses, with their Lots', 'VI.2, 10-11'),
    (7, 'women, with its Lot', 'VI.2, 12'),
    (8, 'death and catastrophes, with its Lot', 'VI.2, 13'),
    (9, 'travel, with its Lot', 'VI.2, 14'),
    (10, 'authority and rank, with its Lot; the mother (VI.2, 8)', 'VI.2, 15'),
    (11, 'hope and friends, with its Lot', 'VI.2, 16'),
    (12, 'enemies and riding animals, with its Lot', 'VI.2, 17'),
)

# (lot id, what it is turned for, citation, note). The ids are the
# engine's LOT_DEFINITIONS rows; the pairing to VI.2's footnotes is Dykes'.
PN4_TURNING_LOTS = (
    ('fortune', 'assets', 'VI.2, 4', ''),
    ('assets_lord2', 'assets', 'VI.2, 4; fn 12', ''),
    ('siblings_hermes', 'siblings', 'VI.2, 5; fn 15',
     'fn 15 reverses it at night (Firmicus); the app\'s Sahl row does not ("for one who was born by day and night")'),
    ('father', 'the fathers', 'VI.2, 6; fn 17', ''),
    ('mother', 'the mother', 'VI.2, 8; fn 20', ''),
    ('children_hermes', 'children', 'VI.2, 9; fn 22', ''),
    ('slaves', 'slaves', 'VI.2, 10; fn 24', ''),
    ('chronic_illness', 'illnesses', 'VI.2, 11; fn 25', ''),
    ('marriage_men', 'women (for men)', 'VI.2, 12; fn 26',
     'fn 26 reverses it at night; the app\'s Sahl row does not'),
    ('marriage_women', 'women (for women)', 'VI.2, 12; fn 26',
     'fn 26 reverses it at night; the app\'s Sahl row does not'),
    ('death', 'death and catastrophes', 'VI.2, 13; fn 27', ''),
    ('travel', 'travel', 'VI.2, 14; fn 28', ''),
    ('work_expedition', 'authority and rank', 'VI.2, 15; fn 29',
     'Sahl, On Nativities 10.2.5, 1: "Calculate by day and night from Saturn to the Moon"; '
     'Gr. Intr. VIII.4, 306: "is taken by day and night from Saturn to the Moon, and it is cast out from the Ascendant."'),
    ('work_action', 'authority and rank (action)', 'VI.2, 15; fn 29', 'the alternative fn 29 names'),
    ('friends', 'hope and friends', 'VI.2, 16; fn 30', ''),
    ('enemies_necessity', 'enemies', 'VI.2, 17; fn 31', 'one of fn 31\'s three'),
    ('enemies_slaves', 'enemies', 'VI.2, 17; fn 31', 'one of fn 31\'s three'),
    ('enemies_hermes', 'enemies', 'VI.2, 17; fn 31', 'one of fn 31\'s three'),
)

def pn4_turning_planet_topics(sect):
    """VI.2, 2, 6 and 8: what each planet is turned for. The parents'
    indicators are the sect planets, "whichever one of the two had the
    shift in the root" (fn 16, 19)."""
    diurnal = sect == 'Diurnal'
    topics = {
        'Sun': 'rank, works, and authority (VI.2, 2)' + ('; the fathers (VI.2, 6)' if diurnal else ''),
        'Moon': 'the accidents of the body (VI.2, 2-3)' + ('' if diurnal else '; the mother (VI.2, 8)'),
        'Jupiter': 'assets (VI.2, 2)',
        'Venus': 'women, amusement, delight, and fornication (VI.2, 2)' + ('; the mother (VI.2, 8)' if diurnal else ''),
        'Saturn': 'what it indicates (VI.2, 2)' + ('' if diurnal else '; the fathers (VI.2, 6)'),
        'Mars': 'what it indicates (VI.2, 2)',
        'Mercury': 'what it indicates (VI.2, 2)',
    }
    return topics

def _pn4_natal_planets_in_sign(planetary_data, sign):
    """VI.2, 1: the "planetary fortune or infortune" a turned point
    reaches. Every natal planet in the sign, tagged."""
    found = []
    for planet in PN4_SEVEN:
        row = planetary_data.get(planet)
        if row and get_zodiac_sign(row['longitude']) == sign:
            tag = 'fortune' if planet in FORTUNES else 'infortune' if planet in INFORTUNES else 'neither'
            found.append(f"{planet} ({tag})")
    return ', '.join(found) if found else 'none'

def pn4_turned_sign(natal_lon, completed_years):
    """VI.2, 1: "a year for every sign", from the point's own position."""
    return get_zodiac_sign(pn4_profect(natal_lon, int(completed_years)))

def _pn4_condition_string(data, planet, *, compare_sahl=False):
    """A planet's condition as the II.3 examination prints it: sign, motion,
    side of the Sun, solar phase. '-' when the planet is absent."""
    r = data.get(planet)
    if not r:
        return '-'
    motion = 'retrograde' if r.get('speed_in_lon', 1.0) < 0 else 'direct'
    phase, side, _el = solar_phase(planet, r['longitude'], data['Sun']['longitude'], r.get('speed_in_lon'))
    condition = f"{get_zodiac_sign(r['longitude'])}, {motion}, {side or '-'}{', ' + phase.lower() if phase else ''}"
    if compare_sahl:
        sahl_phase = solar_phase(planet, r['longitude'], data['Sun']['longitude'],
                                 r.get('speed_in_lon'), source='Sahl')[0]
        sahl_text = 'in the heart' if sahl_phase == 'Cazimi' else (sahl_phase or 'outside the solar bands').lower()
        if planet == 'Sun':
            sahl_text = 'the Sun itself; no solar testimony'
        return f"Abu Ma'shar (heart through 16′): {condition}; Sahl (heart through 1°): {sahl_text}"
    return condition

def _triplicity_lords_in_sect_order(sign, sect, *, table):
    """Order the named table's two principal lords by natal sect, then partner."""
    trip = triplicity_rulers(sign, table=table)
    if sect == 'Diurnal':
        return [trip['Day'], trip['Night'], trip['Participating']]
    return [trip['Night'], trip['Day'], trip['Participating']]


LIFE_LORDS_SOURCE = ("Sahl, On Nativities 2.11, 1-4 (Theophilus; fn 148: Carmen I.24, 1-8); 2.13, 39; 2.19, 5; "
                     "PN IV VI.2, 4 with fn 13 is related testimony; its assumed identity table is declared separately")
LIFE_LORDS_ASCENDANT_SOURCE = "Al-Andarzaghar, reported by al-Qabīsī I.57b: the Ascendant's triplicity lords signify the beginning, middle, and end of life at death; the third also shares its companions' significations. These are qualitative life-stage attributions, with no numerical boundaries given. Sahl, On Nativities 1.29, separately uses these lords to judge upbringing."
LIFE_LORDS_ASCENDANT_HEADING = 'Ascendant triplicity lords: life-stage significations'
LIFE_LORDS_ASCENDANT_STAGES = {
    'first': 'Beginning of life; no numerical boundary specified.',
    'second': 'Middle of life; no numerical boundary specified.',
    'third': 'End of the matter at death; also signifies what its companions signify. No numerical boundary specified.',
}
LIFE_LORDS_PARAGRAPH = "No numerical age ranges are calculated here. The sources give life-stage indications with differing roles for the triplicity lords. Sahl 2.11 gives two luminary triplicity lords their respective times, with a partner supporting both; Sahl 2.19, 5 conditionally connects the third lord with good fortune at life's end. Al-Qabīsī I.57b separately assigns life-stage significations to the Ascendant's triplicity lords. Valens II.2 relates transitions to sign rising times or a chronocrator's return, but the supplied excerpt does not provide a complete calculation. Dykes's note 14 to PN IV VI.2, 4 mentions a second or third lord for an older native without specifying ages. None of these passages prescribes three thirty-year periods."
LIFE_LORDS_TIMES = {
    'first': "the beginning of his life (2.13, 39: 'first' supplied; fn 181 calls the sentence unclear and suggests the second lord); his benefit in its time, if it is the strong one (2.11, 2)",
    'second': "the time of the second lord (2.11, 2); Dykes's fn 14 to VI.2, 4: \"if the native was older\"",
    'third': "the partner: \"supports them both\" (2.11, 4); good fortune at the end of his lifespan if in the seventh "
             "(2.19, 5); Dykes's fn 14 to VI.2, 4: the third lord for the older native \"for many Persian- and Arabic-language "
             "astrologers\"",
}


def triplicity_lords_of_life(chart_data, point='sect light'):
    """Sahl 2.11, 1-4 gives the two sect-light lords their respective times,
    with the partner supporting both. Sahl 2.13, 39's supplied first has
    fn 181's uncertainty; 2.19, 5 conditionally gives fortune at life's end.
    Al-Andarzaghar through al-Qabisi I.57b separately gives the Ascendant's
    lords qualitative life-stage significations; Sahl 1.29, 2-5 uses them
    for upbringing. Valens II.2 supplies transition criteria without a
    complete calculation in the supplied excerpt. No numerical age ranges
    are calculated here. PN IV VI.2, 4 fn 14 is Dykes's qualitative example,
    not an age boundary. Each reference point retains its own source table.
    """
    natal, sect = chart_data['planetary_data'], chart_data['sect']
    if point == 'Ascendant':
        lon, label, source = chart_data['ascendant'], 'the Ascendant', LIFE_LORDS_ASCENDANT_SOURCE
        table = 'al-Qabisi'
    else:
        light = 'Sun' if sect == 'Diurnal' else 'Moon'
        if light not in natal:
            return []
        lon, label, source = natal[light]['longitude'], f'{light}, the sect light', LIFE_LORDS_SOURCE
        table = 'Sahl'
    sign = get_zodiac_sign(lon)
    rows = []
    for rank, lord in zip(('first', 'second', 'third'), _triplicity_lords_in_sect_order(sign, sect, table=table)):
        stage = ({'Life-stage signification': LIFE_LORDS_ASCENDANT_STAGES[rank]} if point == 'Ascendant'
                 else {'Time of life': LIFE_LORDS_TIMES[rank]})
        rows.append({'Triplicity of': f'{sign} ({label})', 'Order': rank, 'Lord': lord,
                     **stage, 'Root condition': _pn4_condition_string(natal, lord, compare_sahl=True),
                     'Source': source, **triplicity_source_fields(sign, table=table)})
    return rows


# al-Andarzaghar's triplicity lords of the twelve houses, as al-Qabisi
# reports them house by house (I.57b-68, in ITA I.13, pp. 71-76). Three
# verbatim fragments per house -- what each lord "signifies" -- and the
# sentence whole. The house-2 entry carries al-Andarzaghar's extra rule
# (the stronger of the three "in being and place" is the authority over
# assets); nothing here weighs the lords, so it is quoted and not applied.
ANDARZAGHAR_TRIPLICITY_LORDS = {
    1: {'section': 'I.57b', 'house': 'the Ascendant',
        'first': "life and the nature of the native or of the one asking, and his delights and pleasures, and what he "
                 "esteems or what he hates, and what good or evil finds him at the beginning of his life",
        'second': "life and the body and virtue and the middle of life",
        'third': "what its companions signified, and it signifies the end of the matter at death",
        'text': "in a nativity the first Lord of the triplicity of the Ascendant signifies life and the nature of the "
                "native or of the one asking, and his delights and pleasures, and what he esteems or what he hates, and "
                "what good or evil finds him at the beginning of his life. And the second Lord of the triplicity "
                "signifies life and the body and virtue and the middle of life. And the third Lord of the triplicity "
                "signifies what its companions signified, and it signifies the end of the matter at death."},
    2: {'section': 'I.58', 'house': 'the house of assets',
        'first': "gives assets at the beginning of life",
        'second': "in the middle",
        'third': "at its end",
        'text': "see which one of them is stronger in being and place: you will make this one deservedly the authority "
                "over assets and the significator of their acquisition. Which if it were in the Mid-heaven, he will "
                "find this from the king; and if it were in the house of faith, it will be better. Likewise, the first "
                "Lord of the triplicity gives assets at the beginning of life, the second one in the middle, and the "
                "third at its end."},
    3: {'section': 'I.59', 'house': 'the house of brothers',
        'first': "older brothers", 'second': "the middle ones", 'third': "the younger ones",
        'text': "the first Lord of the triplicity of the house of brothers signifies older brothers, and the second one "
                "the middle ones, but the third one the younger ones; and their worthiness will be according to their "
                "places."},
    4: {'section': 'I.60', 'house': 'the house of fathers',
        'first': "fathers", 'second': "cities and lands", 'third': "the ends of matters and prisons",
        'text': "the first Lord of the triplicity of the house of fathers signifies fathers, the second cities and "
                "lands, but the third the ends of matters and prisons."},
    5: {'section': 'I.61', 'house': 'the house of children',
        'first': "children and life", 'second': "delight", 'third': "legates",
        'text': "the first Lord of the triplicity of the house of children signifies children and life, the second "
                "delight, but the third signifies legates."},
    6: {'section': 'I.62', 'house': 'the house of infirmities',
        'first': "infirmities and recovery from infirmities", 'second': "domestics and slaves",
        'third': "what will find him in terms of those, and their usefulness and works",
        'text': "the first Lord of the triplicity of the house of infirmities signifies infirmities and recovery from "
                "infirmities, the second one signifies domestics and slaves, the third signifies what will find him in "
                "terms of those, and their usefulness and works."},
    7: {'section': 'I.63', 'house': 'the house of women',
        'first': "women", 'second': "contentions", 'third': "uniting [with others]",
        'text': "the first Lord of the triplicity of the house of women signifies women, the second one contentions, "
                "the third uniting [with others]."},
    8: {'section': 'I.64', 'house': 'the house of death',
        'first': "death", 'second': "ancient things", 'third': "inheritance",
        'text': "the first Lord of the triplicity of the house of death signifies death, and the second one ancient "
                "things, and the third inheritance."},
    9: {'section': 'I.65', 'house': 'the house of pilgrimage',
        'first': "pilgrimage and everything which happens on it",
        'second': "faith and religion and the good state of these things and their manner",
        'third': "wisdom and dreams, also stars and omens and their truth and lying in this",
        'text': "the first Lord of the triplicity of the house of pilgrimage signifies pilgrimage and everything which "
                "happens on it, the second one signifies faith and religion and the good state of these things and "
                "their manner, and the third one is the significator of wisdom and dreams, also stars and omens and "
                "their truth and lying in this."},
    10: {'section': 'I.66', 'house': 'the royal house',
         'first': "work and exaltation, and the highest station", 'second': "[one's] voice and courage in the same",
         'third': "its stability and durability",
         'text': "the first Lord of the triplicity of the royal house signifies work and exaltation, and the highest "
                 "station; the second one signifies [one's] voice and courage in the same; the third signifies its "
                 "stability and durability."},
    11: {'section': 'I.67', 'house': 'the house of trust',
         'first': "trust", 'second': "friends", 'third': "their usefulness",
         'text': "the first Lord of the triplicity of the house of trust signifies trust, and the second one friends, "
                 "the third signifies their usefulness."},
    12: {'section': 'I.68', 'house': 'the house of enemies',
         'first': "enemies", 'second': "labors", 'third': "beasts [for riding] and flock-animals",
         'text': "the first Lord of the triplicity of the house of enemies signifies enemies, the second one labors, "
                 "but the third signifies beasts [for riding] and flock-animals."},
}


def evaluate_andarzaghar_triplicity_lords(asc_lon, sect):
    """al-Andarzaghar's triplicity lords of the twelve houses (al-Qabisi
    I.57b-68, in ITA I.13): for each whole-sign house from the Ascendant,
    the al-Qabisi I.16c lords of its sign's triplicity (Mars for Virgo) in the chart's sect order
    ("first, second, third" as al-Qabisi himself orders them, I.16 in ITA
    I.7: by day the day lord first, by night the night lord -- Abbr. I.86
    and Gr. Intr. V.14, 6 the same, and Sahl 10.2.7, 16) beside what al-Andarzaghar says each
    signifies. Twelve rows; display only. The planets' conditions are not
    read, and I.58's "which one of them is stronger in being and place" is
    quoted, not weighed."""
    start = SIGN_ORDER.index(get_zodiac_sign(asc_lon))
    rows = []
    for house in range(1, 13):
        sign = SIGN_ORDER[(start + house - 1) % 12]
        first, second, third = _triplicity_lords_in_sect_order(sign, sect, table='al-Qabisi')
        entry = ANDARZAGHAR_TRIPLICITY_LORDS[house]
        rows.append({'House': house, 'Sign': sign, 'First lord': first, 'Second lord': second, 'Third lord': third,
                     'Signifies (1st / 2nd / 3rd)': f"{entry['first']} / {entry['second']} / {entry['third']}",
                     'Source': f"al-Qabisi {entry['section']} (al-Andarzaghar), in ITA I.13; table I.16c",
                     **triplicity_source_fields(sign, table='al-Qabisi')})
    return rows


# --- Sahl: indications of fortune and livelihood (On Nativities Ch. 2) ---
# The finding's spine is Theophilus's paragraph as Sahl carries it (2.11,
# 1-5; fn 148: Carmen I.24, 1-8, the source of Abu 'Ali's twelve worked
# charts in JN Ch. 7): the first and second lords of the sect light's
# triplicity read by whole-sign PLACE -- strong (in a stake or what
# follows one) or falling -- with "under the rays" as the one further
# weakness Sahl names (2.11, 5). The chapter's seven approaches (2.1, 2-9)
# are not an algorithm over that pair: 2.11, 1 and 3 give the pattern for
# both strong and both falling, 2.11, 2 gives a TIMING pattern for the
# mixed pair ("his benefit will be in the time of the strong one"), and no
# sentence assigns a numbered class to every combination. So the first
# row is the app's SYNTHESIS, said so on the page: "read by this app as
# class N" with its grounds -- both strong class 1, both weak class 6,
# first strong class 2 and the reverse class 5 (the first lord's time
# being the beginning of life, 2.13, 39) -- modified, with no class step,
# by the partnering (third) lord, which 2.11, 4 makes a stated modifier
# ("supports them both ... through its strength", "brings [them] down")
# and 2.3, 22 ranks third. The Lot step: 2.3, 6 turns the reading to the
# Lot when "the lord of the triplicity" is "made unfortunate" -- the
# sentence's singular states no quantifier, and the app enters on EITHER
# lord weak (falling, or under the rays) or with an infortune on it, its
# own reading, said so. The Lot's sentences give judgments of the native
# (2.20, 1-2 misery; 2.3, 7 and 9 rank and happiness; 2.16, 2 and 4 the
# middle) and Sahl nowhere says how they combine with Theophilus's (2.11,
# 1-3): no replacement, no threshold by the number of weak lords, no
# priority -- he gives an order of investigation (2.3, 6; 2.3, 10), not a
# precedence among verdicts. So the synthesis carries both judgments with
# their sentences: a middling or high level beside the mixed pair's timing
# pattern is mixed by design (no single class); the same level twice is
# concordant; opposed levels -- 2.20's lifelong misery beside the pattern's
# benefit period among them -- or the Lot's own sentences at two levels,
# are unresolved, both judgments kept. Three measures are kept apart:
# the whole-sign place (the spine), 2.3, 17-18's by-sign / by-degree cases
# (their own rows, from the quadrant cusps), and the 15 degrees by
# ascensions (2.13, 48-51; 2.3, 4-5; 2.16, 3), a grade row never folded
# into strong or weak. "Made unfortunate" for the Lot, its lord and the
# lords of places is 2.20, 1's own gloss -- an infortune with it, or
# looking at it from a square or opposition -- by whole sign. Sahl's other
# indications (2.3, 12, 19-21; 2.11, 14; 2.16, 5-6; 2.17; 2.19; 2.21) are
# listed under the synthesis with their sentences. Display only.

PROSPERITY_CLASSES = {
    # Sahl 2.1, 3-9, his own list; 4 duplicates 2 (Dykes's comment at the head of the chapter), so 4 is not a key.
    'high': 'The first: good fortune (2.1, 3)',
    'high to low': 'The second: falls from that good fortune (2.1, 4)',
    'middling': 'The third: livelihood middling (2.1, 5)',
    'low to high': 'The fifth: rises up after wretchedness (2.1, 7)',
    'low': 'The sixth: wretched, not ceasing to be (2.1, 8)',
    'own hands': 'The seventh: profit from his own hands (2.1, 9)',
    'injustice': 'The seventh: from force and injustice (2.1, 9 with fn 1; 2.21, 3)',
}

PROSPERITY_SAHL = {
    '2.3, 1': "look at the lords of the triplicities of the Moon by night, and the lords of the triplicities of the "
              "Sun by day, and the positions of them both from the Ascendant",
    '2.3, 2': "For if you found the two lords of the triplicity cleansed of the infortunes and of defects, and you "
              "found them both in the stakes, then judge that the native will be happy for all the days of his life.",
    '2.3, 6': "if you found the lord of the triplicity of the Sun made unfortunate, then you must work with the Lot "
              "of Fortune.",
    '2.3, 7': "For if you found it in the stakes or what follows them, and the lord of the Lot is eastern or cleansed "
              "of the infortunes and the rays, and it looks at the Lot from a strong position, and a fortune is "
              "looking at them from a powerful position, and a fortune is looking at it as well, and the infortunes "
              "are not looking at it, then the native will be a king, or prominent, and a powerful noble, mighty, "
              "elevated, and especially if the Lot and its lord was with it.",
    '2.3, 9': "Now if you found the lord of the Lot not looking at the Lot, and it occurred in the fifth or eleventh "
              "from the Ascendant, he will be happy; and more excellent than that is if it is looking at the Lot.",
    '2.3, 10': "if you found the Lot of Fortune and the lord of the Lot of Fortune made unfortunate, then look at "
               "the lord of the Ascendant or the lord of the Midheaven, or the lord of the house of hope.",
    '2.3, 12': "look at the eleventh from the Ascendant: for if you found a fortune in it, it increases his good "
               "fortune; and if you found an infortune in it, it takes away from his good and increases in evil.",
    '2.3, 17': "Now if a planet was in one of the stakes by sign, and it was falling from the stakes by degrees, "
               "then it indicates for the native reputation, [but] it corrupts assets.",
    '2.3, 18': "And if you found the planet in what follows the stake, or it is falling in the sign, and is in the "
               "stake [by] degrees, it indicates assets and a fine condition, with difficulty in [his] reputation and "
               "no fame, and especially if the planet was western [and] under the earth.",
    '2.3, 19': "Now if you found the lord of the triplicity in the second and eighth, it indicates a decline of [his] "
               "condition in the matter which the planet likewise produces.",
    '2.3, 20': "Now if that planet was a fortune, then his affairs will be set aright after their corruption.",
    '2.3, 21': "And whenever you find Jupiter in one of these two places, (if he was not the governor of the "
               "triplicity) then it indicates a decline of [his] condition even though he will not provide injustice.",
    '2.3, 22': "And the stronger of the lords of the triplicities is the first one, then the second, then the one "
               "partnering with them both.",
    '2.11, 1': "If you found both of the two lords of the triplicity of the luminary to be strong, they indicate "
               "high rank from the beginning of his life to its end.",
    '2.11, 2': "And if one of the two was strong and the other weak, his benefit will be in the time of the strong "
               "one of them, and his baseness in the time of the one of them [that is falling].",
    '2.11, 3': "And if they were both falling, they indicate baseness from the beginning of his life to its end.",
    '2.11, 4': "And the aspect of the fortunes and infortunes increases in that and subtracts from that, and the "
               "partnering lord of the triplicity supports them both in their elevation, through its strength (if it "
               "was strong), and brings [them] down (if it was a falling [place]).",
    '2.11, 5': "And if the lord of the triplicity was under the rays, then it has no strength.",
    '2.11, 14': "And if it was falling [but] not made unfortunate, then [it indicates] what is middling of assets; "
                "and if it was the reverse, then reverse it.",
    '2.13, 39': "the first lord of the triplicity indicates the end of the father's life, and the beginning of the "
                "native's life",
    '2.13, 40': "But for good fortune, let your reliance be on the Sun by day and the Moon by night, and their "
                "positions, and the positions of the lords of the triplicities of the luminaries in the excellent "
                "and bad places.",
    '2.13, 48': "of the more powerful indications of good fortune is if the first lord of the triplicity of the "
                "glowing one is in a stake or what follows it, and that is the 15° which follows it, by degrees of "
                "ascensions: for if it was like that, it indicates praise and good fortune (and what is less [than "
                "that] in degrees is preferable).",
    '2.13, 49': "Now if it was in the second 15°, it indicates his good fortune is below the first [type].",
    '2.13, 50': "And if it was in the third 15°, it indicates [what is in] the middle of assets.",
    '2.13, 51': "And what is after that in degrees, up to the next stake, is of the nativities of the poor.",
    '2.16, 2': "For if you found it made unfortunate, and you found a fortune looking at it from an excellent place, "
               "and it is eastern, then the good fortune of that native (as well as his good and wealth) will be in "
               "the middle, according to the distribution of that fortune.",
    '2.16, 3': "if you found the lord of the triplicity of the Sun in the fifteenth from the end of the "
               "[ascensional] sign of the stake, then his good fortune will be in the middle.",
    '2.16, 4': "if the fortunes and infortunes were all looking together at the Lot of Fortune, then his livelihood "
               "will be in the middle.",
    '2.16, 5': "look too at the lord of the Ascendant, the lord of the Midheaven, and the lord of the house of hope: "
               "for if you found a motley mixture, [some] of them cleansed of the infortunes and [some] of them made "
               "unfortunate, then the good fortune of that native will be in the middle, sometimes with good, and "
               "sometimes with hardship.",
    '2.16, 6': "If the triplicity lord of the sect light is in a bad place, but that of the other luminary in a good "
               "place, it indicates someone in the middle.",
    '2.17, 2': "Look for one who is born by day at the lords of the triplicities of the Sun, and for one born by "
               "night at the lords of the triplicities of the Moon: for if they were in excellent places, [but] then "
               "the infortunes made them unfortunate, then he will fall from his good fortune.",
    '2.17, 3': "And if you found the Lot of Fortune or its lord in an excellent place, and the infortunes made them "
               "unfortunate, he will also fall from good fortune.",
    '2.17, 4': "if you found the fortunes in the Ascendant or Midheaven, and the infortunes were in the house of "
               "marriage, then it indicates falling from good fortune.",
    '2.17, 5': "if you found Mars or Saturn in the house of assets, and they were not powerful, then it indicates "
               "falling from good fortune.",
    '2.17, 7': "if you found Mars or Saturn in the eleventh from the Ascendant or in the eleventh from the Lot of "
               "Fortune, then it indicates a fall from good fortune.",
    '2.17, 8': "if you found Saturn with the Moon in one of the stakes, even if he was a king, he will fall from good "
               "fortune; and that is worse if Mars looked at it.",
    '2.17, 10': "look also at the Moon: if you saw her separating from the fortunes and connecting with the "
                "infortunes, then it indicates a fall.",
    '2.17, 11': "likewise if the Sun was in the sixth or in the twelfth, and the infortunes looked at him, it "
                "indicates falling.",
    '2.19, 1': "if the infortunes were in the stakes and the fortunes in what follows them, it indicates falling "
               "down, then they will rise.",
    '2.19, 2': "if you found the Moon separating from the infortunes and connecting with the fortunes, then he will "
               "rise after wretchedness.",
    '2.19, 5': "And if the third lord of the triplicity was in the house of marriage, he will gain good fortune at "
               "the end of his lifespan.",
    '2.19, 6': "And a native whose planets you find in the bad places from the Ascendant, and they are looking at "
               "the Lot of Fortune, and its lord in an excellent place, then at the end of his life he will have a "
               "good livelihood.",
    '2.20, 1': "look: if you found the lord of the triplicities of the Sun made unfortunate, or the Lot of Fortune "
               "unfortunate, and you found it in the sixth and twelfth, if the infortunes were with it or they looked "
               "at it from a square or opposition, and you found the lord of the Lot in the house of its fall, or "
               "made unfortunate, powerful in misfortune (and you do not look at the Sun for one born by night, nor "
               "at the Moon for one born by day), and Mars for one born by day is with the Lot, or opposing or in "
               "square to it, then that [native] will not cease to be miserable from the day he is born up to the "
               "day he dies.",
    '2.20, 2': "look likewise at the lord of the Lot: if you found it in the sixth and twelfth, and you saw Jupiter "
               "and Venus made unfortunate in the sixth and twelfth, and they are both not looking at the Moon (and "
               "<worse is if> you found the two infortunes in the stakes or what follows the stakes), <he will be "
               "miserable>.",
    '2.21, 1': "if you found the first lord of the triplicity of the Lot of Fortune in the bound of the fortunes, "
               "and it is in an excellent position in the nativity, and it looked at the Lot of Fortune, his earning "
               "and livelihood will be from his own assets.",
    '2.21, 2': "if you found <the two lords of the triplicity of> the Lot of Fortune not looking at the Lot of "
               "Fortune, and the other fortune is not looking at the Lot of Fortune, his livelihood will increase in "
               "the good due to a foreign man, and good will be said about him.",
    '2.21, 3': "if you found Saturn and Mars in the eleventh from the Lot of Fortune, and you found them both in "
               "their own houses, triplicity, or exaltation, his livelihood will be from force and injustice.",
    '2.21, 4': "And if you found the first lord of the triplicity of the Lot of Fortune not looking at the Lot, "
               "while the second lord of the triplicity does look at the Lot, then judge for him that he will earn "
               "assets at one time and [then] destroy them at another, and will be of those who squander and are "
               "ruined.",
}

# The Book of Aristotle's seven headings (III.2.0) and Abu 'Ali's rule paragraphs (JN Ch. 7), the parallels.
PROSPERITY_ALSO = {
    'frame': "PN I, BA III.2.0: the seven-fold examination, [1] the amount of prosperity ... [7] by violence and "
             "looting; JN Ch. 7, the twelve examples, Figures 10-21",
    'angles': "PN I, JN Ch. 7 [Middling fortune]: \"look at the Lords of the triplicity of the Sun (in diurnal "
              "nativities) and the Lords of the triplicity of the Moon (in the night): which if they were in the "
              "angles, they signify greatness and excellent fortune. If however they were in the succeedents, they "
              "signify a middling amount of fortune. Which if they were cadent, they signify labor and a bad "
              "condition.\" BA III.2.1 [1.2]: cleansed of the infortunes and in a pivot, \"he will rejoice in "
              "perpetual luckiness for all the days of his life\"",
    'succedent': "PN I, JN Ch. 7: Abu 'Ali's rule grades the succeedents middling, but his second example (Figure "
                 "11), both lords \"in succeedents of the angles\", he calls \"prosperity and riches, and a multitude "
                 "of substance\"; this app follows Sahl 2.11, 1 and the example, and 2.3, 18 (the stake by degrees) "
                 "is its own row",
    'cadent': "PN I, JN Ch. 7 [Middling fortune]: \"Which if they were cadent, they signify labor and a bad "
              "condition\"; Figures 10 and 17 (both lords cadent: a pauper; poverty and the bad condition)",
    'mixed': "PN I, JN Ch. 7, Figure 19 (Example 10): the first lord under the rays, \"labor and anxiety in the first "
             "third of his life\", the second in an angle, \"prosperity and the native's good condition after "
             "labor\"",
    'lot': "PN I, BA III.2.1 [1.4]: the sect light's lord corrupted, \"one will have to attend more diligently to "
           "what place the Lot of Fortune holds onto\"; JN Ch. 7, Figure 20: \"whenever the Lords of the triplicity "
           "of the luminary (whose rulership it was) were cadent, look at the Lot of Fortune\"",
    'middling': "PN I, BA III.2.3 [3.1]-[3.5]; JN Ch. 7 [Middling fortune]: the Lot in the angles \"and the fortunes "
                "and bad ones aspected it equally\"",
    'falling': "PN I, BA III.2.2 [2.1]-[2.12]; JN Ch. 7 [The falling of fortune]",
    'rising': "PN I, BA III.2.4 [4.1]-[4.8]; JN Ch. 7 [Advancement after labor]",
    'misery': "PN I, BA III.2.5 [5.1]-[5.5]",
    'hands': "PN I, BA III.2.6 [6-7.1]-[6-7.3]",
    'eleventh from the lot': "PN I, BA III.2.0 [1.7], III.2.1 [1.7]: the eleventh from the Lot \"appears strong like "
                             "the eleventh from the east\" (fn 89: Valens II.21); III.2.2 [2.6]",
    'ascensions': "Sahl 2.3, 4-5 with fnn 82-83 (the degrees \"taken in ascensions as measured from the axial "
                  "degree\"); 2.16, 3 with fn 222 (Carmen I.28, 7: 45° in ascensions out from the degree of an axis, "
                  "the first 15° high, the second next best, the third middling); BA III.2.1 [1.3]",
    'third': "PN I, JN Ch. 7, Figures 13, 14, 20, 21: the third lord \"not impeding\", \"tempered and restrained\", "
             "\"the last Lady of the triplicity\", \"prosperity for the native at the end of his life\"",
}

PROSPERITY_STAKES = (1, 4, 7, 10)
PROSPERITY_FOLLOWING = (2, 5, 8, 11)
PROSPERITY_FALLING = (3, 6, 9, 12)


def _prosperity_signs_apart(lon_a, lon_b):
    return (int((lon_b % 360.0) // 30) - int((lon_a % 360.0) // 30)) % 12


def _prosperity_looks(lon_a, lon_b):
    """Whole-sign aspect from a to b: 'with' (one sign), sextile, square,
    trine, opposition, or None (aversion)."""
    apart = _prosperity_signs_apart(lon_a, lon_b)
    return {0: 'with', 2: 'sextile', 10: 'sextile', 3: 'square', 9: 'square', 4: 'trine', 8: 'trine',
            6: 'opposition'}.get(apart)


def _prosperity_infortunes_on(lon, natal, exclude=()):
    """The infortunes with the degree's sign, or looking at it from a
    square or opposition -- 2.20, 1's own gloss of 'made unfortunate',
    by whole sign. Returns 'Saturn by square'-style strings."""
    out = []
    for p in ('Saturn', 'Mars'):
        if p in exclude or p not in natal:
            continue
        look = _prosperity_looks(natal[p]['longitude'], lon)
        if look in ('with', 'square', 'opposition'):
            out.append(f'{p} {look}' if look == 'with' else f'{p} by {look}')
    return out


def _prosperity_looking(lon, natal, group):
    """Members of GROUP present in the chart that look at the sign of lon
    (with it, or by sextile, square, trine or opposition)."""
    return [p for p in group if p in natal and _prosperity_looks(natal[p]['longitude'], lon) is not None]


def _prosperity_nth(house):
    return {1: '1st', 2: '2nd', 3: '3rd'}.get(house, f'{house}th')


def _prosperity_place_word(house):
    if house in PROSPERITY_STAKES:
        return 'a stake'
    if house in PROSPERITY_FOLLOWING:
        return 'what follows a stake'
    return 'falling from the stakes'


def _prosperity_ascension_from_stake(lon, asc, armc, obliquity, geo_lat, mc=None):
    """Select the preceding actual axis in zodiacal order, THEN measure.

    OA at Asc, OD at Dsc, RA at MC/IC is the declared computational
    convention. Compare only zodiacal distances for selection; comparing
    arcs in different ascensional coordinates can select the wrong axis.
    The provided MC avoids reconstructing an already available axis.
    """
    if not _ascensional_method_applies(obliquity, geo_lat):
        return None
    if mc is None:
        mc = _lon_with_right_ascension(armc, obliquity)
    axes = {'the Ascendant': asc, 'the seventh': (asc + 180.0) % 360.0,
            'the Midheaven': mc, 'the fourth': (mc + 180.0) % 360.0}
    stake = min(axes, key=lambda name: (lon - axes[name]) % 360.0)
    origin = axes[stake]
    if stake == 'the Ascendant':
        measure = lambda x: _oblique_ascension(x, obliquity, geo_lat)
    elif stake == 'the seventh':
        measure = lambda x: _oblique_ascension((x + 180.0) % 360.0, obliquity, geo_lat)
    else:
        measure = lambda x: _ra_decl(x, obliquity)[0]
    # Use the same coordinate function on both endpoints. At the actual
    # axis the subtraction is exactly zero, not a tiny negative modulo 360.
    return stake, (measure(lon) - measure(origin)) % 360.0


def _prosperity_moon_motion(natal):
    """The Moon's last separation and next connection by degree, among the
    planets she looks at by whole sign: (separated-from, connecting-with),
    names or None. Needs speeds; None, None without them."""
    moon = natal.get('Moon')
    if not moon or moon.get('speed_in_lon') is None:
        return None, None
    v = moon['speed_in_lon']
    behind, ahead = [], []
    for p in ('Saturn', 'Jupiter', 'Mars', 'Sun', 'Venus', 'Mercury'):
        r = natal.get(p)
        if not r or r.get('speed_in_lon') is None:
            continue
        apart = _prosperity_signs_apart(moon['longitude'], r['longitude'])
        if apart not in (0, 2, 3, 4, 6, 8, 9, 10):
            continue
        target = 30.0 * min(apart, 12 - apart)
        rel = (moon['longitude'] - r['longitude']) % 360.0
        exact = target if rel <= 180.0 else 360.0 - target
        delta = rel - exact
        if delta == 0.0 or abs(v) <= abs(r['speed_in_lon']):
            continue
        if delta * (v - r['speed_in_lon']) < 0.0:
            ahead.append((abs(delta), p))
        else:
            behind.append((abs(delta), p))
    orb = PLANETARY_ORBS['Moon']
    sep = min(behind)[1] if behind and min(behind)[0] <= orb else None
    con = min(ahead)[1] if ahead and min(ahead)[0] <= orb else None
    return sep, con



@dataclass(frozen=True)
class ProsperityAssessment:
    """One sentence's clauses, not a generic planetary strength score."""
    sentence: str
    clauses: tuple
    readings: tuple = ()

    @property
    def result(self):
        if self.readings:
            values = [v for _, v in self.readings]
            if all(v is True for v in values):
                return True
            if all(v is False for v in values):
                return False
            return UnresolvedResult('whose easternness is required remains open',
                'Sahl, On Nativities ' + self.sentence, self.readings)
        value = doctrinal_and(*(v for _, v in self.clauses))
        if is_unresolved(value):
            return UnresolvedResult(('satisfied under the declared checks; remaining defects not fully assessed'
                                     if self.declared_checks_pass else 'partial / not fully assessed: ' + '; '.join(
                name + ': ' + v.reason for name, v in self.clauses if is_unresolved(v))),
                'Sahl, On Nativities ' + self.sentence,
                (('requirements satisfied', True), ('requirements not satisfied', False)))
        return value

    @property
    def declared_checks_pass(self):
        """The ruling's middle state: every declared check True, only the
        standing 'remaining defects' clause open (the checked list is not
        exhaustive, so the promise is never presented as verified)."""
        open_ = [(n, v) for n, v in self.clauses if is_unresolved(v)]
        return (bool(open_) and all(n.endswith('remaining defects') for n, _ in open_)
                and all(v is True for n, v in self.clauses if not is_unresolved(v)))

    @property
    def evidence(self):
        def text(v):
            return 'met' if v is True else 'not met' if v is False else 'unresolved — ' + v.reason
        return '; '.join(name + ': ' + text(value) for name, value in self.clauses + self.readings)


def _prosperity_unassessed(reason, source):
    return UnresolvedResult(reason, source, (('satisfied', True), ('not satisfied', False)))


def _prosperity_eastern(planet, natal):
    r = natal.get(planet)
    if r is None:
        return _prosperity_unassessed('planetary position unavailable', 'Sahl, On Nativities 1.22')
    speed = r.get('speed_in_lon')
    result = sahl_eastern_1_22(planet, r['longitude'], natal['Sun']['longitude'],
                             0.0 if speed is None else speed).level2
    if planet in ('Venus', 'Mercury') and speed is None and result.status == 'unresolved':
        return _prosperity_unassessed('direct motion not assessed', result.source)
    if result.status == 'not applicable':
        # The excerpts supply no easternness rule for a Sun serving as the Lot's
        # lord (G14-A): unassessed, never a definite failure.
        return _prosperity_unassessed("no easternness criterion for the Sun as the Lot's lord", result.source)
    return result.value


def _prosperity_lifetime(first, second, natal, connections):
    clauses = []
    for f in (first, second):
        lord, lon = f['lord'], natal[f['lord']]['longitude']
        afflictions = []
        for malefic in ('Saturn', 'Mars'):
            if malefic == lord:
                continue
            if malefic not in natal:
                afflictions.append((malefic + ' position', _prosperity_unassessed(
                    'infortune position unavailable', 'Sahl, Introduction 3, 81')))
                continue
            # Introduction 3, 81's three configurations are all relations of the
            # infortune to the planet: with it in its sign (fn 93: assembling --
            # co-presence, not the planet merely standing in the infortune's
            # domicile), connecting with it (F6's Sahl predicate), or looking at
            # it from a square or opposition (by whole sign). The hit is named.
            look = _prosperity_looks(lon, natal[malefic]['longitude'])
            with_it = look == 'with'
            square_opposition = look in ('square', 'opposition')
            pair = next((r for r in connections if {r['p1'], r['p2']} == {lord, malefic}), None)
            connected = (_is_connected_sahl(pair) if pair is not None else
                         _prosperity_unassessed('connection not assessed', 'Sahl, Introduction 3, 81'))
            bad = doctrinal_or(with_it, square_opposition, connected)
            if is_unresolved(bad):
                clear = bad
            elif bad:
                clear = False
            else:
                clear = True
            hit = ('with it in one sign' if with_it else look if square_opposition else
                   'connecting' if connected is True else '')
            afflictions.append((lord + ' clear of ' + malefic + (' — ' + hit if hit else ''), clear))
        speed = natal[lord].get('speed_in_lon')
        clauses.extend([(lord + ' in a stake by whole sign', f['house'] in PROSPERITY_STAKES),
                        *afflictions,
                        (lord + ' free of retrogradation', speed >= 0.0 if speed is not None else
                         _prosperity_unassessed('motion unavailable', 'Sahl, On Nativities 1.22, 10')),
                        (lord + ' clear of Sahl rays', not f['under']),
                        (lord + ' outside fall', get_zodiac_sign(lon) not in FALLS.get(lord, [])),
                        (lord + ' remaining defects', _prosperity_unassessed(
                            'defects not fully assessed; the checked list is not exhaustive', 'Sahl, On Nativities 2.3, 2'))])
    return ProsperityAssessment('2.3, 2', tuple(clauses))


def _prosperity_lot_assessments(natal, asc, lot, lord_strength=None, witness_strength=None):
    """Strength arguments are assessed facts for specification fixtures.

    The chart path supplies neither: no unique definition was ruled for
    either strength phrase. Do not replace them with a place list/score.
    """
    lord = SIGN_TO_DOMICILE[get_zodiac_sign(lot)]
    ll = natal.get(lord)
    if ll is None:
        return {}
    lon = ll['longitude']
    east = {p: _prosperity_eastern(p, natal) for p in set(FORTUNES) | {lord}}
    clear = not _prosperity_infortunes_on(lon, natal, exclude=(lord,))
    rays = solar_phase(lord, lon, natal['Sun']['longitude'], ll.get('speed_in_lon'), source='Sahl')[0]
    if lord_strength is None:
        lord_strength = _prosperity_unassessed('strong position untested', 'Sahl, On Nativities 2.3, 7; fn 149')
    powerful = witness_strength or {}
    witnesses, relief = [], []
    for p in sorted(FORTUNES):
        if p == lord or p not in natal:
            continue
        pos = natal[p]['longitude']
        looks_lord = _prosperity_looks(pos, lon) is not None
        looks_lot = _prosperity_looks(pos, lot) is not None
        strength = powerful.get(p, _prosperity_unassessed(
            p + ' powerful position untested', 'Sahl, On Nativities 2.3, 7'))
        witnesses.append((p + ' witnesses Lot and lord from a powerful position',
                          doctrinal_and(looks_lord, looks_lot, strength)))
        relief.append((p, looks_lord and get_wsh_house(pos, asc) in EXCELLENT_PLACES))
    a7 = ProsperityAssessment('2.3, 7', (
        ('Lot in a stake or following place by whole sign', get_wsh_house(lot, asc) not in PROSPERITY_FALLING),
        (lord + ' easternizes (1.22, second level)', east[lord]),
        (lord + " clear of the Sun's rays", rays not in ('Burned', 'Under the rays')),
        (lord + ' clear of infortunes (with, square or opposition by whole sign)', clear),
        (lord + ' looks at the Lot', _prosperity_looks(lon, lot) is not None),
        (lord + ' strong position', lord_strength),
        ('one external benefic witnessing both', doctrinal_or(*(v for _, v in witnesses))),
        ('no infortune looking at the Lot', not _prosperity_looking(lot, natal, sorted(INFORTUNES))),
    ))
    a9 = ProsperityAssessment('2.3, 9', ((lord + ' in the fifth or eleventh', get_wsh_house(lon, asc) in (5, 11)),))
    fortune_reading = doctrinal_and(not clear, doctrinal_or(*(doctrinal_and(place, east[p]) for p, place in relief)))
    lord_reading = doctrinal_and(not clear, doctrinal_or(*(place for p, place in relief)), east[lord])
    a16 = ProsperityAssessment('2.16, 2',
        ((lord + ' afflicted by infortunes', not clear),
         ('external fortune looks from one of the six excellent places', doctrinal_or(*(v for _, v in relief)))),
        (('fortune eastern', fortune_reading), ('lord eastern', lord_reading)))
    return {'2.3, 7': a7, '2.3, 9': a9, '2.16, 2': a16}


def evaluate_prosperity(chart_data, *, _lot_completion=None):
    """Sahl, On Nativities Ch. 2, the indications of fortune and livelihood,
    read by whole sign. The first row is the app's synthesis -- the class
    number it reads (2.1, 2-9) with its grounds, the partnering lord's
    modification (2.11, 4) and the Lot step (2.3, 6) -- and the rows under
    it are the lords, the partnering lord, and every further rule read,
    each with Sahl's sentence and the parallel in the Book of Aristotle or
    Abu 'Ali. Keys: 'Class', 'Ground', 'Sahl', 'Also', plus 'key' (the
    class word the tests read) and 'Supplement' (True for a step only Abu
    'Ali or BA state). Display only; nothing scores it. Abu 'Ali's twelve
    charts (JN Ch. 7, Figures 10-21) are the fixtures in the tests."""
    natal, sect, asc = chart_data['planetary_data'], chart_data['sect'], chart_data['ascendant']
    cusps = chart_data.get('houses')
    lot = chart_data.get('lot_of_fortune')
    light = 'Sun' if sect == 'Diurnal' else 'Moon'
    if light not in natal or 'Sun' not in natal:
        return []
    sun_lon = natal['Sun']['longitude']
    light_sign = get_zodiac_sign(natal[light]['longitude'])
    lords = _triplicity_lords_in_sect_order(light_sign, sect, table='Sahl')
    rows = []

    def row(key, cls, ground, sahl, also, supplement=False):
        rows.append({'Class': cls, 'Ground': ground, 'Sahl': sahl, 'Also': also, 'key': key,
                     'Supplement': supplement, 'Triplicity table': TRIPLICITY_TABLE_SOURCES['Sahl'],
                     'Virgo source note': VIRGO_PARTNER_NOTE if light_sign == 'Virgo' and (not rows or key in ('lords', 'third')) else ''})

    def sahl(*refs):
        return ' '.join(f'{r}: "{PROSPERITY_SAHL[r]}"' for r in refs)

    # --- the lords of the sect light's triplicity, by place (2.11, 1-5) ---
    facts = {}
    for rank, lord in zip(('first', 'second', 'third'), lords):
        r = natal.get(lord)
        if r is None:
            facts[rank] = None
            continue
        lon = r['longitude']
        house = get_wsh_house(lon, asc)
        phase, _side, _el = solar_phase(lord, lon, sun_lon, r.get('speed_in_lon'), source='Sahl')
        under = phase in ('Burned', 'Under the rays')
        falling = house in PROSPERITY_FALLING
        strong = not falling and not under
        infortunes = _prosperity_infortunes_on(lon, natal, exclude=(lord,))
        # 2.3, 17-18 are sign-against-degree rules, read from the quadrant
        # cusps when the chart carries them: 17, a stake by sign but "falling
        # from the stakes by degrees"; 18, "in what follows the stake, or
        # falling in the sign, and in the stake [by] degrees". Their own rows;
        # the spine reads the whole-sign place alone.
        quadrant = get_house_number(lon, cusps) if cusps and len(cusps) >= 12 else None
        facts[rank] = {'lord': lord, 'house': house, 'strong': strong, 'under': under, 'falling': falling,
                       'infortunes': infortunes, 'word': _prosperity_place_word(house), 'sign': get_zodiac_sign(lon),
                       'quadrant': quadrant,
                       'degree_house': get_effective_house(lon, cusps, angles_only=True) if quadrant is not None else None,
                       # weak (2.11, 3 and 5) or with an infortune on it (2.20, 1's gloss): "made
                       # unfortunate" for 2.3, 6's turn to the Lot, the app's reading
                       'unfortunate': (not strong) or bool(infortunes)}
    first, second, third = facts['first'], facts['second'], facts['third']
    if first is None or second is None:
        return []

    def describe(f):
        s = f"{f['lord']} in {f['sign']}, the {_prosperity_nth(f['house'])}, {f['word']}"
        if f['under']:
            s += ', under the rays (no strength, 2.11, 5)'
        s += ('; infortunes on it: ' + ', '.join(f['infortunes'])) if f['infortunes'] else '; no infortune with it or in its square or opposition'
        if f['quadrant'] is not None:
            s += f"; raw quadrant {f['quadrant']}; degree-strength house {f['degree_house']} (5° before four stakes, inclusive)"
        return s

    def weakness(f):
        """The weakness named per lord: "falling (2.11, 3)", "under the rays
        (2.11, 5)", or both -- never one word for two lords."""
        words = []
        if f['falling']:
            words.append('falling (2.11, 3)')
        if f['under']:
            words.append('under the rays (2.11, 5)')
        return f"{f['lord']} " + ' and '.join(words)

    def read_as(key):
        n = {'high': 1, 'high to low': 2, 'middling': 3, 'low to high': 5, 'low': 6}[key]
        return f"read by this app as class {n}, {PROSPERITY_CLASSES[key][0].lower() + PROSPERITY_CLASSES[key][1:]}"

    lords_text = (f"The {light}, the sect light, in {light_sign}; its lords {lords[0]}, {lords[1]}, {lords[2]}. "
                  f"First: {describe(first)}. Second: {describe(second)}.")
    both_stakes = first['house'] in PROSPERITY_STAKES and second['house'] in PROSPERITY_STAKES
    if first['strong'] and second['strong']:
        key = 'high'
        grounds = ("both lords strong" + (", both in the stakes" if both_stakes else ", in stakes or what follows")
                   + ": high rank from the beginning of his life to its end (2.11, 1), by this app's place-and-rays reading")
        refs = ('2.11, 1',)
        also = PROSPERITY_ALSO['angles' if both_stakes else 'succedent']
    elif not first['strong'] and not second['strong']:
        key = 'low'
        grounds = (f"both lords weak -- {weakness(first)}; {weakness(second)}: baseness from the beginning of his "
                   "life to its end (2.11, 3")
        if first['under'] or second['under']:
            grounds += ("; its word is falling, and a lord under the rays, which has no strength by 2.11, 5, is read "
                        "with it by this app")
        grounds += ")"
        refs = ('2.11, 3', '2.11, 5') if (first['under'] or second['under']) else ('2.11, 3',)
        also = PROSPERITY_ALSO['cadent']
    elif first['strong']:
        key = 'high to low'
        grounds = (f"the first lord strong, the second weak -- {weakness(second)}: benefit in the time of the strong "
                   "one (2.11, 2)")
        refs = ('2.11, 2', '2.13, 39')
        also = PROSPERITY_ALSO['mixed']
    else:
        key = 'low to high'
        grounds = (f"the first lord weak -- {weakness(first)} -- the second strong: benefit in the time of the strong "
                   "one (2.11, 2)")
        refs = ('2.11, 2', '2.13, 39')
        also = PROSPERITY_ALSO['mixed']
    timing = (", the first lord's time being the beginning of life (2.13, 39)" if key in ('high to low', 'low to high')
              else '')
    lords_key = key
    lifetime = _prosperity_lifetime(first, second, natal,
        _pairwise_configurations({p: r for p, r in natal.items() if p in PN4_SEVEN and r.get('speed_in_lon') is not None},
                                 chart_data.get('encounter_history')))
    grounds += ". Lifelong happiness (2.3, 2): " + (
        'met' if lifetime.result is True else 'not met' if lifetime.result is False else
        'satisfied under the declared checks; remaining defects not fully assessed' if lifetime.declared_checks_pass
        else 'not fully assessed')
    grounds += ' — ' + lifetime.evidence
    ground = grounds
    deciding = sahl(*refs)

    # --- the Lot of Fortune (2.3, 6-9; 2.16; 2.20) ---
    lot_rows = []
    lot_assessments = {}
    conditional_lot_rows = []
    lot_house = lot_lord = lot_lord_house = None
    if lot is not None:
        lot_house = get_wsh_house(lot, asc)
        lot_sign = get_zodiac_sign(lot)
        lot_lord = SIGN_TO_DOMICILE[lot_sign]
        ll = natal.get(lot_lord)
        lot_infortunes = _prosperity_infortunes_on(lot, natal)
        fortunes_on_lot = _prosperity_looking(lot, natal, sorted(FORTUNES))
        infortunes_on_lot = _prosperity_looking(lot, natal, sorted(INFORTUNES))
        lot_text = (f"the Lot of Fortune in {lot_sign}, the {_prosperity_nth(lot_house)}, {_prosperity_place_word(lot_house)}; "
                    f"its lord {lot_lord}")
        if ll is not None:
            lot_lord_house = get_wsh_house(ll['longitude'], asc)
            _p, side, _e = solar_phase(lot_lord, ll['longitude'], sun_lon, ll.get('speed_in_lon'))
            lord_looks = _prosperity_looks(ll['longitude'], lot)
            lord_infortunes = _prosperity_infortunes_on(ll['longitude'], natal, exclude=(lot_lord,))
            lord_in_fall = get_zodiac_sign(ll['longitude']) in FALLS.get(lot_lord, [])
            lot_text += (f" in {get_zodiac_sign(ll['longitude'])}, the {_prosperity_nth(lot_lord_house)}, {side or 'the Sun'}, "
                         f"{'looking at the Lot by ' + lord_looks if lord_looks and lord_looks != 'with' else ('with the Lot' if lord_looks else 'not looking at the Lot')}"
                         + ('; infortunes on it: ' + ', '.join(lord_infortunes) if lord_infortunes else '; cleansed by whole sign')
                         + ('; in its fall' if lord_in_fall else ''))
            # 2.20, 1-2: misery confirmed
            lot_bad_place = lot_house in (6, 12) and bool(lot_infortunes)
            lord_bad = lord_in_fall or bool(lord_infortunes)
            mars_day = (sect != 'Diurnal') or ('Mars' in natal and _prosperity_looks(natal['Mars']['longitude'], lot) in ('with', 'square', 'opposition'))
            if lot_bad_place and lord_bad and mars_day:
                lot_rows.append(('low', 'miserable', f"the Lot in the sixth or twelfth with {', '.join(lot_infortunes)}; its lord "
                                 f"{'in its fall' if lord_in_fall else 'made unfortunate (' + ', '.join(lord_infortunes) + ')'}"
                                 + ("; Mars on the Lot by day" if sect == 'Diurnal' else '') + '; ' + lot_text,
                                 sahl('2.20, 1'), PROSPERITY_ALSO['misery'],
                                 "he will not cease to be miserable from the day he is born up to the day he dies (2.20, 1)", '2.20, 1'))
            jv_bad = all(p in natal and get_wsh_house(natal[p]['longitude'], asc) in (6, 12)
                         and _prosperity_infortunes_on(natal[p]['longitude'], natal) for p in ('Jupiter', 'Venus'))
            jv_moon = 'Moon' in natal and all(_prosperity_looks(natal[p]['longitude'], natal['Moon']['longitude']) is None for p in ('Jupiter', 'Venus') if p in natal)
            if lot_lord_house in (6, 12) and jv_bad and jv_moon:
                lot_rows.append(('low', 'miserable', "the lord of the Lot in the sixth or twelfth; Jupiter and Venus made unfortunate there, "
                                 "not looking at the Moon; " + lot_text, sahl('2.20, 2'), PROSPERITY_ALSO['misery'],
                                 "he will be miserable (2.20, 2)", '2.20, 2'))
            lot_assessments = _prosperity_lot_assessments(natal, asc, lot)
            for ref, level, label, promise in (
                ('2.3, 7', 'high', 'good fortune', 'the native will be a king, or prominent, and a powerful noble'),
                ('2.3, 9', 'high', 'good fortune', 'he will be happy'),
                ('2.16, 2', 'middling', 'middling', 'his good fortune will be in the middle'),
            ):
                assessment = lot_assessments[ref]
                result = assessment.result
                detail = assessment.evidence
                if ref == '2.3, 7':
                    detail += "; Dykes's proposed and adopted (fn 87); especially (fn 88) remains an unresolved enhancement"
                if ref == '2.3, 9':
                    detail += '; ' + ('more excellent: the lord looks at the Lot' if lord_looks is not None else
                                     'happy even without looking at the Lot')
                entry = (level, label, detail + '; ' + lot_text, sahl(ref),
                         PROSPERITY_ALSO['middling' if level == 'middling' else 'lot'], promise + ' (' + ref + ')', ref)
                if is_unresolved(result):
                    conditional_lot_rows.append((entry, assessment))
                admitted = (_lot_completion.get(ref, False) if _lot_completion is not None and is_unresolved(result)
                            else result is True)
                if admitted:
                    lot_rows.append(entry)
        if len(fortunes_on_lot) == len([p for p in FORTUNES if p in natal]) == 2 and len(infortunes_on_lot) == 2:
            lot_rows.append(('middling', 'middling', "both fortunes and both infortunes looking at the Lot; " + lot_text,
                             sahl('2.16, 4'), PROSPERITY_ALSO['middling'], "his livelihood will be in the middle (2.16, 4)", '2.16, 4'))

    # --- the Lot step (2.3, 6): "if you found the lord of the triplicity of
    # the Sun made unfortunate, then you must work with the Lot of Fortune".
    # The entry is one lord made unfortunate -- either lord weak, or with an
    # infortune on it -- the app's reading of the sentence's singular (which
    # states no quantifier). The Lot's sentences give judgments of the native
    # (2.3, 7 a king; 2.3, 9 happy; 2.16, 2 and 4 the middle; 2.20, 1-2
    # misery), and Sahl nowhere says how they combine with Theophilus's
    # two-lord judgment (2.11, 1-3): no replacement, no threshold by the
    # number of weak lords, no priority. So the synthesis carries BOTH
    # judgments, each with its sentence -- a level beside a timing pattern
    # (the mixed pair) is mixed by design; the same level twice is
    # concordant; opposed levels, or the Lot's own sentences at two levels,
    # are unresolved -- and says the combination is this app's. (2.3, 7
    # cannot hold with 2.16, 4, 2.16, 2 or 2.20, 1 by predicate; 2.3, 9 can
    # hold with 2.16 or 2.20, and then no order is installed.)
    made_unfortunate = [f['lord'] for f in (first, second) if f['unfortunate']]
    lot_judgments = []                    # (level, clause) per Lot sentence met
    if made_unfortunate:
        who = ' and '.join(made_unfortunate) + ' made unfortunate'
        entry = (f"; the Lot step (2.3, 6), {who} -- the either-lord entry being this app's reading of the sentence's "
                 "singular")
        if lot is None:
            ground += entry + ": no Lot of Fortune in hand"
        elif not lot_rows:
            ground += entry + f": none of 2.20, 1-2, 2.3, 7 and 9, 2.16, 2 and 4 met ({lot_text})"
        else:
            ground += (entry + ": " + "; ".join(f"{cg.replace('; ' + lot_text, '')} -- {cv}" for _k, _w, cg, _s, _a, cv, _r in lot_rows)
                       + f" ({lot_text})")
            deciding += ' ' + ' '.join(cs for _k, _w, _g, cs, _a, _v, _r in lot_rows)
            middle_refs = [r for _k, _w, _g, _s, _a, _v, r in lot_rows if r.startswith('2.16')]
            for ck, _w, _g, _s, _a, _v, r in lot_rows:
                clause = {'2.3, 7': "the Lot promises very high rank, a king, or prominent, and a powerful noble (2.3, 7)",
                          '2.3, 9': "the Lot's lord promises happiness (2.3, 9)",
                          '2.20, 1': "the Lot indicates misery from birth to death (2.20, 1)",
                          '2.20, 2': "the Lot's lord indicates misery (2.20, 2)"}.get(r)
                if clause is None:        # 2.16, 2 and 4: one statement, both may support it, no order between them
                    if r != middle_refs[0]:
                        continue
                    clause = f"the Lot indicates middling livelihood ({'; '.join(middle_refs)})"
                lot_judgments.append((ck, clause))
        deciding += ' ' + sahl('2.3, 6')

    two_lord = {'high': "the two strong triplicity lords indicate high rank from the beginning of his life to its end (2.11, 1)",
                'low': ("the two weak triplicity lords indicate baseness throughout life (2.11, 3, read with 2.11, 5)"
                        if (first['under'] or second['under']) else
                        "the two falling triplicity lords indicate baseness throughout life (2.11, 3)"),
                'high to low': "the two triplicity lords indicate benefit in the first lord's time and hardship in the second's (2.11, 2)",
                'low to high': "the two triplicity lords indicate hardship in the first lord's time and benefit in the second's (2.11, 2)"}[lords_key]
    class_n = {'high': 1, 'high to low': 2, 'low to high': 5, 'low': 6}[lords_key]
    no_precedence = ("the combination is this app's, Sahl giving no express precedence between the Lot's sentences and "
                     "Theophilus's")
    if not lot_judgments:
        key = lords_key
        class_field = f"class {class_n}"
        ground += f". Synthesis: {two_lord} -- {read_as(lords_key)}{timing}"
    else:
        levels = sorted({lv for lv, _c in lot_judgments})
        lot_clause = '; '.join(c for _lv, c in lot_judgments)
        level_word = {'high': {'2.3, 7': 'very high rank', '2.3, 9': 'happiness'}, 'middling': 'middling', 'low': 'misery'}
        if len(levels) > 1:
            key = 'unresolved'
            class_field = UnresolvedResult(
                reason="the Lot's own sentences give different status levels and Sahl states no priority",
                source='Sahl, On Nativities 2.3, 6-9; 2.11, 1-3; 2.16, 2-5; 2.20, 1',
                alternatives=tuple((f'Lot judgment {n}', clause)
                                   for n, (_level, clause) in enumerate(lot_judgments, 1))
                             + (('triplicity lords', two_lord),),
            )
            ground += (f". Synthesis: the Lot's own sentences disagree -- {lot_clause} -- and {two_lord} -- unresolved: "
                       "this app installs no priority among the Lot's sentences nor between them and Theophilus's, Sahl "
                       "giving none; every judgment stands")
        else:
            level = levels[0]
            word = level_word[level] if level != 'high' else ('very high rank' if any('2.3, 7' in c for _l, c in lot_judgments) else 'happiness')
            if lords_key in ('high to low', 'low to high') and level != 'low':
                # a middling or high level beside the timing pattern: mixed by
                # design (2.16, 5 couples a middling condition with variation);
                # 2.20's misery "from the day he is born up to the day he dies"
                # admits no time of benefit, so that pairing is a conflict below
                key = 'mixed'
                class_field = f"{word} by the Lot; class {class_n}'s pattern by the lords"
                ground += (f". Synthesis: {lot_clause}; {two_lord} -- {word} by the Lot; class {class_n}'s pattern by the "
                           f"lords, {PROSPERITY_CLASSES[lords_key][0].lower() + PROSPERITY_CLASSES[lords_key][1:]}{timing}; "
                           f"{no_precedence}")
            elif level == lords_key:
                key = lords_key
                class_field = f"class {class_n}"
                ground += f". Synthesis: {lot_clause}; {two_lord} -- concordant, {read_as(lords_key)}; {no_precedence}"
            else:
                key = 'unresolved'
                class_field = UnresolvedResult(
                    reason='the Lot and the triplicity lords give conflicting status indications and Sahl states no priority',
                    source='Sahl, On Nativities 2.3, 6-9; 2.11, 1-3; 2.16, 2-5; 2.20, 1',
                    alternatives=(('Lot judgment', lot_clause), ('triplicity lords', two_lord)),
                )
                ground += (f". Synthesis: conflicting status indications -- {lot_clause}; {two_lord} -- unresolved: this "
                           f"app installs no priority between them, Sahl giving no express precedence between the Lot's "
                           "sentences and Theophilus's; both judgments stand")

    if _lot_completion is None and conditional_lot_rows:
        # The two conditional sentences cannot coexist: 2.3, 7 requires a
        # cleansed lord, 2.16, 2 an afflicted one. Thus these completions do
        # not independently assign a shared eastern fact contradictory values.
        if made_unfortunate:
            from itertools import product
            refs_open = [entry[-1] for entry, _assessment in conditional_lot_rows]
            alternatives = []
            for values in product((False, True), repeat=len(refs_open)):
                completion = dict(zip(refs_open, values))
                top = evaluate_prosperity(chart_data, _lot_completion=completion)[0]
                name = '; '.join(ref + (' satisfied' if value else ' not satisfied') for ref, value in completion.items())
                alternatives.append((name, top['Class']))
            if not all(repr(v) == repr(alternatives[0][1]) for _, v in alternatives):
                key = 'unresolved'
                class_field = UnresolvedResult('partial or conditional Lot judgments can change the synthesis',
                    'Sahl, On Nativities 2.3, 7; 2.16, 2', tuple(alternatives))
            # The earlier "none met" applies only to definite rows; do not
            # leave it implying that every passage has been decided.
            ground = ground.replace('none of 2.20, 1-2, 2.3, 7 and 9, 2.16, 2 and 4 met',
                                    'no fully established Lot judgment; remaining assessments are conditional')
            ground += '. Conditional Lot assessments: ' + '; '.join(
                entry[-1] + ' — ' + assessment.evidence for entry, assessment in conditional_lot_rows)
            if is_unresolved(class_field):
                ground += '. Synthesis: unresolved — ' + class_field.reason

    # --- the partnering lord (2.11, 4), ranked third (2.3, 22): a stated
    # modifier, no class step ---
    if third is not None:
        if third['strong']:
            effect = "supports them both in their elevation, through its strength"
        elif third['falling']:
            effect = "brings [them] down, a falling place"
        else:
            effect = ("under the rays, no strength (2.11, 5): neither 2.11, 4's support, which wants strength, nor its "
                      "bringing down, which wants a falling place")
        ground += (f". Modified by the partnering lord (2.11, 4): {third['lord']} in {third['sign']}, the "
                   f"{_prosperity_nth(third['house'])}, {third['word']} -- {effect}; no class step")
        deciding += ' ' + sahl('2.11, 4')
    ground += ". The lords: " + lords_text[0].lower() + lords_text[1:]

    row(key, (class_field if isinstance(class_field, UnresolvedResult)
              else 'Synthesis (this app): ' + class_field), ground, deciding, also)
    row('lifetime', lifetime.result if is_unresolved(lifetime.result) else
        ('Lifelong happiness: met' if lifetime.result else 'Lifelong happiness: not met'),
        lifetime.evidence, sahl('2.3, 2'), PROSPERITY_ALSO['angles'])
    for entry, assessment in conditional_lot_rows:
        if _lot_completion is not None:
            continue
        row('lot conditional', assessment.result, entry[2] + ('' if made_unfortunate else
            "; listed: the Lot step is inactive; this does not change the two-lord synthesis"), entry[3], entry[4])
    row('lords', 'The lords, by place', lords_text
        + " Strong is a stake or what follows one, falling the third, sixth, ninth and twelfth (fn 149), by whole "
          "sign; under the rays is 2.11, 5's no strength. The infortunes on a lord are listed: 2.11, 4 makes their "
          "aspect an increase or a subtraction, not a class step, and a lord with one is made unfortunate for 2.3, "
          "6's turn to the Lot.",
        sahl('2.3, 1', '2.13, 40', '2.11, 5'), PROSPERITY_ALSO['frame'])
    if third is not None:
        row('third', 'The partnering lord', f"Third: {describe(third)} -- {effect}; ranked third (2.3, 22)"
            + (f"; in the house of marriage: good fortune at the end of his lifespan" if third['house'] == 7 else ''),
            sahl('2.11, 4', '2.3, 22') + (' ' + sahl('2.19, 5') if third['house'] == 7 else ''), PROSPERITY_ALSO['third'])
    for ck, cw, cg, cs, ca, _cv, _r in lot_rows:
        row('lot ' + ck, f'The Lot: {cw}', cg + ('' if made_unfortunate else
            "; listed: 2.3, 6's turn to the Lot is not met, neither lord being made unfortunate"), cs, ca)

    # --- by sign and by degree (2.3, 17-18), from the quadrant cusps: a
    # second measure, its own rows, never folded into strong or weak ---
    for f in (first, second, third):
        if f is None or f['quadrant'] is None:
            continue
        q = _prosperity_nth(f['quadrant'])
        if f['house'] in PROSPERITY_STAKES and f['degree_house'] in (3, 6, 9, 12):
            row('by degree', 'By sign and by degree', f"{f['lord']} in the {_prosperity_nth(f['house'])}, a stake by sign, "
                f"and falling from the stakes by degrees (the {q} by the quadrant cusps): reputation, but it corrupts "
                "assets. A second measure beside the whole-sign place; it moves nothing.", sahl('2.3, 17'), PROSPERITY_ALSO['frame'])
        elif f['house'] not in PROSPERITY_STAKES and f['degree_house'] in (1, 4, 7, 10):
            row('by degree', 'By sign and by degree', f"{f['lord']} in the {_prosperity_nth(f['house'])}, {f['word']} by sign, "
                f"and in the stake by degrees (raw quadrant {q}; strength house {f['degree_house']}, inclusive 5° before the four stakes): assets and a fine condition, without "
                "fame. A second measure beside the whole-sign place; it moves nothing.", sahl('2.3, 18'), PROSPERITY_ALSO['frame'])

    # --- the fifteen degrees by ascensions (2.13, 48-51): a third measure ---
    armc, obl, glat = chart_data.get('armc'), chart_data.get('obliquity'), chart_data.get('geo_lat')
    if armc is not None and obl is not None and glat is not None:
        for rank, f in (('first', first),):
            got = _prosperity_ascension_from_stake(natal[f['lord']]['longitude'], asc, armc, obl, glat, chart_data.get('mc'))
            if got is None:
                continue
            stake, arc = got
            band = next((i for i, edge in enumerate((15.0, 30.0, 45.0)) if arc <= edge), 3)
            verdict, ref = [("the first 15°: praise and good fortune", '2.13, 48'), ("the second 15°: below the first", '2.13, 49'),
                            ("the third 15°: the middle of assets", '2.13, 50')][band] if band < 3 else ("beyond 45°, up to the next stake: of the nativities of the poor", '2.13, 51')
            row('ascensions', 'Grade by ascensions', f"The {rank} lord {f['lord']}: {arc:.1f}° of ascension after {stake} -- {verdict}. "
                "The preceding axis is selected in zodiacal order, then measured by OA at the Ascendant, OD at the seventh, RA at the Midheaven/fourth; inclusive earlier edges at 15°, 30°, 45°. A third measure beside whole-sign strength, never folded into it.",
                sahl(ref) + (' ' + sahl('2.16, 3') if band == 2 else ''), PROSPERITY_ALSO['ascensions'])

    # --- the eleventh from the Ascendant (2.3, 12) ---
    eleventh = [p for p in ('Saturn', 'Jupiter', 'Mars', 'Venus') if p in natal and get_wsh_house(natal[p]['longitude'], asc) == 11]
    for p in eleventh:
        row('eleventh', 'The eleventh from the Ascendant', f"{p} in the eleventh: "
            + ("increases his good fortune" if p in FORTUNES else "takes away from his good and increases in evil"),
            sahl('2.3, 12'), "PN I, BA III.2.1 [1.7]: the benevolents in the eleventh increase, the infortunes diminish")

    # --- the middle: 2.16, 5, listed (fnn 225-226 dispute the third place) and 2.16, 6 (BA's, the supplement) ---
    mc_lon = chart_data.get('mc', (asc + 270.0) % 360.0)
    place_lords = []
    for label, lon in (('the Ascendant', asc), ('the Midheaven', mc_lon), ('the house of hope', asc + 300.0)):
        lord = SIGN_TO_DOMICILE[get_zodiac_sign(lon)]
        if lord in natal:
            bad = _prosperity_infortunes_on(natal[lord]['longitude'], natal, exclude=(lord,))
            place_lords.append((label, lord, bad))
    if place_lords and any(b for _l, _p, b in place_lords) and any(not b for _l, _p, b in place_lords):
        row('motley', 'The middle: a motley mixture', "; ".join(
            f"the lord of {l}, {p}: " + (', '.join(b) if b else 'cleansed by whole sign') for l, p, b in place_lords)
            + ". Listed, not classed: the third place is disputed (fnn 225-226: the second house).",
            sahl('2.16, 5'), "PN I, BA III.2.3 [3.4]")
    other = 'Moon' if light == 'Sun' else 'Sun'
    if other in natal:
        o_lord = _triplicity_lords_in_sect_order(get_zodiac_sign(natal[other]['longitude']), sect, table='Sahl')[0]
        if o_lord in natal:
            o_house = get_wsh_house(natal[o_lord]['longitude'], asc)
            if first['house'] in PROSPERITY_FALLING and o_house not in PROSPERITY_FALLING:
                row('middling supplement', 'The middle (supplement)',
                    f"the sect light's first lord {first['lord']} falling, the {other}'s first lord {o_lord} in the {_prosperity_nth(o_house)}, "
                    f"{_prosperity_place_word(o_house)}: someone in the middle. Bracketed in Sahl from BA; listed, not classed.",
                    sahl('2.16, 6') + ' (added by Dykes with BA III.2.3, 6)', "PN I, BA III.2.3 [3.5]", supplement=True)
    # 2.11, 14: the lord of the house of assets (the whole-sign second's
    # domicile lord) falling but not made unfortunate -- "what is middling of
    # assets". Listed, not classed; "falling" read as the place (fn 154 on
    # 2.11, 13 asks whether its fall is meant).
    assets_sign = get_zodiac_sign(asc + 30.0)
    assets_lord = SIGN_TO_DOMICILE[assets_sign]
    if assets_lord in natal:
        a_lon = natal[assets_lord]['longitude']
        a_house = get_wsh_house(a_lon, asc)
        a_bad = _prosperity_infortunes_on(a_lon, natal, exclude=(assets_lord,))
        if a_house in PROSPERITY_FALLING and not a_bad:
            row('assets', 'The middle: the lord of the house of assets',
                f"{assets_lord}, the lord of the house of assets ({assets_sign}), in {get_zodiac_sign(a_lon)}, the "
                f"{_prosperity_nth(a_house)}, falling from the stakes, no infortune with it or in its square or opposition: "
                "what is middling of assets. Listed, not classed; falling read as the place (fn 154 on 2.11, 13 asks "
                "whether its fall is meant).",
                sahl('2.11, 14'), PROSPERITY_ALSO['middling'])

    # --- falling (2.17) ---
    houses = {p: get_wsh_house(natal[p]['longitude'], asc) for p in natal}
    if any(houses.get(p) in (1, 10) for p in FORTUNES) and any(houses.get(p) == 7 for p in INFORTUNES):
        row('falling', 'Falling', "a fortune in the Ascendant or Midheaven ("
            + ', '.join(p for p in sorted(FORTUNES) if houses.get(p) in (1, 10)) + ") and an infortune in the house of marriage ("
            + ', '.join(p for p in sorted(INFORTUNES) if houses.get(p) == 7) + ")", sahl('2.17, 4'), PROSPERITY_ALSO['falling'] + "; BA III.2.2 [2.3]")
    # 2.17, 2-3: Sahl's own fall -- the lords in excellent places made
    # unfortunate; the Lot of Fortune or its lord likewise. Listed, not classing.
    for f in (first, second):
        if f['house'] in EXCELLENT_PLACES and f['infortunes']:
            row('falling', 'Falling', f"{f['lord']}, a lord of the sect light's triplicity, in the {_prosperity_nth(f['house'])} "
                f"with {', '.join(f['infortunes'])} on it; this app applies the plural to either lord; ray weakness remains separate", sahl('2.17, 2'), PROSPERITY_ALSO['falling'] + "; BA III.2.2 [2.1]")
    if lot is not None:
        lot_h = get_wsh_house(lot, asc)
        lot_inf = _prosperity_infortunes_on(lot, natal, exclude=())
        if lot_h in EXCELLENT_PLACES and lot_inf:
            row('falling', 'Falling', f"the Lot of Fortune in the {_prosperity_nth(lot_h)} with {', '.join(lot_inf)} on it",
                sahl('2.17, 3'), PROSPERITY_ALSO['falling'])
        if lot_lord_house in EXCELLENT_PLACES and lord_infortunes:
            row('falling', 'Falling', f"the Lot's lord {lot_lord} in the {_prosperity_nth(lot_lord_house)} with "
                + ', '.join(lord_infortunes) + ' on it; the lord is the alternative subject of this same rule',
                sahl('2.17, 3'), PROSPERITY_ALSO['falling'])
    for p in ('Saturn', 'Mars'):
        if houses.get(p) == 2:
            of_sect = (p == 'Saturn') == (sect == 'Diurnal')
            if not of_sect:
                row('falling', 'Falling', f"{p} in the house of assets, not of the sect (fn 232: not powerful is not of the sect)",
                    sahl('2.17, 5'), PROSPERITY_ALSO['falling'] + "; BA III.2.2 [2.4]")
        if houses.get(p) == 11:
            row('falling', 'Falling', f"{p} in the eleventh from the Ascendant; fn 234 is incomplete in the available text, ending at (which makes", sahl('2.17, 7'),
                PROSPERITY_ALSO['falling'] + "; BA III.2.2 [2.5]")
        if lot is not None and p in natal and _prosperity_signs_apart(lot, natal[p]['longitude']) == 10:
            row('falling', 'Falling', f"{p} in the eleventh from the Lot of Fortune; fn 234 is incomplete in the available text, ending at (which makes", sahl('2.17, 7'),
                PROSPERITY_ALSO['eleventh from the lot'])
    if 'Moon' in natal and 'Saturn' in natal and houses['Moon'] in PROSPERITY_STAKES and houses['Saturn'] == houses['Moon']:
        mars = 'Mars' in natal and _prosperity_looks(natal['Mars']['longitude'], natal['Moon']['longitude']) is not None
        row('falling', 'Falling', f"Saturn with the Moon in the {_prosperity_nth(houses['Moon'])}, a stake"
            + ("; Mars looking at it, worse" if mars else ''), sahl('2.17, 8'), PROSPERITY_ALSO['falling'] + "; BA III.2.2 [2.7]")
    if houses.get('Sun') in (6, 12):
        looking = _prosperity_looking(sun_lon, natal, ['Saturn', 'Mars'])
        if looking:
            row('falling', 'Falling', f"the Sun in the {_prosperity_nth(houses['Sun'])}, {', '.join(looking)} looking at him",
                sahl('2.17, 11'), PROSPERITY_ALSO['falling'] + "; BA III.2.2 [2.9]")
    sep, con = _prosperity_moon_motion(natal)
    if sep in FORTUNES and con in INFORTUNES:
        row('falling', 'Falling', f"the Moon separating from {sep} and connecting with {con}, by degree within her orb",
            sahl('2.17, 10'), PROSPERITY_ALSO['falling'] + "; BA III.2.2 [2.10]")

    # --- 2.3, 19-21: a lord of the triplicity in the second or eighth (owner: listed rows) ---
    for f in (first, second, third):
        if f is not None and f['house'] in (2, 8):
            row('falling', 'Decline', f"{f['lord']}, a lord of the sect light's triplicity, in the {_prosperity_nth(f['house'])}"
                + (" -- a fortune: his affairs set aright after their corruption" if f['lord'] in FORTUNES else ''),
                sahl('2.3, 19', '2.3, 20') if f['lord'] in FORTUNES else sahl('2.3, 19'), PROSPERITY_ALSO['falling'])
    if 'Jupiter' in natal and houses.get('Jupiter') in (2, 8) and 'Jupiter' not in lords:
        row('falling', 'Decline', f"Jupiter in the {_prosperity_nth(houses['Jupiter'])}, not a governor of the triplicity",
            sahl('2.3, 21'), PROSPERITY_ALSO['falling'])

    # --- rising (2.19) ---
    present_inf = [p for p in INFORTUNES if p in natal]
    present_for = [p for p in FORTUNES if p in natal]
    if present_inf and present_for and all(houses[p] in PROSPERITY_STAKES for p in present_inf) and all(houses[p] in PROSPERITY_FOLLOWING for p in present_for):
        row('rising', 'Rising', "the infortunes in the stakes and the fortunes in what follows them (every one of each)",
            sahl('2.19, 1'), PROSPERITY_ALSO['rising'] + "; BA III.2.4 [4.1]")
    if sep in INFORTUNES and con in FORTUNES:
        row('rising', 'Rising', f"the Moon separating from {sep} and connecting with {con}, by degree within her orb",
            sahl('2.19, 2'), PROSPERITY_ALSO['rising'] + "; BA III.2.4 [4.2]")
    if lot is not None and lot_lord_house is not None and lot_lord_house in EXCELLENT_PLACES:
        # 2.19, 6 (owner: a listed row, as 2.17, 7 is): planets in the bad places from the
        # Ascendant looking at the Lot, its lord in an excellent place. BA III.2.4 [4.5]
        # differs in the places ("from the east toward the Midheaven"), not the planets.
        bad_looking = [p for p in PN4_SEVEN if p in natal and houses.get(p) in PROSPERITY_FALLING
                       and _prosperity_looks(natal[p]['longitude'], lot) is not None]
        if bad_looking:
            row('rising', 'Rising', f"{', '.join(bad_looking)} in the bad places from the Ascendant, looking at the Lot of Fortune; "
                f"its lord {lot_lord} in the {_prosperity_nth(lot_lord_house)}, an excellent place -- a good livelihood at the end of life",
                sahl('2.19, 6'), PROSPERITY_ALSO['rising'] + "; BA III.2.4 [4.5], which has the stars \"from the east toward the Midheaven\"")

    # --- own hands, force and injustice (2.21) ---
    if lot is not None:
        lot_sign = get_zodiac_sign(lot)
        lot_lords = _triplicity_lords_in_sect_order(lot_sign, sect, table='Sahl')
        l1, l2 = lot_lords[0], lot_lords[1]
        looks = {p: (_prosperity_looks(natal[p]['longitude'], lot) if p in natal else None) for p in (l1, l2)}
        if l1 in natal:
            lon1 = natal[l1]['longitude']
            bound = next((lord for limit, lord in EGYPTIAN_TERMS[get_zodiac_sign(lon1)] if lon1 % 30 < limit), None)
            if bound in FORTUNES and get_wsh_house(lon1, asc) in EXCELLENT_PLACES and looks[l1] is not None:
                row('own hands', PROSPERITY_CLASSES['own hands'], f"{l1}, the first lord of the Lot's triplicity, in the bound of {bound}, "
                    f"in the {_prosperity_nth(get_wsh_house(lon1, asc))}, looking at the Lot", sahl('2.21, 1'), PROSPERITY_ALSO['hands'])
        if l1 in natal and l2 in natal and looks[l1] is None and looks[l2] is not None:
            row('squanders', 'Earns and destroys', f"{l1}, the first lord of the Lot's triplicity, not looking at the Lot; {l2}, the second, looking",
                sahl('2.21, 4'), PROSPERITY_ALSO['hands'] + " [6-7.2]")
        if l1 in natal and l2 in natal and looks[l1] is None and looks[l2] is None and not _prosperity_looking(lot, natal, sorted(FORTUNES)):
            row('foreign', 'From a foreign man', f"neither {l1} nor {l2}, the lords of the Lot's triplicity, looking at the Lot, nor a fortune; this app retains BA's reading (fn 268); the referent of the other fortune remains open",
                sahl('2.21, 2'), PROSPERITY_ALSO['hands'] + " [6-7.1] (fn 268: BA has him dependent on others); differing witness: Carmen has both fortunes looking at the Lot (fn 267)")
        if all(p in natal and _prosperity_signs_apart(lot, natal[p]['longitude']) == 10 for p in ('Saturn', 'Mars')):
            def own(p):
                s = get_zodiac_sign(natal[p]['longitude'])
                trip = triplicity_rulers(s, table='Sahl')
                return SIGN_TO_DOMICILE[s] == p or SIGN_TO_EXALTATION.get(s) == p or p in trip.values()
            if own('Saturn') and own('Mars'):
                row('injustice', PROSPERITY_CLASSES['injustice'], "Saturn and Mars in the eleventh from the Lot of Fortune, each in its own house, "
                    "triplicity or exaltation", sahl('2.21, 3'), PROSPERITY_ALSO['hands'] + " [6-7.3]; " + PROSPERITY_ALSO['eleventh from the lot'])
    return rows


def pn4_turning_triplicity_lords(chart_data, sr):
    """VI.2, 4-5: the triplicity lords examined beside the turning. 4, for
    assets: "every one of the lords of the triplicities of the luminary
    which had the shift in the root" (fn 13: the sect light), "what its
    condition was at that time, and what its condition is in the revolution
    of that year". 5, for siblings: "the triplicity lords of the sign in
    which Mars was at the root ... (and the first lord of its triplicity
    indicates the older ones of the siblings, and the second lord ... the
    middle ones ..., and the third ... the younger ones)". Six rows, the
    Great Introduction lords, assumed across works, in the engine's order (day lord, night lord, partner --
    by night the night lord first), each with its condition in both charts
    as the II.3 evaluators print it. fn 14's age mapping is the editor's
    and is not applied (order PN4R-4f-6, 2026-09-11)."""
    natal, rev = chart_data['planetary_data'], sr['planetary_data']
    sect = chart_data['sect']
    light = 'Sun' if sect == 'Diurnal' else 'Moon'
    rows = []
    for topic, planet, cite in (('assets', light, 'VI.2, 4 method (fn 13: the sect light)'), ('siblings', 'Mars', 'VI.2, 5 method')):
        if planet not in natal:
            continue
        sign = get_zodiac_sign(natal[planet]['longitude'])
        order = _triplicity_lords_in_sect_order(sign, sect, table='Great Introduction')
        for rank, lord in zip(('first', 'second', 'third'), order):
            rows.append({'Topic': topic, 'Triplicity of': f"{sign} ({planet} in the root)", 'Order': rank, 'Lord': lord,
                         'Root condition': _pn4_condition_string(natal, lord),
                         'Revolution condition': _pn4_condition_string(rev, lord),
                         'Siblings (5)': ({'first': 'the older', 'second': 'the middle', 'third': 'the younger'}[rank]
                                          if topic == 'siblings' else '-'),
                         'Source': f'PN IV {cite}; Great Introduction V.14 table assumed',
                         **triplicity_source_fields(sign, table='Great Introduction')})
    return rows

def pn4_turning_rows(chart_data, completed_years, elapsed=None):
    """The turning table of VI.2, 1-26 for one age: every planet, every
    house (with a displaced quadrant cusp turned a second way, VI.2, 22),
    and the Lots VI.2 names, each from its own natal position."""
    planetary = chart_data['planetary_data']
    asc, cusps, sect = chart_data['ascendant'], chart_data['houses'], chart_data['sect']
    age = int(completed_years)

    def row(point, natal_lon, topic, cite, directed):
        sign = pn4_turned_sign(natal_lon, age)
        return {
            'Point': point, 'For the knowledge of': topic,
            'Natal': f"{get_zodiac_sign(natal_lon)} ({get_degree_string(natal_lon)})",
            'Turned to': sign,
            'Its lord': SIGN_TO_DOMICILE.get(sign, '-'),
            'Natal planets there': _pn4_natal_planets_in_sign(planetary, sign),
            'Directed a year per degree': directed,
            'Source': cite,
        }

    # III.1, 12's third case for the planets and the Lots; VI.2, 21's
    # "portions of the hours and the right circle" (fn 33) for a cusp's
    # degree: the DEGREE directed by proportional semi-arcs, where it stands
    # at this age.
    def stand(natal_lon, significator=None):
        return pn4_semi_arc_stand(planetary, natal_lon, chart_data['obliquity'], chart_data['geo_lat'],
                                  chart_data['armc'], age if elapsed is None else elapsed, significator=significator)

    rows = []
    topics = pn4_turning_planet_topics(sect)
    for planet in PN4_SEVEN:
        if planet in planetary:
            rows.append(row(planet, planetary[planet]['longitude'], topics[planet], 'VI.2, 1-2',
                            stand(planetary[planet]['longitude'], significator=planet) + ' (III.1, 12 fn 16)'))

    directed_at = {1: 'the distribution from the Ascendant, above (III.1, 12)',
                   10: 'the distribution from the Midheaven, above (III.1, 12)',
                   4: 'the distribution from the fourth, above (III.1, 12)'}
    for house, topic, cite in PN4_TURNING_HOUSES:
        ws_start = ((asc // 30.0) * 30.0 + 30.0 * (house - 1)) % 360.0
        cusp = cusps[house - 1] if cusps and len(cusps) >= house else None
        # VI.2, 1, 21, 23, 25 direct a house from its cusp's degree; no
        # sentence directs the first degree of the whole sign. The
        # by-counting row therefore stands from the cusp when it shares
        # the sign, and reports the sign only when it does not (the
        # displaced cusp then gets its own row below).
        if house in directed_at:
            directed = directed_at[house]
        elif cusp is not None and get_zodiac_sign(cusp) == get_zodiac_sign(ws_start):
            directed = stand(cusp) + ' (VI.2, 21 fn 33; from the cusp\'s degree, ' + get_degree_string(cusp) + ')'
        else:
            directed = 'the sign by counting; its degree is directed in the row below (VI.2, 21-24)'
        rows.append(row(f'House {house} (by counting)', ws_start, topic, cite, directed))
        if cusp is not None and get_zodiac_sign(cusp) != get_zodiac_sign(ws_start):
            # VI.2, 21: the displaced cusp is directed "from the actual degree it rests in"
            rows.append(row(f'House {house} (its degree, {get_degree_string(cusp)}, in another sign)', cusp,
                            topic + ' -- the second turning, VI.2, 22 [2]', 'VI.2, 21-24',
                            stand(cusp) + ' (VI.2, 21 fn 33)'))

    for lot_id, topic, cite, note in PN4_TURNING_LOTS:
        d = next(x for x in LOT_DEFINITIONS if x['id'] == lot_id)
        lon = lot_by_id(lot_id, planetary, asc, cusps, sect)
        if lon is None:
            continue
        rows.append(row(d['name'] + (f' -- {note}' if note else ''), lon, topic, cite,
                        stand(lon) + ' (III.1, 12 fn 16)'))
    return rows

# --- II.1, 11-24: indicators 6-19 of the year, the FACT each one reads ----
# II.1, 5-24 list nineteen indicators and II.1, 25 ranks them. The first
# five are computed above. Of the rest, each READS a fact from the root
# and the revolution and then judges it in a chapter of its own; the
# facts are computed here, the judgments are not. Three kinds:
#   positional (6, 8, 9, 10, 12, 13, 14, 18, 19): a lookup on charts
#     already cast;
#   connection-dependent (7, 15): II.1, 12 supplies "so long as she is in her own sign".
#
#     The related Moon chapter uses a different expression: II.22, 1 says "her [current] sign".
#     That chapter requires perfection before sign exit, and VI.6, 3-7 concerns the
#     house lords' connections in the revolution; the engine's connection
#     test is a static one for the natal chart under the Configurations
#     page's rule and does not encode II.22's condition -- NOT computed,
#     and the row says so;
#   through the year (16, 17): continuous transits -- NOT tracked.
# Decided by the owner 2026-09-10 (positional nine, honest rows for the
# other four). Nothing here delineates.

def _pn4_transit_grade(sr_lon, natal_lon):
    """V.1, 2-3: a planet in the revolution "reaches its own rooted
    degree", or "the bound which it was in at the root", or "[only] that
    sign in which it was". None if not even the sign."""
    if get_zodiac_sign(sr_lon) != get_zodiac_sign(natal_lon):
        return None
    if abs((sr_lon - natal_lon + 180.0) % 360.0 - 180.0) < 1.0:
        return 'degree'
    if pn4_bound_lord(sr_lon) == pn4_bound_lord(natal_lon):
        return 'bound'
    return 'sign'

def _pn4_house_from(lon, from_lon):
    """Whole-sign house of `lon` counted from the sign of `from_lon`."""
    return get_wsh_house(lon, from_lon)

def _pn4_degree_connection(row):
    """Shared F6 membership/state for I.7 and indicator 15."""
    connected = _is_connected(row)
    return connected, _connection_state(row, connected)


def pn4_indicator_relationships(chart_data, sr, year_lon):
    """VI.6, 1–4, 40–44: one record per role and revolutionary pair."""
    pairs = _pairwise_configurations(sr['planetary_data'])
    records = []
    for role, origin in (('Lord of the natal Ascendant', chart_data['ascendant']),
                         ('Lord of the terminal sign', year_lon),
                         ("Lord of the revolution's Ascendant", sr['ascendant'])):
        sign = get_zodiac_sign(origin)
        indicator = SIGN_TO_DOMICILE[sign]
        ruled = {}
        for house in range(1, 13):
            owner = SIGN_TO_DOMICILE[SIGN_ORDER[(SIGN_ORDER.index(sign) + house - 1) % 12]]
            ruled.setdefault(owner, []).append(house)
        for pair in pairs:
            if indicator not in (pair['p1'], pair['p2']): continue
            partner = pair['p2'] if pair['p1'] == indicator else pair['p1']
            connected, state = _pn4_degree_connection(pair)
            averse = pair['aspect_name'] == 'Aversion'
            aspect = 'body connection' if averse else pair['aspect_name'].lower()
            distance = pair['dist'] if averse else abs(pair['deviation'])
            if is_unresolved(state):
                description = UnresolvedResult(state.reason, state.source, state.alternatives,
                    display_label=f"Unresolved — relationship with {partner}")
            elif averse and connected is not True:
                description = f'in aversion to {partner} by whole sign'
            elif pair['motion'] == 'Applying' and connected is True:
                description = (f"the indicator applies to {partner} by {aspect}" if pair['applicant'] == indicator
                               else f"{partner} applies to the indicator by {aspect}")
            elif pair['motion'] == 'Separating' and state in ('Separated', 'Under a single blanket'):
                description = f"separating from {partner} by {aspect}; {distance:.1f}° past exact; {state.lower()}"
            elif pair['motion'] == 'Applying':
                description = f"approaching exactness with {partner} by {aspect}; {distance:.1f}° from exact"
            else:
                description = f"{state} with {partner} by {aspect}; {distance:.1f}° from exact"
            if not is_unresolved(description):
                if connected is False and not averse:
                    description += "; outside the adopted connection window"
                if averse and connected is True:
                    description += '; in aversion by whole sign'
                if connected is True and pair.get('application_cause') == 'retrogradation':
                    description += '; by retrogradation (Abū Maʿshar, Gr. Intr. VII.5, 24)'
            records.append({'Role': role, 'Origin': sign, 'Indicator': indicator, 'Partner': partner,
                'Ruled houses': tuple(ruled.get(partner, ())), 'Whole-sign relation': pair['aspect_name'],
                'Connected': connected, 'State': state, 'Motion': pair['motion'],
                'Applicant': pair.get('applicant') if not is_unresolved(state) else state,
                'Receiver': pair.get('receiver') if not is_unresolved(state) else state,
                'Distance': distance, 'Description': description})
    return records


def pn4_further_indicators(chart_data, sr, year_lon, moon=None, jd_sr=None, relationships=None):
    """Indicators 6-19 of the year (II.1, 11-24): one row each, the fact
    it reads computed where it is a lookup on the root and the
    revolution, and stated as not computed where it is not."""
    natal, rev = chart_data['planetary_data'], sr['planetary_data']
    n_asc, r_asc = chart_data['ascendant'], sr['ascendant']
    year_sign = get_zodiac_sign(year_lon)
    lord = lambda lon: SIGN_TO_DOMICILE.get(get_zodiac_sign(lon), '-')
    places = (('natal Ascendant', n_asc), ('sign of the terminal point', year_lon),
              ('Ascendant of the revolution', r_asc))

    def planets_in_sign(data, sign):
        return [p for p in PN4_SEVEN if p in data and get_zodiac_sign(data[p]['longitude']) == sign]

    rows = []
    # 6
    rows.append({'#': 6, 'Indicator': 'The Ascendant of the revolution, and its lord',
                 'Reads': f"{get_zodiac_sign(r_asc)} ({get_degree_string(r_asc)}), lord {lord(r_asc)}",
                 'Source': 'II.1, 11; VI.3, 1-2'})
    # 7
    moon_lon = rev['Moon']['longitude']
    if moon is None:
        reads7 = (f"the revolution's Moon in {get_zodiac_sign(moon_lon)} ({get_degree_string(moon_lon)}), lord of her house "
                  f"{lord(moon_lon)}; her connections within the sign are not computed -- II.22, 1-4 need the "
                  f"connection to perfect before she leaves the sign, which the app's static test does not encode")
    elif moon['void']:
        reads7 = (f"the revolution's Moon in {moon['sign']} ({get_degree_string(moon_lon)}) is empty in course -- she "
                  f"leaves the sign on day {moon['exit_day']:.2f} without perfecting a connection -- so the lord of her "
                  f"house, {moon['house_lord']}, stands in (II.22, 4)")
    else:
        reads7 = (f"the revolution's Moon in {moon['sign']} ({get_degree_string(moon_lon)}) connects, before leaving it on "
                  f"day {moon['exit_day']:.2f}, with " + '; '.join(
                      f"{c['planet']} by {c['aspect']} on day {c['day']:.2f}" for c in moon['connections'])
                  + " (II.22, 1-2; the portions of the year below)")
    rows.append({'#': 7, 'Indicator': 'The Moon and the planets she connects with in her sign; if void, the lord of her house',
                 'Reads': reads7, 'Source': 'II.1, 12; II.22, 1-4'})
    # 8
    transits = []
    for p in PN4_SEVEN:
        if p not in rev:
            continue
        for q in PN4_SEVEN:
            if q not in natal:
                continue
            grade = _pn4_transit_grade(rev[p]['longitude'], natal[q]['longitude'])
            if grade:
                transits.append(f"{p} on {'its own' if p == q else 'natal ' + q} place, by {grade}")
    rows.append({'#': 8, 'Indicator': "Transits over rooted positions, one planet's or another's",
                 'Reads': '; '.join(transits) if transits else 'none, even by sign',
                 'Source': 'II.1, 13; V.1, 1-3'})
    # 9
    ly = lord(year_lon)
    ly_lon = rev.get(ly, {}).get('longitude')
    # II.6, 1, the first chapter this row cites, names THREE Ascendants --
    # "one of the stakes of the Ascendant of the root, or of the terminal
    # point, or of the Ascendant of the revolution" -- so the house is read
    # from each, in row 14's format (order PN4R-4g-2; it read the
    # revolution's alone). Whole-sign places, the topic unit.
    rows.append({'#': 9, 'Indicator': 'The lord of the year in one of the twelve houses of the revolution',
                 'Reads': (f"{ly} ({get_zodiac_sign(ly_lon)}, {get_degree_string(ly_lon)}) in house "
                           + ' / '.join(str(_pn4_house_from(ly_lon, from_lon)) for _l, from_lon in places)
                           + " (from the natal Ascendant / the terminal sign / the revolution Ascendant)") if ly_lon is not None else '-',
                 'Source': 'II.1, 14; II.6, 1 (the three Ascendants); II.6, II.9, II.12, II.15, II.18, II.21'})
    # 10
    parts = []
    for label, from_lon in places:
        L = lord(from_lon)
        L_lon = rev.get(L, {}).get('longitude')
        if L_lon is not None:
            parts.append(f"lord of the {label} ({L}): house {_pn4_house_from(L_lon, from_lon)} from {get_zodiac_sign(from_lon)}")
    rows.append({'#': 10, 'Indicator': "The three lords relative to their own places, in the revolution",
                 'Reads': '; '.join(parts), 'Source': 'II.1, 15; VI.6, 1-2 (fn 128: relative to its own Ascendant)'})
    # 11
    rows.append({'#': 11, 'Indicator': 'The turning of the planets and the twelve houses',
                 'Reads': 'the turning table below', 'Source': 'II.1, 16; VI.2'})
    # 12
    h_year, h_rasc = _pn4_house_from(year_lon, n_asc), _pn4_house_from(r_asc, n_asc)
    in_year = planets_in_sign(rev, year_sign)
    in_rasc = planets_in_sign(rev, get_zodiac_sign(r_asc))
    rows.append({'#': 12, 'Indicator': "The terminal sign or the revolution's Ascendant on a natal house, with a revolution planet in it",
                 'Reads': (f"terminal sign {year_sign} = natal house {h_year}, revolution planets there: "
                           f"{', '.join(in_year) or 'none'}; revolution Ascendant {get_zodiac_sign(r_asc)} = natal house "
                           f"{h_rasc}, revolution planets there: {', '.join(in_rasc) or 'none'}; "
                           f"{'the two are one house (VI.3, 3)' if h_year == h_rasc else 'two different houses'}"),
                 'Source': 'II.1, 17; VI.3, 3-5'})
    # 13
    rows.append({'#': 13, 'Indicator': "The terminal sign or the revolution's Ascendant on a natal planet",
                 'Reads': (f"natal planets in the terminal sign {year_sign}: {', '.join(planets_in_sign(natal, year_sign)) or 'none'}; "
                           f"in the revolution's Ascendant {get_zodiac_sign(r_asc)}: "
                           f"{', '.join(planets_in_sign(natal, get_zodiac_sign(r_asc))) or 'none'}"),
                 'Source': 'II.1, 18; VI.4, 1-3'})
    # 14
    moves = []
    for p in PN4_SEVEN:
        if p in natal and p in rev:
            nh = _pn4_house_from(natal[p]['longitude'], n_asc)
            rh = [_pn4_house_from(rev[p]['longitude'], from_lon) for _l, from_lon in places]
            moves.append(f"{p}: natal {nh} -> {rh[0]} / {rh[1]} / {rh[2]}")
    rows.append({'#': 14, 'Indicator': 'A natal planet in another house in the revolution, from the three places',
                 'Reads': '; '.join(moves) + ' (natal house -> from the natal Ascendant / the terminal sign / the revolution Ascendant)',
                 'Source': 'II.1, 19; VI.5, 1-4'})
    # VI.6, 41–44: current states, not future sign-exit perfections.
    relationships = pn4_indicator_relationships(chart_data, sr, year_lon) if relationships is None else relationships
    parts = [f"{r['Role']} ({r['Indicator']}): {pn4_result_text(r['Description'])}; "
             f"{r['Partner']}, lord of house{'s' if len(r['Ruled houses']) > 1 else ''} "
             f"{'/'.join(map(str, r['Ruled houses']))} from {r['Origin']}" for r in relationships]
    rows.append({'#': 15, 'Indicator': 'The connections of the lords of the houses with each other',
                 'Reads': ' | '.join(parts), 'Source': 'II.1, 20; VI.6, 1-4, 40-44; fnn 128-131'})
    # 16, 17
    rows.append({'#': 16, 'Indicator': "Each planet's shifting through the houses, bounds, bodies, rays, twelfth-parts and Lots during the year",
                 'Reads': 'Not tracked: the year\'s transits are not followed', 'Source': 'II.1, 21; Books V, VI, VIII'})
    rows.append({'#': 17, 'Indicator': 'The connections of the planets with each other during the year',
                 'Reads': 'Not tracked: the year\'s transits are not followed', 'Source': 'II.1, 22; VI.6; Book VII'})
    # 18
    dign = []
    for p in PN4_SEVEN:
        if p in rev:
            L, B = lord(rev[p]['longitude']), pn4_bound_lord(rev[p]['longitude'])
            dign.append(f"{p} in {get_zodiac_sign(rev[p]['longitude'])} ({'own house' if L == p else 'house of ' + L}; "
                        f"{'own bound' if B == p else 'bound of ' + B})")
    rows.append({'#': 18, 'Indicator': "Each planet in its own house or another's, its own bound or another's",
                 'Reads': '; '.join(dign), 'Source': 'II.1, 23; VIII.1-15'})
    # 19
    node = rev.get('North Node', {}).get('longitude')
    if node is not None:
        head = [str(_pn4_house_from(node, f)) for _l, f in places]
        tail = [str(_pn4_house_from(node + 180.0, f)) for _l, f in places]
        reads = (f"Head in {get_zodiac_sign(node)}, houses {' / '.join(head)}; Tail in "
                 f"{get_zodiac_sign(node + 180.0)}, houses {' / '.join(tail)} (from the natal Ascendant / the terminal "
                 f"sign / the revolution Ascendant)")
    else:
        reads = '-'
    rows.append({'#': 19, 'Indicator': 'The Head and Tail', 'Reads': reads, 'Source': 'II.1, 24; VII.9, 1'})
    return rows

# --- IX.9, 1-10 and IX.2, 4-7: the "governor" (mustawli) --------------------
# IX.9, 1-9 list "eight special indicators": the lord of the year; the
# distributor from the Ascendant; the distributor from the [longevity]
# releaser; "the partner to them both, by body and rays"; the lord of the
# fardar; the lord of the orb; "the one accepting the connection of the
# Moon, or the lord of her house"; "the first lord of the Ascendant of the
# revolution". IX.9, 10: "if these eight indicators would combine together
# in a single planet, then it alone would be the governor of the
# indication for the condition of the year; and if one of them had [only]
# some of the testimonies, it will be more primary than the others, and
# the rest of them will have a partnership with it".
#
# PARTIAL, on purpose, and the page says so per row. Testimony #3 and the
# releaser's half of #4 need the longevity releaser, which PN IV does not
# supply (III.3, 1 lists the candidates; IX.8, 123 defers the years); since 2026-09-10 the bundle passes the releaser's
# distribution from Sahl (On Nativities 1.15) when one is found, and the
# rows read unavailable otherwise; #7 needs the Moon's
# connection read in the revolution (her house lord is the fallback only
# when she is void), which is not computed. The tally runs over the six
# that are available, names the primary among them, and NEVER prints a
# planet as governor "alone", which IX.9, 10 reserves for all eight. "The
# first lord" of the revolution's Ascendant is read as its domicile lord
# (fn 324; "the first one is stronger in indication"). Decided by the
# owner 2026-09-10.
#
# IX.2, 4 gives a second, sign-level governor for the first month: the
# natal Lot of Fortune in the natal Ascendant, so that the terminal point
# from the Ascendant and from the Lot "is one [and the same] sign", the
# revolution's Ascendant "is also that sign, and in it is the Lot of
# Fortune of the revolution, and that sign is convertible" -- five
# conditions, shown one by one, since most years fail one of them. Fn 37:
# such a sign governs the whole year too. Fn 39 is Dykes' worked case
# (age 39, everything in Cancer, the Moon), and is the fixture. IX.9,
# 11-13 and IX.2, 8-11, the judgments, are not built.

PN4_GOVERNOR_TESTIMONIES = (
    (1, 'The lord of the year', 'IX.9, 2'),
    (2, 'The distributor from the Ascendant', 'IX.9, 3'),
    (3, 'The distributor from the [longevity] releaser', 'IX.9, 4'),
    (4, 'The partner to them both, by body and rays', 'IX.9, 5'),
    (5, 'The lord of the fardar', 'IX.9, 6'),
    (6, 'The lord of the orb', 'IX.9, 7'),
    (7, "The one accepting the Moon's connection, or the lord of her house", 'IX.9, 8'),
    (8, "The first lord of the Ascendant of the revolution", 'IX.9, 9'),
)
PN4_GOVERNOR_RELEASER_REASON = ("unavailable: needs the longevity releaser's distribution, which was not supplied "
                                "(IX.8, 123 defers the years; the app takes the choice from Sahl, "
                                "On Nativities 1.15, when that finds one)")
PN4_GOVERNOR_CONNECTION_REASON = ("unavailable: the Moon's connection is not read in the revolution, and her "
                                  "house lord stands in only when she is void, which is not determined")

PN4_RELEASER_EXPLANATION = (
    "This testimony uses Sahl's chosen longevity releaser with PN IV's direction method for its position "
    "(III.1.12), so it can differ from Sahl's distribution by local ascensions; when no releaser is found, "
    "Sahl's substitute is not counted here.")
PN4_NO_RELEASER = "No qualifying longevity releaser; Sahl's substitute distribution is not used in this tally."


def pn4_point_distribution(chart, longitude, elapsed, label, significator=None):
    """III.1, 12: one positional trajectory, reused for its distributor and partner."""
    lat = chart['geo_lat']
    axis = pn4_axis_of(longitude, chart['ascendant'], chart['mc'])
    data, obl = chart['planetary_data'], chart['obliquity']
    if axis == 'Ascendant':
        segments = pn4_distribution_from_ascendant(data, longitude, obl, lat, significator=significator)
        method = 'oblique ascension of the birth latitude'
    elif axis in ('Midheaven', 'Fourth (IC)'):
        segments = pn4_distribution_from_meridian(data, chart['mc'], obl, start_lon=longitude, label=label, significator=significator)
        method = 'right ascension'
    else:
        segments = pn4_distribution_by_semi_arcs(data, longitude, obl, lat, chart['armc'], label=label, significator=significator)
        method = 'proportional semi-arcs'
    current = pn4_distribution_at_age(segments, elapsed) if segments else None
    note = ('PN IV direction unavailable at this latitude' if segments is None else
            f'elapsed age {elapsed:g} is outside the direction table' if current is None else None)
    return {'identity': label, 'longitude': longitude, 'axis': axis, 'method': method,
            'status': 'available' if current else 'unavailable', 'note': note,
            'segments': segments, 'current': current, 'elapsed': elapsed,
            'distributor': current['distributor'] if current else None,
            'partner': current['partner'] if current else None,
            'rows': _pn4_distribution_rows(segments, current, origin_jd=chart['julian_day'])}


def pn4_releaser_direction(chart, releaser, elapsed):
    if releaser['releaser'] is None:
        return {'identity': None, 'longitude': None, 'axis': None, 'method': None,
                'status': 'absent', 'note': PN4_NO_RELEASER, 'segments': None,
                'current': None, 'elapsed': elapsed, 'distributor': None, 'partner': None, 'rows': []}
    return pn4_point_distribution(chart, releaser['longitude'], elapsed, releaser['releaser'],
                                  significator={'the Sun': 'Sun', 'the Moon': 'Moon'}.get(releaser['releaser']))


@_pn4_opening_aware
def _pn4_governor_alternatives(year_lord, distributor, partner, distribution_note, fardar_lord, orb_lord, sr_ascendant_lon,
                 moon_testimony=None, moon_void=None, releaser_distributor=None, releaser_partner=None,
                 releaser_note=None, releaser_status=None):
    """IX.9, 1-10 over the testimonies this engine can supply.
    Returns (rows, summary): one row per testimony with the planet or the
    reason it is unavailable, and a summary with the tally, the primary
    planet(s) among the available testimonies, and how many of eight
    were counted. `distribution_note` explains a missing distributor
    (refused at the poles, or the age past the table). Testimony #3 is
    the distributor of the releaser's distribution (Sahl, On Nativities
    choice, with PN IV III.1, 12's positional direction), passed in when there is one, else `releaser_note`
    says why not. #4, "the partner to them both", is counted only when
    the two distributions have the SAME partner (a reading); otherwise
    both partners are shown and the row is not counted. When the releaser
    is absent, the Ascendant's share remains; computation failure is distinct."""
    releaser_reason = f"unavailable: {releaser_note}" if releaser_note else PN4_GOVERNOR_RELEASER_REASON
    if distributor and releaser_distributor:
        if partner and partner == releaser_partner:
            both = f"{partner}; partner to both distributions"
        else:
            both = (f"not one partner to both, not counted: the Ascendant's is {partner or 'none'}, the releaser's "
                    f"{releaser_partner or 'none'}")
    elif distributor:
        if releaser_status == 'absent' and partner:
            both = f"{partner}; Ascendant's share retained; no qualifying releaser"
        else:
            both = f"not counted: the Ascendant's partner is {partner or 'none'}; the releaser's {releaser_reason}"

    else:
        both = f"unavailable: {distribution_note}"
    got = {
        1: year_lord,
        2: distributor or f"unavailable: {distribution_note}",
        3: releaser_distributor or releaser_reason,
        4: both,
        5: fardar_lord or 'unavailable',
        6: orb_lord or 'unavailable: natal hour lord unavailable',
        7: ((f"{moon_testimony}; {'the lord of her house, she being empty in course' if moon_void else 'accepting her connection'} (II.22, 1-4)")
            if moon_testimony else PN4_GOVERNOR_CONNECTION_REASON),
        8: SIGN_TO_DOMICILE.get(get_zodiac_sign(sr_ascendant_lon), '-'),
    }
    rows, tally, counted = [], {}, 0
    for n, label, cite in PN4_GOVERNOR_TESTIMONIES:
        value = got[n]
        planet = value.split(';')[0] if isinstance(value, str) else None
        available = planet in PN4_SEVEN
        if available:
            counted += 1
            tally[planet] = tally.get(planet, 0) + 1
        rows.append({'#': n, 'Testimony': label, 'Planet': value,
                     'Counted': 'yes' if available else 'no', 'Source': cite})
    top = max(tally.values()) if tally else 0
    primary = sorted(p for p, c in tally.items() if c == top) if tally else []
    summary = {
        'tally': tally, 'counted': counted, 'primary': primary, 'top': top,
        'text': ((f"{primary[0]} alone is the governor: all eight testimonies combine in it (IX.9, 10).")
                 if counted == 8 and top == 8 else
                 (f"{primary[0] if len(primary) == 1 else ', '.join(primary[:-1]) + ' and ' + primary[-1]} "
                  f"{'is' if len(primary) == 1 else 'are'} primary with {top} of the "
                  f"{counted} testimonies available (of eight); the rest partner with "
                  f"{'it' if len(primary) == 1 else 'them'} (IX.9, 10). No planet is the governor alone here: "
                  + (f"that needs all eight, and {8 - counted} are unavailable." if counted < 8
                     else "that needs all eight to combine in one planet, and they do not."))) if primary else
                'no testimony available',
    }
    return rows, summary

def pn4_governor(*args, **kwargs):
    """Keep a tied partner's testimony conditional in the public tally.

    A row whose Planet is tied is open only where its COUNT differs between
    the tie's alternatives; a row that merged to one answer ('no' under both
    when the releaser's partner is neither, 'yes' under both when the
    Ascendant's share is retained) keeps that answer, and only the planet
    the tally would credit stays open."""
    rows, summary = _pn4_governor_alternatives(*args, **kwargs)
    tied = [row for row in rows if is_unresolved(row['Planet'])]
    open_rows = [row for row in tied if is_unresolved(row['Counted'])]
    for row in open_rows:
        row['Counted'] = UnresolvedResult('the partner testimony depends on the opening tie',
                                          PN4_OPENING_SOURCE, row['Counted'].alternatives)
    bearing = [row for row in tied if row['Counted'] != 'no']
    if bearing:
        confirmed = [row for row in rows if row['Counted'] == 'yes' and not is_unresolved(row['Planet'])]
        summary['conditional_counted'] = summary['counted']
        summary['counted'] = len(confirmed)
        summary['confirmed_tally'] = dict((p, sum(row['Planet'].split(';')[0] == p for row in confirmed))
                                          for p in PN4_SEVEN if any(row['Planet'].split(';')[0] == p for row in confirmed))
        state = 'unresolved' if open_rows else 'counted, its planet tied'
        summary['text'] = (f"{len(confirmed)} testimonies are established; the opening-partner testimony is {state}. "
                           + pn4_result_text(summary['text']))
    return rows, summary


@_pn4_opening_aware
def pn4_governor_condition(governor, chart_data, sr):
    """IX.9, 11-13, the governor's condition, as FACTS with the conclusion
    quoted (order GAP-31). 11, "in its essence": "the three superior ones
    were easternizing from the Sun, in their halb, not retrograde, and not
    made unfortunate, and the inferior ones westernizing from him, in the
    same condition as well, and they are in their halb" -- read in the root
    and the revolution; "made unfortunate" is a judgment (II.4, 2 says what
    it consists of) and is shown as not judged, the infortunes looking at
    it listed as facts. 12, "in terms of its sign": "the sign harmonized
    with it by nature, or it had testimony in it" -- the first half is not
    read, the second is a dignity of its own at its degree. 13, "in terms of
    the rotation of the circle (and that is if it was in a stake or in what
    follows a stake) as well as in its sign": the place half is JUDGED BY
    THE DIVISION (get_effective_house, Alcabitius, the five degrees at the
    four axial degrees) in the revolution -- the owner's ruling of
    2026-09-11 (evening), an ADOPTED DYNAMIC-FITNESS READING under the
    canon (OWNER_RULING_PLACES_VS_DYNAMICS), not the text's own unit: the
    text supplies the requirement (13, with 12's sign condition), the
    canon its operational interpretation (strength/fitness language -> the
    divisions), and Alcabitius with the axial allowance come from that
    convention. Historical confidence in the exclusive division reading is
    QUALIFIED: IX.5, 4 fn 106 (p. 602) glosses "rotation" as "either to
    their being in the good or bad places, or to their dynamic angularity
    (advancing or withdrawing), here and in 7, 11, and 14" -- both spatial
    readings left open in the closest parallel (IX.5, 9, p. 603: fortunes
    "in the stakes or what follows them" -> "might, power, rank,
    reputation, class", the same triad as IX.9, 13). The work order's
    "whole-sign place" was corrected by the ruling. halb: the diurnal
    planets above the earth by day and below by night, the nocturnal the
    reverse, Mercury by his phase -- by altitude where the horizon is
    known, else by the ecliptic proxy (said in the row)."""
    if not governor or governor not in PN4_SEVEN:
        return []
    superior = governor in ('Saturn', 'Jupiter', 'Mars')
    rows = []

    def facts(chart, label):
        data = chart['planetary_data']
        row = data.get(governor)
        if not row:
            return None
        lon = row['longitude'] % 360.0
        phase, side, _el = solar_phase(governor, lon, data['Sun']['longitude'], row.get('speed_in_lon'))
        retro = row.get('speed_in_lon', 1.0) < 0
        if chart.get('armc') is not None and chart.get('geo_lat') is not None and chart.get('obliquity') is not None:
            above = _sin_altitude(lon, row.get('latitude', 0.0), row.get('distance', 1.0), chart['obliquity'], chart['armc'], chart['geo_lat']) > 0
            how = 'by altitude'
        else:
            above = (lon - chart['ascendant']) % 360.0 > 180.0
            how = 'by the ecliptic proxy'
        day = chart['sect'] == 'Diurnal'
        if governor == 'Mercury':
            diurnal = side == 'eastern'
        else:
            diurnal = governor in DIURNAL_SECT_PLANETS
        halb = (above if day else not above) if diurnal else (not above if day else above)
        infortunes = [f"{p} by {a}" for p, a, _l, _d in _pn4_looks_at_sign(data, get_zodiac_sign(lon)) if p in INFORTUNES and p != governor]
        return {'label': label, 'side': side or '-', 'retro': retro, 'halb': halb, 'how': how, 'infortunes': infortunes,
                'sign': get_zodiac_sign(lon), 'lon': lon}

    root, rev = facts(chart_data, 'root'), facts(sr, 'revolution')
    if root is None or rev is None:
        return []
    want_side = 'eastern' if superior else 'western'
    for f in (root, rev):
        f['side_ok'] = f['side'] == want_side
    essence_met = all(f['side_ok'] and f['halb'] and not f['retro'] for f in (root, rev))
    rows.append({'Test': 'IX.9, 11: suitable in its essence',
                 'Criteria': '; '.join(f"{f['label']}: {'eastern' if superior else 'western'} of the Sun {'met' if f['side_ok'] else 'not met'} ({f['side']}); "
                                       f"in its halb {'met' if f['halb'] else 'not met'} ({f['how']}); not retrograde {'not met' if f['retro'] else 'met'}; "
                                       f"not made unfortunate: not judged (infortunes looking: {', '.join(f['infortunes']) or 'none'})" for f in (root, rev)),
                 'Met': 'yes, on the judged criteria' if essence_met else 'no',
                 'Conclusion (quoted)': '"it indicates strength in the soul, thought, cleverness, and excellence in judgment, and the goodness of [his] character"',
                 'Source': 'IX.9, 11'})
    own = []
    for f in (root, rev):
        r_ = get_essential_rulers(f['lon'])
        dign = [k for k, v in (('house', r_['domicile']), ('exaltation', r_['exaltation']),
                               ('triplicity', r_['triplicity_day'] if (chart_data['sect'] == 'Diurnal') else r_['triplicity_night']),
                               ('bound', r_['term']), ('face', r_['face'])) if v == governor]
        f['testimony'] = dign
        own.append(bool(dign))
    rows.append({'Test': 'IX.9, 12: suitable in terms of its sign',
                 'Criteria': '; '.join(f"{f['label']}: in {f['sign']}, testimony in it {'met' if f['testimony'] else 'not met'} "
                                       f"({', '.join(f['testimony']) or 'none of its dignities'}); harmonized by nature: not read" for f in (root, rev)),
                 'Met': 'yes, on the judged criteria' if all(own) else 'no',
                 'Conclusion (quoted)': '"it indicates the strength of the body, and a suitable condition in it"', 'Source': 'IX.9, 12'})
    div = get_effective_house(rev['lon'], sr['houses']) if sr.get('houses') else None
    place_ok = div in PN4_STAKES_AND_SUCCEDENTS if div is not None else None
    rows.append({'Test': 'IX.9, 13: suitable in terms of the rotation of the circle, and in its sign',
                 'Criteria': (f"place half: in the revolution, division {div if div is not None else '-'} -- \"in a stake or in what "
                              f"follows a stake\" {'met' if place_ok else ('not met' if place_ok is not None else 'not computed (no cusps)')}; "
                              f"sign half: {'met' if own[1] else 'not met'} (12, in the revolution). "
                              + PN4_IX_9_13_UNIT_STATEMENTS),
                 'Met': ('yes' if (place_ok and own[1]) else 'no') if place_ok is not None else 'not computed',
                 'Conclusion (quoted)': '"it indicates a suitability of condition in rank, power, and class"', 'Source': 'IX.9, 13'})
    return rows

@_pn4_opening_aware
def _pn4_primary_condition(primary, chart_data, sr):
    return pn4_governor_condition((primary or [None])[0], chart_data, sr)


PN4_STAKES_AND_SUCCEDENTS = (1, 4, 7, 10, 2, 5, 8, 11)
# The three statements the owner's ruling of 2026-09-11 (evening) asks the
# page to carry, separately, on IX.9, 13's place half.
PN4_IX_9_13_UNIT_STATEMENTS = (
    "(i) PN IV IX.9, 13 supplies the requirement itself: \"in a stake or in what follows a stake\", together with the "
    "additional sign-condition requirement (\"as well as in its sign\", from 12). "
    "(ii) This app's convention on places and strength supplies its operational interpretation: "
    "strength/fitness language -> the divisions. "
    "(iii) Alchabitius and the axial 5-degree allowance come from that adopted convention, not from the text. "
    "Historical confidence in the exclusive division reading is qualified: IX.5, 4 fn 106 (p. 602) glosses \"rotation\" as "
    "\"either to their being in the good or bad places, or to their dynamic angularity (advancing or withdrawing), here and "
    "in 7, 11, and 14\" -- both spatial readings expressly left open in the closest parallel, IX.5, 9 (p. 603: fortunes \"in "
    "the stakes or what follows them\" -> \"might, power, rank, reputation, class\", the same triad as IX.9, 13); V.1, 28 fn "
    "15 (\"the house which that sign occupies\") beside it."
)

def pn4_first_month_governor(natal_ascendant, natal_fortune, year_lon, sr_ascendant, sr_fortune):
    """IX.2, 4: the five conditions, one by one, and whether all hold."""
    year_sign = get_zodiac_sign(year_lon)
    asc_sign, lot_sign = get_zodiac_sign(natal_ascendant), get_zodiac_sign(natal_fortune)
    conditions = [
        ('The natal Lot of Fortune is in the natal Ascendant', lot_sign == asc_sign,
         f"Lot in {lot_sign}, Ascendant in {asc_sign}", 'IX.2, 4 [#1, #3]'),
        ('So the terminal point from the Ascendant and from the Lot is one sign', lot_sign == asc_sign,
         f"both reach {year_sign}" if lot_sign == asc_sign else 'they reach different signs', 'IX.2, 4; fn 35'),
        ("The Ascendant of the revolution is that sign", get_zodiac_sign(sr_ascendant) == year_sign,
         f"revolution Ascendant in {get_zodiac_sign(sr_ascendant)}", 'IX.2, 4 [#4]'),
        ("The Lot of Fortune of the revolution is in it", get_zodiac_sign(sr_fortune) == year_sign,
         f"revolution Lot in {get_zodiac_sign(sr_fortune)}", 'IX.2, 4 [#5]'),
        ('That sign is convertible, so its lord is also the lord of its first ninth-part',
         PN4_QUADRUPLICITY.get(year_sign) == 'convertible',
         f"{year_sign} is {PN4_QUADRUPLICITY.get(year_sign)}; ninth-part lord {pn4_first_ninth_part_lord(year_sign)['lord']}, "
         f"sign lord {SIGN_TO_DOMICILE.get(year_sign, '-')}", 'IX.2, 4 [#2]; fn 36'),
    ]
    rows = [{'Condition': c, 'Holds': 'yes' if ok else 'no', 'Reads': reads, 'Source': cite}
            for c, ok, reads, cite in conditions]
    holds = all(ok for _c, ok, _r, _s in conditions)
    if holds:
        verdict = (f"{year_sign} and its lord {SIGN_TO_DOMICILE.get(year_sign, '-')} govern the first month, and the "
                   f"year with it (fn 37)")
    else:
        # IX.2, 5: "while if [only] the generality of the indications belonged
        # to one of them, it will be primary for obtaining information about
        # the year, while the remaining [ones] will have a partnership with it"
        # -- the sign holding most of the five rooted indicators (order
        # PN4R-4h-4). The Lot's terminal point is the Lot's sign turned the
        # same number of signs as the Ascendant's.
        offset = (SIGN_ORDER.index(year_sign) - SIGN_ORDER.index(asc_sign)) % 12
        five = {'the terminal point from the Ascendant': year_sign,
                'the terminal point from the Lot': SIGN_ORDER[(SIGN_ORDER.index(lot_sign) + offset) % 12],
                "the revolution's Ascendant": get_zodiac_sign(sr_ascendant),
                "the revolution's Lot of Fortune": get_zodiac_sign(sr_fortune),
                'the first ninth-part of the sign of the year': pn4_first_ninth_part_lord(year_sign)['ninth_part_sign']}
        tally = {}
        for label, sign in five.items():
            tally.setdefault(sign, []).append(label)
        top = max(len(v) for v in tally.values())
        leaders = [s for s, v in tally.items() if len(v) == top]
        failed = sum(1 for _c, ok, _r, _s in conditions if not ok)
        if len(leaders) == 1 and top >= 2:
            primary = leaders[0]
            partners = ', '.join(f"{s} ({', '.join(v)})" for s, v in tally.items() if s != primary)
            verdict = (f"no governor: {failed} of the five conditions fail. Primary: {primary} ({top} of five: "
                       f"{', '.join(tally[primary])}), partners: {partners} (IX.2, 5; fn 38: the month)")
        else:
            verdict = (f"no governor: {failed} of the five conditions fail. No primary: tied "
                       f"({'; '.join(f'{s}: {len(v)}' for s, v in tally.items())}) (IX.2, 5)")
        rows.append({'Condition': 'IX.2, 5: the sign holding most of the five rooted indicators is primary, the rest partners',
                     'Holds': '-', 'Reads': '; '.join(f"{l}: {s}" for l, s in five.items()), 'Source': 'IX.2, 5; fn 38'})
    return rows, verdict

# --- II.22, 1-4: the Moon's connections in her sign; the portions of the year
# "[4] the planet which the Moon connects with, so long as she is in her
# [current] sign. Now if in that sign she connected with not just one,
# then see how many there are: for if it was two planets, the year is
# divided into two halves; and if her connection in that sign of hers was
# with three planets, then that year is divided into equal thirds; and if
# it increased beyond that, then the year is divided according to their
# number. So, his condition in each one of the portions of the year ...
# will be in accordance with the condition of the planet which owns the
# portion. But if the Moon was empty in course, his situation will be in
# accordance with the condition of the lord of her house, whether it
# looked at her or not" (II.22, 1-4; the fallback again at 17). The
# connection is indicator #7 of every year (II.1, 12) and testimony #7 of
# the governor (IX.9, 8).
#
# READ INTO THE SENTENCES, and said on the page: "connects with" is a
# perfection by degree, of the body or a Ptolemaic ray, before the
# revolution's Moon leaves her sign -- found by the engine's forward
# simulation and _perfection_day, which revalidates the whole-sign
# configuration at the moment of perfection (VII.5, 14: no out-of-sign
# connection); the portions go to the planets in the ORDER she connects
# (not stated); the division is stated inside "If the Moon was the lord
# of the year" (II.22, 1) and is computed every year with the row saying
# whether this year's lord is the Moon (owner's decision 2026-09-10);
# "empty in course" is no such perfection before she leaves the sign.
# II.22, 11's rays, Lots and twelfth-parts are not counted. The
# judgments of II.22, 5-24 are not built. No worked example exists.
PN4_MOON_ASPECTS = ((0.0, 'body'), (60.0, 'sextile'), (90.0, 'square'), (120.0, 'trine'), (180.0, 'opposition'))

def _pn4_luminary_connections(sr_planetary_data, jd_sr, body, horizon_days, step_days, applying_only):
    """The planets a luminary of the revolution connects with before it
    leaves its sign, in day order. `applying_only` keeps a perfection only
    if the luminary is the faster body at that moment -- the one "handing
    over" (II.13, 1) rather than being handed to. The Moon is always the
    faster, so for her it changes nothing."""
    sim = _simulate_forward(sr_planetary_data, jd_sr, horizon_days=horizon_days, step_days=step_days)
    exit_day = next((d for d in sim['events'][body]['sign_exits'] if d > 0.0), None)
    lon0 = sr_planetary_data[body]['longitude'] % 360.0
    sign = get_zodiac_sign(lon0)
    found = []
    for planet in PN4_SEVEN:
        if planet == body or planet not in sr_planetary_data:
            continue
        for target, name in PN4_MOON_ASPECTS:
            day = _perfection_day(sim, body, planet, target, before_day=exit_day)
            if day is None or day <= 0.0:
                continue
            if applying_only:
                v_body = swe.calc_ut(jd_sr + day, PLANET_SWE_IDS[body])[0][3]
                v_planet = swe.calc_ut(jd_sr + day, PLANET_SWE_IDS[planet])[0][3]
                if not v_body > v_planet:
                    continue
            found.append({'day': day, 'planet': planet, 'aspect': name, 'moon_at': _lon_at(sim, body, day)})
    found.sort(key=lambda c: c['day'])
    return {'sign': sign, 'moon_lon': lon0, 'exit_day': exit_day, 'connections': found,
            'void': not found, 'house_lord': SIGN_TO_DOMICILE.get(sign, '-')}

def pn4_moon_connections(sr_planetary_data, jd_sr):
    """The planets the revolution's Moon connects with before she leaves
    her sign (II.22, 1), in the order she reaches them. Returns
    {sign, moon_lon, exit_day, connections: [{day, planet, aspect, moon_at}],
    void, house_lord}; days are from the revolution."""
    return _pn4_luminary_connections(sr_planetary_data, jd_sr, 'Moon', 6, 0.25, applying_only=False)

def pn4_sun_handover(sr_planetary_data, jd_sr):
    """II.13, 1 [3]: "the planet to which the Sun hands over the management
    (so long as it is in its sign)" -- read, with fn 239, as the Sun's
    own connections before he leaves his sign, and "hands over" as the
    Sun being the applying body at the perfection. Same shape as
    pn4_moon_connections; 'moon_at' is the Sun's longitude then."""
    return _pn4_luminary_connections(sr_planetary_data, jd_sr, 'Sun', 40, 1.0, applying_only=True)

def pn4_moon_portions(connections, year_days):
    """II.22, 2-3: the year divided by the number of planets she connects
    with, each portion owned by one of them, in the order of connection."""
    n = len(connections)
    if n == 0:
        return []
    size = float(year_days) / n
    return [{'portion': i + 1, 'of': n, 'planet': c['planet'], 'from_day': i * size, 'to_day': (i + 1) * size}
            for i, c in enumerate(connections)]

def pn4_moon_testimony(moon):
    """IX.9, 8 / II.1, 12: "the one accepting the connection of the Moon,
    or the lord of her house" -- the first planet she connects with in
    her sign, else her house lord."""
    if not moon:
        return None
    if moon['connections']:
        return moon['connections'][0]['planet']
    return moon['house_lord']

# --- III.2: the distribution analysed ------------------------------------
# Three stated pieces, and the prose between them left alone.
#   III.2, 4-9   a checklist of questions about the bound the distribution
#                stands in -- answered here as FACTS, not judged;
#   III.2, 10-17 seven "static" types of distributor and partner, by
#                fortune and infortune (Figure 66) -- delineated at 18-54
#                under conditions in root and revolution, which are not
#                judged here;
#   III.2, 55-86 twenty-four transitions inside a year, by the natures of
#                the outgoing and incoming bound lords and managers, and
#                III.2, 87-101 their twelve indications, one sentence
#                each, quoted verbatim on the page.
# III.2, 105: the twelve concern the manager by its ROOTED body or ray --
# the natal distribution table -- and a revolutionary planet entering the
# bound (43, 46-47, 54) is a different matter, not built. III.2, 110-111
# gate every death statement in the chapter on "the years of the lifespan
# which his [longevity] indicator in the root [had already] pointed out",
# which is the refused releaser: the quoted indications that mention
# death carry that gate on the page.
#
# The Sun, Moon and Mercury are neither fortune nor infortune, and the
# types and transitions speak only of fortunes and infortunes: a
# distribution under one of them reads "no type by nature", a transition
# involving one "not among the twenty-four". Type 5 turns on conditions
# (a corrupting infortune in the bound, a weak fortune) that are not
# judged, so it is never assigned. Decided by the owner 2026-09-10, with
# the indications quoted. No worked example by the author; Figure 67 /
# fn 56 is Dykes' diagram of III.2, 33 (Mars distributing with Venus's
# sextile: type 3), and pins the classification.

def pn4_nature(planet):
    """Fortune, infortune, or None for the Sun, Moon and Mercury."""
    if planet in FORTUNES:
        return 'fortune'
    if planet in INFORTUNES:
        return 'infortune'
    return None

PN4_III2_TYPES = {
    1: ('a fortune, acting alone in the distribution', 'III.2, 11; 18-24'),
    2: ('an infortune, acting alone in the distribution', 'III.2, 12; 25-29'),
    3: ('an infortune distributing, a fortune partnering', 'III.2, 13; 30-35'),
    4: ('a fortune distributing, an infortune partnering', 'III.2, 14; 36-39'),
    5: ('one of the planets, fortune or infortune, under a corrupting infortune -- a condition, not assigned here', 'III.2, 15; 40-43'),
    6: ('both infortunes', 'III.2, 16; 44-48'),
    7: ('both fortunes', 'III.2, 17; 49-54'),
}

@_pn4_opening_aware
def pn4_static_type(distributor, partner):
    """III.2, 10-17: which of the seven the current distribution is, by
    nature alone. Returns (number or None, label, cite)."""
    d, p = pn4_nature(distributor), pn4_nature(partner) if partner else None
    if d is None or (partner and p is None):
        who = distributor if d is None else partner
        return (None, f"no type by nature: {who} is neither fortune nor infortune, and III.2's seven types "
                      f"speak only of fortunes and infortunes", 'III.2, 10-17')
    if not partner:
        n = 1 if d == 'fortune' else 2
    elif d == 'fortune' and p == 'fortune':
        n = 7
    elif d == 'infortune' and p == 'infortune':
        n = 6
    elif d == 'infortune':
        n = 3
    else:
        n = 4
    return (n, PN4_III2_TYPES[n][0], PN4_III2_TYPES[n][1])

# The twenty-four (III.2, 58-86): number -> (kind, from, to, context nature).
_F, _I = 'fortune', 'infortune'
PN4_III2_TRANSITIONS = {
    1: ('bound', _F, _F, None), 2: ('bound', _F, _I, None), 3: ('bound', _I, _F, None), 4: ('bound', _I, _I, None),
    5: ('management', _F, _F, None), 6: ('management', _F, _I, None), 7: ('management', _I, _F, None), 8: ('management', _I, _I, None),
    9: ('bound', _F, _F, _F), 10: ('bound', _F, _F, _I), 11: ('bound', _F, _I, _F), 12: ('bound', _F, _I, _I),
    13: ('bound', _I, _F, _F), 14: ('bound', _I, _F, _I), 15: ('bound', _I, _I, _F), 16: ('bound', _I, _I, _I),
    17: ('management', _F, _F, _F), 18: ('management', _F, _F, _I), 19: ('management', _F, _I, _F), 20: ('management', _F, _I, _I),
    21: ('management', _I, _F, _F), 22: ('management', _I, _F, _I), 23: ('management', _I, _I, _F), 24: ('management', _I, _I, _I),
}

# The twelve indications (III.2, 87-101), quoted. (from, to) -> paired;
# (from, to, context) -> doubled. `death` marks the ones III.2, 110-111
# gate on the longevity indicator's years.
PN4_III2_PAIRED = {
    (_F, _F): ('III.2, 88', 'the duration of good fortune, and the shift from good fortune to good fortune', False),
    (_I, _F): ('III.2, 89', 'a shift from the bottom and humbleness to being raised up and greatness', False),
    (_F, _I): ('III.2, 90', 'a shift from good to bad, and a fear of death', True),
    (_I, _I): ('III.2, 91', 'a fluctuation in tribulations, and a shift from adversity to adversity, and from evil to evil, '
                          'and that indication of the fear of death will be more confirmed', True),
}
PN4_III2_DOUBLED = {
    (_F, _F, _F): ('III.2, 92', 'good fortune and surpassing good', False),
    (_F, _F, _I): ('III.2, 94', 'a suitability of condition, and the lastingness of good fortune, because the fortunes in it are '
                                'greater in testimony -- except that it will blend it with evil and something detestable due to '
                                'the nature of the infortune', False),
    (_I, _F, _F): ('III.2, 95', 'a shift from something detestable and evil to surpassing good fortune', False),
    (_I, _F, _I): ('III.2, 96', 'a middling [condition] in good and evil, and excellence and badness, even though the '
                                'indication of good is stronger', False),
    (_F, _I, _F): ('III.2, 97', 'a middling condition in suitability and corruption, and good and evil, even though the '
                                'indication of evil is stronger', False),
    (_F, _I, _I): ('III.2, 98', 'something detestable and much evil, and the encountering of tribulations and the fear of '
                                'death, and fluctuations in detestable things', True),
    (_I, _I, _F): ('III.2, 99', 'excessive adversity and the fear of death', True),
    (_I, _I, _I): ('III.2, 100', 'varieties of detestable things, evil, and death', True),
}
PN4_III2_DEATH_GATE = (' [death only in the years the longevity indicator pointed out, III.2, 110-111 -- see the '
                       'house-master directed, Sahl On Nativities 1.23, 2, in The releaser chapter]')

def pn4_transition_numbers(kind, frm, to, context):
    """The transition numbers a shift answers to: the isolated one (1-8)
    and, when the context planet has a nature, the qualified one (9-24)."""
    out = [n for n, (k, f, t, c) in PN4_III2_TRANSITIONS.items() if k == kind and f == frm and t == to and c is None]
    if context is not None:
        out += [n for n, (k, f, t, c) in PN4_III2_TRANSITIONS.items() if k == kind and f == frm and t == to and c == context]
    return out

@_pn4_opening_aware
def pn4_classify_shift(prev_seg, next_seg):
    """III.2, 55-101 for one boundary between two segments of the natal
    distribution: zero, one or two shifts (bound and/or management),
    each numbered and given its quoted indication, or explained as not
    among the twenty-four."""
    out = []
    bound_shift = prev_seg['distributor'] != next_seg['distributor']
    mgmt_shift = prev_seg['partner'] != next_seg['partner']
    if bound_shift:
        frm, to = pn4_nature(prev_seg['distributor']), pn4_nature(next_seg['distributor'])
        ctx = pn4_nature(next_seg['partner']) if next_seg['partner'] else None
        label = f"the distribution shifts from the bound of {prev_seg['distributor']} to the bound of {next_seg['distributor']}"
        out.append(_pn4_shift_row('bound', label, frm, to, ctx, next_seg['partner']))
    if mgmt_shift:
        frm = pn4_nature(prev_seg['partner']) if prev_seg['partner'] else None
        to = pn4_nature(next_seg['partner']) if next_seg['partner'] else None
        ctx = pn4_nature(next_seg['distributor'])
        label = (f"the management shifts from {prev_seg['partner'] or 'the distributor alone'} to "
                 f"{next_seg['partner'] or 'the distributor alone'}")
        out.append(_pn4_shift_row('management', label, frm, to, ctx, next_seg['distributor']))
    if bound_shift and mgmt_shift and len(out) == 2 and all(r['numbers'] for r in out):
        natures = {pn4_nature(prev_seg['distributor']), pn4_nature(next_seg['distributor']),
                   pn4_nature(prev_seg['partner']), pn4_nature(next_seg['partner'])}
        if natures == {_F}:
            out[0]['indication'] += ' -- and both shifting from a fortune to a fortune: "good fortune upon good fortune, and of good upon good; and it is the most powerful good fortune, and the most splendid in power, if the four of them were fortunes" (III.2, 93)'
        elif natures == {_I}:
            out[0]['indication'] += ' -- and both shifting from an infortune to an infortune: "of the greatest and harshest adversity if the four of them were infortunes" (III.2, 101)' + PN4_III2_DEATH_GATE
    return out

def _pn4_shift_row(kind, label, frm, to, ctx, context_planet):
    if frm is None or to is None:
        return {'kind': kind, 'label': label, 'numbers': [], 'cite': 'III.2, 58-86',
                'indication': 'not among the twenty-four: one side of the shift is neither fortune nor infortune, or the distributor acts alone'}
    numbers = pn4_transition_numbers(kind, frm, to, ctx)
    paired_cite, paired_text, paired_death = PN4_III2_PAIRED[(frm, to)]
    if ctx is not None:
        d_cite, d_text, d_death = PN4_III2_DOUBLED[(frm, to, ctx)]
        indication = (f'"{d_text}" ({d_cite}; the paired reading at {paired_cite})'
                      + (PN4_III2_DEATH_GATE if d_death else ''))
    else:
        indication = (f'"{paired_text}" ({paired_cite}); the qualified transition needs a fortune or infortune '
                      f'{"managing" if kind == "bound" else "distributing"}, and {context_planet or "none"} is neither'
                      + (PN4_III2_DEATH_GATE if paired_death else ''))
    return {'kind': kind, 'label': label, 'numbers': numbers,
            'cite': 'III.2, ' + ('58-62' if kind == 'bound' and ctx is None else '63-67' if ctx is None
                                 else '68-76' if kind == 'bound' else '77-85'),
            'indication': indication}

@_pn4_opening_aware
def pn4_year_transitions(segments, age):
    """The shifts of the natal distribution that fall inside this year of
    it, [age, age + 1) in years of arc (III.2, 55: "within one of the
    years")."""
    rows = []
    if not segments:
        return rows
    for prev_seg, next_seg in zip(segments, segments[1:]):
        if age <= next_seg['from'] < age + 1:
            for r in pn4_classify_shift(prev_seg, next_seg):
                rows.append({'At age': f"{next_seg['from']:.2f}", 'Shift': r['label'],
                             'Transition': ', '.join(f"#{n}" for n in r['numbers']) or '-',
                             'Indication': r['indication'], 'Source': r['cite']})
    return rows

def _pn4_bound_span(deg):
    """The Egyptian bound a degree stands in: (start, end)."""
    starts = pn4_bound_starts()
    idx = max(i for i, (lon, _l, _s) in enumerate(starts) if lon <= deg % 360.0)
    return starts[idx][0], (starts[idx + 1][0] if idx + 1 < len(starts) else 360.0)

# --- III.2, 38, 43, 46-47, 54; III.8, 7: transits into the bound ----------
# The twenty-four and the seven types read the NATAL distribution (III.2,
# 105); "when there is a planet in that bound in one of the years, either
# by its body or casting a ray to it, then its indication is not in the
# manner we have described" (106). Six sentences do describe it, each
# keyed to a type:
#   38  a fortune distributing with no rooted partner (type 1), and "in
#       the revolution the body or rays of an infortune was in it": "good
#       fortune ... [and] incidental adversity and harm"; worse under 39's
#       conditions;
#   43  in the type-5 discussion (40-42: a corrupting infortune in the
#       bound, a weak fortune -- conditions, not judged here): "the body
#       of a fortune or its rays" in the bound in the revolution "will
#       not have the power to repel death, but his death will be with
#       reverence";
#   46  both infortunes (type 6) and "in the revolution one of the
#       fortunes cast its ray to it": "he will not be freed from death but
#       he will be revered in his illness";
#   47  the same with "the ray of an infortune in it": hardship, "a bad
#       death"; 48: at home if the indicators are in their own places;
#   54  both fortunes (type 7) and "the body of an infortune or its rays"
#       in the bound in the revolution: "good fortune but he will be
#       unhappy with it";
#   III.8, 7  the lord of the year and the distributor both infortunes,
#       in the revolution "both not in their own shares, but a fortune was
#       with each one of them": "a little good" -- a condition on the two
#       lords, shown as facts.
# The death sentences carry III.2, 110-111's gate. The Sun, Moon and
# Mercury entering the bound are addressed by no sentence, and 46-47
# speak of rays, not bodies; both are said in the row. Decided by the
# owner 2026-09-10; no worked example (fn 56's Figure 67 has the
# revolutionary Saturn in the bound, one instance of the fact).

PN4_BOUND_TRANSIT_SENTENCES = {
    34: ('III.2, 34-35', 'it indicates a hindered adversity', False),
    38: ('III.2, 38-39', 'because of the distribution of the fortunes it indicates good fortune, and due to the rays of the '
                        'revolutionary infortune it indicates incidental adversity and harm', False),
    43: ('III.2, 43', 'it will not have the power to repel death, but his death will be with reverence', True),
    46: ('III.2, 46', 'he will not be freed from death but he will be revered in his illness and his conditions, until the '
                     'time in which he dies', True),
    47: ('III.2, 47', 'before death he will encounter hardship, and will be tortured, and it will be burdensome for whoever '
                     'is tending to him, and he will die a bad death', True),
    54: ('III.2, 54', 'he will have good fortune but he will be unhappy with it, and distresses and anxieties will affect '
                     'him, in accordance with the indication of the infortune', False),
}

def pn4_bound_transit_sentence(type_number, entrant_nature, by_ray):
    """Which of the five III.2 sentences fits a revolutionary body or ray
    in the bound, given the static type. Returns (key or None, note)."""
    if entrant_nature is None:
        return None, 'no sentence of III.2 speaks of the Sun, the Moon or Mercury entering the bound'
    if type_number == 2 and entrant_nature == 'fortune':
        return 34, ("under III.2, 33's condition — the distributor made unfortunate by an infortune in the root — "
                    "which is not judged here; 35's death clause under 110–111 not judged")
    if type_number == 1 and entrant_nature == 'infortune':
        return 38, ''
    if type_number == 7 and entrant_nature == 'infortune':
        return 54, ''
    # 43's premise is 40's: "in that bound in the root is a powerful,
    # corrupting infortune or its rays" -- which types 4, 5 and 6 have (an
    # infortune distributing or partnering) and types 2 and 3 do not. An
    # earlier version keyed 43 to 2, 3 and None (order PN4R-4m-1, 2026-09-11).
    under_40 = "under III.2, 40-42's conditions -- a corrupting infortune in the bound, a weak fortune -- which are not judged here"
    if type_number == 6:
        if by_ray:
            return (46 if entrant_nature == 'fortune' else 47), ''
        if entrant_nature == 'fortune':
            return 43, under_40
        return None, 'III.2, 46-47 speak of a ray cast into the bound, not a body in it'
    if entrant_nature == 'fortune' and type_number in (4, 5):
        return 43, under_40
    return None, 'no sentence of III.2 pairs this entrant with this type'

@_pn4_opening_aware
def pn4_bound_transits(chart_data, sr, current, year_lord):
    """The revolution's bodies and rays in the current bound, each keyed
    to its sentence and quoted; then III.8, 7's condition on the lord of
    the year and the distributor, as facts."""
    if not current:
        return []
    rev = sr['planetary_data']
    b_start, b_end = _pn4_bound_span(current['from_lon'])
    t_num, _label, _cite = pn4_static_type(current['distributor'], current['partner'])
    rows = []
    for lon, kind, who, aspect in sorted(pn4_bodies_and_rays(rev), key=lambda m: m[0]):
        if not (b_start <= lon < b_end):
            continue
        key, note = pn4_bound_transit_sentence(t_num, pn4_nature(who), kind == 'ray')
        if key:
            cite, text, death = PN4_BOUND_TRANSIT_SENTENCES[key]
            sentence = f'"{text}" ({cite})' + (f' -- {note}' if note else '') + (PN4_III2_DEATH_GATE if death else '')
        else:
            cite, sentence = 'III.2, 105-106', note
        rows.append({'In the bound, in the revolution': f"{who} by {aspect} at {get_degree_string(lon)}",
                     'Nature': pn4_nature(who) or 'neither',
                     'With the type': f"type {t_num}" if t_num else 'no type by nature',
                     'Sentence': sentence, 'Source': cite})
    # III.8, 7
    dist = current['distributor']
    both_infortunes = pn4_nature(year_lord) == 'infortune' and pn4_nature(dist) == 'infortune'

    def share_and_company(planet):
        row = rev.get(planet)
        if not row:
            return '-'
        r = get_essential_rulers(row['longitude'])
        in_share = planet in (r['domicile'], r['exaltation'], r['triplicity_day'], r['triplicity_night'], r['face'], r.get('term'))
        sign = get_zodiac_sign(row['longitude'])
        with_fortune = [p for p in FORTUNES if p in rev and get_zodiac_sign(rev[p]['longitude']) == sign]
        return (f"{planet} in {sign}: {'in its own share' if in_share else 'not in its own share'}; "
                f"{'a fortune with it: ' + ', '.join(with_fortune) if with_fortune else 'no fortune with it'}")
    if both_infortunes:
        facts = share_and_company(year_lord) + ('' if year_lord == dist else '; ' + share_and_company(dist))
        sentence = f'if both are not in their own shares and a fortune is with each: "they will indicate a little good" (III.8, 7); the facts: {facts}'
    else:
        sentence = (f"III.8, 7 needs the lord of the year and the distributor both infortunes; they are "
                    f"{year_lord} ({pn4_nature(year_lord) or 'neither'}) and {dist} ({pn4_nature(dist) or 'neither'})")
    rows.append({'In the bound, in the revolution': 'The lord of the year and the distributor, in the revolution',
                 'Nature': f"{pn4_nature(year_lord) or 'neither'} / {pn4_nature(dist) or 'neither'}",
                 'With the type': f"type {t_num}" if t_num else 'no type by nature',
                 'Sentence': sentence, 'Source': 'III.8, 7'})
    return rows

def pn4_distribution_checklist(chart_data, sr, year_lon, current):
    """III.2, 4-9 as facts for the bound the distribution stands in now.
    Nothing here is judged: the conditions 5 and 8 ask about are shown
    as what they are (direct or retrograde, the solar phase) and the
    reader judges."""
    if not current:
        return []
    natal, rev = chart_data['planetary_data'], sr['planetary_data']
    n_asc, r_asc = chart_data['ascendant'], sr['ascendant']
    deg = current['from_lon'] % 360.0
    b_start, b_end = _pn4_bound_span(deg)
    lord = current['distributor']
    sign = get_zodiac_sign(b_start)

    def condition(data, planet):
        row = data.get(planet)
        if not row:
            return '-'
        motion = 'retrograde' if row.get('speed_in_lon', 1.0) < 0 else 'direct'
        phase, side, _el = solar_phase(planet, row['longitude'], data['Sun']['longitude'], row.get('speed_in_lon'))
        return f"{get_zodiac_sign(row['longitude'])}, {motion}, {side or '-'}{', ' + phase.lower() if phase else ''}"

    def in_bound(items):
        return [(lon, who, aspect) for lon, kind, who, aspect in items if b_start <= lon < b_end]

    rulers = get_essential_rulers(b_start)
    face = get_essential_rulers(deg)['face']
    trip = rulers['triplicity_day'] if chart_data['sect'] == 'Diurnal' else rulers['triplicity_night']
    natal_meet = in_bound(pn4_bodies_and_rays(natal))
    rev_meet = in_bound(pn4_bodies_and_rays(rev))
    fmt = lambda items: '; '.join(f"{who} by {aspect} at {get_degree_string(lon)}" for lon, who, aspect in items) or 'none'
    return [
        {'Question': '[1a] Whose bound is it', 'Reads': f"{lord}'s, {get_degree_string(b_start)} to {get_degree_string(b_end)}",
         'Source': 'III.2, 5'},
        {'Question': "[1b] Its lord's condition in the root and the revolution (not judged: the facts)",
         'Reads': f"root: {condition(natal, lord)}; revolution: {condition(rev, lord)}", 'Source': 'III.2, 5'},
        {'Question': '[2] Where the bound falls from the natal Ascendant / the terminal sign / the revolution Ascendant',
         'Reads': f"house {get_wsh_house(b_start, n_asc)} / {get_wsh_house(b_start, year_lon)} / {get_wsh_house(b_start, r_asc)}",
         'Source': 'III.2, 6'},
        {'Question': "[3] The sign's house, exaltation, triplicity and face",
         'Reads': f"{sign}: house of {rulers['domicile']}, exaltation of {rulers['exaltation'] if rulers['exaltation'] not in (None, '-', '') else 'none'}, triplicity of "
                  f"{trip} ({'day' if chart_data['sect'] == 'Diurnal' else 'night'}), face of {face} at {get_degree_string(deg)}",
         'Source': 'III.2, 7'},
        {'Question': '[4] Who is in that sign in the root; in the revolution',
         'Reads': f"root: {_pn4_natal_planets_in_sign(natal, sign)}; revolution: {_pn4_natal_planets_in_sign(rev, sign)}",
         'Source': 'III.2, 8'},
        {'Question': '[5] Who casts rays to the bound, and who is in it -- root',
         'Reads': fmt(natal_meet), 'Source': 'III.2, 9'},
        {'Question': '[5] The same in the revolution (a different matter from the twelve, III.2, 105-106)',
         'Reads': fmt(rev_meet), 'Source': 'III.2, 9; 43, 46-47, 54'},
    ]

# --- II.13, 1; II.14, 1; II.22, 1-5: the luminary proxies -----------------
# "If the Sun was the lord of the year, then the majority of that judgment
# in that year should be in accordance with the condition of [1] the lord
# of the sign in which the distribution of the lifespan from the
# [longevity] releaser was ..., and partnering with it in the indication
# is [2] the planet which is in Leo in the root of the nativity or in the
# revolution, and [3] the planet to which the Sun hands over the
# management (so long as it is in its sign), and then along with that you
# see [4] where the Sun is, calling upon [that] as a witness" (II.13, 1);
# II.14, 1 adds "the condition of the distributor"; II.22, 1-5 for the
# Moon: the distributor, the lord of the sign of the releaser's
# distribution, the planet in Cancer, the planet she connects with in her
# sign, her house lord if empty in course, and her own conditions.
#
# PARTIAL, on purpose: the first proxy in every version -- the sign the
# longevity releaser's distribution stands in, and its lord -- needs the
# releaser PN IV does not supply (III.3, 1 lists the candidates; IX.8, 123 defers the years); since 2026-09-10 it is
# filled from the releaser's distribution (Sahl, On Nativities 1.15, 22)
# when one is found, else unavailable. The Sun's hand-over is read per fn 239 as
# the Sun's own connections in his sign, in the REVOLUTION (fn 239 notes
# the book does not say root or revolution), with "hands over" as the Sun
# being the applying body; "where the Sun is" is his sign and its lord
# per fn 241. Shown only in a year whose lord is the Sun or the Moon.
# The delineations (II.13, 2 - II.21; II.22, 5-24) are not built. No
# worked example exists; fn 238 illustrates the missing part.
# Decided by the owner 2026-09-10.
PN4_PROXY_RELEASER = ("unavailable: the sign the longevity releaser's distribution stands in needs the releaser's "
                      "distribution, which was not supplied (IX.8, 123 defers the years; the "
                      "engine takes it from Sahl, On Nativities 1.15, when that finds one)")

def pn4_luminary_proxies(year_lord, chart_data, sr, moon=None, sun=None, releaser=None):
    """The proxies for a year whose lord is the Sun or the Moon, or None
    for any other lord. Rows of {Proxy, Reads, Source}. `releaser`, when
    given, is {'distributor', 'sign', 'lord', 'note'} from the releaser's
    distribution (Sahl, On Nativities 1.15, 22), and fills the first
    proxy of each version; its 'note' explains an absence."""
    natal, rev = chart_data['planetary_data'], sr['planetary_data']
    if year_lord not in ('Sun', 'Moon'):
        # II.22, 23: "in all of the times you should look at the lord of the
        # year, whether it was the Moon or not, connecting or empty in course,
        # in whichever planet's house it is, in the revolution"; 25: "the
        # indication of the lord of the year will be like that when[ever] it
        # is in a house of one of the planets." One row, the house and its
        # lord's condition in the revolution (order PN4R-4i-5; the function
        # had returned None for every lord but the luminaries). The void
        # stand-in of fn 319 (no perfection before leaving the sign) would
        # need the revolution's moment and is not read here.
        row = rev.get(year_lord)
        if not row:
            return None
        sign = get_zodiac_sign(row['longitude'])
        host = SIGN_TO_DOMICILE.get(sign, '-')

        def condition(data, planet):
            r = data.get(planet)
            if not r:
                return '-'
            motion = 'retrograde' if r.get('speed_in_lon', 1.0) < 0 else 'direct'
            phase, side, _el = solar_phase(planet, r['longitude'], data['Sun']['longitude'], r.get('speed_in_lon'))
            return f"{get_zodiac_sign(r['longitude'])}, {motion}, {side or '-'}{', ' + phase.lower() if phase else ''}"

        return [{'Proxy': '[II.22, 23] The house the lord of the year stands in, in the revolution',
                 'Reads': (f"{year_lord} in {sign}, the house of {host}; {host}'s condition in the revolution: "
                           f"{condition(rev, host)} -- 24-25 judge the year by that lord's condition (not pronounced here); "
                           f"whether {year_lord} is connecting or empty in course is not read"),
                 'Source': 'II.22, 23-25'}]
    r_dist = (releaser or {}).get('distributor')
    r_sign = (releaser or {}).get('sign')
    r_note = f"unavailable: {releaser['note']}" if releaser and releaser.get('note') else PN4_PROXY_RELEASER
    r_dist_text = f"{r_dist} (the releaser's distributor; Sahl, On Nativities 1.18, 20)" if r_dist else r_note
    r_sign_text = (f"{r_sign}, lord {(releaser or {}).get('lord', '-')} (the releaser's distribution stands there)"
                   if r_sign else r_note)

    def in_sign(data, sign):
        found = [p for p in PN4_SEVEN if p in data and p != year_lord and get_zodiac_sign(data[p]['longitude']) == sign]
        return ', '.join(found) or 'none'

    def handover(lum):
        if lum is None:
            return 'not computed'
        if lum['void']:
            return (f"none: leaves {lum['sign']} on day {lum['exit_day']:.2f} without a connection"
                    + (f"; the lord of the house, {lum['house_lord']}, stands in (II.22, 4)" if year_lord == 'Moon' else ''))
        return '; '.join(f"{c['planet']} by {c['aspect']} on day {c['day']:.2f}" for c in lum['connections'])

    rows = []
    if year_lord == 'Sun':
        rows.append({'Proxy': '[II.14] The distributor (probably of the longevity releaser, fn 249)',
                     'Reads': r_dist_text, 'Source': 'II.14, 1'})
        rows.append({'Proxy': "[1] The lord of the sign in which the distribution of the lifespan from the releaser is",
                     'Reads': r_sign_text, 'Source': 'II.13, 1; fn 238'})
        rows.append({'Proxy': '[2] The planet in Leo in the root; in the revolution',
                     'Reads': f"root: {in_sign(natal, 'Leo')}; revolution: {in_sign(rev, 'Leo')}", 'Source': 'II.13, 1'})
        rows.append({'Proxy': '[3] The planet to which the Sun hands over the management, so long as he is in his sign',
                     'Reads': handover(sun) + ' -- the revolution\'s Sun, applying (fn 239)', 'Source': 'II.13, 1; fn 239'})
        sun_lon = rev['Sun']['longitude']
        rows.append({'Proxy': '[4] Where the Sun is, as a witness',
                     'Reads': f"{get_zodiac_sign(sun_lon)} ({get_degree_string(sun_lon)}), lord "
                              f"{SIGN_TO_DOMICILE.get(get_zodiac_sign(sun_lon), '-')} (fn 241)", 'Source': 'II.13, 1; fn 241'})
    else:
        rows.append({'Proxy': '[1] The distributor (of the longevity releaser, fn 308)',
                     'Reads': r_dist_text, 'Source': 'II.22, 1; fn 308'})
        rows.append({'Proxy': "[2] The lord of the sign in which the distribution from the releaser is",
                     'Reads': r_sign_text, 'Source': 'II.22, 1; fn 309'})
        rows.append({'Proxy': '[3] The planet in Cancer in the root; in the revolution',
                     'Reads': f"root: {in_sign(natal, 'Cancer')}; revolution: {in_sign(rev, 'Cancer')}", 'Source': 'II.22, 1'})
        rows.append({'Proxy': '[4] The planet the Moon connects with, so long as she is in her sign',
                     'Reads': handover(moon), 'Source': 'II.22, 1-2'})
        rows.append({'Proxy': '[5] If she is empty in course, the lord of her house',
                     'Reads': (f"she is empty in course: {moon['house_lord']}" if moon and moon['void']
                               else 'not empty in course' if moon else 'not computed'), 'Source': 'II.22, 4'})
        m = rev['Moon']
        elong = (m['longitude'] - rev['Sun']['longitude']) % 360.0
        rows.append({'Proxy': "[6] Her conditions (facts; the judgment of II.22, 6-10 is not built)",
                     'Reads': (f"{'northern' if m.get('latitude', 0.0) >= 0 else 'southern'} in latitude "
                               f"({m.get('latitude', 0.0):+.2f}); {'increasing' if elong < 180.0 else 'decreasing'} in glow "
                               f"({elong:.1f}° from the Sun); motion {m.get('speed_in_lon', 0.0):.2f}° a day"),
                     'Source': 'II.22, 5-10'})
    return rows

# --- II.3, 2-19: the sign of the terminal point and its lord, examined ----
# II.3, 2: the sign of the terminal point in the ROOT -- which house of
# the circle ("the stakes or what follows them, or those falling from
# them"), whose house, exaltation and triplicity, which planets, Lots and
# twelfth-parts are in it, who looks at it or casts rays at it and from
# which sign and degree, to what bound, face and degree the rays fall,
# and whether it is devoid of them. II.3, 3: the same in the REVOLUTION,
# with where those planets were in the root and are now, and their
# condition in each. II.3, 5-8: the lord of the year's condition in root
# and revolution compared four ways (Figure 55), and 5-6 say what
# "suitable" is: direct, in its own domain (sect, fn 46), in its own
# glow, in a sign in which it has a claim, safe from the infortunes, in
# an excellent place from the three signs; the contrary: retrograde,
# burned or under the rays, exile, westernized from the Sun (fn 47),
# assembled with or inspected by the infortunes, out of sect. II.3, 9-18
# refine by reception, by a stake of the revolution's Ascendant under a
# non-receiving infortune's square or opposition, and by aversion to the
# Ascendant (the second, sixth, eighth and twelfth).
#
# FACTS, NOT A VERDICT. The book gives the factors and no rule for
# weighing them, so every factor is shown for each chart and Figure 55's
# four sentences are quoted with the cell left to the reader (owner's
# decision 2026-09-10). The engine's own evaluators supply the facts --
# essential and accidental dignity, solar phase, reception -- run on the
# revolution's data as they run on the root's. Read since 2026-09-11 (orders
# PN4R-4l-7 and GAP-34): the twelfth-parts, and fn 37-41's classes of sign
# (VI.4's loving/hating/hostile by aspect; IX.2, 33's pairs) and of degree
# (V.20). Earlier text here: not read; fn 37-41's classes of sign (helpful, hostile, matching in ascensions)
# and of degree (bright, dark, smoky). The delineations II.4-II.21 are
# not built. No worked example exists; Figure 55 is Dykes' table.

PN4_II3_FIGURE_55 = (
    ('suitable in the root, suitable in the revolution', 'II.3, 5',
     'the safety of the body for the owner of the revolution of the year, and the goodness of his soul, and his '
     'delighting in the things which the lord of the year indicates'),
    ('suitable in the root, contrary in the revolution', 'II.3, 6',
     'weakness in that year, and a decrease in everything we stated'),
    ('bad in the root, excellent in the revolution', 'II.3, 7',
     'his condition will improve somewhat in that year, and it will revive things for him in it, which he will delight in'),
    ('bad in both the root and the revolution', 'II.3, 8',
     'an excess of adversity in the category of what it indicates'),
)

def _pn4_house_class(house):
    """II.3, 2 [1]: "the stakes or what follows them, or those falling from
    them"."""
    return 'a stake' if house in (1, 4, 7, 10) else 'following a stake' if house in (2, 5, 8, 11) else 'falling from a stake'

def _pn4_tag(planet):
    n = pn4_nature(planet)
    return f"{planet} ({n or 'neither'})"

def _pn4_looks_at_sign(data, sign, exclude=()):
    """Whole-sign aspects to a sign from the planets of a chart: (planet,
    aspect name, the planet's longitude, the degree its ray reaches in the
    sign). A planet IN the sign is reported as 'in it'."""
    out = []
    target_idx = SIGN_ORDER.index(sign)
    for planet in PN4_SEVEN:
        if planet in exclude or planet not in data:
            continue
        lon = data[planet]['longitude'] % 360.0
        apart = (target_idx - int(lon // 30)) % 12
        apart = min(apart, 12 - apart)
        entry = ASPECT_BY_SIGN_COUNT.get(apart)
        if entry is None:
            continue
        name = 'in it' if apart == 0 else entry[0].lower()
        ray_deg = target_idx * 30.0 + (lon % 30.0)
        out.append((planet, name, lon, ray_deg))
    return out

def _pn4_lots_in_sign(chart, sign):
    names = []
    for d in operative_lot_definitions():
        lon = lot_by_id(d['id'], chart['planetary_data'], chart['ascendant'], chart['houses'], chart['sect'])
        if lon is not None and get_zodiac_sign(lon) == sign:
            names.append(d['name'])
    return names

def _pn4_sign_class_facts(from_lon, aspect, target_sign, ray_deg):
    """II.3, 2's classes of sign and of degree, as facts (order GAP-34):
    fn 37 -> Gr. Intr. VI.4, 4-6, the aspect's kind (sextile and trine
    loving, square hating, opposition hostile); fn 38 -> IX.2, 33, the two
    signs matching in ascensions, in daylight, or in one belt (one domicile
    lord); fn 39, 41 -> Gr. Intr. V.20, the body's and the ray's degree
    class (bright, dusky, empty, dark) -- the editor's "probably"."""
    kind = {'sextile': 'loving', 'trine': 'loving', 'square': 'hating', 'opposition': 'hostile'}.get(aspect, aspect)
    a_sign = get_zodiac_sign(from_lon)
    pair = frozenset({a_sign, target_sign})
    matches = []
    if pair in EQUAL_ASCENSION_PAIRS:
        matches.append('matching in ascensions')
    if pair in EQUAL_DAYLIGHT_PAIRS:
        matches.append('matching in daylight')
    if SIGN_TO_DOMICILE.get(a_sign) == SIGN_TO_DOMICILE.get(target_sign):
        matches.append('one belt')
    return (f"{kind} (VI.4, 4-6); {', '.join(matches) if matches else 'no match'} (IX.2, 33); body in a "
            f"{_brightness_category(from_lon)} degree, the ray in a {_brightness_category(ray_deg)} degree (V.20, 'probably')")

def _pn4_twelfth_parts_in_sign(data, sign):
    """The planets whose twelfth-part (Gr. Intr. V.18, 3, Figure 57) falls in
    the sign, as the II.3, 2 examination asks."""
    return ', '.join(p for p in PN4_SEVEN if p in data and get_zodiac_sign(pn4_twelfth_part(data[p]['longitude'])) == sign) or 'none'

@_connection_aware
def pn4_ii3_examination(chart_data, sr, year, jd_sr):
    """II.3, 2-19 as facts. Returns {root_rows, revolution_rows, lord_rows,
    refinement_rows, figure_55}."""
    natal, rev = chart_data['planetary_data'], sr['planetary_data']
    n_asc, r_asc = chart_data['ascendant'], sr['ascendant']
    sign, year_lon, lord = year['sign'], year['longitude'], year['lord']
    rulers = get_essential_rulers(year_lon)
    trip = rulers['triplicity_day'] if chart_data['sect'] == 'Diurnal' else rulers['triplicity_night']

    # --- II.3, 2: the root ---
    h = get_wsh_house(year_lon, n_asc)
    looks = _pn4_looks_at_sign(natal, sign)
    rays = [(p, a, lon, deg) for p, a, lon, deg in looks if a != 'in it']
    root_rows = [
        {'Question': '[1] Which house of the circle it is', 'Reads': f"house {h} from the natal Ascendant, {_pn4_house_class(h)}",
         'Source': 'II.3, 2; VI.3'},
        {'Question': '[2] Whose house, exaltation and triplicity',
         'Reads': f"house of {_pn4_tag(rulers['domicile'])}; exaltation of "
                  f"{_pn4_tag(rulers['exaltation']) if rulers['exaltation'] not in (None, '-', '') else 'none'}; triplicity of "
                  f"{_pn4_tag(trip)} ({'day' if chart_data['sect'] == 'Diurnal' else 'night'})",
         'Source': 'II.3, 2'},
        {'Question': '[3] Which planets, Lots and twelfth-parts are in it in the root',
         'Reads': f"planets: {_pn4_natal_planets_in_sign(natal, sign)}; Lots: "
                  f"{', '.join(_pn4_lots_in_sign(chart_data, sign)) or 'none'}; twelfth-parts of: {_pn4_twelfth_parts_in_sign(natal, sign)}",
         'Source': 'II.3, 2; VI.4; Gr. Intr. V.18, 3 (Figure 57)'},
        {'Question': '[4-7] Who looks at it or casts rays at it, from which sign and degree, and to what bound and face',
         'Reads': ('; '.join(f"{_pn4_tag(p)} by {a} from {get_degree_string(lon)}, the ray at {get_degree_string(deg)} "
                             f"(bound of {pn4_bound_lord(deg)}, face of {get_essential_rulers(deg)['face']}) -- "
                             f"{_pn4_sign_class_facts(lon, a, sign, deg)}"
                             for p, a, lon, deg in rays) or 'none'),
         'Source': 'II.3, 2; fn 37 (Gr. Intr. VI.4, 4-6); fn 38 (IX.2, 33); fn 39, 41 (Gr. Intr. V.20, "probably")'},
        {'Question': '[8] Whether it falls away from the view of the planets and their rays',
         'Reads': 'yes: devoid of them' if not looks else 'no', 'Source': 'II.3, 2'},
    ]

    # --- II.3, 3: the revolution ---
    r_looks = _pn4_looks_at_sign(rev, sign)
    r_in = [p for p, a, _l, _d in r_looks if a == 'in it']
    r_rays = [(p, a, lon, deg) for p, a, lon, deg in r_looks if a != 'in it']
    ess_n, ess_r = evaluate_essential_dignities(natal, chart_data['sect']), evaluate_essential_dignities(rev, sr['sect'])
    acc_n = evaluate_accidental_dignities(natal, chart_data['houses'], chart_data['sect'], chart_data.get('julian_day'),
                                          armc=chart_data.get('armc'), obliquity=chart_data.get('obliquity'), geo_lat=chart_data.get('geo_lat'))
    acc_r = evaluate_accidental_dignities(rev, sr['houses'], sr['sect'], jd_sr,
                                          armc=sr.get('armc'), obliquity=sr.get('obliquity'), geo_lat=sr.get('geo_lat'))

    def labels(planet, ess, acc):
        return ', '.join((ess.get(planet, {}).get('Essential Labels') or []) + (acc.get(planet, {}).get('Accidental Labels') or [])) or 'none'

    involved = [p for p, _a, _l, _d in r_looks]
    revolution_rows = [
        {'Question': '[1] Which revolution planets are in it, and whose twelfth-parts',
         'Reads': (', '.join(_pn4_tag(p) for p in r_in) or 'none') + f"; twelfth-parts of: {_pn4_twelfth_parts_in_sign(rev, sign)}",
         'Source': 'II.3, 3; VI.3; Gr. Intr. V.18, 3 (Figure 57)'},
        {'Question': '[2, 4, 5] Who looks at it, and from what direction',
         'Reads': '; '.join(f"{_pn4_tag(p)} by {a} from {get_degree_string(lon)}" for p, a, lon, _d in r_rays) or 'none',
         'Source': 'II.3, 3'},
        {'Question': '[3] Whether it is devoid of their alighting in or looking at it',
         'Reads': 'yes: devoid' if not r_looks else 'no', 'Source': 'II.3, 3'},
        {'Question': '[6] Where those planets were in the root, and where they are in the revolution',
         'Reads': '; '.join(f"{p}: house {get_wsh_house(natal[p]['longitude'], n_asc)} -> {get_wsh_house(rev[p]['longitude'], n_asc)} "
                            f"from the natal Ascendant" for p in involved if p in natal) or 'none',
         'Source': 'II.3, 3; VI.5'},
        {'Question': '[7] Their condition in the root; in the revolution (the app\'s labels, not a verdict)',
         'Reads': '; '.join(f"{p}: root {labels(p, ess_n, acc_n)} | revolution {labels(p, ess_r, acc_r)}" for p in involved) or 'none',
         'Source': 'II.3, 3-4; I.7'},
    ]

    # --- II.3, 5-6: the lord of the year's factors, per chart ---
    def factors(data, chart, ess, acc, asc_for_place):
        row = data.get(lord)
        if not row:
            return {}
        e, a = ess.get(lord, {}), acc.get(lord, {})
        phase, side, _el = solar_phase(lord, row['longitude'], data['Sun']['longitude'], row.get('speed_in_lon'))
        claim = [k for k in ('Domicile', 'Exalt', 'Triplicity', 'Term', 'Face') if e.get(k)]
        infortunes = [f"{p} by {asp}" for p, asp, _l, _d in _pn4_looks_at_sign(data, get_zodiac_sign(row['longitude']))
                      if p in INFORTUNES and p != lord]
        return {
            'motion': 'retrograde' if row.get('speed_in_lon', 1.0) < 0 else 'direct',
            'glow': (phase.lower() if phase else 'in its own glow') + (f", {side}" if side else ''),
            'domain': domain_description(a),
            'claim': ('a claim: ' + ', '.join(claim).lower()) if claim else ('exile (detriment)' if e.get('Detriment') else 'fall' if e.get('Fall') else 'peregrine'),
            'infortunes': ', '.join(infortunes) or 'none by whole sign',
            'place': f"house {get_wsh_house(row['longitude'], n_asc)} / {get_wsh_house(row['longitude'], year_lon)} / {get_wsh_house(row['longitude'], r_asc)}",
        }
    f_n, f_r = factors(natal, chart_data, ess_n, acc_n, n_asc), factors(rev, sr, ess_r, acc_r, r_asc)
    lord_rows = [
        {'Factor': label, 'Root': f_n.get(key, '-'), 'Revolution': f_r.get(key, '-'), 'Source': cite}
        for key, label, cite in (
            ('motion', 'Direct in course, or retrograde', 'II.3, 5-6'),
            ('glow', 'In its own glow, or burned / under the rays; eastern or western', 'II.3, 5-6; fn 47'),
            ('domain', 'In its own domain (hayyiz, Gr. Intr. VII.1, 37-39), or the contrary; fn 46 reads it as sect', 'II.3, 5-6; fn 46, 48; Gr. Intr. VII.1, 37-39'),
            ('claim', 'In a sign in which it has a claim, or exile', 'II.3, 5-6'),
            ('infortunes', 'The infortunes assembled with it or inspecting it (whole sign)', 'II.3, 5-6'),
            ('place', 'Its place from the natal Ascendant / the terminal sign / the revolution Ascendant', 'II.3, 5'),
        )
    ]

    # --- II.3, 9-18: the refinements, as facts ---
    def received(data, sect):
        try:
            rows = evaluate_reception(data, sect)
        except Exception:
            return 'not computed'
        by = [r['Receiver'] for r in rows if r.get('Received') == lord or (r.get('Received') == 'each other' and lord in str(r.get('Receiver')))]
        return ('received by ' + ', '.join(sorted(set(by)))) if by else 'not received'

    r_lord = rev.get(lord)
    ref_rows = []
    ref_rows.append({'Refinement': 'Received, or not (root; revolution)',
                     'Reads': f"root: {received(natal, chart_data['sect'])}; revolution: {received(rev, sr['sect'])}",
                     'Source': 'II.3, 9-12 (under the Configurations page\'s reception rule)'})
    if r_lord:
        rh_rasc = get_wsh_house(r_lord['longitude'], r_asc)
        rh_nasc = get_wsh_house(r_lord['longitude'], n_asc)
        harming = [f"{p} by {asp}" for p, asp, _l, _d in _pn4_looks_at_sign(rev, get_zodiac_sign(r_lord['longitude']))
                   if p in INFORTUNES and p != lord and asp in ('square', 'opposition')]
        ref_rows.append({'Refinement': "In a stake of the revolution's Ascendant, and squared or opposed by an infortune",
                         'Reads': f"house {rh_rasc} from the revolution Ascendant ({_pn4_house_class(rh_rasc)}); "
                                  f"infortunes by square or opposition: {', '.join(harming) or 'none'}; whether the infortune "
                                  f"receives it is read from the row above",
                         'Source': 'II.3, 13-15'})
        ref_rows.append({'Refinement': 'Looking at the Ascendant, or in the four positions that do not (2, 6, 8, 12)',
                         'Reads': f"house {rh_nasc} from the natal Ascendant: "
                                  + ('does not look at the Ascendant -- the detestable thing hidden (II.3, 17)' if rh_nasc in (2, 6, 8, 12)
                                     else 'looks at the Ascendant' + (' from a stake' if rh_nasc in (1, 4, 7, 10) else ', not from a stake (II.3, 16)')),
                         'Source': 'II.3, 16-18'})
    figure_55 = [{'Lord of the year': case, 'Indicates': f'"{text}"', 'Source': cite} for case, cite, text in PN4_II3_FIGURE_55]
    return {'root_rows': root_rows, 'revolution_rows': revolution_rows, 'lord_rows': lord_rows,
            'refinement_rows': ref_rows, 'figure_55': figure_55}

# --- I.6, 1-11: the image of the revolution of the year, as an inventory ---
# "write down the planets of the revolution of the year, with their
# conditions ..., and their rays and twelfth-parts, and the twelfth-parts
# of the degrees of the houses" (I.6, 3); "the planets of the root of the
# nativity, with their conditions and rays and twelfth-parts, and the
# twelfth-parts of the signs, and the Lots and Head and Tail" (4); "the
# Ascendant of the root, and the position of the terminal point of the
# year" (5); "the endpoint of the distribution, the distributor and the
# one partnering with it in the management, and the lord of the fardar,
# and the one dividing [its fardar] with it, and the lord of the orb, each
# of them in their signs and bounds" (6); a fixed star on the natal
# Ascendant, Midheaven, a luminary or an angular planet (7). The count
# (8, Figure 52): 14 planets, 98 rays, the Head and Tail twice each, 38
# twelfth-parts (24 of the houses, 14 of the planets) -- 154 -- "and the
# Lots according to how you do it". Within a house, by degree (9-10).
#
# A TABLE, NOT THE WHEEL of I.6, 1: every point by whole-sign house from
# the revolution's Ascendant (fn 33: Dykes drew it that way for clarity;
# I.6, 2's quadrant cusps are listed as points of their own), ordered by
# degree within the house. The twelfth-part construction -- 2.5 degrees
# to a sign, beginning with the sign itself -- is Gr. Intr. V.18, 1-3's
# (Figure 57), as _twelfth_part_sign says. The fixed stars of I.6, 7 are
# pn4_fixed_stars_in_image (Sahl's list, On Nativities 2.2; the Swiss
# Ephemeris catalogue; since 2026-09-11, order PN4R-4n-7). The Lots are the
# engine's, "many or few" (I.6, 8). Decided by the owner 2026-09-10.

def pn4_twelfth_part(lon):
    """The twelfth-part of a degree, as a longitude: each 2.5 degrees of a
    sign maps to one whole sign in order, beginning with the sign itself,
    and the position inside the 2.5 is spread over that sign's 30.
    Gr. Intr. V.18, 3's own calculation (the degree and minute from the
    sign's beginning times 12, cast out from that sign's beginning); the
    sign agrees with _twelfth_part_sign."""
    lon %= 360.0
    sign_idx, within = int(lon // 30), lon % 30.0
    step = int(within // 2.5)
    return (((sign_idx + step) % 12) * 30.0 + (within - step * 2.5) * 12.0) % 360.0

# --- The fixed stars: I.6, 7 and III.8, 9 (order PN4R-4n-7, 2026-09-11) --------
# I.6, 7: "if ... in the root of the nativity, one of the fixed stars was in
# the very degree of the Ascendant, or in the very degree of the stake of the
# Midheaven, or with one of the luminaries, or with one of the seven planets
# which are in the stakes, then write it down in them." III.8, 9: "if in the
# revolution of the year one of the fixed stars indicative of good fortune
# was in the Ascendant of the year, or in the degree of the tenth from it, or
# in the degree of the terminal point, or in the degree of the distribution,
# or with their lords, or with one of the luminaries, it indicates good
# fortune in that year." WHICH stars: PN IV names none; the list is Sahl's,
# On Nativities 2.2 (al-Andarzaghar's chapter on the greatest good fortune),
# where Dykes's table at the chapter's end identifies each star Sahl names
# (his identifications, with Rhetorius Ch. 58's natures); the two stars of
# that table Sahl does not carry (Deneb Adige, Arcturus) are left out; the two
# he carries under ANOTHER nature (Alphecca as Jupiter-Mercury, fn 73;
# Menkalinan with Jupiter-Mars, fn 75 -- not "doubtful", as an earlier label
# said) are kept with Rhetorius's nature and Sahl's named. Positions: the Swiss Ephemeris star catalogue (sefstars.txt),
# SHIPPED with the app as ephe/sefstars.txt (AGPL-3.0, see ephe/README.md)
# and found first there; without any catalogue the table says so and
# computes nothing. Readings, the engine's: "the very degree" and
# "with" are both read as within one degree of longitude; "the stakes" for
# I.6, 7's planets are the whole-sign places 1, 4, 7, 10; latitude is ignored.
SAHL_FIXED_STARS = (
    ('Spica', 'Venus-Mercury'), ('Vega', 'Venus-Mercury'), ('Fomalhaut', 'Venus-Mercury'),
    ('Alphecca', 'Venus-Mercury (Sahl, following al-Andarzaghar, classifies it as Jupiter-Mercury, fn 73)'),
    ('Regulus', 'Jupiter-Mars'), ('Altair', 'Jupiter-Mars'), ('Antares', 'Jupiter-Mars (fn 74)'), ('Sirius', 'Jupiter-Mars'),
    ('Rigel', 'Jupiter-Saturn'), ('Alnilam', 'Jupiter-Saturn'), ('Menkalinan', 'Jupiter-Saturn (Sahl, following al-Andarzaghar, classifies it with Jupiter-Mars, fn 75)'),
    ('Rukbat', 'Jupiter-Saturn'), ('Algol', 'Jupiter-Saturn'), ('Capella', 'Jupiter-Saturn'),
    ('Pollux', 'Mars'), ('Zuben Eschamali', 'Jupiter-Venus'), ('Castor', 'Jupiter-Venus'),
    ('Bellatrix', 'Mars-Venus'), ('Procyon', 'Mars-Venus'), ('Betelgeuse', 'Mars-Venus'), ('Alpheratz', 'Mars-Venus'),
    ('Scheat', 'Mars-Venus'), ('Toliman', 'Venus-Jupiter'), ('Acamar', 'Venus-Jupiter'),
    ('Denebola', 'Saturn-Venus'), ('Zosma', 'Saturn-Venus'), ('Alphard', 'Saturn-Venus'), ('Aldebaran', 'Mars-Venus'),
)
FIXED_STAR_ORB = 1.0
_FIXED_STAR_STATE = {'checked': False, 'ready': False, 'where': None, 'why': None}

def _fixed_star_attach(ephe_dir, source):
    """Point Swiss Ephemeris at `ephe_dir` and prove the catalogue reads
    (Spica at J2000). Records the source on success; the exception's text
    on failure, so the page can say what actually went wrong."""
    try:
        swe.set_ephe_path(str(ephe_dir))
        swe.fixstar2_ut('Spica', 2451545.0, swe.FLG_SWIEPH)
        _FIXED_STAR_STATE.update(ready=True, where=str(source), why=None)
        return True
    except Exception as exc:
        _fixed_star_why(f"found {source} but Swiss Ephemeris could not read it from {ephe_dir}: {exc!r}")
        return False

def _fixed_star_why(text):
    """Accumulate the reasons, in the order tried, for the page."""
    _FIXED_STAR_STATE['why'] = text if not _FIXED_STAR_STATE['why'] else f"{_FIXED_STAR_STATE['why']}; then {text}"

def _fixed_star_catalogue_ready():
    """Find a Swiss Ephemeris star catalogue and point the ephemeris at a
    directory holding nothing but it, so the planets (Moshier, no planetary
    files) are untouched. Looked for, in this order: the catalogue SHIPPED
    with the app (`ephe/sefstars.txt` beside app.py, the same directory
    `atlas.db` is read from; owner's decision 2026-09-11, the app is to be
    portable) -- that directory holds no `.se1` file by construction, so
    the ephemeris is pointed at it DIRECTLY, with no link, no copy and no
    writable user directory (the symlink-or-copy into the user data dir
    was the step a Windows build could fail on, 2026-09-11); then
    $SE_EPHE_PATH, the engine's own data directory, and any package in this
    interpreter's site-packages that ships one, each linked (copied where
    links are not allowed) into a private `ephe_stars` directory under the
    user data dir. `_FIXED_STAR_STATE['why']` says what failed, for the
    page."""
    # Attached per thread. Swiss Ephemeris keeps its ephemeris path in
    # thread-local state and Streamlit runs each session's script in its own
    # thread, so a thread that has not attached the catalogue for itself has
    # no catalogue at all, however ready this module says it is -- the stars
    # went missing from the Timing page of every session after the first
    # when the engine stopped being re-executed each run. The module's own
    # 'checked' flag stays and is still the outer gate, so clearing it
    # forces the whole search again.
    if _FIXED_STAR_STATE['checked'] and getattr(_RUN, 'fixed_stars_attached', False):
        return _FIXED_STAR_STATE['ready']
    _FIXED_STAR_STATE['checked'] = True
    _RUN.fixed_stars_attached = True
    import glob, sys
    bundled = Path(__file__).parent / 'ephe' / 'sefstars.txt'
    if os.path.isfile(str(bundled)) and _fixed_star_attach(bundled.parent, bundled):
        return True
    candidates = []
    if os.environ.get('SE_EPHE_PATH'):
        candidates.append(os.path.join(os.environ['SE_EPHE_PATH'], 'sefstars.txt'))
    try:
        candidates.append(str(_user_data_dir() / 'ephe' / 'sefstars.txt'))
    except Exception:
        pass
    for base in sys.path:
        if base and base.endswith('site-packages'):
            candidates += glob.glob(os.path.join(base, '*', 'sweph', 'sefstars.txt')) + glob.glob(os.path.join(base, '*', 'sefstars.txt'))
    source = next((c for c in candidates if c and os.path.isfile(c)), None)
    if source is None:
        if _FIXED_STAR_STATE['why']:                  # the bundled file was found and failed to attach
            _fixed_star_why("no other sefstars.txt in $SE_EPHE_PATH, the user data directory or site-packages")
        else:
            _fixed_star_why(f"no sefstars.txt at the bundled path {bundled}, in $SE_EPHE_PATH, in the user data directory or in site-packages")
        return False
    try:
        private = _user_data_dir() / 'ephe_stars'
        private.mkdir(parents=True, exist_ok=True)
        link = private / 'sefstars.txt'
        if link.exists() or link.is_symlink():      # an older link may point elsewhere
            link.unlink()
        try:
            link.symlink_to(source)
        except OSError:
            import shutil
            shutil.copyfile(source, link)
    except Exception as exc:
        _fixed_star_why(f"found {source} but could not link it into {_user_data_dir() / 'ephe_stars'}: {exc!r}")
        return False
    return _fixed_star_attach(private, source)

def fixed_star_refusal():
    """The sentence the page prints when no catalogue is attached, with the
    reason recorded by the search."""
    _fixed_star_catalogue_ready()
    return ("no Swiss Ephemeris star catalogue (sefstars.txt) is attached; nothing is computed"
            + (f" -- {_FIXED_STAR_STATE['why']}" if _FIXED_STAR_STATE['why'] else ''))

def fixed_star_longitudes(jd):
    """{name: longitude} for Sahl's stars at jd, or None without a catalogue.
    A star the catalogue cannot read is recorded in
    _FIXED_STAR_STATE['missing'] (name: exception) and the page names it,
    rather than the row silently vanishing."""
    if not _fixed_star_catalogue_ready():
        return None
    out, missing = {}, {}
    for name, _nature in SAHL_FIXED_STARS:
        try:
            out[name] = swe.fixstar2_ut(name, jd, swe.FLG_SWIEPH)[0][0] % 360.0
        except Exception as exc:
            missing[name] = repr(exc)
    _FIXED_STAR_STATE['missing'] = missing
    return out

def fixed_star_missing_note():
    """The sentence naming stars the attached catalogue could not read, or ''."""
    missing = _FIXED_STAR_STATE.get('missing') or {}
    if not missing:
        return ''
    return ("Not in the catalogue attached, so not placed: " + ', '.join(f"{n} ({e})" for n, e in sorted(missing.items())) + ".")

def _star_hits(stars, places, orb=FIXED_STAR_ORB):
    """Rows of (star, place, distance) for every star within orb of a place."""
    rows = []
    for name, nature in SAHL_FIXED_STARS:
        if name not in stars:
            continue
        for label, lon in places:
            d = abs(((stars[name] - lon + 180.0) % 360.0) - 180.0)
            if d <= orb:
                rows.append({'Star': name, 'Longitude': get_degree_string(stars[name]), 'Nature (Rhetorius, per Dykes)': nature,
                             'Place': label, 'Distance': f"{d:.2f} deg"})
    return rows

def pn4_fixed_stars_in_image(chart_data, jd):
    """I.6, 7's four natal places. Returns {'rows', 'refused'}."""
    stars = fixed_star_longitudes(jd)
    if stars is None:
        return {'rows': [], 'refused': fixed_star_refusal()}
    p = chart_data['planetary_data']
    asc = chart_data['ascendant']
    places = [('the very degree of the Ascendant', asc), ('the very degree of the Midheaven', chart_data['mc']),
              ('with the Sun', p['Sun']['longitude']), ('with the Moon', p['Moon']['longitude'])]
    for planet in ('Saturn', 'Jupiter', 'Mars', 'Venus', 'Mercury'):
        if planet in p and get_wsh_house(p[planet]['longitude'], asc) in (1, 4, 7, 10):
            places.append((f"with {planet}, in a stake (whole-sign place {get_wsh_house(p[planet]['longitude'], asc)})", p[planet]['longitude']))
    return {'rows': _star_hits(stars, places), 'refused': None, 'source_dir': _FIXED_STAR_STATE['where'],
            'missing': fixed_star_missing_note()}

def pn4_fixed_stars_in_revolution(sr, jd_sr, year_lon, endpoint_lon):
    """III.8, 9's places in the revolution. Returns {'rows', 'refused'}."""
    stars = fixed_star_longitudes(jd_sr)
    if stars is None:
        return {'rows': [], 'refused': fixed_star_refusal()}
    rev = sr['planetary_data']
    places = [('the Ascendant of the year', sr['ascendant']), ('the degree of the tenth from it', sr['mc']),
              ('the degree of the terminal point', year_lon)]
    if endpoint_lon is not None:
        places.append(('the degree of the distribution', endpoint_lon))
    for label, lon in list(places):
        lord = SIGN_TO_DOMICILE.get(get_zodiac_sign(lon))
        if lord in rev:
            places.append((f"with {lord}, lord of {label.replace('the ', '', 1)}", rev[lord]['longitude']))
    places += [('with the Sun of the revolution', rev['Sun']['longitude']), ('with the Moon of the revolution', rev['Moon']['longitude'])]
    return {'rows': _star_hits(stars, places), 'refused': None}

@_pn4_opening_aware
def pn4_revolution_image(chart_data, sr, year, age, current, fardar, orb, lat, elapsed=None, supplement=False):
    """I.6, 3-8: every point of the image, as rows, and the count. `elapsed`
    is the elapsed years the distribution's endpoint is read at (I.6, 6);
    the integer `age` when not given."""
    natal, rev = chart_data['planetary_data'], sr['planetary_data']
    r_asc = sr['ascendant']
    rows = []

    def add(chart, kind, point, lon, cite):
        lon %= 360.0
        # I.6, 2: the image's houses are "calculat[ed] ... by their degrees
        # and minutes, in the way that you calculate the houses by the
        # portions of hours and the ascensions of the right circle" -- the
        # revolution's cusps (the Alcabitius set this engine computes), not
        # whole signs; fn 33's whole-sign Figure 51 was Dykes's simplification
        # "for clarity". Filed by whole sign until 2026-09-11 (order PN4R-4n-5).
        rows.append({'lon': lon, 'House': get_house_number(lon, sr['houses']), 'Chart': chart, 'Kind': kind, 'Point': point,
                     'Position': get_degree_string(lon), 'Bound': pn4_bound_lord(lon), 'Source': cite})

    counts = {'planets': 0, 'rays': 0, 'nodes': 0, 'twelfth-parts of houses': 0, 'twelfth-parts of planets': 0, 'Lots': 0}
    for label, chart, cite in (('revolution', sr, 'I.6, 3'), ('root', chart_data, 'I.6, 4')):
        data = chart['planetary_data']
        for lon, kind, who, aspect in pn4_bodies_and_rays(data):
            if kind == 'body':
                row = data[who]
                cond = ('retrograde' if row.get('speed_in_lon', 1.0) < 0 else 'direct')
                add(label, 'planet', f"{who} ({cond})", lon, cite)
                counts['planets'] += 1
                add(label, 'twelfth-part of a planet', f"twelfth-part of {who}", pn4_twelfth_part(lon), cite)
                counts['twelfth-parts of planets'] += 1
            else:
                add(label, 'ray', f"{who} by {aspect}", lon, cite)
                counts['rays'] += 1
        node = data.get('North Node')
        if node:
            add(label, 'node', 'Head', node['longitude'], cite)
            add(label, 'node', 'Tail', node['longitude'] + 180.0, cite)
            counts['nodes'] += 2
        for i, cusp in enumerate(list(chart['houses'])[:12]):
            add(label, 'twelfth-part of a house', f"twelfth-part of the degree of house {i + 1} ({get_degree_string(cusp)})",
                pn4_twelfth_part(cusp), cite + ' (fn 31: the quadrant cusps)')
            counts['twelfth-parts of houses'] += 1
        for lot in operative_lot_rows(data, chart['ascendant'], chart['houses'], chart['sect'], supplement):
            add(label, 'Lot', lot['Lot'], lot['Longitude'], 'I.6, 4; 8: "according to how you do it"')
            counts['Lots'] += 1
    add('root', 'point', 'Ascendant of the root', chart_data['ascendant'], 'I.6, 5')
    add('root', 'point', 'Terminal point of the year', year['longitude'], 'I.6, 5')
    if current:
        endpoint = _pn4_seg_degree({'from': float(age if elapsed is None else elapsed)}, chart_data['ascendant'], chart_data, lat)
        add('root', 'time lord', 'Endpoint of the distribution (the degree reached now)', endpoint, 'I.6, 6; fn 34')
        for name, planet in (('the distributor', current['distributor']), ('the partner in the management', current['partner'])):
            if planet and planet in natal:
                add('root', 'time lord', f"{planet}, {name}", natal[planet]['longitude'], 'I.6, 6')
    if fardar:
        for name, planet in (('lord of the fardar', fardar.get('lord')), ('dividing the fardar with it', fardar.get('sub_lord'))):
            if planet and planet in natal:
                add('root', 'time lord', f"{planet}, {name}", natal[planet]['longitude'], 'I.6, 6')
    if fardar and fardar.get('lord') in ('Head', 'Tail') and 'North Node' in natal:
        node_lon = natal['North Node']['longitude'] + (180.0 if fardar['lord'] == 'Tail' else 0.0)
        add('root', 'time lord', f"{fardar['lord']}, lord of the fardar", node_lon, 'I.6, 6; IV.1, 8')
    if orb and orb in natal:
        add('root', 'time lord', f"{orb}, lord of the orb", natal[orb]['longitude'], 'I.6, 6')
    # by degree within the house, counted FROM ITS CUSP: a quadrant house
    # can straddle 0 Aries, where raw longitude would put its last degrees first.
    rows.sort(key=lambda r: (r['House'], (r['lon'] - sr['houses'][r['House'] - 1]) % 360.0))
    for r in rows:
        del r['lon']
    counts['total of I.6, 8'] = counts['planets'] + counts['rays'] + counts['nodes'] + counts['twelfth-parts of houses'] + counts['twelfth-parts of planets']
    return rows, counts

# --- I.7, 1-26: the reading checklist -------------------------------------
# "If you made the image of the revolution of the year, then understand:"
# (I.7, 1) -- twenty-six things. 2-6 concern the revolution's Ascendant:
# its house in the root, who is in it and looks at it in both times, who
# has a claim on it and where they stand (in a share or in exile), and
# whether its lord has one house or two and looks at them. 7-24 run over
# all the planets (fn 39): direct or retrograde (7); strong or weak (8);
# "rising and falling" (9); aversion (10); assembly and whole-sign aspect
# (11, fn 41); rays by degree (12, fn 42); connection and separation
# (13); reception (14); supporting or corrupting (15); hostile or
# friendly, harmonizing or contrary (16); domain (17); their
# twelfth-parts (18); returns to their rooted positions (19, fn 44);
# their course in the signs (20); their transits of the natal,
# revolution and monthly planets (21); the Lots of the year (22, fn 45);
# the stakes (23); the solar phase (24). 25-26: "its indication will be
# according to its place and condition in the two times together".
#
# FACTS, and six things named as not read. The facts (2-7, 10-14,
# 17-19, 22-24) come from the engine's evaluators run on each chart --
# the pairwise configurations and the connection rule the Configurations
# page uses, reception under its rule, accidental dignity's domain, the
# solar phase, the twelfth-part, the transit grade of V.1. NOT read: 8
# "strong or weak", 15 "supporting or corrupting", 16 "hostile or
# friendly" -- judgments the page does not make; 9 "rising and falling",
# which fn 32 could not settle; 20-21, the year's course and transits,
# which the engine does not track. 25-26 is the principle II.3's section
# already applies. Decided by the owner 2026-09-10; no worked example.

PN4_I7_NOT_READ = (
    ('8', 'the strong ones, and the weak', 'a judgment the page does not make'),
    ('9', 'those rising and falling in their direction', 'fn 32 could not settle what this means'),
    ('15', 'those supporting their associate or corrupting them', 'a judgment the page does not make'),
    ('16', 'those hostile to them and friendly towards them, and those harmonizing with others or being contrary to them', 'a judgment the page does not make'),
    ('20', 'their course in the twelve signs', "the year's transits are not tracked"),
    ('21', 'their transiting the planets of the root, the revolution, and the revolution of the months', "the year's transits are not tracked"),
)

def _pn4_share_or_exile(planet, lon):
    """I.7, 5: "in a position in which it has a share, or in the contrary
    of that (being in exile)"."""
    r = get_essential_rulers(lon)
    if planet in (r['domicile'], r['exaltation'], r['triplicity_day'], r['triplicity_night'], r.get('term'), r['face']):
        shares = [k for k, v in (('house', r['domicile']), ('exaltation', r['exaltation']), ('triplicity', r['triplicity_day']),
                                 ('triplicity', r['triplicity_night']), ('bound', r.get('term')), ('face', r['face'])) if v == planet]
        return 'a share: ' + ', '.join(dict.fromkeys(shares))
    sign = get_zodiac_sign(lon)
    if sign in DOMICILES.get(planet, []) or sign in EXALTATIONS.get(planet, []):
        return 'a share'
    opposite = get_zodiac_sign(lon + 180.0)
    if opposite in DOMICILES.get(planet, []):
        return 'exile (detriment)'
    return 'no share (peregrine)'

def pn4_i7_ascendant(chart_data, sr):
    """I.7, 2-6 for the revolution's Ascendant, as facts."""
    natal, rev = chart_data['planetary_data'], sr['planetary_data']
    n_asc, r_asc = chart_data['ascendant'], sr['ascendant']
    sign = get_zodiac_sign(r_asc)
    h = get_wsh_house(r_asc, n_asc)

    def contents(chart, data):
        planets = _pn4_natal_planets_in_sign(data, sign)
        lots = _pn4_lots_in_sign(chart, sign)
        twelfths = [p for p in PN4_SEVEN if p in data and get_zodiac_sign(pn4_twelfth_part(data[p]['longitude'])) == sign]
        return (f"planets: {planets}; Lots: {', '.join(lots) or 'none'}; twelfth-parts of planets falling in it: "
                f"{', '.join(twelfths) or 'none'}")

    def looking(data):
        return ', '.join(f"{p} by {a}" for p, a, _l, _d in _pn4_looks_at_sign(data, sign) if a != 'in it') or 'none'

    r = get_essential_rulers(r_asc)
    trip = r['triplicity_day'] if sr['sect'] == 'Diurnal' else r['triplicity_night']
    claimants = []
    for kind, lord in (('house', r['domicile']), ('exaltation', r['exaltation']), ('triplicity', trip), ('bound', r.get('term')), ('face', r['face'])):
        if lord in (None, '-', '') or lord not in rev:
            continue
        lon = rev[lord]['longitude']
        claimants.append(f"{lord} ({kind}): house {get_wsh_house(lon, r_asc)} from it, in {get_zodiac_sign(lon)}, {_pn4_share_or_exile(lord, lon)}")
    lord = r['domicile']
    houses = DOMICILES.get(lord, [])
    lord_lon = rev.get(lord, {}).get('longitude')
    parts = []
    if lord_lon is not None:
        lord_idx = int(lord_lon // 30)
        for hs in houses:
            idx = SIGN_ORDER.index(hs)
            apart = min((idx - lord_idx) % 12, (lord_idx - idx) % 12)
            entry = ASPECT_BY_SIGN_COUNT.get(apart)
            rel = 'in it' if apart == 0 else (f"looks at it by {entry[0].lower()}" if entry else 'does not look at it (aversion)')
            parts.append(f"{hs} (house {get_wsh_house(idx * 30.0 + 15.0, r_asc)} from the Ascendant): {lord} {rel}; "
                         f"{lord} is house {get_wsh_house(lord_lon, idx * 30.0)} from {hs}")
    return [
        {'I.7': '2', 'Question': "The revolution's Ascendant: which house it is in the root",
         'Reads': f"{sign} ({get_degree_string(r_asc)}) is house {h} from the natal Ascendant, {_pn4_house_class(h)} (fn 37)",
         'Source': 'I.7, 2'},
        {'I.7': '3', 'Question': 'Who was in it in the root; in the revolution',
         'Reads': f"root -- {contents(chart_data, natal)}; revolution -- {contents(sr, rev)}", 'Source': 'I.7, 3'},
        {'I.7': '4', 'Question': 'Who looks at it, in both times (whole sign)',
         'Reads': f"root: {looking(natal)}; revolution: {looking(rev)}", 'Source': 'I.7, 4; fn 41'},
        {'I.7': '5', 'Question': 'Who has a claim on it, where they are relative to it, and in a share or in exile (in the revolution)',
         'Reads': '; '.join(claimants) or '-', 'Source': 'I.7, 5; fn 38'},
        {'I.7': '6', 'Question': f"Its lord, {lord}: one house or two, does it look at them, and where each is relative to the other",
         'Reads': f"{len(houses)} house{'s' if len(houses) != 1 else ''}: " + ('; '.join(parts) or '-'), 'Source': 'I.7, 6'},
    ]

@_connection_aware
def pn4_i7_planets(chart_data, sr):
    """I.7, 7, 10-14, 17-19, 22-24 per planet, in the root and the
    revolution, from the engine's evaluators."""
    rows = []
    natal = chart_data['planetary_data']
    for label, chart in (('root', chart_data), ('revolution', sr)):
        data, asc, sect = chart['planetary_data'], chart['ascendant'], chart['sect']
        acc = evaluate_accidental_dignities(data, chart['houses'], sect, chart.get('julian_day'),
                                            armc=chart.get('armc'), obliquity=chart.get('obliquity'), geo_lat=chart.get('geo_lat'))
        pairs = _pairwise_configurations(data)
        try:
            receptions = evaluate_reception(data, sect)
        except Exception:
            receptions = None
        for planet in PN4_SEVEN:
            if planet not in data:
                continue
            row = data[planet]
            lon = row['longitude']
            a = acc.get(planet, {})
            phase, side, _el = solar_phase(planet, lon, data['Sun']['longitude'], data.get(planet, {}).get('speed_in_lon'))
            my_idx = int(lon // 30)
            assembled, looks, averse = [], [], []
            for other in PN4_SEVEN:
                if other == planet or other not in data:
                    continue
                o_idx = int(data[other]['longitude'] // 30)
                apart = min((o_idx - my_idx) % 12, (my_idx - o_idx) % 12)
                entry = ASPECT_BY_SIGN_COUNT.get(apart)
                if apart == 0:
                    assembled.append(other)
                elif entry:
                    looks.append(f"{other} ({entry[0].lower()})")
                else:
                    averse.append(other)
            by_degree = []
            for pr in pairs:
                if planet not in (pr['p1'], pr['p2']):
                    continue
                connected, state = _pn4_degree_connection(pr)
                if pr['aspect_name'] == 'Aversion' and connected is False:
                    continue
                other = pr['p2'] if pr['p1'] == planet else pr['p1']
                kind = 'body connection (Sahl 20–21)' if pr['aspect_name'] == 'Aversion' else pr['aspect_name'].lower()
                state_text = pn4_result_text(state)
                by_degree.append(f"{other} {kind}, {state_text}" + (', connected' if connected is True else ''))
            if receptions is None:
                received = 'not computed'
            else:
                by = sorted({r['Receiver'] for r in receptions if r.get('Received') == planet
                             or (r.get('Received') == 'each other' and planet in str(r.get('Receiver')))})
                received = ', '.join(by) or 'not received'
            h = get_effective_house(lon, chart['houses'], angles_only=True)
            if label == 'revolution' and planet in natal:
                grade = _pn4_transit_grade(lon, natal[planet]['longitude'])
                others = [q for q in PN4_SEVEN if q != planet and q in natal and _pn4_transit_grade(lon, natal[q]['longitude'])]
                returns = (f"on its own rooted place by {grade}" if grade else 'not on its rooted place') + \
                          (f"; on the rooted place of {', '.join(others)}" if others else '')
            else:
                returns = '-'
            rows.append({
                'Planet': planet, 'Chart': label,
                'Motion (7)': 'retrograde' if row.get('speed_in_lon', 1.0) < 0 else 'direct',
                'Whole sign (10-11)': (f"assembled with {', '.join(assembled)}; " if assembled else '') +
                                      (f"looks at {', '.join(looks)}; " if looks else '') +
                                      (f"in aversion to {', '.join(averse)}" if averse else '') or '-',
                'By degree (12-13)': '; '.join(by_degree) or 'none',
                'Received by (14)': received,
                'Domain (17)': domain_description(a, short=True),
                'Twelfth-part (18)': get_degree_string(pn4_twelfth_part(lon)),
                'Return (19)': returns,
                'Stakes (23)': f"house {h}, {_pn4_house_class(h)}",
                'Sun (24)': (f"{side or '-'}" + (f", {phase.lower()}" if phase else ', in its own glow')),
            })
    return rows

# --- IX.7, 1-72: the nine methods for the days and hours ------------------
# "The days and hours have nine indicators" (IX.7, 1). Six and seven --
# the mighty days and the small days -- are built above. The other seven:
#   1 (2-6)   the days since birth "up to the year which has just been
#             completed", divided by seven: each week to a planet from the
#             lord of the natal Ascendant, "then the one below it in the
#             circle"; the remainder days say whose week opens the year and
#             how much of it is left; each day of a week to the planets in
#             the same order; each hour to them, 3 3/7 hours apiece (fn 164);
#   2 (7-9)   the lord of the orb of that year "grants 7 days" from the
#             first day of the revolution, then the planets below it, round
#             again; days and hours as in 1;
#   3 (10-13) the year, "365 1/4 minus 1/300 of a day", in seven "greater
#             sevenths" (52 d 4 h and a quarter) from the lord of the
#             revolution's Ascendant downward; each in seven "lesser
#             sevenths" (7 d 10 h and about 6/7 of an hour); days and hours
#             as in 1;
#   4 (14-17) the week count of 1, the cycles going to the SIGNS from the
#             natal Ascendant, "not the lord of the sign" (fn 171); a
#             week's 168 hours among the twelve signs, 14 apiece (fn 173);
#   5 (18-20) the days since birth cast out by twelves from the natal
#             Ascendant: the sign reached manages that day and "introduces
#             the year"; each sign then a day; two hours to a sign;
#   8 (34-39) the month's days from the four rooted monthly indicators
#             (fn 181) and the month's Ascendant, Lot and Moon: a day per
#             degree up to thirty, or a day per 12 degrees, 2 1/2 days and
#             then 5 hours to a sign, sixty hours for the twelve;
#   9 (43-72) the ninth-part method from three starts -- the terminal
#             sign, the revolution's Ascendant, the Moon's own ninth-part
#             and degree -- a sign a month of 30 d 10 h 30 m, each
#             ninth-part 3 d 9 h 10 m, subdivided into thirds among its
#             lord and the lords of the fifth and ninth signs from it, then
#             ninths, then thirds again (49-54); worked at 57-69.
# IX.7, 56: everything in equal hours. IX.7, 79 declines day and hour
# CHARTS; these nine are what the author uses instead.
#
# WHAT IS READ IN, and said on the page. A "day" is a whole 24-hour
# period from the birth moment (fn 161: the book does not say whether
# from birth or from dawn). The 'now' is the page's target date at noon.
# Method 8's four rooted indicators are the monthly profections of IX.1,
# 26 already on the page (fn 181). Method 9's subdivision partners, "the
# lord of the fifth sign from the sign of the lord of the ninth-part" and
# "the ninth", are the domicile lords of those signs, which is what the
# worked example does (Capricorn, then Taurus and Virgo: Saturn, Venus,
# Mercury). Two of the example's printed fractions are wrong and fn 195
# and 197 say so; the exact values are computed and the printed ones
# shown beside them as printed errata. The judgments of IX.7, 21-22 and
# 41 are not built; 42's rule -- the succession sign after sign from the
# starting sign, convertible or not -- is applied. Decided by the owner 2026-09-10.
PN4_IX7_YEAR_DAYS = 365.25 - 1.0 / 300.0                 # IX.7, 10
PN4_IX7_GREATER_SEVENTH = PN4_IX7_YEAR_DAYS / 7.0        # 52 d 4 h and a quarter (fn 168)
PN4_IX7_LESSER_SEVENTH = PN4_IX7_GREATER_SEVENTH / 7.0   # 7 d 10 h and about 6/7 of an hour
PN4_IX7_MONTH_DAYS = 365.25 / 12.0                       # IX.7, 54-55: 30 d 10 h 30 m
PN4_IX7_NINTH_PART_DAYS = PN4_IX7_MONTH_DAYS / 9.0       # 3 d 9 h 10 m
PN4_IX7_EXAMPLE_ERRATA = (
    ('IX.7, 62', '7\' 25" 33""', '7\' 24" 26""', 'fn 195'),
    ('IX.7, 66', '2\' 28" 31""', '2\' 28" 09""', 'fn 197'),
)

def _pn4_sign_step(sign, steps):
    return SIGN_ORDER[(SIGN_ORDER.index(sign) + int(steps)) % 12]

def _pn4_hours_of_seven(day_fraction):
    """IX.7, 6: the day's 24 hours among the seven, 3 3/7 apiece."""
    return int((day_fraction * 24.0) // (24.0 / 7.0))

def pn4_ix7_weeks_from_birth(days_since_birth, asc_lord):
    """Method 1, IX.7, 2-6."""
    d = int(days_since_birth // 1)
    weeks, rem = d // 7, d % 7
    week = pn4_hour_lord_from_natal(asc_lord, weeks)
    day = pn4_hour_lord_from_natal(asc_lord, weeks + rem)
    hour = pn4_hour_lord_from_natal(day, _pn4_hours_of_seven(days_since_birth % 1.0))
    return {'weeks': weeks, 'remainder': rem, 'left_of_week': 7 - rem, 'week': week, 'day': day, 'hour': hour}

def pn4_ix7_weeks_from_orb(days_since_revolution, orb_lord):
    """Method 2, IX.7, 7-9."""
    if not orb_lord:
        return None
    d = int(days_since_revolution // 1)
    weeks, rem = d // 7, d % 7
    week = pn4_hour_lord_from_natal(orb_lord, weeks)
    day = pn4_hour_lord_from_natal(orb_lord, weeks + rem)
    hour = pn4_hour_lord_from_natal(day, _pn4_hours_of_seven(days_since_revolution % 1.0))
    return {'weeks': weeks, 'remainder': rem, 'week': week, 'day': day, 'hour': hour}

def pn4_ix7_sevenths(days_since_revolution, sr_asc_lord):
    """Method 3, IX.7, 10-13."""
    t = days_since_revolution
    g = int(t // PN4_IX7_GREATER_SEVENTH)
    within_g = t - g * PN4_IX7_GREATER_SEVENTH
    l = int(within_g // PN4_IX7_LESSER_SEVENTH)
    within_l = within_g - l * PN4_IX7_LESSER_SEVENTH
    greater = pn4_hour_lord_from_natal(sr_asc_lord, g)
    lesser = pn4_hour_lord_from_natal(greater, l)
    day = pn4_hour_lord_from_natal(lesser, int(within_l // 1))
    hour = pn4_hour_lord_from_natal(day, _pn4_hours_of_seven(within_l % 1.0))
    return {'greater_index': g, 'lesser_index': l, 'greater_seventh': greater, 'lesser_seventh': lesser, 'day': day, 'hour': hour}

def pn4_ix7_weeks_to_signs(days_since_birth, asc_sign):
    """Method 4, IX.7, 14-17: the cycles of weeks to the signs; 14 hours a
    sign within the week (fn 173)."""
    d = int(days_since_birth // 1)
    weeks, rem = d // 7, d % 7
    week_sign = _pn4_sign_step(asc_sign, weeks)
    hours_in_week = (days_since_birth - weeks * 7) * 24.0
    now_sign = _pn4_sign_step(week_sign, int(hours_in_week // 14.0))
    return {'weeks': weeks, 'remainder': rem, 'week': week_sign, 'now': now_sign}

def pn4_ix7_days_to_signs(days_since_birth, asc_sign):
    """Method 5, IX.7, 18-20: the days by twelves from the natal
    Ascendant; two hours a sign."""
    d = int(days_since_birth // 1)
    day_sign = _pn4_sign_step(asc_sign, d % 12)
    hour_sign = _pn4_sign_step(day_sign, int(((days_since_birth % 1.0) * 24.0) // 2.0))
    return {'day': day_sign, 'hour': hour_sign}

def pn4_ix7_month_days(days_since_month, starts):
    """Method 8, IX.7, 34-39, for each of the seven starts (name, lon):
    way [1] a day per degree; way [2] a day per twelve degrees, 2 1/2 days
    to a sign, five hours to a sign within that."""
    # IX.7, 39: past the thirtieth day "in the management of the days and
    # hours it will return to the position which it began from" -- both
    # ways wrap at 30 (order PN4R-4p-8; way [1] had run on into day 31).
    d = days_since_month % 30.0
    rows = []
    for name, lon in starts:
        way1 = (lon + d) % 360.0
        sign0 = get_zodiac_sign(lon)
        way2_sign = _pn4_sign_step(sign0, int(d // 2.5))
        hours_in_slot = (d % 2.5) * 24.0
        way2_hour = _pn4_sign_step(way2_sign, int(hours_in_slot // 5.0))
        rows.append({'Start': name, 'Position': get_degree_string(lon),
                     'Way 1: a day per degree, now at': get_degree_string(way1),
                     "Way 2: day's sign (2½-day blocks)": way2_sign, "Way 2: hour's sign (5-hour steps)": way2_hour,
                     'Source': 'IX.7, 35-39'})
    return rows

def pn4_ix7_ninth_parts(days_since_revolution, start_sign, start_offset_days=0.0):
    """Method 9, IX.7, 43-55: a sign a month from `start_sign`, the
    ninth-parts of each sign 3 d 9 h 10 m apiece, each in thirds among the
    lords of its own sign and the fifth and ninth from it, each third in
    ninths continuing the ninth-part sequence, each ninth in thirds again.
    `start_offset_days` places the Moon's own start within her ninth-part
    (IX.7, 71)."""
    N, M = PN4_IX7_NINTH_PART_DAYS, PN4_IX7_MONTH_DAYS
    third, ninth, third_of_ninth = N / 3.0, N / 27.0, N / 81.0
    t = days_since_revolution + start_offset_days
    m = int(t // M)
    u = t - m * M
    month_sign = _pn4_sign_step(start_sign, m)
    first = pn4_first_ninth_part_lord(month_sign)['ninth_part_sign']
    k = int(u // N)
    np_sign = _pn4_sign_step(first, k)
    w = u - k * N
    j = int(w // third)
    third_sign = _pn4_sign_step(np_sign, (0, 4, 8)[j])
    x = w - j * third
    i = int(x // ninth)
    ninth_sign = _pn4_sign_step(third_sign, i)
    y = x - i * ninth
    q = int(y // third_of_ninth)
    q_sign = _pn4_sign_step(ninth_sign, (0, 4, 8)[q])
    lord = lambda s: SIGN_TO_DOMICILE.get(s, '-')
    return {
        'month': m + 1, 'month_sign': month_sign,
        'ninth_part': k + 1, 'ninth_part_sign': np_sign, 'ninth_part_lord': lord(np_sign),
        'ninth_part_days': (m * M + k * N - start_offset_days, m * M + (k + 1) * N - start_offset_days),
        'third': j + 1, 'third_lord': lord(third_sign), 'third_sign': third_sign,
        'ninth': i + 1, 'ninth_lord': lord(ninth_sign), 'ninth_sign': ninth_sign,
        'third_of_ninth': q + 1, 'third_of_ninth_lord': lord(q_sign),
        'durations_hours': {'ninth-part': N * 24.0, 'third': third * 24.0, 'ninth of the third': ninth * 24.0,
                            'third of the ninth': third_of_ninth * 24.0},
    }

def pn4_ix7_moon_start(moon_lon):
    """IX.7, 71: the Moon begins "from that ninth-part and from that degree
    she is in" -- her sign, and the days into the month her degree stands
    for at the ninth-part's rate."""
    within = moon_lon % 30.0
    return get_zodiac_sign(moon_lon), within / (30.0 / 9.0) * PN4_IX7_NINTH_PART_DAYS

def pn4_day_methods(jd_birth, jd_sr, jd_mr, jd_now, natal_asc, sr_asc, orb_lord, year_sign, month_starts, sr_moon_lon):
    """The seven methods at `jd_now`, as rows for the page, with method
    8's and 9's own tables."""
    since_birth, since_rev, since_month = jd_now - jd_birth, jd_now - jd_sr, jd_now - jd_mr
    at_rev = int((jd_sr - jd_birth) // 1)
    asc_sign, asc_lord = get_zodiac_sign(natal_asc), SIGN_TO_DOMICILE.get(get_zodiac_sign(natal_asc), '-')
    sr_lord = SIGN_TO_DOMICILE.get(get_zodiac_sign(sr_asc), '-')
    m1_open = pn4_ix7_weeks_from_birth(float(at_rev), asc_lord)
    m1, m2 = pn4_ix7_weeks_from_birth(since_birth, asc_lord), pn4_ix7_weeks_from_orb(since_rev, orb_lord)
    m3, m4, m5 = pn4_ix7_sevenths(since_rev, sr_lord), pn4_ix7_weeks_to_signs(since_birth, asc_sign), pn4_ix7_days_to_signs(since_birth, asc_sign)
    rows = [
        {'Method': '1. The weeks of days from birth', 'Opens the year': f"{m1_open['week']}'s week, {m1_open['left_of_week']} of its days left ({at_rev} days from birth: {m1_open['weeks']} weeks and {m1_open['remainder']})",
         'This week': m1['week'], 'Today': m1['day'], 'This hour': m1['hour'], 'Source': 'IX.7, 2-6'},
        {'Method': '2. Seven days each from the lord of the orb', 'Opens the year': f"{orb_lord}, the lord of the orb, the first seven days" if orb_lord else 'natal hour lord unavailable',
         'This week': m2['week'] if m2 else '-', 'Today': m2['day'] if m2 else '-', 'This hour': m2['hour'] if m2 else '-', 'Source': 'IX.7, 7-9'},
        {'Method': '3. The greater and lesser sevenths of the year', 'Opens the year': f"{sr_lord}, lord of the revolution\'s Ascendant, the first greater seventh ({PN4_IX7_GREATER_SEVENTH:.3f} days)",
         'This week': f"greater {m3['greater_seventh']} (no. {m3['greater_index'] + 1}), lesser {m3['lesser_seventh']} (no. {m3['lesser_index'] + 1})", 'Today': m3['day'], 'This hour': m3['hour'], 'Source': 'IX.7, 10-13'},
        {'Method': '4. The weeks to the signs from the natal Ascendant', 'Opens the year': f"the week of {pn4_ix7_weeks_to_signs(float(at_rev), asc_sign)['week']}",
         'This week': m4['week'], 'Today': f"{m4['now']} (14 hours a sign, fn 173)", 'This hour': m4['now'], 'Source': 'IX.7, 14-17'},
        {'Method': '5. The days to the signs by twelves from the natal Ascendant', 'Opens the year': pn4_ix7_days_to_signs(float(at_rev), asc_sign)['day'],
         'This week': '-', 'Today': m5['day'], 'This hour': m5['hour'], 'Source': 'IX.7, 18-20'},
        {'Method': '6. The mighty days', 'Opens the year': 'the terminal point', 'This week': '-', 'Today': 'the mighty-days table above', 'This hour': '-', 'Source': 'IX.7, 23-28'},
        {'Method': '7. The small days', 'Opens the year': "the revolution's Ascendant", 'This week': '-', 'Today': 'the small-days table above', 'This hour': '-', 'Source': 'IX.7, 29-33'},
        {'Method': "8. The month's days", 'Opens the year': '-', 'This week': '-', 'Today': 'the table below', 'This hour': 'the table below', 'Source': 'IX.7, 34-39'},
        {'Method': '9. The ninth-parts', 'Opens the year': f"the first ninth-part of {year_sign}", 'This week': '-', 'Today': 'the table below', 'This hour': 'the table below', 'Source': 'IX.7, 43-72'},
    ]
    month_rows = pn4_ix7_month_days(since_month, month_starts)
    moon_sign, moon_offset = pn4_ix7_moon_start(sr_moon_lon)
    ninth_rows = []
    for label, start, offset, cite in (('the sign of the terminal point', year_sign, 0.0, 'IX.7, 44-48'),
                                        ("the revolution's Ascendant", get_zodiac_sign(sr_asc), 0.0, 'IX.7, 70'),
                                        ("the Moon, from her own ninth-part and degree", moon_sign, moon_offset, 'IX.7, 71')):
        r = pn4_ix7_ninth_parts(since_rev, start, offset)
        ninth_rows.append({'Start': label, 'Month': f"{r['month']}: {r['month_sign']}",
                           'Ninth-part (3 d 9 h 10 m)': f"{r['ninth_part']} of 9: {r['ninth_part_sign']}, {r['ninth_part_lord']}, days {r['ninth_part_days'][0]:.2f} to {r['ninth_part_days'][1]:.2f}",
                           'Third (27 h 3 m)': f"{r['third']}: {r['third_lord']} ({r['third_sign']})",
                           'Ninth of the third (3 h 0.4 m)': f"{r['ninth']}: {r['ninth_lord']} ({r['ninth_sign']})",
                           'Third of that (1 h 0.1 m)': f"{r['third_of_ninth']}: {r['third_of_ninth_lord']}",
                           'Source': cite})
    return rows, month_rows, ninth_rows

def pn4_fardar_at_age(age_years, sect):
    """The fardar lord and sub-lord at an age.

    IV.7, 25: "once 75 years are completed for the native, the
    distribution of the fardar returns to THE LUMINARY WHICH HE BEGAN FROM
    at his birth, in the original order" -- so the cycle restarts at the
    light of the sect, not always at the Sun. IV.1, 2's "then it returns
    to the Sun" is the diurnal case of that rule."""
    if age_years < 0:
        return None
    sequence = pn4_fardar_sequence(sect)
    cycles, within = divmod(float(age_years), PN4_FARDAR_CYCLE_YEARS)
    start = 0.0
    for lord, years in sequence:
        if within < start + years:
            offset = within - start
            subs = pn4_fardar_subperiods(lord, years)
            sub_lord, sub_from, sub_to = None, None, None
            if subs:
                each = years / 7.0
                # The sub-period is found against the SAME boundaries it
                # reports (start + k*each), not by offset // each: the
                # floating quotient of a returned sub_to fed back in came
                # out a hair under the next integer and named the expired
                # sub-period again (Astra F12). Half-open, like the main
                # periods: a sub-period does not own its own end.
                k = next((j for j in range(7) if within < start + (j + 1) * each), 6)
                sub_lord = subs[k][0]
                sub_from, sub_to = start + k * each, start + (k + 1) * each
            return {
                'lord': lord, 'years': years,
                'from': cycles * PN4_FARDAR_CYCLE_YEARS + start,
                'to': cycles * PN4_FARDAR_CYCLE_YEARS + start + years,
                'sub_lord': sub_lord,
                'sub_from': None if sub_from is None else cycles * PN4_FARDAR_CYCLE_YEARS + sub_from,
                'sub_to': None if sub_to is None else cycles * PN4_FARDAR_CYCLE_YEARS + sub_to,
                'cycle': int(cycles) + 1,
            }
        start += years
    return None

# --- I.8, 10-26: the Ages of Man -----------------------------------------
# Ptolemy's seven ages, ordered by sphere from the lowest upward, NOT the
# quadrant scheme of Sahl, On Nativities 3.9, 32-36. Each span is a
# planet's lesser years, or one-half or one-tenth of its lesser or middle
# years (I.8, 9): Moon 4 = a tenth of her middle years 39 1/2 (I.8, 12),
# Mercury 10 = half his lesser 20, and Venus 8, Sun 19, Mars 15,
# Jupiter 12, Saturn 30 are lesser years outright.
#
# Saturn's span is OPEN-ENDED. Figure 53 tabulates it as "30 / ages 68-97"
# and 30 is his lesser years, but the prose governs: the seventh age runs
# "until the end of his lifespan" (I.8, 25). A native of 100 is still in
# Saturn's age. I.8, 31-33 reports that some restart the cycle at the Moon
# after Saturn; Abu Ma'shar does not endorse it, so this does not restart.
#
# I.8, 34-35: he also refuses to subdivide an age into sevenths the way a
# fardar is subdivided -- "he will be in the nature of the planet itself,
# for the amount of those years". So there is no sub-lord here.
PN4_AGES_OF_MAN = (
    ('Moon', 4, 'Upbringing'), ('Mercury', 10, 'End of childhood'),
    ('Venus', 8, 'Beginning of youth'), ('Sun', 19, 'End of youth'),
    ('Mars', 15, 'Beginning of maturity'), ('Jupiter', 12, 'Maturity, transition to old age'),
    ('Saturn', 30, 'Old age'),
)

def pn4_age_of_man(age_years):
    """I.8, 10-26. The last age is open-ended (I.8, 25)."""
    if age_years < 0:
        return None
    start = 0.0
    for i, (planet, years, label) in enumerate(PN4_AGES_OF_MAN):
        last = i == len(PN4_AGES_OF_MAN) - 1
        if last or age_years < start + years:
            return {'planet': planet, 'from': start,
                    'to': None if last else start + years,
                    'label': label, 'nominal_years': years}
        start += years
    return None

# --- I.2 and IX.3: the revolutions ---------------------------------------

PN4_MEAN_SOLAR_DAY_MOTION = 0.9856

def _pn4_sun_offset(jd, target_lon):
    res = swe.calc_ut(jd, swe.SUN)[0]
    return ((res[0] - target_lon + 180.0) % 360.0) - 180.0, res[3]

def pn4_revolution_jd(target_lon, jd_guess):
    """Newton search for the moment the TRUE Sun stands at target_lon,
    from a guess within a few days, in the manner calculate_prenatal_syzygy
    searches for the syzygy.

    I.2, 1 defines the revolution as the Sun's return to "his position in
    which he was at the root". Abu Ma'shar computes it from a MEAN Sun and
    then applies the Hipparchan tropical year of 365;14,48 days
    (I.4, 23-31; IX.7, 10) -- Dykes says plainly that this does not make
    sense (Intro Sect. 1, p. 6), and it is not reproduced. This is a true-Sun
    return, which is what the definition actually asks for."""
    jd = float(jd_guess)
    for _ in range(30):
        offset, speed = _pn4_sun_offset(jd, target_lon)
        if abs(offset) < 1e-9:
            break
        if abs(speed) < 1e-6:
            speed = PN4_MEAN_SOLAR_DAY_MOTION
        jd -= offset / speed
    return jd

def pn4_solar_revolution_jd(jd_natal, natal_sun_lon, age):
    """The solar revolution opening the native's `age`-th completed year.
    Cast for the BIRTH LOCATION: PN IV never states the location for the
    annual revolution, but it does for the monthly ones -- "(And we will
    use the birthplace for the location)", Intro Sect. 9, p. 95 -- and the
    excess-of-revolution technique presupposes a fixed one. Dykes' reading;
    flagged as an assumption in the UI, not as Abu Ma'shar's sentence."""
    return pn4_revolution_jd(natal_sun_lon % 360.0,
                             jd_natal + float(age) * 365.2425)

def pn4_monthly_revolution_jd(jd_solar_revolution, natal_sun_lon, month):
    """IX.3, 2: month 1 IS the solar revolution (IX.1, 10; IX.2, 1-2).
    For month n the Sun stands in the n-th sign from his rooted place, "in
    the like degree AND MINUTE which he was in at the root".

    Intro Sect. 2 (p. 7) illustrates this with a natal Sun at 12 22' Gemini
    and then puts the monthly revolutions at 12 23' Cancer and 12 23' Leo.
    The page genuinely prints that; it is an error in Dykes' own book
    (PN4_READTHROUGH_FINDINGS_2026-09-10.md, P-01), contradicted by the
    rule in its own sentence, by IX.1, 23, by IX.3, 2 and by the worked
    example at Intro Sect. 9 p. 95. The degree and minute do not change."""
    month = int(month)
    if month <= 1:
        return jd_solar_revolution
    target = (natal_sun_lon + 30.0 * (month - 1)) % 360.0
    return pn4_revolution_jd(target, jd_solar_revolution + (month - 1) * 30.44)

# --- Profection: II.3, 1 and IX.1 ----------------------------------------

def pn4_profect(lon, steps, forward=True):
    """Move a point `steps` whole signs, keeping its degree within the
    sign. All profection in PN IV is sign-by-sign (IX.1; Intro Sect. 9 p. 90)."""
    idx = int(lon // 30.0)
    moved = (idx + steps) % 12 if forward else (idx - steps) % 12
    return moved * 30.0 + (lon % 30.0)

def pn4_sign_of_the_year(ascendant_lon, completed_years):
    """II.3, 1 and I.2, 5: "for every year the native has completed, cast
    out one sign from it: the sign which the intended year reaches is the
    'sign of the terminal point,' and its lord is the 'lord of the year'
    (and in Persian it is called the *salkhudhah*)."

    The lord of the year is the lord of the SIGN -- not the lord of the
    revolution's Ascendant, and not a victor (Q21). "Governor"
    (Ar. mustawli), the sign on which most of the year's indicators
    coincide (IX.9, 10; IX.2, 4-7), is a separate term."""
    lon = pn4_profect(ascendant_lon, int(completed_years))
    sign = get_zodiac_sign(lon)
    return {'longitude': lon, 'sign': sign, 'lord': SIGN_TO_DOMICILE.get(sign, '-')}

def pn4_first_ninth_part_lord(sign):
    """The lord of the first ninth-part of a sign -- monthly indicator #2
    (IX.1, 36), and the Indian rule for the lord of the year that PN IV
    reports without adopting (III.10, 1).

    The first ninth-part of a convertible sign is that sign; of a fixed
    sign, the ninth from it; of a double-bodied sign, the fifth from it.
    So the lord is always a lord of a convertible sign, which is why the
    Indian rule "restricts the lord of the year to four planets only".
    Verified against Abu Ma'shar's own three worked examples at III.10, 5:
    Taurus -> Saturn, Gemini -> Venus, Cancer -> the Moon.

    It is a function of the SIGN, not of the degree: "if the year
    terminated at 20 deg of Taurus (or less than that or more), then its
    lord would be Saturn" (III.10, 5)."""
    idx = SIGN_ORDER.index(sign)
    kind = PN4_QUADRUPLICITY[sign]
    step = {'convertible': 0, 'fixed': 8, 'double-bodied': 4}[kind]
    target = SIGN_ORDER[(idx + step) % 12]
    return {'ninth_part_sign': target, 'lord': SIGN_TO_DOMICILE.get(target, '-')}

# The direction of monthly profection. Abu Ma'shar's quadruplicity rule
# (IX.1, 26-34) reverses it for convertible signs and for the second half
# of a double-bodied one; Dykes rejects the rule as "complicated, probably
# wrong, and an over-zealous application of quadruplicities" (Intro
# Sect. 9 p. 102 and fn 95) and counts forward always. Owner's decision of
# 2026-09-10: ship both, default to Dykes.
PN4_MONTHLY_TURN_OPTIONS = ('Dykes: always forward', "PN IV IX.1, 26-34")

def pn4_monthly_turn_forward(lon, rule):
    """IX.1, 26-30, applied PER INDICATOR.

    IX.1, 31 is explicit that when the four rooted indicators fall in
    different quadruplicities "one turns EACH ONE OF THEM INDIVIDUALLY",
    so the direction is decided by the sign each indicator itself occupies
    -- not once, globally, by the sign of the year. (The two coincide for
    indicator #1, whose sign IS the sign of the year, and can differ for
    #3, #4 and #5.)

    The boundary in a double-bodied sign is 15 deg 00': forward "from the
    beginning of that sign up to 15 complete degrees" (28), backward "from
    the beginning of the sixteenth degree ... up to the end" (29), because
    the first half is of the nature of the preceding fixed sign and the
    second of the following convertible one (30)."""
    if rule != PN4_MONTHLY_TURN_OPTIONS[1]:
        return True
    kind = PN4_QUADRUPLICITY[get_zodiac_sign(lon)]
    if kind == 'fixed':
        return True
    if kind == 'convertible':
        return False
    return (lon % 30.0) < 15.0

# IX.1, 35-39: the seven monthly indicators, five rooted and two not.
# "Rooted" because they are turned from the positions they hold AT THE
# REVOLUTION OF THE YEAR (IX.1, 37); the last two are not, because each
# "indicates the condition of a single month, and [then] changes in the
# next month" (IX.1, 38). They decrease in universality in that order
# (IX.1, 39).
#
# fn 15 to IX.1, 26 notes that Abu Ma'shar himself "will ignore [#2] the
# ninth-part" through most of these chapters, and fn 3 to II.1, 5 notes
# that the Indian ninth-parts are absent from the nineteen indicators of
# the year. It is carried here because IX.1, 36 names it.
PN4_MONTHLY_INDICATOR_NAMES = (
    (1, 'Sign of the terminal point (profected natal Ascendant)', True),
    (2, "Lord of the month's sign's first ninth-part", True),
    (3, 'Profected natal Lot of Fortune', True),
    (4, 'Ascendant of the revolution of the year', True),
    (5, 'Lot of Fortune of the revolution of the year', True),
    (6, 'Ascendant of the revolution of the month', False),
    (7, 'Lot of Fortune of the revolution of the month', False),
)

def pn4_monthly_indicators(month, completed_years, sign_of_year_lon, natal_fortune_lon,
                           sr_ascendant_lon, sr_fortune_lon,
                           mr_ascendant_lon, mr_fortune_lon, rule):
    """The seven indicators of IX.1, 35-39 for month `month` (1-12).

    Each is first brought to its position for the YEAR, and only then
    turned month by month -- and the four are not brought there the same
    way, which is the thing to get right:

    * #1 is the natal Ascendant profected a sign a year (IX.1, 9).
    * #3 is the natal Lot of Fortune profected a sign a year in its own
      right: "you see where the Lot of Fortune is in the root of the
      nativity, and TURN FROM IT A SIGN FOR EVERY YEAR, up to the year
      which you want" (IX.1, 17-18). It is not the natal Lot itself.
    * #4 and #5 are NOT profected: the revolution's Ascendant and its Lot
      of Fortune are "assign[ed] to the first month" as they stand
      (IX.1, 20-21).
    * #6 and #7 are not turned at all -- they are read from the monthly
      revolution, which is cast afresh each month (IX.1, 38).

    #2 is a different shape from the rest. What turns is the sign of the
    terminal point, and the indicator is the lord of the FIRST ninth-part
    of whatever sign the turning reaches: "the lord of the first
    ninth-part belonging to the second sign from the sign of the terminal
    point ... is the indicator of the condition of the second month"
    (IX.1, 12-14). Abu Ma'shar works it at IX.1, 15-16 -- year at Cancer:
    Moon, then Leo -> Mars, Virgo -> Saturn, Libra -> Venus. It always
    turns forward, "without distinction, whether the sign of the terminal
    point is convertible, fixed, or having two bodies" (IX.1, 32), and it
    ignores the degree entirely (IX.1, 11: "so don't worry about which
    position in that sign is the terminal point of the year").
    """
    steps = max(int(month) - 1, 0)

    def _row(number, lon, direction):
        sign = get_zodiac_sign(lon)
        return {'number': number, 'name': PN4_MONTHLY_INDICATOR_NAMES[number - 1][1],
                'rooted': PN4_MONTHLY_INDICATOR_NAMES[number - 1][2],
                'longitude': lon, 'sign': sign,
                'lord': SIGN_TO_DOMICILE.get(sign, '-'), 'direction': direction}

    out = []
    # #1, #3, #4, #5: turned, each by the quadruplicity of its OWN sign.
    for number, base in ((1, sign_of_year_lon),
                         (3, pn4_profect(natal_fortune_lon, int(completed_years))),
                         (4, sr_ascendant_lon),
                         (5, sr_fortune_lon)):
        forward = pn4_monthly_turn_forward(base, rule)
        out.append(_row(number, pn4_profect(base, steps, forward=forward),
                        'forward' if forward else 'backwards'))

    # #2: the terminal point turns forward; the lord is that sign's first
    # ninth-part lord, so the row names the ninth-part sign it comes from.
    month_sign = get_zodiac_sign(pn4_profect(sign_of_year_lon, steps, forward=True))
    ninth = pn4_first_ninth_part_lord(month_sign)
    out.append({'number': 2, 'name': PN4_MONTHLY_INDICATOR_NAMES[1][1], 'rooted': True,
                'longitude': SIGN_ORDER.index(ninth['ninth_part_sign']) * 30.0,
                'sign': ninth['ninth_part_sign'], 'lord': ninth['lord'],
                'direction': f"forward (from {month_sign})"})

    for number, lon in ((6, mr_ascendant_lon), (7, mr_fortune_lon)):
        out.append(_row(number, lon, 'cast, not turned'))

    out.sort(key=lambda r: r['number'])
    return out

# II.1, 5-24 ranks NINETEEN indicators of the year and II.1, 25 says "each
# one in turn is stronger in indication than the one which is after it".
# The first four are implemented; the rest are delineation material.
#
# PN IV's own order is applied on the PN IV page and RANKS BY SCOPE. Within
# a single year the lord of the year outranks the distributor (II.1, 25;
# II.23, 1). Across several years the distribution is stronger, because
# "the indication of the lord of the terminal point is only over the
# condition of THAT YEAR: but as for the lord of the distribution,
# sometimes its indication ... is for SEVERAL YEARS", and the year's
# indicators are then read "as witnesses" to it (III.2, 2-3).
#
# This does not RESOLVE corpus disagreement #1 (an earlier comment said it
# did, "by scope"): Sahl's two consecutive chapters rank the pair in
# opposite orders AS PRINTED -- 1.23, 33 "the lord of the distribution is
# like a tender [of sheep], and the lord of the year like a hireling"
# (the distributor over the lord of the year) against 1.24, 2 "turning is
# the foundation of the work of the stars ... and it is stronger <than>
# the distributor of time" (the turning over the distributor); Dykes fn
# 245 emends the second ("stronger when combined with the distributor"),
# not adopted. Each of PN IV's two scoped rankings agrees with one Sahl
# sentence: II.1, 25 with 1.24, 2 within the year, III.2, 2-3 with 1.23,
# 33 across years. Both Sahl sentences are shown side by side on the page
# (PN4_YEAR_INDICATOR_SCOPE_NOTE; order GAP-3, 2026-09-11).
#
# Dykes' Introduction Sect. 3 (p. 21, point 3) states the opposite --
# distributions "tend to be more powerful than profections" -- and cites no
# Abu Ma'shar sentence for it. II.1, 25 governs here, not Intro Sect. 3.
PN4_YEAR_INDICATOR_SCOPE_NOTE = (
    "The order above is PN IV's, which ranks the lord of the year and the distributor by scope: within the year the "
    "lord of the year is the stronger (II.1, 25; II.23, 1), across several years the distribution (III.2, 2-3). "
    "Sahl's two consecutive chapters rank them in opposite orders as printed -- On Nativities 1.23, 33: \"the lord "
    "of the distribution is like a tender [of sheep], and the lord of the year like a hireling; so if the tender "
    "committed himself to his sheep in a powerful way, the hireling would have not power over harming the sheep\" "
    "(the distributor over the lord of the year); 1.24, 2: \"turning is the foundation of the work of the stars and "
    "[their] appointed time, and it is stronger <than> the distributor of time\" (the turning over the distributor). "
    "Dykes fn 245 emends the second (\"stronger when combined with the distributor\"), not adopted here. PN IV's "
    "within-the-year ranking agrees with 1.24, 2 and its across-years ranking with 1.23, 33; the disagreement stands "
    "recorded, not resolved."
)
PN4_YEAR_INDICATOR_ORDER = (
    'The sign of the terminal point, and its lord',
    'The distribution and the distributor',
    'The one partnering with the distributor',
    "The fardar lord and its sub-lord",
    'The lord of the orb',
)


# --- Assembling the page -------------------------------------------------

def pn4_datetime_from_jd(jd, calendar=None):
    """The UT civil moment of a Julian Day, as a CivilMoment (not a datetime,
    which cannot hold a Julian-only day such as 1300-02-29): the calendar
    is the one this file uses for that epoch, so feeding it back to
    calculate_traditional_chart -- which reads the digits by the same
    policy -- round-trips. The name is kept for its callers; it formats
    with the same %Y-%m-%d %H:%M:%S directives a datetime does. `calendar`
    writes the moment in a named calendar (the sidebar's UT in the birth
    date's)."""
    return CivilMoment.from_jd(jd, calendar)

def pn4_completed_years(birth_date, target_date):
    """Age in COMPLETED CIVIL ANNIVERSARIES -- the count II.3, 1 asks for
    ("for every year the native has completed"). Not elapsed days over a
    mean year, which lets the sign turn a day either side of the birthday."""
    years = target_date.year - birth_date.year
    if (target_date.month, target_date.day) < (birth_date.month, birth_date.day):
        years -= 1
    return max(years, 0)

def pn4_birthday(birth_date, age):
    """The `age`-th civil anniversary of the birth: the first day on which
    pn4_completed_years(birth_date, day) == age. A 29 February birthday
    falls on 1 March in a common year, which is the first day the count
    of II.3, 1 turns."""
    year = birth_date.year + int(age)
    if civil_is_valid(year, birth_date.month, birth_date.day):
        return CivilDate(year, birth_date.month, birth_date.day)
    return CivilDate(year, 3, 1)

def pn4_ordinal(n):
    """1st, 2nd, 3rd, 4th ... 11th, 12th, 13th, 21st, 42nd."""
    n = int(n)
    if 10 <= n % 100 <= 20:
        suffix = "th"
    else:
        suffix = {1: "st", 2: "nd", 3: "rd"}.get(n % 10, "th")
    return f"{n}{suffix}"

# --- The sidebar's parsers (pure, so the harness can pin them) -------------
TIME_STANDARD_OPTIONS = ("LMT (Local Mean Time)", "Standard time (pytz)", "Manual UTC offset")
TARGET_MODE_OPTIONS = ("Date", "Age")

def parse_iso_date(text):
    """A CivilDate from 'YYYY-MM-DD', or None. Years 1-9999; the digits are
    validated in the calendar civil_calendar() assigns them, so 1300-02-29
    (a Julian leap day) is a date and 1900-02-29 is not."""
    m = re.fullmatch(r"\s*(\d{1,4})-(\d{1,2})-(\d{1,2})\s*", str(text))
    if not m or not civil_is_valid(*m.groups()):
        return None
    return CivilDate(*m.groups())

def parse_lat_lon(text):
    """'45.37, -84.95' typed into the place box, as (lat, lon), or None if
    the text is not two numbers in range."""
    m = re.fullmatch(r"\s*([-+]?\d+(?:\.\d+)?)\s*[, ]\s*([-+]?\d+(?:\.\d+)?)\s*", str(text))
    if not m:
        return None
    lat, lon = float(m.group(1)), float(m.group(2))
    if abs(lat) > 90.0 or abs(lon) > 180.0:
        return None
    return lat, lon

def _pn4_seg_degree(segment, ascendant_lon, chart_data, geo_lat):
    """The zodiacal degree the direction stands on at the start of a
    segment: the arc is in oblique ascension, so the degree comes back
    through the inverse (fn 251's inverse, _lon_with_oblique_ascension)."""
    if not segment:
        return ascendant_lon
    oa = (_oblique_ascension(ascendant_lon, chart_data['obliquity'], geo_lat) + segment['from']) % 360.0
    got = _lon_with_oblique_ascension(oa, chart_data['obliquity'], geo_lat)
    return ascendant_lon if got is None else got

PN4_DIRECTION_YEAR_DAYS = 365.2425    # the year a degree of direction is worth, in days, for the Date column.
# Measured against PN IV Figure 22's eight printed dates (a Janus run): this
# mean year, from the birth in UT, reproduces all eight; 365.25 misses two
# (Oct 19 2024 and Dec 19 2026 fall a day late), and local civil time
# misses two the other way. It is also the year this file already uses for
# fractional ages (calculate_time_lords) and the revolution's first guess.

@_pn4_opening_aware
def _pn4_partner_rank(aspect):
    return f"{pn4_partner_strength(aspect) + 1} of 5"


def _pn4_distribution_rows(segments, current, unit='years', origin_jd=None):
    """A distribution as the Timing page prints it, one row per segment,
    the same shape for the Ascendant, the meridian and the small days.
    `unit` is what the segment bounds are in: years (a degree of
    ascension a year, III.1, 13) or days (59' 08" a day, IX.7, 29).
    `origin_jd`, when given, adds the civil DATE each segment opens on, as
    PN IV Figure 22 prints a distribution (Arc, Date, Distributor,
    Partner): the birth for the year-long ones, the revolution for the
    day-long ones, a mean year of 365.2425 days from the moment in UT --
    the construction that reproduces that figure's dates -- in the
    calendar the epoch uses."""
    if unit == 'years':
        head, lasting = ('From age', 'To age'), lambda d: pn4_format_arc_time(d)
        per_unit = PN4_DIRECTION_YEAR_DAYS
    elif unit == 'days':
        head, lasting = ('From day', 'To day'), lambda d: f"{int(d)}d {(d - int(d)) * 24.0:.1f}h"
        per_unit = 1.0
    else:
        raise ValueError(f"unit must be 'years' or 'days', not {unit!r}")
    return [{
        head[0]: f"{seg['from']:.2f}", head[1]: f"{seg['to']:.2f}",
        **({'Date': f"{pn4_datetime_from_jd(origin_jd + seg['from'] * per_unit):%Y-%m-%d}"} if origin_jd is not None else {}),
        'Lasting': lasting(seg['to'] - seg['from']),
        'Distributor': seg['distributor'],
        'Partner': seg['partner'] if seg['partner'] is not None else 'none',
        'By': seg['partner_aspect'] if seg['partner_aspect'] is not None else '-',
        'Rank': ('-' if seg['partner_aspect'] is None
                 else _pn4_partner_rank(seg['partner_aspect'])),
        'Opened by': seg['opened_by'],
        'Now': 'yes' if current is not None and seg is current else '',
    } for seg in (segments or [])]

@_connection_aware
def pn4_timing_bundle(chart_data, lat, lon, birth_date, target_date, rule, chronocrats=None, supplement=False):
    """Everything the Timing page shows that comes from PN IV, computed
    once. Returns a dict of row-lists plus the raw pieces the captions
    need. Kept in the engine half so it is testable without Streamlit.
    `chronocrats` is calculate_chronocrats' result for the birth, read for
    the natal hour lord the lord of the orb (VI.1) starts from."""
    natal_sun = chart_data['planetary_data']['Sun']['longitude']
    ascendant = chart_data['ascendant']
    jd_target = civil_to_jd(target_date.year, target_date.month, target_date.day, 12.0)

    # THREE COUNTS OF THE YEAR, kept apart (Astra F02/F03):
    #  age     -- completed CIVIL anniversaries, II.3, 1's count for the
    #             profection ("for every year the native has completed"),
    #             the lord of the orb (VI.1, by year) and the age labels;
    #  cycle   -- completed SOLAR RETURNS, I.2, 1-3's year ("until he came
    #             back to his position ... a solar year will have been
    #             concluded"): the revolution, its months and its day
    #             clocks belong to the return that CONTAINS the target,
    #             return_n <= target < return_(n+1). Seeded from the civil
    #             age and then bracketed by the actual returns, so a target
    #             a few hours before this year's return is in the preceding
    #             cycle's twelfth month, not in a revolution still to come;
    #  elapsed -- ELAPSED years on the 365.2425-day mean year, the Fig. 22
    #             mapping this file already uses for the Date column: the
    #             fardar's sevenths (IV.1, 5-6; 11: "1 year, 5 months, 4
    #             days, and approximately 6 hours") and the distributions
    #             (III.1, 13: 5' a month, 1' six days) change at fractional
    #             boundaries, and Figure 22 changes a distributor mid-year.
    #             Feeding them the integer age froze every change until the
    #             next birthday.
    age = pn4_completed_years(birth_date, target_date)
    # The birthday itself as the target stands at the birth's own moment:
    # a target day earlier in the clock than the birth hour is not a
    # negative elapsed time (the hostile pass of 2026-09-22, M3: "-0.000
    # years elapsed"); nothing before the birth reaches here.
    elapsed = max(0.0, (jd_target - chart_data['julian_day']) / PN4_DIRECTION_YEAR_DAYS)

    # I.2, 1-4: the revolution of the year, cast for the birth location.
    cycle = age
    jd_sr = pn4_solar_revolution_jd(chart_data['julian_day'], natal_sun, cycle)
    while cycle > 0 and jd_sr > jd_target:
        cycle -= 1
        jd_sr = pn4_solar_revolution_jd(chart_data['julian_day'], natal_sun, cycle)
    jd_sr_next = pn4_solar_revolution_jd(chart_data['julian_day'], natal_sun, cycle + 1)
    while jd_sr_next <= jd_target:
        cycle += 1
        jd_sr, jd_sr_next = jd_sr_next, pn4_solar_revolution_jd(chart_data['julian_day'], natal_sun, cycle + 1)
    sr = calculate_traditional_chart_jd(jd_sr, lat, lon)

    # IX.1, 9-10: the sign of the year is also month 1, and the months run
    # from the revolution dates, not the calendar (Intro Sect. 9 p. 90).
    # Which month the target date falls in: the last monthly revolution at
    # or before it.
    month, jd_mr = 1, jd_sr
    for m in range(1, 13):
        jd_m = pn4_monthly_revolution_jd(jd_sr, natal_sun, m)
        if jd_m <= jd_target:
            month, jd_mr = m, jd_m
    mr = calculate_traditional_chart_jd(jd_mr, lat, lon)

    year = pn4_sign_of_the_year(ascendant, age)
    ninth = pn4_first_ninth_part_lord(year['sign'])
    fardar = pn4_fardar_at_age(elapsed, chart_data['sect'])
    ages = pn4_age_of_man(age)
    segments = pn4_distribution_from_ascendant(
        chart_data['planetary_data'], ascendant, chart_data['obliquity'], lat)
    current = pn4_distribution_at_age(segments, elapsed) if segments else None

    # III.1, 12: the meridian, by right ascension -- the Midheaven and the
    # fourth, each from its own degree. Never refuses (see the function).
    meridian = {}
    for point in PN4_MERIDIAN_POINTS:
        start_lon = chart_data['mc'] if point == 'Midheaven' else (chart_data['mc'] + 180.0) % 360.0
        segs = pn4_distribution_from_meridian(
            chart_data['planetary_data'], chart_data['mc'], chart_data['obliquity'], point)
        meridian[point] = {'degree': start_lon, 'segments': segs,
                           'current': pn4_distribution_at_age(segs, elapsed)}

    # --- The revolution of the year (I.2, 1-4; I.7, 2) ---
    revolution_rows = [
        {'Item': 'Count of the year',
         'Value': (f"{age} completed civil years (the profection's count); {cycle} solar returns completed "
                   f"(the revolution's cycle); {elapsed:.3f} years elapsed at 365.2425 days a year (the Figure 22 "
                   f"mapping, which the fardar and the distributions run on)"
                   + (" -- the civil birthday and the Sun's return fall either side of this date" if age != cycle else '')),
         'Source': 'II.3, 1; I.2, 1-3; IV.1, 5-6 and 11; III.1, 13'},
        {'Item': 'Moment of the revolution (UTC)', 'Value': f"{pn4_datetime_from_jd(jd_sr):%Y-%m-%d %H:%M:%S}",
         'Source': 'I.2, 1: the Sun returns to his rooted position'},
        {'Item': 'Ascendant of the year', 'Value': get_degree_string(sr['ascendant']),
         'Source': 'I.2, 4; vocabulary at Intro Sect. 8 p. 77'},
        {'Item': "Its house in the root", 'Value': f"House {get_wsh_house(sr['ascendant'], ascendant)}",
         'Source': 'I.7, 2: the first thing asked of the chart'},
        {'Item': 'Lot of Fortune of the year', 'Value': get_degree_string(sr['lot_of_fortune']),
         'Source': 'I.6, 3; monthly indicator #5 at IX.1, 36'},
        {'Item': 'Sect of the revolution', 'Value': sr['sect'], 'Source': 'I.6, 3'},
        {'Item': 'Location', 'Value': 'Birth location (assumed)',
         'Source': "Not stated for the year; stated for the months, Intro Sect. 9 p. 95"},
        {'Item': f'Revolution of the month ({month} of 12)', 'Value': f"{pn4_datetime_from_jd(jd_mr):%Y-%m-%d %H:%M:%S} UTC",
         'Source': 'IX.3, 2: the like degree and minute in the n-th sign'},
        {'Item': 'Ascendant of the month', 'Value': get_degree_string(mr['ascendant']),
         'Source': 'IX.3, 2; monthly indicator #6'},
    ]

    # --- The year's indicators, in II.1, 5-9's order ---
    year_rows = [
        {'#': 1, 'Indicator': PN4_YEAR_INDICATOR_ORDER[0],
         'Active point': f"{year['sign']} ({get_degree_string(year['longitude'])})",
         'Ruler': year['lord'], 'Source': 'II.3, 1; I.2, 5'},
        {'#': 2, 'Indicator': PN4_YEAR_INDICATOR_ORDER[1],
         # I.6, 6 with fn 34: "the very degree which the distribution had
         # reached" -- the direction's degree AT the target, not the bound's
         # opening degree (Astra F11); the opening stands in the
         # distribution table's "Opened by".
         'Active point': ('refused above the polar circle' if segments is None
                          else (f"{get_degree_string(_pn4_seg_degree({'from': elapsed}, ascendant, chart_data, lat))}"
                                if current else
                                f"age {age} is past the {PN4_DISTRIBUTION_SPAN_YEARS:g}-year table")),
         'Ruler': (current or {}).get('distributor', '-') if segments is not None else '-',
         'Source': 'III.1, 11-13'},
        {'#': 3, 'Indicator': PN4_YEAR_INDICATOR_ORDER[2],
         'Active point': ((current or {}).get('partner_from', '-') if segments is not None and current
                          else '-'),
         'Ruler': (pn4_result_text((current or {}).get('partner'), 'none -- the distributor alone')
                   if segments is not None and current else '-'),
         'Source': 'III.1, 15-16, 23-25'},
        {'#': 4, 'Indicator': PN4_YEAR_INDICATOR_ORDER[3],
         'Active point': (f"{elapsed:.2f} years elapsed: cycle {fardar['cycle']}, year "
                          f"{elapsed % PN4_FARDAR_CYCLE_YEARS:.2f} of 75" if fardar else '-'),
         'Ruler': (f"{fardar['lord']}" + (f" / {fardar['sub_lord']}" if fardar['sub_lord'] else " (no sub-period)")) if fardar else '-',
         'Source': 'IV.1, 2-8; II.1, 9'},
    ]

    # --- VI.1: the lord of the orb, indicator #5 (II.1, 10) ---
    natal_hour_lord = (chronocrats or {}).get('Hour Lord')
    hour_approximate = bool((chronocrats or {}).get('Approximate'))
    orb = pn4_lord_of_the_orb(natal_hour_lord, age)
    orb_rows = pn4_named_lords_of_the_orb(natal_hour_lord, age)
    year_rows.append({
        '#': 5, 'Indicator': PN4_YEAR_INDICATOR_ORDER[4],
        'Active point': (f"hour {age % 7 + 1} of 7 from the natal hour lord ({natal_hour_lord}"
                         f"{', by equal hours -- approximate' if hour_approximate else ''}); "
                         f"cycle {age // 12 + 1} of the profection" if orb else 'natal hour lord unavailable'),
        'Ruler': orb or '-',
        'Source': 'VI.1, 4-8; II.1, 10',
    })

    # --- The fardar cycle (IV.1, 2-8; IV.7, 24-25) ---
    fardar_rows, start = [], 0.0
    for lord, years in pn4_fardar_sequence(chart_data['sect']):
        subs = pn4_fardar_subperiods(lord, years)
        fardar_rows.append({
            'Lord': lord, 'Years': int(years),
            'From age': f"{start:g}", 'To age': f"{start + years:g}",
            'Sub-periods': ', '.join(s[0] for s in subs) if subs
                           else 'none -- "they do not have houses" (IV.1, 8)',
            'Active': 'yes' if fardar and fardar['lord'] == lord and fardar['cycle'] >= 1
                      and start <= (elapsed % PN4_FARDAR_CYCLE_YEARS) < start + years else '',
        })
        start += years

    # --- The seven monthly indicators (IX.1, 35-39) ---
    indicators = pn4_monthly_indicators(
        month, age, year['longitude'], chart_data['lot_of_fortune'],
        sr['ascendant'], sr['lot_of_fortune'], mr['ascendant'], mr['lot_of_fortune'], rule)
    monthly_rows = [{
        '#': row['number'], 'Indicator': row['name'],
        'Rooted': 'yes' if row['rooted'] else 'no',
        'Sign': row['sign'], 'Lord': row['lord'], 'Turned': row['direction'],
        'In the root': f"House {get_wsh_house(row['longitude'], ascendant)}",
    } for row in indicators]

    # --- Ages of Man (I.8, 10-26) ---
    age_rows, start = [], 0
    for planet, years, label in PN4_AGES_OF_MAN:
        last = planet == PN4_AGES_OF_MAN[-1][0]
        age_rows.append({
            'Planet': planet, 'Years': years,
            'Ages': f"{start}-{start + years - 1}" if not last else f"{start} onward",
            'Period of life': label,
            'Active': 'yes' if ages and ages['planet'] == planet else '',
        })
        start += years


    # --- the distributions, as forward tables (III.1, 11-16) ---
    distribution_rows = _pn4_distribution_rows(segments, current, origin_jd=chart_data['julian_day'])
    meridian_rows = {point: _pn4_distribution_rows(m['segments'], m['current'], origin_jd=chart_data['julian_day'])
                     for point, m in meridian.items()}

    # --- IX.7, 29-31: the small days, in days from the revolution ---
    small_days = pn4_small_days(sr['planetary_data'], sr['ascendant'])
    day_of_year = jd_target - jd_sr
    small_days_current = pn4_distribution_at_age(small_days, day_of_year)
    small_days_rows = _pn4_distribution_rows(small_days, small_days_current, unit='days', origin_jd=jd_sr)

    # --- II.22, 1-4: the Moon's connections in her sign, and the portions ---
    moon = pn4_moon_connections(sr['planetary_data'], jd_sr)
    year_days = jd_sr_next - jd_sr
    portions = pn4_moon_portions(moon['connections'], year_days)

    # --- SAHL: the releaser (On Nativities 1.15-1.16, 1.20), its distribution
    # (1.15, 22; 1.18, 20-21) and the house-master directed (1.23, 2) ---
    syzygies = sahl_prenatal_meeting_and_fullness(chart_data['julian_day'], lat, lon)
    releaser = sahl_releaser(chart_data['planetary_data'], ascendant, chart_data['houses'], chart_data['sect'],
                             chart_data['lot_of_fortune'], syzygies['meeting']['longitude'],
                             syzygies['fullness']['longitude'])
    releaser_segments = sahl_releaser_distribution(chart_data['planetary_data'], releaser['longitude'],
                                                   chart_data['obliquity'], lat,
                                                   significator={'the Sun': 'Sun', 'the Moon': 'Moon'}.get(releaser['releaser']))
    releaser_current = pn4_distribution_at_age(releaser_segments, elapsed) if releaser_segments else None
    releaser_rows = _pn4_distribution_rows(releaser_segments, releaser_current, origin_jd=chart_data['julian_day'])
    if releaser['longitude'] is None:
        releaser_note = 'no releaser by On Nativities 1.15 (the releaser section)'
    elif releaser_segments is None:
        releaser_note = 'refused above the polar circle'
    elif releaser_current is None:
        releaser_note = f"age {age} is past the {PN4_DISTRIBUTION_SPAN_YEARS:g}-year table"
    else:
        releaser_note = None
    releaser_stand = None
    if releaser_current:
        _deg = _pn4_seg_degree(releaser_current, releaser['longitude'], chart_data, lat)
        releaser_stand = {'distributor': releaser_current['distributor'], 'partner': releaser_current['partner'],
                          'sign': get_zodiac_sign(_deg), 'lord': SIGN_TO_DOMICILE.get(get_zodiac_sign(_deg), '-'),
                          'degree': _deg, 'note': None}
    pn4_releaser = pn4_releaser_direction(dict(chart_data, geo_lat=lat), releaser, elapsed)
    # FINAL-A7: in the empty case the Moon is directed after the Ascendant
    # (1.32, 11-13), by the same operation as the house-master's.
    standin_moon = (sahl_house_master_direction(chart_data['planetary_data'], 'Moon', chart_data['obliquity'], lat,
                                                origin_jd=chart_data['julian_day'], target_policy='stand-in')
                    if releaser['releaser'] is None else None)
    # --- III.1, 12: "the Ascendant and the things in it ... what is in the
    # Midheaven or the fourth" -- a planet ON an axial degree directed as
    # that degree is (orders GAP-37 / PN4R-4b-4; the owner's ruling (e) of
    # 2026-09-11, evening: the degree itself, PN4_AXIS_TOLERANCE, no orb; the
    # division and the carry-over play no part). Every other planet is
    # III.1, 12's third case: its degree directed by proportional semi-arcs
    # (pn4_distribution_by_semi_arcs), with the arc's terms beside it; never
    # by RA or OA instead.
    angle_planets = []
    for planet in PN4_SEVEN:
        if planet not in chart_data['planetary_data']:
            continue
        p_lon = chart_data['planetary_data'][planet]['longitude']
        direction = pn4_point_distribution(dict(chart_data, geo_lat=lat), p_lon, elapsed, planet, significator=planet)
        axis, segs, ap_current = direction['axis'], direction['segments'], direction['current']
        how = direction['method']
        where = {'Ascendant': 'the Ascendant', 'Midheaven': 'the Midheaven',
                 'Fourth (IC)': 'the fourth'}.get(axis, 'not on an axial degree')
        angle_planets.append({'planet': planet, 'axis': axis, 'where': where, 'how': how, 'segments': segs,
                              'current': ap_current, 'degree': p_lon,
                              'rows': _pn4_distribution_rows(segs, ap_current, origin_jd=chart_data['julian_day']),
                              'terms': (pn4_semi_arc_terms_rows(p_lon, chart_data['obliquity'], lat, chart_data['armc'],
                                                                segs, ap_current) if axis is None and segs else [])})

    # --- III.7, 32-42: when each natal indication comes out -- confirmed (42)
    # against every distribution on the page (order PN4R-4a-2) ---
    activation_rows = pn4_activation_ages(
        chart_data['planetary_data'], chart_data['obliquity'], lat, ascendant_lon=ascendant,
        distributions={"the Ascendant's distribution": segments,
                       **{f"the {point}'s distribution": m['segments'] for point, m in meridian.items()},
                       "the releaser's distribution": releaser_segments})
    house_master = releaser['house_master']
    hm_direction = sahl_house_master_direction(chart_data['planetary_data'], house_master, chart_data['obliquity'],
                                               lat, origin_jd=chart_data['julian_day']) if house_master else None
    hm_this_year = [r for r in (hm_direction or []) if r['In the year of age'] == age]
    hm_revolution = sahl_house_master_in_revolution(house_master, chart_data, sr) if house_master else []
    hm_flags = sahl_house_master_flags(house_master, chart_data['planetary_data'], chart_data['houses'])

    # --- II.13, 1; II.14, 1; II.22, 1-5: the luminary proxies ---
    sun = pn4_sun_handover(sr['planetary_data'], jd_sr) if year['lord'] == 'Sun' else None
    proxies = pn4_luminary_proxies(year['lord'], chart_data, sr, moon, sun,
                                   releaser_stand or {'note': releaser_note})

    # --- IX.7, 23-28: the mighty days, the terminal degree through the SR ---
    mighty_days = pn4_mighty_days(sr['planetary_data'], year['longitude'])
    mighty_days_current = pn4_distribution_at_age(mighty_days, day_of_year)
    mighty_days_rows = _pn4_distribution_rows(mighty_days, mighty_days_current, unit='days', origin_jd=jd_sr)

    # IX.9, 1-10 once; the condition rows (11-13) read its primary.
    governor = pn4_governor(
        year['lord'], (current or {}).get('distributor'), (current or {}).get('partner'),
        ('refused above the polar circle' if segments is None
         else f"age {age} is past the {PN4_DISTRIBUTION_SPAN_YEARS:g}-year table"),
        (fardar or {}).get('lord'), orb, sr['ascendant'],
        pn4_moon_testimony(moon), moon['void'],
        pn4_releaser['distributor'], pn4_releaser['partner'], pn4_releaser['note'], pn4_releaser['status'])

    indicator_relationships = pn4_indicator_relationships(chart_data, sr, year['longitude'])
    return {
        'indicator_relationships': indicator_relationships,
        'activation_rows': activation_rows,
        'distribution_rows': distribution_rows,
        'meridian': meridian, 'meridian_rows': meridian_rows,
        'small_days': small_days, 'small_days_current': small_days_current,
        'small_days_rows': small_days_rows, 'day_of_year': day_of_year,
        'mighty_days': mighty_days, 'mighty_days_current': mighty_days_current,
        'mighty_days_rows': mighty_days_rows,
        'orb': orb, 'orb_rows': orb_rows, 'natal_hour_lord': natal_hour_lord,
        'hour_approximate': hour_approximate,
        'turning_rows': pn4_turning_rows(chart_data, age, elapsed=elapsed),
        'turning_triplicity_rows': pn4_turning_triplicity_lords(chart_data, sr),
        'life_lords_rows': triplicity_lords_of_life(chart_data),
        'life_lords_ascendant_rows': triplicity_lords_of_life(chart_data, 'Ascendant'),
        'further_rows': pn4_further_indicators(chart_data, sr, year['longitude'], moon, jd_sr=jd_sr, relationships=indicator_relationships),
        'governor': governor,
        'moon': moon, 'moon_portions': portions, 'year_days': year_days,
        'proxies': proxies, 'sun_handover': sun,
        'pn4_releaser': pn4_releaser,
        'syzygies': syzygies, 'releaser': releaser, 'releaser_segments': releaser_segments,
        'releaser_current': releaser_current, 'releaser_rows': releaser_rows, 'releaser_note': releaser_note,
        'releaser_stand': releaser_stand, 'house_master': house_master, 'hm_direction': hm_direction,
        'hm_this_year': hm_this_year, 'hm_revolution': hm_revolution, 'hm_flags': hm_flags,
        'standin_moon': standin_moon,
        'angle_planets': angle_planets,
        'fixed_stars': pn4_fixed_stars_in_image(chart_data, chart_data['julian_day']),
        'fixed_stars_revolution': pn4_fixed_stars_in_revolution(
            sr, jd_sr, year['longitude'],
            (_pn4_seg_degree({'from': elapsed}, ascendant, chart_data, lat) if (segments and current) else None)),
        'short_life': sahl_short_life_testimonies(chart_data, chart_data['lot_of_fortune']),
        # DIS-10: the father's Lot (4.20, 31-36): the harmers, and 32's two directions
        'father_lot': (lambda selection: (None if isinstance(selection, UnresolvedResult) else (lambda lot: {
            'lot': lot, 'selection': selection,
            'harmers': sahl_father_lot_harmers(chart_data['sect'], chart_data['planetary_data'], lot,
                                               chart_data['planetary_data']['Sun']['longitude']),
            'second': 'Sun' if chart_data['sect'] == 'Diurnal' else 'Saturn',
            'from_lot': sahl_house_master_direction(
                chart_data['planetary_data'], None, chart_data['obliquity'], lat, origin_jd=chart_data['julian_day'],
                start_lon=lot, sun_target=False, target_policy='father',
                target_planets=(('Mars', 'Saturn', 'Mercury') if chart_data['sect'] == 'Diurnal' else ('Mars', 'Mercury'))),
            'from_second': sahl_house_master_direction(
                chart_data['planetary_data'], 'Sun' if chart_data['sect'] == 'Diurnal' else 'Saturn', chart_data['obliquity'], lat,
                origin_jd=chart_data['julian_day'], sun_target=False, target_policy='father',
                target_planets=(('Mars', 'Saturn', 'Mercury') if chart_data['sect'] == 'Diurnal' else ('Mars', 'Mercury'))),
        })(selection['longitude'])))(father_lot_selection(chart_data['planetary_data'], ascendant, chart_data['houses'], chart_data['sect'])),
        # REL-5-7: 1.23, 13-14 -- when a 1.23, 12 flag fires, the lord of the Ascendant, then the Ascendant's degree
        'hm_redirect': (lambda lord: ({
            'lord': lord,
            'lord_direction': sahl_house_master_direction(chart_data['planetary_data'], lord, chart_data['obliquity'], lat,
                                                          origin_jd=chart_data['julian_day']) if lord in chart_data['planetary_data'] else None,
            'ascendant_direction': sahl_house_master_direction(chart_data['planetary_data'], None, chart_data['obliquity'], lat,
                                                               origin_jd=chart_data['julian_day'], start_lon=ascendant),
            'aggravators': [p for p in SAHL_INFORTUNES if p in chart_data['planetary_data'] and (
                SIGN_TO_DOMICILE.get(get_zodiac_sign(chart_data['planetary_data'][lord]['longitude'])) == p
                or SIGN_TO_DOMICILE.get(SIGN_ORDER[(SIGN_ORDER.index(get_zodiac_sign(ascendant)) + 7) % 12]) == p)]
            if lord in chart_data['planetary_data'] else [],
        }) if any('1.23, 12' in f for f in hm_flags) else None)(SIGN_TO_DOMICILE.get(get_zodiac_sign(ascendant))),
        'turning_partner': sahl_turning_reaches_partner(segments, age, ascendant, chart_data['planetary_data']),
        'governor_condition': _pn4_primary_condition(governor[1]['primary'], chart_data, sr),
        'hm_turning': sahl_house_master_turning(house_master, chart_data['planetary_data']) if house_master else [],
        # FINAL-A1 / sheet row 1: the house-master's years from 1.20, 7-34, by the division.
        'hm_years': (sahl_house_master_years(house_master, chart_data['planetary_data'], chart_data['houses'], chart_data['sect'],
                                             evaluate_essential_dignities(chart_data['planetary_data'], chart_data['sect']))
                     if house_master else None),
        # Reconciliation decision 9: Abu 'Ali's ladder where 1.20 is silent (None where Sahl grades); supplement only.
        'hm_years_jn': (jn_years_fallback(house_master, chart_data['planetary_data'], chart_data['houses'], chart_data['sect'],
                                          evaluate_essential_dignities(chart_data['planetary_data'], chart_data['sect']))
                        if house_master else None),
        # JN Ch. 4's additions and subtractions, display only (2026-09-15); supplement only.
        'hm_years_additions': jn_years_additions_rows(house_master, chart_data['planetary_data']) if house_master else [],
        'ii3': pn4_ii3_examination(chart_data, sr, year, jd_sr),
        'iii2_type': pn4_static_type(current['distributor'], current['partner']) if current else None,
        'iii2_checklist': pn4_distribution_checklist(chart_data, sr, year['longitude'], current),
        'iii2_transitions': pn4_year_transitions(segments, age),
        'bound_transits': pn4_bound_transits(chart_data, sr, current, year['lord']) if current else None,
        'image': pn4_revolution_image(chart_data, sr, year, age, current, fardar, orb, lat, elapsed=elapsed, supplement=supplement),
        'i7_ascendant': pn4_i7_ascendant(chart_data, sr),
        'i7_planets': pn4_i7_planets(chart_data, sr),
        'day_methods': pn4_day_methods(
            chart_data['julian_day'], jd_sr, jd_mr, jd_target, ascendant, sr['ascendant'], orb, year['sign'],
            [(f"#{r['number']} {r['name']}", r['longitude']) for r in indicators if r['number'] in (1, 3, 4, 5)]
            + [("the month's Ascendant", mr['ascendant']), ("the month's Lot of Fortune", mr['lot_of_fortune']),
               ("the month's Moon", mr['planetary_data']['Moon']['longitude'])],
            sr['planetary_data']['Moon']['longitude']),
        'moon_rows': [{'Day from the revolution': f"{c['day']:.2f}", 'Planet': c['planet'], 'By': c['aspect'],
                       'Moon at': get_degree_string(c['moon_at'])} for c in moon['connections']],
        'portion_rows': [{'Portion': f"{p['portion']} of {p['of']}", 'Owned by': p['planet'],
                          'From day': f"{p['from_day']:.1f}", 'To day': f"{p['to_day']:.1f}"} for p in portions],
        'first_month_governor': pn4_first_month_governor(
            ascendant, chart_data['lot_of_fortune'], year['longitude'], sr['ascendant'], sr['lot_of_fortune']),
        'age': age, 'cycle': cycle, 'elapsed_years': elapsed,
        'month': month, 'jd_sr': jd_sr, 'jd_sr_next': jd_sr_next, 'jd_mr': jd_mr, 'jd_target': jd_target,
        'sr': sr, 'mr': mr, 'year': year, 'ninth': ninth, 'fardar': fardar,
        'segments': segments, 'current': current, 'ages': ages,
        'revolution_rows': revolution_rows, 'year_rows': year_rows,
        'fardar_rows': fardar_rows, 'monthly_rows': monthly_rows, 'age_rows': age_rows,
        'monthly_indicators': indicators,
    }

def calculate_time_lords(ascendant_lon, birth_date, target_date):
    """Annual Profection (Lord of the Year) and a symbolic 1-degree-per-year
    direction of the Ascendant through the Egyptian bounds.

    The second of these was previously called a "Ptolemaic Distribution."
    It is not one -- see the note on the row it returns."""
    # Age in COMPLETED CIVIL ANNIVERSARIES, not elapsed days over a mean
    # year length. Profection turns on the birthday: dividing by 365.2425
    # let the sign advance up to a day early or late around it, and drifts
    # further the older the native is.
    days_alive = (civil_to_jd(target_date.year, target_date.month, target_date.day)
                  - civil_to_jd(birth_date.year, birth_date.month, birth_date.day))
    integer_age = target_date.year - birth_date.year
    if (target_date.month, target_date.day) < (birth_date.month, birth_date.day):
        integer_age -= 1
    integer_age = max(integer_age, 0)
    fractional_age = days_alive / 365.2425

    # --- Annual Profection ---------------------------------------------
    natal_sign_idx = int(ascendant_lon // 30)
    profected_sign_idx = (natal_sign_idx + integer_age) % 12
    profected_sign = get_zodiac_sign(profected_sign_idx * 30.0 + 15.0)  # mid-sign probe
    lord_of_year = SIGN_TO_DOMICILE.get(profected_sign, '-')

    # --- Distribution (Ptolemaic Egyptian Terms) ------------------------
    directed_asc_lon = (ascendant_lon + fractional_age) % 360.0
    directed_sign = get_zodiac_sign(directed_asc_lon)
    degree_in_sign = directed_asc_lon % 30.0
    distributor = next((lord for limit, lord in EGYPTIAN_TERMS.get(directed_sign, []) if degree_in_sign < limit), '-')

    return [
        {
            'Technique': 'Annual Profection',
            'Active Point': profected_sign,
            'Active Ruler': lord_of_year,
            'Details': f"Age {integer_age} (1 Sign / Year)",
        },
        {
            # NOT a distribution in either Sahl's or Ptolemy's sense, and no
            # longer labelled as one. Real distribution directs a releaser
            # through the bounds by PRIMARY motion, in ascensional degrees
            # for the birth latitude, and names both a distributor (the
            # bound lord) and a partner (the body or ray met on the way) --
            # Sahl, On Nativities Ch.2.13, 48-51 grades the result in three
            # 15-degree ascensional bands. Advancing zodiacal longitude at a
            # flat 1 degree per year is the schoolbook shortcut for that; it
            # reaches a different bound whenever the ascensions of the sign
            # in question are not 1 degree per year, which is most of the
            # time away from the equator.
            'Technique': 'Symbolic direction (1\u00b0/yr, not a distribution)',
            'Active Point': f"{get_degree_string(directed_asc_lon)}",
            'Active Ruler': distributor,
            'Details': "Egyptian bound of the symbolically directed Asc",
        },
    ]

# ==========================================


# ---- The panel behind a click on the wheel -------------------------------
# Two pure functions, display only. Every row either restates a position the
# Chart page already prints or is lifted whole out of a table another page
# already draws -- the essential and accidental dignities, the aspects, the
# receptions, the two Lot tables, the lords of a sign. Nothing is scored
# here and nothing is judged: a row's citation is the one its own source
# table carries, and where that table carries none this carries none either.

POINT_SUMMARY_NAMES = {
    'Exalt': 'Exaltation', 'Term': 'Bound', 'Hayz': 'In its own domain (hayz)',
    'PlanetSect': 'Diurnal planet', 'ContraryDomain': 'Contrary to its domain',
    'Cazimi': 'In the heart of the Sun', 'Combust': 'Burned', 'UnderBeams': 'Under the rays',
}

def point_summary(name, chart_data, essential, accidental, aspects, reception_data, classical_lots, topical_lots):
    """What the app already says about one point, gathered into small row
    lists: where it stands, its essential dignities at its own degree, its
    accidental conditions, the connections it is in, the receptions naming
    it, and the Lots it rules. Returns None for a name the wheel does not
    draw. Every list may be empty -- an angle holds no dignity and rules no
    Lot, and the default chart has no reception at all."""
    # The points the natal wheel draws: the seven planets and the north node
    # from the ephemeris, the south node opposite it with the same motion,
    # the Lot of Fortune, and the four angles, which have no motion at all.
    p_data = chart_data['planetary_data']
    angles = {'Ascendant': 'ascendant', 'Midheaven': 'mc', 'Descendant': 'descendant', 'Imum Coeli': 'ic'}
    if name in p_data:
        lon, speed = p_data[name]['longitude'], p_data[name].get('speed_in_lon')
    elif name == 'South Node':
        node = p_data['North Node']
        lon, speed = (node['longitude'] + 180.0) % 360.0, node.get('speed_in_lon')
    elif name == 'Lot of Fortune':
        lon, speed = chart_data['lot_of_fortune'], None
    elif name in angles:
        lon, speed = chart_data[angles[name]], None
    else:
        return None
    if speed is None:
        motion = '-'
    else:
        motion = 'retrograde' if speed < 0 else 'direct'
    position = [
        {'Reading': 'Position', 'Value': get_degree_string(lon)},
        {'Reading': 'Sign', 'Value': get_zodiac_sign(lon)},
        {'Reading': 'Whole-sign place', 'Value': str(get_wsh_house(lon, chart_data['ascendant']))},
        {'Reading': 'Quadrant division (Alchabitius)', 'Value': str(get_effective_house(lon, chart_data['houses']))},
        {'Reading': 'Motion', 'Value': motion},
    ]
    # The True ones, by their own key: the flags are booleans and the scores
    # and label lists beside them are not, so `is True` takes the conditions
    # and leaves the arithmetic alone.
    # Named in the pages' own words, not by the dicts' keys (the hostile
    # pass of 2026-09-22, M5: the panel printed PlanetSect, ContraryDomain,
    # UnderBeams and Exalt beside the tables' "Under the rays").
    dignities = [{'Dignity': POINT_SUMMARY_NAMES.get(key, key)}
                 for key, value in (essential.get(name) or {}).items() if value is True]
    conditions = [{'Condition': POINT_SUMMARY_NAMES.get(key, key)}
                  for key, value in (accidental.get(name) or {}).items() if value is True]
    connections = [{column: row.get(column) for column in
                    ('Light Planet', 'Aspect', 'Heavy Planet', 'Connecting planet', 'Motion', 'Exact Orb Dist', 'Connection')}
                   for row in (aspects or []) if name in (row.get('Light Planet'), row.get('Heavy Planet'))]
    receptions = [dict(row) for row in (reception_data or [])
                  if name in (row.get('Receiver'), row.get('Received'))]
    lots = []
    for row in (classical_lots or []):
        if row.get('Sign Dispositor') == name:
            lots.append({'Lot': row.get('Lot Name'), 'Position': row.get('Position'), 'WS place': row.get('WS place'),
                         'Table': 'Classical Lots', 'Source': ''})
    for row in (topical_lots or []):
        if row.get('Lord') == name:
            lots.append({'Lot': row.get('Lot'), 'Position': row.get('Position'), 'WS place': row.get('WS place'),
                         'Table': 'Topical Lots', 'Source': row.get('Source') or ''})
    return {
        'point': name,
        'position': position,
        'essential': dignities,
        'accidental': conditions,
        'connections': connections,
        'receptions': receptions,
        'lots': lots,
    }


def sign_summary(index, sect='Diurnal'):
    """The lords of one sign, 0-11: its domicile and exaltation lords, the
    triplicity lord of the chart's own sect with the partner beside it, the
    five Egyptian bounds with their lords in order, and the three faces.
    Read straight off the tables the Reference page prints."""
    i = int(index) % 12
    sign = SIGN_ORDER[i]
    triplicity = triplicity_rulers(sign, table='Great Introduction')
    lords = [
        {'Dignity': 'Domicile', 'Lord': SIGN_TO_DOMICILE.get(sign, '-')},
        {'Dignity': 'Exaltation', 'Lord': SIGN_TO_EXALTATION.get(sign, '-')},
        {'Dignity': f'Triplicity ({sect})', 'Lord': triplicity['Night' if sect == 'Nocturnal' else 'Day']},
        # The Great Introduction table's partner (Mercury for Virgo, triplicity_rulers).
        {'Dignity': 'Triplicity, participating', 'Lord': triplicity['Participating']},
    ]
    bounds, start = [], 0
    for limit, lord in EGYPTIAN_TERMS[sign]:
        bounds.append({'Bound': f'{start}°-{limit}°', 'Lord': lord})
        start = limit
    faces = [{'Face': f'{d}°-{d + 10}°', 'Lord': CHALDEAN_ORDER[(i * 3 + d // 10) % 7]}
             for d in (0, 10, 20)]
    return {'sign': sign, 'index': i, 'element': SIGN_ELEMENT[sign],
            'lords': lords, 'bounds': bounds, 'faces': faces,
            **triplicity_source_fields(sign, table='Great Introduction')}


# The pages reach for these by name through ``from engine import *``, and a
# great many of the names they reach for begin with a single underscore --
# _VS, _simulate_forward, _wheel_palette -- which a star import would
# otherwise leave behind. So the export list is every top-level name this
# module bound, taken as it stands rather than typed out and then kept in
# step by hand.
__all__ = [_n for _n in list(globals()) if not _n.startswith('__')]
