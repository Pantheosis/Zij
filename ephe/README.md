`sefstars.txt` is the Swiss Ephemeris fixed-star catalogue (Astrodienst; the file's own header records its provenance and editing history), copied UNMODIFIED on 2026-09-11 from the `kerykeion` package (version 5.12.9, `kerykeion/sweph/sefstars.txt`), which redistributes it under AGPL-3.0 -- the licence this app carries (`LICENSE`). It is the only ephemeris file shipped: the planets stay on the built-in Moshier ephemeris (no `.se1` file is bundled or needed); the app points `swe.set_ephe_path` at this directory directly, which is why nothing else may be placed here (see `_fixed_star_catalogue_ready` in `app.py` and `docs/BUILD_NOTES.md`).
